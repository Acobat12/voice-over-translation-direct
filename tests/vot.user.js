// ==UserScript==
// @name            [FORK] - Voice Over Translation
// @name:de         [VOT] - Voice-Over-Video-Übersetzung
// @name:es         [VOT] - Traducción de vídeo en off
// @name:fr         [VOT] - Traduction vidéo voix-off
// @name:it         [VOT] - Traduzione Video fuori campo
// @name:ru         [VOT] - Закадровый перевод видео
// @name:zh         [VOT] - 画外音视频翻译
// @namespace       vot-direct
// @version         1.11.5.93
// @author          Toil, SashaXser, MrSoczekXD, mynovelhost, sodapng, Acobat12
// @description     A small extension that adds a Yandex Browser video translation to other browsers
// @description:de  Eine kleine Erweiterung, die eine Voice-over-Übersetzung von Videos aus dem Yandex-Browser zu anderen Browsern hinzufügt
// @description:es  Una pequeña extensión que agrega una traducción de voz en off de un video de Yandex Browser a otros navegadores
// @description:fr  Une petite extension qui ajoute la traduction vocale de la vidéo du Navigateur Yandex à d'autres navigateurs
// @description:it  Una piccola estensione che aggiunge la traduzione vocale del video dal browser Yandex ad altri browser
// @description:ru  Небольшое расширение, которое добавляет закадровый перевод видео из Яндекс Браузера в другие браузеры
// @description:zh  一个小扩展，它增加了视频从Yandex浏览器到其他浏览器的画外音翻译
// @license         MIT
// @icon            https://translate.yandex.ru/icons/favicon.ico
// @homepageURL     https://github.com/Acobat12/voice-over-translation-direct
// @source          https://github.com/Acobat12/voice-over-translation-direct.git
// @supportURL      https://github.com/Acobat12/voice-over-translation-direct/issues
// @downloadURL     https://raw.githubusercontent.com/Acobat12/voice-over-translation-direct/master/dist/vot.user.js
// @updateURL       https://raw.githubusercontent.com/Acobat12/voice-over-translation-direct/master/dist/vot.user.js
// @match           *://*.*
// @match           *://cloud.mail.ru/*
// @match           *://*.cloud.mail.ru/*
// @match           *://*.kodik.info/*
// @match           *://*.kodik.biz/*
// @match           *://*.kodik.cc/*
// @match           *://kodikplayer.com/*
// @match           *://*.kodikplayer.com/*
// @match           *://wikianimex.ru/*
// @match           *://*.wikianimex.ru/*
// @match           *://*.cdnvideohub.com/*
// @match           *://*.okcdn.ru/*
// @match           *://*.solodcdn.com/*
// @match           *://oauth.yandex.com/*
// @match           *://oauth.yandex.ru/*
// @match           *://*.youtube.com/*
// @match           *://*.youtube-nocookie.com/*
// @match           *://*.youtubekids.com/*
// @match           *://*.twitch.tv/*
// @match           *://*.xvideos.com/*
// @match           *://*.xvideos-ar.com/*
// @match           *://*.xvideos005.com/*
// @match           *://*.xv-ru.com/*
// @match           *://*.xhamster.com/*
// @match           *://*.xhamster.desi/*
// @match           *://*.xhvid.com/*
// @match           *://*.spankbang.com/*
// @match           *://*.rule34video.com/*
// @match           *://*.picarto.tv/*
// @match           *://*.olympics.com/*
// @match           *://*.pornhub.com/*
// @match           *://*.pornhub.org/*
// @match           *://*.vk.com/*
// @match           *://*.vkvideo.ru/*
// @match           *://*.vk.ru/*
// @match           *://*.vimeo.com/*
// @match           *://*.imdb.com/*
// @match           *://*.9gag.com/*
// @match           *://*.twitter.com/*
// @match           *://*.x.com/*
// @match           *://*.facebook.com/*
// @match           *://*.rutube.ru/*
// @match           *://*.bilibili.com/*
// @match           *://*.bilibili.tv/*
// @match           *://my.mail.ru/*
// @match           *://*.bitchute.com/*
// @match           *://*.coursera.org/*
// @match           *://*.udemy.com/course/*
// @match           *://*.tiktok.com/*
// @match           *://*.douyin.com/*
// @match           *://rumble.com/*
// @match           *://*.eporner.com/*
// @match           *://*.dailymotion.com/*
// @match           *://*.ok.ru/*
// @match           *://trovo.live/*
// @match           *://disk.yandex.ru/*
// @match           *://disk.yandex.kz/*
// @match           *://disk.yandex.com/*
// @match           *://disk.yandex.com.am/*
// @match           *://disk.yandex.com.ge/*
// @match           *://disk.yandex.com.tr/*
// @match           *://disk.yandex.by/*
// @match           *://disk.yandex.az/*
// @match           *://disk.yandex.co.il/*
// @match           *://disk.yandex.ee/*
// @match           *://disk.yandex.lt/*
// @match           *://disk.yandex.lv/*
// @match           *://disk.yandex.md/*
// @match           *://disk.yandex.net/*
// @match           *://disk.yandex.tj/*
// @match           *://disk.yandex.tm/*
// @match           *://disk.yandex.uz/*
// @match           *://disk.360.yandex.ru/*
// @match           *://drive.google.com/*
// @match           *://docs.google.com/*
// @match           *://youtube.googleapis.com/embed/*
// @match           *://*.banned.video/*
// @match           *://*.madmaxworld.tv/*
// @match           *://*.weverse.io/*
// @match           *://*.newgrounds.com/*
// @match           *://*.egghead.io/*
// @match           *://*.youku.com/*
// @match           *://*.archive.org/*
// @match           *://*.patreon.com/*
// @match           *://*.reddit.com/*
// @match           *://*.kick.com/*
// @match           *://developer.apple.com/*
// @match           *://dev.epicgames.com/*
// @match           *://*.rapid-cloud.co/*
// @match           *://odysee.com/*
// @match           *://learning.sap.com/*
// @match           *://*.watchporn.to/*
// @match           *://*.linkedin.com/*
// @match           *://*.incestflix.net/*
// @match           *://*.incestflix.to/*
// @match           *://*.porntn.com/*
// @match           *://*.dzen.ru/*
// @match           *://*.cloudflarestream.com/*
// @match           *://*.loom.com/*
// @match           *://*.artstation.com/learning/*
// @match           *://*.rt.com/*
// @match           *://*.bitview.net/*
// @match           *://*.kickstarter.com/*
// @match           *://*.thisvid.com/*
// @match           *://*.ign.com/*
// @match           *://*.bunkr.site/*
// @match           *://*.bunkr.black/*
// @match           *://*.bunkr.cat/*
// @match           *://*.bunkr.media/*
// @match           *://*.bunkr.red/*
// @match           *://*.bunkr.ws/*
// @match           *://*.bunkr.org/*
// @match           *://*.bunkr.sk/*
// @match           *://*.bunkr.si/*
// @match           *://*.bunkr.su/*
// @match           *://*.bunkr.ci/*
// @match           *://*.bunkr.cr/*
// @match           *://*.bunkr.fi/*
// @match           *://*.bunkr.ph/*
// @match           *://*.bunkr.pk/*
// @match           *://*.bunkr.ps/*
// @match           *://*.bunkr.ru/*
// @match           *://*.bunkr.la/*
// @match           *://*.bunkr.is/*
// @match           *://*.bunkr.to/*
// @match           *://*.bunkr.ac/*
// @match           *://*.bunkr.ax/*
// @match           *://web.telegram.org/k/*
// @match           *://rust-server-531j.onrender.com/*
// @match           *://mylearn.oracle.com/*
// @match           *://learn.deeplearning.ai/*
// @match           *://learn-staging.deeplearning.ai/*
// @match           *://learn-dev.deeplearning.ai/*
// @match           *://*.netacad.com/content/i2cs/*
// @match           *://*.nicovideo.jp/*
// @match           *://*.zdf.de/*
// @match           *://*.ivi.ru/*
// @match           *://*.kinopoisk.ru/*
// @match           *://*.okko.tv/*
// @match           *://*.okko.ru/*
// @match           *://*.kion.ru/*
// @match           *://*.wink.ru/*
// @match           *://*.seasonvar.ru/*
// @match           *://*.zrok.io/*
// @match           *://*.ts.net/*
// @match           *://*.tunnel4.com/*
// @match           *://*.lhr.life/*
// @match           *://iframe.mediadelivery.net/*
// @match           *://video.bunnycdn.com/*
// @match           *://*.weibo.com/*
// @match           *://*/*.mp4*
// @match           *://*/*.m4v*
// @match           *://*/*.webm*
// @match           *://*/*.mov*
// @match           *://*/*.mkv*
// @match           *://*/*.avi*
// @match           *://*/*.ogv*
// @match           *://*.trycloudflare.com/*
// @match           *://*.ngrok-free.app/*
// @match           *://*.ngrok-free.dev/*
// @match           *://*.ngrok.app/*
// @match           *://*.yewtu.be/*
// @match           *://yt.artemislena.eu/*
// @match           *://invidious.flokinet.to/*
// @match           *://iv.melmac.space/*
// @match           *://inv.nadeko.net/*
// @match           *://inv.tux.pizza/*
// @match           *://invidious.private.coffee/*
// @match           *://yt.drgnz.club/*
// @match           *://vid.puffyan.us/*
// @match           *://invidious.dhusch.de/*
// @match           *://*.piped.video/*
// @match           *://piped.tokhmi.xyz/*
// @match           *://piped.moomoo.me/*
// @match           *://piped.syncpundit.io/*
// @match           *://piped.mha.fi/*
// @match           *://watch.whatever.social/*
// @match           *://piped.garudalinux.org/*
// @match           *://efy.piped.pages.dev/*
// @match           *://watch.leptons.xyz/*
// @match           *://piped.lunar.icu/*
// @match           *://yt.dc09.ru/*
// @match           *://piped.mint.lgbt/*
// @match           *://*.il.ax/*
// @match           *://piped.privacy.com.de/*
// @match           *://piped.esmailelbob.xyz/*
// @match           *://piped.projectsegfau.lt/*
// @match           *://piped.in.projectsegfau.lt/*
// @match           *://piped.us.projectsegfau.lt/*
// @match           *://piped.privacydev.net/*
// @match           *://piped.palveluntarjoaja.eu/*
// @match           *://piped.smnz.de/*
// @match           *://piped.adminforge.de/*
// @match           *://piped.qdi.fi/*
// @match           *://piped.hostux.net/*
// @match           *://piped.chauvet.pro/*
// @match           *://piped.jotoma.de/*
// @match           *://piped.pfcd.me/*
// @match           *://piped.frontendfriendly.xyz/*
// @match           *://proxitok.pabloferreiro.es/*
// @match           *://proxitok.pussthecat.org/*
// @match           *://tok.habedieeh.re/*
// @match           *://proxitok.esmailelbob.xyz/*
// @match           *://proxitok.privacydev.net/*
// @match           *://tok.artemislena.eu/*
// @match           *://tok.adminforge.de/*
// @match           *://tt.vern.cc/*
// @match           *://cringe.whatever.social/*
// @match           *://proxitok.lunar.icu/*
// @match           *://proxitok.privacy.com.de/*
// @match           *://peertube.1312.media/*
// @match           *://tube.shanti.cafe/*
// @match           *://*.bee-tube.fr/*
// @match           *://video.sadmin.io/*
// @match           *://*.dalek.zone/*
// @match           *://review.peertube.biz/*
// @match           *://*.peervideo.club/*
// @match           *://tube.la-dina.net/*
// @match           *://peertube.tmp.rcp.tf/*
// @match           *://*.peertube.su/*
// @match           *://video.blender.org/*
// @match           *://videos.viorsan.com/*
// @match           *://tube-sciences-technologies.apps.education.fr/*
// @match           *://tube-numerique-educatif.apps.education.fr/*
// @match           *://tube-arts-lettres-sciences-humaines.apps.education.fr/*
// @match           *://*.beetoons.tv/*
// @match           *://comics.peertube.biz/*
// @match           *://*.makertube.net/*
// @match           *://*.coursehunter.net/*
// @match           *://*.coursetrain.net/*
// @exclude         *://accounts.youtube.com/*
// @require         https://gist.githubusercontent.com/ilyhalight/6eb5bb4dffc7ca9e3c57d6933e2452f3/raw/7ab38af2228d0bed13912e503bc8a9ee4b11828d/gm-addstyle-polyfill.js
// @connect         cloud-api.yandex.com
// @connect         oauth.yandex.ru
// @connect         yandex.ru
// @connect         disk.yandex.kz
// @connect         disk.yandex.com
// @connect         disk.yandex.com.am
// @connect         disk.yandex.com.ge
// @connect         disk.yandex.com.tr
// @connect         disk.yandex.by
// @connect         disk.yandex.az
// @connect         disk.yandex.co.il
// @connect         disk.yandex.ee
// @connect         disk.yandex.lt
// @connect         disk.yandex.lv
// @connect         disk.yandex.md
// @connect         disk.yandex.net
// @connect         disk.yandex.tj
// @connect         disk.yandex.tm
// @connect         disk.yandex.uz
// @connect         disk.360.yandex.ru
// @connect         yandex.net
// @connect         timeweb.cloud
// @connect         raw.githubusercontent.com
// @connect         vimeo.com
// @connect         vot.toil.cc
// @connect         *.toil.cc
// @connect         toil.cc
// @connect         deno.dev
// @connect         onrender.com
// @connect         workers.dev
// @connect         packaged-media.redd.it
// @connect         cloudflare-dns.com
// @connect         porntn.com
// @connect         youtube.com
// @connect         googlevideo.com
// @connect         vk.com
// @connect         *.vk.com
// @connect         *
// @grant           GM.deleteValue
// @grant           GM.getValue
// @grant           GM.getValues
// @grant           GM.listValues
// @grant           GM.setValue
// @grant           GM_addStyle
// @grant           GM_deleteValue
// @grant           GM_getValue
// @grant           GM_info
// @grant           GM_listValues
// @grant           GM_notification
// @grant           GM_setValue
// @grant           GM_xmlhttpRequest
// @grant           window.focus
// ==/UserScript==

(function () {
  'use strict';

  const t$1=new Set;const importCSS = async e=>{t$1.has(e)||(t$1.add(e),(a=>GM_addStyle(a))(e));};

  const votConfig = {
    host: "api.browser.yandex.ru",
    hostVOT: "vot.toil.cc/v1",
    hostWorker: "vot-worker.toil.cc",
    mediaProxy: "media-proxy.toil.cc",
    userAgent: "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/134.0.0.0 YaBrowser/25.4.0.0 Safari/537.36",
    componentVersion: "25.6.0.2259",
    hmac: "bt8xH3VOlb4mqf0nqAibnDOoiPlXsisf",
    defaultDuration: 343,
    minChunkSize: 5295308,
    loggerLevel: 1,
    version: "2.4.12"
  };
  var LoggerLevel;
  (function(LoggerLevel2) {
    LoggerLevel2[LoggerLevel2["DEBUG"] = 0] = "DEBUG";
    LoggerLevel2[LoggerLevel2["INFO"] = 1] = "INFO";
    LoggerLevel2[LoggerLevel2["WARN"] = 2] = "WARN";
    LoggerLevel2[LoggerLevel2["ERROR"] = 3] = "ERROR";
    LoggerLevel2[LoggerLevel2["SILENCE"] = 4] = "SILENCE";
  })(LoggerLevel || (LoggerLevel = {}));
  class Logger {
    static prefix = `[vot.js v${votConfig.version}]`;
    static canLog(level) {
      return votConfig.loggerLevel <= level;
    }
    static log(...messages) {
      if (!Logger.canLog(LoggerLevel.DEBUG)) {
        return;
      }
      console.log(Logger.prefix, ...messages);
    }
    static info(...messages) {
      if (!Logger.canLog(LoggerLevel.INFO)) {
        return;
      }
      console.info(Logger.prefix, ...messages);
    }
    static warn(...messages) {
      if (!Logger.canLog(LoggerLevel.WARN)) {
        return;
      }
      console.warn(Logger.prefix, ...messages);
    }
    static error(...messages) {
      if (!Logger.canLog(LoggerLevel.ERROR)) {
        return;
      }
      console.error(Logger.prefix, ...messages);
    }
  }
  function varint64read() {
    let lowBits = 0;
    let highBits = 0;
    for (let shift = 0; shift < 28; shift += 7) {
      let b2 = this.buf[this.pos++];
      lowBits |= (b2 & 127) << shift;
      if ((b2 & 128) == 0) {
        this.assertBounds();
        return [lowBits, highBits];
      }
    }
    let middleByte = this.buf[this.pos++];
    lowBits |= (middleByte & 15) << 28;
    highBits = (middleByte & 112) >> 4;
    if ((middleByte & 128) == 0) {
      this.assertBounds();
      return [lowBits, highBits];
    }
    for (let shift = 3; shift <= 31; shift += 7) {
      let b2 = this.buf[this.pos++];
      highBits |= (b2 & 127) << shift;
      if ((b2 & 128) == 0) {
        this.assertBounds();
        return [lowBits, highBits];
      }
    }
    throw new Error("invalid varint");
  }
  function varint64write(lo, hi, bytes) {
    for (let i2 = 0; i2 < 28; i2 = i2 + 7) {
      const shift = lo >>> i2;
      const hasNext = !(shift >>> 7 == 0 && hi == 0);
      const byte = (hasNext ? shift | 128 : shift) & 255;
      bytes.push(byte);
      if (!hasNext) {
        return;
      }
    }
    const splitBits = lo >>> 28 & 15 | (hi & 7) << 4;
    const hasMoreBits = !(hi >> 3 == 0);
    bytes.push((hasMoreBits ? splitBits | 128 : splitBits) & 255);
    if (!hasMoreBits) {
      return;
    }
    for (let i2 = 3; i2 < 31; i2 = i2 + 7) {
      const shift = hi >>> i2;
      const hasNext = !(shift >>> 7 == 0);
      const byte = (hasNext ? shift | 128 : shift) & 255;
      bytes.push(byte);
      if (!hasNext) {
        return;
      }
    }
    bytes.push(hi >>> 31 & 1);
  }
  const TWO_PWR_32_DBL = 4294967296;
  function int64FromString(dec) {
    const minus = dec[0] === "-";
    if (minus) {
      dec = dec.slice(1);
    }
    const base = 1e6;
    let lowBits = 0;
    let highBits = 0;
    function add1e6digit(begin, end) {
      const digit1e6 = Number(dec.slice(begin, end));
      highBits *= base;
      lowBits = lowBits * base + digit1e6;
      if (lowBits >= TWO_PWR_32_DBL) {
        highBits = highBits + (lowBits / TWO_PWR_32_DBL | 0);
        lowBits = lowBits % TWO_PWR_32_DBL;
      }
    }
    add1e6digit(-24, -18);
    add1e6digit(-18, -12);
    add1e6digit(-12, -6);
    add1e6digit(-6);
    return minus ? negate(lowBits, highBits) : newBits(lowBits, highBits);
  }
  function int64ToString(lo, hi) {
    let bits = newBits(lo, hi);
    const negative = bits.hi & 2147483648;
    if (negative) {
      bits = negate(bits.lo, bits.hi);
    }
    const result = uInt64ToString(bits.lo, bits.hi);
    return negative ? "-" + result : result;
  }
  function uInt64ToString(lo, hi) {
    ({ lo, hi } = toUnsigned(lo, hi));
    if (hi <= 2097151) {
      return String(TWO_PWR_32_DBL * hi + lo);
    }
    const low = lo & 16777215;
    const mid = (lo >>> 24 | hi << 8) & 16777215;
    const high = hi >> 16 & 65535;
    let digitA = low + mid * 6777216 + high * 6710656;
    let digitB = mid + high * 8147497;
    let digitC = high * 2;
    const base = 1e7;
    if (digitA >= base) {
      digitB += Math.floor(digitA / base);
      digitA %= base;
    }
    if (digitB >= base) {
      digitC += Math.floor(digitB / base);
      digitB %= base;
    }
    return digitC.toString() + decimalFrom1e7WithLeadingZeros(digitB) + decimalFrom1e7WithLeadingZeros(digitA);
  }
  function toUnsigned(lo, hi) {
    return { lo: lo >>> 0, hi: hi >>> 0 };
  }
  function newBits(lo, hi) {
    return { lo: lo | 0, hi: hi | 0 };
  }
  function negate(lowBits, highBits) {
    highBits = ~highBits;
    if (lowBits) {
      lowBits = ~lowBits + 1;
    } else {
      highBits += 1;
    }
    return newBits(lowBits, highBits);
  }
  const decimalFrom1e7WithLeadingZeros = (digit1e7) => {
    const partial = String(digit1e7);
    return "0000000".slice(partial.length) + partial;
  };
  function varint32write(value, bytes) {
    if (value >= 0) {
      while (value > 127) {
        bytes.push(value & 127 | 128);
        value = value >>> 7;
      }
      bytes.push(value);
    } else {
      for (let i2 = 0; i2 < 9; i2++) {
        bytes.push(value & 127 | 128);
        value = value >> 7;
      }
      bytes.push(1);
    }
  }
  function varint32read() {
    let b2 = this.buf[this.pos++];
    let result = b2 & 127;
    if ((b2 & 128) == 0) {
      this.assertBounds();
      return result;
    }
    b2 = this.buf[this.pos++];
    result |= (b2 & 127) << 7;
    if ((b2 & 128) == 0) {
      this.assertBounds();
      return result;
    }
    b2 = this.buf[this.pos++];
    result |= (b2 & 127) << 14;
    if ((b2 & 128) == 0) {
      this.assertBounds();
      return result;
    }
    b2 = this.buf[this.pos++];
    result |= (b2 & 127) << 21;
    if ((b2 & 128) == 0) {
      this.assertBounds();
      return result;
    }
    b2 = this.buf[this.pos++];
    result |= (b2 & 15) << 28;
    for (let readBytes = 5; (b2 & 128) !== 0 && readBytes < 10; readBytes++)
      b2 = this.buf[this.pos++];
    if ((b2 & 128) != 0)
      throw new Error("invalid varint");
    this.assertBounds();
    return result >>> 0;
  }
  var define_process_env_default = {};
  const protoInt64 = makeInt64Support();
  function makeInt64Support() {
    const dv = new DataView(new ArrayBuffer(8));
    const ok = typeof BigInt === "function" && typeof dv.getBigInt64 === "function" && typeof dv.getBigUint64 === "function" && typeof dv.setBigInt64 === "function" && typeof dv.setBigUint64 === "function" && (!!globalThis.Deno || typeof process != "object" || typeof define_process_env_default != "object" || define_process_env_default.BUF_BIGINT_DISABLE !== "1");
    if (ok) {
      const MIN = BigInt("-9223372036854775808");
      const MAX = BigInt("9223372036854775807");
      const UMIN = BigInt("0");
      const UMAX = BigInt("18446744073709551615");
      return {
        zero: BigInt(0),
        supported: true,
        parse(value) {
          const bi = typeof value == "bigint" ? value : BigInt(value);
          if (bi > MAX || bi < MIN) {
            throw new Error(`invalid int64: ${value}`);
          }
          return bi;
        },
        uParse(value) {
          const bi = typeof value == "bigint" ? value : BigInt(value);
          if (bi > UMAX || bi < UMIN) {
            throw new Error(`invalid uint64: ${value}`);
          }
          return bi;
        },
        enc(value) {
          dv.setBigInt64(0, this.parse(value), true);
          return {
            lo: dv.getInt32(0, true),
            hi: dv.getInt32(4, true)
          };
        },
        uEnc(value) {
          dv.setBigInt64(0, this.uParse(value), true);
          return {
            lo: dv.getInt32(0, true),
            hi: dv.getInt32(4, true)
          };
        },
        dec(lo, hi) {
          dv.setInt32(0, lo, true);
          dv.setInt32(4, hi, true);
          return dv.getBigInt64(0, true);
        },
        uDec(lo, hi) {
          dv.setInt32(0, lo, true);
          dv.setInt32(4, hi, true);
          return dv.getBigUint64(0, true);
        }
      };
    }
    return {
      zero: "0",
      supported: false,
      parse(value) {
        if (typeof value != "string") {
          value = value.toString();
        }
        assertInt64String(value);
        return value;
      },
      uParse(value) {
        if (typeof value != "string") {
          value = value.toString();
        }
        assertUInt64String(value);
        return value;
      },
      enc(value) {
        if (typeof value != "string") {
          value = value.toString();
        }
        assertInt64String(value);
        return int64FromString(value);
      },
      uEnc(value) {
        if (typeof value != "string") {
          value = value.toString();
        }
        assertUInt64String(value);
        return int64FromString(value);
      },
      dec(lo, hi) {
        return int64ToString(lo, hi);
      },
      uDec(lo, hi) {
        return uInt64ToString(lo, hi);
      }
    };
  }
  function assertInt64String(value) {
    if (!/^-?[0-9]+$/.test(value)) {
      throw new Error("invalid int64: " + value);
    }
  }
  function assertUInt64String(value) {
    if (!/^[0-9]+$/.test(value)) {
      throw new Error("invalid uint64: " + value);
    }
  }
  const symbol = Symbol.for("@bufbuild/protobuf/text-encoding");
  function getTextEncoding() {
    if (globalThis[symbol] == void 0) {
      const te = new globalThis.TextEncoder();
      const td = new globalThis.TextDecoder();
      globalThis[symbol] = {
        encodeUtf8(text) {
          return te.encode(text);
        },
        decodeUtf8(bytes) {
          return td.decode(bytes);
        },
        checkUtf8(text) {
          try {
            encodeURIComponent(text);
            return true;
          } catch (_2) {
            return false;
          }
        }
      };
    }
    return globalThis[symbol];
  }
  var WireType;
  (function(WireType2) {
    WireType2[WireType2["Varint"] = 0] = "Varint";
    WireType2[WireType2["Bit64"] = 1] = "Bit64";
    WireType2[WireType2["LengthDelimited"] = 2] = "LengthDelimited";
    WireType2[WireType2["StartGroup"] = 3] = "StartGroup";
    WireType2[WireType2["EndGroup"] = 4] = "EndGroup";
    WireType2[WireType2["Bit32"] = 5] = "Bit32";
  })(WireType || (WireType = {}));
  const FLOAT32_MAX = 34028234663852886e22;
  const FLOAT32_MIN = -34028234663852886e22;
  const UINT32_MAX = 4294967295;
  const INT32_MAX = 2147483647;
  const INT32_MIN = -2147483648;
  class BinaryWriter {
    constructor(encodeUtf8 = getTextEncoding().encodeUtf8) {
      this.encodeUtf8 = encodeUtf8;
      this.stack = [];
      this.chunks = [];
      this.buf = [];
    }
finish() {
      if (this.buf.length) {
        this.chunks.push(new Uint8Array(this.buf));
        this.buf = [];
      }
      let len = 0;
      for (let i2 = 0; i2 < this.chunks.length; i2++)
        len += this.chunks[i2].length;
      let bytes = new Uint8Array(len);
      let offset = 0;
      for (let i2 = 0; i2 < this.chunks.length; i2++) {
        bytes.set(this.chunks[i2], offset);
        offset += this.chunks[i2].length;
      }
      this.chunks = [];
      return bytes;
    }
fork() {
      this.stack.push({ chunks: this.chunks, buf: this.buf });
      this.chunks = [];
      this.buf = [];
      return this;
    }
join() {
      let chunk = this.finish();
      let prev = this.stack.pop();
      if (!prev)
        throw new Error("invalid state, fork stack empty");
      this.chunks = prev.chunks;
      this.buf = prev.buf;
      this.uint32(chunk.byteLength);
      return this.raw(chunk);
    }
tag(fieldNo, type) {
      return this.uint32((fieldNo << 3 | type) >>> 0);
    }
raw(chunk) {
      if (this.buf.length) {
        this.chunks.push(new Uint8Array(this.buf));
        this.buf = [];
      }
      this.chunks.push(chunk);
      return this;
    }
uint32(value) {
      assertUInt32(value);
      while (value > 127) {
        this.buf.push(value & 127 | 128);
        value = value >>> 7;
      }
      this.buf.push(value);
      return this;
    }
int32(value) {
      assertInt32(value);
      varint32write(value, this.buf);
      return this;
    }
bool(value) {
      this.buf.push(value ? 1 : 0);
      return this;
    }
bytes(value) {
      this.uint32(value.byteLength);
      return this.raw(value);
    }
string(value) {
      let chunk = this.encodeUtf8(value);
      this.uint32(chunk.byteLength);
      return this.raw(chunk);
    }
float(value) {
      assertFloat32(value);
      let chunk = new Uint8Array(4);
      new DataView(chunk.buffer).setFloat32(0, value, true);
      return this.raw(chunk);
    }
double(value) {
      let chunk = new Uint8Array(8);
      new DataView(chunk.buffer).setFloat64(0, value, true);
      return this.raw(chunk);
    }
fixed32(value) {
      assertUInt32(value);
      let chunk = new Uint8Array(4);
      new DataView(chunk.buffer).setUint32(0, value, true);
      return this.raw(chunk);
    }
sfixed32(value) {
      assertInt32(value);
      let chunk = new Uint8Array(4);
      new DataView(chunk.buffer).setInt32(0, value, true);
      return this.raw(chunk);
    }
sint32(value) {
      assertInt32(value);
      value = (value << 1 ^ value >> 31) >>> 0;
      varint32write(value, this.buf);
      return this;
    }
sfixed64(value) {
      let chunk = new Uint8Array(8), view = new DataView(chunk.buffer), tc = protoInt64.enc(value);
      view.setInt32(0, tc.lo, true);
      view.setInt32(4, tc.hi, true);
      return this.raw(chunk);
    }
fixed64(value) {
      let chunk = new Uint8Array(8), view = new DataView(chunk.buffer), tc = protoInt64.uEnc(value);
      view.setInt32(0, tc.lo, true);
      view.setInt32(4, tc.hi, true);
      return this.raw(chunk);
    }
int64(value) {
      let tc = protoInt64.enc(value);
      varint64write(tc.lo, tc.hi, this.buf);
      return this;
    }
sint64(value) {
      const tc = protoInt64.enc(value), sign = tc.hi >> 31, lo = tc.lo << 1 ^ sign, hi = (tc.hi << 1 | tc.lo >>> 31) ^ sign;
      varint64write(lo, hi, this.buf);
      return this;
    }
uint64(value) {
      const tc = protoInt64.uEnc(value);
      varint64write(tc.lo, tc.hi, this.buf);
      return this;
    }
  }
  class BinaryReader {
    constructor(buf, decodeUtf8 = getTextEncoding().decodeUtf8) {
      this.decodeUtf8 = decodeUtf8;
      this.varint64 = varint64read;
      this.uint32 = varint32read;
      this.buf = buf;
      this.len = buf.length;
      this.pos = 0;
      this.view = new DataView(buf.buffer, buf.byteOffset, buf.byteLength);
    }
tag() {
      let tag = this.uint32(), fieldNo = tag >>> 3, wireType = tag & 7;
      if (fieldNo <= 0 || wireType < 0 || wireType > 5)
        throw new Error("illegal tag: field no " + fieldNo + " wire type " + wireType);
      return [fieldNo, wireType];
    }
skip(wireType, fieldNo) {
      let start = this.pos;
      switch (wireType) {
        case WireType.Varint:
          while (this.buf[this.pos++] & 128) {
          }
          break;
case WireType.Bit64:
          this.pos += 4;
        case WireType.Bit32:
          this.pos += 4;
          break;
        case WireType.LengthDelimited:
          let len = this.uint32();
          this.pos += len;
          break;
        case WireType.StartGroup:
          for (; ; ) {
            const [fn, wt] = this.tag();
            if (wt === WireType.EndGroup) {
              if (fieldNo !== void 0 && fn !== fieldNo) {
                throw new Error("invalid end group tag");
              }
              break;
            }
            this.skip(wt, fn);
          }
          break;
        default:
          throw new Error("cant skip wire type " + wireType);
      }
      this.assertBounds();
      return this.buf.subarray(start, this.pos);
    }
assertBounds() {
      if (this.pos > this.len)
        throw new RangeError("premature EOF");
    }
int32() {
      return this.uint32() | 0;
    }
sint32() {
      let zze = this.uint32();
      return zze >>> 1 ^ -(zze & 1);
    }
int64() {
      return protoInt64.dec(...this.varint64());
    }
uint64() {
      return protoInt64.uDec(...this.varint64());
    }
sint64() {
      let [lo, hi] = this.varint64();
      let s2 = -(lo & 1);
      lo = (lo >>> 1 | (hi & 1) << 31) ^ s2;
      hi = hi >>> 1 ^ s2;
      return protoInt64.dec(lo, hi);
    }
bool() {
      let [lo, hi] = this.varint64();
      return lo !== 0 || hi !== 0;
    }
fixed32() {
      return this.view.getUint32((this.pos += 4) - 4, true);
    }
sfixed32() {
      return this.view.getInt32((this.pos += 4) - 4, true);
    }
fixed64() {
      return protoInt64.uDec(this.sfixed32(), this.sfixed32());
    }
sfixed64() {
      return protoInt64.dec(this.sfixed32(), this.sfixed32());
    }
float() {
      return this.view.getFloat32((this.pos += 4) - 4, true);
    }
double() {
      return this.view.getFloat64((this.pos += 8) - 8, true);
    }
bytes() {
      let len = this.uint32(), start = this.pos;
      this.pos += len;
      this.assertBounds();
      return this.buf.subarray(start, start + len);
    }
string() {
      return this.decodeUtf8(this.bytes());
    }
  }
  function assertInt32(arg) {
    if (typeof arg == "string") {
      arg = Number(arg);
    } else if (typeof arg != "number") {
      throw new Error("invalid int32: " + typeof arg);
    }
    if (!Number.isInteger(arg) || arg > INT32_MAX || arg < INT32_MIN)
      throw new Error("invalid int32: " + arg);
  }
  function assertUInt32(arg) {
    if (typeof arg == "string") {
      arg = Number(arg);
    } else if (typeof arg != "number") {
      throw new Error("invalid uint32: " + typeof arg);
    }
    if (!Number.isInteger(arg) || arg > UINT32_MAX || arg < 0)
      throw new Error("invalid uint32: " + arg);
  }
  function assertFloat32(arg) {
    if (typeof arg == "string") {
      const o2 = arg;
      arg = Number(arg);
      if (Number.isNaN(arg) && o2 !== "NaN") {
        throw new Error("invalid float32: " + o2);
      }
    } else if (typeof arg != "number") {
      throw new Error("invalid float32: " + typeof arg);
    }
    if (Number.isFinite(arg) && (arg > FLOAT32_MAX || arg < FLOAT32_MIN))
      throw new Error("invalid float32: " + arg);
  }
  var StreamInterval;
  (function(StreamInterval2) {
    StreamInterval2[StreamInterval2["NO_CONNECTION"] = 0] = "NO_CONNECTION";
    StreamInterval2[StreamInterval2["TRANSLATING"] = 10] = "TRANSLATING";
    StreamInterval2[StreamInterval2["STREAMING"] = 20] = "STREAMING";
    StreamInterval2[StreamInterval2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
  })(StreamInterval || (StreamInterval = {}));
  function streamIntervalFromJSON(object) {
    switch (object) {
      case 0:
      case "NO_CONNECTION":
        return StreamInterval.NO_CONNECTION;
      case 10:
      case "TRANSLATING":
        return StreamInterval.TRANSLATING;
      case 20:
      case "STREAMING":
        return StreamInterval.STREAMING;
      case -1:
      case "UNRECOGNIZED":
      default:
        return StreamInterval.UNRECOGNIZED;
    }
  }
  function streamIntervalToJSON(object) {
    switch (object) {
      case StreamInterval.NO_CONNECTION:
        return "NO_CONNECTION";
      case StreamInterval.TRANSLATING:
        return "TRANSLATING";
      case StreamInterval.STREAMING:
        return "STREAMING";
      case StreamInterval.UNRECOGNIZED:
      default:
        return "UNRECOGNIZED";
    }
  }
  function createBaseVideoTranslationHelpObject() {
    return { target: "", targetUrl: "" };
  }
  const VideoTranslationHelpObject = {
    encode(message, writer = new BinaryWriter()) {
      if (message.target !== "") {
        writer.uint32(10).string(message.target);
      }
      if (message.targetUrl !== "") {
        writer.uint32(18).string(message.targetUrl);
      }
      return writer;
    },
    decode(input, length) {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      let end = length === void 0 ? reader.len : reader.pos + length;
      const message = createBaseVideoTranslationHelpObject();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }
            message.target = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }
            message.targetUrl = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },
    fromJSON(object) {
      return {
        target: isSet(object.target) ? globalThis.String(object.target) : "",
        targetUrl: isSet(object.targetUrl) ? globalThis.String(object.targetUrl) : ""
      };
    },
    toJSON(message) {
      const obj = {};
      if (message.target !== "") {
        obj.target = message.target;
      }
      if (message.targetUrl !== "") {
        obj.targetUrl = message.targetUrl;
      }
      return obj;
    },
    create(base) {
      return VideoTranslationHelpObject.fromPartial(base ?? {});
    },
    fromPartial(object) {
      const message = createBaseVideoTranslationHelpObject();
      message.target = object.target ?? "";
      message.targetUrl = object.targetUrl ?? "";
      return message;
    }
  };
  function createBaseVideoTranslationRequest() {
    return {
      url: "",
      deviceId: void 0,
      firstRequest: false,
      duration: 0,
      unknown0: 0,
      language: "",
      forceSourceLang: false,
      unknown1: 0,
      translationHelp: [],
      wasStream: false,
      responseLanguage: "",
      unknown2: 0,
      unknown3: 0,
      bypassCache: false,
      useLivelyVoice: false,
      videoTitle: ""
    };
  }
  const VideoTranslationRequest = {
    encode(message, writer = new BinaryWriter()) {
      if (message.url !== "") {
        writer.uint32(26).string(message.url);
      }
      if (message.deviceId !== void 0) {
        writer.uint32(34).string(message.deviceId);
      }
      if (message.firstRequest !== false) {
        writer.uint32(40).bool(message.firstRequest);
      }
      if (message.duration !== 0) {
        writer.uint32(49).double(message.duration);
      }
      if (message.unknown0 !== 0) {
        writer.uint32(56).int32(message.unknown0);
      }
      if (message.language !== "") {
        writer.uint32(66).string(message.language);
      }
      if (message.forceSourceLang !== false) {
        writer.uint32(72).bool(message.forceSourceLang);
      }
      if (message.unknown1 !== 0) {
        writer.uint32(80).int32(message.unknown1);
      }
      for (const v2 of message.translationHelp) {
        VideoTranslationHelpObject.encode(v2, writer.uint32(90).fork()).join();
      }
      if (message.wasStream !== false) {
        writer.uint32(104).bool(message.wasStream);
      }
      if (message.responseLanguage !== "") {
        writer.uint32(114).string(message.responseLanguage);
      }
      if (message.unknown2 !== 0) {
        writer.uint32(120).int32(message.unknown2);
      }
      if (message.unknown3 !== 0) {
        writer.uint32(128).int32(message.unknown3);
      }
      if (message.bypassCache !== false) {
        writer.uint32(136).bool(message.bypassCache);
      }
      if (message.useLivelyVoice !== false) {
        writer.uint32(144).bool(message.useLivelyVoice);
      }
      if (message.videoTitle !== "") {
        writer.uint32(154).string(message.videoTitle);
      }
      return writer;
    },
    decode(input, length) {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      let end = length === void 0 ? reader.len : reader.pos + length;
      const message = createBaseVideoTranslationRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 3: {
            if (tag !== 26) {
              break;
            }
            message.url = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }
            message.deviceId = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }
            message.firstRequest = reader.bool();
            continue;
          }
          case 6: {
            if (tag !== 49) {
              break;
            }
            message.duration = reader.double();
            continue;
          }
          case 7: {
            if (tag !== 56) {
              break;
            }
            message.unknown0 = reader.int32();
            continue;
          }
          case 8: {
            if (tag !== 66) {
              break;
            }
            message.language = reader.string();
            continue;
          }
          case 9: {
            if (tag !== 72) {
              break;
            }
            message.forceSourceLang = reader.bool();
            continue;
          }
          case 10: {
            if (tag !== 80) {
              break;
            }
            message.unknown1 = reader.int32();
            continue;
          }
          case 11: {
            if (tag !== 90) {
              break;
            }
            message.translationHelp.push(VideoTranslationHelpObject.decode(reader, reader.uint32()));
            continue;
          }
          case 13: {
            if (tag !== 104) {
              break;
            }
            message.wasStream = reader.bool();
            continue;
          }
          case 14: {
            if (tag !== 114) {
              break;
            }
            message.responseLanguage = reader.string();
            continue;
          }
          case 15: {
            if (tag !== 120) {
              break;
            }
            message.unknown2 = reader.int32();
            continue;
          }
          case 16: {
            if (tag !== 128) {
              break;
            }
            message.unknown3 = reader.int32();
            continue;
          }
          case 17: {
            if (tag !== 136) {
              break;
            }
            message.bypassCache = reader.bool();
            continue;
          }
          case 18: {
            if (tag !== 144) {
              break;
            }
            message.useLivelyVoice = reader.bool();
            continue;
          }
          case 19: {
            if (tag !== 154) {
              break;
            }
            message.videoTitle = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },
    fromJSON(object) {
      return {
        url: isSet(object.url) ? globalThis.String(object.url) : "",
        deviceId: isSet(object.deviceId) ? globalThis.String(object.deviceId) : void 0,
        firstRequest: isSet(object.firstRequest) ? globalThis.Boolean(object.firstRequest) : false,
        duration: isSet(object.duration) ? globalThis.Number(object.duration) : 0,
        unknown0: isSet(object.unknown0) ? globalThis.Number(object.unknown0) : 0,
        language: isSet(object.language) ? globalThis.String(object.language) : "",
        forceSourceLang: isSet(object.forceSourceLang) ? globalThis.Boolean(object.forceSourceLang) : false,
        unknown1: isSet(object.unknown1) ? globalThis.Number(object.unknown1) : 0,
        translationHelp: globalThis.Array.isArray(object?.translationHelp) ? object.translationHelp.map((e2) => VideoTranslationHelpObject.fromJSON(e2)) : [],
        wasStream: isSet(object.wasStream) ? globalThis.Boolean(object.wasStream) : false,
        responseLanguage: isSet(object.responseLanguage) ? globalThis.String(object.responseLanguage) : "",
        unknown2: isSet(object.unknown2) ? globalThis.Number(object.unknown2) : 0,
        unknown3: isSet(object.unknown3) ? globalThis.Number(object.unknown3) : 0,
        bypassCache: isSet(object.bypassCache) ? globalThis.Boolean(object.bypassCache) : false,
        useLivelyVoice: isSet(object.useLivelyVoice) ? globalThis.Boolean(object.useLivelyVoice) : false,
        videoTitle: isSet(object.videoTitle) ? globalThis.String(object.videoTitle) : ""
      };
    },
    toJSON(message) {
      const obj = {};
      if (message.url !== "") {
        obj.url = message.url;
      }
      if (message.deviceId !== void 0) {
        obj.deviceId = message.deviceId;
      }
      if (message.firstRequest !== false) {
        obj.firstRequest = message.firstRequest;
      }
      if (message.duration !== 0) {
        obj.duration = message.duration;
      }
      if (message.unknown0 !== 0) {
        obj.unknown0 = Math.round(message.unknown0);
      }
      if (message.language !== "") {
        obj.language = message.language;
      }
      if (message.forceSourceLang !== false) {
        obj.forceSourceLang = message.forceSourceLang;
      }
      if (message.unknown1 !== 0) {
        obj.unknown1 = Math.round(message.unknown1);
      }
      if (message.translationHelp?.length) {
        obj.translationHelp = message.translationHelp.map((e2) => VideoTranslationHelpObject.toJSON(e2));
      }
      if (message.wasStream !== false) {
        obj.wasStream = message.wasStream;
      }
      if (message.responseLanguage !== "") {
        obj.responseLanguage = message.responseLanguage;
      }
      if (message.unknown2 !== 0) {
        obj.unknown2 = Math.round(message.unknown2);
      }
      if (message.unknown3 !== 0) {
        obj.unknown3 = Math.round(message.unknown3);
      }
      if (message.bypassCache !== false) {
        obj.bypassCache = message.bypassCache;
      }
      if (message.useLivelyVoice !== false) {
        obj.useLivelyVoice = message.useLivelyVoice;
      }
      if (message.videoTitle !== "") {
        obj.videoTitle = message.videoTitle;
      }
      return obj;
    },
    create(base) {
      return VideoTranslationRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
      const message = createBaseVideoTranslationRequest();
      message.url = object.url ?? "";
      message.deviceId = object.deviceId ?? void 0;
      message.firstRequest = object.firstRequest ?? false;
      message.duration = object.duration ?? 0;
      message.unknown0 = object.unknown0 ?? 0;
      message.language = object.language ?? "";
      message.forceSourceLang = object.forceSourceLang ?? false;
      message.unknown1 = object.unknown1 ?? 0;
      message.translationHelp = object.translationHelp?.map((e2) => VideoTranslationHelpObject.fromPartial(e2)) || [];
      message.wasStream = object.wasStream ?? false;
      message.responseLanguage = object.responseLanguage ?? "";
      message.unknown2 = object.unknown2 ?? 0;
      message.unknown3 = object.unknown3 ?? 0;
      message.bypassCache = object.bypassCache ?? false;
      message.useLivelyVoice = object.useLivelyVoice ?? false;
      message.videoTitle = object.videoTitle ?? "";
      return message;
    }
  };
  function createBaseVideoTranslationResponse() {
    return {
      url: void 0,
      duration: void 0,
      status: 0,
      remainingTime: void 0,
      unknown0: void 0,
      translationId: "",
      language: void 0,
      message: void 0,
      isLivelyVoice: false,
      unknown2: void 0,
      shouldRetry: void 0,
      unknown3: void 0
    };
  }
  const VideoTranslationResponse = {
    encode(message, writer = new BinaryWriter()) {
      if (message.url !== void 0) {
        writer.uint32(10).string(message.url);
      }
      if (message.duration !== void 0) {
        writer.uint32(17).double(message.duration);
      }
      if (message.status !== 0) {
        writer.uint32(32).int32(message.status);
      }
      if (message.remainingTime !== void 0) {
        writer.uint32(40).int32(message.remainingTime);
      }
      if (message.unknown0 !== void 0) {
        writer.uint32(48).int32(message.unknown0);
      }
      if (message.translationId !== "") {
        writer.uint32(58).string(message.translationId);
      }
      if (message.language !== void 0) {
        writer.uint32(66).string(message.language);
      }
      if (message.message !== void 0) {
        writer.uint32(74).string(message.message);
      }
      if (message.isLivelyVoice !== false) {
        writer.uint32(80).bool(message.isLivelyVoice);
      }
      if (message.unknown2 !== void 0) {
        writer.uint32(88).int32(message.unknown2);
      }
      if (message.shouldRetry !== void 0) {
        writer.uint32(96).int32(message.shouldRetry);
      }
      if (message.unknown3 !== void 0) {
        writer.uint32(104).int32(message.unknown3);
      }
      return writer;
    },
    decode(input, length) {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      let end = length === void 0 ? reader.len : reader.pos + length;
      const message = createBaseVideoTranslationResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }
            message.url = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 17) {
              break;
            }
            message.duration = reader.double();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }
            message.status = reader.int32();
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }
            message.remainingTime = reader.int32();
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }
            message.unknown0 = reader.int32();
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }
            message.translationId = reader.string();
            continue;
          }
          case 8: {
            if (tag !== 66) {
              break;
            }
            message.language = reader.string();
            continue;
          }
          case 9: {
            if (tag !== 74) {
              break;
            }
            message.message = reader.string();
            continue;
          }
          case 10: {
            if (tag !== 80) {
              break;
            }
            message.isLivelyVoice = reader.bool();
            continue;
          }
          case 11: {
            if (tag !== 88) {
              break;
            }
            message.unknown2 = reader.int32();
            continue;
          }
          case 12: {
            if (tag !== 96) {
              break;
            }
            message.shouldRetry = reader.int32();
            continue;
          }
          case 13: {
            if (tag !== 104) {
              break;
            }
            message.unknown3 = reader.int32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },
    fromJSON(object) {
      return {
        url: isSet(object.url) ? globalThis.String(object.url) : void 0,
        duration: isSet(object.duration) ? globalThis.Number(object.duration) : void 0,
        status: isSet(object.status) ? globalThis.Number(object.status) : 0,
        remainingTime: isSet(object.remainingTime) ? globalThis.Number(object.remainingTime) : void 0,
        unknown0: isSet(object.unknown0) ? globalThis.Number(object.unknown0) : void 0,
        translationId: isSet(object.translationId) ? globalThis.String(object.translationId) : "",
        language: isSet(object.language) ? globalThis.String(object.language) : void 0,
        message: isSet(object.message) ? globalThis.String(object.message) : void 0,
        isLivelyVoice: isSet(object.isLivelyVoice) ? globalThis.Boolean(object.isLivelyVoice) : false,
        unknown2: isSet(object.unknown2) ? globalThis.Number(object.unknown2) : void 0,
        shouldRetry: isSet(object.shouldRetry) ? globalThis.Number(object.shouldRetry) : void 0,
        unknown3: isSet(object.unknown3) ? globalThis.Number(object.unknown3) : void 0
      };
    },
    toJSON(message) {
      const obj = {};
      if (message.url !== void 0) {
        obj.url = message.url;
      }
      if (message.duration !== void 0) {
        obj.duration = message.duration;
      }
      if (message.status !== 0) {
        obj.status = Math.round(message.status);
      }
      if (message.remainingTime !== void 0) {
        obj.remainingTime = Math.round(message.remainingTime);
      }
      if (message.unknown0 !== void 0) {
        obj.unknown0 = Math.round(message.unknown0);
      }
      if (message.translationId !== "") {
        obj.translationId = message.translationId;
      }
      if (message.language !== void 0) {
        obj.language = message.language;
      }
      if (message.message !== void 0) {
        obj.message = message.message;
      }
      if (message.isLivelyVoice !== false) {
        obj.isLivelyVoice = message.isLivelyVoice;
      }
      if (message.unknown2 !== void 0) {
        obj.unknown2 = Math.round(message.unknown2);
      }
      if (message.shouldRetry !== void 0) {
        obj.shouldRetry = Math.round(message.shouldRetry);
      }
      if (message.unknown3 !== void 0) {
        obj.unknown3 = Math.round(message.unknown3);
      }
      return obj;
    },
    create(base) {
      return VideoTranslationResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
      const message = createBaseVideoTranslationResponse();
      message.url = object.url ?? void 0;
      message.duration = object.duration ?? void 0;
      message.status = object.status ?? 0;
      message.remainingTime = object.remainingTime ?? void 0;
      message.unknown0 = object.unknown0 ?? void 0;
      message.translationId = object.translationId ?? "";
      message.language = object.language ?? void 0;
      message.message = object.message ?? void 0;
      message.isLivelyVoice = object.isLivelyVoice ?? false;
      message.unknown2 = object.unknown2 ?? void 0;
      message.shouldRetry = object.shouldRetry ?? void 0;
      message.unknown3 = object.unknown3 ?? void 0;
      return message;
    }
  };
  function createBaseVideoTranslationCacheItem() {
    return { status: 0, remainingTime: void 0, message: void 0, unknown0: void 0 };
  }
  const VideoTranslationCacheItem = {
    encode(message, writer = new BinaryWriter()) {
      if (message.status !== 0) {
        writer.uint32(8).int32(message.status);
      }
      if (message.remainingTime !== void 0) {
        writer.uint32(16).int32(message.remainingTime);
      }
      if (message.message !== void 0) {
        writer.uint32(26).string(message.message);
      }
      if (message.unknown0 !== void 0) {
        writer.uint32(32).int32(message.unknown0);
      }
      return writer;
    },
    decode(input, length) {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      let end = length === void 0 ? reader.len : reader.pos + length;
      const message = createBaseVideoTranslationCacheItem();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }
            message.status = reader.int32();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }
            message.remainingTime = reader.int32();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }
            message.message = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }
            message.unknown0 = reader.int32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },
    fromJSON(object) {
      return {
        status: isSet(object.status) ? globalThis.Number(object.status) : 0,
        remainingTime: isSet(object.remainingTime) ? globalThis.Number(object.remainingTime) : void 0,
        message: isSet(object.message) ? globalThis.String(object.message) : void 0,
        unknown0: isSet(object.unknown0) ? globalThis.Number(object.unknown0) : void 0
      };
    },
    toJSON(message) {
      const obj = {};
      if (message.status !== 0) {
        obj.status = Math.round(message.status);
      }
      if (message.remainingTime !== void 0) {
        obj.remainingTime = Math.round(message.remainingTime);
      }
      if (message.message !== void 0) {
        obj.message = message.message;
      }
      if (message.unknown0 !== void 0) {
        obj.unknown0 = Math.round(message.unknown0);
      }
      return obj;
    },
    create(base) {
      return VideoTranslationCacheItem.fromPartial(base ?? {});
    },
    fromPartial(object) {
      const message = createBaseVideoTranslationCacheItem();
      message.status = object.status ?? 0;
      message.remainingTime = object.remainingTime ?? void 0;
      message.message = object.message ?? void 0;
      message.unknown0 = object.unknown0 ?? void 0;
      return message;
    }
  };
  function createBaseVideoTranslationCacheRequest() {
    return { url: "", duration: 0, language: "", responseLanguage: "" };
  }
  const VideoTranslationCacheRequest = {
    encode(message, writer = new BinaryWriter()) {
      if (message.url !== "") {
        writer.uint32(10).string(message.url);
      }
      if (message.duration !== 0) {
        writer.uint32(17).double(message.duration);
      }
      if (message.language !== "") {
        writer.uint32(26).string(message.language);
      }
      if (message.responseLanguage !== "") {
        writer.uint32(34).string(message.responseLanguage);
      }
      return writer;
    },
    decode(input, length) {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      let end = length === void 0 ? reader.len : reader.pos + length;
      const message = createBaseVideoTranslationCacheRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }
            message.url = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 17) {
              break;
            }
            message.duration = reader.double();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }
            message.language = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }
            message.responseLanguage = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },
    fromJSON(object) {
      return {
        url: isSet(object.url) ? globalThis.String(object.url) : "",
        duration: isSet(object.duration) ? globalThis.Number(object.duration) : 0,
        language: isSet(object.language) ? globalThis.String(object.language) : "",
        responseLanguage: isSet(object.responseLanguage) ? globalThis.String(object.responseLanguage) : ""
      };
    },
    toJSON(message) {
      const obj = {};
      if (message.url !== "") {
        obj.url = message.url;
      }
      if (message.duration !== 0) {
        obj.duration = message.duration;
      }
      if (message.language !== "") {
        obj.language = message.language;
      }
      if (message.responseLanguage !== "") {
        obj.responseLanguage = message.responseLanguage;
      }
      return obj;
    },
    create(base) {
      return VideoTranslationCacheRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
      const message = createBaseVideoTranslationCacheRequest();
      message.url = object.url ?? "";
      message.duration = object.duration ?? 0;
      message.language = object.language ?? "";
      message.responseLanguage = object.responseLanguage ?? "";
      return message;
    }
  };
  function createBaseVideoTranslationCacheResponse() {
    return { default: void 0, cloning: void 0 };
  }
  const VideoTranslationCacheResponse = {
    encode(message, writer = new BinaryWriter()) {
      if (message.default !== void 0) {
        VideoTranslationCacheItem.encode(message.default, writer.uint32(10).fork()).join();
      }
      if (message.cloning !== void 0) {
        VideoTranslationCacheItem.encode(message.cloning, writer.uint32(18).fork()).join();
      }
      return writer;
    },
    decode(input, length) {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      let end = length === void 0 ? reader.len : reader.pos + length;
      const message = createBaseVideoTranslationCacheResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }
            message.default = VideoTranslationCacheItem.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }
            message.cloning = VideoTranslationCacheItem.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },
    fromJSON(object) {
      return {
        default: isSet(object.default) ? VideoTranslationCacheItem.fromJSON(object.default) : void 0,
        cloning: isSet(object.cloning) ? VideoTranslationCacheItem.fromJSON(object.cloning) : void 0
      };
    },
    toJSON(message) {
      const obj = {};
      if (message.default !== void 0) {
        obj.default = VideoTranslationCacheItem.toJSON(message.default);
      }
      if (message.cloning !== void 0) {
        obj.cloning = VideoTranslationCacheItem.toJSON(message.cloning);
      }
      return obj;
    },
    create(base) {
      return VideoTranslationCacheResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
      const message = createBaseVideoTranslationCacheResponse();
      message.default = object.default !== void 0 && object.default !== null ? VideoTranslationCacheItem.fromPartial(object.default) : void 0;
      message.cloning = object.cloning !== void 0 && object.cloning !== null ? VideoTranslationCacheItem.fromPartial(object.cloning) : void 0;
      return message;
    }
  };
  function createBaseAudioBufferObject() {
    return { audioFile: new Uint8Array(0), fileId: "" };
  }
  const AudioBufferObject = {
    encode(message, writer = new BinaryWriter()) {
      if (message.audioFile.length !== 0) {
        writer.uint32(18).bytes(message.audioFile);
      }
      if (message.fileId !== "") {
        writer.uint32(10).string(message.fileId);
      }
      return writer;
    },
    decode(input, length) {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      let end = length === void 0 ? reader.len : reader.pos + length;
      const message = createBaseAudioBufferObject();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 2: {
            if (tag !== 18) {
              break;
            }
            message.audioFile = reader.bytes();
            continue;
          }
          case 1: {
            if (tag !== 10) {
              break;
            }
            message.fileId = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },
    fromJSON(object) {
      return {
        audioFile: isSet(object.audioFile) ? bytesFromBase64(object.audioFile) : new Uint8Array(0),
        fileId: isSet(object.fileId) ? globalThis.String(object.fileId) : ""
      };
    },
    toJSON(message) {
      const obj = {};
      if (message.audioFile.length !== 0) {
        obj.audioFile = base64FromBytes(message.audioFile);
      }
      if (message.fileId !== "") {
        obj.fileId = message.fileId;
      }
      return obj;
    },
    create(base) {
      return AudioBufferObject.fromPartial(base ?? {});
    },
    fromPartial(object) {
      const message = createBaseAudioBufferObject();
      message.audioFile = object.audioFile ?? new Uint8Array(0);
      message.fileId = object.fileId ?? "";
      return message;
    }
  };
  function createBasePartialAudioBufferObject() {
    return { audioFile: new Uint8Array(0), chunkId: 0 };
  }
  const PartialAudioBufferObject = {
    encode(message, writer = new BinaryWriter()) {
      if (message.audioFile.length !== 0) {
        writer.uint32(18).bytes(message.audioFile);
      }
      if (message.chunkId !== 0) {
        writer.uint32(8).int32(message.chunkId);
      }
      return writer;
    },
    decode(input, length) {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      let end = length === void 0 ? reader.len : reader.pos + length;
      const message = createBasePartialAudioBufferObject();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 2: {
            if (tag !== 18) {
              break;
            }
            message.audioFile = reader.bytes();
            continue;
          }
          case 1: {
            if (tag !== 8) {
              break;
            }
            message.chunkId = reader.int32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },
    fromJSON(object) {
      return {
        audioFile: isSet(object.audioFile) ? bytesFromBase64(object.audioFile) : new Uint8Array(0),
        chunkId: isSet(object.chunkId) ? globalThis.Number(object.chunkId) : 0
      };
    },
    toJSON(message) {
      const obj = {};
      if (message.audioFile.length !== 0) {
        obj.audioFile = base64FromBytes(message.audioFile);
      }
      if (message.chunkId !== 0) {
        obj.chunkId = Math.round(message.chunkId);
      }
      return obj;
    },
    create(base) {
      return PartialAudioBufferObject.fromPartial(base ?? {});
    },
    fromPartial(object) {
      const message = createBasePartialAudioBufferObject();
      message.audioFile = object.audioFile ?? new Uint8Array(0);
      message.chunkId = object.chunkId ?? 0;
      return message;
    }
  };
  function createBaseChunkAudioObject() {
    return { audioBuffer: void 0, audioPartsLength: 0, fileId: "", version: 0 };
  }
  const ChunkAudioObject = {
    encode(message, writer = new BinaryWriter()) {
      if (message.audioBuffer !== void 0) {
        PartialAudioBufferObject.encode(message.audioBuffer, writer.uint32(10).fork()).join();
      }
      if (message.audioPartsLength !== 0) {
        writer.uint32(16).int32(message.audioPartsLength);
      }
      if (message.fileId !== "") {
        writer.uint32(26).string(message.fileId);
      }
      if (message.version !== 0) {
        writer.uint32(32).int32(message.version);
      }
      return writer;
    },
    decode(input, length) {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      let end = length === void 0 ? reader.len : reader.pos + length;
      const message = createBaseChunkAudioObject();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }
            message.audioBuffer = PartialAudioBufferObject.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }
            message.audioPartsLength = reader.int32();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }
            message.fileId = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }
            message.version = reader.int32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },
    fromJSON(object) {
      return {
        audioBuffer: isSet(object.audioBuffer) ? PartialAudioBufferObject.fromJSON(object.audioBuffer) : void 0,
        audioPartsLength: isSet(object.audioPartsLength) ? globalThis.Number(object.audioPartsLength) : 0,
        fileId: isSet(object.fileId) ? globalThis.String(object.fileId) : "",
        version: isSet(object.version) ? globalThis.Number(object.version) : 0
      };
    },
    toJSON(message) {
      const obj = {};
      if (message.audioBuffer !== void 0) {
        obj.audioBuffer = PartialAudioBufferObject.toJSON(message.audioBuffer);
      }
      if (message.audioPartsLength !== 0) {
        obj.audioPartsLength = Math.round(message.audioPartsLength);
      }
      if (message.fileId !== "") {
        obj.fileId = message.fileId;
      }
      if (message.version !== 0) {
        obj.version = Math.round(message.version);
      }
      return obj;
    },
    create(base) {
      return ChunkAudioObject.fromPartial(base ?? {});
    },
    fromPartial(object) {
      const message = createBaseChunkAudioObject();
      message.audioBuffer = object.audioBuffer !== void 0 && object.audioBuffer !== null ? PartialAudioBufferObject.fromPartial(object.audioBuffer) : void 0;
      message.audioPartsLength = object.audioPartsLength ?? 0;
      message.fileId = object.fileId ?? "";
      message.version = object.version ?? 0;
      return message;
    }
  };
  function createBaseVideoTranslationAudioRequest() {
    return { translationId: "", url: "", partialAudioInfo: void 0, audioInfo: void 0 };
  }
  const VideoTranslationAudioRequest = {
    encode(message, writer = new BinaryWriter()) {
      if (message.translationId !== "") {
        writer.uint32(10).string(message.translationId);
      }
      if (message.url !== "") {
        writer.uint32(18).string(message.url);
      }
      if (message.partialAudioInfo !== void 0) {
        ChunkAudioObject.encode(message.partialAudioInfo, writer.uint32(34).fork()).join();
      }
      if (message.audioInfo !== void 0) {
        AudioBufferObject.encode(message.audioInfo, writer.uint32(50).fork()).join();
      }
      return writer;
    },
    decode(input, length) {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      let end = length === void 0 ? reader.len : reader.pos + length;
      const message = createBaseVideoTranslationAudioRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }
            message.translationId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }
            message.url = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }
            message.partialAudioInfo = ChunkAudioObject.decode(reader, reader.uint32());
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }
            message.audioInfo = AudioBufferObject.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },
    fromJSON(object) {
      return {
        translationId: isSet(object.translationId) ? globalThis.String(object.translationId) : "",
        url: isSet(object.url) ? globalThis.String(object.url) : "",
        partialAudioInfo: isSet(object.partialAudioInfo) ? ChunkAudioObject.fromJSON(object.partialAudioInfo) : void 0,
        audioInfo: isSet(object.audioInfo) ? AudioBufferObject.fromJSON(object.audioInfo) : void 0
      };
    },
    toJSON(message) {
      const obj = {};
      if (message.translationId !== "") {
        obj.translationId = message.translationId;
      }
      if (message.url !== "") {
        obj.url = message.url;
      }
      if (message.partialAudioInfo !== void 0) {
        obj.partialAudioInfo = ChunkAudioObject.toJSON(message.partialAudioInfo);
      }
      if (message.audioInfo !== void 0) {
        obj.audioInfo = AudioBufferObject.toJSON(message.audioInfo);
      }
      return obj;
    },
    create(base) {
      return VideoTranslationAudioRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
      const message = createBaseVideoTranslationAudioRequest();
      message.translationId = object.translationId ?? "";
      message.url = object.url ?? "";
      message.partialAudioInfo = object.partialAudioInfo !== void 0 && object.partialAudioInfo !== null ? ChunkAudioObject.fromPartial(object.partialAudioInfo) : void 0;
      message.audioInfo = object.audioInfo !== void 0 && object.audioInfo !== null ? AudioBufferObject.fromPartial(object.audioInfo) : void 0;
      return message;
    }
  };
  function createBaseVideoTranslationAudioResponse() {
    return { status: 0, remainingChunks: [] };
  }
  const VideoTranslationAudioResponse = {
    encode(message, writer = new BinaryWriter()) {
      if (message.status !== 0) {
        writer.uint32(8).int32(message.status);
      }
      for (const v2 of message.remainingChunks) {
        writer.uint32(18).string(v2);
      }
      return writer;
    },
    decode(input, length) {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      let end = length === void 0 ? reader.len : reader.pos + length;
      const message = createBaseVideoTranslationAudioResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }
            message.status = reader.int32();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }
            message.remainingChunks.push(reader.string());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },
    fromJSON(object) {
      return {
        status: isSet(object.status) ? globalThis.Number(object.status) : 0,
        remainingChunks: globalThis.Array.isArray(object?.remainingChunks) ? object.remainingChunks.map((e2) => globalThis.String(e2)) : []
      };
    },
    toJSON(message) {
      const obj = {};
      if (message.status !== 0) {
        obj.status = Math.round(message.status);
      }
      if (message.remainingChunks?.length) {
        obj.remainingChunks = message.remainingChunks;
      }
      return obj;
    },
    create(base) {
      return VideoTranslationAudioResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
      const message = createBaseVideoTranslationAudioResponse();
      message.status = object.status ?? 0;
      message.remainingChunks = object.remainingChunks?.map((e2) => e2) || [];
      return message;
    }
  };
  function createBaseSubtitlesObject() {
    return { language: "", url: "", unknown0: 0, translatedLanguage: "", translatedUrl: "", unknown1: 0, unknown2: 0 };
  }
  const SubtitlesObject = {
    encode(message, writer = new BinaryWriter()) {
      if (message.language !== "") {
        writer.uint32(10).string(message.language);
      }
      if (message.url !== "") {
        writer.uint32(18).string(message.url);
      }
      if (message.unknown0 !== 0) {
        writer.uint32(24).int32(message.unknown0);
      }
      if (message.translatedLanguage !== "") {
        writer.uint32(34).string(message.translatedLanguage);
      }
      if (message.translatedUrl !== "") {
        writer.uint32(42).string(message.translatedUrl);
      }
      if (message.unknown1 !== 0) {
        writer.uint32(48).int32(message.unknown1);
      }
      if (message.unknown2 !== 0) {
        writer.uint32(56).int32(message.unknown2);
      }
      return writer;
    },
    decode(input, length) {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      let end = length === void 0 ? reader.len : reader.pos + length;
      const message = createBaseSubtitlesObject();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }
            message.language = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }
            message.url = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }
            message.unknown0 = reader.int32();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }
            message.translatedLanguage = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }
            message.translatedUrl = reader.string();
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }
            message.unknown1 = reader.int32();
            continue;
          }
          case 7: {
            if (tag !== 56) {
              break;
            }
            message.unknown2 = reader.int32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },
    fromJSON(object) {
      return {
        language: isSet(object.language) ? globalThis.String(object.language) : "",
        url: isSet(object.url) ? globalThis.String(object.url) : "",
        unknown0: isSet(object.unknown0) ? globalThis.Number(object.unknown0) : 0,
        translatedLanguage: isSet(object.translatedLanguage) ? globalThis.String(object.translatedLanguage) : "",
        translatedUrl: isSet(object.translatedUrl) ? globalThis.String(object.translatedUrl) : "",
        unknown1: isSet(object.unknown1) ? globalThis.Number(object.unknown1) : 0,
        unknown2: isSet(object.unknown2) ? globalThis.Number(object.unknown2) : 0
      };
    },
    toJSON(message) {
      const obj = {};
      if (message.language !== "") {
        obj.language = message.language;
      }
      if (message.url !== "") {
        obj.url = message.url;
      }
      if (message.unknown0 !== 0) {
        obj.unknown0 = Math.round(message.unknown0);
      }
      if (message.translatedLanguage !== "") {
        obj.translatedLanguage = message.translatedLanguage;
      }
      if (message.translatedUrl !== "") {
        obj.translatedUrl = message.translatedUrl;
      }
      if (message.unknown1 !== 0) {
        obj.unknown1 = Math.round(message.unknown1);
      }
      if (message.unknown2 !== 0) {
        obj.unknown2 = Math.round(message.unknown2);
      }
      return obj;
    },
    create(base) {
      return SubtitlesObject.fromPartial(base ?? {});
    },
    fromPartial(object) {
      const message = createBaseSubtitlesObject();
      message.language = object.language ?? "";
      message.url = object.url ?? "";
      message.unknown0 = object.unknown0 ?? 0;
      message.translatedLanguage = object.translatedLanguage ?? "";
      message.translatedUrl = object.translatedUrl ?? "";
      message.unknown1 = object.unknown1 ?? 0;
      message.unknown2 = object.unknown2 ?? 0;
      return message;
    }
  };
  function createBaseSubtitlesRequest() {
    return { url: "", language: "" };
  }
  const SubtitlesRequest = {
    encode(message, writer = new BinaryWriter()) {
      if (message.url !== "") {
        writer.uint32(10).string(message.url);
      }
      if (message.language !== "") {
        writer.uint32(18).string(message.language);
      }
      return writer;
    },
    decode(input, length) {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      let end = length === void 0 ? reader.len : reader.pos + length;
      const message = createBaseSubtitlesRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }
            message.url = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }
            message.language = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },
    fromJSON(object) {
      return {
        url: isSet(object.url) ? globalThis.String(object.url) : "",
        language: isSet(object.language) ? globalThis.String(object.language) : ""
      };
    },
    toJSON(message) {
      const obj = {};
      if (message.url !== "") {
        obj.url = message.url;
      }
      if (message.language !== "") {
        obj.language = message.language;
      }
      return obj;
    },
    create(base) {
      return SubtitlesRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
      const message = createBaseSubtitlesRequest();
      message.url = object.url ?? "";
      message.language = object.language ?? "";
      return message;
    }
  };
  function createBaseSubtitlesResponse() {
    return { waiting: false, subtitles: [] };
  }
  const SubtitlesResponse = {
    encode(message, writer = new BinaryWriter()) {
      if (message.waiting !== false) {
        writer.uint32(8).bool(message.waiting);
      }
      for (const v2 of message.subtitles) {
        SubtitlesObject.encode(v2, writer.uint32(18).fork()).join();
      }
      return writer;
    },
    decode(input, length) {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      let end = length === void 0 ? reader.len : reader.pos + length;
      const message = createBaseSubtitlesResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }
            message.waiting = reader.bool();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }
            message.subtitles.push(SubtitlesObject.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },
    fromJSON(object) {
      return {
        waiting: isSet(object.waiting) ? globalThis.Boolean(object.waiting) : false,
        subtitles: globalThis.Array.isArray(object?.subtitles) ? object.subtitles.map((e2) => SubtitlesObject.fromJSON(e2)) : []
      };
    },
    toJSON(message) {
      const obj = {};
      if (message.waiting !== false) {
        obj.waiting = message.waiting;
      }
      if (message.subtitles?.length) {
        obj.subtitles = message.subtitles.map((e2) => SubtitlesObject.toJSON(e2));
      }
      return obj;
    },
    create(base) {
      return SubtitlesResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
      const message = createBaseSubtitlesResponse();
      message.waiting = object.waiting ?? false;
      message.subtitles = object.subtitles?.map((e2) => SubtitlesObject.fromPartial(e2)) || [];
      return message;
    }
  };
  function createBaseStreamTranslationObject() {
    return { url: "", timestamp: "" };
  }
  const StreamTranslationObject = {
    encode(message, writer = new BinaryWriter()) {
      if (message.url !== "") {
        writer.uint32(10).string(message.url);
      }
      if (message.timestamp !== "") {
        writer.uint32(18).string(message.timestamp);
      }
      return writer;
    },
    decode(input, length) {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      let end = length === void 0 ? reader.len : reader.pos + length;
      const message = createBaseStreamTranslationObject();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }
            message.url = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }
            message.timestamp = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },
    fromJSON(object) {
      return {
        url: isSet(object.url) ? globalThis.String(object.url) : "",
        timestamp: isSet(object.timestamp) ? globalThis.String(object.timestamp) : ""
      };
    },
    toJSON(message) {
      const obj = {};
      if (message.url !== "") {
        obj.url = message.url;
      }
      if (message.timestamp !== "") {
        obj.timestamp = message.timestamp;
      }
      return obj;
    },
    create(base) {
      return StreamTranslationObject.fromPartial(base ?? {});
    },
    fromPartial(object) {
      const message = createBaseStreamTranslationObject();
      message.url = object.url ?? "";
      message.timestamp = object.timestamp ?? "";
      return message;
    }
  };
  function createBaseStreamTranslationRequest() {
    return { url: "", language: "", responseLanguage: "", unknown0: 0, unknown1: 0 };
  }
  const StreamTranslationRequest = {
    encode(message, writer = new BinaryWriter()) {
      if (message.url !== "") {
        writer.uint32(10).string(message.url);
      }
      if (message.language !== "") {
        writer.uint32(18).string(message.language);
      }
      if (message.responseLanguage !== "") {
        writer.uint32(26).string(message.responseLanguage);
      }
      if (message.unknown0 !== 0) {
        writer.uint32(40).int32(message.unknown0);
      }
      if (message.unknown1 !== 0) {
        writer.uint32(48).int32(message.unknown1);
      }
      return writer;
    },
    decode(input, length) {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      let end = length === void 0 ? reader.len : reader.pos + length;
      const message = createBaseStreamTranslationRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }
            message.url = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }
            message.language = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }
            message.responseLanguage = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }
            message.unknown0 = reader.int32();
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }
            message.unknown1 = reader.int32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },
    fromJSON(object) {
      return {
        url: isSet(object.url) ? globalThis.String(object.url) : "",
        language: isSet(object.language) ? globalThis.String(object.language) : "",
        responseLanguage: isSet(object.responseLanguage) ? globalThis.String(object.responseLanguage) : "",
        unknown0: isSet(object.unknown0) ? globalThis.Number(object.unknown0) : 0,
        unknown1: isSet(object.unknown1) ? globalThis.Number(object.unknown1) : 0
      };
    },
    toJSON(message) {
      const obj = {};
      if (message.url !== "") {
        obj.url = message.url;
      }
      if (message.language !== "") {
        obj.language = message.language;
      }
      if (message.responseLanguage !== "") {
        obj.responseLanguage = message.responseLanguage;
      }
      if (message.unknown0 !== 0) {
        obj.unknown0 = Math.round(message.unknown0);
      }
      if (message.unknown1 !== 0) {
        obj.unknown1 = Math.round(message.unknown1);
      }
      return obj;
    },
    create(base) {
      return StreamTranslationRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
      const message = createBaseStreamTranslationRequest();
      message.url = object.url ?? "";
      message.language = object.language ?? "";
      message.responseLanguage = object.responseLanguage ?? "";
      message.unknown0 = object.unknown0 ?? 0;
      message.unknown1 = object.unknown1 ?? 0;
      return message;
    }
  };
  function createBaseStreamTranslationResponse() {
    return { interval: 0, translatedInfo: void 0, pingId: void 0 };
  }
  const StreamTranslationResponse = {
    encode(message, writer = new BinaryWriter()) {
      if (message.interval !== 0) {
        writer.uint32(8).int32(message.interval);
      }
      if (message.translatedInfo !== void 0) {
        StreamTranslationObject.encode(message.translatedInfo, writer.uint32(18).fork()).join();
      }
      if (message.pingId !== void 0) {
        writer.uint32(24).int32(message.pingId);
      }
      return writer;
    },
    decode(input, length) {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      let end = length === void 0 ? reader.len : reader.pos + length;
      const message = createBaseStreamTranslationResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }
            message.interval = reader.int32();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }
            message.translatedInfo = StreamTranslationObject.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }
            message.pingId = reader.int32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },
    fromJSON(object) {
      return {
        interval: isSet(object.interval) ? streamIntervalFromJSON(object.interval) : 0,
        translatedInfo: isSet(object.translatedInfo) ? StreamTranslationObject.fromJSON(object.translatedInfo) : void 0,
        pingId: isSet(object.pingId) ? globalThis.Number(object.pingId) : void 0
      };
    },
    toJSON(message) {
      const obj = {};
      if (message.interval !== 0) {
        obj.interval = streamIntervalToJSON(message.interval);
      }
      if (message.translatedInfo !== void 0) {
        obj.translatedInfo = StreamTranslationObject.toJSON(message.translatedInfo);
      }
      if (message.pingId !== void 0) {
        obj.pingId = Math.round(message.pingId);
      }
      return obj;
    },
    create(base) {
      return StreamTranslationResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
      const message = createBaseStreamTranslationResponse();
      message.interval = object.interval ?? 0;
      message.translatedInfo = object.translatedInfo !== void 0 && object.translatedInfo !== null ? StreamTranslationObject.fromPartial(object.translatedInfo) : void 0;
      message.pingId = object.pingId ?? void 0;
      return message;
    }
  };
  function createBaseStreamPingRequest() {
    return { pingId: 0 };
  }
  const StreamPingRequest = {
    encode(message, writer = new BinaryWriter()) {
      if (message.pingId !== 0) {
        writer.uint32(8).int32(message.pingId);
      }
      return writer;
    },
    decode(input, length) {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      let end = length === void 0 ? reader.len : reader.pos + length;
      const message = createBaseStreamPingRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }
            message.pingId = reader.int32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },
    fromJSON(object) {
      return { pingId: isSet(object.pingId) ? globalThis.Number(object.pingId) : 0 };
    },
    toJSON(message) {
      const obj = {};
      if (message.pingId !== 0) {
        obj.pingId = Math.round(message.pingId);
      }
      return obj;
    },
    create(base) {
      return StreamPingRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
      const message = createBaseStreamPingRequest();
      message.pingId = object.pingId ?? 0;
      return message;
    }
  };
  function createBaseYandexSessionRequest() {
    return { uuid: "", module: "" };
  }
  const YandexSessionRequest = {
    encode(message, writer = new BinaryWriter()) {
      if (message.uuid !== "") {
        writer.uint32(10).string(message.uuid);
      }
      if (message.module !== "") {
        writer.uint32(18).string(message.module);
      }
      return writer;
    },
    decode(input, length) {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      let end = length === void 0 ? reader.len : reader.pos + length;
      const message = createBaseYandexSessionRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }
            message.uuid = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }
            message.module = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },
    fromJSON(object) {
      return {
        uuid: isSet(object.uuid) ? globalThis.String(object.uuid) : "",
        module: isSet(object.module) ? globalThis.String(object.module) : ""
      };
    },
    toJSON(message) {
      const obj = {};
      if (message.uuid !== "") {
        obj.uuid = message.uuid;
      }
      if (message.module !== "") {
        obj.module = message.module;
      }
      return obj;
    },
    create(base) {
      return YandexSessionRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
      const message = createBaseYandexSessionRequest();
      message.uuid = object.uuid ?? "";
      message.module = object.module ?? "";
      return message;
    }
  };
  function createBaseYandexSessionResponse() {
    return { secretKey: "", expires: 0 };
  }
  const YandexSessionResponse = {
    encode(message, writer = new BinaryWriter()) {
      if (message.secretKey !== "") {
        writer.uint32(10).string(message.secretKey);
      }
      if (message.expires !== 0) {
        writer.uint32(16).int32(message.expires);
      }
      return writer;
    },
    decode(input, length) {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      let end = length === void 0 ? reader.len : reader.pos + length;
      const message = createBaseYandexSessionResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }
            message.secretKey = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }
            message.expires = reader.int32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },
    fromJSON(object) {
      return {
        secretKey: isSet(object.secretKey) ? globalThis.String(object.secretKey) : "",
        expires: isSet(object.expires) ? globalThis.Number(object.expires) : 0
      };
    },
    toJSON(message) {
      const obj = {};
      if (message.secretKey !== "") {
        obj.secretKey = message.secretKey;
      }
      if (message.expires !== 0) {
        obj.expires = Math.round(message.expires);
      }
      return obj;
    },
    create(base) {
      return YandexSessionResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
      const message = createBaseYandexSessionResponse();
      message.secretKey = object.secretKey ?? "";
      message.expires = object.expires ?? 0;
      return message;
    }
  };
  function bytesFromBase64(b64) {
    if (globalThis.Buffer) {
      return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
    } else {
      const bin = globalThis.atob(b64);
      const arr = new Uint8Array(bin.length);
      for (let i2 = 0; i2 < bin.length; ++i2) {
        arr[i2] = bin.charCodeAt(i2);
      }
      return arr;
    }
  }
  function base64FromBytes(arr) {
    if (globalThis.Buffer) {
      return globalThis.Buffer.from(arr).toString("base64");
    } else {
      const bin = [];
      arr.forEach((byte) => {
        bin.push(globalThis.String.fromCharCode(byte));
      });
      return globalThis.btoa(bin.join(""));
    }
  }
  function isSet(value) {
    return value !== null && value !== void 0;
  }
  const { componentVersion } = votConfig;
  const utf8Encoder = new TextEncoder();
  function getBrowserCrypto() {
    const cryptoApi = globalThis.crypto ?? (typeof window !== "undefined" ? window.crypto : void 0);
    if (!cryptoApi?.subtle) {
      throw new Error("Web Crypto API is not available in this browser context");
    }
    return cryptoApi;
  }
  async function signHMAC(hashName, hmac, data) {
    const cryptoApi = getBrowserCrypto();
    const key = await cryptoApi.subtle.importKey(
      "raw",
      utf8Encoder.encode(hmac),
      { name: "HMAC", hash: { name: hashName } },
      false,
      ["sign", "verify"]
    );
    return cryptoApi.subtle.sign("HMAC", key, data);
  }
  async function getSignature(body) {
    const signature = await signHMAC("SHA-256", votConfig.hmac, body);
    return new Uint8Array(signature).reduce(
      (result, byte) => result + byte.toString(16).padStart(2, "0"),
      ""
    );
  }
  async function getSecYaHeaders(secType, session, body, path) {
    const { secretKey, uuid } = session;
    const token = `${uuid}:${path}:${componentVersion}`;
    const tokenBody = utf8Encoder.encode(token);
    const tokenSign = await getSignature(tokenBody);
    if (secType === "Ya-Summary") {
      return {
        [`X-${secType}-Sk`]: secretKey,
        [`X-${secType}-Token`]: `${tokenSign}:${token}`
      };
    }
    const sign = await getSignature(body);
    return {
      [`${secType}-Signature`]: sign,
      [`Sec-${secType}-Sk`]: secretKey,
      [`Sec-${secType}-Token`]: `${tokenSign}:${token}`
    };
  }
  function getUUID() {
    const hexDigits = "0123456789ABCDEF";
    let uuid = "";
    for (let index = 0; index < 32; index += 1) {
      uuid += hexDigits[Math.floor(Math.random() * 16)];
    }
    return uuid;
  }
  async function getHmacSha1(hmacKey, salt) {
    try {
      const hmacSalt = utf8Encoder.encode(salt);
      const signature = await signHMAC("SHA-1", hmacKey, hmacSalt);
      return btoa(String.fromCharCode(...new Uint8Array(signature)));
    } catch (error2) {
      Logger.error(error2);
      return false;
    }
  }
  const browserSecHeaders = {
    "sec-ch-ua": `"Chromium";v="134", "YaBrowser";v="${componentVersion.slice(0, 5)}", "Not?A_Brand";v="24", "Yowser";v="2.5"`,
    "sec-ch-ua-full-version-list": `"Chromium";v="134.0.6998.543", "YaBrowser";v="${componentVersion}", "Not?A_Brand";v="24.0.0.0", "Yowser";v="2.5"`,
    "Sec-Fetch-Mode": "no-cors"
  };
  const iso6392to6391 = {
    afr: "af",
    aka: "ak",
    alb: "sq",
    amh: "am",
    ara: "ar",
    arm: "hy",
    asm: "as",
    aym: "ay",
    aze: "az",
    baq: "eu",
    bel: "be",
    ben: "bn",
    bos: "bs",
    bul: "bg",
    bur: "my",
    cat: "ca",
    chi: "zh",
    cos: "co",
    cze: "cs",
    dan: "da",
    div: "dv",
    dut: "nl",
    eng: "en",
    epo: "eo",
    est: "et",
    ewe: "ee",
    fin: "fi",
    fre: "fr",
    fry: "fy",
    geo: "ka",
    ger: "de",
    gla: "gd",
    gle: "ga",
    glg: "gl",
    gre: "el",
    grn: "gn",
    guj: "gu",
    hat: "ht",
    hau: "ha",
    hin: "hi",
    hrv: "hr",
    hun: "hu",
    ibo: "ig",
    ice: "is",
    ind: "id",
    ita: "it",
    jav: "jv",
    jpn: "ja",
    kan: "kn",
    kaz: "kk",
    khm: "km",
    kin: "rw",
    kir: "ky",
    kor: "ko",
    kur: "ku",
    lao: "lo",
    lat: "la",
    lav: "lv",
    lin: "ln",
    lit: "lt",
    ltz: "lb",
    lug: "lg",
    mac: "mk",
    mal: "ml",
    mao: "mi",
    mar: "mr",
    may: "ms",
    mlg: "mg",
    mlt: "mt",
    mon: "mn",
    nep: "ne",
    nor: "no",
    nya: "ny",
    ori: "or",
    orm: "om",
    pan: "pa",
    per: "fa",
    pol: "pl",
    por: "pt",
    pus: "ps",
    que: "qu",
    rum: "ro",
    rus: "ru",
    san: "sa",
    sin: "si",
    slo: "sk",
    slv: "sl",
    smo: "sm",
    sna: "sn",
    snd: "sd",
    som: "so",
    sot: "st",
    spa: "es",
    srp: "sr",
    sun: "su",
    swa: "sw",
    swe: "sv",
    tam: "ta",
    tat: "tt",
    tel: "te",
    tgk: "tg",
    tha: "th",
    tir: "ti",
    tso: "ts",
    tuk: "tk",
    tur: "tr",
    uig: "ug",
    ukr: "uk",
    urd: "ur",
    uzb: "uz",
    vie: "vi",
    wel: "cy",
    xho: "xh",
    yid: "yi",
    yor: "yo",
    zul: "zu"
  };
  async function fetchWithTimeout(url, options = {
    headers: {
      "User-Agent": votConfig.userAgent
    }
  }) {
    const { timeout: timeout2 = 3e3, ...fetchOptions } = options;
    const controller = new AbortController();
    const timeoutId = setTimeout(() => controller.abort(), timeout2);
    const response = await fetch(url, {
      signal: controller.signal,
      ...fetchOptions
    });
    clearTimeout(timeoutId);
    return response;
  }
  function getTimestamp$1() {
    return Math.floor(Date.now() / 1e3);
  }
  function normalizeLang$1(lang2) {
    if (lang2.length === 3) {
      return iso6392to6391[lang2];
    }
    return lang2.toLowerCase().split(/[_;-]/)[0].trim();
  }
  function proxyMedia(url, format = "mp4") {
    const generalUrl = `https://${votConfig.mediaProxy}/v1/proxy/video.${format}?format=base64&force=true`;
    if (!(url instanceof URL)) {
      return `${generalUrl}&url=${btoa(url)}`;
    }
    return `${generalUrl}&url=${btoa(url.href)}&origin=${url.origin}&referer=${url.origin}`;
  }
  class YandexVOTProtobuf {
    static encodeTranslationRequest(url, duration, requestLang, responseLang2, translationHelp, { forceSourceLang = false, wasStream = false, videoTitle = "", bypassCache = false, useLivelyVoice = false, firstRequest = true } = {}) {
      return VideoTranslationRequest.encode({
        url,
        firstRequest,
        duration,
        unknown0: 1,
        language: requestLang,
        forceSourceLang,
        unknown1: 0,
        translationHelp: translationHelp ?? [],
        responseLanguage: responseLang2,
        wasStream,
        unknown2: 1,
        unknown3: 2,
        bypassCache,
        useLivelyVoice,
        videoTitle
      }).finish();
    }
    static decodeTranslationResponse(response) {
      return VideoTranslationResponse.decode(new Uint8Array(response));
    }
    static encodeTranslationCacheRequest(url, duration, requestLang, responseLang2) {
      return VideoTranslationCacheRequest.encode({
        url,
        duration,
        language: requestLang,
        responseLanguage: responseLang2
      }).finish();
    }
    static decodeTranslationCacheResponse(response) {
      return VideoTranslationCacheResponse.decode(new Uint8Array(response));
    }
    static isPartialAudioBuffer(audioBuffer) {
      return "chunkId" in audioBuffer;
    }
    static encodeTranslationAudioRequest(url, translationId, audioBuffer, partialAudio) {
      if (partialAudio && YandexVOTProtobuf.isPartialAudioBuffer(audioBuffer)) {
        return VideoTranslationAudioRequest.encode({
          url,
          translationId,
          partialAudioInfo: {
            ...partialAudio,
            audioBuffer
          }
        }).finish();
      }
      return VideoTranslationAudioRequest.encode({
        url,
        translationId,
        audioInfo: audioBuffer
      }).finish();
    }
    static decodeTranslationAudioResponse(response) {
      return VideoTranslationAudioResponse.decode(new Uint8Array(response));
    }
    static encodeSubtitlesRequest(url, requestLang) {
      return SubtitlesRequest.encode({
        url,
        language: requestLang
      }).finish();
    }
    static decodeSubtitlesResponse(response) {
      return SubtitlesResponse.decode(new Uint8Array(response));
    }
    static encodeStreamPingRequest(pingId) {
      return StreamPingRequest.encode({
        pingId
      }).finish();
    }
    static encodeStreamRequest(url, requestLang, responseLang2) {
      return StreamTranslationRequest.encode({
        url,
        language: requestLang,
        responseLanguage: responseLang2,
        unknown0: 1,
        unknown1: 0
      }).finish();
    }
    static decodeStreamResponse(response) {
      return StreamTranslationResponse.decode(new Uint8Array(response));
    }
  }
  class YandexSessionProtobuf {
    static encodeSessionRequest(uuid, module) {
      return YandexSessionRequest.encode({
        uuid,
        module
      }).finish();
    }
    static decodeSessionResponse(response) {
      return YandexSessionResponse.decode(new Uint8Array(response));
    }
  }
  var VideoTranslationStatus;
  (function(VideoTranslationStatus2) {
    VideoTranslationStatus2[VideoTranslationStatus2["FAILED"] = 0] = "FAILED";
    VideoTranslationStatus2[VideoTranslationStatus2["FINISHED"] = 1] = "FINISHED";
    VideoTranslationStatus2[VideoTranslationStatus2["WAITING"] = 2] = "WAITING";
    VideoTranslationStatus2[VideoTranslationStatus2["LONG_WAITING"] = 3] = "LONG_WAITING";
    VideoTranslationStatus2[VideoTranslationStatus2["PART_CONTENT"] = 5] = "PART_CONTENT";
    VideoTranslationStatus2[VideoTranslationStatus2["AUDIO_REQUESTED"] = 6] = "AUDIO_REQUESTED";
    VideoTranslationStatus2[VideoTranslationStatus2["SESSION_REQUIRED"] = 7] = "SESSION_REQUIRED";
  })(VideoTranslationStatus || (VideoTranslationStatus = {}));
  var AudioDownloadType;
  (function(AudioDownloadType2) {
    AudioDownloadType2["WEB_API_VIDEO_SRC_FROM_IFRAME"] = "web_api_video_src_from_iframe";
    AudioDownloadType2["WEB_API_VIDEO_SRC"] = "web_api_video_src";
    AudioDownloadType2["WEB_API_GET_ALL_GENERATING_URLS_DATA_FROM_IFRAME"] = "web_api_get_all_generating_urls_data_from_iframe";
    AudioDownloadType2["WEB_API_GET_ALL_GENERATING_URLS_DATA_FROM_IFRAME_TMP_EXP"] = "web_api_get_all_generating_urls_data_from_iframe_tmp_exp";
    AudioDownloadType2["WEB_API_REPLACED_FETCH_INSIDE_IFRAME"] = "web_api_replaced_fetch_inside_iframe";
    AudioDownloadType2["ANDROID_API"] = "android_api";
    AudioDownloadType2["WEB_API_SLOW"] = "web_api_slow";
    AudioDownloadType2["WEB_API_STEAL_SIG_AND_N"] = "web_api_steal_sig_and_n";
    AudioDownloadType2["WEB_API_COMBINED"] = "web_api_get_all_generating_urls_data_from_iframe,web_api_steal_sig_and_n";
  })(AudioDownloadType || (AudioDownloadType = {}));
  var VideoService;
  (function(VideoService2) {
    VideoService2["custom"] = "custom";
    VideoService2["directlink"] = "custom";
    VideoService2["youtube"] = "youtube";
    VideoService2["piped"] = "piped";
    VideoService2["invidious"] = "invidious";
    VideoService2["vk"] = "vk";
    VideoService2["nine_gag"] = "nine_gag";
    VideoService2["gag"] = "nine_gag";
    VideoService2["twitch"] = "twitch";
    VideoService2["proxitok"] = "proxitok";
    VideoService2["tiktok"] = "tiktok";
    VideoService2["vimeo"] = "vimeo";
    VideoService2["xvideos"] = "xvideos";
    VideoService2["pornhub"] = "pornhub";
    VideoService2["twitter"] = "twitter";
    VideoService2["x"] = "twitter";
    VideoService2["rumble"] = "rumble";
    VideoService2["facebook"] = "facebook";
    VideoService2["rutube"] = "rutube";
    VideoService2["coub"] = "coub";
    VideoService2["bilibili"] = "bilibili";
    VideoService2["mail_ru"] = "mailru";
    VideoService2["mailru"] = "mailru";
    VideoService2["bitchute"] = "bitchute";
    VideoService2["eporner"] = "eporner";
    VideoService2["peertube"] = "peertube";
    VideoService2["dailymotion"] = "dailymotion";
    VideoService2["trovo"] = "trovo";
    VideoService2["yandexdisk"] = "yandexdisk";
    VideoService2["ok_ru"] = "okru";
    VideoService2["okru"] = "okru";
    VideoService2["googledrive"] = "googledrive";
    VideoService2["bannedvideo"] = "bannedvideo";
    VideoService2["weverse"] = "weverse";
    VideoService2["newgrounds"] = "newgrounds";
    VideoService2["egghead"] = "egghead";
    VideoService2["youku"] = "youku";
    VideoService2["archive"] = "archive";
    VideoService2["kodik"] = "kodik";
    VideoService2["patreon"] = "patreon";
    VideoService2["reddit"] = "reddit";
    VideoService2["kick"] = "kick";
    VideoService2["apple_developer"] = "apple_developer";
    VideoService2["appledeveloper"] = "apple_developer";
    VideoService2["poketube"] = "poketube";
    VideoService2["epicgames"] = "epicgames";
    VideoService2["odysee"] = "odysee";
    VideoService2["coursehunterLike"] = "coursehunterLike";
    VideoService2["sap"] = "sap";
    VideoService2["watchpornto"] = "watchpornto";
    VideoService2["linkedin"] = "linkedin";
    VideoService2["ricktube"] = "ricktube";
    VideoService2["incestflix"] = "incestflix";
    VideoService2["porntn"] = "porntn";
    VideoService2["dzen"] = "dzen";
    VideoService2["cloudflarestream"] = "cloudflarestream";
    VideoService2["loom"] = "loom";
    VideoService2["rtnews"] = "rtnews";
    VideoService2["bitview"] = "bitview";
    VideoService2["thisvid"] = "thisvid";
    VideoService2["ign"] = "ign";
    VideoService2["bunkr"] = "bunkr";
    VideoService2["imdb"] = "imdb";
    VideoService2["telegram"] = "telegram";
  })(VideoService || (VideoService = {}));
  function convertVOT(service, videoId, url) {
    if (service === VideoService.patreon) {
      return {
        service: "mux",
        videoId: new URL(url).pathname.slice(1)
      };
    }
    return {
      service,
      videoId
    };
  }
  class VOTJSError extends Error {
    data;
    constructor(message, data = void 0) {
      super(message);
      this.data = data;
      this.name = "VOTJSError";
      this.message = message;
    }
  }
  function getResponseHeadersObject(headers) {
    try {
      return Object.fromEntries(headers.entries());
    } catch {
      return void 0;
    }
  }
  function parseJsonText(text) {
    if (!text) {
      return void 0;
    }
    try {
      return JSON.parse(text);
    } catch {
      return void 0;
    }
  }
  async function buildBinaryResponseResult(res) {
    const responseTextPromise = res.clone().text().catch(() => void 0);
    const data = await res.arrayBuffer();
    const responseText = await responseTextPromise;
    return {
      success: res.status === 200,
      status: res.status,
      headers: getResponseHeadersObject(res.headers),
      responseText,
      responseJson: parseJsonText(responseText),
      data
    };
  }
  async function buildJsonResponseResult(res) {
    const responseText = await res.text();
    const responseJson = parseJsonText(responseText);
    return {
      success: res.status === 200,
      status: res.status,
      headers: getResponseHeadersObject(res.headers),
      responseText,
      responseJson,
      data: responseJson ?? responseText
    };
  }
  function buildRequestErrorResult(err) {
    return {
      success: false,
      status: void 0,
      headers: void 0,
      responseText: void 0,
      responseJson: void 0,
      errorCode: err?.name,
      errorMessage: err?.message,
      data: err?.message
    };
  }
  class MinimalClient {
    host;
    schema;
    fetch;
    fetchOpts;
    sessions = {};
    userAgent = votConfig.userAgent;
    headers = {
      "User-Agent": this.userAgent,
      Accept: "application/x-protobuf",
      "Accept-Language": "en",
      "Content-Type": "application/x-protobuf",
      Pragma: "no-cache",
      "Cache-Control": "no-cache"
    };
    hostSchemaRe = /(http(s)?):\/\//;
    constructor({ host = votConfig.host, fetchFn = fetchWithTimeout, fetchOpts = {}, headers = {} } = {}) {
      const schema = this.hostSchemaRe.exec(host)?.[1];
      this.host = schema ? host.replace(`${schema}://`, "") : host;
      this.schema = schema ?? "https";
      this.fetch = fetchFn;
      this.fetchOpts = fetchOpts;
      this.headers = { ...this.headers, ...headers };
    }
    async request(path, body, headers = {}, method = "POST") {
      const options = this.getOpts(new Blob([body]), headers, method);
      try {
        const res = await this.fetch(`${this.schema}://${this.host}${path}`, options);
        return await buildBinaryResponseResult(res);
      } catch (err) {
        return buildRequestErrorResult(err);
      }
    }
    async requestJSON(path, body = null, headers = {}, method = "POST") {
      const options = this.getOpts(body, {
        "Content-Type": "application/json",
        ...headers
      }, method);
      try {
        const res = await this.fetch(`${this.schema}://${this.host}${path}`, options);
        return await buildJsonResponseResult(res);
      } catch (err) {
        return buildRequestErrorResult(err);
      }
    }
    getOpts(body, headers = {}, method = "POST") {
      return {
        method,
        headers: {
          ...this.headers,
          ...headers
        },
        body,
        ...this.fetchOpts
      };
    }
    async getSession(module) {
      const timestamp = getTimestamp$1();
      const session = this.sessions[module];
      if (session && session.timestamp + session.expires > timestamp) {
        return session;
      }
      const { secretKey, expires, uuid } = await this.createSession(module);
      this.sessions[module] = {
        secretKey,
        expires,
        timestamp,
        uuid
      };
      return this.sessions[module];
    }
    async createSession(module) {
      const uuid = getUUID();
      const body = YandexSessionProtobuf.encodeSessionRequest(uuid, module);
      const res = await this.request("/session/create", body, {
        "Vtrans-Signature": await getSignature(body)
      });
      if (!res.success) {
        throw new VOTJSError("Failed to request create session", res);
      }
      const sessionResponse = YandexSessionProtobuf.decodeSessionResponse(res.data);
      return {
        ...sessionResponse,
        uuid
      };
    }
  }
  let VOTClient$1 = class VOTClient extends MinimalClient {
    hostVOT;
    schemaVOT;
    apiToken;
    requestLang;
    responseLang;
    paths = {
      videoTranslation: "/video-translation/translate",
      videoTranslationFailAudio: "/video-translation/fail-audio-js",
      videoTranslationAudio: "/video-translation/audio",
      videoTranslationCache: "/video-translation/cache",
      videoSubtitles: "/video-subtitles/get-subtitles",
      streamPing: "/stream-translation/ping-stream",
      streamTranslation: "/stream-translation/translate-stream"
    };
    isCustomLink(url) {
      return !!(/\.(m3u8|m4(a|v)|mpd)/.exec(url) ?? /^https:\/\/cdn\.qstv\.on\.epicgames\.com/.exec(url));
    }
    headersVOT = {
      "User-Agent": `vot.js/${votConfig.version}`,
      "Content-Type": "application/json",
      Pragma: "no-cache",
      "Cache-Control": "no-cache"
    };
    constructor({ host, hostVOT = votConfig.hostVOT, fetchFn, fetchOpts, requestLang = "en", responseLang: responseLang2 = "ru", apiToken, headers } = {}) {
      super({
        host,
        fetchFn,
        fetchOpts,
        headers
      });
      const schemaVOT = this.hostSchemaRe.exec(hostVOT)?.[1];
      this.hostVOT = schemaVOT ? hostVOT.replace(`${schemaVOT}://`, "") : hostVOT;
      this.schemaVOT = schemaVOT ?? "https";
      this.requestLang = requestLang;
      this.responseLang = responseLang2;
      this.apiToken = apiToken;
    }
    get apiTokenHeader() {
      if (!this.apiToken) {
        return {};
      }
      return {
        Authorization: `OAuth ${this.apiToken}`
      };
    }
    async requestVOT(path, body, headers = {}) {
      const options = this.getOpts(JSON.stringify(body), {
        ...this.headersVOT,
        ...headers
      });
      try {
        const res = await this.fetch(`${this.schemaVOT}://${this.hostVOT}${path}`, options);
        return await buildJsonResponseResult(res);
      } catch (err) {
        return buildRequestErrorResult(err);
      }
    }
    async translateVideoYAImpl({ videoData: videoData2, requestLang = this.requestLang, responseLang: responseLang2 = this.responseLang, translationHelp = null, headers = {}, extraOpts = {}, shouldSendFailedAudio = true }) {
      const { url, duration = votConfig.defaultDuration } = videoData2;
      const session = await this.getSession("video-translation");
      const body = YandexVOTProtobuf.encodeTranslationRequest(url, duration, requestLang, responseLang2, translationHelp, extraOpts);
      const path = this.paths.videoTranslation;
      const vtransHeaders = await getSecYaHeaders("Vtrans", session, body, path);
      const apiTokenHeader = extraOpts.useLivelyVoice ? this.apiTokenHeader : {};
      const res = await this.request(path, body, {
        ...vtransHeaders,
        ...apiTokenHeader,
        ...headers
      });
      if (!res.success) {
        throw new VOTJSError("Failed to request video translation", res);
      }
      const translationData = YandexVOTProtobuf.decodeTranslationResponse(res.data);
      Logger.log("translateVideo", translationData);
      const { status, translationId } = translationData;
      switch (status) {
        case VideoTranslationStatus.FAILED:
          throw new VOTJSError("Yandex couldn't translate video", translationData);
        case VideoTranslationStatus.FINISHED:
        case VideoTranslationStatus.PART_CONTENT:
          if (!translationData.url) {
            throw new VOTJSError("Audio link wasn't received from Yandex response", translationData);
          }
          return {
            translationId,
            translated: true,
            url: translationData.url,
            status,
            remainingTime: translationData.remainingTime ?? -1
          };
        case VideoTranslationStatus.WAITING:
        case VideoTranslationStatus.LONG_WAITING:
          return {
            translationId,
            translated: false,
            status,
            remainingTime: translationData.remainingTime
          };
        case VideoTranslationStatus.AUDIO_REQUESTED:
          if (url.startsWith("https://youtu.be/") && shouldSendFailedAudio) {
            await this.requestVtransFailAudio(url);
            await this.requestVtransAudio(url, translationData.translationId, {
              audioFile: new Uint8Array(),
              fileId: AudioDownloadType.WEB_API_GET_ALL_GENERATING_URLS_DATA_FROM_IFRAME
            });
            return await this.translateVideoYAImpl({
              videoData: videoData2,
              requestLang,
              responseLang: responseLang2,
              translationHelp,
              headers,
              shouldSendFailedAudio: false
            });
          }
          return {
            translationId,
            translated: false,
            status,
            remainingTime: translationData.remainingTime ?? -1
          };
        case VideoTranslationStatus.SESSION_REQUIRED:
          throw new VOTJSError("Yandex auth required to translate video. See docs for more info", translationData);
        default:
          Logger.error("Unknown response", translationData);
          throw new VOTJSError("Unknown response from Yandex", translationData);
      }
    }
    async translateVideoVOTImpl({ url, videoId, service, requestLang = this.requestLang, responseLang: responseLang2 = this.responseLang, headers = {}, provider = "yandex" }) {
      const votData = convertVOT(service, videoId, url);
      const res = await this.requestVOT(this.paths.videoTranslation, {
        provider,
        service: votData.service,
        video_id: votData.videoId,
        from_lang: requestLang,
        to_lang: responseLang2,
        raw_video: url
      }, {
        ...headers
      });
      if (!res.success) {
        throw new VOTJSError("Failed to request video translation", res);
      }
      const translationData = res.data;
      switch (translationData.status) {
        case "failed":
          throw new VOTJSError("Yandex couldn't translate video", translationData);
        case "success":
          if (!translationData.translated_url) {
            throw new VOTJSError("Audio link wasn't received from VOT response", translationData);
          }
          return {
            translationId: String(translationData.id),
            translated: true,
            url: translationData.translated_url,
            status: 1,
            remainingTime: -1
          };
        case "waiting":
          return {
            translationId: "",
            translated: false,
            remainingTime: translationData.remaining_time,
            status: 2,
            message: translationData.message
          };
      }
    }
    async requestVtransFailAudio(url) {
      const res = await this.requestJSON(this.paths.videoTranslationFailAudio, JSON.stringify({
        video_url: url
      }), void 0, "PUT");
      if (!res.data || typeof res.data === "string" || res.data.status !== 1) {
        throw new VOTJSError("Failed to request to fake video translation fail audio js", res);
      }
      return res;
    }
    async requestVtransAudio(url, translationId, audioBuffer, partialAudio, headers = {}) {
      const session = await this.getSession("video-translation");
      const body = YandexVOTProtobuf.isPartialAudioBuffer(audioBuffer) ? YandexVOTProtobuf.encodeTranslationAudioRequest(url, translationId, audioBuffer, partialAudio) : YandexVOTProtobuf.encodeTranslationAudioRequest(url, translationId, audioBuffer, void 0);
      const path = this.paths.videoTranslationAudio;
      const vtransHeaders = await getSecYaHeaders("Vtrans", session, body, path);
      const res = await this.request(path, body, {
        ...vtransHeaders,
        ...headers
      }, "PUT");
      if (!res.success) {
        throw new VOTJSError("Failed to request video translation audio", res);
      }
      return YandexVOTProtobuf.decodeTranslationAudioResponse(res.data);
    }
    async translateVideoCache({ videoData: videoData2, requestLang = this.requestLang, responseLang: responseLang2 = this.responseLang, headers = {} }) {
      const { url, duration = votConfig.defaultDuration } = videoData2;
      const session = await this.getSession("video-translation");
      const body = YandexVOTProtobuf.encodeTranslationCacheRequest(url, duration, requestLang, responseLang2);
      const path = this.paths.videoTranslationCache;
      const vtransHeaders = await getSecYaHeaders("Vtrans", session, body, path);
      const res = await this.request(path, body, {
        ...vtransHeaders,
        ...headers
      }, "POST");
      if (!res.success) {
        throw new VOTJSError("Failed to request video translation cache", res);
      }
      return YandexVOTProtobuf.decodeTranslationCacheResponse(res.data);
    }
    async translateVideo({ videoData: videoData2, requestLang = this.requestLang, responseLang: responseLang2 = this.responseLang, translationHelp = null, headers = {}, extraOpts = {}, shouldSendFailedAudio = true }) {
      const { url, videoId, host } = videoData2;
      return this.isCustomLink(url) ? await this.translateVideoVOTImpl({
        url,
        videoId,
        service: host,
        requestLang,
        responseLang: responseLang2,
        headers,
        provider: extraOpts.useLivelyVoice ? "yandex_lively" : "yandex"
      }) : await this.translateVideoYAImpl({
        videoData: videoData2,
        requestLang,
        responseLang: responseLang2,
        translationHelp,
        headers,
        extraOpts,
        shouldSendFailedAudio
      });
    }
    async getSubtitlesYAImpl({ videoData: videoData2, requestLang = this.requestLang, headers = {} }) {
      const { url } = videoData2;
      const session = await this.getSession("video-translation");
      const body = YandexVOTProtobuf.encodeSubtitlesRequest(url, requestLang);
      const path = this.paths.videoSubtitles;
      const vsubsHeaders = await getSecYaHeaders("Vsubs", session, body, path);
      const res = await this.request(path, body, {
        ...vsubsHeaders,
        ...headers
      });
      if (!res.success) {
        throw new VOTJSError("Failed to request video subtitles", res);
      }
      const subtitlesData = YandexVOTProtobuf.decodeSubtitlesResponse(res.data);
      const subtitles = subtitlesData.subtitles.map((subtitle) => {
        const { language, url: url2, translatedLanguage, translatedUrl } = subtitle;
        return {
          language,
          url: url2,
          translatedLanguage,
          translatedUrl
        };
      });
      return {
        waiting: subtitlesData.waiting,
        subtitles
      };
    }
    async getSubtitlesVOTImpl({ url, videoId, service, headers = {} }) {
      const votData = convertVOT(service, videoId, url);
      const res = await this.requestVOT(this.paths.videoSubtitles, {
        provider: "yandex",
        service: votData.service,
        video_id: votData.videoId
      }, headers);
      if (!res.success) {
        throw new VOTJSError("Failed to request video subtitles", res);
      }
      const subtitlesData = res.data;
      const subtitles = subtitlesData.reduce((result, subtitle) => {
        if (!subtitle.lang_from) {
          return result;
        }
        const originalSubtitle = subtitlesData.find((sub) => sub.lang === subtitle.lang_from);
        if (!originalSubtitle) {
          return result;
        }
        result.push({
          language: originalSubtitle.lang,
          url: originalSubtitle.subtitle_url,
          translatedLanguage: subtitle.lang,
          translatedUrl: subtitle.subtitle_url
        });
        return result;
      }, []);
      return {
        waiting: false,
        subtitles
      };
    }
    async getSubtitles({ videoData: videoData2, requestLang = this.requestLang, headers = {} }) {
      const { url, videoId, host } = videoData2;
      return this.isCustomLink(url) ? await this.getSubtitlesVOTImpl({
        url,
        videoId,
        service: host,
        headers
      }) : await this.getSubtitlesYAImpl({
        videoData: videoData2,
        requestLang,
        headers
      });
    }
    async pingStream({ pingId, headers = {} }) {
      const session = await this.getSession("video-translation");
      const body = YandexVOTProtobuf.encodeStreamPingRequest(pingId);
      const path = this.paths.streamPing;
      const vtransHeaders = await getSecYaHeaders("Vtrans", session, body, path);
      const res = await this.request(path, body, {
        ...vtransHeaders,
        ...headers
      });
      if (!res.success) {
        throw new VOTJSError("Failed to request stream ping", res);
      }
      return true;
    }
    async translateStream({ videoData: videoData2, requestLang = this.requestLang, responseLang: responseLang2 = this.responseLang, headers = {} }) {
      const { url } = videoData2;
      if (this.isCustomLink(url)) {
        throw new VOTJSError("Unsupported video URL for getting stream translation");
      }
      const session = await this.getSession("video-translation");
      const body = YandexVOTProtobuf.encodeStreamRequest(url, requestLang, responseLang2);
      const path = this.paths.streamTranslation;
      const vtransHeaders = await getSecYaHeaders("Vtrans", session, body, path);
      const res = await this.request(path, body, {
        ...vtransHeaders,
        ...headers
      });
      if (!res.success) {
        throw new VOTJSError("Failed to request stream translation", res);
      }
      const translateResponse = YandexVOTProtobuf.decodeStreamResponse(res.data);
      const interval = translateResponse.interval;
      switch (interval) {
        case StreamInterval.NO_CONNECTION:
        case StreamInterval.TRANSLATING:
          return {
            translated: false,
            interval,
            message: interval === StreamInterval.NO_CONNECTION ? "streamNoConnectionToServer" : "translationTakeFewMinutes"
          };
        case StreamInterval.STREAMING: {
          return {
            translated: true,
            interval,
            pingId: translateResponse.pingId,
            result: translateResponse.translatedInfo
          };
        }
        default:
          Logger.error("Unknown response", translateResponse);
          throw new VOTJSError("Unknown response from Yandex", translateResponse);
      }
    }
  };
  let VOTWorkerClient$1 = class VOTWorkerClient extends VOTClient$1 {
    constructor(opts = {}) {
      opts.host = opts.host ?? votConfig.hostWorker;
      super(opts);
    }
    async request(path, body, headers = {}, method = "POST") {
      const options = this.getOpts(JSON.stringify({
        headers: {
          ...this.headers,
          ...headers
        },
        body: Array.from(body)
      }), {
        "Content-Type": "application/json"
      }, method);
      try {
        const res = await this.fetch(`${this.schema}://${this.host}${path}`, options);
        return await buildBinaryResponseResult(res);
      } catch (err) {
        return buildRequestErrorResult(err);
      }
    }
    async requestJSON(path, body = null, headers = {}, method = "POST") {
      const options = this.getOpts(JSON.stringify({
        headers: {
          ...this.headers,
          "Content-Type": "application/json",
          Accept: "application/json",
          ...headers
        },
        body
      }), {
        Accept: "application/json",
        "Content-Type": "application/json"
      }, method);
      try {
        const res = await this.fetch(`${this.schema}://${this.host}${path}`, options);
        return await buildJsonResponseResult(res);
      } catch (err) {
        return buildRequestErrorResult(err);
      }
    }
  };
  class VOTClient2 extends VOTClient$1 {
    constructor(opts) {
      super(opts);
      this.headers = {
        ...browserSecHeaders,
        ...this.headers
      };
    }
  }
  class VOTWorkerClient2 extends VOTWorkerClient$1 {
    constructor(opts) {
      super(opts);
      this.headers = {
        ...browserSecHeaders,
        ...this.headers
      };
    }
  }
  class VideoDataError extends Error {
    constructor(message) {
      super(message);
      this.name = "VideoDataError";
      this.message = message;
    }
  }
  const localLinkRe = /(file:\/\/(\/)?|(http(s)?:\/\/)(127\.0\.0\.1|localhost|192\.168\.(\d){1,3}\.(\d){1,3}))/;
  const sitesInvidious = [
    "yewtu.be",
    "yt.artemislena.eu",
    "invidious.flokinet.to",
    "iv.melmac.space",
    "inv.nadeko.net",
    "inv.tux.pizza",
    "invidious.private.coffee",
    "yt.drgnz.club",
    "vid.puffyan.us",
    "invidious.dhusch.de"
  ];
  const sitesPiped = [
    "piped.video",
    "piped.tokhmi.xyz",
    "piped.moomoo.me",
    "piped.syncpundit.io",
    "piped.mha.fi",
    "watch.whatever.social",
    "piped.garudalinux.org",
    "efy.piped.pages.dev",
    "watch.leptons.xyz",
    "piped.lunar.icu",
    "yt.dc09.ru",
    "piped.mint.lgbt",
    "il.ax",
    "piped.privacy.com.de",
    "piped.esmailelbob.xyz",
    "piped.projectsegfau.lt",
    "piped.in.projectsegfau.lt",
    "piped.us.projectsegfau.lt",
    "piped.privacydev.net",
    "piped.palveluntarjoaja.eu",
    "piped.smnz.de",
    "piped.adminforge.de",
    "piped.qdi.fi",
    "piped.hostux.net",
    "piped.chauvet.pro",
    "piped.jotoma.de",
    "piped.pfcd.me",
    "piped.frontendfriendly.xyz"
  ];
  const sitesProxiTok = [
    "proxitok.pabloferreiro.es",
    "proxitok.pussthecat.org",
    "tok.habedieeh.re",
    "proxitok.esmailelbob.xyz",
    "proxitok.privacydev.net",
    "tok.artemislena.eu",
    "tok.adminforge.de",
    "tt.vern.cc",
    "cringe.whatever.social",
    "proxitok.lunar.icu",
    "proxitok.privacy.com.de"
  ];
  const sitesPeertube = [
    "peertube.1312.media",
    "tube.shanti.cafe",
    "bee-tube.fr",
    "video.sadmin.io",
    "dalek.zone",
    "review.peertube.biz",
    "peervideo.club",
    "tube.la-dina.net",
    "peertube.tmp.rcp.tf",
    "peertube.su",
    "video.blender.org",
    "videos.viorsan.com",
    "tube-sciences-technologies.apps.education.fr",
    "tube-numerique-educatif.apps.education.fr",
    "tube-arts-lettres-sciences-humaines.apps.education.fr",
    "beetoons.tv",
    "comics.peertube.biz",
    "makertube.net"
  ];
  const sitesPoketube = [
    "poketube.fun",
    "pt.sudovanilla.org",
    "poke.ggtyler.dev",
    "poke.uk2.littlekai.co.uk",
    "poke.blahai.gay"
  ];
  const sitesRicktube = ["ricktube.ru"];
  const sitesCoursehunterLike = ["coursehunter.net", "coursetrain.net"];
  var ExtVideoService;
  (function(ExtVideoService2) {
    ExtVideoService2["udemy"] = "udemy";
    ExtVideoService2["coursera"] = "coursera";
    ExtVideoService2["douyin"] = "douyin";
    ExtVideoService2["artstation"] = "artstation";
    ExtVideoService2["kickstarter"] = "kickstarter";
    ExtVideoService2["oraclelearn"] = "oraclelearn";
    ExtVideoService2["deeplearningai"] = "deeplearningai";
    ExtVideoService2["netacad"] = "netacad";
  })(ExtVideoService || (ExtVideoService = {}));
  ({
    ...VideoService,
    ...ExtVideoService
  });
  const sites = [
    {
      additionalData: "mobile",
      host: VideoService.youtube,
      url: "https://youtu.be/",
      match: /^m.youtube.com$/,
      selector: ".player-container",
      needExtraData: true
    },
    {
      host: VideoService.youtube,
      url: "https://youtu.be/",
      match: /^(www.)?youtube(-nocookie|kids)?.com$/,
      selector: ".html5-video-container:not(#inline-player *)",
      needExtraData: true
    },
    {
      host: VideoService.invidious,
      url: "https://youtu.be/",
      match: sitesInvidious,
      selector: "#player",
      needBypassCSP: true
    },
    {
      host: VideoService.piped,
      url: "https://youtu.be/",
      match: sitesPiped,
      selector: ".shaka-video-container",
      needBypassCSP: true
    },
    {
      host: VideoService.poketube,
      url: "https://youtu.be/",
      match: sitesPoketube,
      selector: ".video-player-container"
    },
    {
      host: VideoService.ricktube,
      url: "https://youtu.be/",
      match: sitesRicktube,
      selector: "#oframeplayer > pjsdiv:has(video)"
    },
    {
      additionalData: "mobile",
      host: VideoService.vk,
      url: "https://vk.com/video?z=",
      match: [/^m.vk.(com|ru)$/, /^m.vkvideo.ru$/],
      selector: "vk-video-player",
      shadowRoot: true,
      needExtraData: true
    },
    {
      additionalData: "clips",
      host: VideoService.vk,
      url: "https://vk.com/video?z=",
      match: /^(www.|m.)?vk.(com|ru)$/,
      selector: 'div[data-testid="clipcontainer-video"]',
      needExtraData: true
    },
    {
      host: VideoService.vk,
      url: "https://vk.com/video?z=",
      match: [/^(www.|m.)?vk.(com|ru)$/, /^(www.|m.)?vkvideo.ru$/],
      selector: ".videoplayer_media",
      needExtraData: true
    },
    {
      host: VideoService.nine_gag,
      url: "https://9gag.com/gag/",
      match: /^9gag.com$/,
      selector: ".video-post",
      needExtraData: true
    },
    {
      host: VideoService.twitch,
      url: "https://twitch.tv/",
      match: [
        /^m.twitch.tv$/,
        /^(www.)?twitch.tv$/,
        /^clips.twitch.tv$/,
        /^player.twitch.tv$/
      ],
      needExtraData: true,
      selector: ".video-ref, main > div > section > div > div > div"
    },
    {
      host: VideoService.proxitok,
      url: "https://www.tiktok.com/",
      match: sitesProxiTok,
      selector: ".column.has-text-centered"
    },
    {
      host: VideoService.tiktok,
      url: "https://www.tiktok.com/",
      match: /^(www.)?tiktok.com$/,
      selector: null
    },
    {
      host: ExtVideoService.douyin,
      url: "https://www.douyin.com/",
      match: /^(www.)?douyin.com/,
      selector: ".xg-video-container",
      needExtraData: true,
      needBypassCSP: true
    },
    {
      host: VideoService.vimeo,
      url: "https://vimeo.com/",
      match: /^vimeo.com$/,
      needExtraData: true,
      selector: ".player"
    },
    {
      host: VideoService.vimeo,
      url: "https://player.vimeo.com/",
      match: /^player.vimeo.com$/,
      additionalData: "embed",
      needExtraData: true,
      needBypassCSP: true,
      selector: ".player"
    },
    {
      host: VideoService.xvideos,
      url: "https://www.xvideos.com/",
      match: [
        /^(www.)?xvideos(-ar)?.com$/,
        /^(www.)?xvideos(\d\d\d).com$/,
        /^(www.)?xv-ru.com$/
      ],
      selector: "#hlsplayer",
      needBypassCSP: true
    },
    {
      host: VideoService.pornhub,
      url: "https://rt.pornhub.com/view_video.php?viewkey=",
      match: /^[a-z]+.pornhub.(com|org)$/,
      selector: ".mainPlayerDiv > .video-element-wrapper-js > div",
      eventSelector: ".mgp_eventCatcher"
    },
    {
      additionalData: "embed",
      host: VideoService.pornhub,
      url: "https://rt.pornhub.com/view_video.php?viewkey=",
      match: (url) => /^[a-z]+.pornhub.(com|org)$/.exec(url.host) && url.pathname.startsWith("/embed/"),
      selector: "#player"
    },
    {
      host: VideoService.twitter,
      url: "https://twitter.com/i/status/",
      match: /^(twitter|x).com$/,
      selector: 'div[data-testid="videoComponent"] > div:nth-child(1) > div',
      eventSelector: 'div[data-testid="videoPlayer"]',
      needBypassCSP: true
    },
    {
      host: VideoService.rumble,
      url: "https://rumble.com/",
      match: /^rumble.com$/,
      selector: "#videoPlayer > .videoPlayer-Rumble-cls > div"
    },
    {
      host: VideoService.facebook,
      url: "https://facebook.com/",
      match: (url) => url.host.includes("facebook.com") && url.pathname.includes("/videos/"),
      selector: 'div[role="main"] div[data-pagelet$="video" i]',
      needBypassCSP: true
    },
    {
      additionalData: "reels",
      host: VideoService.facebook,
      url: "https://facebook.com/",
      match: (url) => url.host.includes("facebook.com") && url.pathname.includes("/reel/"),
      selector: 'div[role="main"]',
      needBypassCSP: true
    },
    {
      host: VideoService.rutube,
      url: "https://rutube.ru/video/",
      match: /^rutube.ru$/,
      selector: ".video-player > div > div > div:nth-child(2)"
    },
    {
      additionalData: "embed",
      host: VideoService.rutube,
      url: "https://rutube.ru/video/",
      match: /^rutube.ru$/,
      selector: "#app > div > div"
    },
    {
      host: VideoService.bilibili,
      url: "https://www.bilibili.com/",
      match: /^(www|m|player).bilibili.com$/,
      selector: ".bpx-player-video-wrap"
    },
    {
      additionalData: "old",
      host: VideoService.bilibili,
      url: "https://www.bilibili.com/",
      match: /^(www|m).bilibili.com$/,
      selector: null
    },
    {
      host: VideoService.mailru,
      url: "https://my.mail.ru/",
      match: /^my.mail.ru$/,
      selector: "#b-video-wrapper"
    },
    {
      host: VideoService.bitchute,
      url: "https://www.bitchute.com/video/",
      match: /^(www.)?bitchute.com$/,
      selector: ".video-js"
    },
    {
      host: VideoService.eporner,
      url: "https://www.eporner.com/",
      match: /^(www.)?eporner.com$/,
      selector: ".vjs-v7"
    },
    {
      host: VideoService.peertube,
      url: "stub",
      match: sitesPeertube,
      selector: ".vjs-v7"
    },
    {
      host: VideoService.dailymotion,
      url: "https://dai.ly/",
      match: /^geo([\d]+)?.dailymotion.com$/,
      selector: ".player"
    },
    {
      host: VideoService.trovo,
      url: "https://trovo.live/s/",
      match: /^trovo.live$/,
      selector: ".player-video"
    },
    {
      host: VideoService.yandexdisk,
      url: "https://yadi.sk/",
      match: /^disk.yandex.(ru|kz|com(\.(am|ge|tr))?|by|az|co\.il|ee|lt|lv|md|net|tj|tm|uz)$/,
      selector: ".video-player__player > div:nth-child(1)",
      eventSelector: ".video-player__player",
      needBypassCSP: true,
      needExtraData: true
    },
    {
      host: VideoService.okru,
      url: "https://ok.ru/video/",
      match: /^ok.ru$/,
      selector: "vk-video-player",
      shadowRoot: true
    },
    {
      host: VideoService.googledrive,
      url: "https://drive.google.com/file/d/",
      match: /^youtube.googleapis.com$/,
      selector: ".html5-video-container"
    },
    {
      host: VideoService.bannedvideo,
      url: "https://madmaxworld.tv/watch?id=",
      match: /^(www.)?banned.video|madmaxworld.tv$/,
      selector: ".vjs-v7",
      needExtraData: true
    },
    {
      host: VideoService.weverse,
      url: "https://weverse.io/",
      match: /^weverse.io$/,
      selector: ".webplayer-internal-source-wrapper",
      needExtraData: true
    },
    {
      host: VideoService.newgrounds,
      url: "https://www.newgrounds.com/",
      match: /^(www.)?newgrounds.com$/,
      selector: ".ng-video-player"
    },
    {
      host: VideoService.egghead,
      url: "https://egghead.io/",
      match: /^egghead.io$/,
      selector: ".cueplayer-react-video-holder"
    },
    {
      host: VideoService.youku,
      url: "https://v.youku.com/",
      match: /^v.youku.com$/,
      selector: "#ykPlayer"
    },
    {
      host: VideoService.archive,
      url: "https://archive.org/details/",
      match: /^archive.org$/,
      selector: ".jw-media"
    },
    {
      host: VideoService.kodik,
      url: "stub",
      match: /^kodik.(info|biz|cc)$/,
      selector: ".fp-player",
      needExtraData: true
    },
    {
      host: VideoService.patreon,
      url: "stub",
      match: /^(www.)?patreon.com$/,
      selector: 'div[data-tag="post-card"] div[elevation="subtle"] > div > div > div > div',
      needExtraData: true
    },
    {
      additionalData: "old",
      host: VideoService.reddit,
      url: "stub",
      match: /^old.reddit.com$/,
      selector: ".reddit-video-player-root",
      needExtraData: true,
      needBypassCSP: true
    },
    {
      host: VideoService.reddit,
      url: "stub",
      match: /^(www.|new.)?reddit.com$/,
      selector: "div[slot=post-media-container]",
      shadowRoot: true,
      needExtraData: true,
      needBypassCSP: true
    },
    {
      host: VideoService.kick,
      url: "https://kick.com/",
      match: /^kick.com$/,
      selector: "#injected-embedded-channel-player-video > div",
      needExtraData: true
    },
    {
      host: VideoService.appledeveloper,
      url: "https://developer.apple.com/",
      match: /^developer.apple.com$/,
      selector: ".developer-video-player",
      needExtraData: true,
      needBypassCSP: true
    },
    {
      host: VideoService.epicgames,
      url: "https://dev.epicgames.com/community/learning/",
      match: /^dev.epicgames.com$/,
      selector: ".vjs-v7",
      needExtraData: true
    },
    {
      host: VideoService.odysee,
      url: "stub",
      match: /^odysee.com$/,
      selector: ".vjs-v7",
      needExtraData: true
    },
    {
      host: VideoService.coursehunterLike,
      url: "stub",
      match: sitesCoursehunterLike,
      selector: "#oframeplayer > pjsdiv:has(video)",
      needExtraData: true
    },
    {
      host: VideoService.sap,
      url: "https://learning.sap.com/courses/",
      match: /^learning.sap.com$/,
      selector: ".playkit-container",
      eventSelector: ".playkit-player",
      needExtraData: true,
      needBypassCSP: true
    },
    {
      host: ExtVideoService.udemy,
      url: "https://www.udemy.com/",
      match: /udemy.com$/,
      selector: 'div[data-purpose="curriculum-item-viewer-content"] > section > div > div > div > div:nth-of-type(2)',
      needExtraData: true
    },
    {
      host: ExtVideoService.coursera,
      url: "https://www.coursera.org/",
      match: /coursera.org$/,
      selector: ".vjs-v8",
      needExtraData: true
    },
    {
      host: VideoService.watchpornto,
      url: "https://watchporn.to/",
      match: /^watchporn.to$/,
      selector: ".fp-player"
    },
    {
      host: VideoService.linkedin,
      url: "https://www.linkedin.com/learning/",
      match: /^(www.)?linkedin.com$/,
      selector: ".vjs-v7",
      needExtraData: true,
      needBypassCSP: true
    },
    {
      host: VideoService.incestflix,
      url: "https://www.incestflix.net/watch/",
      match: /^(www.)?incestflix.(net|to|com)$/,
      selector: "#incflix-stream",
      needExtraData: true
    },
    {
      host: VideoService.porntn,
      url: "https://porntn.com/videos/",
      match: /^porntn.com$/,
      selector: ".fp-player",
      needExtraData: true
    },
    {
      host: VideoService.dzen,
      url: "https://dzen.ru/video/watch/",
      match: /^dzen.ru$/,
      selector: ".zen-ui-video-video-player"
    },
    {
      host: VideoService.cloudflarestream,
      url: "stub",
      match: /^(watch|embed|iframe|customer-[^.]+).cloudflarestream.com$/,
      selector: null
    },
    {
      host: VideoService.loom,
      url: "https://www.loom.com/share/",
      match: /^(www.)?loom.com$/,
      selector: ".VideoLayersContainer",
      needExtraData: true,
      needBypassCSP: true
    },
    {
      host: ExtVideoService.artstation,
      url: "https://www.artstation.com/learning/",
      match: /^(www.)?artstation.com$/,
      selector: ".vjs-v7",
      needExtraData: true
    },
    {
      host: VideoService.rtnews,
      url: "https://www.rt.com/",
      match: /^(www.)?rt.com$/,
      selector: ".jw-media",
      needExtraData: true
    },
    {
      host: VideoService.bitview,
      url: "https://www.bitview.net/watch?v=",
      match: /^(www.)?bitview.net$/,
      selector: ".vlScreen",
      needExtraData: true
    },
    {
      host: ExtVideoService.kickstarter,
      url: "https://www.kickstarter.com/",
      match: /^(www.)?kickstarter.com/,
      selector: ".ksr-video-player",
      needExtraData: true
    },
    {
      host: VideoService.thisvid,
      url: "https://thisvid.com/",
      match: /^(www.)?thisvid.com$/,
      selector: ".fp-player"
    },
    {
      additionalData: "regional",
      host: VideoService.ign,
      url: "https://de.ign.com/",
      match: /^(\w{2}.)?ign.com$/,
      needExtraData: true,
      selector: ".video-container"
    },
    {
      host: VideoService.ign,
      url: "https://www.ign.com/",
      match: /^(www.)?ign.com$/,
      selector: ".player",
      needExtraData: true
    },
    {
      host: VideoService.bunkr,
      url: "https://bunkr.site/",
      match: /^bunkr\.(site|black|cat|media|red|site|ws|org|s[kiu]|c[ir]|fi|p[hks]|ru|la|is|to|a[cx])$/,
      needExtraData: true,
      selector: ".plyr__video-wrapper"
    },
    {
      host: VideoService.imdb,
      url: "https://www.imdb.com/video/",
      match: /^(www\.)?imdb\.com$/,
      selector: ".jw-media"
    },
    {
      host: VideoService.telegram,
      url: "https://t.me/",
      match: (url) => /^web\.telegram\.org$/.test(url.hostname) && url.pathname.startsWith("/k"),
      selector: ".ckin__player"
    },
    {
      host: ExtVideoService.oraclelearn,
      url: "https://mylearn.oracle.com/ou/course/",
      match: /^mylearn\.oracle\.com/,
      selector: ".vjs-v7",
      needExtraData: true,
      needBypassCSP: true
    },
    {
      host: ExtVideoService.deeplearningai,
      url: "https://learn.deeplearning.ai/courses/",
      match: /^learn(-dev|-staging)?\.deeplearning\.ai/,
      selector: ".lesson-video-player",
      needExtraData: true
    },
    {
      host: ExtVideoService.netacad,
      url: "https://www.netacad.com/",
      match: /^(www\.)?netacad\.com/,
      selector: ".vjs-v8",
      needExtraData: true
    },
    {
      host: VideoService.custom,
      url: "stub",
      match: (url) => /([^.]+)\.(mp4|webm)/.test(url.pathname),
      rawResult: true
    }
  ];
  class VideoHelperError extends Error {
    constructor(message) {
      super(message);
      this.name = "VideoHelper";
      this.message = message;
    }
  }
  class BaseHelper {
    API_ORIGIN = window.location.origin;
    fetch;
    extraInfo;
    referer;
    origin;
    service;
    video;
    language;
    constructor({ fetchFn = fetchWithTimeout, extraInfo = true, referer = document.referrer ?? `${window.location.origin}/`, origin = window.location.origin, service, video, language = "en" } = {}) {
      this.fetch = fetchFn;
      this.extraInfo = extraInfo;
      this.referer = referer;
      this.origin = /^(http(s)?):\/\//.test(String(origin)) ? origin : window.location.origin;
      this.service = service;
      this.video = video;
      this.language = language;
    }
    async getVideoData(_videoId) {
      return void 0;
    }
    async getVideoId(_url) {
      return void 0;
    }
    returnBaseData(videoId) {
      if (!this.service) {
        return void 0;
      }
      return {
        url: this.service.url + videoId,
        videoId,
        host: this.service.host,
        duration: void 0
      };
    }
  }
  class AppleDeveloperHelper extends BaseHelper {
    API_ORIGIN = "https://developer.apple.com";
    async getVideoData(videoId) {
      try {
        const contentUrl2 = document.querySelector("meta[property='og:video']")?.content;
        if (!contentUrl2) {
          throw new VideoHelperError("Failed to find content url");
        }
        return {
          url: contentUrl2
        };
      } catch (err) {
        Logger.error(`Failed to get apple developer video data by video ID: ${videoId}`, err.message);
        return void 0;
      }
    }
    async getVideoId(url) {
      return /videos\/play\/([^/]+)\/([\d]+)/.exec(url.pathname)?.[0];
    }
  }
  class ArchiveHelper extends BaseHelper {
    async getVideoId(url) {
      return /(details|embed)\/([^/]+)/.exec(url.pathname)?.[2];
    }
  }
  class ArtstationHelper extends BaseHelper {
    API_ORIGIN = "https://www.artstation.com/api/v2/learning";
    getCSRFToken() {
      return document.querySelector('meta[name="public-csrf-token"]')?.content;
    }
    async getCourseInfo(courseId) {
      try {
        const res = await this.fetch(`${this.API_ORIGIN}/courses/${courseId}/autoplay.json`, {
          method: "POST",
          headers: {
            "PUBLIC-CSRF-TOKEN": this.getCSRFToken()
          }
        });
        return await res.json();
      } catch (err) {
        Logger.error(`Failed to get artstation course info by courseId: ${courseId}.`, err.message);
        return false;
      }
    }
    async getVideoUrl(chapterId) {
      try {
        const res = await this.fetch(`${this.API_ORIGIN}/quicksilver/video_url.json?chapter_id=${chapterId}`);
        const data = await res.json();
        return data.url.replace("qsep://", "https://");
      } catch (err) {
        Logger.error(`Failed to get artstation video url by chapterId: ${chapterId}.`, err.message);
        return false;
      }
    }
    async getVideoData(videoId) {
      const [, courseId, , , chapterId] = videoId.split("/");
      const courseInfo = await this.getCourseInfo(courseId);
      if (!courseInfo) {
        return void 0;
      }
      const chapter = courseInfo.chapters.find((chapter2) => chapter2.hash_id === chapterId);
      if (!chapter) {
        return void 0;
      }
      const videoUrl = await this.getVideoUrl(chapter.id);
      if (!videoUrl) {
        return void 0;
      }
      const { title, duration, subtitles: videoSubtitles } = chapter;
      const subtitles = videoSubtitles.filter((subtitle) => subtitle.format === "vtt").map((subtitle) => ({
        language: normalizeLang$1(subtitle.locale),
        source: "artstation",
        format: "vtt",
        url: subtitle.file_url
      }));
      return {
        url: videoUrl,
        title,
        duration,
        subtitles
      };
    }
    async getVideoId(url) {
      return /courses\/(\w{3,5})\/([^/]+)\/chapters\/(\w{3,5})/.exec(url.pathname)?.[0];
    }
  }
  class BannedVideoHelper extends BaseHelper {
    API_ORIGIN = "https://api.banned.video";
    async getVideoInfo(videoId) {
      try {
        const res = await this.fetch(`${this.API_ORIGIN}/graphql`, {
          method: "POST",
          body: JSON.stringify({
            operationName: "GetVideo",
            query: `query GetVideo($id: String!) {
            getVideo(id: $id) {
              title
              description: summary
              duration: videoDuration
              videoUrl: directUrl
              isStream: live
            }
          }`,
            variables: {
              id: videoId
            }
          }),
          headers: {
            "User-Agent": "bannedVideoFrontEnd",
            "apollographql-client-name": "banned-web",
            "apollographql-client-version": "1.3",
            "content-type": "application/json"
          }
        });
        return await res.json();
      } catch (err) {
        console.error(`Failed to get bannedvideo video info by videoId: ${videoId}.`, err.message);
        return false;
      }
    }
    async getVideoData(videoId) {
      const videoInfo = await this.getVideoInfo(videoId);
      if (!videoInfo) {
        return void 0;
      }
      const { videoUrl, duration, isStream, description, title } = videoInfo.data.getVideo;
      return {
        url: videoUrl,
        duration,
        isStream,
        title,
        description
      };
    }
    async getVideoId(url) {
      return url.searchParams.get("id") ?? void 0;
    }
  }
  class BilibiliHelper extends BaseHelper {
    sleep(ms) {
      return new Promise((resolve) => setTimeout(resolve, ms));
    }
    getCurrentVideo() {
      return document.querySelector("video");
    }
    getPlayer() {
      return window.player;
    }
    isValidBilibiliMp4Url(url) {
      if (typeof url !== "string") return false;
      if (url.startsWith("blob:")) return false;
      if (url.includes(".m3u8")) return false;
      try {
        const parsed = new URL(url);
        return /\.mp4$/i.test(parsed.pathname) && (parsed.hostname.includes("bilivideo.com") || parsed.hostname.includes("akamaized.net") || parsed.hostname.includes("hdslb.com"));
      } catch {
        return false;
      }
    }
    getCheesePlayurlApiFromPerformance() {
      try {
        return performance.getEntriesByType("resource").map((entry) => entry.name).reverse().find((url) => url.includes("/pugv/player/web/playurl"));
      } catch {
        return void 0;
      }
    }
    async getCheeseVideoUrlFromApi() {
      const apiUrl = this.getCheesePlayurlApiFromPerformance();
      if (!apiUrl) return void 0;
      try {
        const url = new URL(apiUrl);
        url.searchParams.set("qn", "16");
        url.searchParams.set("type", "mp4");
        url.searchParams.set("platform", "html5");
        url.searchParams.set("high_quality", "0");
        url.searchParams.set("fnver", "0");
        url.searchParams.delete("fnval");
        url.searchParams.delete("fourk");
        url.searchParams.delete("drm_tech_type");
        const response = await fetch(url.href, {
          credentials: "include",
          redirect: "follow"
        });
        const data = await response.json();
        const urls = [];
        for (const item of data?.result?.durl || data?.data?.durl || []) {
          urls.push(item?.url, ...item?.backup_url || []);
        }
        const mp4 = urls.find((url2) => this.isValidBilibiliMp4Url(url2));
        if (mp4) {
          console.log("[VOT][bilibili] selected cheese api mp4 url", mp4);
        } else {
          console.warn("[VOT][bilibili] cheese api has no mp4", {
            apiUrl: url.href,
            data
          });
        }
        return mp4;
      } catch (error2) {
        console.warn("[VOT][bilibili] cheese api failed", {
          apiUrl,
          error: error2
        });
        return void 0;
      }
    }
    async fetchText(url, timeout2 = 1e4) {
      if (typeof this.fetchFn === "function") {
        const response = await this.fetchFn(url, {
          method: "GET",
          redirect: "follow",
          timeout: timeout2,
          headers: {
            "User-Agent": "Mozilla/5.0 (iPhone; CPU iPhone OS 12_0 like Mac OS X) AppleWebKit/605.1.15 (KHTML, like Gecko) Version/12.0 Mobile/15E148 Safari/604.1",
            Referer: "https://m.bilibili.com/"
          }
        });
        return await response.text();
      }
      const controller = new AbortController();
      const timeoutId = setTimeout(() => controller.abort(), timeout2);
      try {
        const response = await fetch(url, {
          method: "GET",
          redirect: "follow",
          credentials: "omit",
          signal: controller.signal
        });
        return await response.text();
      } finally {
        clearTimeout(timeoutId);
      }
    }
    getBilibiliVideoId(url) {
      const bangumiId = /bangumi\/play\/([^/]+)/.exec(url.pathname)?.[0];
      if (bangumiId) return bangumiId;
      const cheeseId = /\/cheese\/play\/([^/?#]+)/.exec(url.pathname)?.[1];
      if (cheeseId) return `cheese/play/${cheeseId}`;
      const bvid = url.searchParams.get("bvid");
      if (bvid) return `video/${bvid}`;
      let vid = /\/video\/([^/?#]+)/.exec(url.pathname)?.[1];
      if (vid) {
        vid = `video/${vid}`;
        if (url.searchParams.get("p") !== null) {
          vid += `/?p=${url.searchParams.get("p")}`;
        }
      }
      return vid;
    }
    getBvidAndCid(currentUrl) {
      const bvid = window.__INITIAL_STATE__?.bvid || window.__INITIAL_STATE__?.videoData?.bvid || /\/video\/(BV[^/?#]+)/.exec(currentUrl.pathname)?.[1];
      const cid = window.__INITIAL_STATE__?.cid || window.__INITIAL_STATE__?.videoData?.cid || window.__INITIAL_STATE__?.videoData?.pages?.[0]?.cid;
      return { bvid, cid };
    }
    async getCidByBvid(bvid) {
      if (!bvid) return void 0;
      try {
        const response = await fetch(
          `https://api.bilibili.com/x/web-interface/view?bvid=${encodeURIComponent(bvid)}`,
          {
            credentials: "include",
            redirect: "follow"
          }
        );
        const data = await response.json();
        return data?.data?.pages?.[0]?.cid;
      } catch (error2) {
        console.warn("[VOT][bilibili] failed to get cid by bvid", {
          bvid,
          error: error2
        });
        return void 0;
      }
    }
    async getVideoUrlFromApi(bvid, cid) {
      if (!bvid || !cid) return void 0;
      try {
        const response = await fetch(
          `https://api.bilibili.com/x/player/playurl?bvid=${encodeURIComponent(bvid)}&cid=${encodeURIComponent(cid)}&qn=16&type=mp4&platform=html5&high_quality=0`,
          {
            credentials: "include",
            redirect: "follow"
          }
        );
        const data = await response.json();
        const urls = [];
        for (const item of data?.data?.durl || []) {
          urls.push(item?.url, ...item?.backup_url || []);
        }
        const mp4 = urls.find((url) => this.isValidBilibiliMp4Url(url));
        if (mp4) {
          console.log("[VOT][bilibili] selected api mp4 url", mp4);
        } else {
          console.warn("[VOT][bilibili] playurl api has no mp4", {
            bvid,
            cid,
            data
          });
        }
        return mp4;
      } catch (error2) {
        console.warn("[VOT][bilibili] playurl api failed", {
          bvid,
          cid,
          error: error2
        });
        return void 0;
      }
    }
    getMobileUrl(url) {
      const id = this.getBilibiliVideoId(url);
      return id ? `https://m.bilibili.com/${id}` : void 0;
    }
    extractMp4FromText(text) {
      if (typeof text !== "string") return void 0;
      const decoded = text.replace(/\\u002F/g, "/").replace(/\\\//g, "/").replace(/&amp;/g, "&").replace(/%2F/gi, "/").replace(/%3A/gi, ":").replace(/%3F/gi, "?").replace(/%26/gi, "&").replace(/%3D/gi, "=");
      const matches = decoded.match(/https?:\/\/[^"'<>\\\s|{}]+\.mp4[^"'<>\\\s|{}]*/gi) || [];
      return matches.find((url) => this.isValidBilibiliMp4Url(url));
    }
    async getVideoUrlFromMobilePage(pageUrl) {
      if (!pageUrl) return void 0;
      try {
        console.log("[VOT][bilibili] fetching mobile page", pageUrl);
        const html = await this.fetchText(pageUrl, 1e4);
        const mp4 = this.extractMp4FromText(html);
        if (this.isValidBilibiliMp4Url(mp4)) {
          console.log("[VOT][bilibili] selected mobile mp4 url", mp4);
          return mp4;
        }
        console.warn("[VOT][bilibili] mobile page has no mp4", pageUrl);
      } catch (error2) {
        console.warn("[VOT][bilibili] failed to fetch mobile page", {
          pageUrl,
          error: error2
        });
      }
      return void 0;
    }
    findMp4Deep(value, seen = new WeakSet()) {
      if (!value) return void 0;
      if (typeof value === "string") {
        const found = this.extractMp4FromText(value);
        return found ?? (this.isValidBilibiliMp4Url(value) ? value : void 0);
      }
      if (typeof value !== "object") return void 0;
      if (seen.has(value)) return void 0;
      seen.add(value);
      for (const item of Array.isArray(value) ? value : Object.values(value)) {
        const found = this.findMp4Deep(item, seen);
        if (found) return found;
      }
      return void 0;
    }
    getVideoUrlFromPlayerConfig() {
      return this.findMp4Deep(this.getPlayer()?.config);
    }
    getVideoUrlFromPerformance() {
      try {
        const entries = performance.getEntriesByType("resource").map((entry) => entry.name).reverse();
        for (const url of entries) {
          if (this.isValidBilibiliMp4Url(url)) {
            return url;
          }
          const extracted = this.extractMp4FromText(url);
          if (this.isValidBilibiliMp4Url(extracted)) {
            return extracted;
          }
        }
        return void 0;
      } catch {
        return void 0;
      }
    }
    async waitForVideoUrl(timeout2 = 5e3) {
      const startedAt = Date.now();
      while (Date.now() - startedAt < timeout2) {
        const video = this.getCurrentVideo();
        const candidates = [
          this.getVideoUrlFromPlayerConfig(),
          this.getVideoUrlFromPerformance(),
          video?.src,
          video?.currentSrc,
          video?.querySelector("source")?.src
        ];
        for (const src of candidates) {
          if (this.isValidBilibiliMp4Url(src)) {
            console.log("[VOT][bilibili] selected current page mp4 url", src);
            return src;
          }
        }
        await this.sleep(300);
      }
      return void 0;
    }
    async getVideoData(_videoId) {
      console.log("[VOT][bilibili] getVideoData called", _videoId);
      const currentUrl = new URL(window.location.href);
      const video = this.getCurrentVideo();
      const bilibiliId = this.getBilibiliVideoId(currentUrl) ?? currentUrl.pathname.replace(/^\/+/, "");
      const { bvid, cid: initialCid } = this.getBvidAndCid(currentUrl);
      const cid = initialCid ?? await this.getCidByBvid(bvid);
      const mobileUrl = this.getMobileUrl(currentUrl);
      const isCheese = /\/cheese\/play\//.test(currentUrl.pathname);
      const videoUrl = isCheese ? await this.getCheeseVideoUrlFromApi() ?? await this.getVideoUrlFromMobilePage(mobileUrl) ?? this.getVideoUrlFromPerformance() ?? await this.waitForVideoUrl(5e3) : await this.getVideoUrlFromApi(bvid, cid) ?? await this.getVideoUrlFromMobilePage(mobileUrl) ?? this.getVideoUrlFromPerformance() ?? await this.waitForVideoUrl(5e3);
      if (!videoUrl) {
        console.warn("[VOT][bilibili] no mp4 url found", {
          pageUrl: window.location.href,
          mobileUrl,
          bvid,
          cid,
          videoSrc: video?.src,
          videoCurrentSrc: video?.currentSrc,
          playerConfig: this.getPlayer()?.config
        });
        return void 0;
      }
      return {
        url: videoUrl,
        videoId: `bilibili:${bvid ?? bilibiliId}`,
        host: "bilibili",
        duration: video?.duration,
        isStream: false
      };
    }
    async getVideoId(url) {
      console.log("[VOT][bilibili] getVideoId called", url.href);
      const { bvid } = this.getBvidAndCid(url);
      if (bvid) return `bilibili:${bvid}`;
      const id = this.getBilibiliVideoId(url);
      return id ? `bilibili:${id}` : void 0;
    }
  }
  class BitchuteHelper extends BaseHelper {
    async getVideoId(url) {
      return /(video|embed)\/([^/]+)/.exec(url.pathname)?.[2];
    }
  }
  class BitviewHelper extends BaseHelper {
    async getVideoData(videoId) {
      try {
        const videoUrl = document.querySelector(".vlScreen > video")?.src;
        if (!videoUrl) {
          throw new VideoHelperError("Failed to find video URL");
        }
        return {
          url: videoUrl
        };
      } catch (err) {
        Logger.error(`Failed to get Bitview data by videoId: ${videoId}`, err.message);
        return void 0;
      }
    }
    async getVideoId(url) {
      return url.searchParams.get("v");
    }
  }
  class BunkrHelper extends BaseHelper {
    async getVideoData(_videoId) {
      const url = document.querySelector('#player > source[type="video/mp4"]')?.src;
      if (!url) {
        return void 0;
      }
      return {
        url
      };
    }
    async getVideoId(url) {
      return /\/f\/([^/]+)/.exec(url.pathname)?.[1];
    }
  }
  class CloudflareStreamHelper extends BaseHelper {
    async getVideoId(url) {
      return url.pathname + url.search;
    }
  }
  class CoursehunterLikeHelper extends BaseHelper {
    API_ORIGIN = this.origin ?? "https://coursehunter.net";
    async getCourseId() {
      const courseId = window.course_id;
      if (courseId !== void 0) {
        return String(courseId);
      }
      return document.querySelector('input[name="course_id"]')?.value;
    }
    async getLessonsData(courseId) {
      const lessons = window.lessons;
      if (lessons?.length) {
        return lessons;
      }
      try {
        const res = await this.fetch(`${this.API_ORIGIN}/api/v1/course/${courseId}/lessons`);
        return await res.json();
      } catch (err) {
        Logger.error(`Failed to get CoursehunterLike lessons data by courseId: ${courseId}, because ${err.message}`);
        return void 0;
      }
    }
    getLessondId(videoId) {
      let lessondId = videoId.split("?lesson=")?.[1];
      if (lessondId) {
        return +lessondId;
      }
      const activeLessondEl = document.querySelector(".lessons-item_active");
      lessondId = activeLessondEl?.dataset?.index;
      if (lessondId) {
        return +lessondId;
      }
      return 1;
    }
    async getVideoData(videoId) {
      const courseId = await this.getCourseId();
      if (!courseId) {
        return void 0;
      }
      const lessonsData = await this.getLessonsData(courseId);
      if (!lessonsData) {
        return void 0;
      }
      const lessonId = this.getLessondId(videoId);
      const currentLesson = lessonsData?.[lessonId - 1];
      const { file: videoUrl, duration, title } = currentLesson;
      if (!videoUrl) {
        return void 0;
      }
      return {
        url: proxyMedia(videoUrl),
        duration,
        title
      };
    }
    async getVideoId(url) {
      const courseId = /course\/([^/]+)/.exec(url.pathname)?.[0];
      return courseId ? courseId + url.search : void 0;
    }
  }
  class VideoJSHelper extends BaseHelper {
    SUBTITLE_SOURCE = "videojs";
    SUBTITLE_FORMAT = "vtt";
    static getPlayer() {
      return document.querySelector(".video-js")?.player;
    }
    getVideoDataByPlayer(videoId) {
      try {
        const player2 = VideoJSHelper.getPlayer();
        if (!player2) {
          throw new Error(`Video player doesn't have player option, videoId ${videoId}`);
        }
        const duration = player2.duration();
        const sources = Array.isArray(player2.currentSources) ? player2.currentSources : player2.getCache()?.sources;
        const { tracks_: tracks } = player2.textTracks();
        const videoUrl = sources.find((source) => source.type === "video/mp4" || source.type === "video/webm");
        if (!videoUrl) {
          throw new Error(`Failed to find video url for videoID ${videoId}`);
        }
        const subtitles = tracks.filter((track) => track.src && track.kind !== "metadata").map((track) => ({
          language: normalizeLang$1(track.language),
          source: this.SUBTITLE_SOURCE,
          format: this.SUBTITLE_FORMAT,
          url: track.src
        }));
        return {
          url: videoUrl.src,
          duration,
          subtitles
        };
      } catch (err) {
        Logger.error("Failed to get videojs video data", err.message);
        return void 0;
      }
    }
  }
  const availableLangs = [
    "auto",
    "ru",
    "en",
    "zh",
    "ko",
    "lt",
    "lv",
    "ar",
    "fr",
    "it",
    "es",
    "de",
    "ja"
  ];
  const availableTTS = ["ru", "en", "kk"];
  class CourseraHelper extends VideoJSHelper {
    API_ORIGIN = "https://www.coursera.org/api";
    SUBTITLE_SOURCE = "coursera";
    async getCourseData(courseId) {
      try {
        const response = await this.fetch(`${this.API_ORIGIN}/onDemandCourses.v1/${courseId}`);
        const resJSON = await response.json();
        return resJSON?.elements?.[0];
      } catch (err) {
        Logger.error(`Failed to get course data by courseId: ${courseId}`, err.message);
        return void 0;
      }
    }
    static getPlayer() {
      return VideoJSHelper.getPlayer();
    }
    async getVideoData(videoId) {
      const data = this.getVideoDataByPlayer(videoId);
      if (!data) {
        return void 0;
      }
      const { options_: options } = CourseraHelper.getPlayer() ?? {};
      if (!data.subtitles?.length && options) {
        data.subtitles = options.tracks.map((track) => ({
          url: track.src,
          language: normalizeLang$1(track.srclang),
          source: this.SUBTITLE_SOURCE,
          format: this.SUBTITLE_FORMAT
        }));
      }
      const courseId = options?.courseId;
      if (!courseId) {
        return data;
      }
      let courseLang = "en";
      const courseData = await this.getCourseData(courseId);
      if (courseData) {
        const { primaryLanguageCodes: [primaryLangauge] } = courseData;
        courseLang = primaryLangauge ? normalizeLang$1(primaryLangauge) : "en";
      }
      if (!availableLangs.includes(courseLang)) {
        courseLang = "en";
      }
      const subtitleItem = data.subtitles.find((subtitle) => subtitle.language === courseLang) ?? data.subtitles?.[0];
      const subtitleUrl = subtitleItem?.url;
      if (!subtitleUrl) {
        Logger.warn("Failed to find any subtitle file");
      }
      const { url, duration } = data;
      const translationHelp = subtitleUrl ? [
        {
          target: "subtitles_file_url",
          targetUrl: subtitleUrl
        },
        {
          target: "video_file_url",
          targetUrl: url
        }
      ] : null;
      return {
        ...subtitleUrl ? {
          url: this.service?.url + videoId,
          translationHelp
        } : {
          url,
          translationHelp
        },
        detectedLanguage: courseLang,
        duration
      };
    }
    async getVideoId(url) {
      const matched = /learn\/([^/]+)\/lecture\/([^/]+)/.exec(url.pathname) ?? /lecture\/([^/]+)\/([^/]+)/.exec(url.pathname);
      return matched?.[0];
    }
  }
  class DailymotionHelper extends BaseHelper {
    async getVideoId(_url) {
      const plainPlayerConfig = Array.from(document.querySelectorAll("*")).filter((s2) => s2.innerHTML.trim().includes(".m3u8"));
      const videoUrl = plainPlayerConfig?.[1]?.lastChild?.src;
      return videoUrl ? /\/video\/(\w+)\.m3u8/.exec(videoUrl)?.[1] : void 0;
    }
  }
  class DeeplearningAIHelper extends BaseHelper {
    async getVideoData(_videoId) {
      if (!this.video) {
        return void 0;
      }
      const sourceUrl = this.video.querySelector('source[type="application/x-mpegurl"]')?.src;
      if (!sourceUrl) {
        return void 0;
      }
      return {
        url: sourceUrl
      };
    }
    async getVideoId(url) {
      return /courses\/(([^/]+)\/lesson\/([^/]+)\/([^/]+))/.exec(url.pathname)?.[1];
    }
  }
  class DouyinHelper extends BaseHelper {
    static getPlayer() {
      if (typeof player === "undefined") {
        return void 0;
      }
      return player;
    }
    sleep(ms) {
      return new Promise((resolve) => setTimeout(resolve, ms));
    }
    isValidDouyinMp4Url(url) {
      return typeof url === "string" && !url.startsWith("blob:") && !url.includes(".m3u8") && !url.includes("/third/stream-") && url.includes("zjcdn.com") && url.includes("/video/tos/") && url.includes("mime_type=video_mp4");
    }
    cleanDouyinUrl(url) {
      return url.split("?")[0];
    }
    getAwemeIdFromUrl(url) {
      return url.searchParams.get("modal_id") ?? /\/share\/video\/(\d+)/.exec(url.pathname)?.[1] ?? /\/video\/(\d+)/.exec(url.pathname)?.[1] ?? void 0;
    }
    getMediaVideoIdFromUrl(url) {
      try {
        return new URL(url).searchParams.get("video_id") ?? void 0;
      } catch {
        return void 0;
      }
    }
    getCurrentVideo() {
      return document.querySelector("video");
    }
    getCurrentVideoSrc() {
      const video = this.getCurrentVideo();
      return video?.currentSrc || video?.src || void 0;
    }
    async fetchWithTimeout(url, timeout2 = 3e3) {
      const controller = new AbortController();
      const timeoutId = setTimeout(() => controller.abort(), timeout2);
      try {
        return await fetch(url, {
          method: "GET",
          redirect: "follow",
          credentials: "omit",
          signal: controller.signal
        });
      } finally {
        clearTimeout(timeoutId);
      }
    }
    async resolvePlaywmUrl(playwmUrl, timeout2 = 3e3) {
      if (!playwmUrl?.includes("/aweme/v1/playwm/")) {
        return void 0;
      }
      try {
        const response = await this.fetchWithTimeout(playwmUrl, timeout2);
        const finalUrl = response.url;
        if (this.isValidDouyinMp4Url(finalUrl)) {
          return finalUrl;
        }
        console.warn("[VOT][douyin] invalid playwm final url", {
          playwmUrl,
          finalUrl
        });
      } catch (error2) {
        console.warn("[VOT][douyin] playwm fetch failed", {
          playwmUrl,
          error: error2
        });
      }
      return void 0;
    }
    async getMobileVideoUrl(timeout2 = 5e3) {
      const startedAt = Date.now();
      let lastSrc;
      while (Date.now() - startedAt < timeout2) {
        const src = this.getCurrentVideoSrc();
        if (src && src !== lastSrc) {
          lastSrc = src;
          if (this.isValidDouyinMp4Url(src)) {
            return src;
          }
          const resolvedUrl = await this.resolvePlaywmUrl(src, 2500);
          if (resolvedUrl) {
            return resolvedUrl;
          }
        }
        await this.sleep(200);
      }
      return void 0;
    }
    async getVideoUrlByMediaVideoId(mediaVideoId) {
      if (!mediaVideoId) {
        return void 0;
      }
      const playwmUrl = `https://m.douyin.com/aweme/v1/playwm/?video_id=${mediaVideoId}&ratio=720p&line=0`;
      return this.resolvePlaywmUrl(playwmUrl, 3e3);
    }
    async waitForTestSpeedUrl(timeout2 = 5e3) {
      const startedAt = Date.now();
      while (Date.now() - startedAt < timeout2) {
        const testSpeedUrl = DouyinHelper.getPlayer()?.config?.TestSpeed?.url;
        if (this.isValidDouyinMp4Url(testSpeedUrl)) {
          return testSpeedUrl;
        }
        await this.sleep(300);
      }
      return void 0;
    }
    async getVideoData(_videoId) {
      const xgPlayer = DouyinHelper.getPlayer();
      const currentUrl = new URL(window.location.href);
      const video = this.getCurrentVideo();
      const awemeId = this.getAwemeIdFromUrl(currentUrl);
      const currentVideoSrc = this.getCurrentVideoSrc();
      const mediaVideoId = this.getMediaVideoIdFromUrl(currentVideoSrc) ?? xgPlayer?.config?.vid;
      let videoUrl;
      if (currentVideoSrc?.includes("/aweme/v1/playwm/")) {
        videoUrl = await this.getMobileVideoUrl();
      }
      if (!videoUrl) {
        videoUrl = await this.waitForTestSpeedUrl();
      }
      if (!videoUrl && mediaVideoId) {
        videoUrl = await this.getVideoUrlByMediaVideoId(mediaVideoId);
      }
      if (!videoUrl) {
        console.warn("[VOT][douyin] no valid video url found", {
          awemeId,
          mediaVideoId,
          testSpeed: xgPlayer?.config?.TestSpeed,
          currentVideoSrc
        });
        return void 0;
      }
      const cleanVideoUrl = this.cleanDouyinUrl(videoUrl);
      const douyinId = awemeId ?? mediaVideoId ?? xgPlayer?.config?.vid ?? cleanVideoUrl;
      const duration = xgPlayer?.config?.duration ?? video?.duration;
      const lang2 = xgPlayer?.config?.lang;
      const isStream = xgPlayer?.config?.isLive ?? false;
      console.log("[VOT][douyin] selected video url", {
        douyinId,
        videoUrl,
        cleanVideoUrl,
        currentVideoSrc
      });
      return {
        url: cleanVideoUrl,
        videoId: `douyin:${douyinId}`,
        host: "douyin",
        duration,
        isStream,
        ...availableLangs.includes(lang2) ? { detectedLanguage: lang2 } : {}
      };
    }
    async getVideoId(url) {
      const awemeId = this.getAwemeIdFromUrl(url);
      if (awemeId) {
        return `douyin:${awemeId}`;
      }
      const currentVideoSrc = this.getCurrentVideoSrc();
      const mediaVideoId = this.getMediaVideoIdFromUrl(currentVideoSrc) ?? DouyinHelper.getPlayer()?.config?.vid;
      return mediaVideoId ? `douyin:${mediaVideoId}` : void 0;
    }
  }
  class DzenHelper extends BaseHelper {
    async getVideoId(url) {
      return /video\/watch\/([^/]+)/.exec(url.pathname)?.[1];
    }
  }
  class EggheadHelper extends BaseHelper {
    async getVideoId(url) {
      return url.pathname.slice(1);
    }
  }
  class EpicGamesHelper extends BaseHelper {
    API_ORIGIN = "https://dev.epicgames.com/community/api/learning";
    async getPostInfo(videoId) {
      try {
        const res = await this.fetch(`${this.API_ORIGIN}/post.json?hash_id=${videoId}`);
        return await res.json();
      } catch (err) {
        Logger.error(`Failed to get epicgames post info by videoId: ${videoId}.`, err.message);
        return false;
      }
    }
    getVideoBlock() {
      const videoUrlRe = /videoUrl\s?=\s"([^"]+)"?/;
      const script = Array.from(document.body.querySelectorAll("script")).find((s2) => videoUrlRe.exec(s2.innerHTML));
      if (!script) {
        return void 0;
      }
      const content = script.innerHTML.trim();
      const playlistUrl = videoUrlRe.exec(content)?.[1]?.replace("qsep://", "https://");
      if (!playlistUrl) {
        return void 0;
      }
      let subtitlesString = /sources\s?=\s(\[([^\]]+)\])?/.exec(content)?.[1];
      if (!subtitlesString) {
        return {
          playlistUrl,
          subtitles: []
        };
      }
      try {
        subtitlesString = (subtitlesString.replace(/src:(\s)+?(videoUrl)/g, 'src:"removed"').substring(0, subtitlesString.lastIndexOf("},")) + "]").split("\n").map((line) => line.replace(/([^\s]+):\s?(?!.*\1)/, '"$1":')).join("\n");
        const subtitlesObj = JSON.parse(subtitlesString);
        const subtitles = subtitlesObj.filter((sub) => sub.type === "captions");
        return {
          playlistUrl,
          subtitles
        };
      } catch {
        return {
          playlistUrl,
          subtitles: []
        };
      }
    }
    async getVideoData(videoId) {
      const courseId = videoId.split(":")?.[1];
      const postInfo = await this.getPostInfo(courseId);
      if (!postInfo) {
        return void 0;
      }
      const videoBlock = this.getVideoBlock();
      if (!videoBlock) {
        return void 0;
      }
      const { playlistUrl, subtitles: videoSubtitles } = videoBlock;
      const { title, description } = postInfo;
      const subtitles = videoSubtitles.map((caption) => ({
        language: normalizeLang$1(caption.srclang),
        source: "epicgames",
        format: "vtt",
        url: caption.src
      }));
      return {
        url: playlistUrl,
        title,
        description,
        subtitles
      };
    }
    async getVideoId(_url) {
      return new Promise((resolve) => {
        const origin = "https://dev.epicgames.com";
        const reqId = btoa(window.location.href);
        window.addEventListener("message", (e2) => {
          if (e2.origin !== origin) {
            return void 0;
          }
          if (!(typeof e2.data === "string" && e2.data.startsWith("getVideoId:"))) {
            return void 0;
          }
          const videoId = e2.data.replace("getVideoId:", "");
          return resolve(videoId);
        });
        window.top.postMessage(`getVideoId:${reqId}`, origin);
      });
    }
  }
  class EpornerHelper extends BaseHelper {
    async getVideoId(url) {
      return /video-([^/]+)\/([^/]+)/.exec(url.pathname)?.[0];
    }
  }
  class FacebookHelper extends BaseHelper {
    async getVideoId(url) {
      return url.pathname.slice(1);
    }
  }
  class GoogleDriveHelper extends BaseHelper {
    getPlayerData() {
      const playerEl = document.querySelector("#movie_player");
      return playerEl?.getVideoData?.call() ?? void 0;
    }
    async getVideoId(_url) {
      return this.getPlayerData()?.video_id;
    }
  }
  class IgnHelper extends BaseHelper {
    getVideoDataBySource(videoId) {
      const url = document.querySelector('.icms.video > source[type="video/mp4"][data-quality="360"]')?.src;
      if (!url) {
        return this.returnBaseData(videoId);
      }
      return {
        url: proxyMedia(url)
      };
    }
    getVideoDataByNext(videoId) {
      try {
        const nextContent = document.getElementById("__NEXT_DATA__")?.textContent;
        if (!nextContent) {
          throw new VideoDataError("Not found __NEXT_DATA__ content");
        }
        const data = JSON.parse(nextContent);
        const { props: { pageProps: { page: { description, title, video: { videoMetadata: { duration }, assets } } } } } = data;
        const videoUrl = assets.find((asset) => asset.height === 360 && asset.url.includes(".mp4"))?.url;
        if (!videoUrl) {
          throw new VideoDataError("Not found video URL in assets");
        }
        return {
          url: proxyMedia(videoUrl),
          duration,
          title,
          description
        };
      } catch (err) {
        Logger.warn(`Failed to get ign video data by video ID: ${videoId}, because ${err.message}. Using clear link instead...`);
        return this.returnBaseData(videoId);
      }
    }
    async getVideoData(videoId) {
      if (document.getElementById("__NEXT_DATA__")) {
        return this.getVideoDataByNext(videoId);
      }
      return this.getVideoDataBySource(videoId);
    }
    async getVideoId(url) {
      return /([^/]+)\/([\d]+)\/video\/([^/]+)/.exec(url.pathname)?.[0] ?? /\/videos\/([^/]+)/.exec(url.pathname)?.[0];
    }
  }
  class IMDbHelper extends BaseHelper {
    async getVideoId(url) {
      return /video\/([^/]+)/.exec(url.pathname)?.[1];
    }
  }
  class IncestflixHelper extends BaseHelper {
    async getVideoData(videoId) {
      try {
        const sourceEl = document.querySelector("#incflix-stream source:first-of-type");
        if (!sourceEl) {
          throw new VideoHelperError("Failed to find source element");
        }
        const srcLink = sourceEl.getAttribute("src");
        if (!srcLink) {
          throw new VideoHelperError("Failed to find source link");
        }
        const source = new URL(srcLink.startsWith("//") ? `https:${srcLink}` : srcLink);
        source.searchParams.append("media-proxy", "video.mp4");
        return {
          url: proxyMedia(source)
        };
      } catch (err) {
        Logger.error(`Failed to get Incestflix data by videoId: ${videoId}`, err.message);
        return void 0;
      }
    }
    async getVideoId(url) {
      return /\/watch\/([^/]+)/.exec(url.pathname)?.[1];
    }
  }
  class KickHelper extends BaseHelper {
    API_ORIGIN = "https://kick.com/api";
    async getClipInfo(clipId) {
      try {
        const res = await this.fetch(`${this.API_ORIGIN}/v2/clips/${clipId}`);
        const data = await res.json();
        const { clip_url: url, duration, title } = data.clip;
        return {
          url,
          duration,
          title
        };
      } catch (err) {
        Logger.error(`Failed to get kick clip info by clipId: ${clipId}.`, err.message);
        return void 0;
      }
    }
    async getVideoInfo(videoId) {
      try {
        const res = await this.fetch(`${this.API_ORIGIN}/v1/video/${videoId}`);
        const data = await res.json();
        const { source: url, livestream } = data;
        const { session_title: title, duration } = livestream;
        return {
          url,
          duration: Math.round(duration / 1e3),
          title
        };
      } catch (err) {
        Logger.error(`Failed to get kick video info by videoId: ${videoId}.`, err.message);
        return void 0;
      }
    }
    async getVideoData(videoId) {
      return videoId.startsWith("videos") ? await this.getVideoInfo(videoId.replace("videos/", "")) : await this.getClipInfo(videoId.replace("clips/", ""));
    }
    async getVideoId(url) {
      return /([^/]+)\/((videos|clips)\/([^/]+))/.exec(url.pathname)?.[2];
    }
  }
  class KickstarterHelper extends BaseHelper {
    async getVideoData(videoId) {
      try {
        const videoEl = document.querySelector(".ksr-video-player > video");
        const url = videoEl?.querySelector("source[type^='video/mp4']")?.src;
        if (!url) {
          throw new VideoHelperError("Failed to find video URL");
        }
        const subtitles = videoEl?.querySelectorAll("track") ?? [];
        return {
          url,
          subtitles: Array.from(subtitles).reduce((result, sub) => {
            const lang2 = sub.getAttribute("srclang");
            const url2 = sub.getAttribute("src");
            if (!lang2 || !url2) {
              return result;
            }
            result.push({
              language: normalizeLang$1(lang2),
              url: url2,
              format: "vtt",
              source: "kickstarter"
            });
            return result;
          }, [])
        };
      } catch (err) {
        Logger.error(`Failed to get Kickstarter data by videoId: ${videoId}`, err.message);
        return void 0;
      }
    }
    async getVideoId(url) {
      return url.pathname.slice(1);
    }
  }
  class KodikHelper extends BaseHelper {
    API_ORIGIN = window.location.origin;
    getSecureData(videoPath) {
      try {
        const [videoType, videoId, hash] = videoPath.split("/").filter((a2) => a2);
        const allScripts = Array.from(document.getElementsByTagName("script"));
        const secureScript = allScripts.filter((s2) => s2.innerHTML.includes(`videoId = "${videoId}"`) || s2.innerHTML.includes(`serialId = Number(${videoId})`));
        if (!secureScript.length) {
          throw new VideoHelperError("Failed to find secure script");
        }
        const secureContent = /'{[^']+}'/.exec(secureScript[0].textContent.trim())?.[0];
        if (!secureContent) {
          throw new VideoHelperError("Secure json wasn't found in secure script");
        }
        const secureJSON = JSON.parse(secureContent.replaceAll("'", ""));
        if (videoType !== "serial") {
          return {
            videoType,
            videoId,
            hash,
            ...secureJSON
          };
        }
        const videoInfoContent = allScripts.find((s2) => s2.innerHTML.includes(`var videoInfo = {}`))?.textContent?.trim();
        if (!videoInfoContent) {
          throw new VideoHelperError("Failed to find videoInfo content");
        }
        const realVideoType = /videoInfo\.type\s+?=\s+?'([^']+)'/.exec(videoInfoContent)?.[1];
        const realVideoId = /videoInfo\.id\s+?=\s+?'([^']+)'/.exec(videoInfoContent)?.[1];
        const realHash = /videoInfo\.hash\s+?=\s+?'([^']+)'/.exec(videoInfoContent)?.[1];
        if (!realVideoType || !realVideoId || !realHash) {
          throw new VideoHelperError("Failed to parse videoInfo content");
        }
        return {
          videoType: realVideoType,
          videoId: realVideoId,
          hash: realHash,
          ...secureJSON
        };
      } catch (err) {
        Logger.error(`Failed to get kodik secure data by videoPath: ${videoPath}.`, err.message);
        return false;
      }
    }
    async getFtor(secureData) {
      const { videoType, videoId: id, hash, d: d2, d_sign, pd, pd_sign, ref, ref_sign } = secureData;
      try {
        const res = await this.fetch(this.API_ORIGIN + "/ftor", {
          method: "POST",
          headers: {
            "User-Agent": votConfig.userAgent,
            Origin: this.API_ORIGIN,
            Referer: `${this.API_ORIGIN}/${videoType}/${id}/${hash}/360p`
          },
          body: new URLSearchParams({
            d: d2,
            d_sign,
            pd,
            pd_sign,
            ref: decodeURIComponent(ref),
            ref_sign,
            bad_user: "false",
            cdn_is_working: "true",
            info: "{}",
            type: videoType,
            hash,
            id
          })
        });
        return await res.json();
      } catch (err) {
        Logger.error(`Failed to get kodik video data (type: ${videoType}, id: ${id}, hash: ${hash})`, err.message);
        return false;
      }
    }
    decryptUrl(encryptedUrl) {
      const decryptedUrl = atob(encryptedUrl.replace(/[a-zA-Z]/g, function(e2) {
        const charCode = e2.charCodeAt(0) + 18;
        const pos = e2 <= "Z" ? 90 : 122;
        return String.fromCharCode(pos >= charCode ? charCode : charCode - 26);
      }));
      return "https:" + decryptedUrl;
    }
    async getVideoData(videoId) {
      const secureData = this.getSecureData(videoId);
      if (!secureData) {
        return void 0;
      }
      const videoData2 = await this.getFtor(secureData);
      if (!videoData2) {
        return void 0;
      }
      const videoDataLinks = Object.entries(videoData2.links[videoData2.default.toString()]);
      const videoLink = videoDataLinks.find(([, data]) => data.type === "application/x-mpegURL")?.[1];
      if (!videoLink) {
        return void 0;
      }
      return {
        url: videoLink.src.startsWith("//") ? `https:${videoLink.src}` : this.decryptUrl(videoLink.src)
      };
    }
    async getVideoId(url) {
      return /\/(uv|video|seria|episode|season|serial)\/([^/]+)\/([^/]+)\/([\d]+)p/.exec(url.pathname)?.[0];
    }
  }
  class LinkedinHelper extends VideoJSHelper {
    SUBTITLE_SOURCE = "linkedin";
    async getVideoData(videoId) {
      const data = this.getVideoDataByPlayer(videoId);
      if (!data) {
        return void 0;
      }
      const { url, duration, subtitles } = data;
      return {
        url: proxyMedia(new URL(url)),
        duration,
        subtitles
      };
    }
    async getVideoId(url) {
      return /\/learning\/(([^/]+)\/([^/]+))/.exec(url.pathname)?.[1];
    }
  }
  var TypeName;
  (function(TypeName2) {
    TypeName2["Channel"] = "Channel";
    TypeName2["Video"] = "Video";
  })(TypeName || (TypeName = {}));
  class LoomHelper extends BaseHelper {
    getClientVersion() {
      if (typeof SENTRY_RELEASE === "undefined") {
        return void 0;
      }
      return SENTRY_RELEASE.id;
    }
    async getVideoData(videoId) {
      try {
        const clientVer = this.getClientVersion();
        if (!clientVer) {
          throw new VideoHelperError("Failed to get client version");
        }
        const res = await this.fetch("https://www.loom.com/graphql", {
          headers: {
            "User-Agent": votConfig.userAgent,
            "content-type": "application/json",
            "x-loom-request-source": `loom_web_${clientVer}`,
            "apollographql-client-name": "web",
            "apollographql-client-version": clientVer,
            "Alt-Used": "www.loom.com"
          },
          body: `{"operationName":"FetchCaptions","variables":{"videoId":"${videoId}"},"query":"query FetchCaptions($videoId: ID!, $password: String) {\\n  fetchVideoTranscript(videoId: $videoId, password: $password) {\\n    ... on VideoTranscriptDetails {\\n      id\\n      captions_source_url\\n      language\\n      __typename\\n    }\\n    ... on GenericError {\\n      message\\n      __typename\\n    }\\n    __typename\\n  }\\n}"}`,
          method: "POST"
        });
        if (res.status !== 200) {
          throw new VideoHelperError("Failed to get data from graphql");
        }
        const result = await res.json();
        const data = result.data.fetchVideoTranscript;
        if (data.__typename === "GenericError") {
          throw new VideoHelperError(data.message);
        }
        return {
          url: this.service.url + videoId,
          subtitles: [
            {
              format: "vtt",
              language: normalizeLang$1(data.language),
              source: "loom",
              url: data.captions_source_url
            }
          ]
        };
      } catch (err) {
        Logger.error(`Failed to get Loom video data, because: ${err.message}`);
        return this.returnBaseData(videoId);
      }
    }
    async getVideoId(url) {
      return /(embed|share)\/([^/]+)?/.exec(url.pathname)?.[2];
    }
  }
  class MailRuHelper extends BaseHelper {
    API_ORIGIN = "https://my.mail.ru";
    async getVideoMeta(videoId) {
      try {
        const res = await this.fetch(`${this.API_ORIGIN}/+/video/meta/${videoId}?xemail=&ajax_call=1&func_name=&mna=&mnb=&ext=1&_=${( new Date()).getTime()}`);
        return await res.json();
      } catch (err) {
        Logger.error("Failed to get mail.ru video data", err.message);
        return void 0;
      }
    }
    async getVideoId(url) {
      const pathname = url.pathname;
      if (/\/(v|mail|bk|inbox)\//.exec(pathname)) {
        return pathname.slice(1);
      }
      const videoId = /video\/embed\/([^/]+)/.exec(pathname)?.[1];
      if (!videoId) {
        return void 0;
      }
      const videoData2 = await this.getVideoMeta(videoId);
      if (!videoData2) {
        return void 0;
      }
      return videoData2.meta.url.replace("//my.mail.ru/", "");
    }
  }
  class NetacadHelper extends VideoJSHelper {
    SUBTITLE_SOURCE = "netacad";
    async getVideoData(videoId) {
      const data = this.getVideoDataByPlayer(videoId);
      if (!data) {
        return void 0;
      }
      const { url, duration, subtitles } = data;
      return {
        url: proxyMedia(new URL(url)),
        duration,
        subtitles
      };
    }
    async getVideoId(url) {
      return url.pathname + url.search;
    }
  }
  class NewgroundsHelper extends BaseHelper {
    async getVideoId(url) {
      return /([^/]+)\/(view)\/([^/]+)/.exec(url.pathname)?.[0];
    }
  }
  class NineGAGHelper extends BaseHelper {
    async getVideoData(videoId) {
      const data = this.returnBaseData(videoId);
      if (!data) {
        return data;
      }
      try {
        if (!this.video) {
          throw new Error("Video element not found");
        }
        const videoUrl = this.video.querySelector('source[type^="video/mp4"], source[type^="video/webm"]')?.src;
        if (!videoUrl || !/^https?:\/\//.test(videoUrl)) {
          throw new Error("Video source not found");
        }
        return {
          ...data,
          translationHelp: [
            {
              target: "video_file_url",
              targetUrl: videoUrl
            }
          ]
        };
      } catch {
        return data;
      }
    }
    async getVideoId(url) {
      return /gag\/([^/]+)/.exec(url.pathname)?.[1];
    }
  }
  class OdyseeHelper extends BaseHelper {
    API_ORIGIN = "https://odysee.com";
    async getVideoData(videoId) {
      try {
        const res = await this.fetch(`${this.API_ORIGIN}/${videoId}`);
        const content = await res.text();
        const url = /"contentUrl":(\s)?"([^"]+)"/.exec(content)?.[2];
        if (!url) {
          throw new VideoHelperError("Odysee url doesn't parsed");
        }
        return { url };
      } catch (err) {
        Logger.error(`Failed to get odysee video data by video ID: ${videoId}`, err.message);
        return void 0;
      }
    }
    async getVideoId(url) {
      return url.pathname.slice(1);
    }
  }
  class OKRuHelper extends BaseHelper {
    async getVideoId(url) {
      return /\/video\/(\d+)/.exec(url.pathname)?.[1];
    }
  }
  class OracleLearnHelper extends VideoJSHelper {
    SUBTITLE_SOURCE = "oraclelearn";
    async getVideoData(videoId) {
      const data = this.getVideoDataByPlayer(videoId);
      if (!data) {
        return void 0;
      }
      const { url, duration, subtitles } = data;
      const baseData = this.returnBaseData(videoId);
      const videoUrl = proxyMedia(new URL(url));
      if (!baseData) {
        return {
          url: videoUrl,
          duration,
          subtitles
        };
      }
      return {
        url: baseData.url,
        duration,
        subtitles,
        translationHelp: [
          {
            target: "video_file_url",
            targetUrl: videoUrl
          }
        ]
      };
    }
    async getVideoId(url) {
      return /\/ou\/course\/(([^/]+)\/(\d+)\/(\d+))/.exec(url.pathname)?.[1];
    }
  }
  class PatreonHelper extends BaseHelper {
    API_ORIGIN = "https://www.patreon.com/api";
    async getPosts(postId) {
      try {
        const res = await this.fetch(`${this.API_ORIGIN}/posts/${postId}?json-api-use-default-includes=false`);
        return await res.json();
      } catch (err) {
        Logger.error(`Failed to get patreon posts by postId: ${postId}.`, err.message);
        return false;
      }
    }
    async getVideoData(postId) {
      const postData = await this.getPosts(postId);
      if (!postData) {
        return void 0;
      }
      const postFileUrl = postData.data.attributes.post_file.url;
      if (!postFileUrl) {
        return void 0;
      }
      return {
        url: postFileUrl
      };
    }
    async getVideoId(url) {
      const fullPostId = /posts\/([^/]+)/.exec(url.pathname)?.[1];
      if (!fullPostId) {
        return void 0;
      }
      return fullPostId.replace(/[^\d.]/g, "");
    }
  }
  class PeertubeHelper extends BaseHelper {
    async getVideoId(url) {
      return /\/w\/([^/]+)/.exec(url.pathname)?.[0];
    }
  }
  class PornhubHelper extends BaseHelper {
    async getVideoId(url) {
      return url.searchParams.get("viewkey") ?? /embed\/([^/]+)/.exec(url.pathname)?.[1];
    }
  }
  class PornTNHelper extends BaseHelper {
    async getVideoData(videoId) {
      try {
        if (typeof flashvars === "undefined") {
          return void 0;
        }
        const { rnd, video_url: source, video_title: title } = flashvars;
        if (!source || !rnd) {
          throw new VideoHelperError("Failed to find video source or rnd");
        }
        const getFileUrl = new URL(source);
        getFileUrl.searchParams.append("rnd", rnd);
        Logger.log("PornTN get_file link", getFileUrl.href);
        const cdnResponse = await this.fetch(getFileUrl.href, { method: "head" });
        const cdnUrl = new URL(cdnResponse.url);
        Logger.log("PornTN cdn link", cdnUrl.href);
        const proxiedUrl = proxyMedia(cdnUrl);
        return {
          url: proxiedUrl,
          title
        };
      } catch (err) {
        Logger.error(`Failed to get PornTN data by videoId: ${videoId}`, err.message);
        return void 0;
      }
    }
    async getVideoId(url) {
      return /\/videos\/(([^/]+)\/([^/]+))/.exec(url.pathname)?.[1];
    }
  }
  class RedditHelper extends BaseHelper {
    API_ORIGIN = "https://www.reddit.com";
    async getContentUrl(_videoId) {
      if (this.service?.additionalData !== "old") {
        return document.querySelector("shreddit-player-2")?.src;
      }
      const playerEl = document.querySelector("[data-hls-url]");
      return playerEl?.dataset.hlsUrl?.replaceAll("&amp;", "&");
    }
    async getVideoData(videoId) {
      try {
        const contentUrl2 = await this.getContentUrl(videoId);
        if (!contentUrl2) {
          throw new VideoHelperError("Failed to find content url");
        }
        return {
          url: decodeURIComponent(contentUrl2)
        };
      } catch (err) {
        Logger.error(`Failed to get reddit video data by video ID: ${videoId}`, err.message);
        return void 0;
      }
    }
    async getVideoId(url) {
      return /\/r\/(([^/]+)\/([^/]+)\/([^/]+)\/([^/]+))/.exec(url.pathname)?.[1];
    }
  }
  class RtNewsHelper extends BaseHelper {
    async getVideoData(videoId) {
      const videoEl = document.querySelector(".jw-video, .media__video_noscript");
      if (!videoEl) {
        return void 0;
      }
      let videoSrc = videoEl.getAttribute("src");
      if (!videoSrc) {
        return void 0;
      }
      if (videoSrc.endsWith(".MP4")) {
        videoSrc = proxyMedia(videoSrc);
      }
      return {
        videoId,
        url: videoSrc
      };
    }
    async getVideoId(url) {
      return url.pathname.slice(1);
    }
  }
  class RumbleHelper extends BaseHelper {
    async getVideoId(url) {
      return url.pathname.slice(1);
    }
  }
  class RutubeHelper extends BaseHelper {
    async getVideoId(url) {
      return /(?:video|embed)\/([^/]+)/.exec(url.pathname)?.[1];
    }
  }
  class SapHelper extends BaseHelper {
    API_ORIGIN = "https://learning.sap.com/";
    async requestKaltura(kalturaDomain, partnerId, entryId) {
      const clientTag = "html5:v3.17.22";
      const apiVersion = "3.3.0";
      try {
        const res = await this.fetch(`https://${kalturaDomain}/api_v3/service/multirequest`, {
          method: "POST",
          body: JSON.stringify({
            "1": {
              service: "session",
              action: "startWidgetSession",
              widgetId: `_${partnerId}`
            },
            "2": {
              service: "baseEntry",
              action: "list",
              ks: "{1:result:ks}",
              filter: { redirectFromEntryId: entryId },
              responseProfile: {
                type: 1,
                fields: "id,referenceId,name,description,dataUrl,duration,flavorParamsIds,type,dvrStatus,externalSourceType,createdAt,updatedAt,endDate,plays,views,downloadUrl,creatorId"
              }
            },
            "3": {
              service: "baseEntry",
              action: "getPlaybackContext",
              entryId: "{2:result:objects:0:id}",
              ks: "{1:result:ks}",
              contextDataParams: {
                objectType: "KalturaContextDataParams",
                flavorTags: "all"
              }
            },
            apiVersion,
            format: 1,
            ks: "",
            clientTag,
            partnerId
          }),
          headers: {
            "Content-Type": "application/json"
          }
        });
        return await res.json();
      } catch (err) {
        Logger.error("Failed to request kaltura data", err.message);
        return void 0;
      }
    }
    async getKalturaData(videoId) {
      try {
        const scriptEl = document.querySelector('script[data-nscript="beforeInteractive"]');
        if (!scriptEl) {
          throw new VideoHelperError("Failed to find script element");
        }
        const sapData = /https:\/\/([^"]+)\/p\/([^"]+)\/embedPlaykitJs\/uiconf_id\/([^"]+)/.exec(scriptEl?.src);
        if (!sapData) {
          throw new VideoHelperError(`Failed to get sap data for videoId: ${videoId}`);
        }
        const [, kalturaDomain, partnerId] = sapData;
        let entryId = document.querySelector("#shadow")?.firstChild?.getAttribute("id");
        if (!entryId) {
          const nextDataEl = document.querySelector("#__NEXT_DATA__");
          if (!nextDataEl) {
            throw new VideoHelperError("Failed to find next data element");
          }
          entryId = /"sourceId":\s?"([^"]+)"/.exec(nextDataEl.innerText)?.[1];
        }
        if (!kalturaDomain || Number.isNaN(+partnerId) || !entryId) {
          throw new VideoHelperError(`One of the necessary parameters for getting a link to a sap video in wasn't found for ${videoId}. Params: kalturaDomain = ${kalturaDomain}, partnerId = ${partnerId}, entryId = ${entryId}`);
        }
        return await this.requestKaltura(kalturaDomain, partnerId, entryId);
      } catch (err) {
        Logger.error("Failed to get kaltura data", err.message);
        return void 0;
      }
    }
    async getVideoData(videoId) {
      const kalturaData = await this.getKalturaData(videoId);
      if (!kalturaData) {
        return void 0;
      }
      const [, baseEntryList, playbackContext] = kalturaData;
      const { duration } = baseEntryList.objects[0];
      const videoUrl = playbackContext.sources.find((source) => source.format === "url" && source.protocols === "http,https" && source.url.includes(".mp4"))?.url;
      if (!videoUrl) {
        return void 0;
      }
      const subtitles = playbackContext.playbackCaptions.map((caption) => {
        return {
          language: normalizeLang$1(caption.languageCode),
          source: "sap",
          format: "vtt",
          url: caption.webVttUrl,
          isAutoGenerated: caption.label.includes("auto-generated")
        };
      });
      return {
        url: videoUrl,
        subtitles,
        duration
      };
    }
    async getVideoId(url) {
      return /((courses|learning-journeys)\/([^/]+)(\/[^/]+)?)/.exec(url.pathname)?.[1];
    }
  }
  class TelegramHelper extends BaseHelper {
    static getMediaViewer() {
      if (typeof appMediaViewer === "undefined") {
        return void 0;
      }
      return appMediaViewer;
    }
    async getVideoId(_url) {
      const mediaViewer = TelegramHelper.getMediaViewer();
      if (!mediaViewer) {
        return void 0;
      }
      if (mediaViewer.live) {
        return void 0;
      }
      const message = mediaViewer.target.message;
      if (message.peer_id._ !== "peerChannel") {
        return void 0;
      }
      const media = message.media;
      if (media._ !== "messageMediaDocument") {
        return void 0;
      }
      if (media.document.type !== "video") {
        return void 0;
      }
      const postId = message.mid & 4294967295;
      const username = await mediaViewer.managers.appPeersManager.getPeerUsername(message.peerId);
      return `${username}/${postId}`;
    }
  }
  class ThisVidHelper extends BaseHelper {
    async getVideoId(url) {
      return /(videos|embed)\/[^/]+/.exec(url.pathname)?.[0];
    }
  }
  class TikTokHelper extends BaseHelper {
    async getVideoId(url) {
      return /([^/]+)\/video\/([^/]+)/.exec(url.pathname)?.[0];
    }
  }
  class TrovoHelper extends BaseHelper {
    async getVideoId(url) {
      const vid = url.searchParams.get("vid");
      const path = /([^/]+)\/([\d]+)/.exec(url.pathname)?.[0];
      if (!vid || !path) {
        return void 0;
      }
      return `${path}?vid=${vid}`;
    }
  }
  class TwitchHelper extends BaseHelper {
    API_ORIGIN = "https://clips.twitch.tv";
    async getClipLink(pathname, clipId) {
      const schema = document.querySelector("script[type='application/ld+json']");
      const clearPathname = pathname.slice(1);
      if (schema) {
        const schemaJSON = JSON.parse(schema.innerText);
        const channelLink2 = schemaJSON["@graph"].find((obj) => obj["@type"] === "VideoObject")?.creator.url;
        if (!channelLink2) {
          throw new VideoHelperError("Failed to find channel link");
        }
        const channelName2 = channelLink2.replace("https://www.twitch.tv/", "");
        return `${channelName2}/clip/${clearPathname}`;
      }
      const isEmbed = clearPathname === "embed";
      const channelLink = document.querySelector(isEmbed ? ".tw-link[data-test-selector='stream-info-card-component__stream-avatar-link']" : ".clips-player a:not([class])");
      if (!channelLink) {
        return void 0;
      }
      const channelName = channelLink.href.replace("https://www.twitch.tv/", "");
      return `${channelName}/clip/${isEmbed ? clipId : clearPathname}`;
    }
    async getVideoData(videoId) {
      const title = document.querySelector('[data-a-target="stream-title"], [data-test-selector="stream-info-card-component__subtitle"]')?.innerText;
      const isStream = !!document.querySelector('[data-a-target="animated-channel-viewers-count"], .channel-status-info--live, .top-bar--pointer-enabled .tw-channel-status-text-indicator');
      return {
        url: this.service.url + videoId,
        isStream,
        title
      };
    }
    async getVideoId(url) {
      const pathname = url.pathname;
      if (/^m\.twitch\.tv$/.test(pathname)) {
        return /videos\/([^/]+)/.exec(url.href)?.[0] ?? pathname.slice(1);
      } else if (/^player\.twitch\.tv$/.test(url.hostname)) {
        return `videos/${url.searchParams.get("video")}`;
      }
      const clipPath = /([^/]+)\/(?:clip)\/([^/]+)/.exec(pathname);
      if (clipPath) {
        return clipPath[0];
      }
      const isClipsDomain = /^clips\.twitch\.tv$/.test(url.hostname);
      if (isClipsDomain) {
        return await this.getClipLink(pathname, url.searchParams.get("clip"));
      }
      const videoPath = /(?:videos)\/([^/]+)/.exec(pathname);
      if (videoPath) {
        return videoPath[0];
      }
      const isUserOfflinePage = document.querySelector(".home-offline-hero .tw-link");
      if (isUserOfflinePage?.href) {
        const pageUrl = new URL(isUserOfflinePage.href);
        return /(?:videos)\/([^/]+)/.exec(pageUrl.pathname)?.[0];
      }
      return document.querySelector(".persistent-player") ? pathname : void 0;
    }
  }
  class TwitterHelper extends BaseHelper {
    async getVideoId(url) {
      const videoId = /status\/([^/]+)/.exec(url.pathname)?.[1];
      if (videoId) {
        return videoId;
      }
      const postEl = this.video?.closest('[data-testid="tweet"]');
      const newLink = postEl?.querySelector('a[role="link"][aria-label]')?.href;
      return newLink ? /status\/([^/]+)/.exec(newLink)?.[1] : void 0;
    }
  }
  class UdemyHelper extends BaseHelper {
    API_ORIGIN = "https://www.udemy.com/api-2.0";
    getModuleData() {
      const appLoaderEl = document.querySelector(".ud-app-loader[data-module-id='course-taking']");
      const moduleData = appLoaderEl?.dataset?.moduleArgs;
      if (!moduleData) {
        return void 0;
      }
      return JSON.parse(moduleData);
    }
    getLectureId() {
      return /learn\/lecture\/([^/]+)/.exec(window.location.pathname)?.[1];
    }
    isErrorData(data) {
      return Object.hasOwn(data, "error");
    }
    async getLectureData(courseId, lectureId) {
      try {
        const res = await this.fetch(`${this.API_ORIGIN}/users/me/subscribed-courses/${courseId}/lectures/${lectureId}/?` + new URLSearchParams({
          "fields[lecture]": "title,description,asset",
          "fields[asset]": "length,media_sources,captions"
        }).toString());
        const data = await res.json();
        if (this.isErrorData(data)) {
          throw new VideoHelperError(data.detail ?? "unknown error");
        }
        return data;
      } catch (err) {
        Logger.error(`Failed to get lecture data by courseId: ${courseId} and lectureId: ${lectureId}`, err.message);
        return void 0;
      }
    }
    async getCourseLang(courseId) {
      try {
        const res = await this.fetch(`${this.API_ORIGIN}/users/me/subscribed-courses/${courseId}?` + new URLSearchParams({
          "fields[course]": "locale"
        }).toString());
        const data = await res.json();
        if (this.isErrorData(data)) {
          throw new VideoHelperError(data.detail ?? "unknown error");
        }
        return data;
      } catch (err) {
        Logger.error(`Failed to get course lang by courseId: ${courseId}`, err.message);
        return void 0;
      }
    }
    findVideoUrl(sources) {
      return sources?.find((src) => src.type === "video/mp4")?.src;
    }
    findSubtitleUrl(captions, detectedLanguage) {
      const subtitle = captions?.find((caption) => normalizeLang$1(caption.locale_id) === detectedLanguage) ?? captions?.find((caption) => normalizeLang$1(caption.locale_id) === "en") ?? captions?.[0];
      return subtitle?.url;
    }
    async getVideoData(videoId) {
      const moduleData = this.getModuleData();
      if (!moduleData) {
        return void 0;
      }
      const { courseId } = moduleData;
      const lectureId = this.getLectureId();
      Logger.log(`[Udemy] courseId: ${courseId}, lectureId: ${lectureId}`);
      if (!lectureId) {
        return void 0;
      }
      const lectureData = await this.getLectureData(courseId, lectureId);
      if (!lectureData) {
        return void 0;
      }
      const { title, description, asset } = lectureData;
      const { length: duration, media_sources, captions } = asset;
      const videoUrl = this.findVideoUrl(media_sources);
      if (!videoUrl) {
        Logger.log("Failed to find .mp4 video file in media_sources", media_sources);
        return void 0;
      }
      let courseLang = "en";
      const courseLangData = await this.getCourseLang(courseId);
      if (courseLangData) {
        const { locale: { locale: courseLocale } } = courseLangData;
        courseLang = courseLocale ? normalizeLang$1(courseLocale) : courseLang;
      }
      if (!availableLangs.includes(courseLang)) {
        courseLang = "en";
      }
      const subtitleUrl = this.findSubtitleUrl(captions, courseLang);
      if (!subtitleUrl) {
        Logger.log("Failed to find subtitle file in captions", captions);
      }
      return {
        ...subtitleUrl ? {
          url: this.service?.url + videoId,
          translationHelp: [
            {
              target: "subtitles_file_url",
              targetUrl: subtitleUrl
            },
            {
              target: "video_file_url",
              targetUrl: videoUrl
            }
          ],
          detectedLanguage: courseLang
        } : {
          url: videoUrl,
          translationHelp: null
        },
        duration,
        title,
        description
      };
    }
    async getVideoId(url) {
      return url.pathname.slice(1);
    }
  }
  class VimeoHelper extends BaseHelper {
    API_KEY = "";
    DEFAULT_SITE_ORIGIN = "https://vimeo.com";
    SITE_ORIGIN = this.service?.url?.slice(0, -1) ?? this.DEFAULT_SITE_ORIGIN;
    isErrorData(data) {
      return Object.hasOwn(data, "error");
    }
    isPrivatePlayer() {
      return this.referer && !this.referer.includes("vimeo.com") && this.origin.endsWith("player.vimeo.com");
    }
    async getViewerData() {
      try {
        const res = await this.fetch("https://vimeo.com/_next/viewer");
        const data = await res.json();
        const { apiUrl, jwt } = data;
        this.API_ORIGIN = `https://${apiUrl}`;
        this.API_KEY = `jwt ${jwt}`;
        return data;
      } catch (err) {
        Logger.error(`Failed to get default viewer data.`, err.message);
        return false;
      }
    }
    async getVideoInfo(videoId) {
      try {
        const params = new URLSearchParams({
          fields: "name,link,description,duration"
        }).toString();
        const res = await this.fetch(`${this.API_ORIGIN}/videos/${videoId}?${params}`, {
          headers: {
            Authorization: this.API_KEY
          }
        });
        const data = await res.json();
        if (this.isErrorData(data)) {
          throw new Error(data.developer_message ?? data.error);
        }
        return data;
      } catch (err) {
        Logger.error(`Failed to get video info by video ID: ${videoId}`, err.message);
        return false;
      }
    }
    async getPrivateVideoSource(files) {
      try {
        const { default_cdn, cdns } = files.dash;
        const cdnUrl = cdns[default_cdn].url;
        const res = await this.fetch(cdnUrl);
        if (res.status !== 200) {
          throw new VideoHelperError(await res.text());
        }
        const data = await res.json();
        const baseUrl = new URL(data.base_url, cdnUrl);
        const videoData2 = data.audio.find((v2) => v2.mime_type === "audio/mp4" && v2.format === "dash");
        if (!videoData2) {
          throw new VideoHelperError("Failed to find video data");
        }
        const segmentUrl = videoData2.segments?.[0]?.url;
        if (!segmentUrl) {
          throw new VideoHelperError("Failed to find first segment url");
        }
        const [videoName, videoParams] = segmentUrl.split("?", 2);
        const params = new URLSearchParams(videoParams);
        params.delete("range");
        return new URL(`${videoData2.base_url}${videoName}?${params.toString()}`, baseUrl).href;
      } catch (err) {
        Logger.error(`Failed to get private video source`, err.message);
        return false;
      }
    }
    async getPrivateVideoInfo(videoId) {
      try {
        if (typeof playerConfig === "undefined") {
          return void 0;
        }
        const videoSource = await this.getPrivateVideoSource(playerConfig.request.files);
        if (!videoSource) {
          throw new VideoHelperError("Failed to get private video source");
        }
        const { video: { title, duration }, request: { text_tracks: subs } } = playerConfig;
        return {
          url: `${this.SITE_ORIGIN}/${videoId}`,
          video_url: videoSource,
          title,
          duration,
          subs
        };
      } catch (err) {
        Logger.error(`Failed to get private video info by video ID: ${videoId}`, err.message);
        return false;
      }
    }
    async getSubsInfo(videoId) {
      try {
        const params = new URLSearchParams({
          per_page: "100",
          fields: "language,type,link"
        }).toString();
        const res = await this.fetch(`${this.API_ORIGIN}/videos/${videoId}/texttracks?${params}`, {
          headers: {
            Authorization: this.API_KEY
          }
        });
        const content = await res.json();
        if (this.isErrorData(content)) {
          throw new Error(content.developer_message ?? content.error);
        }
        return content.data;
      } catch (err) {
        Logger.error(`Failed to get subtitles info by video ID: ${videoId}`, err.message);
        return [];
      }
    }
    async getVideoData(videoId) {
      const isPrivate = this.isPrivatePlayer();
      if (isPrivate) {
        const videoInfo2 = await this.getPrivateVideoInfo(videoId);
        if (!videoInfo2) {
          return void 0;
        }
        const { url: url2, subs, video_url, title: title2, duration: duration2 } = videoInfo2;
        const subtitles2 = subs.map((sub) => ({
          language: normalizeLang$1(sub.lang),
          source: "vimeo",
          format: "vtt",
          url: this.SITE_ORIGIN + sub.url,
          isAutoGenerated: sub.lang.includes("autogenerated")
        }));
        const translationHelp = subtitles2.length ? [
          { target: "video_file_url", targetUrl: video_url },
          { target: "subtitles_file_url", targetUrl: subtitles2[0].url }
        ] : null;
        return {
          ...translationHelp ? {
            url: url2,
            translationHelp
          } : {
            url: video_url
          },
          subtitles: subtitles2,
          title: title2,
          duration: duration2
        };
      }
      if (!this.extraInfo) {
        return this.returnBaseData(videoId);
      }
      if (videoId.includes("/")) {
        videoId = videoId.replace("/", ":");
      }
      const viewerData = await this.getViewerData();
      if (!viewerData) {
        return this.returnBaseData(videoId);
      }
      const videoInfo = await this.getVideoInfo(videoId);
      if (!videoInfo) {
        return this.returnBaseData(videoId);
      }
      const subsData = await this.getSubsInfo(videoId);
      const subtitles = subsData.map((caption) => ({
        language: normalizeLang$1(caption.language),
        source: "vimeo",
        format: "vtt",
        url: caption.link,
        isAutoGenerated: caption.language.includes("autogen")
      }));
      const { link: url, duration, name: title, description } = videoInfo;
      return {
        url,
        title,
        description,
        subtitles,
        duration
      };
    }
    async getVideoId(url) {
      const embedId = /video\/[^/]+$/.exec(url.pathname)?.[0];
      if (this.isPrivatePlayer()) {
        return embedId;
      }
      if (embedId) {
        const hash = url.searchParams.get("h");
        const videoId = embedId.replace("video/", "");
        return hash ? `${videoId}/${hash}` : videoId;
      }
      const categoriesVideoId = /channels\/[^/]+\/([^/]+)/.exec(url.pathname)?.[1] ?? /groups\/[^/]+\/videos\/([^/]+)/.exec(url.pathname)?.[1] ?? /(showcase|album)\/[^/]+\/video\/([^/]+)/.exec(url.pathname)?.[2];
      if (categoriesVideoId) {
        return categoriesVideoId;
      }
      return /([^/]+\/)?[^/]+$/.exec(url.pathname)?.[0];
    }
  }
  const sleep = (ms) => new Promise((resolve) => setTimeout(resolve, ms));
  class VKHelper extends BaseHelper {
    static getPlayer() {
      if (typeof Videoview === "undefined") {
        return void 0;
      }
      return Videoview?.getPlayerObject?.call(void 0);
    }
    async getVideoData(videoId) {
      const player2 = await this.waitForPlayerWithSubs();
      if (!player2) {
        return this.returnBaseData(videoId);
      }
      try {
        const { description: descriptionHTML, duration, md_title: title } = player2.vars;
        const parser = new DOMParser();
        const doc = parser.parseFromString(descriptionHTML, "text/html");
        const description = Array.from(doc.body.childNodes).filter((el) => el.nodeName !== "BR").map((el) => el.textContent).join("\n");
        let subtitles;
        if (Object.hasOwn(player2.vars, "subs")) {
          subtitles = player2.vars.subs.map((sub) => ({
            language: normalizeLang$1(sub.lang),
            source: "vk",
            format: "vtt",
            url: sub.url,
            isAutoGenerated: !!sub.is_auto
          }));
        }
        return {
          url: this.service.url + videoId,
          title,
          description,
          duration,
          subtitles
        };
      } catch (err) {
        Logger.error(`Failed to get VK video data, because: ${err.message}`);
        return this.returnBaseData(videoId);
      }
    }
    async waitForPlayerWithSubs(timeoutMs = 3e3) {
      const startedAt = Date.now();
      let lastPlayer;
      while (Date.now() - startedAt < timeoutMs) {
        const player2 = VKHelper.getPlayer();
        lastPlayer = player2;
        const subs = player2?.vars?.subs;
        if (Array.isArray(subs) && subs.length > 0) {
          return player2;
        }
        await sleep(200);
      }
      return lastPlayer;
    }
    async getVideoId(url) {
      const pathID = /^\/(video|clip)-?\d{8,9}_\d{9}$/.exec(url.pathname);
      if (pathID) {
        return pathID[0].slice(1);
      }
      const idInsidePlaylist = /\/playlist\/[^/]+\/(video-?\d{8,9}_\d{9})/.exec(url.pathname);
      if (idInsidePlaylist) {
        return idInsidePlaylist[1];
      }
      const paramZ = url.searchParams.get("z");
      if (paramZ) {
        return paramZ.split("/")[0];
      }
      const paramOID = url.searchParams.get("oid");
      const paramID = url.searchParams.get("id");
      if (paramOID && paramID) {
        return `video-${Math.abs(parseInt(paramOID))}_${paramID}`;
      }
      return void 0;
    }
  }
  class WatchPornToHelper extends BaseHelper {
    async getVideoId(url) {
      return /(video|embed)\/(\d+)(\/[^/]+\/)?/.exec(url.pathname)?.[0];
    }
  }
  class WeverseHelper extends BaseHelper {
    API_ORIGIN = "https://global.apis.naver.com/weverse/wevweb";
    API_APP_ID = "be4d79eb8fc7bd008ee82c8ec4ff6fd4";
    API_HMAC_KEY = "1b9cb6378d959b45714bec49971ade22e6e24e42";
    HEADERS = {
      Accept: "application/json, text/plain, */*",
      Origin: "https://weverse.io",
      Referer: "https://weverse.io/"
    };
    getURLData() {
      return {
        appId: this.API_APP_ID,
        language: "en",
        os: "WEB",
        platform: "WEB",
        wpf: "pc"
      };
    }
    async createHash(pathname) {
      const timestamp = Date.now();
      const salt = pathname.substring(0, Math.min(255, pathname.length)) + timestamp;
      const sign = await getHmacSha1(this.API_HMAC_KEY, salt);
      if (!sign) {
        throw new VideoHelperError("Failed to get weverse HMAC signature");
      }
      return {
        wmsgpad: timestamp.toString(),
        wmd: sign
      };
    }
    async getHashURLParams(pathname) {
      const hash = await this.createHash(pathname);
      return new URLSearchParams(hash).toString();
    }
    async getPostPreview(postId) {
      const pathname = `/post/v1.0/post-${postId}/preview?` + new URLSearchParams({
        fieldSet: "postForPreview",
        ...this.getURLData()
      }).toString();
      try {
        const urlParams = await this.getHashURLParams(pathname);
        const res = await this.fetch(this.API_ORIGIN + pathname + "&" + urlParams, {
          headers: this.HEADERS
        });
        return await res.json();
      } catch (err) {
        Logger.error(`Failed to get weverse post preview by postId: ${postId}`, err.message);
        return false;
      }
    }
    async getVideoInKey(videoId) {
      const pathname = `/video/v1.1/vod/${videoId}/inKey?` + new URLSearchParams({
        gcc: "RU",
        ...this.getURLData()
      }).toString();
      try {
        const urlParams = await this.getHashURLParams(pathname);
        const res = await this.fetch(this.API_ORIGIN + pathname + "&" + urlParams, {
          method: "POST",
          headers: this.HEADERS
        });
        return await res.json();
      } catch (err) {
        Logger.error(`Failed to get weverse InKey by videoId: ${videoId}`, err.message);
        return false;
      }
    }
    async getVideoInfo(infraVideoId, inkey, serviceId) {
      const timestamp = Date.now();
      try {
        const urlParams = new URLSearchParams({
          key: inkey,
          sid: serviceId,
          nonce: timestamp.toString(),
          devt: "html5_pc",
          prv: "N",
          aup: "N",
          stpb: "N",
          cpl: "en",
          env: "prod",
          lc: "en",
          adi: JSON.stringify([
            {
              adSystem: null
            }
          ]),
          adu: "/"
        }).toString();
        const res = await this.fetch(`https://global.apis.naver.com/rmcnmv/rmcnmv/vod/play/v2.0/${infraVideoId}?` + urlParams, {
          headers: this.HEADERS
        });
        return await res.json();
      } catch (err) {
        Logger.error(`Failed to get weverse video info (infraVideoId: ${infraVideoId}, inkey: ${inkey}, serviceId: ${serviceId}`, err.message);
        return false;
      }
    }
    extractVideoInfo(videoList) {
      return videoList.find((video) => video.useP2P === false && video.source.includes(".mp4"));
    }
    async getVideoData(videoId) {
      const videoPreview = await this.getPostPreview(videoId);
      if (!videoPreview) {
        return void 0;
      }
      const { videoId: internalVideoId, serviceId, infraVideoId } = videoPreview.extension.video;
      if (!(internalVideoId && serviceId && infraVideoId)) {
        return void 0;
      }
      const inkeyData = await this.getVideoInKey(internalVideoId);
      if (!inkeyData) {
        return void 0;
      }
      const videoInfo = await this.getVideoInfo(infraVideoId, inkeyData.inKey, serviceId);
      if (!videoInfo) {
        return void 0;
      }
      const videoItem = this.extractVideoInfo(videoInfo.videos.list);
      if (!videoItem) {
        return void 0;
      }
      return {
        url: videoItem.source,
        duration: videoItem.duration
      };
    }
    async getVideoId(url) {
      return /([^/]+)\/(live|media)\/([^/]+)/.exec(url.pathname)?.[3];
    }
  }
  class XVideosHelper extends BaseHelper {
    async getVideoId(url) {
      return /[^/]+\/[^/]+$/.exec(url.pathname)?.[0];
    }
  }
  class YandexDiskHelper extends BaseHelper {
    API_ORIGIN = window.location.origin;
    CLIENT_PREFIX = "/client/disk";
    INLINE_PREFIX = "/i/";
    DISK_PREFIX = "/d/";
    isErrorData(data) {
      return Object.hasOwn(data, "error");
    }
    async getClientVideoData(videoId) {
      const url = new URL(window.location.href);
      const dialogId = url.searchParams.get("idDialog");
      if (!dialogId) {
        return void 0;
      }
      const preloadedScript = document.querySelector("#preloaded-data");
      if (!preloadedScript) {
        return void 0;
      }
      try {
        const preloadedData = JSON.parse(preloadedScript.innerText);
        const { idClient, sk } = preloadedData.config;
        const res = await this.fetch(this.API_ORIGIN + "/models-v2?m=mpfs/info", {
          method: "POST",
          body: JSON.stringify({
            apiMethod: "mpfs/info",
            connection_id: idClient,
            requestParams: {
              path: dialogId
            },
            sk
          }),
          headers: {
            "Content-Type": "application/json"
          }
        });
        const data = await res.json();
        if (this.isErrorData(data)) {
          throw new VideoHelperError(data.error?.message ?? data.error?.code);
        }
        if (data?.type !== "file") {
          throw new VideoHelperError("Failed to get resource info");
        }
        const { meta: { short_url, video_info }, name } = data;
        if (!video_info) {
          throw new VideoHelperError("There's no video open right now");
        }
        if (!short_url) {
          throw new VideoHelperError("Access to the video is limited");
        }
        const title = this.clearTitle(name);
        const duration = Math.round(video_info.duration / 1e3);
        return {
          url: short_url,
          title,
          duration
        };
      } catch (err) {
        Logger.error(`Failed to get yandex disk video data by video ID: ${videoId}, because ${err.message}`);
        return void 0;
      }
    }
    clearTitle(title) {
      return title.replace(/(\.[^.]+)$/, "");
    }
    getBodyHash(fileHash, sk) {
      const data = JSON.stringify({
        hash: fileHash,
        sk
      });
      return encodeURIComponent(data);
    }
    async fetchList(dirHash, sk) {
      const body = this.getBodyHash(dirHash, sk);
      const res = await this.fetch(this.API_ORIGIN + "/public/api/fetch-list", {
        method: "POST",
        body
      });
      const data = await res.json();
      if (Object.hasOwn(data, "error")) {
        throw new VideoHelperError("Failed to fetch folder list");
      }
      return data.resources;
    }
    async getDownloadUrl(fileHash, sk) {
      const body = this.getBodyHash(fileHash, sk);
      const res = await this.fetch(this.API_ORIGIN + "/public/api/download-url", {
        method: "POST",
        body
      });
      const data = await res.json();
      if (data.error) {
        throw new VideoHelperError("Failed to get download url");
      }
      return data.data.url;
    }
    async getDiskVideoData(videoId) {
      try {
        const prefetchEl = document.getElementById("store-prefetch");
        if (!prefetchEl) {
          throw new VideoHelperError("Failed to get prefetch data");
        }
        const resourcePaths = videoId.split("/").slice(3);
        if (!resourcePaths.length) {
          throw new VideoHelperError("Failed to find video file path");
        }
        const data = JSON.parse(prefetchEl.innerText);
        const { resources, rootResourceId, environment: { sk } } = data;
        const rootResource = resources[rootResourceId];
        const resourcePathsLastIdx = resourcePaths.length - 1;
        const resourcePath = resourcePaths.filter((_2, idx) => idx !== resourcePathsLastIdx).join("/");
        let resourcesList = Object.values(resources);
        if (resourcePath.includes("/")) {
          resourcesList = await this.fetchList(`${rootResource.hash}:/${resourcePath}`, sk);
        }
        const resource = resourcesList.find((resource2) => resource2.name === resourcePaths[resourcePathsLastIdx]);
        if (!resource) {
          throw new VideoHelperError("Failed to find resource");
        }
        if (resource && resource.type === "dir") {
          throw new VideoHelperError("Path is dir, but expected file");
        }
        const { meta: { short_url, mediatype, videoDuration }, path, name } = resource;
        if (mediatype !== "video") {
          throw new VideoHelperError("Resource isn't a video");
        }
        const title = this.clearTitle(name);
        const duration = Math.round(videoDuration / 1e3);
        if (short_url) {
          return {
            url: short_url,
            duration,
            title
          };
        }
        const downloadUrl = await this.getDownloadUrl(path, sk);
        return {
          url: proxyMedia(new URL(downloadUrl)),
          duration,
          title
        };
      } catch (err) {
        Logger.error(`Failed to get yandex disk video data by disk video ID: ${videoId}`, err.message);
        return void 0;
      }
    }
    async getVideoData(videoId) {
      if (videoId.startsWith(this.INLINE_PREFIX) || /^\/d\/([^/]+)$/.exec(videoId)) {
        return {
          url: this.service.url + videoId.slice(1)
        };
      }
      videoId = decodeURIComponent(videoId);
      if (videoId.startsWith(this.CLIENT_PREFIX)) {
        return await this.getClientVideoData(videoId);
      }
      return await this.getDiskVideoData(videoId);
    }
    async getVideoId(url) {
      if (url.pathname.startsWith(this.CLIENT_PREFIX)) {
        return url.pathname + url.search;
      }
      const fileId = /\/i\/([^/]+)/.exec(url.pathname)?.[0];
      if (fileId) {
        return fileId;
      }
      return /\/d\/([^/]+)/.exec(url.pathname) ? url.pathname : void 0;
    }
  }
  class YoukuHelper extends BaseHelper {
    async getVideoId(url) {
      return /v_show\/id_[\w=]+/.exec(url.pathname)?.[0];
    }
  }
  let YoutubeHelper$1 = class YoutubeHelper extends BaseHelper {
    static isMobile() {
      return /^m\.youtube\.com$/.test(window.location.hostname);
    }
    static getPlayer() {
      if (window.location.pathname.startsWith("/shorts/") && !YoutubeHelper.isMobile()) {
        return document.querySelector("#shorts-player");
      }
      return document.querySelector("#movie_player");
    }
    static getPlayerResponse() {
      return YoutubeHelper.getPlayer()?.getPlayerResponse?.call(void 0);
    }
    static getPlayerData() {
      return YoutubeHelper.getPlayer()?.getVideoData?.call(void 0);
    }
    static getVolume() {
      const player2 = YoutubeHelper.getPlayer();
      if (player2?.getVolume) {
        return player2.getVolume() / 100;
      }
      return 1;
    }
    static setVolume(volume) {
      const player2 = YoutubeHelper.getPlayer();
      if (player2?.setVolume) {
        player2.setVolume(Math.round(volume * 100));
        return true;
      }
      return false;
    }
    static isMuted() {
      const player2 = YoutubeHelper.getPlayer();
      if (player2?.isMuted) {
        return player2.isMuted();
      }
      return false;
    }
    static videoSeek(video, time) {
      Logger.log("videoSeek", time);
      const preTime = YoutubeHelper.getPlayer()?.getProgressState()?.seekableEnd ?? video.currentTime;
      const finalTime = preTime - time;
      video.currentTime = finalTime;
    }
    static getPoToken() {
      const player2 = YoutubeHelper.getPlayer();
      if (!player2) {
        return void 0;
      }
      const audioTrack = player2.getAudioTrack?.call(void 0);
      if (!audioTrack?.captionTracks?.length) {
        return void 0;
      }
      const audioTrackWithPoToken = audioTrack.captionTracks.find((captionTrack) => captionTrack.url.includes("&pot="));
      if (!audioTrackWithPoToken) {
        return void 0;
      }
      return /&pot=([^&]+)/.exec(audioTrackWithPoToken.url)?.[1];
    }
    static getGlobalConfig() {
      if (typeof yt !== "undefined") {
        return yt?.config_;
      }
      return typeof ytcfg !== "undefined" ? ytcfg?.data_ : void 0;
    }
    static getDeviceParams() {
      const ytconfig = YoutubeHelper.getGlobalConfig();
      if (!ytconfig) {
        return "c=WEB";
      }
      const innertubeClient = ytconfig.INNERTUBE_CONTEXT?.client;
      const deviceParams = new URLSearchParams(ytconfig.DEVICE);
      deviceParams.delete("ceng");
      deviceParams.delete("cengver");
      deviceParams.set("c", innertubeClient?.clientName ?? ytconfig.INNERTUBE_CLIENT_NAME);
      deviceParams.set("cver", innertubeClient?.clientVersion ?? ytconfig.INNERTUBE_CLIENT_VERSION);
      deviceParams.set("cplayer", "UNIPLAYER");
      return deviceParams.toString();
    }
    static getSubtitles(userLang) {
      const response = YoutubeHelper.getPlayerResponse();
      const playerCaptions = response?.captions?.playerCaptionsTracklistRenderer;
      if (!playerCaptions) {
        return [];
      }
      const captionTracks = playerCaptions.captionTracks ?? [];
      const translationLanguages = playerCaptions.translationLanguages ?? [];
      const userLangSupported = translationLanguages.find((language) => language.languageCode === userLang);
      const asrSubtitleItem = captionTracks.find((captionTrack) => captionTrack?.kind === "asr");
      const asrLang = asrSubtitleItem?.languageCode ?? "en";
      const subtitles = captionTracks.reduce((result, captionTrack) => {
        if (!("languageCode" in captionTrack)) {
          return result;
        }
        const language = captionTrack.languageCode ? normalizeLang$1(captionTrack.languageCode) : void 0;
        const url = captionTrack.baseUrl;
        if (!language || !url) {
          return result;
        }
        const captionUrl = `${url.startsWith("http") ? url : `${window.location.origin}/${url}`}&fmt=json3`;
        result.push({
          source: "youtube",
          format: "json",
          language,
          isAutoGenerated: captionTrack?.kind === "asr",
          url: captionUrl
        });
        if (userLangSupported && captionTrack.isTranslatable && captionTrack.languageCode === asrLang && userLang !== language) {
          result.push({
            source: "youtube",
            format: "json",
            language: userLang,
            isAutoGenerated: captionTrack?.kind === "asr",
            translatedFromLanguage: language,
            url: `${captionUrl}&tlang=${userLang}`
          });
        }
        return result;
      }, []);
      Logger.log("youtube subtitles:", subtitles);
      return subtitles;
    }
    static getLanguage() {
      if (!YoutubeHelper.isMobile()) {
        const player2 = YoutubeHelper.getPlayer();
        const trackInfo = player2?.getAudioTrack?.call(void 0)?.getLanguageInfo();
        if (trackInfo && trackInfo.id !== "und") {
          return normalizeLang$1(trackInfo.id.split(".")[0]);
        }
      }
      const response = YoutubeHelper.getPlayerResponse();
      const autoCaption = response?.captions?.playerCaptionsTracklistRenderer.captionTracks.find((caption) => caption.kind === "asr" && caption.languageCode);
      return autoCaption ? normalizeLang$1(autoCaption.languageCode) : void 0;
    }
    async getVideoData(videoId) {
      const { title: localizedTitle } = YoutubeHelper.getPlayerData() ?? {};
      const { shortDescription: description, isLive: isStream, title } = YoutubeHelper.getPlayerResponse()?.videoDetails ?? {};
      const subtitles = YoutubeHelper.getSubtitles(this.language);
      let detectedLanguage = YoutubeHelper.getLanguage();
      if (detectedLanguage && !availableLangs.includes(detectedLanguage)) {
        detectedLanguage = void 0;
      }
      const duration = YoutubeHelper.getPlayer()?.getDuration?.call(void 0) ?? void 0;
      return {
        url: this.service.url + videoId,
        isStream,
        title,
        localizedTitle,
        detectedLanguage,
        description,
        subtitles,
        duration
      };
    }
    async getVideoId(url) {
      if (url.hostname === "youtu.be") {
        url.search = `?v=${url.pathname.replace("/", "")}`;
        url.pathname = "/watch";
      }
      if (url.searchParams.has("enablejsapi")) {
        const videoUrl = YoutubeHelper.getPlayer()?.getVideoUrl();
        url = videoUrl ? new URL(videoUrl) : url;
      }
      return /(?:watch|embed|shorts|live)\/([^/]+)/.exec(url.pathname)?.[1] ?? url.searchParams.get("v");
    }
  };
  const availableHelpers = {
    [VideoService.mailru]: MailRuHelper,
    [VideoService.weverse]: WeverseHelper,
    [VideoService.kodik]: KodikHelper,
    [VideoService.patreon]: PatreonHelper,
    [VideoService.reddit]: RedditHelper,
    [VideoService.bannedvideo]: BannedVideoHelper,
    [VideoService.kick]: KickHelper,
    [VideoService.appledeveloper]: AppleDeveloperHelper,
    [VideoService.epicgames]: EpicGamesHelper,
    [VideoService.odysee]: OdyseeHelper,
    [VideoService.coursehunterLike]: CoursehunterLikeHelper,
    [VideoService.twitch]: TwitchHelper,
    [VideoService.sap]: SapHelper,
    [VideoService.linkedin]: LinkedinHelper,
    [VideoService.vimeo]: VimeoHelper,
    [VideoService.yandexdisk]: YandexDiskHelper,
    [VideoService.vk]: VKHelper,
    [VideoService.trovo]: TrovoHelper,
    [VideoService.incestflix]: IncestflixHelper,
    [VideoService.porntn]: PornTNHelper,
    [VideoService.googledrive]: GoogleDriveHelper,
    [VideoService.bilibili]: BilibiliHelper,
    [VideoService.xvideos]: XVideosHelper,
    [VideoService.watchpornto]: WatchPornToHelper,
    [VideoService.archive]: ArchiveHelper,
    [VideoService.dailymotion]: DailymotionHelper,
    [VideoService.youku]: YoukuHelper,
    [VideoService.egghead]: EggheadHelper,
    [VideoService.newgrounds]: NewgroundsHelper,
    [VideoService.okru]: OKRuHelper,
    [VideoService.peertube]: PeertubeHelper,
    [VideoService.eporner]: EpornerHelper,
    [VideoService.bitchute]: BitchuteHelper,
    [VideoService.rutube]: RutubeHelper,
    [VideoService.facebook]: FacebookHelper,
    [VideoService.rumble]: RumbleHelper,
    [VideoService.twitter]: TwitterHelper,
    [VideoService.pornhub]: PornhubHelper,
    [VideoService.tiktok]: TikTokHelper,
    [VideoService.proxitok]: TikTokHelper,
    [VideoService.nine_gag]: NineGAGHelper,
    [VideoService.youtube]: YoutubeHelper$1,
    [VideoService.ricktube]: YoutubeHelper$1,
    [VideoService.invidious]: YoutubeHelper$1,
    [VideoService.poketube]: YoutubeHelper$1,
    [VideoService.piped]: YoutubeHelper$1,
    [VideoService.dzen]: DzenHelper,
    [VideoService.cloudflarestream]: CloudflareStreamHelper,
    [VideoService.loom]: LoomHelper,
    [VideoService.rtnews]: RtNewsHelper,
    [VideoService.bitview]: BitviewHelper,
    [VideoService.thisvid]: ThisVidHelper,
    [VideoService.ign]: IgnHelper,
    [VideoService.bunkr]: BunkrHelper,
    [VideoService.imdb]: IMDbHelper,
    [VideoService.telegram]: TelegramHelper,
    [ExtVideoService.udemy]: UdemyHelper,
    [ExtVideoService.coursera]: CourseraHelper,
    [ExtVideoService.douyin]: DouyinHelper,
    [ExtVideoService.artstation]: ArtstationHelper,
    [ExtVideoService.kickstarter]: KickstarterHelper,
    [ExtVideoService.oraclelearn]: OracleLearnHelper,
    [ExtVideoService.deeplearningai]: DeeplearningAIHelper,
    [ExtVideoService.netacad]: NetacadHelper
  };
  class VideoHelper {
    helpersData;
    constructor(helpersData = {}) {
      this.helpersData = helpersData;
    }
    getHelper(service) {
      return new availableHelpers[service](this.helpersData);
    }
  }
  function getService() {
    if (localLinkRe.exec(window.location.href)) {
      return [];
    }
    const hostname = window.location.hostname;
    const enteredURL = new URL(window.location.href);
    const isMatches = (match) => {
      if (match instanceof RegExp) {
        return match.test(hostname);
      } else if (typeof match === "string") {
        return hostname.includes(match);
      } else if (typeof match === "function") {
        return match(enteredURL);
      }
      return false;
    };
    return sites.filter((e2) => {
      return (Array.isArray(e2.match) ? e2.match.some(isMatches) : isMatches(e2.match)) && e2.host && e2.url;
    });
  }
  async function getVideoID(service, opts = {}) {
    const url = new URL(window.location.href);
    const serviceHost = service.host;
    if (Object.keys(availableHelpers).includes(serviceHost)) {
      const helper = new VideoHelper(opts).getHelper(serviceHost);
      return await helper.getVideoId(url);
    }
    return serviceHost === VideoService.custom ? url.href : void 0;
  }
  async function getVideoData(service, opts = {}) {
    const videoId = await getVideoID(service, opts);
    if (!videoId) {
      throw new VideoDataError(`Entered unsupported link: "${service.host}"`);
    }
    const origin = window.location.origin;
    if ([
      VideoService.peertube,
      VideoService.coursehunterLike,
      VideoService.cloudflarestream
    ].includes(service.host)) {
      service.url = origin;
    }
    if (service.rawResult) {
      return {
        url: videoId,
        videoId,
        host: service.host,
        duration: void 0
      };
    }
    if (!service.needExtraData) {
      return {
        url: service.url + videoId,
        videoId,
        host: service.host,
        duration: void 0
      };
    }
    const helper = new VideoHelper({
      ...opts,
      service,
      origin
    }).getHelper(service.host);
    const result = await helper.getVideoData(videoId);
    if (!result) {
      throw new VideoDataError(`Failed to get video raw url for ${service.host}`);
    }
    return {
      ...result,
      videoId,
      host: service.host
    };
  }
  const config = {
    version: "1.0.6",
    debug: false,
    fetchFn: fetch.bind(window)
  };
  const debug$1 = {
    log: (...text) => {
      if (!config.debug) {
        return;
      }
      return console.log(`%c✦ chaimu.js v${config.version} ✦`, "background: #000; color: #fff; padding: 0 8px", ...text);
    }
  };
  const videoLipSyncEvents = [
    "playing",
    "ratechange",
    "play",
    "waiting",
    "pause",
    "seeked"
  ];
  function initAudioContext() {
    const audioContext = window.AudioContext || window.webkitAudioContext;
    return audioContext ? new audioContext() : void 0;
  }
  class BasePlayer {
    static name = "BasePlayer";
    chaimu;
    fetch;
    _src;
    fetchOpts;
    constructor(chaimu, src) {
      this.chaimu = chaimu;
      this._src = src;
      this.fetch = this.chaimu.fetchFn;
      this.fetchOpts = this.chaimu.fetchOpts;
    }
    async init() {
      return this;
    }
    async clear() {
      return this;
    }
    lipSync(_mode = false) {
      return this;
    }
    handleVideoEvent = (event) => {
      debug$1.log(`handle video ${event.type}`);
      this.lipSync(event.type);
      return this;
    };
    removeVideoEvents() {
      for (const e2 of videoLipSyncEvents) {
        this.chaimu.video?.removeEventListener(e2, this.handleVideoEvent);
      }
      return this;
    }
    addVideoEvents() {
      for (const e2 of videoLipSyncEvents) {
        this.chaimu.video?.addEventListener(e2, this.handleVideoEvent);
      }
      return this;
    }
    async play() {
      return this;
    }
    async pause() {
      return this;
    }
    get name() {
      return this.constructor.name;
    }
    set src(url) {
      this._src = url;
    }
    get src() {
      return this._src;
    }
    get currentSrc() {
      return this._src;
    }
    set volume(_value) {
      return;
    }
    get volume() {
      return 0;
    }
    get playbackRate() {
      return 0;
    }
    set playbackRate(_value) {
      return;
    }
    get currentTime() {
      return 0;
    }
  }
  class AudioPlayer extends BasePlayer {
    static name = "AudioPlayer";
    audio;
    gainNode;
    audioSource;
    constructor(chaimu, src) {
      super(chaimu, src);
      this.updateAudio();
    }
    initAudioBooster() {
      if (!this.chaimu.audioContext) {
        return this;
      }
      this.disconnectAudioNodes();
      this.gainNode = this.chaimu.audioContext.createGain();
      this.gainNode.connect(this.chaimu.audioContext.destination);
      this.audioSource = this.chaimu.audioContext.createMediaElementSource(this.audio);
      this.audioSource.connect(this.gainNode);
      return this;
    }
    disconnectAudioNodes() {
      if (this.audioSource) {
        this.audioSource.disconnect();
        this.audioSource = void 0;
      }
      if (this.gainNode) {
        this.gainNode.disconnect();
        this.gainNode = void 0;
      }
    }
    updateAudio() {
      this.audio = new Audio(this.src);
      this.audio.crossOrigin = "anonymous";
      return this;
    }
    async init() {
      this.updateAudio();
      this.initAudioBooster();
      return this;
    }
    audioErrorHandle = (e2) => {
      if (e2 instanceof DOMException && e2.name === "NotAllowedError") {
        console.warn("[AudioPlayer] autoplay blocked, waiting for user gesture");
      } else {
        console.error("[AudioPlayer]", e2);
      }
    };
    lipSync(mode = false) {
      debug$1.log("[AudioPlayer] lipsync video", this.chaimu.video);
      if (!this.chaimu.video) {
        return this;
      }
      this.audio.currentTime = this.chaimu.video.currentTime;
      this.audio.playbackRate = this.chaimu.video.playbackRate;
      if (!mode) {
        debug$1.log("[AudioPlayer] lipsync mode isn't set");
        return this;
      }
      debug$1.log(`[AudioPlayer] lipsync mode is ${mode}`);
      switch (mode) {
        case "play":
        case "playing":
        case "seeked": {
          if (!this.chaimu.video.paused) {
            this.syncPlay();
          }
          return this;
        }
        case "pause":
        case "waiting": {
          void this.pause();
          return this;
        }
        default: {
          return this;
        }
      }
    }
    async clear() {
      this.audio.pause();
      this.audio.src = "";
      this.audio.removeAttribute("src");
      this.disconnectAudioNodes();
      return this;
    }
    syncPlay() {
      debug$1.log("[AudioPlayer] sync play called");
      if (this.audio) {
        this.audio.play().catch(this.audioErrorHandle);
      }
      return this;
    }
    async play() {
      debug$1.log("[AudioPlayer] play called");
      if (this.audio) {
        await this.audio.play().catch(this.audioErrorHandle);
      }
      return this;
    }
    async pause() {
      debug$1.log("[AudioPlayer] pause called");
      if (this.audio) {
        this.audio.pause();
      }
      return this;
    }
    set src(url) {
      this._src = url;
      if (!url) {
        void this.clear();
        return;
      }
      this.audio.src = url;
    }
    get src() {
      return this._src;
    }
    get currentSrc() {
      return this.audio.currentSrc;
    }
    set volume(value) {
      if (this.gainNode) {
        this.gainNode.gain.value = value;
        return;
      }
      this.audio.volume = value;
    }
    get volume() {
      return this.gainNode ? this.gainNode.gain.value : this.audio.volume;
    }
    get playbackRate() {
      return this.audio.playbackRate;
    }
    set playbackRate(value) {
      this.audio.playbackRate = value;
    }
    get currentTime() {
      return this.audio.currentTime;
    }
  }
  class ChaimuPlayer extends BasePlayer {
    static name = "ChaimuPlayer";
    audioBuffer;
    audioElement;
    mediaElementSource;
    gainNode;
    blobUrl;
    isClearing = false;
    isInitializing = false;
    clearingPromise;
    async fetchAudio() {
      if (!this._src) {
        throw new Error("No audio source provided");
      }
      if (!this.chaimu.audioContext) {
        throw new Error("No audio context available");
      }
      debug$1.log(`[ChaimuPlayer] Fetching audio from ${this._src}...`);
      let tempBlobUrl;
      try {
        const res = await this.fetch(this._src, this.fetchOpts);
        debug$1.log(`[ChaimuPlayer] Decoding fetched audio...`);
        const data = await res.arrayBuffer();
        const blob = new Blob([data]);
        tempBlobUrl = URL.createObjectURL(blob);
        this.audioBuffer = await this.chaimu.audioContext.decodeAudioData(data);
        if (this.blobUrl) {
          URL.revokeObjectURL(this.blobUrl);
        }
        this.blobUrl = tempBlobUrl;
        tempBlobUrl = void 0;
      } catch (err) {
        if (tempBlobUrl) {
          URL.revokeObjectURL(tempBlobUrl);
        }
        throw new Error(`Failed to fetch audio file, because ${err.message}`);
      }
      return this;
    }
    initAudioBooster() {
      if (!this.chaimu.audioContext) {
        return this;
      }
      this.disconnectAudioNodes();
      this.gainNode = this.chaimu.audioContext.createGain();
      return this;
    }
    disconnectAudioNodes() {
      if (this.mediaElementSource) {
        this.mediaElementSource.disconnect();
        this.mediaElementSource = void 0;
      }
      if (this.gainNode) {
        this.gainNode.disconnect();
        this.gainNode = void 0;
      }
    }
    async init() {
      if (this.isInitializing) {
        throw new Error("Initialization already in progress");
      }
      this.isInitializing = true;
      try {
        await this.fetchAudio();
        this.initAudioBooster();
        this.createAudioElement();
        return this;
      } finally {
        this.isInitializing = false;
      }
    }
    createAudioElement() {
      if (!this.chaimu.audioContext) {
        throw new Error("No audio context available");
      }
      if (!this.blobUrl) {
        throw new Error("No blob URL available.");
      }
      const audio = new Audio(this.blobUrl);
      audio.crossOrigin = "anonymous";
      if ("preservesPitch" in audio) {
        audio.preservesPitch = true;
        if ("mozPreservesPitch" in audio)
          audio.mozPreservesPitch = true;
        if ("webkitPreservesPitch" in audio)
          audio.webkitPreservesPitch = true;
      }
      this.audioElement = audio;
      this.mediaElementSource = this.chaimu.audioContext.createMediaElementSource(audio);
      this.mediaElementSource.connect(this.gainNode);
      this.gainNode.connect(this.chaimu.audioContext.destination);
    }
    lipSync(mode = false) {
      debug$1.log("[ChaimuPlayer] lipsync video", this.chaimu.video, this);
      if (!this.chaimu.video) {
        return this;
      }
      if (!mode) {
        debug$1.log("[ChaimuPlayer] lipsync mode isn't set");
        return this;
      }
      debug$1.log(`[ChaimuPlayer] lipsync mode is ${mode}`);
      switch (mode) {
        case "play":
        case "playing":
        case "ratechange":
        case "seeked": {
          if (!this.chaimu.video.paused) {
            void this.start();
          }
          return this;
        }
        case "pause":
        case "waiting": {
          void this.pause();
          return this;
        }
        default: {
          return this;
        }
      }
    }
    async reopenCtx() {
      if (!this.chaimu.audioContext) {
        throw new Error("No audio context available");
      }
      try {
        if (this.chaimu.audioContext.state !== "closed") {
          await this.chaimu.audioContext.close();
        }
      } catch (err) {
        debug$1.log("[ChaimuPlayer] Failed to close audio context:", err);
      }
      this.chaimu.audioContext = initAudioContext();
      return this;
    }
    async clear() {
      if (this.isClearing && this.clearingPromise) {
        return this.clearingPromise;
      }
      if (!this.chaimu.audioContext) {
        throw new Error("No audio context available");
      }
      debug$1.log("clear audio context");
      this.isClearing = true;
      this.clearingPromise = (async () => {
        try {
          await this.pause();
          if (this.audioElement) {
            this.audioElement.pause();
            this.audioElement = void 0;
          }
          if (this.blobUrl) {
            URL.revokeObjectURL(this.blobUrl);
            this.blobUrl = void 0;
          }
          this.disconnectAudioNodes();
          const oldVolume = this.gainNode ? this.gainNode.gain.value : 1;
          await this.reopenCtx();
          if (this.chaimu.audioContext) {
            this.initAudioBooster();
            this.volume = oldVolume;
          }
          return this;
        } finally {
          this.isClearing = false;
          this.clearingPromise = void 0;
        }
      })();
      return this.clearingPromise;
    }
    async start() {
      if (!this.chaimu.audioContext) {
        throw new Error("No audio context available");
      }
      if (!this.audioElement) {
        throw new Error("Audio element is missing");
      }
      if (this.isClearing && this.clearingPromise) {
        debug$1.log("The other cleaner is still running, waiting...");
        await this.clearingPromise;
      }
      debug$1.log("starting audio via HTMLAudioElement");
      await this.play();
      if (this.chaimu.video) {
        this.audioElement.currentTime = this.chaimu.video.currentTime;
        this.audioElement.playbackRate = this.chaimu.video.playbackRate;
      }
      this.audioElement.play().catch((err) => debug$1.log("[ChaimuPlayer] Play audioElement failed:", err));
      return this;
    }
    async pause() {
      if (!this.chaimu.audioContext) {
        throw new Error("No audio context available");
      }
      if (this.audioElement) {
        this.audioElement.pause();
      }
      if (this.chaimu.audioContext.state === "running") {
        await this.chaimu.audioContext.suspend();
      }
      return this;
    }
    async play() {
      if (!this.chaimu.audioContext) {
        throw new Error("No audio context available");
      }
      await this.chaimu.audioContext.resume();
      return this;
    }
    set src(url) {
      this._src = url;
    }
    get src() {
      return this._src;
    }
    get currentSrc() {
      return this._src;
    }
    set volume(value) {
      if (this.gainNode) {
        this.gainNode.gain.value = value;
      }
    }
    get volume() {
      return this.gainNode ? this.gainNode.gain.value : 0;
    }
    set playbackRate(value) {
      if (this.audioElement) {
        this.audioElement.playbackRate = value;
      }
    }
    get playbackRate() {
      return this.audioElement ? this.audioElement.playbackRate : this.chaimu.video?.playbackRate ?? 1;
    }
    get currentTime() {
      return this.chaimu.video?.currentTime ?? 0;
    }
  }
  const MAIN_BOOT_KEY = "__VOT_MAIN_BOOT_STATE__";
  function isBootstrapStatus(value) {
    return value === "idle" || value === "booting" || value === "booted" || value === "failed";
  }
  function isBootstrapState(value) {
    if (!value || typeof value !== "object") return false;
    const candidate = value;
    return isBootstrapStatus(candidate.status);
  }
  function getOrCreateBootState(bootKey = MAIN_BOOT_KEY) {
    const scope = globalThis;
    const existing = scope[bootKey];
    if (isBootstrapState(existing)) {
      return existing;
    }
    const created = {
      status: "idle",
      promise: null,
      error: null
    };
    scope[bootKey] = created;
    return created;
  }
  function asMatchedVideo(candidate) {
    return candidate instanceof HTMLVideoElement ? candidate : null;
  }
  function findMatchingVideoInRoot(root, selectors) {
    for (const selector of selectors) {
      try {
        const directMatch = asMatchedVideo(root.querySelector(selector));
        if (directMatch) {
          return directMatch;
        }
      } catch {
      }
    }
    return null;
  }
  function querySelectorExists(root, selector) {
    if (typeof root.querySelector !== "function") {
      return false;
    }
    try {
      return Boolean(root.querySelector(selector));
    } catch {
      return false;
    }
  }
  function nodeMatchesSelectors(node, selectors) {
    if (node.nodeType !== Node.ELEMENT_NODE && node.nodeType !== Node.DOCUMENT_FRAGMENT_NODE && node.nodeType !== Node.DOCUMENT_NODE) {
      return false;
    }
    const root = node;
    for (const selector of selectors) {
      if (node instanceof Element) {
        try {
          if (node.matches(selector)) {
            return true;
          }
        } catch {
        }
      }
      if (querySelectorExists(root, selector)) {
        return true;
      }
      if (node instanceof Element && node.shadowRoot) {
        if (querySelectorExists(node.shadowRoot, selector)) {
          return true;
        }
      }
    }
    return false;
  }
  function startLightweightVideoProbe(options) {
    let active = true;
    let observedRoot = null;
    let observer = null;
    let readyListener = null;
    let pollTimer = null;
    const selectors = options.selectors?.filter((selector) => String(selector || "").trim()) ?? [];
    const probeSelectors = selectors.length > 0 ? selectors : ["video"];
    const strategy = options.strategy ?? "mutation";
    const pollIntervalMs = Math.max(25, Math.trunc(options.pollIntervalMs ?? 75));
    const pollTimeoutMs = Math.max(
      250,
      Math.trunc(options.pollTimeoutMs ?? 4e3)
    );
    const pollDeadlineAt = Date.now() + pollTimeoutMs;
    const shouldLogMobileOverlay2 = /^(m|music)\.youtube\.com$/i.test(
      String(globalThis.location.hostname || "")
    );
    const cleanup = () => {
      if (!active) {
        return;
      }
      if (shouldLogMobileOverlay2) {
        console.log(
          "[VOT][mobile-overlay][probe] cleanup lightweight video probe",
          {
            strategy
          }
        );
      }
      active = false;
      observer?.disconnect();
      observer = null;
      observedRoot = null;
      if (pollTimer !== null) {
        clearTimeout(pollTimer);
        pollTimer = null;
      }
      if (readyListener) {
        document.removeEventListener("readystatechange", readyListener);
        readyListener = null;
      }
    };
    const activate = (reason, video) => {
      if (!active) {
        return;
      }
      if (shouldLogMobileOverlay2) {
        console.log(
          "[VOT][mobile-overlay][probe] activate lightweight video probe",
          {
            reason,
            hasVideo: Boolean(video),
            src: video?.currentSrc || video?.src || ""
          }
        );
      }
      cleanup();
      options.onVideoDetected(reason, video);
    };
    const inspectExistingTree = () => {
      const matchedVideo = findMatchingVideoInRoot(document, probeSelectors);
      if (matchedVideo) {
        activate("probe-existing-video", matchedVideo);
        return true;
      }
      if (nodeMatchesSelectors(document, probeSelectors)) {
        activate("probe-existing-video");
        return true;
      }
      return false;
    };
    const schedulePoll = () => {
      if (!active) {
        return;
      }
      if (inspectExistingTree()) {
        return;
      }
      if (Date.now() >= pollDeadlineAt) {
        cleanup();
        options.onProbeExhausted?.("probe-timeout");
        return;
      }
      pollTimer = globalThis.setTimeout(schedulePoll, pollIntervalMs);
    };
    const ensureObserver = () => {
      if (!active || observer) {
        return;
      }
      observer = new MutationObserver((mutations) => {
        for (const mutation of mutations) {
          if (mutation.type !== "childList") {
            continue;
          }
          for (const node of mutation.addedNodes) {
            if (node instanceof HTMLVideoElement) {
              activate("probe-video-added", node);
              return;
            }
            if (node instanceof Document || node instanceof DocumentFragment || node instanceof Element) {
              const matchedVideo = findMatchingVideoInRoot(node, probeSelectors);
              if (matchedVideo) {
                activate("probe-video-added", matchedVideo);
                return;
              }
            }
            if (nodeMatchesSelectors(node, probeSelectors)) {
              activate("probe-video-added");
              return;
            }
          }
        }
      });
      const root = document.documentElement;
      if (!root) {
        readyListener = () => {
          if (!active || observedRoot) {
            return;
          }
          const nextRoot = document.documentElement;
          if (!nextRoot) {
            return;
          }
          if (readyListener) {
            document.removeEventListener("readystatechange", readyListener);
          }
          readyListener = null;
          observedRoot = nextRoot;
          if (strategy === "mutation") {
            observer?.observe(nextRoot, { childList: true, subtree: true });
            inspectExistingTree();
            return;
          }
          schedulePoll();
        };
        document.addEventListener("readystatechange", readyListener);
        return;
      }
      observedRoot = root;
      if (strategy === "mutation") {
        observer.observe(root, { childList: true, subtree: true });
        return;
      }
      schedulePoll();
    };
    if (!inspectExistingTree()) {
      ensureObserver();
    }
    return cleanup;
  }
  const workerHost = "api.browser.yandex.ru";
  const m3u8ProxyHost = "media-proxy.toil.cc/v1/proxy/m3u8";
  const proxyWorkerHost = "vot-worker.kload.workers.dev";
  const votBackendUrl = "https://vot.toil.cc/v1";
  const foswlyTranslateUrl = "https://translate-backend.transly.workers.dev/v2";
  const detectRustServerUrl = "https://rust-server-531j.onrender.com/detect";
  const yandexOauthClientId = "b4dd2893e5ec40bf835cc0615b2992e3";
  const authCallbackOrigin = "https://oauth.yandex.ru";
  const authCallbackPath = "/verification_code";
  const authCallbackUrl = `${authCallbackOrigin}${authCallbackPath}`;
  const avatarServerUrl = "https://avatars.mds.yandex.net/get-yapic";
  const repoPath = "Acobat12/voice-over-translation-direct";
  const contentUrl = `https://raw.githubusercontent.com/${repoPath}`;
  const repositoryUrl = `https://github.com/${repoPath}`;
  const defaultAutoVolume = 15;
  const maxAudioVolume = 900;
  const minLongWaitingCount = 5;
  const defaultTranslationService = "yandexbrowser";
  const defaultDetectService = "yandexbrowser";
  const nonProxyExtensions = ["Tampermonkey", "Violentmonkey"];
  const proxyOnlyCountries = ["UA", "LV", "LT"];
  const defaultAutoHideDelay = 1e3;
  const actualCompatVersion = "2025-05-09";
  const storageKeys = [
    "autoTranslate",
    "autoSubtitles",
    "dontTranslateLanguages",
    "enabledDontTranslateLanguages",
    "enabledAutoVolume",
    "enabledSmartDucking",
    "autoVolume",
    "buttonPos",
    "showVideoSlider",
    "syncVolume",
    "downloadWithName",
    "sendNotifyOnComplete",
    "subtitlesMaxLength",
    "subtitlesSmartLayout",
    "highlightWords",
    "subtitlesFontSize",
    "subtitlesFontFamily",
    "subtitlesOpacity",
    "subtitlesDownloadFormat",
    "responseLanguage",
    "defaultVolume",
    "onlyBypassMediaCSP",
    "newAudioPlayer",
    "showPiPButton",
    "translateAPIErrors",
    "translationService",
    "detectService",
    "translationHotkey",
    "subtitlesHotkey",
    "m3u8ProxyHost",
    "proxyWorkerHost",
    "translateProxyEnabled",
    "translateProxyEnabledDefault",
    "audioBooster",
    "useLivelyVoice",
    "autoHideButtonDelay",
    "useAudioDownload",
    "compatVersion",
    "localePhrases",
    "localeLang",
    "localeHash",
    "localeVersion",
    "localeUpdatedAt",
    "localeLangOverride",
    "account"
  ];
  const log = (...text) => {
    console.log(
      "%c[VOT DEBUG]",
      "background: #3700ffff; color: #fff; padding: 5px;",
      ...text
    );
  };
  const warn = (...text) => {
    console.warn(
      "%c[VOT DEBUG]",
      "background: #e1ff00ff; color: #fff; padding: 5px;",
      ...text
    );
  };
  const error = (...text) => {
    console.error(
      "%c[VOT DEBUG]",
      "background: #F2452D; color: #fff; padding: 5px;",
      ...text
    );
  };
  const debug = { log, warn, error };
  function getNavigatorLang() {
    return navigator.language?.substring(0, 2).toLowerCase() || "en";
  }
  const slavicLangs = new Set([
    "uk",
    "be",
    "bg",
    "mk",
    "sr",
    "bs",
    "hr",
    "sl",
    "pl",
    "sk",
    "cs"
  ]);
  function resolveCalculatedResLang(baseLang2) {
    if (availableTTS.includes(baseLang2)) {
      return baseLang2;
    }
    if (slavicLangs.has(baseLang2)) {
      return "ru";
    }
    return "en";
  }
  const lang = getNavigatorLang();
  const calculatedResLang = resolveCalculatedResLang(lang);
  const DEFAULT_OBJECT_URL_REVOKE_DELAY_MS = 3e4;
  const INVALID_FILENAME_CHARS_RE = /[\\/:*?"'<>|]+/g;
  const URL_PROTOCOL_RE = /^https?:\/\//i;
  const MULTIPLE_DASHES_RE = /-{2,}/g;
  const EDGE_FILE_CHARS_RE = /^[.\s-]+|[.\s-]+$/g;
  function getDateFallbackFilename() {
    return ( new Date()).toISOString().slice(0, 10);
  }
  function stripAsciiControlChars(value) {
    let out = "";
    for (let index = 0; index < value.length; index++) {
      const code = value.codePointAt(index) ?? 0;
      if (code >= 32 && code !== 127) {
        out += value[index];
      }
    }
    return out;
  }
  function stableStringify(value) {
    const seen = new WeakSet();
    return JSON.stringify(value, (_key, val) => {
      if (val && typeof val === "object") {
        if (seen.has(val)) {
          return "[Circular]";
        }
        seen.add(val);
        if (Array.isArray(val)) {
          return val;
        }
        const sorted = {};
        const keys = Object.keys(val).sort(
          (a2, b2) => a2.localeCompare(b2)
        );
        for (const key of keys) {
          sorted[key] = val[key];
        }
        return sorted;
      }
      return val;
    });
  }
  function fnv1a32ToKeyPart(str) {
    let hash = 2166136261;
    let i2 = 0;
    while (i2 < str.length) {
      const codePoint = str.codePointAt(i2) ?? 0;
      hash ^= codePoint;
      hash = Math.imul(hash, 16777619);
      i2 += codePoint > 65535 ? 2 : 1;
    }
    return (hash >>> 0).toString(36);
  }
  const isPiPAvailable = () => Boolean(document.pictureInPictureEnabled);
  async function writeBlobToHandle(handle, blob) {
    try {
      const writable = await handle.createWritable();
      await writable.write(blob);
      await writable.close();
      return true;
    } catch {
      return false;
    }
  }
  async function shareBlob(blob, filename) {
    const nav = typeof navigator === "undefined" ? void 0 : navigator;
    if (!nav?.share || typeof File === "undefined") {
      return "unsupported";
    }
    let file;
    try {
      file = new File([blob], filename, {
        type: blob.type || "application/octet-stream"
      });
    } catch {
      return "unsupported";
    }
    if (typeof nav.canShare === "function" && !nav.canShare({ files: [file] })) {
      return "unsupported";
    }
    try {
      await nav.share({ files: [file], title: filename });
      return "shared";
    } catch (err) {
      if (err instanceof DOMException && err.name === "AbortError") {
        return "shared";
      }
      return "error";
    }
  }
  function triggerBlobDownload(blob, filename) {
    const url = URL.createObjectURL(blob);
    const anchor = document.createElement("a");
    anchor.href = url;
    anchor.download = filename;
    anchor.rel = "noopener noreferrer";
    anchor.target = "_blank";
    anchor.style.position = "fixed";
    anchor.style.left = "-9999px";
    anchor.style.top = "0";
    (document.body ?? document.documentElement).append(anchor);
    try {
      anchor.click();
      return true;
    } catch {
      return false;
    } finally {
      anchor.remove();
      revokeObjectUrlLater(url);
    }
  }
  async function downloadBlob(blob, filename, options = {}) {
    if (options.fileHandle) {
      const saved = await writeBlobToHandle(options.fileHandle, blob);
      if (saved) {
        return true;
      }
    }
    if (options.preferShare) {
      const shareResult = await shareBlob(blob, filename);
      if (shareResult === "shared") {
        return true;
      }
    }
    return triggerBlobDownload(blob, filename);
  }
  function revokeObjectUrlLater(url, delayMs = DEFAULT_OBJECT_URL_REVOKE_DELAY_MS) {
    const safeDelayMs = Number.isFinite(delayMs) && delayMs >= 0 ? delayMs : DEFAULT_OBJECT_URL_REVOKE_DELAY_MS;
    globalThis.setTimeout(() => URL.revokeObjectURL(url), safeDelayMs);
  }
  function clearFileName(filename) {
    const trimmed = filename.trim();
    if (!trimmed) return getDateFallbackFilename();
    const sanitized = stripAsciiControlChars(trimmed).replace(URL_PROTOCOL_RE, "").replaceAll(INVALID_FILENAME_CHARS_RE, "-").replaceAll(MULTIPLE_DASHES_RE, "-").replaceAll(EDGE_FILE_CHARS_RE, "");
    return sanitized || getDateFallbackFilename();
  }
  const getTimestamp = () => Math.floor(Date.now() / 1e3);
  const getHeaders = (headers) => headers ? Object.fromEntries(new Headers(headers).entries()) : {};
  function clamp$2(value, min = 0, max = 100) {
    const lower = Math.min(min, max);
    const upper = Math.max(min, max);
    return Math.min(Math.max(value, lower), upper);
  }
  function toFlatObj(data) {
    const out = {};
    const stack = Object.entries(data);
    while (stack.length) {
      const entry = stack.pop();
      if (!entry) continue;
      const [key, val] = entry;
      if (val === void 0) continue;
      const isFlattenable = val !== null && typeof val === "object" && !Array.isArray(val);
      if (!isFlattenable) {
        out[key] = val;
        continue;
      }
      for (const [k2, v2] of Object.entries(val)) {
        stack.push([`${key}.${k2}`, v2]);
      }
    }
    return out;
  }
  const YANDEX_TTL_MS = 2 * 60 * 60 * 1e3;
  const RESPONSE_CACHE_CREATED_AT_HEADER = "x-vot-cache-created-at";
  const RESPONSE_CACHE_KEY_HEADER = "x-vot-cache-key";
  const DEFAULT_RESPONSE_CACHE_NAME = "vot-http-cache-v1";
  const MAX_MEMORY_CACHE_ENTRIES = 500;
  const VOT_SESSION_STORAGE_KEY = "VOTSession";
  const EXPIRY_FIELD_BY_FIELD = {
    translation: "translationExpiresAt",
    subtitles: "subtitlesExpiresAt"
  };
  function getCurrentUnixTimestampSeconds() {
    return Math.floor(Date.now() / 1e3);
  }
  function isVOTSession(value) {
    if (!value || typeof value !== "object") {
      return false;
    }
    const candidate = value;
    return typeof candidate.expires === "number" && Number.isFinite(candidate.expires) && typeof candidate.timestamp === "number" && Number.isFinite(candidate.timestamp) && typeof candidate.uuid === "string" && candidate.uuid.length > 0 && typeof candidate.secretKey === "string" && candidate.secretKey.length > 0;
  }
  function sanitizeVOTSessions(value) {
    if (!value || typeof value !== "object") {
      return {};
    }
    const now2 = getCurrentUnixTimestampSeconds();
    const entries = Object.entries(value).flatMap(
      ([module, session]) => {
        if (!isVOTSession(session)) {
          return [];
        }
        if (session.timestamp + session.expires <= now2) {
          return [];
        }
        return [[module, session]];
      }
    );
    return Object.fromEntries(entries);
  }
  function isStoredVOTSession(value) {
    if (!value || typeof value !== "object") {
      return false;
    }
    const candidate = value;
    return typeof candidate.expiresAt === "number" && Number.isFinite(candidate.expiresAt) && typeof candidate.secretKey === "string" && candidate.secretKey.length > 0 && typeof candidate.uuid === "string" && candidate.uuid.length > 0;
  }
  class VOTSessionStorageCache {
    constructor(storage = votStorage) {
      this.storage = storage;
    }
    getStorageKey(host) {
      return VOT_SESSION_STORAGE_KEY;
    }
    async restore(host, currentSessions = {}) {
      const storageKey = this.getStorageKey(host);
      const rawStoredSession = await this.storage.getRaw(storageKey);
      if (!isStoredVOTSession(rawStoredSession)) {
        return currentSessions;
      }
      const nowMs = Date.now();
      if (rawStoredSession.expiresAt <= nowMs) {
        await this.storage.deleteRaw(storageKey);
        return currentSessions;
      }
      const remainingSeconds = Math.max(
        1,
        Math.ceil((rawStoredSession.expiresAt - nowMs) / 1e3)
      );
      return {
        ...currentSessions,
        "video-translation": {
          secretKey: rawStoredSession.secretKey,
          uuid: rawStoredSession.uuid,
          expires: remainingSeconds,
          timestamp: Math.floor(nowMs / 1e3)
        }
      };
    }
    async persist(host, sessions) {
      const storageKey = this.getStorageKey(host);
      const translationSession = sanitizeVOTSessions(sessions)["video-translation"];
      if (!translationSession) {
        await this.storage.deleteRaw(storageKey);
        return;
      }
      await this.storage.setRaw(storageKey, {
        secretKey: translationSession.secretKey,
        uuid: translationSession.uuid,
        expiresAt: (translationSession.timestamp + translationSession.expires) * 1e3
      });
    }
  }
  class InMemoryCacheManager {
    cache = new Map();
clear() {
      this.cache.clear();
    }
    getTranslation(key) {
      return this.getValue(key, "translation");
    }
    setTranslation(key, translation) {
      this.setValue(key, "translation", translation);
    }
    getSubtitles(key) {
      return this.getValue(key, "subtitles");
    }
    setSubtitles(key, subtitles) {
      this.setValue(key, "subtitles", subtitles);
    }
    deleteSubtitles(key) {
      this.deleteValue(key, "subtitles");
    }
    getValue(key, field) {
      const now2 = Date.now();
      const entry = this.cache.get(key);
      if (!entry) return void 0;
      const expiryField = EXPIRY_FIELD_BY_FIELD[field];
      const expiresAt = entry[expiryField];
      if (expiresAt !== void 0 && expiresAt <= now2) {
        entry[field] = void 0;
        entry[expiryField] = void 0;
        this.evictIfEmpty(key, entry);
        return void 0;
      }
      return entry[field];
    }
    setValue(key, field, value) {
      const now2 = Date.now();
      const entry = this.getOrCreateEntry(key);
      const expiresAt = now2 + YANDEX_TTL_MS;
      const expiryField = EXPIRY_FIELD_BY_FIELD[field];
      entry[field] = value;
      entry[expiryField] = expiresAt;
    }
    deleteValue(key, field) {
      const entry = this.cache.get(key);
      if (!entry) return;
      const expiryField = EXPIRY_FIELD_BY_FIELD[field];
      entry[field] = void 0;
      entry[expiryField] = void 0;
      this.evictIfEmpty(key, entry);
    }
    evictIfEmpty(key, entry) {
      if (entry.translation === void 0 && entry.subtitles === void 0) {
        this.cache.delete(key);
      }
    }
    getOrCreateEntry(key) {
      const existing = this.cache.get(key);
      if (existing) return existing;
      const entry = {};
      this.cache.set(key, entry);
      return entry;
    }
  }
  class ResponseCacheManager {
    memoryCache = new Map();
    inFlightRequests = new Map();
    async execute(context, options, fetcher) {
      if (!options || options.ttlMs <= 0) {
        return fetcher();
      }
      const key = options.key ?? this.buildDefaultCacheKey(context);
      if (!key) {
        return fetcher();
      }
      const method = this.normalizeMethod(context.method);
      const ttlMs = options.ttlMs;
      const cacheName = options.cacheName || DEFAULT_RESPONSE_CACHE_NAME;
      const useMemory = options.useMemory !== false;
      const useCacheApi = options.useCacheApi !== false && method === "GET" && this.supportsCacheApi();
      const cacheApiKey = useCacheApi ? fnv1a32ToKeyPart(key) : "";
      const dedupe = options.dedupe !== false;
      const allowStaleOnError = options.allowStaleOnError !== false;
      const nowMs = Date.now();
      let staleFallback;
      if (useMemory) {
        const memoryHit = this.readMemoryCache(key, nowMs);
        if (memoryHit.fresh) {
          return memoryHit.fresh;
        }
        staleFallback = memoryHit.stale ?? staleFallback;
      }
      if (useCacheApi) {
        const cacheApiHit = await this.readCacheApi(
          cacheName,
          context.url,
          cacheApiKey,
          ttlMs,
          nowMs,
          allowStaleOnError
        );
        if (cacheApiHit.fresh) {
          if (useMemory) {
            this.writeMemoryCache(
              key,
              cacheApiHit.fresh.clone(),
              cacheApiHit.expiresAt ?? nowMs + ttlMs
            );
          }
          return cacheApiHit.fresh;
        }
        staleFallback = staleFallback ?? cacheApiHit.stale;
      }
      const runNetworkRequest = async () => {
        const response = await fetcher();
        if (!response.ok) {
          return response;
        }
        const createdAtMs = Date.now();
        const expiresAt = this.computeExpiresAt(createdAtMs, ttlMs);
        if (useMemory) {
          this.writeMemoryCache(key, response.clone(), expiresAt);
        }
        if (useCacheApi) {
          const storable = this.toStorableResponse(response.clone(), createdAtMs);
          await this.writeCacheApi(cacheName, context.url, cacheApiKey, storable);
        }
        return response;
      };
      if (!dedupe) {
        try {
          return await runNetworkRequest();
        } catch (err) {
          if (allowStaleOnError && staleFallback) {
            return staleFallback;
          }
          throw err;
        }
      }
      const inFlight = this.inFlightRequests.get(key);
      if (inFlight !== void 0) {
        return (await inFlight).clone();
      }
      const networkPromise = (async () => {
        try {
          return await runNetworkRequest();
        } catch (err) {
          if (allowStaleOnError && staleFallback) {
            return staleFallback.clone();
          }
          throw err;
        }
      })();
      this.inFlightRequests.set(key, networkPromise);
      try {
        return (await networkPromise).clone();
      } finally {
        this.inFlightRequests.delete(key);
      }
    }
    computeExpiresAt(createdAtMs, ttlMs) {
      if (!Number.isFinite(ttlMs) || ttlMs <= 0) {
        return createdAtMs;
      }
      const maxAdd = Number.MAX_SAFE_INTEGER - createdAtMs;
      if (ttlMs >= maxAdd) {
        return Number.MAX_SAFE_INTEGER;
      }
      return createdAtMs + ttlMs;
    }
    normalizeMethod(method) {
      return (method || "GET").toUpperCase();
    }
    resolveBodyKey(body) {
      if (body == null) return "";
      if (typeof body === "string") return body;
      if (body instanceof URLSearchParams) return body.toString();
      return void 0;
    }
    buildDefaultCacheKey(context) {
      const method = this.normalizeMethod(context.method);
      if (method === "GET" || method === "HEAD") {
        return `${method}:${context.url}`;
      }
      const bodyKey = this.resolveBodyKey(context.body);
      if (bodyKey === void 0) return void 0;
      return `${method}:${context.url}#${fnv1a32ToKeyPart(bodyKey)}`;
    }
    supportsCacheApi() {
      return typeof caches !== "undefined" && typeof caches.open === "function";
    }
    readCreatedAtMs(response) {
      const raw = response.headers.get(RESPONSE_CACHE_CREATED_AT_HEADER);
      if (!raw) return null;
      const value = Number(raw);
      return Number.isFinite(value) ? value : null;
    }
    ensureVaryByCacheKey(headers) {
      const varyRaw = headers.get("vary");
      if (!varyRaw) {
        headers.set("vary", RESPONSE_CACHE_KEY_HEADER);
        return;
      }
      const varyParts = new Set(
        varyRaw.split(",").map((part) => part.trim().toLowerCase())
      );
      if (!varyParts.has("*") && !varyParts.has(RESPONSE_CACHE_KEY_HEADER)) {
        headers.set("vary", `${varyRaw}, ${RESPONSE_CACHE_KEY_HEADER}`);
      }
    }
    toStorableResponse(response, createdAtMs) {
      const headers = new Headers(response.headers);
      headers.set(RESPONSE_CACHE_CREATED_AT_HEADER, String(createdAtMs));
      this.ensureVaryByCacheKey(headers);
      return new Response(response.body, {
        status: response.status,
        statusText: response.statusText,
        headers
      });
    }
    readMemoryCache(key, nowMs) {
      const entry = this.memoryCache.get(key);
      if (!entry) return {};
      if (entry.expiresAt > nowMs) {
        this.touchMemoryCache(key, entry);
        return {
          fresh: entry.response.clone(),
          expiresAt: entry.expiresAt
        };
      }
      this.memoryCache.delete(key);
      return {
        stale: entry.response.clone(),
        expiresAt: entry.expiresAt
      };
    }
    touchMemoryCache(key, entry) {
      this.memoryCache.delete(key);
      this.memoryCache.set(key, entry);
    }
    trimMemoryCache() {
      while (this.memoryCache.size > MAX_MEMORY_CACHE_ENTRIES) {
        const first = this.memoryCache.keys().next().value;
        if (typeof first !== "string") break;
        this.memoryCache.delete(first);
      }
    }
    writeMemoryCache(key, response, expiresAt) {
      if (this.memoryCache.has(key)) {
        this.memoryCache.delete(key);
      }
      this.memoryCache.set(key, {
        response,
        expiresAt
      });
      this.trimMemoryCache();
    }
    async readCacheApi(cacheName, url, cacheKey2, ttlMs, nowMs, allowStaleOnError) {
      try {
        const request = new Request(url, {
          method: "GET",
          headers: {
            [RESPONSE_CACHE_KEY_HEADER]: cacheKey2
          }
        });
        const cache = await caches.open(cacheName);
        const cached = await cache.match(request);
        if (!cached) return {};
        const createdAtMs = this.readCreatedAtMs(cached);
        if (createdAtMs === null) {
          await cache.delete(request);
          return {};
        }
        const expiresAt = this.computeExpiresAt(createdAtMs, ttlMs);
        if (expiresAt > nowMs) {
          return {
            fresh: cached.clone(),
            expiresAt
          };
        }
        if (!allowStaleOnError) {
          await cache.delete(request);
          return {};
        }
        const stale = cached.clone();
        await cache.delete(request);
        return {
          stale,
          expiresAt
        };
      } catch {
        return {};
      }
    }
    async writeCacheApi(cacheName, url, cacheKey2, response) {
      try {
        const request = new Request(url, {
          method: "GET",
          headers: {
            [RESPONSE_CACHE_KEY_HEADER]: cacheKey2
          }
        });
        const cache = await caches.open(cacheName);
        await cache.put(request, response);
      } catch {
      }
    }
  }
  const responseCacheManager = new ResponseCacheManager();
  async function executeWithResponseCache(context, options, fetcher) {
    return responseCacheManager.execute(context, options, fetcher);
  }
  function stringifyUnknownObject(value) {
    const seen = new WeakSet();
    try {
      const serialized = JSON.stringify(value, (_key, currentValue) => {
        if (typeof currentValue !== "object" || currentValue === null) {
          return currentValue;
        }
        if (seen.has(currentValue)) {
          return "[Circular]";
        }
        seen.add(currentValue);
        return currentValue;
      });
      return serialized ?? null;
    } catch {
      return null;
    }
  }
  function toErrorMessage(error2, fallback = "Unknown error") {
    if (error2 instanceof Error) {
      return error2.message || fallback;
    }
    if (typeof error2 === "string") {
      return error2 || fallback;
    }
    if (error2 === null || error2 === void 0) {
      return fallback;
    }
    if (typeof error2 === "object") {
      const anyErr = error2;
      if (typeof anyErr?.data?.message === "string" && anyErr.data.message) {
        return anyErr.data.message;
      }
      if (typeof anyErr?.error?.message === "string" && anyErr.error.message) {
        return anyErr.error.message;
      }
      if (typeof anyErr?.message === "string" && anyErr.message) {
        return anyErr.message;
      }
      const serialized = stringifyUnknownObject(error2);
      if (serialized && serialized !== "{}") {
        return serialized;
      }
      const ctorName = error2.constructor?.name;
      return ctorName ? `[${ctorName}]` : fallback;
    }
    if (typeof error2 === "number" || typeof error2 === "boolean" || typeof error2 === "bigint") {
      return `${error2}`;
    }
    if (typeof error2 === "symbol") {
      return error2.description ? `Symbol(${error2.description})` : "Symbol";
    }
    if (typeof error2 === "function") {
      return error2.name ? `[Function ${error2.name}]` : "[Function]";
    }
    return fallback;
  }
  function getErrorMessage(error2) {
    return toErrorMessage(error2, "");
  }
  function isAbortError(err) {
    const anyErr = err;
    return typeof DOMException !== "undefined" && anyErr instanceof DOMException && anyErr.name === "AbortError" || anyErr instanceof Error && anyErr.name === "AbortError" || anyErr?.message === "AbortError";
  }
  function makeAbortError(message = "Aborted") {
    try {
      return new DOMException(message, "AbortError");
    } catch {
      const err = new Error(message);
      err.name = "AbortError";
      return err;
    }
  }
  const NEVER_ABORTED_SIGNAL = new AbortController().signal;
  function throwIfAborted(signal) {
    const maybeThrow = signal.throwIfAborted;
    if (typeof maybeThrow === "function") {
      try {
        maybeThrow.call(signal);
        return;
      } catch (e2) {
        if (signal.aborted || isAbortError(e2)) {
          throw makeAbortError();
        }
        throw e2 instanceof Error ? e2 : new Error(String(e2));
      }
    }
    if (signal.aborted) {
      throw makeAbortError();
    }
  }
  function createTimeoutSignal(timeoutMs, external) {
    const hasEffectiveTimeout = Number.isFinite(timeoutMs) && timeoutMs > 0;
    if (!hasEffectiveTimeout) {
      return {
        signal: external ?? NEVER_ABORTED_SIGNAL,
        cleanup: () => {
        }
      };
    }
    const controller = new AbortController();
    let timeoutId;
    const onExternalAbort = () => {
      if (timeoutId !== void 0) {
        clearTimeout(timeoutId);
        timeoutId = void 0;
      }
      controller.abort(external?.reason);
    };
    if (external) {
      external.addEventListener("abort", onExternalAbort, { once: true });
      if (external.aborted) {
        onExternalAbort();
      }
    }
    if (!controller.signal.aborted) {
      timeoutId = setTimeout(() => {
        controller.abort(makeAbortError("Timeout"));
        timeoutId = void 0;
      }, timeoutMs);
    }
    return {
      signal: controller.signal,
      cleanup: () => {
        if (timeoutId !== void 0) {
          clearTimeout(timeoutId);
          timeoutId = void 0;
        }
        external?.removeEventListener("abort", onExternalAbort);
      }
    };
  }
  const YANDEX_API_HOST = "api.browser.yandex.ru";
  const GOOGLEVIDEO_HOST_SUFFIX = "googlevideo.com";
  const VOT_BACKEND_HOST_SUFFIX = "toil.cc";
  const WORKERS_DEV_HOST_SUFFIX = "workers.dev";
  const ONRENDER_HOST_SUFFIX = "onrender.com";
  const CLOUDFLARE_DNS_HOST = "cloudflare-dns.com";
  const HEADER_LINE_RE = /^([\w-]+):\s*(.+)$/;
  const URL_SCHEME_RE = /^[a-zA-Z][a-zA-Z\d+.-]*:/;
  const scriptHandler = typeof GM_info === "undefined" ? void 0 : GM_info?.scriptHandler;
  const isProxyOnlyExtension = (

!!scriptHandler && !nonProxyExtensions.includes(scriptHandler)
  );
  const isSupportGM4 = typeof GM !== "undefined";
  const isSupportGMXhr = typeof GM_xmlhttpRequest !== "undefined";
  function getRequestHost(url) {
    const normalizedUrl = url.trim();
    try {
      return new URL(normalizedUrl).hostname.toLowerCase();
    } catch {
      if (!URL_SCHEME_RE.test(normalizedUrl)) {
        try {
          return new URL(`https://${normalizedUrl}`).hostname.toLowerCase();
        } catch {
        }
      }
      return void 0;
    }
  }
  function serializeGmBody(body) {
    if (body == null) return void 0;
    if (typeof body === "string") return body;
    if (body instanceof URLSearchParams) return body.toString();
    if (body instanceof FormData) return body;
    if (body instanceof Blob) return body;
    if (body instanceof ArrayBuffer) return body;
    return body;
  }
  function isHostOrSubdomain(host, targetHost) {
    return host === targetHost || host.endsWith(`.${targetHost}`);
  }
  function shouldUseGmXhr(host, url, forceGmXhr = false) {
    if (forceGmXhr) {
      return true;
    }
    if (!host) {
      const lowerUrl = url.toLowerCase();
      return lowerUrl.includes(YANDEX_API_HOST) || lowerUrl.includes(GOOGLEVIDEO_HOST_SUFFIX) || lowerUrl.includes(VOT_BACKEND_HOST_SUFFIX) || lowerUrl.includes(WORKERS_DEV_HOST_SUFFIX) || lowerUrl.includes(ONRENDER_HOST_SUFFIX) || lowerUrl.includes(CLOUDFLARE_DNS_HOST);
    }
    return isHostOrSubdomain(host, YANDEX_API_HOST) || isHostOrSubdomain(host, GOOGLEVIDEO_HOST_SUFFIX) || isHostOrSubdomain(host, VOT_BACKEND_HOST_SUFFIX) || isHostOrSubdomain(host, WORKERS_DEV_HOST_SUFFIX) || isHostOrSubdomain(host, ONRENDER_HOST_SUFFIX) || isHostOrSubdomain(host, CLOUDFLARE_DNS_HOST);
  }
  function toRequestUrl(url) {
    if (typeof url === "string") {
      return url;
    }
    if (url instanceof URL) {
      return url.href;
    }
    return url.url;
  }
  function resolveRequestMethod(url, method) {
    if (method) {
      return method.toUpperCase();
    }
    if (url instanceof Request) {
      return (url.method || "GET").toUpperCase();
    }
    return "GET";
  }
  function parseResponseHeaders(rawHeaders) {
    if (typeof rawHeaders !== "string" || rawHeaders.length === 0) {
      return {};
    }
    return rawHeaders.split(/\r?\n/).reduce((acc, line) => {
      const headerMatch = HEADER_LINE_RE.exec(line);
      if (!headerMatch) {
        return acc;
      }
      const [, key, value] = headerMatch;
      acc[key] = value;
      return acc;
    }, {});
  }
  function getGmXhrErrorMessage(error2) {
    const maybeError = error2;
    if (typeof maybeError?.error === "string") {
      return maybeError.error;
    }
    if (typeof maybeError?.statusText === "string") {
      return maybeError.statusText;
    }
    return getErrorMessage(error2) || "Unknown error";
  }
  async function gmXhrFetch(urlStr, timeout2, fetchOptions) {
    const headers = getHeaders(fetchOptions.headers);
    return await new Promise((resolve, reject) => {
      const gmXhr = typeof GM_xmlhttpRequest === "undefined" ? globalThis.GM_xmlhttpRequest : GM_xmlhttpRequest;
      if (typeof gmXhr !== "function") {
        reject(new TypeError("GM_xmlhttpRequest is not available"));
        return;
      }
      let settled = false;
      let onAbort;
      const cleanupAbort = () => {
        if (onAbort) {
          fetchOptions.signal?.removeEventListener("abort", onAbort);
        }
      };
      const failOnce = (error2) => {
        if (settled) return;
        settled = true;
        cleanupAbort();
        reject(error2);
      };
      const request = gmXhr({
        method: fetchOptions.method || "GET",
        url: urlStr,
        responseType: "blob",
        data: serializeGmBody(fetchOptions.body),
        timeout: timeout2,
        headers,
        onload: (resp) => {
          if (settled) return;
          settled = true;
          cleanupAbort();
          const responseHeaders = parseResponseHeaders(resp.responseHeaders);
          const response = new Response(resp.response, {
            status: resp.status,
            statusText: typeof resp.statusText === "string" ? resp.statusText : "",
            headers: responseHeaders
          });
          Object.defineProperty(response, "url", {
            value: resp.finalUrl ?? urlStr
          });
          resolve(response);
        },
        ontimeout: () => failOnce(new Error("Timeout")),
        onerror: (error2) => failOnce(new Error(getGmXhrErrorMessage(error2))),
        onabort: () => failOnce(makeAbortError())
      });
      onAbort = () => {
        try {
          request?.abort?.();
        } catch {
        }
        failOnce(makeAbortError());
      };
      if (fetchOptions.signal) {
        fetchOptions.signal.addEventListener("abort", onAbort, { once: true });
        if (fetchOptions.signal.aborted) {
          onAbort();
          return;
        }
      }
    });
  }
  async function GM_fetch(url, opts = {}) {
    const {
      timeout: timeout2 = 15e3,
      forceGmXhr = false,
      responseCache,
      ...fetchOptions
    } = opts;
    const urlStr = toRequestUrl(url);
    const host = getRequestHost(urlStr);
    const method = resolveRequestMethod(url, fetchOptions.method);
    const performRequest = async () => {
      if (shouldUseGmXhr(host, urlStr, forceGmXhr)) {
        debug.log("GM_fetch: routing request via GM_xmlhttpRequest", {
          host: host ?? "unknown",
          reason: forceGmXhr ? "forced" : "host-policy",
          url: urlStr
        });
        return await gmXhrFetch(urlStr, timeout2, fetchOptions);
      }
      const { signal, cleanup } = createTimeoutSignal(
        timeout2,
        fetchOptions.signal
      );
      try {
        return await fetch(url, {
          ...fetchOptions,
          signal
        });
      } catch (err) {
        if (signal.aborted || isAbortError(err)) {
          throw err;
        }
        debug.log(
          "GM_fetch preventing CORS by GM_xmlhttpRequest",
          getErrorMessage(err) || "Unknown error"
        );
        return await gmXhrFetch(urlStr, timeout2, fetchOptions);
      } finally {
        cleanup();
      }
    };
    if (!responseCache) {
      return await performRequest();
    }
    return await executeWithResponseCache(
      {
        url: urlStr,
        method,
        body: fetchOptions.body
      },
      responseCache,
      performRequest
    );
  }
  const compatMay2025Data = {
    numToBool: [
      ["autoTranslate"],
      ["dontTranslateYourLang", "enabledDontTranslateLanguages"],
      ["autoSetVolumeYandexStyle", "enabledAutoVolume"],
      ["showVideoSlider"],
      ["syncVolume"],
      ["downloadWithName"],
      ["sendNotifyOnComplete"],
      ["highlightWords"],
      ["onlyBypassMediaCSP"],
      ["newAudioPlayer"],
      ["showPiPButton"],
      ["translateAPIErrors"],
      ["audioBooster"],
      ["useNewModel", "useLivelyVoice"]
    ],
    number: [["autoVolume"]],
    array: [["dontTranslateLanguage", "dontTranslateLanguages"]],
    string: [
      ["hotkeyButton", "translationHotkey"],
      ["locale-lang-override", "localeLangOverride"],
      ["locale-lang", "localeLang"]
    ]
  };
  const compatRules = Object.entries(compatMay2025Data).flatMap(
    ([category, entries]) => entries.map(([oldKey, maybeNewKey]) => ({
      category,
      oldKey,
      newKey: maybeNewKey ?? oldKey,
      shouldDeleteOldKey: Boolean(maybeNewKey)
    }))
  );
  const compatRuleByOldKey = new Map(
    compatRules.map((rule) => [rule.oldKey, rule])
  );
  const compatKeysToRead = Array.from(
    new Set(compatRules.map((rule) => rule.oldKey))
  );
  function createUndefinedDefaults(keys) {
    const defaults = {};
    for (const key of keys) {
      defaults[key] = void 0;
    }
    return defaults;
  }
  function isCompatValue(category, value) {
    switch (category) {
      case "numToBool":
      case "number":
        return typeof value === "number";
      case "array":
        return Array.isArray(value);
      case "string":
        return typeof value === "string" || value === null;
      default:
        return false;
    }
  }
  function convertByCompatCategory(category, value) {
    switch (category) {
      case "string":
      case "array":
      case "number":
        return value;
      default:
        return !!value;
    }
  }
  function normalizeCompatValue(rule, value) {
    let convertedValue = convertByCompatCategory(rule.category, value);
    if (rule.oldKey === "autoVolume" && typeof value === "number" && value < 1) {
      convertedValue = Math.round(value * 100);
    }
    return convertedValue;
  }
  function areStorageValuesEqual(a2, b2) {
    if (Array.isArray(a2) && Array.isArray(b2)) {
      return a2.length === b2.length && a2.every((item, index) => Object.is(item, b2[index]));
    }
    return Object.is(a2, b2);
  }
  async function updateConfig(data) {
    if (data.compatVersion === actualCompatVersion) {
      return data;
    }
    const keysToRead = new Set([
      ...Object.keys(data),
      ...compatKeysToRead
    ]);
    const persistedValues = await votStorage.getValues(createUndefinedDefaults(keysToRead));
    const newData = { ...data };
    const writeOperations = [];
    const deleteOperations = [];
    for (const [key, storedValue] of Object.entries(persistedValues)) {
      if (storedValue === void 0) {
        continue;
      }
      const compatRule = compatRuleByOldKey.get(key);
      if (!compatRule || !isCompatValue(compatRule.category, storedValue)) {
        continue;
      }
      const convertedValue = normalizeCompatValue(compatRule, storedValue);
      newData[compatRule.newKey] = convertedValue;
      const existingNewValue = persistedValues[compatRule.newKey];
      if (compatRule.shouldDeleteOldKey || !areStorageValuesEqual(existingNewValue, convertedValue)) {
        writeOperations.push(votStorage.set(compatRule.newKey, convertedValue));
      }
      if (compatRule.shouldDeleteOldKey) {
        deleteOperations.push(votStorage.delete(compatRule.oldKey));
      }
    }
    await Promise.all([...writeOperations, ...deleteOperations]);
    return {
      ...newData,
      compatVersion: actualCompatVersion
    };
  }
  class VOTStorage {
    support = null;
    resolveSupport() {
      if (this.support) {
        return this.support;
      }
      const support = {
        legacyGet: typeof GM_getValue === "function",
        legacySet: typeof GM_setValue === "function",
        legacyDelete: typeof GM_deleteValue === "function",
        legacyList: typeof GM_listValues === "function",
        promiseGet: isSupportGM4 && typeof GM?.getValue === "function",
        promiseGetValues: isSupportGM4 && typeof GM?.getValues === "function",
        promiseSet: isSupportGM4 && typeof GM?.setValue === "function",
        promiseDelete: isSupportGM4 && typeof GM?.deleteValue === "function",
        promiseList: isSupportGM4 && typeof GM?.listValues === "function"
      };
      this.support = support;
      debug.log(
        `[VOT Storage] GM Promises: ${support.promiseGet} | GM legacy: ${support.legacyGet}`
      );
      return support;
    }
get isSupportOnlyLS() {
      const support = this.resolveSupport();
      return !support.legacyGet && !support.legacySet && !support.legacyDelete && !support.legacyList && !support.promiseGet && !support.promiseGetValues && !support.promiseSet && !support.promiseDelete && !support.promiseList;
    }
    syncGetByName(name, def, support) {
      if (support.legacyGet) {
        return GM_getValue(name, def);
      }
      const val = globalThis.localStorage.getItem(name);
      if (val === null) {
        return def;
      }
      try {
        return JSON.parse(val);
      } catch {
        return def;
      }
    }
    async getRaw(name, def) {
      const support = this.resolveSupport();
      if (support.promiseGet && GM.getValue) {
        return await GM.getValue(name, def);
      }
      return this.syncGetByName(name, def, support);
    }
    async get(name, def) {
      return this.getRaw(name, def);
    }
    async getValues(data) {
      const support = this.resolveSupport();
      if (support.promiseGetValues && GM.getValues) {
        return await GM.getValues(data);
      }
      const entries = Object.entries(data);
      if (support.promiseGet && GM.getValue) {
        const values = await Promise.all(
          entries.map(async ([key, value]) => {
            const storedValue = await GM.getValue(key, value);
            return [key, storedValue];
          })
        );
        return Object.fromEntries(values);
      }
      return Object.fromEntries(
        entries.map(([key, value]) => [
          key,
          this.syncGetByName(key, value, support)
        ])
      );
    }
    syncSetByName(name, value, support) {
      if (support.legacySet) {
        return GM_setValue(name, value);
      }
      return globalThis.localStorage.setItem(name, JSON.stringify(value));
    }
    async setRaw(name, value) {
      const support = this.resolveSupport();
      if (support.promiseSet && GM.setValue) {
        return await GM.setValue(name, value);
      }
      return this.syncSetByName(name, value, support);
    }
    async set(name, value) {
      return this.setRaw(name, value);
    }
    syncDeleteByName(name, support) {
      if (support.legacyDelete) {
        return GM_deleteValue(name);
      }
      return globalThis.localStorage.removeItem(name);
    }
    async deleteRaw(name) {
      const support = this.resolveSupport();
      if (support.promiseDelete && GM.deleteValue) {
        return await GM.deleteValue(name);
      }
      return this.syncDeleteByName(name, support);
    }
    async delete(name) {
      return this.deleteRaw(name);
    }
    syncList(support) {
      if (support.legacyList) {
        return GM_listValues();
      }
      return storageKeys;
    }
    async list() {
      const support = this.resolveSupport();
      if (support.promiseList && GM.listValues) {
        return await GM.listValues();
      }
      return this.syncList(support);
    }
  }
  const VOT_STORAGE_GLOBAL_KEY = "__VOT_STORAGE_SINGLETON__";
  const votStorage = (() => {
    const scope = globalThis;
    const existing = scope[VOT_STORAGE_GLOBAL_KEY];
    if (existing instanceof VOTStorage) {
      return existing;
    }
    const created = new VOTStorage();
    scope[VOT_STORAGE_GLOBAL_KEY] = created;
    return created;
  })();
  function getProfilePayload() {
    const payload = globalThis._userData;
    if (!payload || typeof payload !== "object") return null;
    const candidate = payload;
    if (typeof candidate.avatar_id !== "string" || typeof candidate.username !== "string" || !candidate.avatar_id || !candidate.username) {
      return null;
    }
    return {
      avatar_id: candidate.avatar_id,
      username: candidate.username
    };
  }
  function base64UrlEncode(bytes) {
    let binary = "";
    for (const b2 of bytes) binary += String.fromCharCode(b2);
    return btoa(binary).replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/g, "");
  }
  async function sha256Base64Url(input) {
    const data = new TextEncoder().encode(input);
    const digest = await crypto.subtle.digest("SHA-256", data);
    return base64UrlEncode(new Uint8Array(digest));
  }
  function randomString(length = 64) {
    const alphabet = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-._~";
    const bytes = new Uint8Array(length);
    crypto.getRandomValues(bytes);
    let result = "";
    for (let i2 = 0; i2 < length; i2 += 1) {
      result += alphabet[bytes[i2] % alphabet.length];
    }
    return result;
  }
  async function createPkceAuthUrl() {
    const state = randomString(32);
    const codeVerifier = randomString(64);
    const codeChallenge = await sha256Base64Url(codeVerifier);
    sessionStorage.setItem("vot-yandex-oauth-state", state);
    sessionStorage.setItem("vot-yandex-oauth-code-verifier", codeVerifier);
    const params = new URLSearchParams({
      response_type: "code",
      client_id: yandexOauthClientId,
      redirect_uri: authCallbackUrl,
      state,
      code_challenge: codeChallenge,
      code_challenge_method: "S256"
    });
    return `https://oauth.yandex.ru/authorize?${params.toString()}`;
  }
  function gmPostForm(url, body) {
    return new Promise((resolve, reject) => {
      GM_xmlhttpRequest({
        method: "POST",
        url,
        headers: {
          "Content-Type": "application/x-www-form-urlencoded; charset=UTF-8"
        },
        data: body,
        onload: (res) => {
          resolve({
            status: res.status,
            responseText: res.responseText || ""
          });
        },
        onerror: (err) => {
          console.error("[VOT] GM OAuth request failed:", err);
          reject(new Error("[VOT] GM OAuth request network error"));
        },
        ontimeout: () => {
          reject(new Error("[VOT] GM OAuth request timeout"));
        }
      });
    });
  }
  async function exchangeCodeForToken(code) {
    const codeVerifier = sessionStorage.getItem("vot-yandex-oauth-code-verifier");
    if (!codeVerifier) {
      throw new Error("[VOT] Missing PKCE code_verifier in opener");
    }
    const body = new URLSearchParams({
      grant_type: "authorization_code",
      code,
      client_id: yandexOauthClientId,
      code_verifier: codeVerifier,
      redirect_uri: authCallbackUrl
    }).toString();
    const res = await gmPostForm("https://oauth.yandex.ru/token", body);
    const rawText = res.responseText || "";
    let data = {};
    try {
      data = rawText ? JSON.parse(rawText) : {};
    } catch {
      data = {};
    }
    if (res.status < 200 || res.status >= 300 || data?.error) {
      const err = data?.error ?? `http_${res.status}`;
      const desc = data?.error_description ? ` (${data.error_description})` : "";
      throw new Error(
        `[VOT] Failed to exchange verification code: ${err}${desc}. Response: ${rawText}`
      );
    }
    if (!data?.access_token) {
      throw new Error(
        `[VOT] access_token was not returned by Yandex. Response: ${rawText}`
      );
    }
    if (typeof data.expires_in !== "number") {
      throw new Error(
        `[VOT] expires_in was not returned or invalid. Response: ${rawText}`
      );
    }
    return {
      access_token: data.access_token,
      expires_in: data.expires_in,
      refresh_token: data.refresh_token
    };
  }
  async function saveOAuthAccount(tokenData) {
    await votStorage.set("account", {
      token: tokenData.access_token,
      expires: Date.now() + tokenData.expires_in * 1e3,
      username: void 0,
      avatarId: void 0
    });
    sessionStorage.removeItem("vot-yandex-oauth-state");
    sessionStorage.removeItem("vot-yandex-oauth-code-verifier");
  }
  async function postResultToOpener(message) {
    if (globalThis.opener && !globalThis.opener.closed) {
      globalThis.opener.postMessage(message, "*");
    }
  }
  async function handleAuthCallbackPage() {
    const params = new URLSearchParams(globalThis.location.search);
    const code = params.get("code");
    const state = params.get("state") ?? void 0;
    const error2 = params.get("error");
    const errorDescription = params.get("error_description") ?? void 0;
    if (error2) {
      await postResultToOpener({
        source: "vot-auth",
        type: "error",
        error: error2,
        error_description: errorDescription,
        state
      });
      globalThis.close();
      return;
    }
    if (!code) {
      await postResultToOpener({
        source: "vot-auth",
        type: "error",
        error: "missing_code",
        state
      });
      globalThis.close();
      return;
    }
    await postResultToOpener({
      source: "vot-auth",
      type: "code",
      code,
      state
    });
    globalThis.close();
  }
  async function handleProfilePage() {
    const payload = getProfilePayload();
    if (!payload) {
      throw new Error("[VOT] Invalid user data");
    }
    const data = await votStorage.get("account");
    if (!data) {
      throw new Error("[VOT] No account data found");
    }
    await votStorage.set("account", {
      ...data,
      username: payload.username,
      avatarId: payload.avatar_id
    });
  }
  async function initAuth() {
    if (globalThis.location.origin === authCallbackOrigin && globalThis.location.pathname === authCallbackPath) {
      return handleAuthCallbackPage();
    }
    if (globalThis.location.pathname === "/my/profile") {
      return handleProfilePage();
    }
  }
  const t = globalThis, i = (t2) => t2, s = t.trustedTypes, e = s ? s.createPolicy("lit-html", { createHTML: (t2) => t2 }) : void 0, h = "$lit$", o = `lit$${Math.random().toFixed(9).slice(2)}$`, n = "?" + o, r = `<${n}>`, l = document, c = () => l.createComment(""), a = (t2) => null === t2 || "object" != typeof t2 && "function" != typeof t2, u = Array.isArray, d = (t2) => u(t2) || "function" == typeof t2?.[Symbol.iterator], f = "[ 	\n\f\r]", v = /<(?:(!--|\/[^a-zA-Z])|(\/?[a-zA-Z][^>\s]*)|(\/?$))/g, _ = /-->/g, m = />/g, p = RegExp(`>|${f}(?:([^\\s"'>=/]+)(${f}*=${f}*(?:[^ 	
\f\r"'\`<>=]|("|')|))|$)`, "g"), g = /'/g, $ = /"/g, y = /^(?:script|style|textarea|title)$/i, x = (t2) => (i2, ...s2) => ({ _$litType$: t2, strings: i2, values: s2 }), b = x(1), w = x(2), E = Symbol.for("lit-noChange"), A = Symbol.for("lit-nothing"), C = new WeakMap(), P = l.createTreeWalker(l, 129);
  function V(t2, i2) {
    if (!u(t2) || !t2.hasOwnProperty("raw")) throw Error("invalid template strings array");
    return void 0 !== e ? e.createHTML(i2) : i2;
  }
  const N = (t2, i2) => {
    const s2 = t2.length - 1, e2 = [];
    let n2, l2 = 2 === i2 ? "<svg>" : 3 === i2 ? "<math>" : "", c2 = v;
    for (let i3 = 0; i3 < s2; i3++) {
      const s3 = t2[i3];
      let a2, u2, d2 = -1, f2 = 0;
      for (; f2 < s3.length && (c2.lastIndex = f2, u2 = c2.exec(s3), null !== u2); ) f2 = c2.lastIndex, c2 === v ? "!--" === u2[1] ? c2 = _ : void 0 !== u2[1] ? c2 = m : void 0 !== u2[2] ? (y.test(u2[2]) && (n2 = RegExp("</" + u2[2], "g")), c2 = p) : void 0 !== u2[3] && (c2 = p) : c2 === p ? ">" === u2[0] ? (c2 = n2 ?? v, d2 = -1) : void 0 === u2[1] ? d2 = -2 : (d2 = c2.lastIndex - u2[2].length, a2 = u2[1], c2 = void 0 === u2[3] ? p : '"' === u2[3] ? $ : g) : c2 === $ || c2 === g ? c2 = p : c2 === _ || c2 === m ? c2 = v : (c2 = p, n2 = void 0);
      const x2 = c2 === p && t2[i3 + 1].startsWith("/>") ? " " : "";
      l2 += c2 === v ? s3 + r : d2 >= 0 ? (e2.push(a2), s3.slice(0, d2) + h + s3.slice(d2) + o + x2) : s3 + o + (-2 === d2 ? i3 : x2);
    }
    return [V(t2, l2 + (t2[s2] || "<?>") + (2 === i2 ? "</svg>" : 3 === i2 ? "</math>" : "")), e2];
  };
  class S {
    constructor({ strings: t2, _$litType$: i2 }, e2) {
      let r2;
      this.parts = [];
      let l2 = 0, a2 = 0;
      const u2 = t2.length - 1, d2 = this.parts, [f2, v2] = N(t2, i2);
      if (this.el = S.createElement(f2, e2), P.currentNode = this.el.content, 2 === i2 || 3 === i2) {
        const t3 = this.el.content.firstChild;
        t3.replaceWith(...t3.childNodes);
      }
      for (; null !== (r2 = P.nextNode()) && d2.length < u2; ) {
        if (1 === r2.nodeType) {
          if (r2.hasAttributes()) for (const t3 of r2.getAttributeNames()) if (t3.endsWith(h)) {
            const i3 = v2[a2++], s2 = r2.getAttribute(t3).split(o), e3 = /([.?@])?(.*)/.exec(i3);
            d2.push({ type: 1, index: l2, name: e3[2], strings: s2, ctor: "." === e3[1] ? I : "?" === e3[1] ? L : "@" === e3[1] ? z : H }), r2.removeAttribute(t3);
          } else t3.startsWith(o) && (d2.push({ type: 6, index: l2 }), r2.removeAttribute(t3));
          if (y.test(r2.tagName)) {
            const t3 = r2.textContent.split(o), i3 = t3.length - 1;
            if (i3 > 0) {
              r2.textContent = s ? s.emptyScript : "";
              for (let s2 = 0; s2 < i3; s2++) r2.append(t3[s2], c()), P.nextNode(), d2.push({ type: 2, index: ++l2 });
              r2.append(t3[i3], c());
            }
          }
        } else if (8 === r2.nodeType) if (r2.data === n) d2.push({ type: 2, index: l2 });
        else {
          let t3 = -1;
          for (; -1 !== (t3 = r2.data.indexOf(o, t3 + 1)); ) d2.push({ type: 7, index: l2 }), t3 += o.length - 1;
        }
        l2++;
      }
    }
    static createElement(t2, i2) {
      const s2 = l.createElement("template");
      return s2.innerHTML = t2, s2;
    }
  }
  function M(t2, i2, s2 = t2, e2) {
    if (i2 === E) return i2;
    let h2 = void 0 !== e2 ? s2._$Co?.[e2] : s2._$Cl;
    const o2 = a(i2) ? void 0 : i2._$litDirective$;
    return h2?.constructor !== o2 && (h2?._$AO?.(false), void 0 === o2 ? h2 = void 0 : (h2 = new o2(t2), h2._$AT(t2, s2, e2)), void 0 !== e2 ? (s2._$Co ??= [])[e2] = h2 : s2._$Cl = h2), void 0 !== h2 && (i2 = M(t2, h2._$AS(t2, i2.values), h2, e2)), i2;
  }
  class R {
    constructor(t2, i2) {
      this._$AV = [], this._$AN = void 0, this._$AD = t2, this._$AM = i2;
    }
    get parentNode() {
      return this._$AM.parentNode;
    }
    get _$AU() {
      return this._$AM._$AU;
    }
    u(t2) {
      const { el: { content: i2 }, parts: s2 } = this._$AD, e2 = (t2?.creationScope ?? l).importNode(i2, true);
      P.currentNode = e2;
      let h2 = P.nextNode(), o2 = 0, n2 = 0, r2 = s2[0];
      for (; void 0 !== r2; ) {
        if (o2 === r2.index) {
          let i3;
          2 === r2.type ? i3 = new k(h2, h2.nextSibling, this, t2) : 1 === r2.type ? i3 = new r2.ctor(h2, r2.name, r2.strings, this, t2) : 6 === r2.type && (i3 = new Z(h2, this, t2)), this._$AV.push(i3), r2 = s2[++n2];
        }
        o2 !== r2?.index && (h2 = P.nextNode(), o2++);
      }
      return P.currentNode = l, e2;
    }
    p(t2) {
      let i2 = 0;
      for (const s2 of this._$AV) void 0 !== s2 && (void 0 !== s2.strings ? (s2._$AI(t2, s2, i2), i2 += s2.strings.length - 2) : s2._$AI(t2[i2])), i2++;
    }
  }
  class k {
    get _$AU() {
      return this._$AM?._$AU ?? this._$Cv;
    }
    constructor(t2, i2, s2, e2) {
      this.type = 2, this._$AH = A, this._$AN = void 0, this._$AA = t2, this._$AB = i2, this._$AM = s2, this.options = e2, this._$Cv = e2?.isConnected ?? true;
    }
    get parentNode() {
      let t2 = this._$AA.parentNode;
      const i2 = this._$AM;
      return void 0 !== i2 && 11 === t2?.nodeType && (t2 = i2.parentNode), t2;
    }
    get startNode() {
      return this._$AA;
    }
    get endNode() {
      return this._$AB;
    }
    _$AI(t2, i2 = this) {
      t2 = M(this, t2, i2), a(t2) ? t2 === A || null == t2 || "" === t2 ? (this._$AH !== A && this._$AR(), this._$AH = A) : t2 !== this._$AH && t2 !== E && this._(t2) : void 0 !== t2._$litType$ ? this.$(t2) : void 0 !== t2.nodeType ? this.T(t2) : d(t2) ? this.k(t2) : this._(t2);
    }
    O(t2) {
      return this._$AA.parentNode.insertBefore(t2, this._$AB);
    }
    T(t2) {
      this._$AH !== t2 && (this._$AR(), this._$AH = this.O(t2));
    }
    _(t2) {
      this._$AH !== A && a(this._$AH) ? this._$AA.nextSibling.data = t2 : this.T(l.createTextNode(t2)), this._$AH = t2;
    }
    $(t2) {
      const { values: i2, _$litType$: s2 } = t2, e2 = "number" == typeof s2 ? this._$AC(t2) : (void 0 === s2.el && (s2.el = S.createElement(V(s2.h, s2.h[0]), this.options)), s2);
      if (this._$AH?._$AD === e2) this._$AH.p(i2);
      else {
        const t3 = new R(e2, this), s3 = t3.u(this.options);
        t3.p(i2), this.T(s3), this._$AH = t3;
      }
    }
    _$AC(t2) {
      let i2 = C.get(t2.strings);
      return void 0 === i2 && C.set(t2.strings, i2 = new S(t2)), i2;
    }
    k(t2) {
      u(this._$AH) || (this._$AH = [], this._$AR());
      const i2 = this._$AH;
      let s2, e2 = 0;
      for (const h2 of t2) e2 === i2.length ? i2.push(s2 = new k(this.O(c()), this.O(c()), this, this.options)) : s2 = i2[e2], s2._$AI(h2), e2++;
      e2 < i2.length && (this._$AR(s2 && s2._$AB.nextSibling, e2), i2.length = e2);
    }
    _$AR(t2 = this._$AA.nextSibling, s2) {
      for (this._$AP?.(false, true, s2); t2 !== this._$AB; ) {
        const s3 = i(t2).nextSibling;
        i(t2).remove(), t2 = s3;
      }
    }
    setConnected(t2) {
      void 0 === this._$AM && (this._$Cv = t2, this._$AP?.(t2));
    }
  }
  class H {
    get tagName() {
      return this.element.tagName;
    }
    get _$AU() {
      return this._$AM._$AU;
    }
    constructor(t2, i2, s2, e2, h2) {
      this.type = 1, this._$AH = A, this._$AN = void 0, this.element = t2, this.name = i2, this._$AM = e2, this.options = h2, s2.length > 2 || "" !== s2[0] || "" !== s2[1] ? (this._$AH = Array(s2.length - 1).fill(new String()), this.strings = s2) : this._$AH = A;
    }
    _$AI(t2, i2 = this, s2, e2) {
      const h2 = this.strings;
      let o2 = false;
      if (void 0 === h2) t2 = M(this, t2, i2, 0), o2 = !a(t2) || t2 !== this._$AH && t2 !== E, o2 && (this._$AH = t2);
      else {
        const e3 = t2;
        let n2, r2;
        for (t2 = h2[0], n2 = 0; n2 < h2.length - 1; n2++) r2 = M(this, e3[s2 + n2], i2, n2), r2 === E && (r2 = this._$AH[n2]), o2 ||= !a(r2) || r2 !== this._$AH[n2], r2 === A ? t2 = A : t2 !== A && (t2 += (r2 ?? "") + h2[n2 + 1]), this._$AH[n2] = r2;
      }
      o2 && !e2 && this.j(t2);
    }
    j(t2) {
      t2 === A ? this.element.removeAttribute(this.name) : this.element.setAttribute(this.name, t2 ?? "");
    }
  }
  class I extends H {
    constructor() {
      super(...arguments), this.type = 3;
    }
    j(t2) {
      this.element[this.name] = t2 === A ? void 0 : t2;
    }
  }
  class L extends H {
    constructor() {
      super(...arguments), this.type = 4;
    }
    j(t2) {
      this.element.toggleAttribute(this.name, !!t2 && t2 !== A);
    }
  }
  class z extends H {
    constructor(t2, i2, s2, e2, h2) {
      super(t2, i2, s2, e2, h2), this.type = 5;
    }
    _$AI(t2, i2 = this) {
      if ((t2 = M(this, t2, i2, 0) ?? A) === E) return;
      const s2 = this._$AH, e2 = t2 === A && s2 !== A || t2.capture !== s2.capture || t2.once !== s2.once || t2.passive !== s2.passive, h2 = t2 !== A && (s2 === A || e2);
      e2 && this.element.removeEventListener(this.name, this, s2), h2 && this.element.addEventListener(this.name, this, t2), this._$AH = t2;
    }
    handleEvent(t2) {
      "function" == typeof this._$AH ? this._$AH.call(this.options?.host ?? this.element, t2) : this._$AH.handleEvent(t2);
    }
  }
  class Z {
    constructor(t2, i2, s2) {
      this.element = t2, this.type = 6, this._$AN = void 0, this._$AM = i2, this.options = s2;
    }
    get _$AU() {
      return this._$AM._$AU;
    }
    _$AI(t2) {
      M(this, t2);
    }
  }
  const B = t.litHtmlPolyfillSupport;
  B?.(S, k), (t.litHtmlVersions ??= []).push("3.3.2");
  const D = (t2, i2, s2) => {
    const e2 = i2;
    let h2 = e2._$litPart$;
    if (void 0 === h2) {
      const t3 = null;
      e2._$litPart$ = h2 = new k(i2.insertBefore(c(), t3), t3, void 0, {});
    }
    return h2._$AI(t2), h2;
  };
  const TRANSLATE_ICON_SVG = w`<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24">
  <path
    id="vot-translate-icon"
    fill-rule="evenodd"
    d="M15.778 18.95L14.903 21.375C14.8364 21.5583 14.7197 21.7083 14.553 21.825C14.3864 21.9417 14.203 22 14.003 22C13.6697 22 13.3989 21.8625 13.1905 21.5875C12.9822 21.3125 12.9447 21.0083 13.078 20.675L16.878 10.625C16.9614 10.4417 17.0864 10.2917 17.253 10.175C17.4197 10.0583 17.603 10 17.803 10H18.553C18.753 10 18.9364 10.0583 19.103 10.175C19.2697 10.2917 19.3947 10.4417 19.478 10.625L23.278 20.7C23.4114 21.0167 23.378 21.3125 23.178 21.5875C22.978 21.8625 22.7114 22 22.378 22C22.1614 22 21.9739 21.9375 21.8155 21.8125C21.6572 21.6875 21.5364 21.525 21.453 21.325L20.628 18.95H15.778ZM19.978 17.2H16.378L18.228 12.25L19.978 17.2Z"
  ></path>
  <path
    d="M9 14L4.7 18.3C4.51667 18.4833 4.28333 18.575 4 18.575C3.71667 18.575 3.48333 18.4833 3.3 18.3C3.11667 18.1167 3.025 17.8833 3.025 17.6C3.025 17.3167 3.11667 17.0833 3.3 16.9L7.65 12.55C7.01667 11.85 6.4625 11.125 5.9875 10.375C5.5125 9.625 5.1 8.83333 4.75 8H6.85C7.15 8.6 7.47083 9.14167 7.8125 9.625C8.15417 10.1083 8.56667 10.6167 9.05 11.15C9.78333 10.35 10.3917 9.52917 10.875 8.6875C11.3583 7.84583 11.7667 6.95 12.1 6H2C1.71667 6 1.47917 5.90417 1.2875 5.7125C1.09583 5.52083 1 5.28333 1 5C1 4.71667 1.09583 4.47917 1.2875 4.2875C1.47917 4.09583 1.71667 4 2 4H8V3C8 2.71667 8.09583 2.47917 8.2875 2.2875C8.47917 2.09583 8.71667 2 9 2C9.28333 2 9.52083 2.09583 9.7125 2.2875C9.90417 2.47917 10 2.71667 10 3V4H16C16.2833 4 16.5208 4.09583 16.7125 4.2875C16.9042 4.47917 17 4.71667 17 5C17 5.28333 16.9042 5.52083 16.7125 5.7125C16.5208 5.90417 16.2833 6 16 6H14.1C13.75 7.18333 13.275 8.33333 12.675 9.45C12.075 10.5667 11.3333 11.6167 10.45 12.6L12.85 15.05L12.1 17.1L9 14Z"
  ></path>
  <path
    id="vot-loading-icon"
    style="display:none"
    d="M19.8081 16.3697L18.5842 15.6633V13.0832C18.5842 12.9285 18.5228 12.7801 18.4134 12.6707C18.304 12.5613 18.1556 12.4998 18.0009 12.4998C17.8462 12.4998 17.6978 12.5613 17.5884 12.6707C17.479 12.7801 17.4176 12.9285 17.4176 13.0832V15.9998C17.4176 16.1022 17.4445 16.2028 17.4957 16.2915C17.5469 16.3802 17.6205 16.4538 17.7092 16.505L19.2247 17.38C19.2911 17.4189 19.3645 17.4443 19.4407 17.4547C19.5169 17.4652 19.5945 17.4604 19.6688 17.4407C19.7432 17.4211 19.813 17.3869 19.8741 17.3402C19.9352 17.2934 19.9864 17.2351 20.0249 17.1684C20.0634 17.1018 20.0883 17.0282 20.0982 16.952C20.1081 16.8757 20.1028 16.7982 20.0827 16.7239C20.0625 16.6497 20.0279 16.5802 19.9808 16.5194C19.9336 16.4586 19.8749 16.4077 19.8081 16.3697ZM18.0015 10C16.8478 10 15.6603 10.359 14.7011 11C13.7418 11.641 12.9415 12.4341 12.5 13.5C12.0585 14.5659 11.8852 16.0369 12.1103 17.1684C12.3353 18.3 12.8736 19.4942 13.6894 20.31C14.5053 21.1258 15.8684 21.7749 17 22C18.1316 22.2251 19.4341 21.9415 20.5 21.5C21.5659 21.0585 22.359 20.2573 23 19.298C23.641 18.3387 24.0015 17.1537 24.0015 16C23.9998 14.4534 23.5951 13.0936 22.5015 12C21.4079 10.9064 19.5481 10.0017 18.0015 10ZM18.0009 20.6665C17.0779 20.6665 16.1757 20.3928 15.4082 19.88C14.6408 19.3672 14.0427 18.6384 13.6894 17.7857C13.3362 16.933 13.2438 15.9947 13.4239 15.0894C13.604 14.1842 14.0484 13.3527 14.7011 12.7C15.3537 12.0474 16.1852 11.6029 17.0905 11.4228C17.9957 11.2428 18.934 11.3352 19.7867 11.6884C20.6395 12.0416 21.3683 12.6397 21.8811 13.4072C22.3939 14.1746 22.6676 15.0769 22.6676 15.9998C22.666 17.237 22.1738 18.4231 21.299 19.298C20.4242 20.1728 19.2381 20.665 18.0009 20.6665Z"
  ></path>
</svg>`;
  const PIP_ICON_SVG = w`<svg xmlns="http://www.w3.org/2000/svg" height="24" viewBox="0 -960 960 960" width="24">
  <path
    d="M120-520q-17 0-28.5-11.5T80-560q0-17 11.5-28.5T120-600h104L80-743q-12-12-12-28.5T80-800q12-12 28.5-12t28.5 12l143 144v-104q0-17 11.5-28.5T320-800q17 0 28.5 11.5T360-760v200q0 17-11.5 28.5T320-520H120Zm40 360q-33 0-56.5-23.5T80-240v-160q0-17 11.5-28.5T120-440q17 0 28.5 11.5T160-400v160h280q17 0 28.5 11.5T480-200q0 17-11.5 28.5T440-160H160Zm680-280q-17 0-28.5-11.5T800-480v-240H480q-17 0-28.5-11.5T440-760q0-17 11.5-28.5T480-800h320q33 0 56.5 23.5T880-720v240q0 17-11.5 28.5T840-440ZM600-160q-17 0-28.5-11.5T560-200v-120q0-17 11.5-28.5T600-360h240q17 0 28.5 11.5T880-320v120q0 17-11.5 28.5T840-160H600Z"
  />
</svg>`;
  const MENU_ICON = w`<svg xmlns="http://www.w3.org/2000/svg" height="24" viewBox="0 -960 960 960" width="24">
  <path
    d="M480-160q-33 0-56.5-23.5T400-240q0-33 23.5-56.5T480-320q33 0 56.5 23.5T560-240q0 33-23.5 56.5T480-160Zm0-240q-33 0-56.5-23.5T400-480q0-33 23.5-56.5T480-560q33 0 56.5 23.5T560-480q0 33-23.5 56.5T480-400Zm0-240q-33 0-56.5-23.5T400-720q0-33 23.5-56.5T480-800q33 0 56.5 23.5T560-720q0 33-23.5 56.5T480-640Z"
  />
</svg>`;
  const DOWNLOAD_ICON = w`<svg xmlns="http://www.w3.org/2000/svg" width="24" height="100%" viewBox="0 0 24 24" class="vot-loader" id="vot-loader-download">
  <path class="vot-loader-main" d="M12 15.575C11.8667 15.575 11.7417 15.5542 11.625 15.5125C11.5083 15.4708 11.4 15.4 11.3 15.3L7.7 11.7C7.5 11.5 7.40417 11.2667 7.4125 11C7.42083 10.7333 7.51667 10.5 7.7 10.3C7.9 10.1 8.1375 9.99583 8.4125 9.9875C8.6875 9.97917 8.925 10.075 9.125 10.275L11 12.15V5C11 4.71667 11.0958 4.47917 11.2875 4.2875C11.4792 4.09583 11.7167 4 12 4C12.2833 4 12.5208 4.09583 12.7125 4.2875C12.9042 4.47917 13 4.71667 13 5V12.15L14.875 10.275C15.075 10.075 15.3125 9.97917 15.5875 9.9875C15.8625 9.99583 16.1 10.1 16.3 10.3C16.4833 10.5 16.5792 10.7333 16.5875 11C16.5958 11.2667 16.5 11.5 16.3 11.7L12.7 15.3C12.6 15.4 12.4917 15.4708 12.375 15.5125C12.2583 15.5542 12.1333 15.575 12 15.575ZM6 20C5.45 20 4.97917 19.8042 4.5875 19.4125C4.19583 19.0208 4 18.55 4 18V16C4 15.7167 4.09583 15.4792 4.2875 15.2875C4.47917 15.0958 4.71667 15 5 15C5.28333 15 5.52083 15.0958 5.7125 15.2875C5.90417 15.4792 6 15.7167 6 16V18H18V16C18 15.7167 18.0958 15.4792 18.2875 15.2875C18.4792 15.0958 18.7167 15 19 15C19.2833 15 19.5208 15.0958 19.7125 15.2875C19.9042 15.4792 20 15.7167 20 16V18C20 18.55 19.8042 19.0208 19.4125 19.4125C19.0208 19.8042 18.55 20 18 20H6Z"/>
  <circle class="vot-loader-progress" cx="12" cy="12" r="9"></circle>
</svg>`;
  const SUBTITLES_ICON = w`<svg xmlns="http://www.w3.org/2000/svg" width="24" height="100%" viewBox="0 0 24 24">
  <path d="M4 20q-.825 0-1.413-.588T2 18V6q0-.825.588-1.413T4 4h16q.825 0 1.413.588T22 6v12q0 .825-.588 1.413T20 20H4Zm2-4h8v-2H6v2Zm10 0h2v-2h-2v2ZM6 12h2v-2H6v2Zm4 0h8v-2h-8v2Z"/>
</svg>`;
  const SETTINGS_ICON = w`<svg xmlns="http://www.w3.org/2000/svg" width="24" height="100%" viewBox="0 -960 960 960">
  <path d="M555-80H405q-15 0-26-10t-13-25l-12-93q-13-5-24.5-12T307-235l-87 36q-14 5-28 1t-22-17L96-344q-8-13-5-28t15-24l75-57q-1-7-1-13.5v-27q0-6.5 1-13.5l-75-57q-12-9-15-24t5-28l74-129q7-14 21.5-17.5T220-761l87 36q11-8 23-15t24-12l12-93q2-15 13-25t26-10h150q15 0 26 10t13 25l12 93q13 5 24.5 12t22.5 15l87-36q14-5 28-1t22 17l74 129q8 13 5 28t-15 24l-75 57q1 7 1 13.5v27q0 6.5-2 13.5l75 57q12 9 15 24t-5 28l-74 128q-8 13-22.5 17.5T738-199l-85-36q-11 8-23 15t-24 12l-12 93q-2 15-13 25t-26 10Zm-73-260q58 0 99-41t41-99q0-58-41-99t-99-41q-59 0-99.5 41T342-480q0 58 40.5 99t99.5 41Zm0-80q-25 0-42.5-17.5T422-480q0-25 17.5-42.5T482-540q25 0 42.5 17.5T542-480q0 25-17.5 42.5T482-420Zm-2-60Zm-40 320h79l14-106q31-8 57.5-23.5T639-327l99 41 39-68-86-65q5-14 7-29.5t2-31.5q0-16-2-31.5t-7-29.5l86-65-39-68-99 42q-22-23-48.5-38.5T533-694l-13-106h-79l-14 106q-31 8-57.5 23.5T321-633l-99-41-39 68 86 64q-5 15-7 30t-2 32q0 16 2 31t7 30l-86 65 39 68 99-42q22 23 48.5 38.5T427-266l13 106Z"/>
</svg>`;
  const CHEVRON_ICON = w`<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" >
  <path
    d="M12 14.975q-.2 0-.375-.062T11.3 14.7l-4.6-4.6q-.275-.275-.275-.7t.275-.7q.275-.275.7-.275t.7.275l3.9 3.9l3.9-3.9q.275-.275.7-.275t.7.275q.275.275.275.7t-.275.7l-4.6 4.6q-.15.15-.325.213t-.375.062Z"
  />
</svg>`;
  const ARROW_RIGHT_ICON = w`<svg xmlns="http://www.w3.org/2000/svg" height="24" viewBox="0 -960 960 960" width="24">
  <path
    d="M647-440H200q-17 0-28.5-11.5T160-480q0-17 11.5-28.5T200-520h447L451-716q-12-12-11.5-28t12.5-28q12-11 28-11.5t28 11.5l264 264q6 6 8.5 13t2.5 15q0 8-2.5 15t-8.5 13L508-188q-11 11-27.5 11T452-188q-12-12-12-28.5t12-28.5l195-195Z"
  />
</svg>`;
  const CLOSE_ICON = w`<svg xmlns="http://www.w3.org/2000/svg" width="24" height="100%" viewBox="0 -960 960 960">
  <path d="M480-424 284-228q-11 11-28 11t-28-11q-11-11-11-28t11-28l196-196-196-196q-11-11-11-28t11-28q11-11 28-11t28 11l196 196 196-196q11-11 28-11t28 11q11 11 11 28t-11 28L536-480l196 196q11 11 11 28t-11 28q-11 11-28 11t-28-11L480-424Z"/>
</svg>`;
  const CHECK_ICON = w`<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 -960 960 960">
  <path d="M382-240 154-468l57-57 171 171 367-367 57 57-424 424Z"/>
</svg>`;
  const VOICE_WAVE_ICON = w`<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" aria-hidden="true">
  <rect class="vot-voice-wave-bar vot-voice-wave-bar-1" x="3" y="8.5" width="3" height="7" rx="1.5" fill="currentColor"></rect>
  <rect class="vot-voice-wave-bar vot-voice-wave-bar-2" x="8" y="6" width="3" height="12" rx="1.5" fill="currentColor"></rect>
  <rect class="vot-voice-wave-bar vot-voice-wave-bar-3" x="13" y="4.5" width="3" height="15" rx="1.5" fill="currentColor"></rect>
  <rect class="vot-voice-wave-bar vot-voice-wave-bar-4" x="18" y="7" width="3" height="10" rx="1.5" fill="currentColor"></rect>
</svg>`;
  const WARNING_ICON = w`<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24">
  <g fill="none">
    <path d="m12.593 23.258l-.011.002l-.071.035l-.02.004l-.014-.004l-.071-.035q-.016-.005-.024.005l-.004.01l-.017.428l.005.02l.01.013l.104.074l.015.004l.012-.004l.104-.074l.012-.016l.004-.017l-.017-.427q-.004-.016-.017-.018m.265-.113l-.013.002l-.185.093l-.01.01l-.003.011l.018.43l.005.012l.008.007l.201.093q.019.005.029-.008l.004-.014l-.034-.614q-.005-.018-.02-.022m-.715.002a.02.02 0 0 0-.027.006l-.006.014l-.034.614q.001.018.017.024l.015-.002l.201-.093l.01-.008l.004-.011l.017-.43l-.003-.012l-.01-.01z"/><path fill="currentColor" d="M12 2c5.523 0 10 4.477 10 10s-4.477 10-10 10S2 17.523 2 12S6.477 2 12 2m0 2a8 8 0 1 0 0 16a8 8 0 0 0 0-16m0 11a1 1 0 1 1 0 2a1 1 0 0 1 0-2m0-9a1 1 0 0 1 1 1v6a1 1 0 1 1-2 0V7a1 1 0 0 1 1-1"/>
  </g>
</svg>`;
  const HELP_ICON = w`<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24">
  <g fill="none">
    <path d="m12.593 23.258l-.011.002l-.071.035l-.02.004l-.014-.004l-.071-.035q-.016-.005-.024.005l-.004.01l-.017.428l.005.02l.01.013l.104.074l.015.004l.012-.004l.104-.074l.012-.016l.004-.017l-.017-.427q-.004-.016-.017-.018m.265-.113l-.013.002l-.185.093l-.01.01l-.003.011l.018.43l.005.012l.008.007l.201.093q.019.005.029-.008l.004-.014l-.034-.614q-.005-.018-.02-.022m-.715.002a.02.02 0 0 0-.027.006l-.006.014l-.034.614q.001.018.017.024l.015-.002l.201-.093l.01-.008l.004-.011l.017-.43l-.003-.012l-.01-.01z"/><path fill="currentColor" d="M12 2c5.523 0 10 4.477 10 10s-4.477 10-10 10S2 17.523 2 12S6.477 2 12 2m0 2a8 8 0 1 0 0 16a8 8 0 0 0 0-16m0 12a1 1 0 1 1 0 2a1 1 0 0 1 0-2m0-9.5a3.625 3.625 0 0 1 1.348 6.99a.8.8 0 0 0-.305.201c-.044.05-.051.114-.05.18L13 14a1 1 0 0 1-1.993.117L11 14v-.25c0-1.153.93-1.845 1.604-2.116a1.626 1.626 0 1 0-2.229-1.509a1 1 0 1 1-2 0A3.625 3.625 0 0 1 12 6.5"/>
  </g>
</svg>`;
  const REFRESH_ICON = w`<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24">
  <g fill="none">
    <path d="m12.594 23.258l-.012.002l-.071.035l-.02.004l-.014-.004l-.071-.036q-.016-.004-.024.006l-.004.01l-.017.428l.005.02l.01.013l.104.074l.015.004l.012-.004l.104-.074l.012-.016l.004-.017l-.017-.427q-.004-.016-.016-.018m.264-.113l-.014.002l-.184.093l-.01.01l-.003.011l.018.43l.005.012l.008.008l.201.092q.019.005.029-.008l.004-.014l-.034-.614q-.005-.019-.02-.022m-.715.002a.02.02 0 0 0-.027.006l-.006.014l-.034.614q.001.018.017.024l.015-.002l.201-.093l.01-.008l.003-.011l.018-.43l-.003-.012l-.01-.01z"/>
    <path fill="currentColor" d="M20 9a1 1 0 0 1 1 1v1a8 8 0 0 1-8 8H9.414l.793.793a1 1 0 0 1-1.414 1.414l-2.496-2.496a1 1 0 0 1-.287-.567L6 17.991a1 1 0 0 1 .237-.638l.056-.06l2.5-2.5a1 1 0 0 1 1.414 1.414L9.414 17H13a6 6 0 0 0 6-6v-1a1 1 0 0 1 1-1m-4.793-6.207l2.5 2.5a1 1 0 0 1 0 1.414l-2.5 2.5a1 1 0 1 1-1.414-1.414L14.586 7H11a6 6 0 0 0-6 6v1a1 1 0 1 1-2 0v-1a8 8 0 0 1 8-8h3.586l-.793-.793a1 1 0 0 1 1.414-1.414"/>
  </g>
</svg>`;
  const KEY_ICON = w`<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24">
  <path fill="currentColor" d="M7 15q1.25 0 2.125-.875T10 12t-.875-2.125T7 9t-2.125.875T4 12t.875 2.125T7 15m0 3q-2.5 0-4.25-1.75T1 12t1.75-4.25T7 6q2.025 0 3.538 1.15T12.65 10h8.375L23 11.975l-3.5 4L17 14l-2 2l-2-2h-.35q-.625 1.8-2.175 2.9T7 18"/>
  </svg>`;
  const GOOGLE_DRIVE_BRIDGE_SYNC_EVENT = "vot:google-drive-bridge-sync";
  function dispatchGoogleDriveBridgeSync() {
    try {
      globalThis.dispatchEvent(new CustomEvent(GOOGLE_DRIVE_BRIDGE_SYNC_EVENT));
    } catch {
    }
  }
  const GOOGLE_DRIVE_OVERLAY_ROOT_ATTR = "data-vot-google-drive-overlay-root";
  const GOOGLE_DRIVE_POSITION_PATCH_ATTR = "data-vot-google-drive-overlay-position-patched";
  const GOOGLE_DRIVE_PROXY_CONTROLS_ATTR = "data-vot-google-drive-proxy-controls";
  const GOOGLE_DRIVE_PROXY_BUTTON_ATTR = "data-vot-google-drive-proxy-button";
  const GOOGLE_DRIVE_BRIDGE_FLAG$1 = "__votGoogleDriveBridge";
  const GOOGLE_DRIVE_ACTIVE_HANDLER_KEY$1 = "__VOT_GOOGLE_DRIVE_ACTIVE_HANDLER__";
  const GOOGLE_DRIVE_ACTIVITY_HIDE_DELAY_MS = 4e3;
  const PRIMARY_PLAYER_ROOT_SELECTORS = [
    "#movie_player",
    ".html5-video-player",
    "#player"
  ];
  const FALLBACK_PLAYER_ROOT_SELECTOR = ".html5-video-container";
  const PLAYER_ROOT_SELECTOR = [
    ...PRIMARY_PLAYER_ROOT_SELECTORS,
    FALLBACK_PLAYER_ROOT_SELECTOR
  ].join(", ");
  let installed$3 = false;
  let syncScheduled = false;
  let hideTimer = null;
  let playerRootObserver = null;
  let observedPlayerRoot = null;
  let buttonObserver = null;
  let observedButton = null;
  let topFrameBridgeConnected = false;
  function getHost() {
    return String(globalThis.location.hostname || "").toLowerCase();
  }
  function getWebkitFullscreenElement() {
    const doc = document;
    return doc.webkitFullscreenElement ?? null;
  }
  function getCurrentFullscreenElement() {
    const fullscreenEl = document.fullscreenElement ?? getWebkitFullscreenElement();
    return fullscreenEl instanceof HTMLElement ? fullscreenEl : null;
  }
  function isGoogleDriveInlineOverlayContext() {
    return getHost() === "youtube.googleapis.com" && globalThis.location.pathname.startsWith("/embed");
  }
  function findPlayerRootInScope(scope) {
    if (!scope || typeof scope.querySelector !== "function") {
      return null;
    }
    for (const selector of PRIMARY_PLAYER_ROOT_SELECTORS) {
      const matched = scope.querySelector(selector);
      if (matched?.isConnected) {
        return matched;
      }
    }
    const fallback = scope.querySelector(
      FALLBACK_PLAYER_ROOT_SELECTOR
    );
    return fallback?.isConnected ? fallback : null;
  }
  function findPreferredPlayerRootAncestor(start) {
    let fallback = null;
    let current = start;
    while (current instanceof HTMLElement) {
      if (PRIMARY_PLAYER_ROOT_SELECTORS.some(
        (selector) => current.matches(selector)
      )) {
        return current;
      }
      if (!fallback && current.matches(FALLBACK_PLAYER_ROOT_SELECTOR)) {
        fallback = current;
      }
      current = current.parentElement;
    }
    return fallback;
  }
  function resolveBestPlayerRoot(container) {
    const globalMoviePlayer = document.getElementById("movie_player");
    if (globalMoviePlayer instanceof HTMLElement && globalMoviePlayer.isConnected) {
      return globalMoviePlayer;
    }
    const globalHtml5Player = document.querySelector(
      ".html5-video-player"
    );
    if (globalHtml5Player?.isConnected) {
      return globalHtml5Player;
    }
    const fullscreenEl = getCurrentFullscreenElement();
    const video = document.querySelector("video");
    const explicitClosest = findPreferredPlayerRootAncestor(container);
    const videoClosest = findPreferredPlayerRootAncestor(video);
    const candidates = [
      fullscreenEl?.matches?.(PLAYER_ROOT_SELECTOR) ? fullscreenEl : null,
      findPlayerRootInScope(fullscreenEl),
      explicitClosest instanceof HTMLElement ? explicitClosest : null,
      findPlayerRootInScope(container),
      videoClosest instanceof HTMLElement ? videoClosest : null,
      findPlayerRootInScope(video?.parentElement ?? null),
      findPlayerRootInScope(document),
      video?.parentElement instanceof HTMLElement ? video.parentElement : null,
      container instanceof HTMLElement ? container : null,
      document.body,
      document.documentElement
    ];
    return candidates.find(
      (candidate) => Boolean(candidate?.isConnected)
    ) ?? null;
  }
  function ensurePlayerRootPositioning(playerRoot) {
    const computed = globalThis.getComputedStyle(playerRoot);
    if (computed.position !== "static") {
      return;
    }
    playerRoot.setAttribute(GOOGLE_DRIVE_POSITION_PATCH_ATTR, "true");
    playerRoot.style.position = "relative";
  }
  function ensureOverlayRootStyles(overlayRoot2) {
    overlayRoot2.style.position = "fixed";
    overlayRoot2.style.left = "0";
    overlayRoot2.style.top = "0";
    overlayRoot2.style.width = "0";
    overlayRoot2.style.height = "0";
    overlayRoot2.style.margin = "0";
    overlayRoot2.style.padding = "0";
    overlayRoot2.style.border = "0";
    overlayRoot2.style.background = "transparent";
    overlayRoot2.style.overflow = "visible";
    overlayRoot2.style.pointerEvents = "none";
    overlayRoot2.style.zIndex = "2147483647";
    overlayRoot2.style.isolation = "isolate";
  }
  function syncOverlayRootGeometry(overlayRoot2, playerRoot) {
    const rect = playerRoot.getBoundingClientRect();
    overlayRoot2.style.left = `${Math.round(rect.left)}px`;
    overlayRoot2.style.top = `${Math.round(rect.top)}px`;
    overlayRoot2.style.width = `${Math.round(rect.width)}px`;
    overlayRoot2.style.height = `${Math.round(rect.height)}px`;
  }
  function getExistingOverlayRoot() {
    return document.querySelector(
      `[${GOOGLE_DRIVE_OVERLAY_ROOT_ATTR}="1"]`
    );
  }
  function ensureGoogleDriveOverlayRoot(container) {
    if (!isGoogleDriveInlineOverlayContext()) {
      return null;
    }
    const playerRoot = resolveBestPlayerRoot(container);
    if (!playerRoot) {
      return null;
    }
    ensurePlayerRootPositioning(playerRoot);
    let overlayRoot2 = getExistingOverlayRoot();
    if (!overlayRoot2) {
      overlayRoot2 = document.createElement("vot-block");
      overlayRoot2.setAttribute(GOOGLE_DRIVE_OVERLAY_ROOT_ATTR, "1");
      ensureOverlayRootStyles(overlayRoot2);
    } else {
      ensureOverlayRootStyles(overlayRoot2);
    }
    syncOverlayRootGeometry(overlayRoot2, playerRoot);
    const host = document.body ?? document.documentElement;
    if (overlayRoot2.parentElement !== host) {
      host.appendChild(overlayRoot2);
    } else if (overlayRoot2 !== host.lastElementChild) {
      host.appendChild(overlayRoot2);
    }
    return overlayRoot2;
  }
  function getPreferredControlsContainer(playerRoot) {
    const selectors = [
      ".ytp-right-controls",
      ".ytp-left-controls",
      ".ytp-chrome-controls",
      ".ytp-chrome-bottom"
    ];
    for (const selector of selectors) {
      const matched = playerRoot.querySelector(selector);
      if (matched?.isConnected) {
        return matched;
      }
    }
    return null;
  }
  function ensureGoogleDriveControlsHost(container) {
    if (!isGoogleDriveInlineOverlayContext()) {
      return null;
    }
    const playerRoot = resolveBestPlayerRoot(container);
    if (!playerRoot) {
      return null;
    }
    const controlsContainer = getPreferredControlsContainer(playerRoot) ?? playerRoot;
    return controlsContainer;
  }
  function createProxyButton(kind, iconTemplate, ariaLabel) {
    const button2 = document.createElement("button");
    button2.type = "button";
    button2.className = "ytp-button vot-google-drive-proxy-button";
    button2.dataset.votGoogleDriveProxyButton = kind;
    button2.setAttribute(GOOGLE_DRIVE_PROXY_BUTTON_ATTR, kind);
    button2.setAttribute("aria-label", ariaLabel);
    const icon = document.createElement("span");
    icon.className = `vot-google-drive-proxy-button-icon vot-google-drive-proxy-button-icon--${kind}`;
    D(iconTemplate, icon);
    button2.appendChild(icon);
    return button2;
  }
  function getExistingProxyControls() {
    const container = document.querySelector(
      `[${GOOGLE_DRIVE_PROXY_CONTROLS_ATTR}="1"]`
    );
    if (!container) {
      return null;
    }
    const translateButton = container.querySelector(
      `[${GOOGLE_DRIVE_PROXY_BUTTON_ATTR}="translate"]`
    );
    const voiceMenuButton = container.querySelector(
      `[${GOOGLE_DRIVE_PROXY_BUTTON_ATTR}="voice-menu"]`
    );
    const pipButton = container.querySelector(
      `[${GOOGLE_DRIVE_PROXY_BUTTON_ATTR}="pip"]`
    );
    const menuButton = container.querySelector(
      `[${GOOGLE_DRIVE_PROXY_BUTTON_ATTR}="menu"]`
    );
    if (!translateButton || !voiceMenuButton || !pipButton || !menuButton) {
      return null;
    }
    return {
      container,
      translateButton,
      voiceMenuButton,
      pipButton,
      menuButton
    };
  }
  function findCurrentVotButtonContainer() {
    return document.querySelector(".vot-segmented-button");
  }
  function findCurrentVotButtonPart(selector) {
    return findCurrentVotButtonContainer()?.querySelector(selector) ?? null;
  }
  function findCurrentVoiceMenuButton() {
    return findCurrentVotButtonPart(".vot-voice-menu-button");
  }
  function findCurrentPipButton() {
    const container = findCurrentVotButtonContainer();
    const icons = Array.from(
      container?.querySelectorAll(".vot-segment-only-icon") ?? []
    );
    return icons.find(
      (icon) => /picture in picture|pip/i.test(icon.getAttribute("aria-label") ?? "")
    ) ?? icons[0] ?? null;
  }
  function findCurrentMenuButton() {
    const container = findCurrentVotButtonContainer();
    const icons = Array.from(
      container?.querySelectorAll(".vot-segment-only-icon") ?? []
    );
    return icons.find((icon) => /menu/i.test(icon.getAttribute("aria-label") ?? "")) ?? icons[1] ?? null;
  }
  function getCurrentQuickMenuHeaderButtons() {
    const header = document.querySelector(
      ".vot-menu-header-container"
    );
    if (!header) {
      return {
        downloadTranslation: null,
        downloadSubtitles: null,
        openSettings: null
      };
    }
    const buttons = Array.from(header.children).filter(
      (child) => child instanceof HTMLElement && child.classList.contains("vot-icon-button")
    );
    return {
      downloadTranslation: buttons[0] ?? null,
      downloadSubtitles: buttons[1] ?? null,
      openSettings: buttons[2] ?? null
    };
  }
  function dispatchUnderlyingPrimaryAction(target) {
    if (!target) {
      return;
    }
    const pointerEvent = new PointerEvent("pointerdown", {
      bubbles: true,
      cancelable: true,
      composed: true,
      button: 0,
      pointerType: "mouse",
      isPrimary: true
    });
    target.dispatchEvent(pointerEvent);
    target.click?.();
  }
  function findCurrentVoiceModeMenuItem(mode) {
    return document.querySelector(
      `.vot-voice-mode-menu-item[data-mode="${mode}"]`
    );
  }
  function getActiveGoogleDriveHandler() {
    const value = globalThis[GOOGLE_DRIVE_ACTIVE_HANDLER_KEY$1];
    return value && typeof value === "object" ? value : null;
  }
  function getActiveGoogleDriveUiManager() {
    return getActiveGoogleDriveHandler()?.uiManager ?? null;
  }
  function collectBridgeStatePayload() {
    const button2 = findCurrentVotButtonContainer();
    const labelText = button2?.querySelector(".vot-segment-label")?.textContent?.trim() ?? "";
    const uiManager = getActiveGoogleDriveUiManager();
    const quickMenu2 = uiManager?.getDriveQuickMenuState?.() ?? null;
    const voiceState = uiManager?.getDriveVoiceState?.() ?? null;
    const isLoading = typeof voiceState?.loading === "boolean" ? voiceState.loading : button2?.dataset.loading === "true";
    return {
      status: button2?.dataset.status ?? "none",
      loading: isLoading,
      voiceMode: voiceState?.voiceMode ?? button2?.dataset.voiceMode ?? "standard",
      voicePlaybackState: voiceState?.voicePlaybackState ?? button2?.dataset.voicePlaybackState ?? "idle",
      labelText,
      pipVisible: !findCurrentPipButton()?.hidden,
      canDownloadTranslation: !getCurrentQuickMenuHeaderButtons().downloadTranslation?.hidden,
      canDownloadSubtitles: !getCurrentQuickMenuHeaderButtons().downloadSubtitles?.hidden,
      quickMenu: quickMenu2
    };
  }
  function postBridgeStateToParent() {
    if (!topFrameBridgeConnected || globalThis.parent === globalThis) {
      return;
    }
    globalThis.parent.postMessage(
      {
        [GOOGLE_DRIVE_BRIDGE_FLAG$1]: true,
        kind: "state",
        state: collectBridgeStatePayload()
      },
      "*"
    );
  }
  async function executeBridgeCommand(command, mode, value) {
    const handler = getActiveGoogleDriveHandler();
    const uiManager = handler?.uiManager;
    if (command === "request-state") {
      await handler?.ensureSubtitlesForCurrentLangPair?.();
      await handler?.updateSubtitlesLangSelect?.();
      return;
    }
    if (command === "select-voice-mode") {
      dispatchUnderlyingPrimaryAction(
        mode ? findCurrentVoiceModeMenuItem(mode) : null
      );
      return;
    }
    if (command === "translate") {
      dispatchUnderlyingPrimaryAction(
        findCurrentVotButtonPart(".vot-translate-button")
      );
      return;
    }
    if (command === "pip") {
      dispatchUnderlyingPrimaryAction(findCurrentPipButton());
      return;
    }
    if (command === "menu") {
      dispatchUnderlyingPrimaryAction(findCurrentMenuButton());
      return;
    }
    const headerButtons = getCurrentQuickMenuHeaderButtons();
    if (command === "open-settings") {
      dispatchUnderlyingPrimaryAction(headerButtons.openSettings);
      return;
    }
    if (command === "download-translation") {
      dispatchUnderlyingPrimaryAction(headerButtons.downloadTranslation);
      return;
    }
    if (command === "download-subtitles") {
      dispatchUnderlyingPrimaryAction(headerButtons.downloadSubtitles);
      return;
    }
    if (command === "set-from-language" && typeof value === "string") {
      await uiManager?.applyDriveFromLanguage?.(value);
      return;
    }
    if (command === "set-to-language" && typeof value === "string") {
      await uiManager?.applyDriveToLanguage?.(value);
      return;
    }
    if (command === "set-subtitles" && typeof value === "string") {
      await uiManager?.applyDriveSubtitles?.(value);
      return;
    }
    if (command === "set-video-volume" && typeof value === "number") {
      uiManager?.applyDriveVideoVolume?.(value);
      return;
    }
    if (command === "set-translation-volume" && typeof value === "number") {
      uiManager?.applyDriveTranslationVolume?.(value);
      return;
    }
    if (command === "set-sync-volume" && typeof value === "boolean") {
      uiManager?.applyDriveSyncVolume?.(value);
      return;
    }
    if (command === "set-show-video-slider" && typeof value === "boolean") {
      uiManager?.applyDriveShowVideoSlider?.(value);
      return;
    }
    if (command === "set-audio-booster" && typeof value === "boolean") {
      uiManager?.applyDriveAudioBooster?.(value);
      return;
    }
    if (command === "set-use-lively-voice" && typeof value === "boolean") {
      uiManager?.applyDriveUseLivelyVoice?.(value);
    }
  }
  function handleTopFrameBridgeMessage(event) {
    const data = event.data;
    if (!data || typeof data !== "object" || !(data[GOOGLE_DRIVE_BRIDGE_FLAG$1] === true)) {
      return;
    }
    const payload = data;
    const kind = payload.kind;
    if (kind === "hello") {
      topFrameBridgeConnected = true;
      scheduleSyncAndShow();
      postBridgeStateToParent();
      return;
    }
    if (kind !== "command") {
      return;
    }
    topFrameBridgeConnected = true;
    const command = payload.command;
    const mode = payload.mode;
    const value = payload.value;
    if (command === "translate" || command === "pip" || command === "menu" || command === "request-state" || command === "select-voice-mode" || command === "open-settings" || command === "download-translation" || command === "download-subtitles" || command === "set-from-language" || command === "set-to-language" || command === "set-subtitles" || command === "set-video-volume" || command === "set-translation-volume" || command === "set-sync-volume" || command === "set-show-video-slider" || command === "set-audio-booster" || command === "set-use-lively-voice") {
      void executeBridgeCommand(
        command,
        mode === "lively" || mode === "standard" ? mode : void 0,
        typeof value === "string" || typeof value === "number" || typeof value === "boolean" ? value : void 0
      ).finally(() => {
        globalThis.setTimeout(() => {
          scheduleSyncAndShow();
          postBridgeStateToParent();
        }, 0);
      });
    }
  }
  function bindProxyButton(proxyButton, resolveTarget) {
    if (proxyButton.dataset.bound === "true") {
      return;
    }
    proxyButton.dataset.bound = "true";
    proxyButton.addEventListener("click", (event) => {
      event.preventDefault();
      event.stopPropagation();
      dispatchUnderlyingPrimaryAction(resolveTarget());
    });
  }
  function ensureGoogleDriveProxyControls(controlsHost) {
    let controls = getExistingProxyControls();
    if (!controls) {
      const container = document.createElement("div");
      container.className = "vot-google-drive-proxy-controls";
      container.setAttribute(GOOGLE_DRIVE_PROXY_CONTROLS_ATTR, "1");
      const translateButton = createProxyButton(
        "translate",
        TRANSLATE_ICON_SVG,
        "Translate video"
      );
      const voiceMenuButton = createProxyButton(
        "voice-menu",
        CHEVRON_ICON,
        "Select voice mode"
      );
      voiceMenuButton.setAttribute("aria-haspopup", "menu");
      voiceMenuButton.setAttribute("aria-expanded", "false");
      const pipButton = createProxyButton(
        "pip",
        PIP_ICON_SVG,
        "Picture in picture"
      );
      const menuButton = createProxyButton("menu", MENU_ICON, "Menu");
      menuButton.setAttribute("aria-haspopup", "dialog");
      menuButton.setAttribute("aria-expanded", "false");
      container.append(translateButton, voiceMenuButton, pipButton, menuButton);
      controls = {
        container,
        translateButton,
        voiceMenuButton,
        pipButton,
        menuButton
      };
    }
    bindProxyButton(
      controls.translateButton,
      () => findCurrentVotButtonPart(".vot-translate-button")
    );
    bindProxyButton(controls.voiceMenuButton, findCurrentVoiceMenuButton);
    bindProxyButton(controls.pipButton, findCurrentPipButton);
    bindProxyButton(controls.menuButton, findCurrentMenuButton);
    if (topFrameBridgeConnected) {
      controls.container.remove();
      return controls;
    }
    const insertionAnchor = controlsHost.querySelector(
      ".ytp-subtitles-button, .ytp-settings-button"
    ) ?? controlsHost.firstElementChild;
    if (controls.container.parentElement !== controlsHost) {
      if (insertionAnchor && insertionAnchor.parentElement === controlsHost) {
        controlsHost.insertBefore(controls.container, insertionAnchor);
      } else {
        controlsHost.appendChild(controls.container);
      }
    } else if (insertionAnchor && insertionAnchor.parentElement === controlsHost && controls.container.nextElementSibling !== insertionAnchor) {
      controlsHost.insertBefore(controls.container, insertionAnchor);
    }
    return controls;
  }
  function syncProxyControlsState(proxyControls, button2) {
    if (!button2) {
      proxyControls.container.hidden = true;
      return;
    }
    if (topFrameBridgeConnected) {
      proxyControls.container.remove();
      return;
    }
    proxyControls.container.hidden = false;
    const status = button2.dataset.status ?? "none";
    const voiceMode = button2.dataset.voiceMode ?? "standard";
    const playbackState = button2.dataset.voicePlaybackState ?? "idle";
    const loading = button2.dataset.loading === "true";
    const translateSource = button2.querySelector(
      ".vot-translate-button"
    );
    const voiceMenuSource = findCurrentVoiceMenuButton();
    const pipSource = findCurrentPipButton();
    const menuSource = findCurrentMenuButton();
    proxyControls.container.dataset.status = status;
    proxyControls.container.dataset.voiceMode = voiceMode;
    proxyControls.container.dataset.voicePlaybackState = playbackState;
    proxyControls.container.dataset.loading = String(loading);
    proxyControls.translateButton.dataset.status = status;
    proxyControls.translateButton.dataset.voiceMode = voiceMode;
    proxyControls.translateButton.dataset.voicePlaybackState = playbackState;
    proxyControls.translateButton.dataset.loading = String(loading);
    proxyControls.translateButton.setAttribute(
      "aria-label",
      translateSource?.getAttribute("aria-label") || "Translate video"
    );
    proxyControls.voiceMenuButton.dataset.voiceMode = voiceMode;
    proxyControls.voiceMenuButton.setAttribute(
      "aria-label",
      voiceMenuSource?.getAttribute("aria-label") || "Select voice mode"
    );
    proxyControls.voiceMenuButton.setAttribute(
      "aria-expanded",
      voiceMenuSource?.getAttribute("aria-expanded") || "false"
    );
    proxyControls.menuButton.setAttribute(
      "aria-label",
      menuSource?.getAttribute("aria-label") || "Menu"
    );
    proxyControls.menuButton.setAttribute(
      "aria-expanded",
      menuSource?.getAttribute("aria-expanded") || "false"
    );
    proxyControls.pipButton.hidden = Boolean(pipSource?.hidden);
    proxyControls.pipButton.setAttribute(
      "aria-label",
      pipSource?.getAttribute("aria-label") || "Picture in picture"
    );
    const isActive = status === "success" || loading || playbackState === "loading" || playbackState === "playing" || playbackState === "paused";
    proxyControls.translateButton.classList.toggle("is-active", isActive);
    proxyControls.translateButton.classList.toggle(
      "is-disabled",
      status === "disabled"
    );
  }
  function getOverlayNodes() {
    const overlayRoot2 = ensureGoogleDriveOverlayRoot();
    if (!overlayRoot2) {
      return null;
    }
    const playerRoot = resolveBestPlayerRoot();
    if (!(playerRoot instanceof HTMLElement)) {
      return null;
    }
    const controlsHost = ensureGoogleDriveControlsHost(playerRoot);
    if (!(controlsHost instanceof HTMLElement)) {
      return null;
    }
    const proxyControls = ensureGoogleDriveProxyControls(controlsHost);
    return {
      overlayRoot: overlayRoot2,
      playerRoot,
      controlsHost,
      proxyControls,
      button: document.querySelector(".vot-segmented-button"),
      menu: document.querySelector(".vot-menu"),
      voiceModeMenu: document.querySelector(".vot-voice-mode-menu")
    };
  }
  function syncOverlayNodes() {
    const nodes = getOverlayNodes();
    if (!nodes) {
      return null;
    }
    if (nodes.button) {
      nodes.button.classList.remove("vot-google-drive-native-controls");
      nodes.button.classList.add("vot-google-drive-proxy-source");
      nodes.button.style.position = "absolute";
      nodes.button.style.left = "-9999px";
      nodes.button.style.top = "-9999px";
      nodes.button.style.right = "auto";
      nodes.button.style.bottom = "auto";
      nodes.button.style.transform = "none";
      nodes.button.style.margin = "0";
      nodes.button.style.maxWidth = "0";
      nodes.button.style.zIndex = "1";
      if (nodes.button.parentElement !== nodes.overlayRoot) {
        nodes.overlayRoot.appendChild(nodes.button);
      }
      syncProxyControlsState(nodes.proxyControls, nodes.button);
    }
    for (const node of [nodes.menu, nodes.voiceModeMenu]) {
      if (!node) continue;
      if (node.parentElement !== nodes.overlayRoot) {
        nodes.overlayRoot.appendChild(node);
      } else if (node !== nodes.overlayRoot.lastElementChild) {
        nodes.overlayRoot.appendChild(node);
      }
    }
    return nodes;
  }
  function isMenuOpen(node) {
    return Boolean(node && !node.hidden);
  }
  function clearHideTimer() {
    if (hideTimer !== null) {
      globalThis.clearTimeout(hideTimer);
      hideTimer = null;
    }
  }
  function hideButton() {
    const nodes = getOverlayNodes();
    if (!nodes?.button) {
      return;
    }
    const activeElement = document.activeElement;
    const shouldKeepVisible = isMenuOpen(nodes.menu) || isMenuOpen(nodes.voiceModeMenu) || activeElement instanceof Node && (nodes.overlayRoot.contains(activeElement) || nodes.button.contains(activeElement));
    if (shouldKeepVisible) {
      return;
    }
    nodes.button.style.opacity = "0";
    nodes.button.classList.add("vot-segmented-button--hidden");
  }
  function showButton() {
    const nodes = syncOverlayNodes();
    if (!nodes?.button) {
      return;
    }
    nodes.button.hidden = false;
    nodes.button.removeAttribute("hidden");
    nodes.button.style.opacity = "1";
    nodes.button.classList.remove("vot-segmented-button--hidden");
    clearHideTimer();
    hideTimer = globalThis.setTimeout(
      () => hideButton(),
      GOOGLE_DRIVE_ACTIVITY_HIDE_DELAY_MS
    );
  }
  function isActivityInsidePlayer(target) {
    if (!(target instanceof Node)) {
      return false;
    }
    const nodes = getOverlayNodes();
    if (!nodes) {
      return false;
    }
    return nodes.playerRoot.contains(target) || nodes.overlayRoot.contains(target);
  }
  function scheduleSyncAndShow() {
    if (syncScheduled) {
      return;
    }
    syncScheduled = true;
    globalThis.requestAnimationFrame(() => {
      syncScheduled = false;
      syncOverlayNodes();
      showButton();
      observeCurrentPlayerRoot();
      observeCurrentButton();
      postBridgeStateToParent();
    });
  }
  function observeCurrentPlayerRoot() {
    const nodes = getOverlayNodes();
    const nextPlayerRoot = nodes?.playerRoot ?? null;
    if (observedPlayerRoot === nextPlayerRoot) {
      return;
    }
    playerRootObserver?.disconnect();
    playerRootObserver = null;
    observedPlayerRoot = nextPlayerRoot;
    if (!nextPlayerRoot) {
      return;
    }
    playerRootObserver = new MutationObserver(() => {
      scheduleSyncAndShow();
    });
    playerRootObserver.observe(nextPlayerRoot, {
      attributes: true,
      attributeFilter: ["class", "style", "hidden"],
      childList: true,
      subtree: true
    });
  }
  function observeCurrentButton() {
    const nextButton = findCurrentVotButtonContainer();
    if (observedButton === nextButton) {
      return;
    }
    buttonObserver?.disconnect();
    buttonObserver = null;
    observedButton = nextButton;
    if (!nextButton) {
      return;
    }
    buttonObserver = new MutationObserver(() => {
      scheduleSyncAndShow();
    });
    buttonObserver.observe(nextButton, {
      attributes: true,
      attributeFilter: [
        "class",
        "style",
        "hidden",
        "aria-expanded",
        "aria-label",
        "data-status",
        "data-loading",
        "data-voice-mode",
        "data-voice-playback-state"
      ],
      characterData: true,
      childList: true,
      subtree: true
    });
  }
  function installGoogleDriveOverlayPatch() {
    if (installed$3 || !isGoogleDriveInlineOverlayContext()) {
      return;
    }
    installed$3 = true;
    globalThis.addEventListener("message", handleTopFrameBridgeMessage);
    globalThis.addEventListener(GOOGLE_DRIVE_BRIDGE_SYNC_EVENT, () => {
      scheduleSyncAndShow();
    });
    const activityHandler = (event) => {
      if (!isActivityInsidePlayer(event.target)) {
        return;
      }
      showButton();
    };
    scheduleSyncAndShow();
    document.addEventListener("pointermove", activityHandler, {
      passive: true,
      capture: true
    });
    document.addEventListener("mousemove", activityHandler, {
      passive: true,
      capture: true
    });
    document.addEventListener("pointerdown", activityHandler, {
      passive: true,
      capture: true
    });
    document.addEventListener("pointerenter", activityHandler, {
      passive: true,
      capture: true
    });
    document.addEventListener(
      "focusin",
      () => {
        showButton();
      },
      true
    );
    document.addEventListener(
      "play",
      () => {
        scheduleSyncAndShow();
      },
      true
    );
    document.addEventListener(
      "playing",
      () => {
        scheduleSyncAndShow();
      },
      true
    );
    document.addEventListener(
      "pause",
      () => {
        scheduleSyncAndShow();
      },
      true
    );
    const fullscreenHandler = () => {
      scheduleSyncAndShow();
    };
    document.addEventListener("fullscreenchange", fullscreenHandler, true);
    document.addEventListener("webkitfullscreenchange", fullscreenHandler, true);
    window.addEventListener("resize", fullscreenHandler, { passive: true });
    window.addEventListener("scroll", fullscreenHandler, {
      passive: true,
      capture: true
    });
    const observer = new MutationObserver(() => {
      scheduleSyncAndShow();
    });
    observer.observe(document.documentElement, {
      childList: true,
      subtree: true
    });
  }
  const recommended$Z = "aanbeveel";
  const translateVideo$Z = "Vertaal video";
  const disableTranslate$Z = "Skakel af";
  const translationSettings$Z = "Vertaling instellings";
  const subtitlesSettings$Z = "Onderskrifte instellings";
  const resetSettings$Z = "Herstel instellings";
  const videoBeingTranslated$Z = "Die video word vertaal";
  const videoLanguage$Z = "Video taal";
  const translationLanguage$Z = "Vertaaltaal";
  const translationTake$Z = "Die vertaling sal";
  const translationTakeMoreThanHour$Z = "Die vertaling sal meer as'n uur neem";
  const translationTakeAboutMinute$Z = "Die vertaling sal ongeveer'n minuut neem";
  const translationTakeFewMinutes$Z = "Die vertaling sal'n paar minute neem";
  const translationTakeApproximatelyMinutes$Z = "Die vertaling sal ongeveer {0} minute neem";
  const translationTakeApproximatelyMinute$Z = "Die vertaling sal ongeveer {0} minute neem";
  const requestTranslationFailed$Z = "Kon nie video-vertaling aanvra nie";
  const audioNotReceived$Z = "Oudio skakel nie ontvang nie";
  const audioFormatNotSupported$Z = "Die oudioformaat word nie ondersteun nie";
  const VOTAutoTranslate$Z = "Vertaal op open";
  const VOTDontTranslateYourLang$Z = "Moenie uit my taal vertaal nie";
  const VOTVolume$Z = "Video-volume:";
  const VOTVolumeTranslation$Z = "Vertaalvolume:";
  const VOTAutoSetVolume$Z = "Verminder video volume tot";
  const VOTShowVideoSlider$Z = "Video volume skuifbalk";
  const VOTSyncVolume$Z = "Skakel vertaling en video volume";
  const VOTDisableFromYourLang$Z = "U het die vertaling van die video in u taal gedeaktiveer";
  const VOTVideoIsTooLong$Z = "Die Video is te lank";
  const VOTNoVideoIDFound$Z = "Geen video-ID gevind nie";
  const VOTSubtitles$Z = "Onderskrifte";
  const VOTSubtitlesDisabled$Z = "Gedeaktiveer";
  const VOTSubtitlesMaxLength$Z = "Onderskrifte maksimum lengte";
  const VOTHighlightWords$Z = "Verlig woorde";
  const VOTTranslatedFrom$Z = "vertaling van";
  const VOTAutogenerated$Z = "outomaties gegenereer";
  const VOTSettings$Z = "Vot Instellings";
  const VOTMenuLanguage$Z = "Kieslystaal";
  const VOTAuthors$Z = "Skrywers";
  const VOTVersion$Z = "Weergawe";
  const VOTLoader$Z = "Laaier";
  const VOTBrowser$Z = "Blaaier";
  const VOTShowPiPButton$Z = "Wys Pypknoppie";
  const langs$Z = { "auto": "Outomatiese", "af": "Afrikaans", "ak": "Akan", "sq": "Albanees", "am": "Amharies", "ar": "Arabiese", "hy": "Armeens", "as": "Assamese", "ay": "Aymara", "az": "Azerbeidjans", "bn": "Bangla", "eu": "Baskies", "be": "Wit-russies", "bho": "Bhojpuri", "bs": "Bosnies", "bg": "Bulgaars", "my": "Birmaanse", "ca": "Katalaans", "ceb": "Cebuano", "zh": "Chinese", "zh-Hans": "Chinese (Vereenvoudigde)", "zh-Hant": "Sjinees (Tradisioneel)", "co": "Korsikaans", "hr": "Kroaties", "cs": "Tsjeggies", "da": "Deense", "dv": "Die Divehi", "nl": "Nederlands", "en": "Engels", "eo": "Esperanto", "et": "Estnies", "ee": "Ewe", "fil": "Filippynse", "fi": "Finse taal", "fr": "Frans", "gl": "Galisies", "lg": "Die Ganda", "ka": "Georgies", "de": "Duits", "el": "Grieks", "gn": "Die Guarani", "gu": "Gujarati", "ht": "Haïtiaanse Kreools", "ha": "Hausa", "haw": "Hawaiiaanse", "iw": "Hebreeus", "hi": "Hindi", "hmn": "Hmong", "hu": "Hongaars", "is": "Yslands", "ig": "Igbo", "id": "Indonesiese", "ga": "Ierse", "it": "Italiaanse", "ja": "Japannese", "jv": "Javaanse", "kn": "Kannada", "kk": "Kazakhname", "km": "Khmer", "rw": "Kinyarwanda", "ko": "Koreaans", "kri": "Krio", "ku": "Koerdiese", "ky": "Kirgisies", "lo": "Lao", "la": "Latyn", "lv": "Letse", "ln": "Lingala", "lt": "Litaus", "lb": "Luxemburgse", "mk": "Macedonies", "mg": "Malgassiese", "ms": "Maleis", "ml": "Malayalam", "mt": "Maltese", "mi": "M", "mr": "Marathi", "mn": "Mongoliese", "ne": "Nepali", "nso": "Noord-Sotho", "no": "Noorse", "ny": "Nyanja", "or": "Odia", "om": "Oromo", "ps": "Paasjoen", "fa": "Persiese", "pl": "Pools", "pt": "Portugees", "pa": "Punjabi", "qu": "Quechua", "ro": "Roemeens", "ru": "Russiese", "sm": "Samoaans", "sa": "Sanskrit", "gd": "Skotse Gaelies", "sr": "Serwies", "sn": "Shona", "sd": "Sindhi", "si": "Sinhalese", "sk": "Slowaaks", "sl": "Sloweens", "so": "Somali", "st": "Suid-Sotho", "es": "Spaans", "su": "Sundanese", "sw": "Swahili", "sv": "Sweedse", "tg": "Tadzjiekse", "ta": "Tamil", "tt": "Tatar", "te": "Die taal", "th": "Thai", "ti": "Tigrinya", "ts": "Tsonga", "tr": "Turks", "tk": "Turkmeens", "uk": "Oekraïens", "ur": "Urdu", "ug": "Oeigoere", "uz": "Oezbeeks", "vi": "Viëtnamese", "cy": "Wallies", "fy": "Wes-Fries", "xh": "Xhosa", "yi": "Jiddisj", "yo": "Joruba", "zu": "Zoeloe" };
  const streamNoConnectionToServer$Z = "Daar is geen verbinding met die bediener nie";
  const searchField$Z = "Soek...";
  const VOTTranslateAPIErrors$Z = "Vertaal foute UIT DIE API";
  const VOTDetectService$Z = "Taalopsporingsdiens";
  const VOTProxyWorkerHost$Z = "Voer die volmagwerker-adres in";
  const VOTM3u8ProxyHost$Z = "Voer die adres van die m3u8-volmagwerker in";
  const proxySettings$Z = "Instaaninstellings";
  const translationTakeApproximatelyMinute2$Z = "Die vertaling sal ongeveer {0} minute neem";
  const VOTAudioBooster$Z = "Uitgebreide vertaalvolume-verhoging";
  const VOTSubtitlesDesign$Z = "Onderskrifte ontwerp";
  const VOTSubtitlesFontSize$Z = "Lettergrootte van onderskrifte";
  const VOTSubtitlesOpacity$Z = "Deursigtigheid van die ondertitel agtergrond";
  const VOTSubtitlesDownloadFormat$Z = "Die formaat vir die aflaai van onderskrifte";
  const VOTDownloadWithName$Z = "Laai lêers af met die video naam";
  const VOTUpdateLocaleFiles$Z = "Dateer lokaliseringslêers op";
  const VOTLocaleHash$Z = "Plaaslike hash";
  const VOTUpdatedAt$Z = "Opgedateer om";
  const VOTNeedWebAudioAPI$Z = "Om dit te aktiveer, moet jy'n Web Audio API";
  const VOTMediaCSPEnabledOnSite$Z = "MEDIA CSP is geaktiveer op hierdie webwerf";
  const VOTOnlyBypassMediaCSP$Z = "Gebruik dit slegs vir omseil Media CSP";
  const VOTNewAudioPlayer$Z = "Gebruik die nuwe oudiospeler";
  const VOTUseNewModel$Z = "Gebruik'n eksperimentele variasie Van Yandex-stemme vir sommige video's";
  const TranslationDelayed$Z = "Die vertaling is effens vertraag";
  const VOTTranslationCompletedNotify$Z = "Die vertaling op die {0} is voltooi!";
  const VOTSendNotifyOnComplete$Z = "Stuur'n kennisgewing dat die video vertaal is";
  const VOTBugReport$Z = "Rapporteer'n fout";
  const VOTTranslateProxyDisabled$Z = "Gedeaktiveer";
  const VOTTranslateProxyEnabled$Z = "Geaktiveer";
  const VOTTranslateProxyEverything$Z = "Volmag alles";
  const VOTTranslateProxyStatus$Z = "Volmagmodus";
  const VOTTranslatedBy$Z = "Vertaal deur {0}";
  const VOTStreamNotAvailable$Z = "Vertaal stroom is nie beskikbaar nie";
  const VOTTranslationTextService$Z = "Teksvertalingsdiens";
  const VOTNotAffectToVoice$Z = "Beïnvloed nie die vertaling van teks in voice over nie";
  const DontTranslateSelectedLanguages$Z = "Moenie uit geselekteerde tale vertaal nie";
  const showVideoVolumeSlider$Z = "Vertoon die video volume skuifbalk";
  const hotkeysSettings$Z = "Sneltoetse instellings";
  const None$Z = "Geen";
  const VOTUseLivelyVoice$Z = "Gebruik lewendige stemme. Sprekers klink soos Inheemse Russe.";
  const miscSettings$Z = "Verskeie instellings";
  const services$Z = { "yandexbrowser": "Yandex Browser", "msedge": "Microsoft Edge (Afrikaans)", "rust-server": "Rust Bediener" };
  const aboutExtension$Z = "Oor uitbreiding";
  const appearance$Z = "Voorkoms";
  const buttonPosition$Z = "Knoppie posisie in die speler";
  const position$Z = { "left": "Links", "right": "Regs", "top": "Boonste", "default": "Verstek" };
  const secs$Z = "sek";
  const autoHideButtonDelay$Z = "Vertraag voordat u die translate-knoppie wegsteek";
  const notFound$Z = "nie gevind nie";
  const minButtonPositionContainer$Z = "Die knoppie posisie verander slegs in spelers groter as 600 pixels.";
  const VOTTranslateProxyStatusDefault$Z = "As u die volmag in u land heeltemal deaktiveer, kan dit die uitbreiding verbreek";
  const PressTheKeyCombination$Z = "Druk die sleutelkombinasie...";
  const VOTUseAudioDownload$Z = "Gebruik klank aflaai";
  const VOTUseAudioDownloadWarning$Z = "As u klank aflaai deaktiveer, kan dit die funksionaliteit van die uitbreiding beïnvloed";
  const VOTAccountRequired$Z = "U moet aanmeld om hierdie funksie te gebruik";
  const VOTMyAccount$Z = "My rekening";
  const VOTLogin$Z = "Aanmelding";
  const VOTLogout$Z = "Afmeld";
  const VOTRefresh$Z = "Verfris";
  const VOTYandexToken$Z = "Tik Die Yandex Oauth Token";
  const VOTYandexTokenInfo$Z = "U kan die rekeningteken handmatig in hierdie veld instel. Let asseblief daarop dat ons nie die geldigheid daarvan nagaan voordat u'n translate-versoek stuur nie";
  const VOTLoginViaToken$Z = "Teken in via token";
  const subtitlesSmartLayout$Z = "Slim onderskrif uitleg";
  const smartDucking$Z = "Aanpasbare volume";
  const VOTAutoSubtitles$Z = "Onderskrifte op open";
  const VOTSubtitlesFont$Z = "Onderskrif skrif tipe";
  const __vite_glob_0_0 = {
    recommended: recommended$Z,
    translateVideo: translateVideo$Z,
    disableTranslate: disableTranslate$Z,
    translationSettings: translationSettings$Z,
    subtitlesSettings: subtitlesSettings$Z,
    resetSettings: resetSettings$Z,
    videoBeingTranslated: videoBeingTranslated$Z,
    videoLanguage: videoLanguage$Z,
    translationLanguage: translationLanguage$Z,
    translationTake: translationTake$Z,
    translationTakeMoreThanHour: translationTakeMoreThanHour$Z,
    translationTakeAboutMinute: translationTakeAboutMinute$Z,
    translationTakeFewMinutes: translationTakeFewMinutes$Z,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$Z,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$Z,
    requestTranslationFailed: requestTranslationFailed$Z,
    audioNotReceived: audioNotReceived$Z,
    audioFormatNotSupported: audioFormatNotSupported$Z,
    VOTAutoTranslate: VOTAutoTranslate$Z,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$Z,
    VOTVolume: VOTVolume$Z,
    VOTVolumeTranslation: VOTVolumeTranslation$Z,
    VOTAutoSetVolume: VOTAutoSetVolume$Z,
    VOTShowVideoSlider: VOTShowVideoSlider$Z,
    VOTSyncVolume: VOTSyncVolume$Z,
    VOTDisableFromYourLang: VOTDisableFromYourLang$Z,
    VOTVideoIsTooLong: VOTVideoIsTooLong$Z,
    VOTNoVideoIDFound: VOTNoVideoIDFound$Z,
    VOTSubtitles: VOTSubtitles$Z,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$Z,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$Z,
    VOTHighlightWords: VOTHighlightWords$Z,
    VOTTranslatedFrom: VOTTranslatedFrom$Z,
    VOTAutogenerated: VOTAutogenerated$Z,
    VOTSettings: VOTSettings$Z,
    VOTMenuLanguage: VOTMenuLanguage$Z,
    VOTAuthors: VOTAuthors$Z,
    VOTVersion: VOTVersion$Z,
    VOTLoader: VOTLoader$Z,
    VOTBrowser: VOTBrowser$Z,
    VOTShowPiPButton: VOTShowPiPButton$Z,
    langs: langs$Z,
    streamNoConnectionToServer: streamNoConnectionToServer$Z,
    searchField: searchField$Z,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$Z,
    VOTDetectService: VOTDetectService$Z,
    VOTProxyWorkerHost: VOTProxyWorkerHost$Z,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$Z,
    proxySettings: proxySettings$Z,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$Z,
    VOTAudioBooster: VOTAudioBooster$Z,
    VOTSubtitlesDesign: VOTSubtitlesDesign$Z,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$Z,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$Z,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$Z,
    VOTDownloadWithName: VOTDownloadWithName$Z,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$Z,
    VOTLocaleHash: VOTLocaleHash$Z,
    VOTUpdatedAt: VOTUpdatedAt$Z,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$Z,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$Z,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$Z,
    VOTNewAudioPlayer: VOTNewAudioPlayer$Z,
    VOTUseNewModel: VOTUseNewModel$Z,
    TranslationDelayed: TranslationDelayed$Z,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$Z,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$Z,
    VOTBugReport: VOTBugReport$Z,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$Z,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$Z,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$Z,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$Z,
    VOTTranslatedBy: VOTTranslatedBy$Z,
    VOTStreamNotAvailable: VOTStreamNotAvailable$Z,
    VOTTranslationTextService: VOTTranslationTextService$Z,
    VOTNotAffectToVoice: VOTNotAffectToVoice$Z,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$Z,
    showVideoVolumeSlider: showVideoVolumeSlider$Z,
    hotkeysSettings: hotkeysSettings$Z,
    None: None$Z,
    VOTUseLivelyVoice: VOTUseLivelyVoice$Z,
    miscSettings: miscSettings$Z,
    services: services$Z,
    aboutExtension: aboutExtension$Z,
    appearance: appearance$Z,
    buttonPosition: buttonPosition$Z,
    position: position$Z,
    secs: secs$Z,
    autoHideButtonDelay: autoHideButtonDelay$Z,
    notFound: notFound$Z,
    minButtonPositionContainer: minButtonPositionContainer$Z,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$Z,
    PressTheKeyCombination: PressTheKeyCombination$Z,
    VOTUseAudioDownload: VOTUseAudioDownload$Z,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$Z,
    VOTAccountRequired: VOTAccountRequired$Z,
    VOTMyAccount: VOTMyAccount$Z,
    VOTLogin: VOTLogin$Z,
    VOTLogout: VOTLogout$Z,
    VOTRefresh: VOTRefresh$Z,
    VOTYandexToken: VOTYandexToken$Z,
    VOTYandexTokenInfo: VOTYandexTokenInfo$Z,
    VOTLoginViaToken: VOTLoginViaToken$Z,
    subtitlesSmartLayout: subtitlesSmartLayout$Z,
    smartDucking: smartDucking$Z,
    VOTAutoSubtitles: VOTAutoSubtitles$Z,
    VOTSubtitlesFont: VOTSubtitlesFont$Z
  };
  const recommended$Y = "የሚመከር";
  const translateVideo$Y = "ቪዲዮ ተርጉም";
  const disableTranslate$Y = "አጥፉ";
  const translationSettings$Y = "የትርጉም ቅንብሮች";
  const subtitlesSettings$Y = "ንዑስ ርዕሶች ቅንብሮች";
  const resetSettings$Y = "ዳግም አስጀምር";
  const videoBeingTranslated$Y = "ቪዲዮ እየተተረጎመ ነው";
  const videoLanguage$Y = "የቪዲዮ ቋንቋ";
  const translationLanguage$Y = "የትርጉም ቋንቋ";
  const translationTake$Y = "ትርጉሙ ይወስዳል ። ";
  const translationTakeMoreThanHour$Y = "ትርጉሙ ከአንድ ሰዓት በላይ ይወስዳል";
  const translationTakeAboutMinute$Y = "ትርጉሙ አንድ ደቂቃ ያህል ይወስዳል";
  const translationTakeFewMinutes$Y = "ትርጉሙ ጥቂት ደቂቃዎችን ይወስዳል ። ";
  const translationTakeApproximatelyMinutes$Y = "ትርጉሙ በግምት ይወስዳል {0} ደቂቃዎች";
  const translationTakeApproximatelyMinute$Y = "ትርጉሙ በግምት ይወስዳል {0} ደቂቃዎች";
  const requestTranslationFailed$Y = "የቪዲዮ ትርጉም መጠየቅ አልተሳካም";
  const audioNotReceived$Y = "የድምፅ አገናኝ አልተቀበለም";
  const audioFormatNotSupported$Y = "የድምጽ ቅርጸት አይደገፍም";
  const VOTAutoTranslate$Y = "ክፈት ላይ መተርጎም";
  const VOTDontTranslateYourLang$Y = "ከቋንቋዬ አትበል";
  const VOTVolume$Y = "የቪዲዮ ጥራዝ:";
  const VOTVolumeTranslation$Y = "የትርጉም ጥራዝ:";
  const VOTAutoSetVolume$Y = "የቪዲዮ ጥራዝ ወደ ይቀንሱ";
  const VOTShowVideoSlider$Y = "የቪዲዮ ጥራዝ ተንሸራታች";
  const VOTSyncVolume$Y = "አገናኝ ትርጉም እና የቪዲዮ ጥራዝ";
  const VOTDisableFromYourLang$Y = "በእርስዎ ቋንቋ ውስጥ ቪዲዮ ትርጉም ተሰናክሏል";
  const VOTVideoIsTooLong$Y = "ቪዲዮው በጣም ረጅም ነው ። ";
  const VOTNoVideoIDFound$Y = "ምንም የቪዲዮ መታወቂያ አልተገኘም";
  const VOTSubtitles$Y = "ንዑስ ርዕሶች";
  const VOTSubtitlesDisabled$Y = "አካል ጉዳተኞች";
  const VOTSubtitlesMaxLength$Y = "ንዑስ ርዕሶች ከፍተኛ ርዝመት";
  const VOTHighlightWords$Y = "ጎላ ያሉ ቃላት";
  const VOTTranslatedFrom$Y = "ከ ተተርጉሟል";
  const VOTAutogenerated$Y = "በራስ-ሰር";
  const VOTSettings$Y = "የድምፅ ቅንብሮች";
  const VOTMenuLanguage$Y = "ምናሌ ቋንቋ";
  const VOTAuthors$Y = "ደራሲዎች";
  const VOTVersion$Y = "ትርጉም";
  const VOTLoader$Y = "ጫን";
  const VOTBrowser$Y = "መቃኛ";
  const VOTShowPiPButton$Y = "አሳይ ፒፕ አዝራር";
  const langs$Y = { "auto": "ራስ", "af": "አፍሪቃውያን", "ak": "አካን", "sq": "አልባኒኛ", "am": "አማርኛ", "ar": "አረብኛ", "hy": "አርሜኒያን", "as": "አሳም", "ay": "አይማራ", "az": "አዘርባጃኒ", "bn": "ባንግላ", "eu": "ባስክ", "be": "ቤላሩስኛ", "bho": "ቡጁፒሪ", "bs": "ቦስኒያኛ", "bg": "ቡልጋሪያኛ", "my": "በርማኛ", "ca": "ካታላን", "ceb": "ሴቡዋኖ", "zh": "ቻይንኛ", "zh-Hans": "ቻይንኛ (ቀለል ያለ)", "zh-Hant": "ቻይንኛ (ባህላዊ)", "co": "ኮርሲካን", "hr": "ክሮኤሽያኛ", "cs": "ቼክኛ", "da": "ዴኒሽ", "dv": "ዲዌሂ", "nl": "ደች", "en": "አማርኛ", "eo": "ኤስፔራንቶ", "et": "ኤስቶንኛ", "ee": "ኤዌ", "fil": "ፊሊፒንስ", "fi": "ፊኒሽ", "fr": "ፈረንሳይኛ", "gl": "ጋሊሺያኛ", "lg": "ጋንዳ", "ka": "ጆርጂያኛ", "de": "ጀርመንኛ", "el": "ግሪክኛ", "gn": "ጋራኒ", "gu": "ጉጃራቲ", "ht": "የሄይቲ ክሪኦል", "ha": "ሃውሳ", "haw": "የሃዋይ", "iw": "ዕብራስጥ", "hi": "ሂንዲ", "hmn": "ሃሞንግ", "hu": "ሀንጋሪኛ", "is": "አይስላንድኛ", "ig": "ኢግቦ", "id": "ኢንዶኔዥኛ", "ga": "አይሪሽ", "it": "ጣሊያንኛ", "ja": "ጃፓንኛ", "jv": "ጃቫኛ", "kn": "ካናዳኛ", "kk": "ካዛክኛ", "km": "ክመር", "rw": "ኪኒያሩዋንዳ", "ko": "ኮሪያኛ", "kri": "ክሪዮ", "ku": "ኩርድኛ", "ky": "ኪርጊዝ", "lo": "ላኦ", "la": "ላቲን", "lv": "ላትቪያኛ", "ln": "ሊንጋላ", "lt": "ሊቱዌኒያ", "lb": "ሉክሰምበርጊሽ", "mk": "መቄዶንያ", "mg": "ማላጋሲ", "ms": "ማላይኛ", "ml": "ማላያላም", "mt": "ማልታ", "mi": "Amharic አማርኛ", "mr": "ማራቲ", "mn": "ሞንጎሊያኛ", "ne": "ኔፓሊኛ", "nso": "ሰሜናዊ ሶቶ", "no": "Ianዌጂያን", "ny": "ኒያንጃ", "or": "ኦድያ", "om": "ኦሮሞ", "ps": "ፓሽቶ", "fa": "ፋርስኛ", "pl": "ፖላንድኛ", "pt": "ፖርቱጋልኛ", "pa": "ፑንጃቢ", "qu": "ኬችዋ", "ro": "ሮማኒያን", "ru": "ሩሲያኛ", "sm": "ሳሞአ", "sa": "ሳንስክሪት", "gd": "የስኮትላንድ ጌሊክ", "sr": "ሰርቢኛ", "sn": "ሾና", "sd": "ሲንዲ", "si": "ሲንሃላ", "sk": "ስሎቫክኛ", "sl": "ስሎቬኒያን", "so": "ሶማሊኛ", "st": "ደቡብ ሶቶ", "es": "ስፓኒሽ", "su": "ስቱዲዮዎች", "sw": "ስዋሂሊ", "sv": "ስዊድንኛ", "tg": "ታጂክ", "ta": "ታሚልኛ", "tt": "ታታር", "te": "ስፓኒሽ", "th": "ታይኛ", "ti": "ትግርኛ", "ts": "ጦንጋ", "tr": "ቱርክኛ", "tk": "ቱርክመን", "uk": "ዩክሬንኛ", "ur": "ኡርዱ", "ug": "ኡይጉር", "uz": "ኡዝቤክኛ", "vi": "ቬትናምኛ", "cy": "ዌልስ", "fy": "ምዕራባዊ ፍሪሲያን", "xh": "የሻሳ", "yi": "አማርኛ", "yo": "ዮሩባ", "zu": "ዙሉ" };
  const streamNoConnectionToServer$Y = "ከአገልጋዩ ጋር ምንም ግንኙነት የለም ። ";
  const searchField$Y = "Search።..";
  const VOTTranslateAPIErrors$Y = "ከ API ስህተቶችን ይተረጉሙ";
  const VOTDetectService$Y = "የቋንቋ ማወቂያ አገልግሎት";
  const VOTProxyWorkerHost$Y = "ተኪ ሠራተኛ አድራሻ ያስገቡ";
  const VOTM3u8ProxyHost$Y = "የ m3u8 ተኪ ሰራተኛ አድራሻ ያስገቡ";
  const proxySettings$Y = "የተኪ ቅንብሮች";
  const translationTakeApproximatelyMinute2$Y = "ትርጉሙ በግምት ይወስዳል {0} ደቂቃዎች";
  const VOTAudioBooster$Y = "የተራዘመ የትርጉም መጠን መጨመር";
  const VOTSubtitlesDesign$Y = "ንዑስ ርዕሶች ንድፍ";
  const VOTSubtitlesFontSize$Y = "የትርጉም ጽሑፎች ቅርጸ-ቁምፊ መጠን";
  const VOTSubtitlesOpacity$Y = "የትርጉም ዳራ ግልፅነት";
  const VOTSubtitlesDownloadFormat$Y = "ንዑስ ርዕሶችን ለማውረድ ቅርጸት";
  const VOTDownloadWithName$Y = "ፋይሎችን በቪዲዮ ስም ያውርዱ";
  const VOTUpdateLocaleFiles$Y = "የአካባቢ ፋይሎችን አዘምን";
  const VOTLocaleHash$Y = "አካባቢያዊ ሃሽ";
  const VOTUpdatedAt$Y = "ላይ የተዘመነ";
  const VOTNeedWebAudioAPI$Y = "ይህንን ለማስቻል, አንድ የድር ኦዲዮ ኤፒአይ ሊኖርዎት ይገባል";
  const VOTMediaCSPEnabledOnSite$Y = "በዚህ ድረ ገጽ ላይ የፒ";
  const VOTOnlyBypassMediaCSP$Y = "የሚዲያ ሲ ኤስ ፒ ለማለፍ ብቻ ይጠቀሙ";
  const VOTNewAudioPlayer$Y = "አዲሱን የድምፅ ማጫወቻ ይጠቀሙ";
  const VOTUseNewModel$Y = "ለአንዳንድ ቪዲዮዎች የ Yandex ድምፆች የሙከራ ልዩነት ይጠቀሙ";
  const TranslationDelayed$Y = "ትርጉሙ ትንሽ ዘግይቷል";
  const VOTTranslationCompletedNotify$Y = "በ {0} ላይ ያለው ትርጉም ተጠናቅቋል!";
  const VOTSendNotifyOnComplete$Y = "ቪዲዮው የተተረጎመ መሆኑን ማሳወቂያ ይላኩ";
  const VOTBugReport$Y = "ሳንካ ሪፖርት ያድርጉ";
  const VOTTranslateProxyDisabled$Y = "አካል ጉዳተኞች";
  const VOTTranslateProxyEnabled$Y = "ነቅቷል";
  const VOTTranslateProxyEverything$Y = "ሁሉንም ነገር ተኪ";
  const VOTTranslateProxyStatus$Y = "ፕሮክሲ ሁነታ";
  const VOTTranslatedBy$Y = "በ {0}የተተረጎመ";
  const VOTStreamNotAvailable$Y = "ትራንስፎርሜሽን አይገኝም ። ";
  const VOTTranslationTextService$Y = "የጽሑፍ ትርጉም አገልግሎት";
  const VOTNotAffectToVoice$Y = "በድምጽ ትርጉም ላይ ተጽዕኖ አያሳድርም ። ";
  const DontTranslateSelectedLanguages$Y = "ከተመረጡ ቋንቋዎች አይተርጉሙ";
  const showVideoVolumeSlider$Y = "የቪዲዮ ጥራዝ ተንሸራታች አሳይ";
  const hotkeysSettings$Y = "Hotkeys ቅንብሮች";
  const None$Y = "ምንም";
  const VOTUseLivelyVoice$Y = "ሕያው ድምጾችን ይጠቀሙ. ተናጋሪዎች እንደ ሩሲያውያን ይመስላሉ።";
  const miscSettings$Y = "ኤስሲ ቅንብሮች";
  const services$Y = { "yandexbrowser": "Yandex መቃኛ", "msedge": "የማይክሮሶፍት ጠርዝ", "rust-server": "_የሰርቨር ስም ፦ " };
  const aboutExtension$Y = "ስለ ቅጥያ";
  const appearance$Y = "መልክ";
  const buttonPosition$Y = "የሚሰጡዋቸውን የሚሰጡዋቸውን ተጫዋች";
  const position$Y = { "left": "ግራ", "right": "ቀኝ", "top": "ከፍተኛ", "default": "የነበረው" };
  const secs$Y = "ሰከንዶች";
  const autoHideButtonDelay$Y = "የትርጉም አዝራሩን ከመደበቅ በፊት መዘግየት";
  const notFound$Y = "አልተገኘም";
  const minButtonPositionContainer$Y = "ከ 600 ፒክስል በላይ በሆኑ ተጫዋቾች ላይ የአዝራር አቀማመጥ ብቻ ይለወጣል።";
  const VOTTranslateProxyStatusDefault$Y = "በአገርዎ ውስጥ ፕሮክሲን ሙሉ በሙሉ ማሰናከል ቅጥያውን ሊያፈርስ ይችላል ። ";
  const PressTheKeyCombination$Y = "ቁልፍ ጥምረት ይጫኑ...";
  const VOTUseAudioDownload$Y = "የድምጽ ማውረድ ይጠቀሙ";
  const VOTUseAudioDownloadWarning$Y = "የድምጽ ማውረዶችን ማሰናከል የቅጥያውን ተግባር ላይ ተጽዕኖ ሊያሳድር ይችላል ። ";
  const VOTAccountRequired$Y = "ይህንን ባህሪ ለመጠቀም መግባት አለብዎት ። ";
  const VOTMyAccount$Y = "የእኔ መለያ";
  const VOTLogin$Y = "አዲስ ዝምብሎ ይግባ";
  const VOTLogout$Y = "ግባ";
  const VOTRefresh$Y = "በድጋሚ ጀምር";
  const VOTYandexToken$Y = "የ Yandex Out Toket አስገባ";
  const VOTYandexTokenInfo$Y = "በዚህ መስክ የመለያ ምልክቱን እራስዎ ማዘጋጀት ይችላሉ። የትርጉም ጥያቄ ከመላክዎ በፊት ትክክለኛነቱን እንደማንፈትሽ እባክዎ ልብ ይበሉ ። ";
  const VOTLoginViaToken$Y = "በቶከን በኩል ይግቡ";
  const subtitlesSmartLayout$Y = "ዘመናዊ ንዑስ ርዕስ አቀማመጥ";
  const smartDucking$Y = "የመላመድ ድምጽ";
  const VOTAutoSubtitles$Y = "ክፍት ላይ ንዑስ ርዕሶች";
  const VOTSubtitlesFont$Y = "የፊደል ቅርጽ";
  const __vite_glob_0_1 = {
    recommended: recommended$Y,
    translateVideo: translateVideo$Y,
    disableTranslate: disableTranslate$Y,
    translationSettings: translationSettings$Y,
    subtitlesSettings: subtitlesSettings$Y,
    resetSettings: resetSettings$Y,
    videoBeingTranslated: videoBeingTranslated$Y,
    videoLanguage: videoLanguage$Y,
    translationLanguage: translationLanguage$Y,
    translationTake: translationTake$Y,
    translationTakeMoreThanHour: translationTakeMoreThanHour$Y,
    translationTakeAboutMinute: translationTakeAboutMinute$Y,
    translationTakeFewMinutes: translationTakeFewMinutes$Y,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$Y,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$Y,
    requestTranslationFailed: requestTranslationFailed$Y,
    audioNotReceived: audioNotReceived$Y,
    audioFormatNotSupported: audioFormatNotSupported$Y,
    VOTAutoTranslate: VOTAutoTranslate$Y,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$Y,
    VOTVolume: VOTVolume$Y,
    VOTVolumeTranslation: VOTVolumeTranslation$Y,
    VOTAutoSetVolume: VOTAutoSetVolume$Y,
    VOTShowVideoSlider: VOTShowVideoSlider$Y,
    VOTSyncVolume: VOTSyncVolume$Y,
    VOTDisableFromYourLang: VOTDisableFromYourLang$Y,
    VOTVideoIsTooLong: VOTVideoIsTooLong$Y,
    VOTNoVideoIDFound: VOTNoVideoIDFound$Y,
    VOTSubtitles: VOTSubtitles$Y,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$Y,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$Y,
    VOTHighlightWords: VOTHighlightWords$Y,
    VOTTranslatedFrom: VOTTranslatedFrom$Y,
    VOTAutogenerated: VOTAutogenerated$Y,
    VOTSettings: VOTSettings$Y,
    VOTMenuLanguage: VOTMenuLanguage$Y,
    VOTAuthors: VOTAuthors$Y,
    VOTVersion: VOTVersion$Y,
    VOTLoader: VOTLoader$Y,
    VOTBrowser: VOTBrowser$Y,
    VOTShowPiPButton: VOTShowPiPButton$Y,
    langs: langs$Y,
    streamNoConnectionToServer: streamNoConnectionToServer$Y,
    searchField: searchField$Y,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$Y,
    VOTDetectService: VOTDetectService$Y,
    VOTProxyWorkerHost: VOTProxyWorkerHost$Y,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$Y,
    proxySettings: proxySettings$Y,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$Y,
    VOTAudioBooster: VOTAudioBooster$Y,
    VOTSubtitlesDesign: VOTSubtitlesDesign$Y,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$Y,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$Y,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$Y,
    VOTDownloadWithName: VOTDownloadWithName$Y,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$Y,
    VOTLocaleHash: VOTLocaleHash$Y,
    VOTUpdatedAt: VOTUpdatedAt$Y,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$Y,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$Y,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$Y,
    VOTNewAudioPlayer: VOTNewAudioPlayer$Y,
    VOTUseNewModel: VOTUseNewModel$Y,
    TranslationDelayed: TranslationDelayed$Y,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$Y,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$Y,
    VOTBugReport: VOTBugReport$Y,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$Y,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$Y,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$Y,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$Y,
    VOTTranslatedBy: VOTTranslatedBy$Y,
    VOTStreamNotAvailable: VOTStreamNotAvailable$Y,
    VOTTranslationTextService: VOTTranslationTextService$Y,
    VOTNotAffectToVoice: VOTNotAffectToVoice$Y,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$Y,
    showVideoVolumeSlider: showVideoVolumeSlider$Y,
    hotkeysSettings: hotkeysSettings$Y,
    None: None$Y,
    VOTUseLivelyVoice: VOTUseLivelyVoice$Y,
    miscSettings: miscSettings$Y,
    services: services$Y,
    aboutExtension: aboutExtension$Y,
    appearance: appearance$Y,
    buttonPosition: buttonPosition$Y,
    position: position$Y,
    secs: secs$Y,
    autoHideButtonDelay: autoHideButtonDelay$Y,
    notFound: notFound$Y,
    minButtonPositionContainer: minButtonPositionContainer$Y,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$Y,
    PressTheKeyCombination: PressTheKeyCombination$Y,
    VOTUseAudioDownload: VOTUseAudioDownload$Y,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$Y,
    VOTAccountRequired: VOTAccountRequired$Y,
    VOTMyAccount: VOTMyAccount$Y,
    VOTLogin: VOTLogin$Y,
    VOTLogout: VOTLogout$Y,
    VOTRefresh: VOTRefresh$Y,
    VOTYandexToken: VOTYandexToken$Y,
    VOTYandexTokenInfo: VOTYandexTokenInfo$Y,
    VOTLoginViaToken: VOTLoginViaToken$Y,
    subtitlesSmartLayout: subtitlesSmartLayout$Y,
    smartDucking: smartDucking$Y,
    VOTAutoSubtitles: VOTAutoSubtitles$Y,
    VOTSubtitlesFont: VOTSubtitlesFont$Y
  };
  const recommended$X = "موصى به";
  const translateVideo$X = "ترجمة الفيديو";
  const disableTranslate$X = "إيقاف التشغيل";
  const translationSettings$X = "إعدادات الترجمة";
  const subtitlesSettings$X = "إعدادات الترجمة";
  const resetSettings$X = "إعادة تعيين الإعدادات";
  const videoBeingTranslated$X = "يتم ترجمة الفيديو";
  const videoLanguage$X = "لغة الفيديو";
  const translationLanguage$X = "لغة الترجمة";
  const translationTake$X = "ستستغرق الترجمة";
  const translationTakeMoreThanHour$X = "ستستغرق الترجمة أكثر من ساعة";
  const translationTakeAboutMinute$X = "ستستغرق الترجمة حوالي دقيقة";
  const translationTakeFewMinutes$X = "ستستغرق الترجمة بضع دقائق";
  const translationTakeApproximatelyMinutes$X = "ستستغرق الترجمة تقريبا {0} دقائق";
  const translationTakeApproximatelyMinute$X = "ستستغرق الترجمة تقريبا {0} دقيقة";
  const requestTranslationFailed$X = "فشل طلب ترجمة الفيديو";
  const audioNotReceived$X = "لم يتم استلام رابط الصوت";
  const audioFormatNotSupported$X = "تنسيق الصوت غير مدعوم";
  const VOTAutoTranslate$X = "الترجمة عند الفتح";
  const VOTDontTranslateYourLang$X = "لا تترجم من لغتي";
  const VOTVolume$X = "حجم الفيديو:";
  const VOTVolumeTranslation$X = "حجم الترجمة:";
  const VOTAutoSetVolume$X = "تقليل حجم الفيديو إلى";
  const VOTShowVideoSlider$X = "شريط تحكم حجم الفيديو";
  const VOTSyncVolume$X = "اربط حجم الترجمة والفيديو";
  const VOTDisableFromYourLang$X = "لقد قمت بتعطيل ترجمة الفيديو بلغتك";
  const VOTVideoIsTooLong$X = "الفيديو طويل جداً";
  const VOTNoVideoIDFound$X = "لم يتم العثور على معرف الفيديو";
  const VOTSubtitles$X = "ترجمات";
  const VOTSubtitlesDisabled$X = "عاجز";
  const VOTSubtitlesMaxLength$X = "أقصى طول للترجمات";
  const VOTHighlightWords$X = "تمييز الكلمات";
  const VOTTranslatedFrom$X = "مترجم من";
  const VOTAutogenerated$X = "يستخرج تلقائيا";
  const VOTSettings$X = "إعدادات VOT.";
  const VOTMenuLanguage$X = "لغة القائمة";
  const VOTAuthors$X = "المؤلفون";
  const VOTVersion$X = "إصدار";
  const VOTLoader$X = "محمل";
  const VOTBrowser$X = "المتصفح";
  const VOTShowPiPButton$X = "إظهار زر PiP";
  const langs$X = { "auto": "تلقائيا", "az": "الأذربيجانية", "hy": "الأرمنية", "as": "الأسامية", "es": "الإسبانية", "eo": "الإسبرانتو", "et": "الإستونية", "af": "الأفريقانية", "ak": "الأكانية", "sq": "الألبانية", "de": "الألمانية", "am": "الأمهرية", "en": "الإنجليزية", "id": "الإندونيسية", "ur": "الأوردية", "om": "الأورومية", "or": "الأورية", "uz": "الأوزبكية", "uk": "الأوكرانية", "ug": "الأويغورية", "ig": "الإيجبو", "ga": "الأيرلندية", "is": "الأيسلندية", "it": "الإيطالية", "ay": "الأيمارا", "ee": "الإيوي", "eu": "الباسكية", "pt": "البرتغالية", "ps": "البشتو", "bg": "البلغارية", "pa": "البنجابية", "bn": "البنغالية", "bho": "البهوجبورية", "my": "البورمية", "bs": "البوسنية", "pl": "البولندية", "be": "البيلاروسية", "ta": "التاميلية", "th": "التايلاندية", "tt": "التترية", "tk": "التركمانية", "tr": "التركية", "cs": "التشيكية", "ti": "التغرينية", "te": "التيلوغوية", "gl": "الجاليكية", "jv": "الجاوية", "ka": "الجورجية", "km": "الخميرية", "xh": "الخوسا", "da": "الدانمركية", "ru": "الروسية", "ro": "الرومانية", "zu": "الزولو", "sm": "الساموائية", "sk": "السلوفاكية", "sl": "السلوفانية", "sd": "السندية", "sa": "السنسكريتية", "si": "السنهالية", "sw": "السواحلية", "st": "السوتو الجنوبية", "nso": "السوتو الشمالية", "ts": "السونجا", "su": "السوندانية", "sv": "السويدية", "ceb": "السيبيوانية", "sn": "الشونا", "sr": "الصربية", "so": "الصومالية", "zh": "الصينية", "zh-Hant": "الصينية (التقليدية)", "zh-Hans": "الصينية (المبسطة)", "tg": "الطاجيكية", "iw": "العبرية", "ar": "العربية", "lg": "الغاندا", "gn": "الغوارانية", "gu": "الغوجاراتية", "gd": "الغيلية الأسكتلندية", "fa": "الفارسية", "fr": "الفرنسية", "fy": "الفريزيان", "fil": "الفلبينية", "fi": "الفنلندية", "vi": "الفيتنامية", "ky": "القيرغيزية", "kk": "الكازاخستانية", "kn": "الكانادا", "ca": "الكتالانية", "ku": "الكردية", "hr": "الكرواتية", "ht": "الكريولية الهايتية", "co": "الكورسيكية", "ko": "الكورية", "qu": "الكويتشوا", "rw": "الكينيارواندا", "lv": "اللاتفية", "la": "اللاتينية", "lo": "اللاوية", "lb": "اللكسمبورغية", "lt": "الليتوانية", "ln": "اللينجالا", "mr": "الماراثية", "ml": "المالايالامية", "dv": "المالديفية", "mt": "المالطية", "ms": "الماليزية", "mi": "الماورية", "mk": "المقدونية", "mg": "الملغاشي", "mn": "المنغولية", "no": "النرويجية", "ny": "النيانجا", "ne": "النيبالية", "hmn": "الهمونجية", "hi": "الهندية", "hu": "الهنغارية", "ha": "الهوسا", "nl": "الهولندية", "cy": "الويلزية", "ja": "اليابانية", "yi": "اليديشية", "yo": "اليوروبا", "el": "اليونانية", "haw": "لغة هاواي", "kri": "Kri" };
  const streamNoConnectionToServer$X = "لا يوجد اتصال بالخادم";
  const searchField$X = "بحث...";
  const VOTTranslateAPIErrors$X = "ترجمة الأخطاء من واجهة برمجة التطبيقات";
  const VOTDetectService$X = "خدمة كشف اللغة";
  const VOTProxyWorkerHost$X = "أدخل عنوان عامل الوكيل";
  const VOTM3u8ProxyHost$X = "أدخل عنوان عامل الوكيل إم 3 يو 8";
  const proxySettings$X = "إعدادات الوكيل";
  const translationTakeApproximatelyMinute2$X = "ستستغرق الترجمة حوالي {0} دقيقة";
  const VOTAudioBooster$X = "زيادة حجم الترجمة الموسعة";
  const VOTSubtitlesDesign$X = "تصميم الترجمة";
  const VOTSubtitlesFontSize$X = "حجم الخط من ترجمات";
  const VOTSubtitlesOpacity$X = "شفافية خلفية العنوان الفرعي";
  const VOTSubtitlesDownloadFormat$X = "تنسيق تنزيل الترجمة";
  const VOTDownloadWithName$X = "تحميل الملفات مع اسم الفيديو";
  const VOTUpdateLocaleFiles$X = "تحديث ملفات الترجمة";
  const VOTLocaleHash$X = "تجزئة اللغة";
  const VOTUpdatedAt$X = "تحديث في";
  const VOTNeedWebAudioAPI$X = "لتمكين هذا ، يجب أن يكون لديك واجهة برمجة تطبيقات صوت الويب";
  const VOTMediaCSPEnabledOnSite$X = "يتم تمكين الطاقة الشمسية المركزة وسائل الإعلام على هذا الموقع";
  const VOTOnlyBypassMediaCSP$X = "استخدامه فقط لتجاوز وسائل الإعلام سب";
  const VOTNewAudioPlayer$X = "استخدم مشغل الصوت الجديد";
  const VOTUseNewModel$X = "استخدم تباينا تجريبيا لأصوات ياندكس لبعض مقاطع الفيديو";
  const TranslationDelayed$X = "الترجمة متأخرة قليلا";
  const VOTTranslationCompletedNotify$X = "تم الانتهاء من الترجمة على {0}!";
  const VOTSendNotifyOnComplete$X = "إرسال إشعار بأن الفيديو قد تمت ترجمته";
  const VOTBugReport$X = "الإبلاغ عن خطأ";
  const VOTTranslateProxyDisabled$X = "معاق";
  const VOTTranslateProxyEnabled$X = "ممكن";
  const VOTTranslateProxyEverything$X = "وكيل كل شيء";
  const VOTTranslateProxyStatus$X = "وضع الوكيل";
  const VOTTranslatedBy$X = "ترجمه {0}";
  const VOTStreamNotAvailable$X = "ترجمة تيار غير متوفر";
  const VOTTranslationTextService$X = "خدمة ترجمة النصوص";
  const VOTNotAffectToVoice$X = "لا يؤثر على ترجمة النص في الصوت";
  const DontTranslateSelectedLanguages$X = "لا تترجم من لغات محددة";
  const showVideoVolumeSlider$X = "عرض شريط تمرير حجم الفيديو";
  const hotkeysSettings$X = "إعدادات مفاتيح الاختصار";
  const None$X = "لا يوجد";
  const VOTUseLivelyVoice$X = "استخدم أصواتا حية. مكبرات الصوت تبدو مثل الروس الأصليين.";
  const miscSettings$X = "إعدادات متفرقات";
  const services$X = { "yandexbrowser": "متصفح ياندكس", "msedge": "مايكروسوفت إيدج", "rust-server": "خادم الصدأ" };
  const aboutExtension$X = "حول التمديد";
  const appearance$X = "المظهر";
  const buttonPosition$X = "موضع الزر في المشغل";
  const position$X = { "left": "اليسار", "right": "صحيح", "top": "أعلى", "default": "الافتراضي" };
  const secs$X = "ثانية";
  const autoHideButtonDelay$X = "تأخير قبل إخفاء زر الترجمة";
  const notFound$X = "غير موجود";
  const minButtonPositionContainer$X = "يتغير موضع الزر فقط في اللاعبين الأكبر من 600 بكسل.";
  const VOTTranslateProxyStatusDefault$X = "قد يؤدي تعطيل البروكسي تماما في بلدك إلى كسر الامتداد";
  const PressTheKeyCombination$X = "اضغط على مجموعة المفاتيح...";
  const VOTUseAudioDownload$X = "استخدام تنزيل الصوت";
  const VOTUseAudioDownloadWarning$X = "قد يؤثر تعطيل تنزيلات الصوت على وظيفة الامتداد";
  const VOTAccountRequired$X = "تحتاج إلى تسجيل الدخول لاستخدام هذه الميزة";
  const VOTMyAccount$X = "حسابي";
  const VOTLogin$X = "تسجيل الدخول";
  const VOTLogout$X = "تسجيل الخروج";
  const VOTRefresh$X = "تحديث";
  const VOTYandexToken$X = "أدخل رمز ياندكس أوث";
  const VOTYandexTokenInfo$X = "يمكنك تعيين رمز الحساب يدويا في هذا الحقل. يرجى ملاحظة أننا لا نتحقق من صلاحيتها قبل إرسال طلب ترجمة";
  const VOTLoginViaToken$X = "تسجيل الدخول عبر الرمز المميز";
  const subtitlesSmartLayout$X = "تخطيط الترجمة الذكية";
  const smartDucking$X = "حجم التكيف";
  const VOTAutoSubtitles$X = "ترجمات على فتح";
  const VOTSubtitlesFont$X = "خط الترجمة";
  const __vite_glob_0_2 = {
    recommended: recommended$X,
    translateVideo: translateVideo$X,
    disableTranslate: disableTranslate$X,
    translationSettings: translationSettings$X,
    subtitlesSettings: subtitlesSettings$X,
    resetSettings: resetSettings$X,
    videoBeingTranslated: videoBeingTranslated$X,
    videoLanguage: videoLanguage$X,
    translationLanguage: translationLanguage$X,
    translationTake: translationTake$X,
    translationTakeMoreThanHour: translationTakeMoreThanHour$X,
    translationTakeAboutMinute: translationTakeAboutMinute$X,
    translationTakeFewMinutes: translationTakeFewMinutes$X,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$X,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$X,
    requestTranslationFailed: requestTranslationFailed$X,
    audioNotReceived: audioNotReceived$X,
    audioFormatNotSupported: audioFormatNotSupported$X,
    VOTAutoTranslate: VOTAutoTranslate$X,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$X,
    VOTVolume: VOTVolume$X,
    VOTVolumeTranslation: VOTVolumeTranslation$X,
    VOTAutoSetVolume: VOTAutoSetVolume$X,
    VOTShowVideoSlider: VOTShowVideoSlider$X,
    VOTSyncVolume: VOTSyncVolume$X,
    VOTDisableFromYourLang: VOTDisableFromYourLang$X,
    VOTVideoIsTooLong: VOTVideoIsTooLong$X,
    VOTNoVideoIDFound: VOTNoVideoIDFound$X,
    VOTSubtitles: VOTSubtitles$X,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$X,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$X,
    VOTHighlightWords: VOTHighlightWords$X,
    VOTTranslatedFrom: VOTTranslatedFrom$X,
    VOTAutogenerated: VOTAutogenerated$X,
    VOTSettings: VOTSettings$X,
    VOTMenuLanguage: VOTMenuLanguage$X,
    VOTAuthors: VOTAuthors$X,
    VOTVersion: VOTVersion$X,
    VOTLoader: VOTLoader$X,
    VOTBrowser: VOTBrowser$X,
    VOTShowPiPButton: VOTShowPiPButton$X,
    langs: langs$X,
    streamNoConnectionToServer: streamNoConnectionToServer$X,
    searchField: searchField$X,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$X,
    VOTDetectService: VOTDetectService$X,
    VOTProxyWorkerHost: VOTProxyWorkerHost$X,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$X,
    proxySettings: proxySettings$X,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$X,
    VOTAudioBooster: VOTAudioBooster$X,
    VOTSubtitlesDesign: VOTSubtitlesDesign$X,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$X,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$X,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$X,
    VOTDownloadWithName: VOTDownloadWithName$X,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$X,
    VOTLocaleHash: VOTLocaleHash$X,
    VOTUpdatedAt: VOTUpdatedAt$X,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$X,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$X,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$X,
    VOTNewAudioPlayer: VOTNewAudioPlayer$X,
    VOTUseNewModel: VOTUseNewModel$X,
    TranslationDelayed: TranslationDelayed$X,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$X,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$X,
    VOTBugReport: VOTBugReport$X,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$X,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$X,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$X,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$X,
    VOTTranslatedBy: VOTTranslatedBy$X,
    VOTStreamNotAvailable: VOTStreamNotAvailable$X,
    VOTTranslationTextService: VOTTranslationTextService$X,
    VOTNotAffectToVoice: VOTNotAffectToVoice$X,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$X,
    showVideoVolumeSlider: showVideoVolumeSlider$X,
    hotkeysSettings: hotkeysSettings$X,
    None: None$X,
    VOTUseLivelyVoice: VOTUseLivelyVoice$X,
    miscSettings: miscSettings$X,
    services: services$X,
    aboutExtension: aboutExtension$X,
    appearance: appearance$X,
    buttonPosition: buttonPosition$X,
    position: position$X,
    secs: secs$X,
    autoHideButtonDelay: autoHideButtonDelay$X,
    notFound: notFound$X,
    minButtonPositionContainer: minButtonPositionContainer$X,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$X,
    PressTheKeyCombination: PressTheKeyCombination$X,
    VOTUseAudioDownload: VOTUseAudioDownload$X,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$X,
    VOTAccountRequired: VOTAccountRequired$X,
    VOTMyAccount: VOTMyAccount$X,
    VOTLogin: VOTLogin$X,
    VOTLogout: VOTLogout$X,
    VOTRefresh: VOTRefresh$X,
    VOTYandexToken: VOTYandexToken$X,
    VOTYandexTokenInfo: VOTYandexTokenInfo$X,
    VOTLoginViaToken: VOTLoginViaToken$X,
    subtitlesSmartLayout: subtitlesSmartLayout$X,
    smartDucking: smartDucking$X,
    VOTAutoSubtitles: VOTAutoSubtitles$X,
    VOTSubtitlesFont: VOTSubtitlesFont$X
  };
  const recommended$W = "tövsiyə";
  const translateVideo$W = "Videonu tərcümə edin";
  const disableTranslate$W = "Söndürün";
  const translationSettings$W = "Tərcümə parametrləri";
  const subtitlesSettings$W = "Altyazı parametrləri";
  const resetSettings$W = "Zavod sıfırlaması";
  const videoBeingTranslated$W = "Video tərcümə olunur";
  const videoLanguage$W = "Video dili";
  const translationLanguage$W = "Tərcümə dili";
  const translationTake$W = "Tərcümə edəcək";
  const translationTakeMoreThanHour$W = "Tərcümə bir saatdan çox çəkəcək";
  const translationTakeAboutMinute$W = "Tərcümə təxminən bir dəqiqə çəkəcək";
  const translationTakeFewMinutes$W = "Tərcümə bir neçə dəqiqə çəkəcək";
  const translationTakeApproximatelyMinutes$W = "Tərcümə təxminən {0} dəqiqə çəkəcək";
  const translationTakeApproximatelyMinute$W = "Tərcümə təxminən {0} dəqiqə çəkəcək";
  const requestTranslationFailed$W = "Videonun tərcüməsini tələb etmək mümkün olmadı";
  const audioNotReceived$W = "Səs bağlantısı alınmadı";
  const audioFormatNotSupported$W = "Audio format dəstəklənmir";
  const VOTAutoTranslate$W = "Açıq tərcümə";
  const VOTDontTranslateYourLang$W = "Dilimdən tərcümə etmə";
  const VOTVolume$W = "Video həcmi:";
  const VOTVolumeTranslation$W = "Tərcümə həcmi:";
  const VOTAutoSetVolume$W = "Videonun səsini aşağı salın";
  const VOTShowVideoSlider$W = "Video həcmi kaydırıcısı";
  const VOTSyncVolume$W = "Link tərcümə və video həcmi";
  const VOTDisableFromYourLang$W = "Videonun dilinizə tərcüməsini deaktiv etdiniz";
  const VOTVideoIsTooLong$W = "Video çox uzundur";
  const VOTNoVideoIDFound$W = "Video Id tapılmadı";
  const VOTSubtitles$W = "Altyazı";
  const VOTSubtitlesDisabled$W = "Əlil";
  const VOTSubtitlesMaxLength$W = "Maksimum Altyazı uzunluğu";
  const VOTHighlightWords$W = "Sözləri vurğulayın";
  const VOTTranslatedFrom$W = "tərcümə edildi";
  const VOTAutogenerated$W = "avtomatik yaradılan";
  const VOTSettings$W = "VOT parametrləri";
  const VOTMenuLanguage$W = "Menyu dili";
  const VOTAuthors$W = "Müəlliflər";
  const VOTVersion$W = "Versiya";
  const VOTLoader$W = "Yükləyici";
  const VOTBrowser$W = "Brauzer";
  const VOTShowPiPButton$W = "Nöqtəli göstər düyməsini basın";
  const langs$W = { "auto": "Avto", "af": "Afrikaans", "ak": "Akan", "sq": "Alban", "am": "Amhar", "ar": "Ərəb dili", "hy": "Erməni", "as": "Assam", "ay": "Aymara", "az": "Azərbaycan", "bn": "Bangla", "eu": "Bask", "be": "Belarus", "bho": "Bhojpuri", "bs": "Bosniya", "bg": "Bolqar", "my": "Birma", "ca": "Kataloniya", "ceb": "Cebuano", "zh": "Çin", "zh-Hans": "Çin (sadələşdirilmiş)", "zh-Hant": "Çin (ənənəvi)", "co": "Korsikalı", "hr": "Xorvat", "cs": "Çex", "da": "Danimarka", "dv": "Divehi", "nl": "Holland", "en": "İngilis dili", "eo": "Esperanto", "et": "Estoniya", "ee": "Qoyun", "fil": "Filipinli", "fi": "Fin", "fr": "Fransız dili", "gl": "Qalisian", "lg": "Ganda", "ka": "Gürcü", "de": "Alman", "el": "Yunan", "gn": "Guarani", "gu": "Gujarati", "ht": "Haiti kreolu", "ha": "Ev", "haw": "Havay", "iw": "Yəhudi", "hi": "Hindi", "hmn": "Hmong", "hu": "Macar", "is": "İslandiya", "ig": "İqbo", "id": "İndoneziya", "ga": "İrlandiya", "it": "İtalyan", "ja": "Yapon", "jv": "Cava", "kn": "Kannada", "kk": "Qazax", "km": "Khmer", "rw": "Kinyaruanda", "ko": "Koreya", "kri": "Krio", "ku": "Kürd", "ky": "Qırğızıstan", "lo": "Laos", "la": "Latın", "lv": "Latviya", "ln": "Lingala", "lt": "Litva", "lb": "Lüksemburq", "mk": "Makedoniya", "mg": "Malaqas", "ms": "Malay dili", "ml": "Malayalam", "mt": "Malta", "mi": "Maori", "mr": "Marathi", "mn": "Monqol", "ne": "Nepal", "nso": "Şimali Soto", "no": "Norveç", "ny": "Nyanja", "or": "Odia", "om": "Oromo", "ps": "Puştu", "fa": "Farsca", "pl": "Polşa", "pt": "Portuqaliya", "pa": "Panjabi", "qu": "Quechua", "ro": "Rumıniya", "ru": "Rus", "sm": "Samoa", "sa": "Sanskrit dili", "gd": "Şotlandiya Gal", "sr": "Serb", "sn": "Sean", "sd": "Sindhi", "si": "Sinhala", "sk": "Slovak", "sl": "Sloven", "so": "Somali", "st": "Cənubi Soto", "es": "İspan", "su": "Sundansky", "sw": "Suahili", "sv": "İsveç", "tg": "Tacikistan", "ta": "Tamil", "tt": "Tatar", "te": "Telugu", "th": "Tay", "ti": "Tiqrinya", "ts": "Tsonga", "tr": "Türk", "tk": "Türkmən", "uk": "Ukrayna", "ur": "Urdu", "ug": "Uyğur", "uz": "Özbək", "vi": "Vyetnam", "cy": "Uels", "fy": "Qərbi friz", "xh": "Örgü", "yi": "Yiddish", "yo": "Yoruba", "zu": "Zulu" };
  const streamNoConnectionToServer$W = "Server bağlantısı yoxdur";
  const searchField$W = "Axtarış...";
  const VOTTranslateAPIErrors$W = "API-dən səhvlərin tərcüməsi";
  const VOTDetectService$W = "Dil tərifi xidməti";
  const VOTProxyWorkerHost$W = "Proxy serverin iş ünvanını daxil edin";
  const VOTM3u8ProxyHost$W = "M3u8 işləyən proxy server ünvanını daxil edin";
  const proxySettings$W = "Proxy server parametrləri";
  const translationTakeApproximatelyMinute2$W = "Tərcümə təxminən {0} dəqiqə çəkəcək";
  const VOTAudioBooster$W = "Genişləndirilmiş tərcümə həcminin artırılması";
  const VOTSubtitlesDesign$W = "Altyazı dizaynı";
  const VOTSubtitlesFontSize$W = "Altyazı şrift ölçüsü";
  const VOTSubtitlesOpacity$W = "Altyazı fon şəffaflığı";
  const VOTSubtitlesDownloadFormat$W = "Altyazıları yükləmək üçün format";
  const VOTDownloadWithName$W = "Video adı ilə faylları yükləyin";
  const VOTUpdateLocaleFiles$W = "Yerli faylları yeniləyin";
  const VOTLocaleHash$W = "Hash yerli";
  const VOTUpdatedAt$W = "Saat yeniləndi";
  const VOTNeedWebAudioAPI$W = "Bunu aktivləşdirmək üçün veb Audio API olmalıdır";
  const VOTMediaCSPEnabledOnSite$W = "Media CSP bu saytda aktivdir";
  const VOTOnlyBypassMediaCSP$W = "Yalnız Media CSP-ni keçmək üçün istifadə edin";
  const VOTNewAudioPlayer$W = "Yeni audio pleyerdən istifadə edin";
  const VOTUseNewModel$W = "Bəzi videolar üçün Yandex voices-in eksperimental versiyasından istifadə edin";
  const TranslationDelayed$W = "Tərcümə bir az gecikir";
  const VOTTranslationCompletedNotify$W = "Tərcümə {0} tamamlandı!";
  const VOTSendNotifyOnComplete$W = "Videonun tərcümə olunduğunu bildirən bildiriş göndərin";
  const VOTBugReport$W = "Səhv bildirin";
  const VOTTranslateProxyDisabled$W = "Əlil";
  const VOTTranslateProxyEnabled$W = "Daxildir";
  const VOTTranslateProxyEverything$W = "Hər şey üçün proxy server";
  const VOTTranslateProxyStatus$W = "Proksasiya rejimi";
  const VOTTranslatedBy$W = "Tərcümə {0}";
  const VOTStreamNotAvailable$W = "Tərcümə yayımı mövcud deyil";
  const VOTTranslationTextService$W = "Mətn tərcümə xidmətləri";
  const VOTNotAffectToVoice$W = "Səs verərkən mətnin tərcüməsinə təsir göstərmir";
  const DontTranslateSelectedLanguages$W = "Seçilmiş dillərdən tərcümə etməyin";
  const showVideoVolumeSlider$W = "Video həcmi kaydırıcısını göstərin";
  const hotkeysSettings$W = "İsti düymə parametrləri";
  const None$W = "Heç kim";
  const VOTUseLivelyVoice$W = "Canlı səslərdən istifadə edin. Natiqlər yerli ruslar kimi danışırlar.";
  const miscSettings$W = "Müxtəlif parametrlər";
  const services$W = { "yandexbrowser": "Yandex Brauzeri", "msedge": "Microsoft Edge", "rust-server": "Server Rust" };
  const aboutExtension$W = "Genişləndirmə haqqında";
  const appearance$W = "Görünüş";
  const buttonPosition$W = "Pleyerdəki düymə mövqeyi";
  const position$W = { "left": "Solda", "right": "Düzgün", "top": "Üst", "default": "Default" };
  const secs$W = "sec";
  const autoHideButtonDelay$W = "Tərcümə düyməsini gizlətmədən əvvəl Saxlayın";
  const notFound$W = "tapılmadı";
  const minButtonPositionContainer$W = "Düymənin mövqeyi yalnız 600 pikseldən böyük pleyerlərdə dəyişir.";
  const VOTTranslateProxyStatusDefault$W = "Ölkənizdə bir proxy serverin tamamilə söndürülməsi uzantının pozulmasına səbəb ola bilər";
  const PressTheKeyCombination$W = "Düymə birləşməsini basın...";
  const VOTUseAudioDownload$W = "Audio yükləmədən istifadə edin";
  const VOTUseAudioDownloadWarning$W = "Səs yükləməsini söndürmək uzantının funksionallığına təsir göstərə bilər";
  const VOTAccountRequired$W = "Bu xüsusiyyəti istifadə etmək üçün daxil olmalısınız";
  const VOTMyAccount$W = "Mənim Hesabım";
  const VOTLogin$W = "Daxil ol";
  const VOTLogout$W = "Sistemdən çıxın";
  const VOTRefresh$W = "Təzələmək";
  const VOTYandexToken$W = "OAuth daxil edin-Yandex Token";
  const VOTYandexTokenInfo$W = "Bu sahədə hesab nişanını əl ilə təyin edə bilərsiniz. Xahiş edirik unutmayın ki, köçürmə sorğusu göndərməzdən əvvəl onun etibarlılığını yoxlamırıq";
  const VOTLoginViaToken$W = "Token ilə daxil olun";
  const subtitlesSmartLayout$W = "Düşüncəli Altyazı düzeni";
  const smartDucking$W = "Adaptiv həcm";
  const VOTAutoSubtitles$W = "Açıldıqda altyazılar";
  const VOTSubtitlesFont$W = "Altyazı şrifti";
  const __vite_glob_0_3 = {
    recommended: recommended$W,
    translateVideo: translateVideo$W,
    disableTranslate: disableTranslate$W,
    translationSettings: translationSettings$W,
    subtitlesSettings: subtitlesSettings$W,
    resetSettings: resetSettings$W,
    videoBeingTranslated: videoBeingTranslated$W,
    videoLanguage: videoLanguage$W,
    translationLanguage: translationLanguage$W,
    translationTake: translationTake$W,
    translationTakeMoreThanHour: translationTakeMoreThanHour$W,
    translationTakeAboutMinute: translationTakeAboutMinute$W,
    translationTakeFewMinutes: translationTakeFewMinutes$W,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$W,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$W,
    requestTranslationFailed: requestTranslationFailed$W,
    audioNotReceived: audioNotReceived$W,
    audioFormatNotSupported: audioFormatNotSupported$W,
    VOTAutoTranslate: VOTAutoTranslate$W,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$W,
    VOTVolume: VOTVolume$W,
    VOTVolumeTranslation: VOTVolumeTranslation$W,
    VOTAutoSetVolume: VOTAutoSetVolume$W,
    VOTShowVideoSlider: VOTShowVideoSlider$W,
    VOTSyncVolume: VOTSyncVolume$W,
    VOTDisableFromYourLang: VOTDisableFromYourLang$W,
    VOTVideoIsTooLong: VOTVideoIsTooLong$W,
    VOTNoVideoIDFound: VOTNoVideoIDFound$W,
    VOTSubtitles: VOTSubtitles$W,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$W,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$W,
    VOTHighlightWords: VOTHighlightWords$W,
    VOTTranslatedFrom: VOTTranslatedFrom$W,
    VOTAutogenerated: VOTAutogenerated$W,
    VOTSettings: VOTSettings$W,
    VOTMenuLanguage: VOTMenuLanguage$W,
    VOTAuthors: VOTAuthors$W,
    VOTVersion: VOTVersion$W,
    VOTLoader: VOTLoader$W,
    VOTBrowser: VOTBrowser$W,
    VOTShowPiPButton: VOTShowPiPButton$W,
    langs: langs$W,
    streamNoConnectionToServer: streamNoConnectionToServer$W,
    searchField: searchField$W,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$W,
    VOTDetectService: VOTDetectService$W,
    VOTProxyWorkerHost: VOTProxyWorkerHost$W,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$W,
    proxySettings: proxySettings$W,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$W,
    VOTAudioBooster: VOTAudioBooster$W,
    VOTSubtitlesDesign: VOTSubtitlesDesign$W,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$W,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$W,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$W,
    VOTDownloadWithName: VOTDownloadWithName$W,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$W,
    VOTLocaleHash: VOTLocaleHash$W,
    VOTUpdatedAt: VOTUpdatedAt$W,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$W,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$W,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$W,
    VOTNewAudioPlayer: VOTNewAudioPlayer$W,
    VOTUseNewModel: VOTUseNewModel$W,
    TranslationDelayed: TranslationDelayed$W,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$W,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$W,
    VOTBugReport: VOTBugReport$W,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$W,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$W,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$W,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$W,
    VOTTranslatedBy: VOTTranslatedBy$W,
    VOTStreamNotAvailable: VOTStreamNotAvailable$W,
    VOTTranslationTextService: VOTTranslationTextService$W,
    VOTNotAffectToVoice: VOTNotAffectToVoice$W,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$W,
    showVideoVolumeSlider: showVideoVolumeSlider$W,
    hotkeysSettings: hotkeysSettings$W,
    None: None$W,
    VOTUseLivelyVoice: VOTUseLivelyVoice$W,
    miscSettings: miscSettings$W,
    services: services$W,
    aboutExtension: aboutExtension$W,
    appearance: appearance$W,
    buttonPosition: buttonPosition$W,
    position: position$W,
    secs: secs$W,
    autoHideButtonDelay: autoHideButtonDelay$W,
    notFound: notFound$W,
    minButtonPositionContainer: minButtonPositionContainer$W,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$W,
    PressTheKeyCombination: PressTheKeyCombination$W,
    VOTUseAudioDownload: VOTUseAudioDownload$W,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$W,
    VOTAccountRequired: VOTAccountRequired$W,
    VOTMyAccount: VOTMyAccount$W,
    VOTLogin: VOTLogin$W,
    VOTLogout: VOTLogout$W,
    VOTRefresh: VOTRefresh$W,
    VOTYandexToken: VOTYandexToken$W,
    VOTYandexTokenInfo: VOTYandexTokenInfo$W,
    VOTLoginViaToken: VOTLoginViaToken$W,
    subtitlesSmartLayout: subtitlesSmartLayout$W,
    smartDucking: smartDucking$W,
    VOTAutoSubtitles: VOTAutoSubtitles$W,
    VOTSubtitlesFont: VOTSubtitlesFont$W
  };
  const recommended$V = "препоръчано";
  const translateVideo$V = "Превод на видео";
  const disableTranslate$V = "Изключи";
  const translationSettings$V = "Настройки за превод";
  const subtitlesSettings$V = "Настройки на субтитрите";
  const resetSettings$V = "Нулиране на настройките";
  const videoBeingTranslated$V = "Видеото се превежда";
  const videoLanguage$V = "Видео език";
  const translationLanguage$V = "Език на превода";
  const translationTake$V = "Преводът ще бъде";
  const translationTakeMoreThanHour$V = "Преводът ще отнеме повече от час";
  const translationTakeAboutMinute$V = "Преводът ще отнеме около минута.";
  const translationTakeFewMinutes$V = "Преводът ще отнеме няколко минути.";
  const translationTakeApproximatelyMinutes$V = "Преводът ще отнеме около {0} минути";
  const translationTakeApproximatelyMinute$V = "Преводът ще отнеме около {0} минути";
  const requestTranslationFailed$V = "Неуспешно искане за превод на видео";
  const audioNotReceived$V = "Аудио връзката не е получена";
  const audioFormatNotSupported$V = "Аудио форматът не се поддържа";
  const VOTAutoTranslate$V = "Превод на отворен";
  const VOTDontTranslateYourLang$V = "Не превеждайте от моя език";
  const VOTVolume$V = "Видео обем:";
  const VOTVolumeTranslation$V = "Обем на превода:";
  const VOTAutoSetVolume$V = "Намалете силата на звука на видеото";
  const VOTShowVideoSlider$V = "Слайдер за видео обем";
  const VOTSyncVolume$V = "Линк превод и видео обем";
  const VOTDisableFromYourLang$V = "Деактивирали сте превода на видеото на вашия език";
  const VOTVideoIsTooLong$V = "Видеото е твърде дълго";
  const VOTNoVideoIDFound$V = "Не е намерен видео идентификатор";
  const VOTSubtitles$V = "Субтитри";
  const VOTSubtitlesDisabled$V = "Изключено";
  const VOTSubtitlesMaxLength$V = "Субтитри максимална дължина";
  const VOTHighlightWords$V = "Подчертаване на думи";
  const VOTTranslatedFrom$V = "превод от";
  const VOTAutogenerated$V = "АвтоГенериран";
  const VOTSettings$V = "Настройки на глас";
  const VOTMenuLanguage$V = "Език на менюто";
  const VOTAuthors$V = "Автори";
  const VOTVersion$V = "Версия";
  const VOTLoader$V = "Товарач";
  const VOTBrowser$V = "Браузър";
  const VOTShowPiPButton$V = "Показване на Пип бутон";
  const langs$V = { "auto": "Авто", "af": "Африкаанал", "ak": "Акан", "sq": "Албански", "am": "Амхарски", "ar": "Арабски", "hy": "Арменски", "as": "Асамски", "ay": "Аймара", "az": "Азербайджански", "bn": "Бангла", "eu": "Баските", "be": "Белоруски", "bho": "Боджпури", "bs": "Босненски", "bg": "Български", "my": "Бирмански", "ca": "Каталонски", "ceb": "Себуано", "zh": "Китайски", "zh-Hans": "Китайски (Опростен)", "zh-Hant": "Китайски (Традиционен)", "co": "Корсикански", "hr": "Хърватски", "cs": "Чешка", "da": "Датски", "dv": "Дивехи", "nl": "Холандски", "en": "Български", "eo": "Есперанто", "et": "Естонски", "ee": "Ове", "fil": "Филипински", "fi": "Финландски", "fr": "Френски", "gl": "Галисийски", "lg": "Ганда", "ka": "Грузински", "de": "Немски", "el": "Гръцки", "gn": "Гуарани", "gu": "Гуджарати", "ht": "Хаитянски Креолски", "ha": "Хауса", "haw": "Хавайски", "iw": "Иврит", "hi": "Хинди", "hmn": "Хмонг", "hu": "Унгарски", "is": "Исландски", "ig": "Игбо", "id": "Индонезийски", "ga": "Ирландски", "it": "Италиански", "ja": "Японско", "jv": "Явански", "kn": "Каннада", "kk": "Казахски", "km": "Кхмерски", "rw": "Киняруанда", "ko": "Корейски", "kri": "Крио", "ku": "Кюрдски", "ky": "Киргизки", "lo": "Лао", "la": "Латински", "lv": "Латвийски", "ln": "Лингала", "lt": "Литовски", "lb": "Люксембургски", "mk": "Македонски", "mg": "Малагаси", "ms": "Малайски", "ml": "Малаялам", "mt": "Малтийски", "mi": "Мā", "mr": "Маратхи", "mn": "Монголски", "ne": "Непалски", "nso": "Северна Сото", "no": "Норвежки", "ny": "Няня", "or": "Одия", "om": "Оромо", "ps": "Пущу", "fa": "Персийски", "pl": "Полски", "pt": "Португалски", "pa": "Пенджабски", "qu": "Кечуа", "ro": "Румънски", "ru": "Руски", "sm": "Самоански", "sa": "Санскрит", "gd": "Шотландски Келтски", "sr": "Сръбски", "sn": "Шона", "sd": "Синдхи", "si": "Синхала", "sk": "Словашки", "sl": "Словенски", "so": "Сомалийски", "st": "Южен Сото", "es": "Испански", "su": "Ластик", "sw": "Суахили", "sv": "Шведски", "tg": "Таджикски", "ta": "Тамилски", "tt": "Татарски", "te": "Телугу", "th": "Тайландски", "ti": "Тигриня", "ts": "Цонга", "tr": "Турски", "tk": "Туркменски", "uk": "Украински", "ur": "Урду", "ug": "Уйгурски", "uz": "Узбекски", "vi": "Виетнамски", "cy": "Уелски", "fy": "Западнофризийски", "xh": "Хоса", "yi": "Идиш", "yo": "Йоруба", "zu": "Зулу" };
  const streamNoConnectionToServer$V = "Няма връзка със сървъра";
  const searchField$V = "Търси...";
  const VOTTranslateAPIErrors$V = "Грешки при превод от АПИ";
  const VOTDetectService$V = "Услуга за откриване на езици";
  const VOTProxyWorkerHost$V = "Въведете адреса на пълномощника";
  const VOTM3u8ProxyHost$V = "Въведете адреса на прокси служителя м3у8";
  const proxySettings$V = "Настройки На Прокси Сървъра";
  const translationTakeApproximatelyMinute2$V = "Преводът ще отнеме около {0} минути";
  const VOTAudioBooster$V = "Увеличаване на обема на превода";
  const VOTSubtitlesDesign$V = "Субтитри дизайн";
  const VOTSubtitlesFontSize$V = "Размер на шрифта на субтитрите";
  const VOTSubtitlesOpacity$V = "Прозрачност на фона на субтитрите";
  const VOTSubtitlesDownloadFormat$V = "Формат за изтегляне на субтитри";
  const VOTDownloadWithName$V = "Изтегляне на файлове с името на видеото";
  const VOTUpdateLocaleFiles$V = "Актуализиране на локализационните файлове";
  const VOTLocaleHash$V = "Локален хеш";
  const VOTUpdatedAt$V = "Актуализирано в";
  const VOTNeedWebAudioAPI$V = "За да разрешите това, трябва да имате уеб аудио интерфейс";
  const VOTMediaCSPEnabledOnSite$V = "Медиа ДУСП е активирана на този сайт";
  const VOTOnlyBypassMediaCSP$V = "Използвайте го само за заобикаляне на медийния ДУУ";
  const VOTNewAudioPlayer$V = "Използвайте новия аудио плейър";
  const VOTUseNewModel$V = "Използвайте експериментална вариация на Яндекс гласове за някои видеоклипове";
  const TranslationDelayed$V = "Преводът леко закъснява.";
  const VOTTranslationCompletedNotify$V = "Преводът на {0} е завършен!";
  const VOTSendNotifyOnComplete$V = "Изпратете съобщение, че видеото е преведено";
  const VOTBugReport$V = "Съобщете за грешка";
  const VOTTranslateProxyDisabled$V = "Изключено";
  const VOTTranslateProxyEnabled$V = "Включено";
  const VOTTranslateProxyEverything$V = "Прокси всичко";
  const VOTTranslateProxyStatus$V = "Прокси режим";
  const VOTTranslatedBy$V = "Преведено от {0}";
  const VOTStreamNotAvailable$V = "Превод поток не е наличен";
  const VOTTranslationTextService$V = "Превод на текст";
  const VOTNotAffectToVoice$V = "Не влияе на превода на текст в глас над";
  const DontTranslateSelectedLanguages$V = "Не превеждайте от избрани езици";
  const showVideoVolumeSlider$V = "Показване на слайдер за силата на звука";
  const hotkeysSettings$V = "Настройки на клавишните комбинации";
  const None$V = "Няма";
  const VOTUseLivelyVoice$V = "Използвайте живи гласове. Говорителите звучат като местни руснаци.";
  const miscSettings$V = "Настройки на различни";
  const services$V = { "yandexbrowser": "Яндекс Браузър", "msedge": "Майкрософт Едж", "rust-server": "Ръжда Сървър" };
  const aboutExtension$V = "За разширението";
  const appearance$V = "Външен вид";
  const buttonPosition$V = "Позиция на бутона в плейъра";
  const position$V = { "left": "Ляво", "right": "Дясно", "top": "Топ", "default": "По подразбиране" };
  const secs$V = "сек";
  const autoHideButtonDelay$V = "Забавяне преди скриване на бутона за превод";
  const notFound$V = "не е намерен";
  const minButtonPositionContainer$V = "Позицията на бутона се променя само при играчи, по-големи от 600 пиксела.";
  const VOTTranslateProxyStatusDefault$V = "Пълното деактивиране на прокси услугите във вашата страна може да наруши разширението";
  const PressTheKeyCombination$V = "Натиснете клавишната комбинация...";
  const VOTUseAudioDownload$V = "Използване на аудио изтегляне";
  const VOTUseAudioDownloadWarning$V = "Деактивирането на аудио изтеглянията може да повлияе на функционалността на разширението";
  const VOTAccountRequired$V = "Трябва да влезете, за да използвате тази функция";
  const VOTMyAccount$V = "Моят акаунт";
  const VOTLogin$V = "Вход";
  const VOTLogout$V = "Изход";
  const VOTRefresh$V = "Обновяване";
  const VOTYandexToken$V = "Въведете Яндекс Оут токен";
  const VOTYandexTokenInfo$V = "Можете ръчно да настроите маркера за акаунт в това поле. Моля, имайте предвид, че не проверяваме валидността му, преди да изпратим заявка за превод";
  const VOTLoginViaToken$V = "Вход чрез токен";
  const subtitlesSmartLayout$V = "Интелигентно оформление на субтитрите";
  const smartDucking$V = "Адаптивен обем";
  const VOTAutoSubtitles$V = "Субтитри на открито";
  const VOTSubtitlesFont$V = "Шрифт за субтитрите";
  const __vite_glob_0_4 = {
    recommended: recommended$V,
    translateVideo: translateVideo$V,
    disableTranslate: disableTranslate$V,
    translationSettings: translationSettings$V,
    subtitlesSettings: subtitlesSettings$V,
    resetSettings: resetSettings$V,
    videoBeingTranslated: videoBeingTranslated$V,
    videoLanguage: videoLanguage$V,
    translationLanguage: translationLanguage$V,
    translationTake: translationTake$V,
    translationTakeMoreThanHour: translationTakeMoreThanHour$V,
    translationTakeAboutMinute: translationTakeAboutMinute$V,
    translationTakeFewMinutes: translationTakeFewMinutes$V,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$V,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$V,
    requestTranslationFailed: requestTranslationFailed$V,
    audioNotReceived: audioNotReceived$V,
    audioFormatNotSupported: audioFormatNotSupported$V,
    VOTAutoTranslate: VOTAutoTranslate$V,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$V,
    VOTVolume: VOTVolume$V,
    VOTVolumeTranslation: VOTVolumeTranslation$V,
    VOTAutoSetVolume: VOTAutoSetVolume$V,
    VOTShowVideoSlider: VOTShowVideoSlider$V,
    VOTSyncVolume: VOTSyncVolume$V,
    VOTDisableFromYourLang: VOTDisableFromYourLang$V,
    VOTVideoIsTooLong: VOTVideoIsTooLong$V,
    VOTNoVideoIDFound: VOTNoVideoIDFound$V,
    VOTSubtitles: VOTSubtitles$V,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$V,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$V,
    VOTHighlightWords: VOTHighlightWords$V,
    VOTTranslatedFrom: VOTTranslatedFrom$V,
    VOTAutogenerated: VOTAutogenerated$V,
    VOTSettings: VOTSettings$V,
    VOTMenuLanguage: VOTMenuLanguage$V,
    VOTAuthors: VOTAuthors$V,
    VOTVersion: VOTVersion$V,
    VOTLoader: VOTLoader$V,
    VOTBrowser: VOTBrowser$V,
    VOTShowPiPButton: VOTShowPiPButton$V,
    langs: langs$V,
    streamNoConnectionToServer: streamNoConnectionToServer$V,
    searchField: searchField$V,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$V,
    VOTDetectService: VOTDetectService$V,
    VOTProxyWorkerHost: VOTProxyWorkerHost$V,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$V,
    proxySettings: proxySettings$V,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$V,
    VOTAudioBooster: VOTAudioBooster$V,
    VOTSubtitlesDesign: VOTSubtitlesDesign$V,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$V,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$V,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$V,
    VOTDownloadWithName: VOTDownloadWithName$V,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$V,
    VOTLocaleHash: VOTLocaleHash$V,
    VOTUpdatedAt: VOTUpdatedAt$V,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$V,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$V,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$V,
    VOTNewAudioPlayer: VOTNewAudioPlayer$V,
    VOTUseNewModel: VOTUseNewModel$V,
    TranslationDelayed: TranslationDelayed$V,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$V,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$V,
    VOTBugReport: VOTBugReport$V,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$V,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$V,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$V,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$V,
    VOTTranslatedBy: VOTTranslatedBy$V,
    VOTStreamNotAvailable: VOTStreamNotAvailable$V,
    VOTTranslationTextService: VOTTranslationTextService$V,
    VOTNotAffectToVoice: VOTNotAffectToVoice$V,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$V,
    showVideoVolumeSlider: showVideoVolumeSlider$V,
    hotkeysSettings: hotkeysSettings$V,
    None: None$V,
    VOTUseLivelyVoice: VOTUseLivelyVoice$V,
    miscSettings: miscSettings$V,
    services: services$V,
    aboutExtension: aboutExtension$V,
    appearance: appearance$V,
    buttonPosition: buttonPosition$V,
    position: position$V,
    secs: secs$V,
    autoHideButtonDelay: autoHideButtonDelay$V,
    notFound: notFound$V,
    minButtonPositionContainer: minButtonPositionContainer$V,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$V,
    PressTheKeyCombination: PressTheKeyCombination$V,
    VOTUseAudioDownload: VOTUseAudioDownload$V,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$V,
    VOTAccountRequired: VOTAccountRequired$V,
    VOTMyAccount: VOTMyAccount$V,
    VOTLogin: VOTLogin$V,
    VOTLogout: VOTLogout$V,
    VOTRefresh: VOTRefresh$V,
    VOTYandexToken: VOTYandexToken$V,
    VOTYandexTokenInfo: VOTYandexTokenInfo$V,
    VOTLoginViaToken: VOTLoginViaToken$V,
    subtitlesSmartLayout: subtitlesSmartLayout$V,
    smartDucking: smartDucking$V,
    VOTAutoSubtitles: VOTAutoSubtitles$V,
    VOTSubtitlesFont: VOTSubtitlesFont$V
  };
  const recommended$U = "সুপারিশ করা হয়";
  const translateVideo$U = "ভিডিও অনুবাদ করুন";
  const disableTranslate$U = "বন্ধ করুন";
  const translationSettings$U = "অনুবাদ সেটিংস";
  const subtitlesSettings$U = "সাবটাইটেল সেটিংস";
  const resetSettings$U = "সেটিংস রিসেট করুন";
  const videoBeingTranslated$U = "ভিডিও অনুবাদ করা হচ্ছে";
  const videoLanguage$U = "ভিডিও ভাষা";
  const translationLanguage$U = "অনুবাদ ভাষা";
  const translationTake$U = "অনুবাদ করার সময়";
  const translationTakeMoreThanHour$U = "এক ঘন্টার বেশি সময় একটি অনুবাদ নেয়";
  const translationTakeAboutMinute$U = "এক মিনিটে অনুবাদ করা হয়";
  const translationTakeFewMinutes$U = "কয়েকটি মিনিট একটি অনুবাদ নেয়";
  const translationTakeApproximatelyMinutes$U = "একটি অনুবাদ প্রায় {0} মিনিট সময় নেবে";
  const translationTakeApproximatelyMinute$U = "একটি অনুবাদ প্রায় {0} মিনিট সময় নেবে";
  const requestTranslationFailed$U = "ভিডিও অনুবাদের অনুরোধ ব্যর্থ হয়েছে";
  const audioNotReceived$U = "অডিও লিঙ্ক পাওয়া যায়নি";
  const audioFormatNotSupported$U = "অডিও ফরম্যাটটি সমর্থিত নয়";
  const VOTAutoTranslate$U = "খোলার সময় অনুবাদ করুন";
  const VOTDontTranslateYourLang$U = "নিজের ভাষা অনুবাদ না করুন";
  const VOTVolume$U = "ভিডিও ভলিউম:";
  const VOTVolumeTranslation$U = "অনুবাদ ভলিউম:";
  const VOTAutoSetVolume$U = "ভিডিও ভলিউম হ্রাস করুন";
  const VOTShowVideoSlider$U = "ভিডিও ভলিউম স্লাইডার দেখান";
  const VOTSyncVolume$U = "অনুবাদ এবং ভিডিও ভলিউম সিঙ্ক করুন";
  const VOTDisableFromYourLang$U = "আপনি আপনার ভাষায় ভিডিও অনুবাদ নিষ্ক্রিয় করেছেন";
  const VOTVideoIsTooLong$U = "ভিডিওটি খুব দীর্ঘদিন";
  const VOTNoVideoIDFound$U = "ভিডিও আইডি পাওয়া যায়নি";
  const VOTSubtitles$U = "সাবটাইটেল";
  const VOTSubtitlesDisabled$U = "অক্ষম";
  const VOTSubtitlesMaxLength$U = "সাবটাইটেলের সর্বাধিক দৈর্ঘ্য";
  const VOTHighlightWords$U = "শব্দগুলি হাইলাইট করুন";
  const VOTTranslatedFrom$U = "এর থেকে অনুবাদকৃত";
  const VOTAutogenerated$U = "স্বয়ংক্রিয়ভাবে তৈরি হয়েছে";
  const VOTSettings$U = "VOT সেটিংস";
  const VOTMenuLanguage$U = "মেনু ভাষা";
  const VOTAuthors$U = "লেখক";
  const VOTVersion$U = "সংস্করণ";
  const VOTLoader$U = "লোডার";
  const VOTBrowser$U = "ব্রাউজার";
  const VOTShowPiPButton$U = "PiP বোতাম দেখান";
  const langs$U = { "auto": "স্বয়ংক্রিয়ভাবে", "az": "আজারবাইজানী", "ay": "আইমারা", "ak": "আকান", "sq": "আলবেনীয়", "am": "আমহারিক", "en": "ইংরেজি", "ar": "আরবি", "hy": "আর্মেনিয়", "as": "অসমীয়া", "af": "আফ্রিকান্স", "eu": "বাস্ক", "be": "বেলারুশিয়", "bn": "বাংলা", "my": "বর্মি", "bg": "বুলগেরিয়ান", "bs": "বসনীয়", "bho": "ভোজপুরী", "cy": "ওয়েলশ", "hu": "হাঙ্গেরীয়", "vi": "ভিয়েতনামী", "haw": "হাওয়াইয়ান", "ht": "হাইতিয়ান", "gl": "গ্যালিশিয়ান", "lg": "গান্ডা", "el": "গ্রীক", "ka": "জর্জিয়", "gn": "গুয়ারানি", "gu": "গুজরাটি", "gd": "স্কটস গ্যালিক", "da": "ডেনমার্ক", "fy": "পশ্চিমফ্রিসিয়ান", "zu": "জুলু", "iw": "হিব্রু", "ig": "ইগবো", "yi": "ইয়িদ্দিশ", "id": "ইন্দোনেশীয়", "ga": "আয়ারল্যান্ডীয়", "is": "আইসলণ্ডীয়", "es": "স্প্যানিশ", "it": "ইতালিয়", "yo": "ইওরুবা", "kk": "কাজাখ", "kn": "কন্নড়", "ca": "কাতালান", "qu": "কেচুয়া", "rw": "কিনয়ারোয়ান্ডা", "ky": "কির্গিজ", "zh": "চীনা", "zh-Hant": "চীনা (ঐতিহ্যবাহী)", "zh-Hans": "চীনা (সহজোগ্রহণযোগ্য)", "ko": "কোরিয়ান", "co": "কর্সিকান", "xh": "খোসা", "ku": "কুর্দি", "km": "খ্মের", "lo": "লাওস", "la": "লাতিন", "lv": "লাটভিয়ান", "ln": "লিঙ্গালা", "lt": "লিথুইনীয়", "lb": "লুক্সেমবার্গীয়", "mk": "ম্যাসেডোনীয়", "mg": "মালাগাসি", "ms": "মালয়", "ml": "মলায়ালম", "dv": "ধিবি", "mt": "মল্টিয়", "mi": "মাওরি", "mr": "মারাঠি", "mn": "মঙ্গোলিয়", "de": "জার্মানি", "ne": "নেপালি", "nl": "ওলন্দাজ", "no": "নরওয়েজীয়", "ny": "নয়ানজা", "or": "ওড়িয়া", "om": "ওরোমো", "pa": "পাঞ্জাবী", "fa": "ফার্সি", "pl": "পোলন্ডীয়", "pt": "পর্তুগীজ", "ps": "পাশতু", "ro": "রোমানীয়", "ru": "রশিয়ান", "sm": "সামোয়ান", "sa": "সংস্কৃত", "ceb": "সিবুয়ানো", "nso": "উত্তর সোথো", "sr": "সার্বিয়ান", "si": "সিংহলী", "sd": "সিন্ধি", "sk": "স্লোভাক", "sl": "স্লোভেনীয়", "so": "সোমালীয়", "sw": "সোয়াহিলি", "su": "সুদানী", "tg": "তাজিক", "th": "থাই", "ta": "তামিল", "tt": "তাতার", "te": "তেলুগু", "ti": "টিগরিনিয়া", "ts": "সঙ্গা", "tr": "তুর্কি", "tk": "তুর্কমেনি", "uz": "উজবেকীয়", "ug": "উইঘুর", "uk": "ইউক্রেনীয়", "ur": "উর্দু", "fil": "ফিলিপিনো", "fi": "ফিনিশ", "fr": "ফরাসি", "ha": "হাউসা", "hi": "হিন্দি", "hmn": "হমোঙ", "hr": "ক্রোয়েশীয়", "cs": "চেক", "sv": "সুয়েডীয়", "sn": "শোনা", "ee": "ইওয়ে", "eo": "এস্পেরান্তো", "et": "এস্তোনীয়", "st": "দক্ষিণ সোথো", "jv": "জাভানি", "ja": "জাপানি", "kri": "ক্রি" };
  const streamNoConnectionToServer$U = "সার্ভারের সাথে কোন সংযোগ নেই";
  const searchField$U = "অনুসন্ধান...";
  const VOTTranslateAPIErrors$U = "এপিআই থেকে ত্রুটিগুলি অনুবাদ করুন";
  const VOTDetectService$U = "ভাষা সনাক্তকরণ পরিষেবা";
  const VOTProxyWorkerHost$U = "প্রক্সি কর্মী ঠিকানা লিখুন";
  const VOTM3u8ProxyHost$U = "এম 3 ইউ 8 প্রক্সি কর্মীর ঠিকানা লিখুন";
  const proxySettings$U = "প্রক্সি সেটিংস";
  const translationTakeApproximatelyMinute2$U = "অনুবাদটি প্রায় {0} মিনিট সময় নেবে";
  const VOTAudioBooster$U = "বর্ধিত অনুবাদ ভলিউম বৃদ্ধি";
  const VOTSubtitlesDesign$U = "সাবটাইটেল ডিজাইন";
  const VOTSubtitlesFontSize$U = "সাবটাইটেলের ফন্টের আকার";
  const VOTSubtitlesOpacity$U = "সাবটাইটেল পটভূমির স্বচ্ছতা";
  const VOTSubtitlesDownloadFormat$U = "সাবটাইটেল ডাউনলোড করার জন্য ফর্ম্যাট";
  const VOTDownloadWithName$U = "ভিডিও নাম সহ ফাইলগুলি ডাউনলোড করুন";
  const VOTUpdateLocaleFiles$U = "স্থানীয়করণ ফাইল আপডেট করুন";
  const VOTLocaleHash$U = "লোকেল হ্যাশ";
  const VOTUpdatedAt$U = "এ আপডেট হয়েছে";
  const VOTNeedWebAudioAPI$U = "এটি সক্ষম করতে আপনার অবশ্যই একটি ওয়েব অডিও এপিআই থাকতে হবে";
  const VOTMediaCSPEnabledOnSite$U = "এই সাইটে মিডিয়া সিএসপি সক্ষম করা হয়েছে";
  const VOTOnlyBypassMediaCSP$U = "মিডিয়া সিএসপি বাইপাস করার জন্য এটি ব্যবহার করুন";
  const VOTNewAudioPlayer$U = "নতুন অডিও প্লেয়ার ব্যবহার করুন";
  const VOTUseNewModel$U = "কিছু ভিডিওর জন্য ইয়ানডেক্স ভয়েসের একটি পরীক্ষামূলক বৈচিত্র ব্যবহার করুন";
  const TranslationDelayed$U = "অনুবাদ সামান্য বিলম্বিত হয়";
  const VOTTranslationCompletedNotify$U = "{0} এর অনুবাদ সম্পন্ন হয়েছে!";
  const VOTSendNotifyOnComplete$U = "একটি বিজ্ঞপ্তি পাঠান যে ভিডিওটি অনুবাদ করা হয়েছে";
  const VOTBugReport$U = "একটি বাগ রিপোর্ট করুন";
  const VOTTranslateProxyDisabled$U = "নিষ্ক্রিয়";
  const VOTTranslateProxyEnabled$U = "সক্রিয়";
  const VOTTranslateProxyEverything$U = "প্রক্সি সবকিছু";
  const VOTTranslateProxyStatus$U = "প্রক্সি মোড";
  const VOTTranslatedBy$U = "অনুবাদ করেছেন {0}";
  const VOTStreamNotAvailable$U = "অনুবাদ স্ট্রিম উপলব্ধ নয়";
  const VOTTranslationTextService$U = "পাঠ্য অনুবাদ পরিষেবা";
  const VOTNotAffectToVoice$U = "ভয়েস ওভার টেক্সট অনুবাদ প্রভাবিত করে না";
  const DontTranslateSelectedLanguages$U = "নির্বাচিত ভাষা থেকে অনুবাদ করবেন না";
  const showVideoVolumeSlider$U = "ভিডিও ভলিউম স্লাইডার প্রদর্শন";
  const hotkeysSettings$U = "হটকি সেটিংস";
  const None$U = "কোনটিই নয়";
  const VOTUseLivelyVoice$U = "প্রাণবন্ত কণ্ঠ ব্যবহার করুন. স্পিকার নেটিভ রাশিয়ান মত শব্দ.";
  const miscSettings$U = "বিবিধ সেটিংস";
  const services$U = { "yandexbrowser": "ইয়ানডেক্স ব্রাউজার", "msedge": "মাইক্রোসফট এজ", "rust-server": "মরিচা সার্ভার" };
  const aboutExtension$U = "এক্সটেনশন সম্পর্কে";
  const appearance$U = "চেহারা";
  const buttonPosition$U = "প্লেয়ারে বোতামের অবস্থান";
  const position$U = { "left": "বাম", "right": "ঠিক আছে", "top": "শীর্ষ", "default": "ডিফল্ট" };
  const secs$U = "সেকেন্ড";
  const autoHideButtonDelay$U = "অনুবাদ বোতামটি লুকানোর আগে বিলম্ব করুন";
  const notFound$U = "পাওয়া যায়নি";
  const minButtonPositionContainer$U = "বোতাম অবস্থান শুধুমাত্র খেলোয়াড়দের চেয়ে বড় পরিবর্তন 600 পিক্সেল.";
  const VOTTranslateProxyStatusDefault$U = "আপনার দেশে প্রক্সিংকে সম্পূর্ণরূপে অক্ষম করা এক্সটেনশনটি ভেঙে দিতে পারে";
  const PressTheKeyCombination$U = "কী সমন্বয় টিপুন...";
  const VOTUseAudioDownload$U = "অডিও ডাউনলোড ব্যবহার করুন";
  const VOTUseAudioDownloadWarning$U = "অডিও ডাউনলোডগুলি অক্ষম করা এক্সটেনশনের কার্যকারিতাকে প্রভাবিত করতে পারে";
  const VOTAccountRequired$U = "এই বৈশিষ্ট্যটি ব্যবহার করতে আপনাকে লগ ইন করতে হবে";
  const VOTMyAccount$U = "আমার অ্যাকাউন্ট";
  const VOTLogin$U = "লগইন";
  const VOTLogout$U = "লগআউট";
  const VOTRefresh$U = "রিফ্রেশ";
  const VOTYandexToken$U = "ইয়ানডেক্স ওউথ টোকেন লিখুন";
  const VOTYandexTokenInfo$U = "আপনি ম্যানুয়ালি এই ক্ষেত্রে অ্যাকাউন্ট টোকেন সেট করতে পারেন. অনুগ্রহ করে মনে রাখবেন যে আমরা একটি অনুবাদ অনুরোধ পাঠানোর আগে এর বৈধতা পরীক্ষা করি না";
  const VOTLoginViaToken$U = "টোকেনের মাধ্যমে লগইন করুন";
  const subtitlesSmartLayout$U = "স্মার্ট সাবটাইটেল বিন্যাস";
  const smartDucking$U = "অভিযোজিত ভলিউম";
  const VOTAutoSubtitles$U = "সাবটাইটেল খোলা";
  const VOTSubtitlesFont$U = "সাবটাইটেল ফন্ট";
  const __vite_glob_0_5 = {
    recommended: recommended$U,
    translateVideo: translateVideo$U,
    disableTranslate: disableTranslate$U,
    translationSettings: translationSettings$U,
    subtitlesSettings: subtitlesSettings$U,
    resetSettings: resetSettings$U,
    videoBeingTranslated: videoBeingTranslated$U,
    videoLanguage: videoLanguage$U,
    translationLanguage: translationLanguage$U,
    translationTake: translationTake$U,
    translationTakeMoreThanHour: translationTakeMoreThanHour$U,
    translationTakeAboutMinute: translationTakeAboutMinute$U,
    translationTakeFewMinutes: translationTakeFewMinutes$U,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$U,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$U,
    requestTranslationFailed: requestTranslationFailed$U,
    audioNotReceived: audioNotReceived$U,
    audioFormatNotSupported: audioFormatNotSupported$U,
    VOTAutoTranslate: VOTAutoTranslate$U,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$U,
    VOTVolume: VOTVolume$U,
    VOTVolumeTranslation: VOTVolumeTranslation$U,
    VOTAutoSetVolume: VOTAutoSetVolume$U,
    VOTShowVideoSlider: VOTShowVideoSlider$U,
    VOTSyncVolume: VOTSyncVolume$U,
    VOTDisableFromYourLang: VOTDisableFromYourLang$U,
    VOTVideoIsTooLong: VOTVideoIsTooLong$U,
    VOTNoVideoIDFound: VOTNoVideoIDFound$U,
    VOTSubtitles: VOTSubtitles$U,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$U,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$U,
    VOTHighlightWords: VOTHighlightWords$U,
    VOTTranslatedFrom: VOTTranslatedFrom$U,
    VOTAutogenerated: VOTAutogenerated$U,
    VOTSettings: VOTSettings$U,
    VOTMenuLanguage: VOTMenuLanguage$U,
    VOTAuthors: VOTAuthors$U,
    VOTVersion: VOTVersion$U,
    VOTLoader: VOTLoader$U,
    VOTBrowser: VOTBrowser$U,
    VOTShowPiPButton: VOTShowPiPButton$U,
    langs: langs$U,
    streamNoConnectionToServer: streamNoConnectionToServer$U,
    searchField: searchField$U,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$U,
    VOTDetectService: VOTDetectService$U,
    VOTProxyWorkerHost: VOTProxyWorkerHost$U,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$U,
    proxySettings: proxySettings$U,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$U,
    VOTAudioBooster: VOTAudioBooster$U,
    VOTSubtitlesDesign: VOTSubtitlesDesign$U,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$U,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$U,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$U,
    VOTDownloadWithName: VOTDownloadWithName$U,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$U,
    VOTLocaleHash: VOTLocaleHash$U,
    VOTUpdatedAt: VOTUpdatedAt$U,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$U,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$U,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$U,
    VOTNewAudioPlayer: VOTNewAudioPlayer$U,
    VOTUseNewModel: VOTUseNewModel$U,
    TranslationDelayed: TranslationDelayed$U,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$U,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$U,
    VOTBugReport: VOTBugReport$U,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$U,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$U,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$U,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$U,
    VOTTranslatedBy: VOTTranslatedBy$U,
    VOTStreamNotAvailable: VOTStreamNotAvailable$U,
    VOTTranslationTextService: VOTTranslationTextService$U,
    VOTNotAffectToVoice: VOTNotAffectToVoice$U,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$U,
    showVideoVolumeSlider: showVideoVolumeSlider$U,
    hotkeysSettings: hotkeysSettings$U,
    None: None$U,
    VOTUseLivelyVoice: VOTUseLivelyVoice$U,
    miscSettings: miscSettings$U,
    services: services$U,
    aboutExtension: aboutExtension$U,
    appearance: appearance$U,
    buttonPosition: buttonPosition$U,
    position: position$U,
    secs: secs$U,
    autoHideButtonDelay: autoHideButtonDelay$U,
    notFound: notFound$U,
    minButtonPositionContainer: minButtonPositionContainer$U,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$U,
    PressTheKeyCombination: PressTheKeyCombination$U,
    VOTUseAudioDownload: VOTUseAudioDownload$U,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$U,
    VOTAccountRequired: VOTAccountRequired$U,
    VOTMyAccount: VOTMyAccount$U,
    VOTLogin: VOTLogin$U,
    VOTLogout: VOTLogout$U,
    VOTRefresh: VOTRefresh$U,
    VOTYandexToken: VOTYandexToken$U,
    VOTYandexTokenInfo: VOTYandexTokenInfo$U,
    VOTLoginViaToken: VOTLoginViaToken$U,
    subtitlesSmartLayout: subtitlesSmartLayout$U,
    smartDucking: smartDucking$U,
    VOTAutoSubtitles: VOTAutoSubtitles$U,
    VOTSubtitlesFont: VOTSubtitlesFont$U
  };
  const recommended$T = "preporučeno";
  const translateVideo$T = "Prevedi video";
  const disableTranslate$T = "Isključi";
  const translationSettings$T = "Postavke prijevoda";
  const subtitlesSettings$T = "Postavke titlova";
  const resetSettings$T = "Resetuj postavke";
  const videoBeingTranslated$T = "Video se prevodi";
  const videoLanguage$T = "Jezik videa";
  const translationLanguage$T = "Jezik prevođenja";
  const translationTake$T = "Prevod će trajati";
  const translationTakeMoreThanHour$T = "Prevod će trajati više od sat vremena";
  const translationTakeAboutMinute$T = "Prevod će trajati oko minut";
  const translationTakeFewMinutes$T = "Prevod će trajati nekoliko minuta";
  const translationTakeApproximatelyMinutes$T = "Prevod će trajati otprilike {0} minuta";
  const translationTakeApproximatelyMinute$T = "Prevod će trajati otprilike {0} minuta";
  const requestTranslationFailed$T = "Neuspjela prijava.";
  const audioNotReceived$T = "Audio link nije primljen";
  const audioFormatNotSupported$T = "Audio format nije podržan";
  const VOTAutoTranslate$T = "Prevedi na open";
  const VOTDontTranslateYourLang$T = "Ne prevodite sa mog jezika";
  const VOTVolume$T = "Video disk:";
  const VOTVolumeTranslation$T = "Volumen prijevoda:";
  const VOTAutoSetVolume$T = "Smanjite zapreminu video zapisa na";
  const VOTShowVideoSlider$T = "Video volume slider";
  const VOTSyncVolume$T = "Prevod linka i volumen videa";
  const VOTDisableFromYourLang$T = "Onemogućili ste prevod videa na svoj jezik";
  const VOTVideoIsTooLong$T = "Video je predugačak";
  const VOTNoVideoIDFound$T = "Nedostaje XPM zaglavlje";
  const VOTSubtitles$T = "Titlovi";
  const VOTSubtitlesDisabled$T = "Isključeno";
  const VOTSubtitlesMaxLength$T = "Titlovi maksimalna dužina";
  const VOTHighlightWords$T = "Istaknite riječi";
  const VOTTranslatedFrom$T = "prevedeno iz";
  const VOTAutogenerated$T = "autogenerated";
  const VOTSettings$T = "Vot postavke";
  const VOTMenuLanguage$T = "Jezik menija";
  const VOTAuthors$T = "Autori";
  const VOTVersion$T = "_sesija";
  const VOTLoader$T = "Utovarivač";
  const VOTBrowser$T = "Preglednik";
  const VOTShowPiPButton$T = "Pokaži dugme za pipanje";
  const langs$T = { "auto": "Automatski", "af": "Afrikaans", "ak": "Akan", "sq": "Albanski", "am": "Amharski", "ar": "Arapski", "hy": "Armenski", "as": "Asamski", "ay": "Aymara", "az": "Azerbejdžanski", "bn": "Bangla", "eu": "Baskijski", "be": "Bjeloruski", "bho": "Bhojpuri", "bs": "Bosanski", "bg": "Bugarski", "my": "Burmanski", "ca": "Katalonski", "ceb": "Cebuano", "zh": "Kineski", "zh-Hans": "Kineski (Pojednostavljeni)", "zh-Hant": "Kineski (Tradicionalni)", "co": "Korzikanski", "hr": "Hrvatski", "cs": "Češki", "da": "Danski", "dv": "Divehi", "nl": "Holandski", "en": "Bosanski", "eo": "Esperanto", "et": "Estonski", "ee": "Ewe", "fil": "Filipinski", "fi": "Finski", "fr": "Francuski", "gl": "Galicijski", "lg": "Ganda", "ka": "Gruzijski", "de": "Njemački", "el": "Grčki", "gn": "Guarani", "gu": "Gudĺľarati", "ht": "Haićanski Kreolski", "ha": "Hausa", "haw": "Hawaiian", "iw": "Hebrejski", "hi": "Hindi", "hmn": "Hmong", "hu": "Mađarski", "is": "Islandski", "ig": "Igbo", "id": "Indonezijski", "ga": "Irski", "it": "Italijanski", "ja": "Japanski", "jv": "Javanski", "kn": "Kanada", "kk": "Kazaški", "km": "Kmerski", "rw": "Kinyarwanda", "ko": "Korejski", "kri": "Krio", "ku": "Kurdski", "ky": "Kirgiski", "lo": "Laoski", "la": "Latiniäťno", "lv": "Latvijski", "ln": "Lingala", "lt": "Litvanski", "lb": "Luksemburški", "mk": "Makedonski", "mg": "Malgaške", "ms": "Malajski", "ml": "Malajalamski", "mt": "Malteški", "mi": "Maori", "mr": "Marati", "mn": "Mongolski", "ne": "Nepalski", "nso": "Sjeverni Sotho", "no": "Norveški", "ny": "Nyanja", "or": "Odia", "om": "Oromo", "ps": "Paštu", "fa": "Perzijski", "pl": "Poljski", "pt": "Portugalski", "pa": "Pandžabi", "qu": "Quechua", "ro": "Rumunski", "ru": "Ruski", "sm": "Samoanski", "sa": "Sanskrit", "gd": "Škotski Galski", "sr": "Srpski", "sn": "Shona", "sd": "Sindhi", "si": "Sinhala", "sk": "Slovački", "sl": "Slovenski", "so": "Somalijski", "st": "Južni Sotho", "es": "Španski", "su": "Sundanese", "sw": "Svahili", "sv": "Švedski", "tg": "Tadžički", "ta": "Tamilski", "tt": "Tatarski", "te": "Telugu", "th": "Tajlandski", "ti": "Tigrinya", "ts": "Tsonga", "tr": "Turski", "tk": "Turkmenski", "uk": "Ukrajinski", "ur": "Urdu", "ug": "Ujgurski", "uz": "Uzbečki", "vi": "Vijetnamski", "cy": "Velški", "fy": "Zapadnofrizijske", "xh": "Xhosa", "yi": "Jidiš", "yo": "Joruba", "zu": "Zulu" };
  const streamNoConnectionToServer$T = "Nema veze sa serverom";
  const searchField$T = "Traži...";
  const VOTTranslateAPIErrors$T = "Prevedi greške iz API-ja";
  const VOTDetectService$T = "Usluga otkrivanja jezika";
  const VOTProxyWorkerHost$T = "Unesite adresu proxy radnika";
  const VOTM3u8ProxyHost$T = "Unesite adresu m3u8 proxy radnika";
  const proxySettings$T = "_postavke ... ";
  const translationTakeApproximatelyMinute2$T = "Prevod će trajati otprilike {0} minuta";
  const VOTAudioBooster$T = "Prošireno povećanje obima prevođenja";
  const VOTSubtitlesDesign$T = "Subtitles design";
  const VOTSubtitlesFontSize$T = "Veličina titlova";
  const VOTSubtitlesOpacity$T = "Transparentnost pozadine titlova";
  const VOTSubtitlesDownloadFormat$T = "Format za preuzimanje titlova";
  const VOTDownloadWithName$T = "Preuzmite datoteke sa imenom video zapisa";
  const VOTUpdateLocaleFiles$T = "Ažurirajte datoteke za lokalizaciju";
  const VOTLocaleHash$T = "Locale hash";
  const VOTUpdatedAt$T = "Ažurirano u";
  const VOTNeedWebAudioAPI$T = "Da biste to omogućili, morate imati Web Audio API";
  const VOTMediaCSPEnabledOnSite$T = "Media CSP je omogućen na ovom sajtu";
  const VOTOnlyBypassMediaCSP$T = "Koristi ga samo za zaobilaženje medija CSP";
  const VOTNewAudioPlayer$T = "Koristi novi audio player";
  const VOTUseNewModel$T = "Upotrijebite eksperimentalnu varijaciju Yandex glasova za neke videozapise";
  const TranslationDelayed$T = "Prevod je malo odložen";
  const VOTTranslationCompletedNotify$T = "Prevod na {0} je završen!";
  const VOTSendNotifyOnComplete$T = "Pošaljite obavještenje da je video preveden";
  const VOTBugReport$T = "Prijavi grešku";
  const VOTTranslateProxyDisabled$T = "Isključeno";
  const VOTTranslateProxyEnabled$T = "Uključeno";
  const VOTTranslateProxyEverything$T = "Proxy sve";
  const VOTTranslateProxyStatus$T = "Proxying mode";
  const VOTTranslatedBy$T = "Translated by {0}";
  const VOTStreamNotAvailable$T = "Translate stream nije dostupan";
  const VOTTranslationTextService$T = "Usluga prevođenja teksta";
  const VOTNotAffectToVoice$T = "Ne utiče na prevod teksta u glasu";
  const DontTranslateSelectedLanguages$T = "Nemoj prevoditi sa izabranih jezika";
  const showVideoVolumeSlider$T = "Prikaži klizač za jačinu zvuka videa";
  const hotkeysSettings$T = "Postavke prečica";
  const None$T = "Nijedan";
  const VOTUseLivelyVoice$T = "Koristi živahne glasove. Zvučnici zvuče kao domaći Rusi.";
  const miscSettings$T = "Misc postavke";
  const services$T = { "yandexbrowser": "Yandex Preglednik", "msedge": "Microsoft Edge", "rust-server": "Rust Server" };
  const aboutExtension$T = "O produženju";
  const appearance$T = "Izgled";
  const buttonPosition$T = "Pozicija dugmeta u plejeru";
  const position$T = { "left": "Lijevo", "right": "Desno", "top": "Na vrh", "default": "Uobičajeno" };
  const secs$T = "secs";
  const autoHideButtonDelay$T = "Vrijeme prije skrivanja dugmeta za prevođenje";
  const notFound$T = "nijedan pogodak nije pronađen.";
  const minButtonPositionContainer$T = "Pozicija dugmeta se menja samo kod plejera većih od 600 piksela.";
  const VOTTranslateProxyStatusDefault$T = "Potpuno onemogućavanje proksiranja u vašoj zemlji može prekinuti proširenje";
  const PressTheKeyCombination$T = "Pritisnite kombinaciju tastera...";
  const VOTUseAudioDownload$T = "Koristi preuzimanje zvuka";
  const VOTUseAudioDownloadWarning$T = "Onemogućavanje preuzimanja zvuka može uticati na funkcionalnost ekstenzije";
  const VOTAccountRequired$T = "Morate se prijaviti da biste koristili ovu funkciju";
  const VOTMyAccount$T = "Moj nalog";
  const VOTLogin$T = "Prijava";
  const VOTLogout$T = "Sound event";
  const VOTRefresh$T = "Osvježi";
  const VOTYandexToken$T = "Unesite Yandex OAuth Token";
  const VOTYandexTokenInfo$T = "Možete ručno podesiti token računa u ovom polju. Imajte na umu da ne provjeravamo njegovu valjanost prije slanja zahtjeva za prevođenje";
  const VOTLoginViaToken$T = "Prijava putem tokena";
  const subtitlesSmartLayout$T = "Smart izgled titlova";
  const smartDucking$T = "Adaptivni volumen";
  const VOTAutoSubtitles$T = "Subtitles on open";
  const VOTSubtitlesFont$T = "Font za titlove";
  const __vite_glob_0_6 = {
    recommended: recommended$T,
    translateVideo: translateVideo$T,
    disableTranslate: disableTranslate$T,
    translationSettings: translationSettings$T,
    subtitlesSettings: subtitlesSettings$T,
    resetSettings: resetSettings$T,
    videoBeingTranslated: videoBeingTranslated$T,
    videoLanguage: videoLanguage$T,
    translationLanguage: translationLanguage$T,
    translationTake: translationTake$T,
    translationTakeMoreThanHour: translationTakeMoreThanHour$T,
    translationTakeAboutMinute: translationTakeAboutMinute$T,
    translationTakeFewMinutes: translationTakeFewMinutes$T,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$T,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$T,
    requestTranslationFailed: requestTranslationFailed$T,
    audioNotReceived: audioNotReceived$T,
    audioFormatNotSupported: audioFormatNotSupported$T,
    VOTAutoTranslate: VOTAutoTranslate$T,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$T,
    VOTVolume: VOTVolume$T,
    VOTVolumeTranslation: VOTVolumeTranslation$T,
    VOTAutoSetVolume: VOTAutoSetVolume$T,
    VOTShowVideoSlider: VOTShowVideoSlider$T,
    VOTSyncVolume: VOTSyncVolume$T,
    VOTDisableFromYourLang: VOTDisableFromYourLang$T,
    VOTVideoIsTooLong: VOTVideoIsTooLong$T,
    VOTNoVideoIDFound: VOTNoVideoIDFound$T,
    VOTSubtitles: VOTSubtitles$T,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$T,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$T,
    VOTHighlightWords: VOTHighlightWords$T,
    VOTTranslatedFrom: VOTTranslatedFrom$T,
    VOTAutogenerated: VOTAutogenerated$T,
    VOTSettings: VOTSettings$T,
    VOTMenuLanguage: VOTMenuLanguage$T,
    VOTAuthors: VOTAuthors$T,
    VOTVersion: VOTVersion$T,
    VOTLoader: VOTLoader$T,
    VOTBrowser: VOTBrowser$T,
    VOTShowPiPButton: VOTShowPiPButton$T,
    langs: langs$T,
    streamNoConnectionToServer: streamNoConnectionToServer$T,
    searchField: searchField$T,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$T,
    VOTDetectService: VOTDetectService$T,
    VOTProxyWorkerHost: VOTProxyWorkerHost$T,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$T,
    proxySettings: proxySettings$T,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$T,
    VOTAudioBooster: VOTAudioBooster$T,
    VOTSubtitlesDesign: VOTSubtitlesDesign$T,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$T,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$T,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$T,
    VOTDownloadWithName: VOTDownloadWithName$T,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$T,
    VOTLocaleHash: VOTLocaleHash$T,
    VOTUpdatedAt: VOTUpdatedAt$T,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$T,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$T,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$T,
    VOTNewAudioPlayer: VOTNewAudioPlayer$T,
    VOTUseNewModel: VOTUseNewModel$T,
    TranslationDelayed: TranslationDelayed$T,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$T,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$T,
    VOTBugReport: VOTBugReport$T,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$T,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$T,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$T,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$T,
    VOTTranslatedBy: VOTTranslatedBy$T,
    VOTStreamNotAvailable: VOTStreamNotAvailable$T,
    VOTTranslationTextService: VOTTranslationTextService$T,
    VOTNotAffectToVoice: VOTNotAffectToVoice$T,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$T,
    showVideoVolumeSlider: showVideoVolumeSlider$T,
    hotkeysSettings: hotkeysSettings$T,
    None: None$T,
    VOTUseLivelyVoice: VOTUseLivelyVoice$T,
    miscSettings: miscSettings$T,
    services: services$T,
    aboutExtension: aboutExtension$T,
    appearance: appearance$T,
    buttonPosition: buttonPosition$T,
    position: position$T,
    secs: secs$T,
    autoHideButtonDelay: autoHideButtonDelay$T,
    notFound: notFound$T,
    minButtonPositionContainer: minButtonPositionContainer$T,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$T,
    PressTheKeyCombination: PressTheKeyCombination$T,
    VOTUseAudioDownload: VOTUseAudioDownload$T,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$T,
    VOTAccountRequired: VOTAccountRequired$T,
    VOTMyAccount: VOTMyAccount$T,
    VOTLogin: VOTLogin$T,
    VOTLogout: VOTLogout$T,
    VOTRefresh: VOTRefresh$T,
    VOTYandexToken: VOTYandexToken$T,
    VOTYandexTokenInfo: VOTYandexTokenInfo$T,
    VOTLoginViaToken: VOTLoginViaToken$T,
    subtitlesSmartLayout: subtitlesSmartLayout$T,
    smartDucking: smartDucking$T,
    VOTAutoSubtitles: VOTAutoSubtitles$T,
    VOTSubtitlesFont: VOTSubtitlesFont$T
  };
  const recommended$S = "recomanat";
  const translateVideo$S = "Traduir vídeo";
  const disableTranslate$S = "Apaga";
  const translationSettings$S = "Arranjament de la traducció";
  const subtitlesSettings$S = "Arranjament dels subtítols";
  const resetSettings$S = "Restableix els paràmetres";
  const videoBeingTranslated$S = "El vídeo està traduït";
  const videoLanguage$S = "Idioma del vídeo";
  const translationLanguage$S = "Llengua de traducció";
  const translationTake$S = "La traducció es farà";
  const translationTakeMoreThanHour$S = "La traducció trigarà més d'una hora";
  const translationTakeAboutMinute$S = "La traducció trigarà aproximadament un minut";
  const translationTakeFewMinutes$S = "La traducció trigarà uns minuts";
  const translationTakeApproximatelyMinutes$S = "La traducció trigarà aproximadament {0} minuts";
  const translationTakeApproximatelyMinute$S = "La traducció trigarà aproximadament {0} minuts";
  const requestTranslationFailed$S = "Ha fallat en demanar la traducció de vídeo";
  const audioNotReceived$S = "No s 'ha rebut l' enllaç d ' àudio";
  const audioFormatNotSupported$S = "El format d'àudio no és compatible";
  const VOTAutoTranslate$S = "Tradueix en obert";
  const VOTDontTranslateYourLang$S = "No tradueixis de la meva llengua";
  const VOTVolume$S = "Volum de vídeo:";
  const VOTVolumeTranslation$S = "Volum de traducció:";
  const VOTAutoSetVolume$S = "Redueix el volum de vídeo a";
  const VOTShowVideoSlider$S = "Control lliscant de volum de vídeo";
  const VOTSyncVolume$S = "Traducció d'enllaços i volum de vídeo";
  const VOTDisableFromYourLang$S = "Heu desactivat la traducció del vídeo en el vostre idioma";
  const VOTVideoIsTooLong$S = "El vídeo és massa llarg";
  const VOTNoVideoIDFound$S = "No s ' ha trobat cap identificador de vídeo";
  const VOTSubtitles$S = "Subtítols";
  const VOTSubtitlesDisabled$S = "Deshabilitat";
  const VOTSubtitlesMaxLength$S = "Longitud màxima dels subtítols";
  const VOTHighlightWords$S = "Ressalta les paraules";
  const VOTTranslatedFrom$S = "traduït de";
  const VOTAutogenerated$S = "autogenerat";
  const VOTSettings$S = "Configuració DEL VOT";
  const VOTMenuLanguage$S = "Menú idioma";
  const VOTAuthors$S = "Autors";
  const VOTVersion$S = "Versió";
  const VOTLoader$S = "Carregador";
  const VOTBrowser$S = "Navegador";
  const VOTShowPiPButton$S = "Mostra el botó pip";
  const langs$S = { "auto": "Automàtic", "af": "Asiàtiques", "ak": "Aqan", "sq": "Albanès", "am": "Amhàric", "ar": "Àrab", "hy": "Armeni", "as": "Assamès", "ay": "Aimara", "az": "Àzeri", "bn": "Bangla", "eu": "Basc", "be": "Bielorús", "bho": "Bhojpuri", "bs": "Bosnià", "bg": "Búlgar", "my": "Birmà", "ca": "Català", "ceb": "Cebuà", "zh": "Xinès", "zh-Hans": "Xinès (Simplificat)", "zh-Hant": "Xinès (Tradicional)", "co": "Cors", "hr": "Croata", "cs": "Txec", "da": "Danès", "dv": "Divehi", "nl": "Holandès", "en": "Anglès", "eo": "Esperanto", "et": "Estonià", "ee": "Ovella", "fil": "Filipí", "fi": "Finlandès", "fr": "Francès", "gl": "Gallec", "lg": "Ganda", "ka": "Georgià", "de": "Alemany", "el": "Grec", "gn": "Guaraní", "gu": "Gujarati", "ht": "Crioll Haitià", "ha": "Hausses", "haw": "Haitià", "iw": "Hebreu", "hi": "Hindi", "hmn": "Hmong", "hu": "Hongarès", "is": "Islandès", "ig": "Ibos", "id": "Indonèsia", "ga": "Irlandès", "it": "Italià", "ja": "Japonès", "jv": "Javanès", "kn": "Canada", "kk": "Bagà", "km": "Jemer", "rw": "Bh mallorca magaluf (mallorca)", "ko": "Coreà", "kri": "Crio", "ku": "Català", "ky": "Cyrgyz", "lo": "Laos", "la": "Llatí", "lv": "Letó", "ln": "Lingala", "lt": "Lituà", "lb": "Luxemburguès", "mk": "Macedònia", "mg": "Malgaix", "ms": "Malai", "ml": "Malaiàlam", "mt": "Maltès", "mi": "M ermori", "mr": "Marathi", "mn": "Mongol", "ne": "Nepalès", "nso": "Sotho Septentrional", "no": "Noruec", "ny": "Nyanja", "or": "Odia", "om": "Oromo", "ps": "Paixtu", "fa": "Persa", "pl": "Polonès", "pt": "Portuguès", "pa": "Panjabi", "qu": "Quítxua", "ro": "Romanès", "ru": "Rus", "sm": "Samoana", "sa": "Sànscrit", "gd": "Gaèlic Escocès", "sr": "Serbi", "sn": "Xona", "sd": "Sindhi", "si": "Singalès", "sk": "Eslovac", "sl": "Eslovè", "so": "Somali", "st": "Sotho Meridional", "es": "Castellà", "su": "Tèxtil", "sw": "Suahili", "sv": "Suec", "tg": "Tadjic", "ta": "Tàmil", "tt": "Tàrtar", "te": "Telugu", "th": "Tailandès", "ti": "Tigrinya", "ts": "Tsonga", "tr": "Turc", "tk": "Turcmans", "uk": "Ucraïnès", "ur": "Urdú", "ug": "Uigur", "uz": "Uzbec", "vi": "Vietnamita", "cy": "Gales", "fy": "Frisó Occidental", "xh": "Xosa", "yi": "Jiddisch", "yo": "Ioruba", "zu": "Zulu" };
  const streamNoConnectionToServer$S = "No hi ha connexió amb el servidor";
  const searchField$S = "Cerca...";
  const VOTTranslateAPIErrors$S = "Traduir errors des DE L'API";
  const VOTDetectService$S = "Servei de detecció d'idiomes";
  const VOTProxyWorkerHost$S = "Introduïu l'adreça del treballador del servidor intermediari";
  const VOTM3u8ProxyHost$S = "Introduïu l'adreça del treballador intermediari m3u8";
  const proxySettings$S = "Arranjament Del Servidor Intermediari";
  const translationTakeApproximatelyMinute2$S = "La traducció trigarà aproximadament {0} minuts";
  const VOTAudioBooster$S = "Ampliació del volum de traducció";
  const VOTSubtitlesDesign$S = "Disseny de subtítols";
  const VOTSubtitlesFontSize$S = "Mida del tipus de lletra dels subtítols";
  const VOTSubtitlesOpacity$S = "Transparència del fons dels subtítols";
  const VOTSubtitlesDownloadFormat$S = "El format per descarregar subtítols";
  const VOTDownloadWithName$S = "Baixeu fitxers amb el nom del vídeo";
  const VOTUpdateLocaleFiles$S = "Actualitza els fitxers de localització";
  const VOTLocaleHash$S = "Hash de localització";
  const VOTUpdatedAt$S = "Actualització a les";
  const VOTNeedWebAudioAPI$S = "Per activar-ho, heu de tenir UNA API D'Àudio";
  const VOTMediaCSPEnabledOnSite$S = "Media CSP està habilitat en aquest lloc";
  const VOTOnlyBypassMediaCSP$S = "Utilitzeu lo només per evitar Els Mitjans Csp";
  const VOTNewAudioPlayer$S = "Utilitzeu el nou reproductor d'àudio";
  const VOTUseNewModel$S = "Utilitzeu una variació experimental De veus De Yandex per a alguns vídeos";
  const TranslationDelayed$S = "La traducció es retarda lleugerament";
  const VOTTranslationCompletedNotify$S = "La traducció al {0} s'ha completat!";
  const VOTSendNotifyOnComplete$S = "Envia una notificació que el vídeo ha estat traduït";
  const VOTBugReport$S = "Informa d ' un error";
  const VOTTranslateProxyDisabled$S = "Deshabilitat";
  const VOTTranslateProxyEnabled$S = "Habilitat";
  const VOTTranslateProxyEverything$S = "Proxy tot";
  const VOTTranslateProxyStatus$S = "Mode de Proxying";
  const VOTTranslatedBy$S = "Traduït per {0}";
  const VOTStreamNotAvailable$S = "El flux de traducció no està disponible";
  const VOTTranslationTextService$S = "Servei de traducció de textos";
  const VOTNotAffectToVoice$S = "No afecta la traducció de text en veu en off";
  const DontTranslateSelectedLanguages$S = "No tradueixis dels idiomes seleccionats";
  const showVideoVolumeSlider$S = "Mostra el control lliscant del volum del vídeo";
  const hotkeysSettings$S = "Arranjament de les tecles d ' accés ràpid";
  const None$S = "Cap";
  const VOTUseLivelyVoice$S = "Utilitzeu veus animades. Els parlants sonen Com Russos nadius.";
  const miscSettings$S = "Arranjaments diversos";
  const services$S = { "yandexbrowser": "Navegador Yandex", "msedge": "Microsoft Edge", "rust-server": "Servidor Rust" };
  const aboutExtension$S = "Sobre l'extensió";
  const appearance$S = "Aparença";
  const buttonPosition$S = "Posició del botó al reproductor";
  const position$S = { "left": "Esquerra", "right": "Dret", "top": "Dalt", "default": "Per omissió" };
  const secs$S = "sec";
  const autoHideButtonDelay$S = "Retard abans d'ocultar el botó traduir";
  const notFound$S = "no trobat";
  const minButtonPositionContainer$S = "La posició del botó només canvia en jugadors de més de 600 píxels.";
  const VOTTranslateProxyStatusDefault$S = "Desactivar completament el proxy al vostre país pot trencar l'extensió";
  const PressTheKeyCombination$S = "Premeu la combinació de tecles...";
  const VOTUseAudioDownload$S = "Utilitza la descàrrega d'àudio";
  const VOTUseAudioDownloadWarning$S = "La desactivació de les descàrregues d'àudio pot afectar la funcionalitat de l'extensió";
  const VOTAccountRequired$S = "Heu d'iniciar sessió per utilitzar aquesta funció";
  const VOTMyAccount$S = "El meu compte";
  const VOTLogin$S = "Accés";
  const VOTLogout$S = "Sortida";
  const VOTRefresh$S = "Refresca";
  const VOTYandexToken$S = "Introduïu El Testimoni Yandex OAuth";
  const VOTYandexTokenInfo$S = "Podeu configurar manualment el testimoni del compte en aquest camp. Tingueu en compte que no comprovem la seva validesa abans d'enviar una petició de traducció";
  const VOTLoginViaToken$S = "Inicieu sessió mitjançant testimoni";
  const subtitlesSmartLayout$S = "Disseny de subtítols intel ligent";
  const smartDucking$S = "Volum adaptatiu";
  const VOTAutoSubtitles$S = "Subtítols en obert";
  const VOTSubtitlesFont$S = "Tipus de lletra dels subtítols";
  const __vite_glob_0_7 = {
    recommended: recommended$S,
    translateVideo: translateVideo$S,
    disableTranslate: disableTranslate$S,
    translationSettings: translationSettings$S,
    subtitlesSettings: subtitlesSettings$S,
    resetSettings: resetSettings$S,
    videoBeingTranslated: videoBeingTranslated$S,
    videoLanguage: videoLanguage$S,
    translationLanguage: translationLanguage$S,
    translationTake: translationTake$S,
    translationTakeMoreThanHour: translationTakeMoreThanHour$S,
    translationTakeAboutMinute: translationTakeAboutMinute$S,
    translationTakeFewMinutes: translationTakeFewMinutes$S,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$S,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$S,
    requestTranslationFailed: requestTranslationFailed$S,
    audioNotReceived: audioNotReceived$S,
    audioFormatNotSupported: audioFormatNotSupported$S,
    VOTAutoTranslate: VOTAutoTranslate$S,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$S,
    VOTVolume: VOTVolume$S,
    VOTVolumeTranslation: VOTVolumeTranslation$S,
    VOTAutoSetVolume: VOTAutoSetVolume$S,
    VOTShowVideoSlider: VOTShowVideoSlider$S,
    VOTSyncVolume: VOTSyncVolume$S,
    VOTDisableFromYourLang: VOTDisableFromYourLang$S,
    VOTVideoIsTooLong: VOTVideoIsTooLong$S,
    VOTNoVideoIDFound: VOTNoVideoIDFound$S,
    VOTSubtitles: VOTSubtitles$S,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$S,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$S,
    VOTHighlightWords: VOTHighlightWords$S,
    VOTTranslatedFrom: VOTTranslatedFrom$S,
    VOTAutogenerated: VOTAutogenerated$S,
    VOTSettings: VOTSettings$S,
    VOTMenuLanguage: VOTMenuLanguage$S,
    VOTAuthors: VOTAuthors$S,
    VOTVersion: VOTVersion$S,
    VOTLoader: VOTLoader$S,
    VOTBrowser: VOTBrowser$S,
    VOTShowPiPButton: VOTShowPiPButton$S,
    langs: langs$S,
    streamNoConnectionToServer: streamNoConnectionToServer$S,
    searchField: searchField$S,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$S,
    VOTDetectService: VOTDetectService$S,
    VOTProxyWorkerHost: VOTProxyWorkerHost$S,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$S,
    proxySettings: proxySettings$S,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$S,
    VOTAudioBooster: VOTAudioBooster$S,
    VOTSubtitlesDesign: VOTSubtitlesDesign$S,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$S,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$S,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$S,
    VOTDownloadWithName: VOTDownloadWithName$S,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$S,
    VOTLocaleHash: VOTLocaleHash$S,
    VOTUpdatedAt: VOTUpdatedAt$S,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$S,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$S,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$S,
    VOTNewAudioPlayer: VOTNewAudioPlayer$S,
    VOTUseNewModel: VOTUseNewModel$S,
    TranslationDelayed: TranslationDelayed$S,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$S,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$S,
    VOTBugReport: VOTBugReport$S,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$S,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$S,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$S,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$S,
    VOTTranslatedBy: VOTTranslatedBy$S,
    VOTStreamNotAvailable: VOTStreamNotAvailable$S,
    VOTTranslationTextService: VOTTranslationTextService$S,
    VOTNotAffectToVoice: VOTNotAffectToVoice$S,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$S,
    showVideoVolumeSlider: showVideoVolumeSlider$S,
    hotkeysSettings: hotkeysSettings$S,
    None: None$S,
    VOTUseLivelyVoice: VOTUseLivelyVoice$S,
    miscSettings: miscSettings$S,
    services: services$S,
    aboutExtension: aboutExtension$S,
    appearance: appearance$S,
    buttonPosition: buttonPosition$S,
    position: position$S,
    secs: secs$S,
    autoHideButtonDelay: autoHideButtonDelay$S,
    notFound: notFound$S,
    minButtonPositionContainer: minButtonPositionContainer$S,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$S,
    PressTheKeyCombination: PressTheKeyCombination$S,
    VOTUseAudioDownload: VOTUseAudioDownload$S,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$S,
    VOTAccountRequired: VOTAccountRequired$S,
    VOTMyAccount: VOTMyAccount$S,
    VOTLogin: VOTLogin$S,
    VOTLogout: VOTLogout$S,
    VOTRefresh: VOTRefresh$S,
    VOTYandexToken: VOTYandexToken$S,
    VOTYandexTokenInfo: VOTYandexTokenInfo$S,
    VOTLoginViaToken: VOTLoginViaToken$S,
    subtitlesSmartLayout: subtitlesSmartLayout$S,
    smartDucking: smartDucking$S,
    VOTAutoSubtitles: VOTAutoSubtitles$S,
    VOTSubtitlesFont: VOTSubtitlesFont$S
  };
  const recommended$R = "doporučeno";
  const translateVideo$R = "Přeložit video";
  const disableTranslate$R = "Vypnout";
  const translationSettings$R = "Nastavení překladu";
  const subtitlesSettings$R = "Nastavení titulků";
  const resetSettings$R = "Obnovit nastavení";
  const videoBeingTranslated$R = "Video se překládá";
  const videoLanguage$R = "Jazyk videa";
  const translationLanguage$R = "Jazyk překladu";
  const translationTake$R = "Překlad zabere";
  const translationTakeMoreThanHour$R = "Překlad zabere více než hodinu";
  const translationTakeAboutMinute$R = "Překlad zabere asi minutu";
  const translationTakeFewMinutes$R = "Překlad zabere několik minut";
  const translationTakeApproximatelyMinutes$R = "Překlad zabere přibližně {0} minut";
  const translationTakeApproximatelyMinute$R = "Překlad zabere přibližně {0} minuty";
  const requestTranslationFailed$R = "Nepodařilo se požádat o překlad videa";
  const audioNotReceived$R = "Nepřišel odkaz na zvuk";
  const audioFormatNotSupported$R = "Formát zvuku není podporován";
  const VOTAutoTranslate$R = "Překládat automaticky při otevření";
  const VOTDontTranslateYourLang$R = "Nepřekládat z vašeho jazyka";
  const VOTVolume$R = "Hlasitost videa:";
  const VOTVolumeTranslation$R = "Objem překladu:";
  const VOTAutoSetVolume$R = "Snižte hlasitost videa na";
  const VOTShowVideoSlider$R = "Zobrazit posuvník hlasitosti videa";
  const VOTSyncVolume$R = "Synchronizovat hlasitost překladu a videa";
  const VOTDisableFromYourLang$R = "Vypnout překlad videa ve vašem jazyce";
  const VOTVideoIsTooLong$R = "Video je příliš dlouhé";
  const VOTNoVideoIDFound$R = "Nenalezeno ID videa";
  const VOTSubtitles$R = "Titulky";
  const VOTSubtitlesDisabled$R = "Vypnuto";
  const VOTSubtitlesMaxLength$R = "Maximální délka titulků";
  const VOTHighlightWords$R = "Zvýrazňovat slova";
  const VOTTranslatedFrom$R = "Přeloženo z";
  const VOTAutogenerated$R = "Automaticky vygenerováno";
  const VOTSettings$R = "Nastavení VOT";
  const VOTMenuLanguage$R = "Jazyk menu";
  const VOTAuthors$R = "Autoři";
  const VOTVersion$R = "Verze";
  const VOTLoader$R = "Nakladač";
  const VOTBrowser$R = "Prohlížeč";
  const VOTShowPiPButton$R = "Zobrazit tlačítko PiP";
  const langs$R = { "auto": "Automaticky", "az": "Ázerbájdžánština", "ay": "Ajmarština", "ak": "Akanština", "sq": "Albánština", "am": "Amharština", "en": "Angličtina", "ar": "Arabština", "hy": "Arménština", "as": "Asámština", "af": "Afrikánština", "eu": "Baskičtina", "be": "Běloruština", "bn": "Bengálština", "my": "Barmština", "bg": "Bulharština", "bs": "Bosenština", "bho": "Bhódžpurština", "cy": "Velština", "hu": "Maďarština", "vi": "Vietnamština", "haw": "Havajština", "ht": "Haitská kreolština", "gl": "Galícijština", "lg": "Gandština", "el": "Řečtina", "ka": "Gruzínština", "gn": "Guaranština", "gu": "Gudžarátština", "gd": "Skotská gaelština", "da": "Dánština", "fy": "Západofríština", "zu": "Zuluština", "iw": "Hebrejština", "ig": "Igboština", "yi": "Jidiš", "id": "Indonéština", "ga": "Irština", "is": "Islandština", "es": "Španělština", "it": "Italština", "yo": "Jorubština", "kk": "Kazaština", "kn": "Kannadština", "ca": "Katalánština", "qu": "Kečuánština", "rw": "Kiňjarwandština", "ky": "Kyrgyzština", "zh": "Čínština", "zh-Hant": "Čínština (tradiční)", "zh-Hans": "Čínština (zjednodušená)", "ko": "Korejština", "co": "Korsičtina", "xh": "Xhoština", "ku": "Kurdština", "km": "Khmerština", "lo": "Laoština", "la": "Latina", "lv": "Lotyština", "ln": "Lingalština", "lt": "Litevština", "lb": "Lucemburština", "mk": "Makedonština", "mg": "Malgaština", "ms": "Malajština", "ml": "Malajálamština", "dv": "Maledivština", "mt": "Maltština", "mi": "Maorština", "mr": "Maráthština", "mn": "Mongolština", "de": "Němčina", "ne": "Nepálština", "nl": "Nizozemština", "no": "Norština", "ny": "Njandžština", "or": "Orija", "om": "Oromština", "pa": "Paňdžábština", "fa": "Perština", "pl": "Polština", "pt": "Portugalština", "ps": "Paštština", "ro": "Rumunština", "ru": "Ruština", "sm": "Samojština", "sa": "Sanskrt", "ceb": "Cebuánština", "nso": "Severní sothština", "sr": "Srbský", "si": "Sinhálština", "sd": "Sindhština", "sk": "Slovenčina", "sl": "Slovinština", "so": "Somálština", "sw": "Svahilština", "su": "Sundanština", "tg": "Tádžičtina", "th": "Thajština", "ta": "Tamilština", "tt": "Tatarština", "te": "Telugština", "ti": "Tigrinština", "ts": "Tsongština", "tr": "Turečtina", "tk": "Turkmenština", "uz": "Uzbečtina", "ug": "Ujgurština", "uk": "Ukrajinština", "ur": "Urdština", "fil": "Filipínština", "fi": "Finština", "fr": "Francouzština", "ha": "Hauština", "hi": "Hindština", "hmn": "Hmongština", "hr": "Chorvatština", "cs": "Čeština", "sv": "Švédština", "sn": "Šonština", "ee": "Eweština", "eo": "Esperanto", "et": "Estonština", "st": "Sotština", "jv": "Javánština", "ja": "Japonština", "kri": "Krio" };
  const streamNoConnectionToServer$R = "Neexistuje žádné připojení k serveru";
  const searchField$R = "Vyhledávání...";
  const VOTTranslateAPIErrors$R = "Přeložit chyby z API";
  const VOTDetectService$R = "Služba detekce jazyků";
  const VOTProxyWorkerHost$R = "Zadejte adresu proxy pracovníka";
  const VOTM3u8ProxyHost$R = "Zadejte adresu pracovníka proxy m3u8";
  const proxySettings$R = "Nastavení Serveru Proxy";
  const translationTakeApproximatelyMinute2$R = "Překlad bude trvat přibližně {0} minut";
  const VOTAudioBooster$R = "Rozšíření objemu překladu";
  const VOTSubtitlesDesign$R = "Titulky design";
  const VOTSubtitlesFontSize$R = "Velikost písma titulků";
  const VOTSubtitlesOpacity$R = "Průhlednost pozadí titulků";
  const VOTSubtitlesDownloadFormat$R = "Formát pro stahování titulků";
  const VOTDownloadWithName$R = "Stahujte soubory s názvem videa";
  const VOTUpdateLocaleFiles$R = "Aktualizace lokalizačních souborů";
  const VOTLocaleHash$R = "Národní hash";
  const VOTUpdatedAt$R = "Aktualizováno v";
  const VOTNeedWebAudioAPI$R = "Chcete-li to povolit, musíte mít webové Audio API";
  const VOTMediaCSPEnabledOnSite$R = "Media CSP je povoleno na tomto webu";
  const VOTOnlyBypassMediaCSP$R = "Použijte jej pouze pro obcházení médií CSP";
  const VOTNewAudioPlayer$R = "Použijte nový audio přehrávač";
  const VOTUseNewModel$R = "U některých videí použijte experimentální variantu hlasů Yandex";
  const TranslationDelayed$R = "Překlad je mírně zpožděn";
  const VOTTranslationCompletedNotify$R = "Překlad na {0} byl dokončen!";
  const VOTSendNotifyOnComplete$R = "Pošlete oznámení, že video bylo přeloženo";
  const VOTBugReport$R = "Nahlásit chybu";
  const VOTTranslateProxyDisabled$R = "Zakázán";
  const VOTTranslateProxyEnabled$R = "Povolit";
  const VOTTranslateProxyEverything$R = "Proxy vše";
  const VOTTranslateProxyStatus$R = "Režim proxy";
  const VOTTranslatedBy$R = "Přeložil {0}";
  const VOTStreamNotAvailable$R = "Translate stream není k dispozici";
  const VOTTranslationTextService$R = "Překladatelská služba";
  const VOTNotAffectToVoice$R = "Nemá vliv na překlad textu v voice over";
  const DontTranslateSelectedLanguages$R = "Nepřekládat z vybraných jazyků";
  const showVideoVolumeSlider$R = "Zobrazení posuvníku hlasitosti videa";
  const hotkeysSettings$R = "Nastavení klávesových zkratek";
  const None$R = "Žádný";
  const VOTUseLivelyVoice$R = "Používejte živé hlasy. Reproduktory znějí jako rodilí Rusové.";
  const miscSettings$R = "Různé nastavení";
  const services$R = { "yandexbrowser": "Prohlížeč Yandex", "msedge": "Microsoft Edge", "rust-server": "Rust Server" };
  const aboutExtension$R = "O rozšíření";
  const appearance$R = "Vzhled";
  const buttonPosition$R = "Pozice tlačítka v přehrávači";
  const position$R = { "left": "Levá", "right": "Právo", "top": "Horný", "default": "Default" };
  const secs$R = "sekunda";
  const autoHideButtonDelay$R = "Zpoždění před skrytím tlačítka přeložit";
  const notFound$R = "nalézt";
  const minButtonPositionContainer$R = "Pozice tlačítka se mění pouze u hráčů větších než 600 pixelů.";
  const VOTTranslateProxyStatusDefault$R = "Úplné deaktivace proxy ve vaší zemi může rozšíření přerušit";
  const PressTheKeyCombination$R = "Stiskněte kombinaci kláves...";
  const VOTUseAudioDownload$R = "Použijte stahování zvuku";
  const VOTUseAudioDownloadWarning$R = "Zakázání stahování zvuku může ovlivnit funkčnost rozšíření";
  const VOTAccountRequired$R = "Chcete-li tuto funkci používat, musíte se přihlásit";
  const VOTMyAccount$R = "Můj účet";
  const VOTLogin$R = "Přihlášení";
  const VOTLogout$R = "Odhlášení";
  const VOTRefresh$R = "Aktualizovat";
  const VOTYandexToken$R = "Zadejte Token Yandex OAuth";
  const VOTYandexTokenInfo$R = "V tomto poli můžete ručně nastavit token účtu. Vezměte prosím na vědomí, že před odesláním požadavku na překlad nekontrolujeme jeho platnost";
  const VOTLoginViaToken$R = "Přihlášení přes token";
  const subtitlesSmartLayout$R = "Inteligentní rozložení titulků";
  const smartDucking$R = "Adaptivní objem";
  const VOTAutoSubtitles$R = "Titulky na open";
  const VOTSubtitlesFont$R = "Písmo titulků";
  const __vite_glob_0_8 = {
    recommended: recommended$R,
    translateVideo: translateVideo$R,
    disableTranslate: disableTranslate$R,
    translationSettings: translationSettings$R,
    subtitlesSettings: subtitlesSettings$R,
    resetSettings: resetSettings$R,
    videoBeingTranslated: videoBeingTranslated$R,
    videoLanguage: videoLanguage$R,
    translationLanguage: translationLanguage$R,
    translationTake: translationTake$R,
    translationTakeMoreThanHour: translationTakeMoreThanHour$R,
    translationTakeAboutMinute: translationTakeAboutMinute$R,
    translationTakeFewMinutes: translationTakeFewMinutes$R,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$R,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$R,
    requestTranslationFailed: requestTranslationFailed$R,
    audioNotReceived: audioNotReceived$R,
    audioFormatNotSupported: audioFormatNotSupported$R,
    VOTAutoTranslate: VOTAutoTranslate$R,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$R,
    VOTVolume: VOTVolume$R,
    VOTVolumeTranslation: VOTVolumeTranslation$R,
    VOTAutoSetVolume: VOTAutoSetVolume$R,
    VOTShowVideoSlider: VOTShowVideoSlider$R,
    VOTSyncVolume: VOTSyncVolume$R,
    VOTDisableFromYourLang: VOTDisableFromYourLang$R,
    VOTVideoIsTooLong: VOTVideoIsTooLong$R,
    VOTNoVideoIDFound: VOTNoVideoIDFound$R,
    VOTSubtitles: VOTSubtitles$R,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$R,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$R,
    VOTHighlightWords: VOTHighlightWords$R,
    VOTTranslatedFrom: VOTTranslatedFrom$R,
    VOTAutogenerated: VOTAutogenerated$R,
    VOTSettings: VOTSettings$R,
    VOTMenuLanguage: VOTMenuLanguage$R,
    VOTAuthors: VOTAuthors$R,
    VOTVersion: VOTVersion$R,
    VOTLoader: VOTLoader$R,
    VOTBrowser: VOTBrowser$R,
    VOTShowPiPButton: VOTShowPiPButton$R,
    langs: langs$R,
    streamNoConnectionToServer: streamNoConnectionToServer$R,
    searchField: searchField$R,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$R,
    VOTDetectService: VOTDetectService$R,
    VOTProxyWorkerHost: VOTProxyWorkerHost$R,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$R,
    proxySettings: proxySettings$R,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$R,
    VOTAudioBooster: VOTAudioBooster$R,
    VOTSubtitlesDesign: VOTSubtitlesDesign$R,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$R,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$R,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$R,
    VOTDownloadWithName: VOTDownloadWithName$R,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$R,
    VOTLocaleHash: VOTLocaleHash$R,
    VOTUpdatedAt: VOTUpdatedAt$R,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$R,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$R,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$R,
    VOTNewAudioPlayer: VOTNewAudioPlayer$R,
    VOTUseNewModel: VOTUseNewModel$R,
    TranslationDelayed: TranslationDelayed$R,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$R,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$R,
    VOTBugReport: VOTBugReport$R,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$R,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$R,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$R,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$R,
    VOTTranslatedBy: VOTTranslatedBy$R,
    VOTStreamNotAvailable: VOTStreamNotAvailable$R,
    VOTTranslationTextService: VOTTranslationTextService$R,
    VOTNotAffectToVoice: VOTNotAffectToVoice$R,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$R,
    showVideoVolumeSlider: showVideoVolumeSlider$R,
    hotkeysSettings: hotkeysSettings$R,
    None: None$R,
    VOTUseLivelyVoice: VOTUseLivelyVoice$R,
    miscSettings: miscSettings$R,
    services: services$R,
    aboutExtension: aboutExtension$R,
    appearance: appearance$R,
    buttonPosition: buttonPosition$R,
    position: position$R,
    secs: secs$R,
    autoHideButtonDelay: autoHideButtonDelay$R,
    notFound: notFound$R,
    minButtonPositionContainer: minButtonPositionContainer$R,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$R,
    PressTheKeyCombination: PressTheKeyCombination$R,
    VOTUseAudioDownload: VOTUseAudioDownload$R,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$R,
    VOTAccountRequired: VOTAccountRequired$R,
    VOTMyAccount: VOTMyAccount$R,
    VOTLogin: VOTLogin$R,
    VOTLogout: VOTLogout$R,
    VOTRefresh: VOTRefresh$R,
    VOTYandexToken: VOTYandexToken$R,
    VOTYandexTokenInfo: VOTYandexTokenInfo$R,
    VOTLoginViaToken: VOTLoginViaToken$R,
    subtitlesSmartLayout: subtitlesSmartLayout$R,
    smartDucking: smartDucking$R,
    VOTAutoSubtitles: VOTAutoSubtitles$R,
    VOTSubtitlesFont: VOTSubtitlesFont$R
  };
  const recommended$Q = "argymhellir";
  const translateVideo$Q = "Cyfieithu fideo";
  const disableTranslate$Q = "Trowch i ffwrdd";
  const translationSettings$Q = "Gosodiadau cyfieithu";
  const subtitlesSettings$Q = "Gosodiadau isdeitlau";
  const resetSettings$Q = "Ailosod gosodiadau";
  const videoBeingTranslated$Q = "Mae'r fideo yn cael ei gyfieithu";
  const videoLanguage$Q = "Iaith fideo";
  const translationLanguage$Q = "Cyfieithu iaith";
  const translationTake$Q = "Bydd y cyfieithiad yn cymryd";
  const translationTakeMoreThanHour$Q = "Bydd y cyfieithiad yn cymryd mwy nag awr";
  const translationTakeAboutMinute$Q = "Bydd y cyfieithiad yn cymryd tua munud";
  const translationTakeFewMinutes$Q = "Bydd y cyfieithiad yn cymryd ychydig funudau";
  const translationTakeApproximatelyMinutes$Q = "Bydd y cyfieithiad yn cymryd oddeutu {0} munud";
  const translationTakeApproximatelyMinute$Q = "Bydd y cyfieithiad yn cymryd oddeutu {0} munud";
  const requestTranslationFailed$Q = "Methu gofyn am gyfieithiad fideo";
  const audioNotReceived$Q = "Ni dderbyniwyd y cyswllt sain";
  const audioFormatNotSupported$Q = "Ni chynhelir y fformat sain";
  const VOTAutoTranslate$Q = "Cyfieithu ar agor";
  const VOTDontTranslateYourLang$Q = "Peidiwch â chyfieithu o fy iaith";
  const VOTVolume$Q = "Cyfrol fideo:";
  const VOTVolumeTranslation$Q = "Cyfrol cyfieithu:";
  const VOTAutoSetVolume$Q = "Lleihau maint y fideo i";
  const VOTShowVideoSlider$Q = "Llithrydd cyfrol fideo";
  const VOTSyncVolume$Q = "Cyfieithu cyswllt a chyfrol fideo";
  const VOTDisableFromYourLang$Q = "Rydych wedi analluogi cyfieithu'r fideo yn eich iaith";
  const VOTVideoIsTooLong$Q = "Fideo yn rhy hir";
  const VOTNoVideoIDFound$Q = "Ni chanfuwyd ID fideo";
  const VOTSubtitles$Q = "_isdeitlau";
  const VOTSubtitlesDisabled$Q = "Analluogwyd";
  const VOTSubtitlesMaxLength$Q = "Is-deitlau hyd max";
  const VOTHighlightWords$Q = "Amlygu geiriau";
  const VOTTranslatedFrom$Q = "wedi ei gyfieithu o";
  const VOTAutogenerated$Q = "awtogynhyrchwyd";
  const VOTSettings$Q = "GOSODIADAU PLEIDLEISIO";
  const VOTMenuLanguage$Q = "Iaith y ddewislen";
  const VOTAuthors$Q = "Awduron";
  const VOTVersion$Q = "Fersiwn";
  const VOTLoader$Q = "Llwythwr";
  const VOTBrowser$Q = "Porwr";
  const VOTShowPiPButton$Q = "Dangos botwm PiP";
  const langs$Q = { "auto": "Awto", "af": "Afrikaans", "ak": "Akan", "sq": "Albaneg", "am": "Amharig", "ar": "Arabeg", "hy": "Armeneg", "as": "Asamese", "ay": "Aymara", "az": "Azerbaijani", "bn": "Bangla", "eu": "Basgeg", "be": "Belarwseg", "bho": "Bhojpuri", "bs": "Bosnia", "bg": "Bwlgareg", "my": "Burma", "ca": "Catalaneg", "ceb": "Cebuano", "zh": "Tseiniaidd", "zh-Hans": "Tsieineaidd (Wedi'i Symleiddio)", "zh-Hant": "Tsieineaidd (Traddodiadol)", "co": "Corsica", "hr": "Croateg", "cs": "Tsieceg", "da": "Daneg", "dv": "Divehi", "nl": "Iseldireg", "en": "English", "eo": "Esperanto", "et": "Estoneg", "ee": "Ewe", "fil": "Tagalog", "fi": "Ffinneg", "fr": "Ffrangeg", "gl": "Galiseg", "lg": "Ganda", "ka": "Georgeg", "de": "Almaeneg", "el": "Groeg", "gn": "Guarani", "gu": "Gwjarati", "ht": "Haitian Creole", "ha": "Hausa", "haw": "Hawaiian", "iw": "Hebraeg", "hi": "Hindi", "hmn": "Hmong", "hu": "Hwngareg", "is": "Islandeg", "ig": "Igbo", "id": "Indonesian", "ga": "Gwyddeleg", "it": "Italiano", "ja": "Japaneg", "jv": "Javanese", "kn": "Canareg", "kk": "Kazakh", "km": "Chmereg", "rw": "Kinyarwanda", "ko": "Corëeg", "kri": "Krio", "ku": "Cwrdeg", "ky": "Kyrgyz", "lo": "Lao", "la": "Lladin", "lv": "Latfieg", "ln": "Lingala", "lt": "Lithwaneg", "lb": "Lwcsembwrgeg", "mk": "Macedoneg", "mg": "Malagasy", "ms": "Malay", "ml": "Malayalam", "mt": "Malta", "mi": "Māori", "mr": "Marathi", "mn": "Mongoleg", "ne": "Nepaleg", "nso": "Northern Sotho", "no": "Norwyeg", "ny": "Nyanja", "or": "Odia", "om": "Oromo", "ps": "Pashto", "fa": "Perseg", "pl": "Pwyleg", "pt": "Português", "pa": "Pwnjabi", "qu": "Quechua", "ro": "Rwmaneg", "ru": "Russian", "sm": "Samoan", "sa": "Sansgrit", "gd": "Gaeleg Yr Alban", "sr": "Serbeg", "sn": "Shona", "sd": "Sindhi", "si": "Sinhala", "sk": "Slofaceg", "sl": "Slofeneg", "so": "Somalieg", "st": "Sotho Southern", "es": "Spanish", "su": "Sundanese", "sw": "Swahili", "sv": "Swedeg", "tg": "Tajik", "ta": "Tamil", "tt": "Tatar", "te": "Telwgw", "th": "Siameg", "ti": "Tigrinya", "ts": "Tsonga", "tr": "Tyrceg", "tk": "Turkmen", "uk": "Wcreineg", "ur": "Urdu", "ug": "Uyghur", "uz": "Wsbeceg", "vi": "Fietnameg", "cy": "Cymraeg", "fy": "Ffriseg Gorllewinol", "xh": "Xhosa", "yi": "Cynnyrch", "yo": "Yoruba", "zu": "Zulu" };
  const streamNoConnectionToServer$Q = "Nid oes cysylltiad i'r defnyddiwr";
  const searchField$Q = "Chwilio...";
  const VOTTranslateAPIErrors$Q = "Cyfieithu gwallau O'R API";
  const VOTDetectService$Q = "Gwasanaeth canfod iaith";
  const VOTProxyWorkerHost$Q = "Rhowch gyfeiriad y gweithiwr dirprwy";
  const VOTM3u8ProxyHost$Q = "Rhowch gyfeiriad y gweithiwr dirprwy m3u8";
  const proxySettings$Q = "Gosodiadau Dirprwy";
  const translationTakeApproximatelyMinute2$Q = "Bydd y cyfieithiad yn cymryd oddeutu {0} munud";
  const VOTAudioBooster$Q = "Cynyddu cyfaint cyfieithu estynedig";
  const VOTSubtitlesDesign$Q = "Isdeitlau dylunio";
  const VOTSubtitlesFontSize$Q = "Maint ffont isdeitlau";
  const VOTSubtitlesOpacity$Q = "Tryloywder cefndir yr is-deitl";
  const VOTSubtitlesDownloadFormat$Q = "Y fformat ar gyfer lawrlwytho isdeitlau";
  const VOTDownloadWithName$Q = "Lawrlwytho ffeiliau gydag enw'r fideo";
  const VOTUpdateLocaleFiles$Q = "Diweddaru ffeiliau lleoleiddio";
  const VOTLocaleHash$Q = "Hash Locale";
  const VOTUpdatedAt$Q = "Diweddarwyd am";
  const VOTNeedWebAudioAPI$Q = "I alluogi HYN, rhaid bod GENNYCH API Sain Gwe";
  const VOTMediaCSPEnabledOnSite$Q = "Mae csp cyfryngau wedi'i alluogi ar y wefan hon";
  const VOTOnlyBypassMediaCSP$Q = "Defnyddiwch ef yn unig ar gyfer osgoi Cyfryngau CSP";
  const VOTNewAudioPlayer$Q = "Defnyddio'r chwaraewr sain newydd";
  const VOTUseNewModel$Q = "Defnyddiwch amrywiad arbrofol o leisiau Yandex ar gyfer rhai fideos";
  const TranslationDelayed$Q = "Mae'r cyfieithiad ychydig yn oedi";
  const VOTTranslationCompletedNotify$Q = "Mae'r cyfieithiad ar y {0} wedi ei gwblhau!";
  const VOTSendNotifyOnComplete$Q = "Anfon hysbysiad bod y fideo wedi'i gyfieithu";
  const VOTBugReport$Q = "Rhoi gwybod am nam";
  const VOTTranslateProxyDisabled$Q = "Analluogwyd";
  const VOTTranslateProxyEnabled$Q = "Galluogwyd";
  const VOTTranslateProxyEverything$Q = "Dirprwy popeth";
  const VOTTranslateProxyStatus$Q = "Modd dirprwyo";
  const VOTTranslatedBy$Q = "Wedi ei gyfieithu gan {0}";
  const VOTStreamNotAvailable$Q = "Nid yw'r ffrwd cyfieithu ar gael";
  const VOTTranslationTextService$Q = "Gwasanaeth cyfieithu testun";
  const VOTNotAffectToVoice$Q = "Nid yw'n effeithio ar y cyfieithiad o destun yn llais dros";
  const DontTranslateSelectedLanguages$Q = "Peidiwch â chyfieithu o ieithoedd dethol";
  const showVideoVolumeSlider$Q = "Dangos y llithrydd cyfrol fideo";
  const hotkeysSettings$Q = "Gosodiadau Hotkeys";
  const None$Q = "Dim";
  const VOTUseLivelyVoice$Q = "Defnyddio lleisiau bywiog. Mae siaradwyr Yn swnio fel Rwsiaid brodorol.";
  const miscSettings$Q = "Gosodiadau amrywiol";
  const services$Q = { "yandexbrowser": "Porwr Yandex", "msedge": "Microsoft Edge", "rust-server": "Gweinydd Rust" };
  const aboutExtension$Q = "Ynghylch estyniad";
  const appearance$Q = "Ymddangosiad";
  const buttonPosition$Q = "Safle'r botwm yn y chwaraewr";
  const position$Q = { "left": "Chwith", "right": "De", "top": "Brig", "default": "Rhagosodiad" };
  const secs$Q = "eiliadau";
  const autoHideButtonDelay$Q = "Saib cyn cuddio'r botwm cyfieithu";
  const notFound$Q = "heb ei ganfod";
  const minButtonPositionContainer$Q = "Mae safle'r botwm ond yn newid mewn chwaraewyr sy'n fwy na 600 picsel.";
  const VOTTranslateProxyStatusDefault$Q = "Gall analluogi dirprwyo'n llwyr yn eich gwlad dorri'r estyniad";
  const PressTheKeyCombination$Q = "Pwyswch y cyfuniad allweddol...";
  const VOTUseAudioDownload$Q = "Defnyddio lawrlwytho sain";
  const VOTUseAudioDownloadWarning$Q = "Gall analluogi lawrlwythiadau sain effeithio ar ymarferoldeb yr estyniad";
  const VOTAccountRequired$Q = "Mae angen i chi fewngofnodi i ddefnyddio'r nodwedd hon";
  const VOTMyAccount$Q = "Fy nghyfrif";
  const VOTLogin$Q = "Login";
  const VOTLogout$Q = "Allgofnodi";
  const VOTRefresh$Q = "Adnewyddu";
  const VOTYandexToken$Q = "Rhowch Y Tocyn Yandex OAuth";
  const VOTYandexTokenInfo$Q = "Gallwch osod y tocyn cyfrif â llaw yn y maes hwn. Nodwch nad ydym yn gwirio ei ddilysrwydd cyn anfon cais cyfieithu";
  const VOTLoginViaToken$Q = "Mewngofnodi trwy docyn";
  const subtitlesSmartLayout$Q = "Cynllun is-deitl Smart";
  const smartDucking$Q = "Cyfrol addasol";
  const VOTAutoSubtitles$Q = "Isdeitlau ar agor";
  const VOTSubtitlesFont$Q = "Ffont isdeitlau";
  const __vite_glob_0_9 = {
    recommended: recommended$Q,
    translateVideo: translateVideo$Q,
    disableTranslate: disableTranslate$Q,
    translationSettings: translationSettings$Q,
    subtitlesSettings: subtitlesSettings$Q,
    resetSettings: resetSettings$Q,
    videoBeingTranslated: videoBeingTranslated$Q,
    videoLanguage: videoLanguage$Q,
    translationLanguage: translationLanguage$Q,
    translationTake: translationTake$Q,
    translationTakeMoreThanHour: translationTakeMoreThanHour$Q,
    translationTakeAboutMinute: translationTakeAboutMinute$Q,
    translationTakeFewMinutes: translationTakeFewMinutes$Q,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$Q,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$Q,
    requestTranslationFailed: requestTranslationFailed$Q,
    audioNotReceived: audioNotReceived$Q,
    audioFormatNotSupported: audioFormatNotSupported$Q,
    VOTAutoTranslate: VOTAutoTranslate$Q,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$Q,
    VOTVolume: VOTVolume$Q,
    VOTVolumeTranslation: VOTVolumeTranslation$Q,
    VOTAutoSetVolume: VOTAutoSetVolume$Q,
    VOTShowVideoSlider: VOTShowVideoSlider$Q,
    VOTSyncVolume: VOTSyncVolume$Q,
    VOTDisableFromYourLang: VOTDisableFromYourLang$Q,
    VOTVideoIsTooLong: VOTVideoIsTooLong$Q,
    VOTNoVideoIDFound: VOTNoVideoIDFound$Q,
    VOTSubtitles: VOTSubtitles$Q,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$Q,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$Q,
    VOTHighlightWords: VOTHighlightWords$Q,
    VOTTranslatedFrom: VOTTranslatedFrom$Q,
    VOTAutogenerated: VOTAutogenerated$Q,
    VOTSettings: VOTSettings$Q,
    VOTMenuLanguage: VOTMenuLanguage$Q,
    VOTAuthors: VOTAuthors$Q,
    VOTVersion: VOTVersion$Q,
    VOTLoader: VOTLoader$Q,
    VOTBrowser: VOTBrowser$Q,
    VOTShowPiPButton: VOTShowPiPButton$Q,
    langs: langs$Q,
    streamNoConnectionToServer: streamNoConnectionToServer$Q,
    searchField: searchField$Q,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$Q,
    VOTDetectService: VOTDetectService$Q,
    VOTProxyWorkerHost: VOTProxyWorkerHost$Q,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$Q,
    proxySettings: proxySettings$Q,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$Q,
    VOTAudioBooster: VOTAudioBooster$Q,
    VOTSubtitlesDesign: VOTSubtitlesDesign$Q,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$Q,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$Q,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$Q,
    VOTDownloadWithName: VOTDownloadWithName$Q,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$Q,
    VOTLocaleHash: VOTLocaleHash$Q,
    VOTUpdatedAt: VOTUpdatedAt$Q,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$Q,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$Q,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$Q,
    VOTNewAudioPlayer: VOTNewAudioPlayer$Q,
    VOTUseNewModel: VOTUseNewModel$Q,
    TranslationDelayed: TranslationDelayed$Q,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$Q,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$Q,
    VOTBugReport: VOTBugReport$Q,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$Q,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$Q,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$Q,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$Q,
    VOTTranslatedBy: VOTTranslatedBy$Q,
    VOTStreamNotAvailable: VOTStreamNotAvailable$Q,
    VOTTranslationTextService: VOTTranslationTextService$Q,
    VOTNotAffectToVoice: VOTNotAffectToVoice$Q,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$Q,
    showVideoVolumeSlider: showVideoVolumeSlider$Q,
    hotkeysSettings: hotkeysSettings$Q,
    None: None$Q,
    VOTUseLivelyVoice: VOTUseLivelyVoice$Q,
    miscSettings: miscSettings$Q,
    services: services$Q,
    aboutExtension: aboutExtension$Q,
    appearance: appearance$Q,
    buttonPosition: buttonPosition$Q,
    position: position$Q,
    secs: secs$Q,
    autoHideButtonDelay: autoHideButtonDelay$Q,
    notFound: notFound$Q,
    minButtonPositionContainer: minButtonPositionContainer$Q,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$Q,
    PressTheKeyCombination: PressTheKeyCombination$Q,
    VOTUseAudioDownload: VOTUseAudioDownload$Q,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$Q,
    VOTAccountRequired: VOTAccountRequired$Q,
    VOTMyAccount: VOTMyAccount$Q,
    VOTLogin: VOTLogin$Q,
    VOTLogout: VOTLogout$Q,
    VOTRefresh: VOTRefresh$Q,
    VOTYandexToken: VOTYandexToken$Q,
    VOTYandexTokenInfo: VOTYandexTokenInfo$Q,
    VOTLoginViaToken: VOTLoginViaToken$Q,
    subtitlesSmartLayout: subtitlesSmartLayout$Q,
    smartDucking: smartDucking$Q,
    VOTAutoSubtitles: VOTAutoSubtitles$Q,
    VOTSubtitlesFont: VOTSubtitlesFont$Q
  };
  const recommended$P = "anbefalet";
  const translateVideo$P = "Oversæt video";
  const disableTranslate$P = "Slukke";
  const translationSettings$P = "Oversættelsesindstillinger";
  const subtitlesSettings$P = "Undertekster indstillinger";
  const resetSettings$P = "Nulstil indstillinger";
  const videoBeingTranslated$P = "Videoen bliver oversat";
  const videoLanguage$P = "Video sprog";
  const translationLanguage$P = "Oversættelsessprog";
  const translationTake$P = "Oversættelsen vil tage";
  const translationTakeMoreThanHour$P = "Oversættelsen tager mere end en time";
  const translationTakeAboutMinute$P = "Oversættelsen tager cirka et minut";
  const translationTakeFewMinutes$P = "Oversættelsen tager et par minutter";
  const translationTakeApproximatelyMinutes$P = "Oversættelsen tager cirka {0} minutter";
  const translationTakeApproximatelyMinute$P = "Oversættelsen tager cirka {0} minutter";
  const requestTranslationFailed$P = "Kunne ikke anmode om videooversættelse";
  const audioNotReceived$P = "Audio link ikke modtaget";
  const audioFormatNotSupported$P = "Lydformatet understøttes ikke";
  const VOTAutoTranslate$P = "Oversæt på åben";
  const VOTDontTranslateYourLang$P = "Oversæt ikke fra mit sprog";
  const VOTVolume$P = "Video volumen:";
  const VOTVolumeTranslation$P = "Oversættelsesvolumen:";
  const VOTAutoSetVolume$P = "Reducer videolydstyrken til";
  const VOTShowVideoSlider$P = "Video volume slider";
  const VOTSyncVolume$P = "Link Oversættelse og video volumen";
  const VOTDisableFromYourLang$P = "Du har deaktiveret oversættelsen af videoen på dit sprog";
  const VOTVideoIsTooLong$P = "Videoen er for lang";
  const VOTNoVideoIDFound$P = "Ingen video-ID fundet";
  const VOTSubtitles$P = "Undertekster";
  const VOTSubtitlesDisabled$P = "Deaktiveret";
  const VOTSubtitlesMaxLength$P = "Undertekster maks længde";
  const VOTHighlightWords$P = "Fremhæv ord";
  const VOTTranslatedFrom$P = "oversat fra";
  const VOTAutogenerated$P = "autogenereret";
  const VOTSettings$P = "VOT-indstillinger";
  const VOTMenuLanguage$P = "Menusprog";
  const VOTAuthors$P = "Forfattere";
  const VOTVersion$P = "Version";
  const VOTLoader$P = "Loader";
  const VOTBrowser$P = "Browser";
  const VOTShowPiPButton$P = "Vis PiP-knap";
  const langs$P = { "auto": "Auto", "af": "Afrikaans", "ak": "Akan", "sq": "Albansk", "am": "Amharisk", "ar": "Arabisk", "hy": "Armensk", "as": "Assamisk", "ay": "Aymara", "az": "Aserbajdsjanske", "bn": "Bangla", "eu": "Baskisk", "be": "Belarussisk", "bho": "Bhojpuri", "bs": "Bosnisk", "bg": "Bulgarske", "my": "Burmesisk", "ca": "Catalansk", "ceb": "Cebuano", "zh": "Kinesisk", "zh-Hans": "Kinesisk (Forenklet)", "zh-Hant": "Kinesisk (Traditionelt)", "co": "Korsika", "hr": "Kroatisk", "cs": "Tjekkisk", "da": "Dansk", "dv": "Divehi", "nl": "Hollandsk", "en": "Engelsk", "eo": "Esperanto", "et": "Estisk", "ee": "Moderfår", "fil": "Filippinsk", "fi": "Finsk", "fr": "Fransk", "gl": "Galicisk", "lg": "Ganda", "ka": "Georgisk", "de": "Tysk", "el": "Græsk", "gn": "Guarani", "gu": "Gujarati", "ht": "Haitisk Kreolsk", "ha": "Hausa", "haw": "Hawaii", "iw": "Hebraisk", "hi": "Hindi", "hmn": "Hmong", "hu": "Ungarsk", "is": "Islandsk", "ig": "Igbo", "id": "Indonesisk", "ga": "Irsk", "it": "Italiensk", "ja": "Japan", "jv": "Javanesisk", "kn": "Kannada", "kk": "Kasakhstan", "km": "Khmer", "rw": "Kinyarvanda", "ko": "Koreansk", "kri": "Krio", "ku": "Kurdisk", "ky": "Kirgisistan", "lo": "Lao", "la": "Latin", "lv": "Lettisk", "ln": "Lingala", "lt": "Litauisk", "lb": "Dansk", "mk": "Makedonsk", "mg": "Madagaskisk", "ms": "Malajisk", "ml": "Malayalam", "mt": "Maltesisk", "mi": "Maori", "mr": "Marathi", "mn": "Mongolsk", "ne": "Nepali", "nso": "Nordlige Sotho", "no": "Norsk", "ny": "Nyanja", "or": "Odia", "om": "Oromo", "ps": "Pashto", "fa": "Persisk", "pl": "Polere", "pt": "Portugisisk", "pa": "Punjabi", "qu": "Skov", "ro": "Rumænsk", "ru": "Russisk", "sm": "Samoansk", "sa": "Sanskrit", "gd": "Skotsk Gælisk", "sr": "Serbisk", "sn": "Shona", "sd": "Sindhi", "si": "Sinhala", "sk": "Slovakisk", "sl": "Slovensk", "so": "Somalia", "st": "Sydlige Sotho", "es": "Spansk", "su": "Sundansk", "sw": "Svahili", "sv": "Svensk", "tg": "Tadsjikiske", "ta": "Tamilsk", "tt": "Tatar", "te": "Telugu", "th": "Thailandsk", "ti": "Tigrinya", "ts": "Tsonga", "tr": "Tyrkisk", "tk": "Turkmenske", "uk": "Ukrainsk", "ur": "Urdu", "ug": "Uighur", "uz": "Usbekistan", "vi": "Sydvietnamesiske", "cy": "Velsh", "fy": "Vestfrisisk", "xh": "Skov", "yi": "Jiddisch", "yo": "Yoruba", "zu": "Sulu" };
  const streamNoConnectionToServer$P = "Der er ingen forbindelse til serveren";
  const searchField$P = "Søge...";
  const VOTTranslateAPIErrors$P = "Oversæt fejl fra API";
  const VOTDetectService$P = "Sprogdetekteringstjeneste";
  const VOTProxyWorkerHost$P = "Indtast adressen på fuldmægtig";
  const VOTM3u8ProxyHost$P = "Indtast adressen på M3U8 fuldmægtig";
  const proxySettings$P = "Fuldmagtsindstillinger";
  const translationTakeApproximatelyMinute2$P = "Oversættelsen tager cirka {0} minutter";
  const VOTAudioBooster$P = "Udvidet oversættelse volumen stigning";
  const VOTSubtitlesDesign$P = "Undertekster design";
  const VOTSubtitlesFontSize$P = "Skriftstørrelse på undertekster";
  const VOTSubtitlesOpacity$P = "Gennemsigtighed i undertekstbaggrunden";
  const VOTSubtitlesDownloadFormat$P = "Formatet til hentning af undertekster";
  const VOTDownloadWithName$P = "Hent filer med videonavnet";
  const VOTUpdateLocaleFiles$P = "Opdater lokaliseringsfiler";
  const VOTLocaleHash$P = "Locale hash";
  const VOTUpdatedAt$P = "Opdateret kl.";
  const VOTNeedWebAudioAPI$P = "For at aktivere dette skal du have en Internet Audio API";
  const VOTMediaCSPEnabledOnSite$P = "Media CSP er aktiveret på dette site";
  const VOTOnlyBypassMediaCSP$P = "Brug den kun til at omgå Media CSP";
  const VOTNewAudioPlayer$P = "Brug den nye lydafspiller";
  const VOTUseNewModel$P = "Brug en eksperimentel variation af Yandeks stemmer til nogle videoer";
  const TranslationDelayed$P = "Oversættelsen er lidt forsinket";
  const VOTTranslationCompletedNotify$P = "Oversættelsen på {0} er afsluttet!";
  const VOTSendNotifyOnComplete$P = "Send en meddelelse om, at videoen er blevet oversat";
  const VOTBugReport$P = "Rapporter en fejl";
  const VOTTranslateProxyDisabled$P = "Deaktiveret";
  const VOTTranslateProxyEnabled$P = "Aktiveret";
  const VOTTranslateProxyEverything$P = "Fuldmagt alt";
  const VOTTranslateProxyStatus$P = "Fuldførelsestilstand";
  const VOTTranslatedBy$P = "Oversat af {0}";
  const VOTStreamNotAvailable$P = "Translate stream er ikke tilgængelig";
  const VOTTranslationTextService$P = "Tekstoversættelsestjeneste";
  const VOTNotAffectToVoice$P = "Påvirker ikke oversættelsen af tekst i voice over";
  const DontTranslateSelectedLanguages$P = "Oversæt ikke fra udvalgte sprog";
  const showVideoVolumeSlider$P = "Vis skyderen video volume";
  const hotkeysSettings$P = "Genvejstaster indstillinger";
  const None$P = "Ingen";
  const VOTUseLivelyVoice$P = "Brug livlige stemmer. Højttalere lyder som indfødte russere.";
  const miscSettings$P = "Diverse indstillinger";
  const services$P = { "yandexbrowser": "Danish Krone (DKK)", "msedge": "Microsoft Edge", "rust-server": "Rust Server" };
  const aboutExtension$P = "Om udvidelse";
  const appearance$P = "Udseende";
  const buttonPosition$P = "Knap position i afspilleren";
  const position$P = { "left": "Venstre", "right": "Ret", "top": "Top", "default": "Standard" };
  const secs$P = "sek";
  const autoHideButtonDelay$P = "Forsink, før du skjuler knappen Oversæt";
  const notFound$P = "ikke fundet";
  const minButtonPositionContainer$P = "Knappositionen ændres kun i spillere, der er større end 600 punkter.";
  const VOTTranslateProxyStatusDefault$P = "Fuldstændig deaktivering af fuldmagt i dit land kan bryde udvidelsen";
  const PressTheKeyCombination$P = "Tryk på tastekombinationen...";
  const VOTUseAudioDownload$P = "Brug lydoverførsel";
  const VOTUseAudioDownloadWarning$P = "Deaktivering af lydoverførsler kan påvirke udvidelsens funktionalitet";
  const VOTAccountRequired$P = "Du skal logge ind for at bruge denne funktion";
  const VOTMyAccount$P = "Min konto";
  const VOTLogin$P = "Login";
  const VOTLogout$P = "Log ud";
  const VOTRefresh$P = "Opdatere";
  const VOTYandexToken$P = "Indtast Yandeks OAuth Token";
  const VOTYandexTokenInfo$P = "Du kan manuelt indstille kontotokenet i dette felt. Bemærk, at vi ikke kontrollerer dens gyldighed, før vi sender en oversættelsesanmodning";
  const VOTLoginViaToken$P = "Log ind via token";
  const subtitlesSmartLayout$P = "Smart undertekstlayout";
  const smartDucking$P = "Adaptiv volumen";
  const VOTAutoSubtitles$P = "Undertekster på åben";
  const VOTSubtitlesFont$P = "Undertekstfont";
  const __vite_glob_0_10 = {
    recommended: recommended$P,
    translateVideo: translateVideo$P,
    disableTranslate: disableTranslate$P,
    translationSettings: translationSettings$P,
    subtitlesSettings: subtitlesSettings$P,
    resetSettings: resetSettings$P,
    videoBeingTranslated: videoBeingTranslated$P,
    videoLanguage: videoLanguage$P,
    translationLanguage: translationLanguage$P,
    translationTake: translationTake$P,
    translationTakeMoreThanHour: translationTakeMoreThanHour$P,
    translationTakeAboutMinute: translationTakeAboutMinute$P,
    translationTakeFewMinutes: translationTakeFewMinutes$P,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$P,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$P,
    requestTranslationFailed: requestTranslationFailed$P,
    audioNotReceived: audioNotReceived$P,
    audioFormatNotSupported: audioFormatNotSupported$P,
    VOTAutoTranslate: VOTAutoTranslate$P,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$P,
    VOTVolume: VOTVolume$P,
    VOTVolumeTranslation: VOTVolumeTranslation$P,
    VOTAutoSetVolume: VOTAutoSetVolume$P,
    VOTShowVideoSlider: VOTShowVideoSlider$P,
    VOTSyncVolume: VOTSyncVolume$P,
    VOTDisableFromYourLang: VOTDisableFromYourLang$P,
    VOTVideoIsTooLong: VOTVideoIsTooLong$P,
    VOTNoVideoIDFound: VOTNoVideoIDFound$P,
    VOTSubtitles: VOTSubtitles$P,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$P,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$P,
    VOTHighlightWords: VOTHighlightWords$P,
    VOTTranslatedFrom: VOTTranslatedFrom$P,
    VOTAutogenerated: VOTAutogenerated$P,
    VOTSettings: VOTSettings$P,
    VOTMenuLanguage: VOTMenuLanguage$P,
    VOTAuthors: VOTAuthors$P,
    VOTVersion: VOTVersion$P,
    VOTLoader: VOTLoader$P,
    VOTBrowser: VOTBrowser$P,
    VOTShowPiPButton: VOTShowPiPButton$P,
    langs: langs$P,
    streamNoConnectionToServer: streamNoConnectionToServer$P,
    searchField: searchField$P,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$P,
    VOTDetectService: VOTDetectService$P,
    VOTProxyWorkerHost: VOTProxyWorkerHost$P,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$P,
    proxySettings: proxySettings$P,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$P,
    VOTAudioBooster: VOTAudioBooster$P,
    VOTSubtitlesDesign: VOTSubtitlesDesign$P,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$P,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$P,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$P,
    VOTDownloadWithName: VOTDownloadWithName$P,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$P,
    VOTLocaleHash: VOTLocaleHash$P,
    VOTUpdatedAt: VOTUpdatedAt$P,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$P,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$P,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$P,
    VOTNewAudioPlayer: VOTNewAudioPlayer$P,
    VOTUseNewModel: VOTUseNewModel$P,
    TranslationDelayed: TranslationDelayed$P,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$P,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$P,
    VOTBugReport: VOTBugReport$P,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$P,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$P,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$P,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$P,
    VOTTranslatedBy: VOTTranslatedBy$P,
    VOTStreamNotAvailable: VOTStreamNotAvailable$P,
    VOTTranslationTextService: VOTTranslationTextService$P,
    VOTNotAffectToVoice: VOTNotAffectToVoice$P,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$P,
    showVideoVolumeSlider: showVideoVolumeSlider$P,
    hotkeysSettings: hotkeysSettings$P,
    None: None$P,
    VOTUseLivelyVoice: VOTUseLivelyVoice$P,
    miscSettings: miscSettings$P,
    services: services$P,
    aboutExtension: aboutExtension$P,
    appearance: appearance$P,
    buttonPosition: buttonPosition$P,
    position: position$P,
    secs: secs$P,
    autoHideButtonDelay: autoHideButtonDelay$P,
    notFound: notFound$P,
    minButtonPositionContainer: minButtonPositionContainer$P,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$P,
    PressTheKeyCombination: PressTheKeyCombination$P,
    VOTUseAudioDownload: VOTUseAudioDownload$P,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$P,
    VOTAccountRequired: VOTAccountRequired$P,
    VOTMyAccount: VOTMyAccount$P,
    VOTLogin: VOTLogin$P,
    VOTLogout: VOTLogout$P,
    VOTRefresh: VOTRefresh$P,
    VOTYandexToken: VOTYandexToken$P,
    VOTYandexTokenInfo: VOTYandexTokenInfo$P,
    VOTLoginViaToken: VOTLoginViaToken$P,
    subtitlesSmartLayout: subtitlesSmartLayout$P,
    smartDucking: smartDucking$P,
    VOTAutoSubtitles: VOTAutoSubtitles$P,
    VOTSubtitlesFont: VOTSubtitlesFont$P
  };
  const recommended$O = "es wird empfohlen";
  const translateVideo$O = "Video übersetzen";
  const disableTranslate$O = "Ausschalten";
  const translationSettings$O = "Übersetzungseinstellungen";
  const subtitlesSettings$O = "Untertiteleinstellungen";
  const resetSettings$O = "Einstellungen zurücksetzen";
  const videoBeingTranslated$O = "Das Video wird übersetzt";
  const videoLanguage$O = "Sprache Video";
  const translationLanguage$O = "Zielsprache";
  const translationTake$O = "Die Übersetzung dauert";
  const translationTakeMoreThanHour$O = "Die Übersetzung dauert mehr als eine Stunde";
  const translationTakeAboutMinute$O = "Die Übersetzung dauert ungefähr eine Minute";
  const translationTakeFewMinutes$O = "Die Übersetzung dauert einige Minuten";
  const translationTakeApproximatelyMinutes$O = "Die Übersetzung dauert ungefähr {0} Minuten";
  const translationTakeApproximatelyMinute$O = "Die Übersetzung dauert ungefähr {0} Minuten";
  const requestTranslationFailed$O = "Videoübersetzung konnte nicht angefordert werden";
  const audioNotReceived$O = "Audiolink nicht empfangen";
  const audioFormatNotSupported$O = "Das Audioformat wird nicht unterstützt";
  const VOTAutoTranslate$O = "Beim Öffnen übersetzen";
  const VOTDontTranslateYourLang$O = "Nicht aus Ihrer Sprache übersetzen";
  const VOTVolume$O = "Videolautstärke:";
  const VOTVolumeTranslation$O = "Übersetzungsvolumen:";
  const VOTAutoSetVolume$O = "Reduzieren Sie die Videolautstärke auf";
  const VOTShowVideoSlider$O = "Video-Lautstärkeregler";
  const VOTSyncVolume$O = "Übersetzungs- und Videolautstärke verknüpfen";
  const VOTDisableFromYourLang$O = "Sie haben die Übersetzung des Videos in Ihrer Sprache deaktiviert";
  const VOTVideoIsTooLong$O = "Video ist zu lang";
  const VOTNoVideoIDFound$O = "Keine Video-ID gefunden";
  const VOTSubtitles$O = "Untertitel";
  const VOTSubtitlesDisabled$O = "Deaktiviert";
  const VOTSubtitlesMaxLength$O = "Untertitel maximale Länge";
  const VOTHighlightWords$O = "Markieren Sie Wörter";
  const VOTTranslatedFrom$O = "übersetzt aus";
  const VOTAutogenerated$O = "automatisch generiert";
  const VOTSettings$O = "VOT Einstellungen";
  const VOTMenuLanguage$O = "Menüsprache";
  const VOTAuthors$O = "Autoren";
  const VOTVersion$O = "Ausführung";
  const VOTLoader$O = "Lader";
  const VOTBrowser$O = "Browser";
  const VOTShowPiPButton$O = "PiP-Schaltfläche anzeigen";
  const langs$O = { "auto": "Automatisch", "af": "Afrikaans", "ak": "Akan", "sq": "Albanisch", "am": "Amharisch", "ar": "Arabisch", "hy": "Armenisch", "az": "Aserbaidschanisch", "as": "Assamesisch", "ay": "Aymara", "eu": "Baskisch", "be": "Belarussisch", "bn": "Bengalisch", "bho": "Bhodschpuri", "my": "Birmanisch", "bs": "Bosnisch", "bg": "Bulgarisch", "ceb": "Cebuano", "zh": "Chinesisch", "zh-Hant": "Chinesisch (Traditionell)", "zh-Hans": "Chinesisch (Vereinfacht)", "da": "Dänisch", "de": "Deutsch", "dv": "Dhivehi", "en": "Englisch", "eo": "Esperanto", "et": "Estnisch", "ee": "Ewe", "fil": "Filipino", "fi": "Finnisch", "fr": "Französisch", "gl": "Galicisch", "gd": "Gälisch (Schottland)", "lg": "Ganda", "ka": "Georgisch", "el": "Griechisch", "gn": "Guaraní", "gu": "Gujarati", "ht": "Haiti-Kreolisch", "ha": "Haussa", "haw": "Hawaiisch", "iw": "Hebräisch", "hi": "Hindi", "ig": "Igbo", "id": "Indonesisch", "ga": "Irisch", "is": "Isländisch", "it": "Italienisch", "ja": "Japanisch", "jv": "Javanisch", "yi": "Jiddisch", "kn": "Kannada", "kk": "Kasachisch", "ca": "Katalanisch", "km": "Khmer", "rw": "Kinyarwanda", "ky": "Kirgisisch", "ko": "Koreanisch", "co": "Korsisch", "kri": "Krio", "hr": "Kroatisch", "ku": "Kurdisch", "lo": "Laotisch", "la": "Latein", "lv": "Lettisch", "ln": "Lingala", "lt": "Litauisch", "lb": "Luxemburgisch", "mg": "Malagasy", "ms": "Malaiisch", "ml": "Malayalam", "mt": "Maltesisch", "mi": "Māori", "mr": "Marathi", "mk": "Mazedonisch", "hmn": "Miao", "mn": "Mongolisch", "ne": "Nepalesisch", "nl": "Niederländisch", "nso": "Nord-Sotho", "no": "Norwegisch", "ny": "Nyanja", "or": "Oriya", "om": "Oromo", "ps": "Paschtu", "fa": "Persisch", "pl": "Polnisch", "pt": "Portugiesisch", "pa": "Punjabi", "qu": "Quechua", "ro": "Rumänisch", "ru": "Russisch", "sm": "Samoanisch", "sa": "Sanskrit", "sv": "Schwedisch", "sr": "Serbisch", "sn": "Shona", "sd": "Sindhi", "si": "Singhalesisch", "sk": "Slowakisch", "sl": "Slowenisch", "so": "Somali", "es": "Spanisch", "sw": "Suaheli", "st": "Süd-Sotho", "su": "Sundanesisch", "tg": "Tadschikisch", "ta": "Tamil", "tt": "Tatarisch", "te": "Telugu", "th": "Thailändisch", "ti": "Tigrinya", "cs": "Tschechisch", "ts": "Tsonga", "tr": "Türkisch", "tk": "Turkmenisch", "ug": "Uigurisch", "uk": "Ukrainisch", "hu": "Ungarisch", "ur": "Urdu", "uz": "Usbekisch", "vi": "Vietnamesisch", "cy": "Walisisch", "fy": "Westfriesisch", "xh": "Xhosa", "yo": "Yoruba", "zu": "Zulu" };
  const streamNoConnectionToServer$O = "Es besteht keine Verbindung zum Server";
  const searchField$O = "Suche...";
  const VOTTranslateAPIErrors$O = "Fehler aus der API übersetzen";
  const VOTDetectService$O = "Spracherkennungsdienst";
  const VOTProxyWorkerHost$O = "Geben Sie die Proxy-Worker-Adresse ein";
  const VOTM3u8ProxyHost$O = "Geben Sie die Adresse des m3u8-Proxy-Mitarbeiters ein";
  const proxySettings$O = "Proxyeinstellungen";
  const translationTakeApproximatelyMinute2$O = "Die Übersetzung dauert ungefähr {0} Minuten";
  const VOTAudioBooster$O = "Erhöhung des erweiterten Übersetzungsvolumens";
  const VOTSubtitlesDesign$O = "Untertitel-Design";
  const VOTSubtitlesFontSize$O = "Schriftgröße der Untertitel";
  const VOTSubtitlesOpacity$O = "Transparenz des Untertitelhintergrunds";
  const VOTSubtitlesDownloadFormat$O = "Das Format zum Herunterladen von Untertiteln";
  const VOTDownloadWithName$O = "Laden Sie Dateien mit dem Videonamen herunter";
  const VOTUpdateLocaleFiles$O = "Lokalisierungsdateien aktualisieren";
  const VOTLocaleHash$O = "Gebietsschema-Hash";
  const VOTUpdatedAt$O = "Aktualisiert um";
  const VOTNeedWebAudioAPI$O = "Um dies zu aktivieren, benötigen Sie eine Web-Audio-API";
  const VOTMediaCSPEnabledOnSite$O = "Medien-CSP ist auf dieser Site aktiviert";
  const VOTOnlyBypassMediaCSP$O = "Verwenden Sie es nur zum Umgehen von Medien-CSP";
  const VOTNewAudioPlayer$O = "Verwenden Sie den neuen Audioplayer";
  const VOTUseNewModel$O = "Verwenden Sie für einige Videos eine experimentelle Variante von Yandex-Stimmen";
  const TranslationDelayed$O = "Die Übersetzung ist etwas verzögert";
  const VOTTranslationCompletedNotify$O = "Die Übersetzung auf der {0} ist abgeschlossen!";
  const VOTSendNotifyOnComplete$O = "Senden Sie eine Benachrichtigung, dass das Video übersetzt wurde";
  const VOTBugReport$O = "Einen Fehler melden";
  const VOTTranslateProxyDisabled$O = "Behinderte";
  const VOTTranslateProxyEnabled$O = "Aktiviert";
  const VOTTranslateProxyEverything$O = "Proxy alles";
  const VOTTranslateProxyStatus$O = "Proxy-Modus";
  const VOTTranslatedBy$O = "Übersetzt von {0}";
  const VOTStreamNotAvailable$O = "Übersetzungs-Stream ist nicht verfügbar";
  const VOTTranslationTextService$O = "Textübersetzungsdienst";
  const VOTNotAffectToVoice$O = "Hat keinen Einfluss auf die Übersetzung von Text im Voice-Over";
  const DontTranslateSelectedLanguages$O = "Nicht aus ausgewählten Sprachen übersetzen";
  const showVideoVolumeSlider$O = "Zeigen Sie den Schieberegler für die Videolautstärke an";
  const hotkeysSettings$O = "Hotkeys-Einstellungen";
  const None$O = "Kein";
  const VOTUseLivelyVoice$O = "Verwenden Sie lebhafte Stimmen. Lautsprecher klingen wie einheimische Russen.";
  const miscSettings$O = "Sonstige Einstellungen";
  const services$O = { "yandexbrowser": "Yandex Browser", "msedge": "Microsoft Rand", "rust-server": "Rust-Server" };
  const aboutExtension$O = "Über die Erweiterung";
  const appearance$O = "Aussehen";
  const buttonPosition$O = "Tastenposition im Player";
  const position$O = { "left": "Links", "right": "Recht", "top": "Oberen", "default": "Standard" };
  const secs$O = "sekunden";
  const autoHideButtonDelay$O = "Verzögerung vor dem Ausblenden der Übersetzungstaste";
  const notFound$O = "nicht gefunden";
  const minButtonPositionContainer$O = "Die Tastenposition ändert sich nur bei Playern, die größer als 600 Pixel sind.";
  const VOTTranslateProxyStatusDefault$O = "Das vollständige Deaktivieren des Proxy in Ihrem Land kann die Erweiterung beschädigen";
  const PressTheKeyCombination$O = "Drücken Sie die Tastenkombination...";
  const VOTUseAudioDownload$O = "Audio-Download verwenden";
  const VOTUseAudioDownloadWarning$O = "Das Deaktivieren von Audio-Downloads kann die Funktionalität der Erweiterung beeinträchtigen";
  const VOTAccountRequired$O = "Sie müssen sich anmelden, um diese Funktion nutzen zu können";
  const VOTMyAccount$O = "Mein Konto";
  const VOTLogin$O = "Login";
  const VOTLogout$O = "Abmelden";
  const VOTRefresh$O = "Aktualisieren";
  const VOTYandexToken$O = "Geben Sie das Yandex OAuth-Token ein";
  const VOTYandexTokenInfo$O = "In diesem Feld können Sie das Konto-Token manuell festlegen. Bitte beachten Sie, dass wir die Gültigkeit nicht überprüfen, bevor wir eine Übersetzungsanfrage senden";
  const VOTLoginViaToken$O = "Anmeldung über Token";
  const subtitlesSmartLayout$O = "Intelligentes Untertitel-Layout";
  const smartDucking$O = "Adaptives Volumen";
  const VOTAutoSubtitles$O = "Untertitel auf open";
  const VOTSubtitlesFont$O = "Schriftart für Untertitel";
  const __vite_glob_0_11 = {
    recommended: recommended$O,
    translateVideo: translateVideo$O,
    disableTranslate: disableTranslate$O,
    translationSettings: translationSettings$O,
    subtitlesSettings: subtitlesSettings$O,
    resetSettings: resetSettings$O,
    videoBeingTranslated: videoBeingTranslated$O,
    videoLanguage: videoLanguage$O,
    translationLanguage: translationLanguage$O,
    translationTake: translationTake$O,
    translationTakeMoreThanHour: translationTakeMoreThanHour$O,
    translationTakeAboutMinute: translationTakeAboutMinute$O,
    translationTakeFewMinutes: translationTakeFewMinutes$O,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$O,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$O,
    requestTranslationFailed: requestTranslationFailed$O,
    audioNotReceived: audioNotReceived$O,
    audioFormatNotSupported: audioFormatNotSupported$O,
    VOTAutoTranslate: VOTAutoTranslate$O,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$O,
    VOTVolume: VOTVolume$O,
    VOTVolumeTranslation: VOTVolumeTranslation$O,
    VOTAutoSetVolume: VOTAutoSetVolume$O,
    VOTShowVideoSlider: VOTShowVideoSlider$O,
    VOTSyncVolume: VOTSyncVolume$O,
    VOTDisableFromYourLang: VOTDisableFromYourLang$O,
    VOTVideoIsTooLong: VOTVideoIsTooLong$O,
    VOTNoVideoIDFound: VOTNoVideoIDFound$O,
    VOTSubtitles: VOTSubtitles$O,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$O,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$O,
    VOTHighlightWords: VOTHighlightWords$O,
    VOTTranslatedFrom: VOTTranslatedFrom$O,
    VOTAutogenerated: VOTAutogenerated$O,
    VOTSettings: VOTSettings$O,
    VOTMenuLanguage: VOTMenuLanguage$O,
    VOTAuthors: VOTAuthors$O,
    VOTVersion: VOTVersion$O,
    VOTLoader: VOTLoader$O,
    VOTBrowser: VOTBrowser$O,
    VOTShowPiPButton: VOTShowPiPButton$O,
    langs: langs$O,
    streamNoConnectionToServer: streamNoConnectionToServer$O,
    searchField: searchField$O,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$O,
    VOTDetectService: VOTDetectService$O,
    VOTProxyWorkerHost: VOTProxyWorkerHost$O,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$O,
    proxySettings: proxySettings$O,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$O,
    VOTAudioBooster: VOTAudioBooster$O,
    VOTSubtitlesDesign: VOTSubtitlesDesign$O,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$O,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$O,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$O,
    VOTDownloadWithName: VOTDownloadWithName$O,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$O,
    VOTLocaleHash: VOTLocaleHash$O,
    VOTUpdatedAt: VOTUpdatedAt$O,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$O,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$O,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$O,
    VOTNewAudioPlayer: VOTNewAudioPlayer$O,
    VOTUseNewModel: VOTUseNewModel$O,
    TranslationDelayed: TranslationDelayed$O,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$O,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$O,
    VOTBugReport: VOTBugReport$O,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$O,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$O,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$O,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$O,
    VOTTranslatedBy: VOTTranslatedBy$O,
    VOTStreamNotAvailable: VOTStreamNotAvailable$O,
    VOTTranslationTextService: VOTTranslationTextService$O,
    VOTNotAffectToVoice: VOTNotAffectToVoice$O,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$O,
    showVideoVolumeSlider: showVideoVolumeSlider$O,
    hotkeysSettings: hotkeysSettings$O,
    None: None$O,
    VOTUseLivelyVoice: VOTUseLivelyVoice$O,
    miscSettings: miscSettings$O,
    services: services$O,
    aboutExtension: aboutExtension$O,
    appearance: appearance$O,
    buttonPosition: buttonPosition$O,
    position: position$O,
    secs: secs$O,
    autoHideButtonDelay: autoHideButtonDelay$O,
    notFound: notFound$O,
    minButtonPositionContainer: minButtonPositionContainer$O,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$O,
    PressTheKeyCombination: PressTheKeyCombination$O,
    VOTUseAudioDownload: VOTUseAudioDownload$O,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$O,
    VOTAccountRequired: VOTAccountRequired$O,
    VOTMyAccount: VOTMyAccount$O,
    VOTLogin: VOTLogin$O,
    VOTLogout: VOTLogout$O,
    VOTRefresh: VOTRefresh$O,
    VOTYandexToken: VOTYandexToken$O,
    VOTYandexTokenInfo: VOTYandexTokenInfo$O,
    VOTLoginViaToken: VOTLoginViaToken$O,
    subtitlesSmartLayout: subtitlesSmartLayout$O,
    smartDucking: smartDucking$O,
    VOTAutoSubtitles: VOTAutoSubtitles$O,
    VOTSubtitlesFont: VOTSubtitlesFont$O
  };
  const recommended$N = "προτεινόμενη";
  const translateVideo$N = "Μετάφραση βίντεο";
  const disableTranslate$N = "Απενεργοποιήσετε";
  const translationSettings$N = "Ρυθμίσεις μετάφρασης";
  const subtitlesSettings$N = "Ρυθμίσεις υπότιτλων";
  const resetSettings$N = "Επαναφορά ρυθμίσεων";
  const videoBeingTranslated$N = "Το βίντεο μεταφράζεται";
  const videoLanguage$N = "Γλώσσα βίντεο";
  const translationLanguage$N = "Γλώσσα μετάφρασης";
  const translationTake$N = "Η μετάφραση θα λάβει";
  const translationTakeMoreThanHour$N = "Η μετάφραση θα διαρκέσει περισσότερο από μία ώρα";
  const translationTakeAboutMinute$N = "Η μετάφραση θα διαρκέσει περίπου ένα λεπτό";
  const translationTakeFewMinutes$N = "Η μετάφραση θα διαρκέσει λίγα λεπτά";
  const translationTakeApproximatelyMinutes$N = "Η μετάφραση θα διαρκέσει περίπου {0} λεπτά";
  const translationTakeApproximatelyMinute$N = "Η μετάφραση θα διαρκέσει περίπου {0} λεπτά";
  const requestTranslationFailed$N = "Αποτυχία αίτησης μετάφρασης βίντεο";
  const audioNotReceived$N = "Δεν έχει ληφθεί σύνδεσμος ήχου";
  const audioFormatNotSupported$N = "Η μορφή ήχου δεν υποστηρίζεται";
  const VOTAutoTranslate$N = "Μετάφραση σε ανοιχτό";
  const VOTDontTranslateYourLang$N = "Μην μεταφράζετε από τη γλώσσα μου";
  const VOTVolume$N = "Όγκος βίντεο:";
  const VOTVolumeTranslation$N = "Τόμος μετάφρασης:";
  const VOTAutoSetVolume$N = "Μειώστε την ένταση του βίντεο σε";
  const VOTShowVideoSlider$N = "Ρυθμιστικό έντασης βίντεο";
  const VOTSyncVolume$N = "Μετάφραση συνδέσμου και όγκος βίντεο";
  const VOTDisableFromYourLang$N = "Έχετε απενεργοποιήσει τη μετάφραση του βίντεο στη γλώσσα σας";
  const VOTVideoIsTooLong$N = "Το βίντεο είναι πολύ μεγάλο";
  const VOTNoVideoIDFound$N = "Δεν βρέθηκε αναγνωριστικό βίντεο";
  const VOTSubtitles$N = "Υπότιτλοι";
  const VOTSubtitlesDisabled$N = "Απενεργοποιημένη";
  const VOTSubtitlesMaxLength$N = "Υπότιτλοι ανώτατο μήκος";
  const VOTHighlightWords$N = "Επισημάνετε λέξεις";
  const VOTTranslatedFrom$N = "μεταφρασμένο από";
  const VOTAutogenerated$N = "αυτογεννημένο";
  const VOTSettings$N = "Ρυθμίσεις VOT";
  const VOTMenuLanguage$N = "Γλώσσα μενού";
  const VOTAuthors$N = "Συγγραφέας";
  const VOTVersion$N = "Έκδοση";
  const VOTLoader$N = "Φορτωτής";
  const VOTBrowser$N = "Περιήγησης";
  const VOTShowPiPButton$N = "Εμφάνιση κουμπιού PiP";
  const langs$N = { "auto": "Αυτόματη", "af": "Ελληνικά", "ak": "Ακάν", "sq": "Αλβανική", "am": "Αμχαρικά", "ar": "Αραβική", "hy": "Αρμενίων", "as": "Ασαμικά", "ay": "Αϊμάρα", "az": "Αζερμπαϊτζάν", "bn": "Μπανγκλά", "eu": "Βάσκων", "be": "Λευκορωσία", "bho": "Μποτζπούρι", "bs": "Βοσνία", "bg": "Βουλγαρικός", "my": "Βιρμανίας", "ca": "Καταλονίας", "ceb": "Σεμπούανο", "zh": "Κινέζικος", "zh-Hans": "Κινέζικα (Απλοποιημένα)", "zh-Hant": "Κινέζικα (Παραδοσιακά)", "co": "Κορσικής", "hr": "Κροατική", "cs": "Τσεχική", "da": "Δανική", "dv": "Ντιβέχι", "nl": "Ολλανδική", "en": "Αγγλική", "eo": "Εσπεράντο", "et": "Εσθονική", "ee": "Προβατίνα", "fil": "Φιλιππινέζικο", "fi": "Φινλανδική", "fr": "Γαλλική", "gl": "Γαλικίας", "lg": "Γκάντα", "ka": "Γεωργιανός", "de": "Γερμανική", "el": "Ελληνική", "gn": "Γκουαρανί", "gu": "Γκουτζαράτι", "ht": "Κρεολικά Της Αϊτής", "ha": "Χάουσα", "haw": "Χαβάης", "iw": "Εβραϊκή", "hi": "Ινδική", "hmn": "Χμόνγκ", "hu": "Ουγγρική", "is": "Ισλανδική", "ig": "Ίγκμπο", "id": "Ινδονησιακά", "ga": "Ιρλανδική", "it": "Ιταλική", "ja": "Ιαπωνική", "jv": "Ιάβας", "kn": "Κανάντα", "kk": "Καζακστάν", "km": "Χμερ", "rw": "Κινιάρβαντα", "ko": "Κορέα", "kri": "Κρίο", "ku": "Κουρδικά", "ky": "Κιργιζίας", "lo": "Λάος", "la": "Λατινικό", "lv": "Λετονική", "ln": "Λινγκάλα", "lt": "Λιθουανική", "lb": "Λουξεμβούργο", "mk": "Μακεδονική", "mg": "Μαδαγασκάρη", "ms": "Μαλαισιανή", "ml": "Μαλαγιαλαμικά", "mt": "Μάλτα", "mi": "Μαορί", "mr": "Μαράθι", "mn": "Μογγολική", "ne": "Νεπάλ", "nso": "Βόρειος Σότο", "no": "Νορβηγική", "ny": "Νιάντζα", "or": "Οδία", "om": "Ορόμο", "ps": "Παστού", "fa": "Περσική", "pl": "Πολωνική", "pt": "Πορτογαλική", "pa": "Πουντζάμπι", "qu": "Κετσουά", "ro": "Ρουμάνος", "ru": "Ρωσική", "sm": "Σαμόα", "sa": "Σανσκριτική", "gd": "Σκωτική Γαελική", "sr": "Σέρβος", "sn": "Σόνα", "sd": "Σίντι", "si": "Σινχάλα", "sk": "Σλοβακική", "sl": "Σλοβενική", "so": "Σομαλία", "st": "Νότια Σότο", "es": "Ισπανικά", "su": "Σουντανέζικα", "sw": "Σουαχίλι", "sv": "Σουηδός", "tg": "Τατζικιστάν", "ta": "Ταμίλ", "tt": "Τατάρ", "te": "Τελουγκού", "th": "Ταϊλανδέζικο", "ti": "Τιγκρίνια", "ts": "Τσόνγκα", "tr": "Τουρκική", "tk": "Τουρκμενικά", "uk": "Ουκρανική", "ur": "Ουρντού", "ug": "Ουιγούρων", "uz": "Ουζμπεκιστάν", "vi": "Βιετνάμ", "cy": "Ελληνικά", "fy": "Δυτική Φριζική", "xh": "Χόσα", "yi": "Γίντις", "yo": "Γιορούμπα", "zu": "Ζουλού" };
  const streamNoConnectionToServer$N = "Δεν υπάρχει σύνδεση με το διακομιστή";
  const searchField$N = "Αναζήτηση...";
  const VOTTranslateAPIErrors$N = "Μετάφραση σφαλμάτων από το API";
  const VOTDetectService$N = "Υπηρεσία ανίχνευσης γλωσσών";
  const VOTProxyWorkerHost$N = "Εισαγάγετε τη διεύθυνση διακομιστή μεσολάβησης";
  const VOTM3u8ProxyHost$N = "Εισαγάγετε τη διεύθυνση του διακομιστή μεσολάβησης m3u8";
  const proxySettings$N = "Ρυθμίσεις Διακομιστή Μεσολάβησης";
  const translationTakeApproximatelyMinute2$N = "Η μετάφραση θα διαρκέσει περίπου {0} λεπτά";
  const VOTAudioBooster$N = "Εκτεταμένη αύξηση όγκου μετάφρασης";
  const VOTSubtitlesDesign$N = "Σχεδιασμός υπότιτλων";
  const VOTSubtitlesFontSize$N = "Μέγεθος γραμματοσειράς υπότιτλων";
  const VOTSubtitlesOpacity$N = "Διαφάνεια του φόντου υποτίτλων";
  const VOTSubtitlesDownloadFormat$N = "Η μορφή λήψης υπότιτλων";
  const VOTDownloadWithName$N = "Λήψη αρχείων με το όνομα του βίντεο";
  const VOTUpdateLocaleFiles$N = "Ενημέρωση αρχείων εντοπισμού";
  const VOTLocaleHash$N = "Τοπικό χασίς";
  const VOTUpdatedAt$N = "Ενημερώθηκε στις";
  const VOTNeedWebAudioAPI$N = "Για να το ενεργοποιήσετε, πρέπει να έχετε ένα API Web Audio";
  const VOTMediaCSPEnabledOnSite$N = "Το Media CSP είναι ενεργοποιημένο σε αυτόν τον ιστότοπο";
  const VOTOnlyBypassMediaCSP$N = "Χρησιμοποιήστε το μόνο για παράκαμψη CSP πολυμέσων";
  const VOTNewAudioPlayer$N = "Χρησιμοποιήστε τη νέα συσκευή αναπαραγωγής ήχου";
  const VOTUseNewModel$N = "Χρησιμοποιήστε μια πειραματική παραλλαγή των φωνών Yandex για ορισμένα βίντεο";
  const TranslationDelayed$N = "Η μετάφραση καθυστερεί ελαφρώς";
  const VOTTranslationCompletedNotify$N = "Η μετάφραση στο {0} έχει ολοκληρωθεί!";
  const VOTSendNotifyOnComplete$N = "Στείλτε μια ειδοποίηση ότι το βίντεο έχει μεταφραστεί";
  const VOTBugReport$N = "Αναφορά σφάλματος";
  const VOTTranslateProxyDisabled$N = "Απενεργοποιημένη";
  const VOTTranslateProxyEnabled$N = "Ενεργοποιήσετε";
  const VOTTranslateProxyEverything$N = "Πληρεξούσιο τα πάντα";
  const VOTTranslateProxyStatus$N = "Λειτουργία μεσολάβησης";
  const VOTTranslatedBy$N = "Μετάφραση {0}";
  const VOTStreamNotAvailable$N = "Η ροή μετάφρασης δεν είναι διαθέσιμη";
  const VOTTranslationTextService$N = "Υπηρεσία μετάφρασης κειμένου";
  const VOTNotAffectToVoice$N = "Δεν επηρεάζει τη μετάφραση κειμένου σε φωνή";
  const DontTranslateSelectedLanguages$N = "Μην μεταφράζετε από επιλεγμένες γλώσσες";
  const showVideoVolumeSlider$N = "Εμφάνιση του Ρυθμιστικού έντασης βίντεο";
  const hotkeysSettings$N = "Ρυθμίσεις πλήκτρων πρόσβασης";
  const None$N = "Καμία";
  const VOTUseLivelyVoice$N = "Χρησιμοποιήστε ζωντανές φωνές. Οι ομιλητές ακούγονται σαν γηγενείς Ρωσική.";
  const miscSettings$N = "Διάφορες ρυθμίσεις";
  const services$N = { "yandexbrowser": "Yandex Browser", "msedge": "Microsoft Edge", "rust-server": "Διακομιστής Σκουριάς" };
  const aboutExtension$N = "Σχετικά με την επέκταση";
  const appearance$N = "Εμφάνιση";
  const buttonPosition$N = "Θέση κουμπιού στη συσκευή αναπαραγωγής";
  const position$N = { "left": "Αριστερά", "right": "Δικαίωμα", "top": "Κορυφή", "default": "Προεπιλεγμένη" };
  const secs$N = "δευτ.";
  const autoHideButtonDelay$N = "Καθυστέρηση πριν από την απόκρυψη του κουμπιού μετάφρασης";
  const notFound$N = "δεν βρέθηκε";
  const minButtonPositionContainer$N = "Η θέση του κουμπιού αλλάζει μόνο σε παίκτες μεγαλύτερους από 600 εικονοστοιχεία.";
  const VOTTranslateProxyStatusDefault$N = "Η πλήρης απενεργοποίηση του διακομιστή μεσολάβησης στη χώρα σας ενδέχεται να σπάσει την επέκταση";
  const PressTheKeyCombination$N = "Πατήστε το συνδυασμό πλήκτρων...";
  const VOTUseAudioDownload$N = "Χρήση λήψης ήχου";
  const VOTUseAudioDownloadWarning$N = "Η απενεργοποίηση των λήψεων ήχου ενδέχεται να επηρεάσει τη λειτουργικότητα της επέκτασης";
  const VOTAccountRequired$N = "Πρέπει να συνδεθείτε για να χρησιμοποιήσετε αυτήν τη δυνατότητα";
  const VOTMyAccount$N = "Ο λογαριασμός μου";
  const VOTLogin$N = "Σύνδεση";
  const VOTLogout$N = "Αποσύνδεση";
  const VOTRefresh$N = "Ανανέωση";
  const VOTYandexToken$N = "Εισαγάγετε το διακριτικό Yandex OAuth";
  const VOTYandexTokenInfo$N = "Μπορείτε να ορίσετε χειροκίνητα το διακριτικό λογαριασμού σε αυτό το πεδίο. Λάβετε υπόψη ότι δεν ελέγχουμε την εγκυρότητά του πριν στείλουμε ένα αίτημα μετάφρασης";
  const VOTLoginViaToken$N = "Σύνδεση μέσω διακριτικού";
  const subtitlesSmartLayout$N = "Έξυπνη διάταξη υποτίτλων";
  const smartDucking$N = "Προσαρμοστικός όγκος";
  const VOTAutoSubtitles$N = "Υπότιτλοι σε ανοιχτό";
  const VOTSubtitlesFont$N = "Γραμματοσειρά υποτίτλων";
  const __vite_glob_0_12 = {
    recommended: recommended$N,
    translateVideo: translateVideo$N,
    disableTranslate: disableTranslate$N,
    translationSettings: translationSettings$N,
    subtitlesSettings: subtitlesSettings$N,
    resetSettings: resetSettings$N,
    videoBeingTranslated: videoBeingTranslated$N,
    videoLanguage: videoLanguage$N,
    translationLanguage: translationLanguage$N,
    translationTake: translationTake$N,
    translationTakeMoreThanHour: translationTakeMoreThanHour$N,
    translationTakeAboutMinute: translationTakeAboutMinute$N,
    translationTakeFewMinutes: translationTakeFewMinutes$N,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$N,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$N,
    requestTranslationFailed: requestTranslationFailed$N,
    audioNotReceived: audioNotReceived$N,
    audioFormatNotSupported: audioFormatNotSupported$N,
    VOTAutoTranslate: VOTAutoTranslate$N,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$N,
    VOTVolume: VOTVolume$N,
    VOTVolumeTranslation: VOTVolumeTranslation$N,
    VOTAutoSetVolume: VOTAutoSetVolume$N,
    VOTShowVideoSlider: VOTShowVideoSlider$N,
    VOTSyncVolume: VOTSyncVolume$N,
    VOTDisableFromYourLang: VOTDisableFromYourLang$N,
    VOTVideoIsTooLong: VOTVideoIsTooLong$N,
    VOTNoVideoIDFound: VOTNoVideoIDFound$N,
    VOTSubtitles: VOTSubtitles$N,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$N,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$N,
    VOTHighlightWords: VOTHighlightWords$N,
    VOTTranslatedFrom: VOTTranslatedFrom$N,
    VOTAutogenerated: VOTAutogenerated$N,
    VOTSettings: VOTSettings$N,
    VOTMenuLanguage: VOTMenuLanguage$N,
    VOTAuthors: VOTAuthors$N,
    VOTVersion: VOTVersion$N,
    VOTLoader: VOTLoader$N,
    VOTBrowser: VOTBrowser$N,
    VOTShowPiPButton: VOTShowPiPButton$N,
    langs: langs$N,
    streamNoConnectionToServer: streamNoConnectionToServer$N,
    searchField: searchField$N,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$N,
    VOTDetectService: VOTDetectService$N,
    VOTProxyWorkerHost: VOTProxyWorkerHost$N,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$N,
    proxySettings: proxySettings$N,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$N,
    VOTAudioBooster: VOTAudioBooster$N,
    VOTSubtitlesDesign: VOTSubtitlesDesign$N,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$N,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$N,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$N,
    VOTDownloadWithName: VOTDownloadWithName$N,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$N,
    VOTLocaleHash: VOTLocaleHash$N,
    VOTUpdatedAt: VOTUpdatedAt$N,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$N,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$N,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$N,
    VOTNewAudioPlayer: VOTNewAudioPlayer$N,
    VOTUseNewModel: VOTUseNewModel$N,
    TranslationDelayed: TranslationDelayed$N,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$N,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$N,
    VOTBugReport: VOTBugReport$N,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$N,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$N,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$N,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$N,
    VOTTranslatedBy: VOTTranslatedBy$N,
    VOTStreamNotAvailable: VOTStreamNotAvailable$N,
    VOTTranslationTextService: VOTTranslationTextService$N,
    VOTNotAffectToVoice: VOTNotAffectToVoice$N,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$N,
    showVideoVolumeSlider: showVideoVolumeSlider$N,
    hotkeysSettings: hotkeysSettings$N,
    None: None$N,
    VOTUseLivelyVoice: VOTUseLivelyVoice$N,
    miscSettings: miscSettings$N,
    services: services$N,
    aboutExtension: aboutExtension$N,
    appearance: appearance$N,
    buttonPosition: buttonPosition$N,
    position: position$N,
    secs: secs$N,
    autoHideButtonDelay: autoHideButtonDelay$N,
    notFound: notFound$N,
    minButtonPositionContainer: minButtonPositionContainer$N,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$N,
    PressTheKeyCombination: PressTheKeyCombination$N,
    VOTUseAudioDownload: VOTUseAudioDownload$N,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$N,
    VOTAccountRequired: VOTAccountRequired$N,
    VOTMyAccount: VOTMyAccount$N,
    VOTLogin: VOTLogin$N,
    VOTLogout: VOTLogout$N,
    VOTRefresh: VOTRefresh$N,
    VOTYandexToken: VOTYandexToken$N,
    VOTYandexTokenInfo: VOTYandexTokenInfo$N,
    VOTLoginViaToken: VOTLoginViaToken$N,
    subtitlesSmartLayout: subtitlesSmartLayout$N,
    smartDucking: smartDucking$N,
    VOTAutoSubtitles: VOTAutoSubtitles$N,
    VOTSubtitlesFont: VOTSubtitlesFont$N
  };
  const recommended$M = "recommended";
  const translateVideo$M = "Translate video";
  const disableTranslate$M = "Turn off";
  const translationSettings$M = "Translation settings";
  const subtitlesSettings$M = "Subtitles settings";
  const subtitlesSmartLayout$M = "Smart subtitle layout";
  const resetSettings$M = "Reset settings";
  const videoBeingTranslated$M = "The video is being translated";
  const videoLanguage$M = "Video language";
  const translationLanguage$M = "Translation language";
  const translationTake$M = "The translation will take";
  const translationTakeMoreThanHour$M = "The translation will take more than an hour";
  const translationTakeAboutMinute$M = "The translation will take about a minute";
  const translationTakeFewMinutes$M = "The translation will take a few minutes";
  const translationTakeApproximatelyMinutes$M = "The translation will take approximately {0} minutes";
  const translationTakeApproximatelyMinute$M = "The translation will take approximately {0} minutes";
  const requestTranslationFailed$M = "Failed to request video translation";
  const audioNotReceived$M = "Audio link not received";
  const VOTFailedDownloadAudio$1 = "Failed to download audio";
  const audioFormatNotSupported$M = "The audio format is not supported";
  const VOTStartVideoForTranslation$1 = "Start the video to translate";
  const VOTAudioNotDetected$1 = "No audio detected";
  const VOTAudioNotYetAvailable$1 = "Audio is not available yet";
  const VOTAutoTranslate$M = "Translate on open";
  const VOTAutoSubtitles$M = "Subtitles on open";
  const VOTDontTranslateYourLang$M = "Don't translate from my language";
  const VOTVolume$M = "Video volume:";
  const VOTVolumeTranslation$M = "Translation volume:";
  const VOTAutoSetVolume$M = "Reduce video volume to";
  const VOTShowVideoSlider$M = "Video volume slider";
  const VOTSyncVolume$M = "Link translation and video volume";
  const VOTDisableFromYourLang$M = "You have disabled the translation of the video in your language";
  const VOTVideoIsTooLong$M = "Video is too long";
  const VOTNoVideoIDFound$M = "No video ID found";
  const VOTSubtitles$M = "Subtitles";
  const VOTSubtitlesDisabled$M = "Disabled";
  const VOTSubtitlesMaxLength$M = "Subtitles max length";
  const VOTHighlightWords$M = "Highlight words";
  const VOTTranslatedFrom$M = "translated from";
  const VOTAutogenerated$M = "autogenerated";
  const VOTSettings$M = "VOT Settings";
  const VOTMenuLanguage$M = "Menu language";
  const VOTAuthors$M = "Authors";
  const VOTVersion$M = "Version";
  const VOTLoader$M = "Loader";
  const VOTBrowser$M = "Browser";
  const VOTShowPiPButton$M = "Show PiP button";
  const langs$M = { "auto": "Auto", "af": "Afrikaans", "ak": "Akan", "sq": "Albanian", "am": "Amharic", "ar": "Arabic", "hy": "Armenian", "as": "Assamese", "ay": "Aymara", "az": "Azerbaijani", "bn": "Bangla", "eu": "Basque", "be": "Belarusian", "bho": "Bhojpuri", "bs": "Bosnian", "bg": "Bulgarian", "my": "Burmese", "ca": "Catalan", "ceb": "Cebuano", "zh": "Chinese", "zh-Hans": "Chinese (Simplified)", "zh-Hant": "Chinese (Traditional)", "co": "Corsican", "hr": "Croatian", "cs": "Czech", "da": "Danish", "dv": "Divehi", "nl": "Dutch", "en": "English", "eo": "Esperanto", "et": "Estonian", "ee": "Ewe", "fil": "Filipino", "fi": "Finnish", "fr": "French", "gl": "Galician", "lg": "Ganda", "ka": "Georgian", "de": "German", "el": "Greek", "gn": "Guarani", "gu": "Gujarati", "ht": "Haitian Creole", "ha": "Hausa", "haw": "Hawaiian", "iw": "Hebrew", "hi": "Hindi", "hmn": "Hmong", "hu": "Hungarian", "is": "Icelandic", "ig": "Igbo", "id": "Indonesian", "ga": "Irish", "it": "Italian", "ja": "Japanese", "jv": "Javanese", "kn": "Kannada", "kk": "Kazakh", "km": "Khmer", "rw": "Kinyarwanda", "ko": "Korean", "kri": "Krio", "ku": "Kurdish", "ky": "Kyrgyz", "lo": "Lao", "la": "Latin", "lv": "Latvian", "ln": "Lingala", "lt": "Lithuanian", "lb": "Luxembourgish", "mk": "Macedonian", "mg": "Malagasy", "ms": "Malay", "ml": "Malayalam", "mt": "Maltese", "mi": "Māori", "mr": "Marathi", "mn": "Mongolian", "ne": "Nepali", "nso": "Northern Sotho", "no": "Norwegian", "ny": "Nyanja", "or": "Odia", "om": "Oromo", "ps": "Pashto", "fa": "Persian", "pl": "Polish", "pt": "Portuguese", "pa": "Punjabi", "qu": "Quechua", "ro": "Romanian", "ru": "Russian", "sm": "Samoan", "sa": "Sanskrit", "gd": "Scottish Gaelic", "sr": "Serbian", "sn": "Shona", "sd": "Sindhi", "si": "Sinhala", "sk": "Slovak", "sl": "Slovenian", "so": "Somali", "st": "Southern Sotho", "es": "Spanish", "su": "Sundanese", "sw": "Swahili", "sv": "Swedish", "tg": "Tajik", "ta": "Tamil", "tt": "Tatar", "te": "Telugu", "th": "Thai", "ti": "Tigrinya", "ts": "Tsonga", "tr": "Turkish", "tk": "Turkmen", "uk": "Ukrainian", "ur": "Urdu", "ug": "Uyghur", "uz": "Uzbek", "vi": "Vietnamese", "cy": "Welsh", "fy": "Western Frisian", "xh": "Xhosa", "yi": "Yiddish", "yo": "Yoruba", "zu": "Zulu" };
  const streamNoConnectionToServer$M = "There is no connection to the server";
  const searchField$M = "Search...";
  const VOTTranslateAPIErrors$M = "Translate errors from the API";
  const VOTDetectService$M = "Language detection service";
  const VOTProxyWorkerHost$M = "Enter the proxy worker address";
  const VOTM3u8ProxyHost$M = "Enter the address of the m3u8 proxy worker";
  const proxySettings$M = "Proxy Settings";
  const translationTakeApproximatelyMinute2$M = "The translation will take approximately {0} minutes";
  const VOTAudioBooster$M = "Extended translation volume increase";
  const VOTSubtitlesDesign$M = "Subtitles design";
  const VOTSubtitlesFont$M = "Subtitle font";
  const VOTSubtitlesFontSize$M = "Font size of subtitles";
  const VOTSubtitlesOpacity$M = "Transparency of the subtitle background";
  const VOTSubtitlesDownloadFormat$M = "The format for downloading subtitles";
  const VOTDownloadWithName$M = "Download files with the video name";
  const VOTUpdateLocaleFiles$M = "Update localization files";
  const VOTLocaleHash$M = "Locale hash";
  const VOTUpdatedAt$M = "Updated at";
  const VOTNeedWebAudioAPI$M = "To enable this, you must have a Web Audio API";
  const VOTMediaCSPEnabledOnSite$M = "Media CSP is enabled on this site";
  const VOTOnlyBypassMediaCSP$M = "Use it only for bypassing Media CSP";
  const VOTNewAudioPlayer$M = "Use the new audio player";
  const VOTUseNewModel$M = "Use an experimental variation of Yandex voices for some videos";
  const TranslationDelayed$M = "The translation is slightly delayed";
  const VOTTranslationCompletedNotify$M = "The translation on the {0} has been completed!";
  const VOTSendNotifyOnComplete$M = "Send a notification that the video has been translated";
  const VOTBugReport$M = "Report a bug";
  const VOTTranslateProxyDisabled$M = "Disabled";
  const VOTTranslateProxyEnabled$M = "Enabled";
  const VOTTranslateProxyEverything$M = "Proxy everything";
  const VOTTranslateProxyStatus$M = "Proxying mode";
  const VOTTranslatedBy$M = "Translated by {0}";
  const VOTStreamNotAvailable$M = "Translate stream isn't available";
  const VOTTranslationTextService$M = "Text translation service";
  const VOTNotAffectToVoice$M = "Doesn't affect the translation of text in voice over";
  const DontTranslateSelectedLanguages$M = "Don't translate from selected languages";
  const showVideoVolumeSlider$M = "Display the video volume slider";
  const hotkeysSettings$M = "Hotkeys settings";
  const None$M = "None";
  const VOTUseLivelyVoice$M = "Use lively voices. Speakers sound like native Russians.";
  const VOTStandardVoices$1 = "Regular voices";
  const VOTStandardVoicesDescription$1 = "Fast and high-quality voice over";
  const VOTLivelyVoices$1 = "Lively voices";
  const VOTLivelyVoicesDescription$1 = "Maximum similarity. Sounds more natural";
  const VOTVoiceModeMenuTitle$1 = "Voice mode";
  const VOTRailTranslateAndDub$1 = "Translate and dub";
  const VOTRailDisableTranslation$1 = "Turn off translation";
  const VOTRailTranslatingEta$1 = "Translating, {0} left";
  const VOTRailCompactMinute$1 = "~1 min";
  const VOTRailCompactMinutes$1 = "~{0} min";
  const VOTRailCompactMoreThanHour$1 = ">1 hr";
  const miscSettings$M = "Misc settings";
  const services$M = { "yandexbrowser": "Yandex Browser", "msedge": "Microsoft Edge", "rust-server": "Rust Server" };
  const aboutExtension$M = "About extension";
  const appearance$M = "Appearance";
  const buttonPosition$M = "Button position in the player";
  const position$M = { "left": "Left", "right": "Right", "top": "Top", "default": "Default" };
  const secs$M = "secs";
  const autoHideButtonDelay$M = "Delay before hiding the translate button";
  const notFound$M = "not found";
  const minButtonPositionContainer$M = "The button position only changes in players larger than 600 pixels.";
  const VOTTranslateProxyStatusDefault$M = "Completely disabling proxying in your country may break the extension";
  const PressTheKeyCombination$M = "Press the key combination...";
  const VOTUseAudioDownload$M = "Use audio download";
  const VOTUseAudioDownloadWarning$M = "Disabling audio downloads may affect the functionality of the extension";
  const VOTAccountRequired$M = "You need to log in to use this feature";
  const VOTMyAccount$M = "My account";
  const VOTLogin$M = "Login";
  const VOTLogout$M = "Logout";
  const VOTRefresh$M = "Refresh";
  const VOTYandexToken$M = "Enter the Yandex OAuth Token";
  const VOTYandexTokenInfo$M = "You can manually set the account token in this field. Please note that we don't check its validity before sending a translate request";
  const VOTLoginViaToken$M = "Login via token";
  const smartDucking$M = "Adaptive volume";
  const __vite_glob_0_13 = {
    recommended: recommended$M,
    translateVideo: translateVideo$M,
    disableTranslate: disableTranslate$M,
    translationSettings: translationSettings$M,
    subtitlesSettings: subtitlesSettings$M,
    subtitlesSmartLayout: subtitlesSmartLayout$M,
    resetSettings: resetSettings$M,
    videoBeingTranslated: videoBeingTranslated$M,
    videoLanguage: videoLanguage$M,
    translationLanguage: translationLanguage$M,
    translationTake: translationTake$M,
    translationTakeMoreThanHour: translationTakeMoreThanHour$M,
    translationTakeAboutMinute: translationTakeAboutMinute$M,
    translationTakeFewMinutes: translationTakeFewMinutes$M,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$M,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$M,
    requestTranslationFailed: requestTranslationFailed$M,
    audioNotReceived: audioNotReceived$M,
    VOTFailedDownloadAudio: VOTFailedDownloadAudio$1,
    audioFormatNotSupported: audioFormatNotSupported$M,
    VOTStartVideoForTranslation: VOTStartVideoForTranslation$1,
    VOTAudioNotDetected: VOTAudioNotDetected$1,
    VOTAudioNotYetAvailable: VOTAudioNotYetAvailable$1,
    VOTAutoTranslate: VOTAutoTranslate$M,
    VOTAutoSubtitles: VOTAutoSubtitles$M,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$M,
    VOTVolume: VOTVolume$M,
    VOTVolumeTranslation: VOTVolumeTranslation$M,
    VOTAutoSetVolume: VOTAutoSetVolume$M,
    VOTShowVideoSlider: VOTShowVideoSlider$M,
    VOTSyncVolume: VOTSyncVolume$M,
    VOTDisableFromYourLang: VOTDisableFromYourLang$M,
    VOTVideoIsTooLong: VOTVideoIsTooLong$M,
    VOTNoVideoIDFound: VOTNoVideoIDFound$M,
    VOTSubtitles: VOTSubtitles$M,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$M,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$M,
    VOTHighlightWords: VOTHighlightWords$M,
    VOTTranslatedFrom: VOTTranslatedFrom$M,
    VOTAutogenerated: VOTAutogenerated$M,
    VOTSettings: VOTSettings$M,
    VOTMenuLanguage: VOTMenuLanguage$M,
    VOTAuthors: VOTAuthors$M,
    VOTVersion: VOTVersion$M,
    VOTLoader: VOTLoader$M,
    VOTBrowser: VOTBrowser$M,
    VOTShowPiPButton: VOTShowPiPButton$M,
    langs: langs$M,
    streamNoConnectionToServer: streamNoConnectionToServer$M,
    searchField: searchField$M,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$M,
    VOTDetectService: VOTDetectService$M,
    VOTProxyWorkerHost: VOTProxyWorkerHost$M,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$M,
    proxySettings: proxySettings$M,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$M,
    VOTAudioBooster: VOTAudioBooster$M,
    VOTSubtitlesDesign: VOTSubtitlesDesign$M,
    VOTSubtitlesFont: VOTSubtitlesFont$M,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$M,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$M,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$M,
    VOTDownloadWithName: VOTDownloadWithName$M,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$M,
    VOTLocaleHash: VOTLocaleHash$M,
    VOTUpdatedAt: VOTUpdatedAt$M,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$M,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$M,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$M,
    VOTNewAudioPlayer: VOTNewAudioPlayer$M,
    VOTUseNewModel: VOTUseNewModel$M,
    TranslationDelayed: TranslationDelayed$M,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$M,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$M,
    VOTBugReport: VOTBugReport$M,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$M,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$M,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$M,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$M,
    VOTTranslatedBy: VOTTranslatedBy$M,
    VOTStreamNotAvailable: VOTStreamNotAvailable$M,
    VOTTranslationTextService: VOTTranslationTextService$M,
    VOTNotAffectToVoice: VOTNotAffectToVoice$M,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$M,
    showVideoVolumeSlider: showVideoVolumeSlider$M,
    hotkeysSettings: hotkeysSettings$M,
    None: None$M,
    VOTUseLivelyVoice: VOTUseLivelyVoice$M,
    VOTStandardVoices: VOTStandardVoices$1,
    VOTStandardVoicesDescription: VOTStandardVoicesDescription$1,
    VOTLivelyVoices: VOTLivelyVoices$1,
    VOTLivelyVoicesDescription: VOTLivelyVoicesDescription$1,
    VOTVoiceModeMenuTitle: VOTVoiceModeMenuTitle$1,
    VOTRailTranslateAndDub: VOTRailTranslateAndDub$1,
    VOTRailDisableTranslation: VOTRailDisableTranslation$1,
    VOTRailTranslatingEta: VOTRailTranslatingEta$1,
    VOTRailCompactMinute: VOTRailCompactMinute$1,
    VOTRailCompactMinutes: VOTRailCompactMinutes$1,
    VOTRailCompactMoreThanHour: VOTRailCompactMoreThanHour$1,
    miscSettings: miscSettings$M,
    services: services$M,
    aboutExtension: aboutExtension$M,
    appearance: appearance$M,
    buttonPosition: buttonPosition$M,
    position: position$M,
    secs: secs$M,
    autoHideButtonDelay: autoHideButtonDelay$M,
    notFound: notFound$M,
    minButtonPositionContainer: minButtonPositionContainer$M,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$M,
    PressTheKeyCombination: PressTheKeyCombination$M,
    VOTUseAudioDownload: VOTUseAudioDownload$M,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$M,
    VOTAccountRequired: VOTAccountRequired$M,
    VOTMyAccount: VOTMyAccount$M,
    VOTLogin: VOTLogin$M,
    VOTLogout: VOTLogout$M,
    VOTRefresh: VOTRefresh$M,
    VOTYandexToken: VOTYandexToken$M,
    VOTYandexTokenInfo: VOTYandexTokenInfo$M,
    VOTLoginViaToken: VOTLoginViaToken$M,
    smartDucking: smartDucking$M
  };
  const recommended$L = "recomendado";
  const translateVideo$L = "Traducir vídeo";
  const disableTranslate$L = "Apagar";
  const translationSettings$L = "Configuración de traducción";
  const subtitlesSettings$L = "Configuración de subtítulos";
  const resetSettings$L = "Restablecer la configuración";
  const videoBeingTranslated$L = "El video está siendo traducido";
  const videoLanguage$L = "Lenguaje de video";
  const translationLanguage$L = "Idioma de traducción";
  const translationTake$L = "La traducción tomará";
  const translationTakeMoreThanHour$L = "La traducción tardará más de una hora";
  const translationTakeAboutMinute$L = "La traducción tardará aproximadamente un minuto";
  const translationTakeFewMinutes$L = "La traducción tardará unos minutos";
  const translationTakeApproximatelyMinutes$L = "La traducción tardará aproximadamente {0} minutos";
  const translationTakeApproximatelyMinute$L = "La traducción tardará aproximadamente {0} minutos";
  const requestTranslationFailed$L = "Error al solicitar la traducción de vídeo";
  const audioNotReceived$L = "Enlace de audio no recibido";
  const audioFormatNotSupported$L = "El formato de audio no es compatible";
  const VOTAutoTranslate$L = "Traducir en abierto";
  const VOTDontTranslateYourLang$L = "No traduzcas de mi idioma";
  const VOTVolume$L = "Volumen de video:";
  const VOTVolumeTranslation$L = "Volumen de traducción:";
  const VOTAutoSetVolume$L = "Reduzca el volumen del video a";
  const VOTShowVideoSlider$L = "Control deslizante de volumen de video";
  const VOTSyncVolume$L = "Traducción de enlaces y volumen de vídeo";
  const VOTDisableFromYourLang$L = "Has desactivado la traducción del vídeo en tu idioma";
  const VOTVideoIsTooLong$L = "El vídeo es demasiado largo";
  const VOTNoVideoIDFound$L = "No se encontró ID de video";
  const VOTSubtitles$L = "Subtítulos";
  const VOTSubtitlesDisabled$L = "Discapacitados";
  const VOTSubtitlesMaxLength$L = "Duración máxima de los subtítulos";
  const VOTHighlightWords$L = "Resaltar palabras";
  const VOTTranslatedFrom$L = "traducido de";
  const VOTAutogenerated$L = "autogenerados";
  const VOTSettings$L = "Configuración de VOT";
  const VOTMenuLanguage$L = "Idioma del menú";
  const VOTAuthors$L = "Autores";
  const VOTVersion$L = "Versión";
  const VOTLoader$L = "Cargador";
  const VOTBrowser$L = "Navegador";
  const VOTShowPiPButton$L = "Botón Mostrar PiP";
  const langs$L = { "auto": "Auto", "af": "Afrikáans", "ak": "Akan", "sq": "Albanés", "am": "Amárico", "ar": "Árabe", "hy": "Armenio", "as": "Asamés", "ay": "Aymara", "az": "Azerbaiyano", "bn": "Bengalí", "eu": "Euskera", "be": "Bielorruso", "bho": "Bhojpuri", "bs": "Bosnio", "bg": "Búlgaro", "my": "Birmano", "ca": "Catalán", "ceb": "Cebuano", "zh": "Chino", "zh-Hans": "Chino (simplificado)", "zh-Hant": "Chino (Tradicional)", "co": "Corso", "hr": "Croata", "cs": "Checo", "da": "Danés", "dv": "Divehi", "nl": "Neerlandés", "en": "Inglés", "eo": "Esperanto", "et": "Estonio", "ee": "Oveja", "fil": "Filipino", "fi": "Finlandés", "fr": "Francés", "gl": "Gallego", "lg": "Ganda", "ka": "Georgiano", "de": "Alemán", "el": "Griego", "gn": "Guaraní", "gu": "Gujarati", "ht": "Criollo Haitiano", "ha": "Hausa", "haw": "Hawaiano", "iw": "Hebreo", "hi": "Hindi", "hmn": "Hmong", "hu": "Húngaro", "is": "Islandés", "ig": "Igbo", "id": "Indonesio", "ga": "Irlandés", "it": "Italiano", "ja": "Japonés", "jv": "Javanés", "kn": "Canarés", "kk": "Kazajo", "km": "Jemer", "rw": "Kinyarwanda", "ko": "Coreano", "kri": "Krio", "ku": "Kurdo", "ky": "Kirguís", "lo": "Lao", "la": "Latín", "lv": "Letón", "ln": "Lingala", "lt": "Lituano", "lb": "Luxemburgués", "mk": "Macedonio", "mg": "Malgache", "ms": "Malayo", "ml": "Malayalam", "mt": "Maltés", "mi": "Maorí", "mr": "Maratí", "mn": "Mongol", "ne": "Nepalí", "nso": "Sotho del Norte", "no": "Noruego", "ny": "Nyanja", "or": "Odia", "om": "Oromo", "ps": "Pastún", "fa": "Persa", "pl": "Polaco", "pt": "Portugués", "pa": "Punyabí", "qu": "Quechua", "ro": "Rumano", "ru": "Ruso", "sm": "Samoano", "sa": "Sánscrito", "gd": "Gaélico Escocés", "sr": "Serbio", "sn": "Shona", "sd": "Sindhi", "si": "Cingalés", "sk": "Eslovaco", "sl": "Esloveno", "so": "Somalí", "st": "Sotho del Sur", "es": "Español", "su": "Sundanés", "sw": "Swahili", "sv": "Sueco", "tg": "Tayiko", "ta": "Tamil", "tt": "Tártaro", "te": "Telugu", "th": "Tailandés", "ti": "Tigriña", "ts": "Tsonga", "tr": "Turco", "tk": "Turcomanos", "uk": "Ucraniano", "ur": "Urdu", "ug": "Uigur", "uz": "Uzbeko", "vi": "Vietnamita", "cy": "Galés", "fy": "Frisón Occidental", "xh": "Xhosa", "yi": "Yidis", "yo": "Yoruba", "zu": "Zulú" };
  const streamNoConnectionToServer$L = "No hay conexión con el servidor";
  const searchField$L = "Búsqueda...";
  const VOTTranslateAPIErrors$L = "Traducir errores de la API";
  const VOTDetectService$L = "Servicio de detección de idioma";
  const VOTProxyWorkerHost$L = "Ingrese la dirección del trabajador proxy";
  const VOTM3u8ProxyHost$L = "Ingrese la dirección del trabajador proxy m3u8";
  const proxySettings$L = "Configuración de Proxy";
  const translationTakeApproximatelyMinute2$L = "La traducción tardará aproximadamente {0} minutos";
  const VOTAudioBooster$L = "Aumento del volumen de traducción extendida";
  const VOTSubtitlesDesign$L = "Diseño de subtítulos";
  const VOTSubtitlesFontSize$L = "Tamaño de fuente de los subtítulos";
  const VOTSubtitlesOpacity$L = "Transparencia del fondo de los subtítulos";
  const VOTSubtitlesDownloadFormat$L = "El formato para descargar subtítulos";
  const VOTDownloadWithName$L = "Descargar archivos con el nombre del video";
  const VOTUpdateLocaleFiles$L = "Actualizar archivos de localización";
  const VOTLocaleHash$L = "Hash de configuración regional";
  const VOTUpdatedAt$L = "Actualizado a las";
  const VOTNeedWebAudioAPI$L = "Para habilitar esto, debe tener una API de audio web";
  const VOTMediaCSPEnabledOnSite$L = "Media CSP está habilitado en este sitio";
  const VOTOnlyBypassMediaCSP$L = "Úselo solo para omitir CSP de medios";
  const VOTNewAudioPlayer$L = "Usa el nuevo reproductor de audio";
  const VOTUseNewModel$L = "Usa una variación experimental de las voces de Yandex para algunos videos";
  const TranslationDelayed$L = "La traducción se retrasa ligeramente";
  const VOTTranslationCompletedNotify$L = "¡La traducción del {0} se ha completado!";
  const VOTSendNotifyOnComplete$L = "Enviar una notificación de que el video ha sido traducido";
  const VOTBugReport$L = "Reportar un error";
  const VOTTranslateProxyDisabled$L = "Discapacitados";
  const VOTTranslateProxyEnabled$L = "Habilitado";
  const VOTTranslateProxyEverything$L = "Proxy de todo";
  const VOTTranslateProxyStatus$L = "Modo proxy";
  const VOTTranslatedBy$L = "Traducido por {0}";
  const VOTStreamNotAvailable$L = "Traducir stream no está disponible";
  const VOTTranslationTextService$L = "Servicio de traducción de textos";
  const VOTNotAffectToVoice$L = "No afecta a la traducción de texto en locución";
  const DontTranslateSelectedLanguages$L = "No traduzca desde idiomas seleccionados";
  const showVideoVolumeSlider$L = "Mostrar el control deslizante de volumen de video";
  const hotkeysSettings$L = "Configuración de teclas de acceso rápido";
  const None$L = "Ninguna";
  const VOTUseLivelyVoice$L = "Usa voces animadas. Los hablantes suenan como rusos nativos.";
  const miscSettings$L = "Configuraciones misceláneas";
  const services$L = { "yandexbrowser": "Navegador Yandex", "msedge": "Microsoft Edge", "rust-server": "Servidor Rust" };
  const aboutExtension$L = "Acerca de la extensión";
  const appearance$L = "Apariencia";
  const buttonPosition$L = "Posición del botón en el reproductor";
  const position$L = { "left": "Izquierda", "right": "Correcto", "top": "Arriba", "default": "Por defecto" };
  const secs$L = "sec";
  const autoHideButtonDelay$L = "Demora antes de ocultar el botón traducir";
  const notFound$L = "no encontrado";
  const minButtonPositionContainer$L = "La posición del botón solo cambia en reproductores de más de 600 píxeles.";
  const VOTTranslateProxyStatusDefault$L = "Deshabilitar completamente el proxy en su país puede romper la extensión";
  const PressTheKeyCombination$L = "Presione la combinación de teclas...";
  const VOTUseAudioDownload$L = "Usar descarga de audio";
  const VOTUseAudioDownloadWarning$L = "Deshabilitar las descargas de audio puede afectar la funcionalidad de la extensión";
  const VOTAccountRequired$L = "Necesita iniciar sesión para usar esta función";
  const VOTMyAccount$L = "Mi cuenta";
  const VOTLogin$L = "Iniciar sesión";
  const VOTLogout$L = "Cerrar sesión";
  const VOTRefresh$L = "Refrescar";
  const VOTYandexToken$L = "Ingrese el Token Yandex OAuth";
  const VOTYandexTokenInfo$L = "Puede configurar manualmente el token de la cuenta en este campo. Tenga en cuenta que no verificamos su validez antes de enviar una solicitud de traducción";
  const VOTLoginViaToken$L = "Iniciar sesión a través de token";
  const subtitlesSmartLayout$L = "Diseño inteligente de subtítulos";
  const smartDucking$L = "Volumen adaptable";
  const VOTAutoSubtitles$L = "Subtítulos en abierto";
  const VOTSubtitlesFont$L = "Fuente de subtítulos";
  const __vite_glob_0_14 = {
    recommended: recommended$L,
    translateVideo: translateVideo$L,
    disableTranslate: disableTranslate$L,
    translationSettings: translationSettings$L,
    subtitlesSettings: subtitlesSettings$L,
    resetSettings: resetSettings$L,
    videoBeingTranslated: videoBeingTranslated$L,
    videoLanguage: videoLanguage$L,
    translationLanguage: translationLanguage$L,
    translationTake: translationTake$L,
    translationTakeMoreThanHour: translationTakeMoreThanHour$L,
    translationTakeAboutMinute: translationTakeAboutMinute$L,
    translationTakeFewMinutes: translationTakeFewMinutes$L,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$L,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$L,
    requestTranslationFailed: requestTranslationFailed$L,
    audioNotReceived: audioNotReceived$L,
    audioFormatNotSupported: audioFormatNotSupported$L,
    VOTAutoTranslate: VOTAutoTranslate$L,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$L,
    VOTVolume: VOTVolume$L,
    VOTVolumeTranslation: VOTVolumeTranslation$L,
    VOTAutoSetVolume: VOTAutoSetVolume$L,
    VOTShowVideoSlider: VOTShowVideoSlider$L,
    VOTSyncVolume: VOTSyncVolume$L,
    VOTDisableFromYourLang: VOTDisableFromYourLang$L,
    VOTVideoIsTooLong: VOTVideoIsTooLong$L,
    VOTNoVideoIDFound: VOTNoVideoIDFound$L,
    VOTSubtitles: VOTSubtitles$L,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$L,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$L,
    VOTHighlightWords: VOTHighlightWords$L,
    VOTTranslatedFrom: VOTTranslatedFrom$L,
    VOTAutogenerated: VOTAutogenerated$L,
    VOTSettings: VOTSettings$L,
    VOTMenuLanguage: VOTMenuLanguage$L,
    VOTAuthors: VOTAuthors$L,
    VOTVersion: VOTVersion$L,
    VOTLoader: VOTLoader$L,
    VOTBrowser: VOTBrowser$L,
    VOTShowPiPButton: VOTShowPiPButton$L,
    langs: langs$L,
    streamNoConnectionToServer: streamNoConnectionToServer$L,
    searchField: searchField$L,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$L,
    VOTDetectService: VOTDetectService$L,
    VOTProxyWorkerHost: VOTProxyWorkerHost$L,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$L,
    proxySettings: proxySettings$L,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$L,
    VOTAudioBooster: VOTAudioBooster$L,
    VOTSubtitlesDesign: VOTSubtitlesDesign$L,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$L,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$L,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$L,
    VOTDownloadWithName: VOTDownloadWithName$L,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$L,
    VOTLocaleHash: VOTLocaleHash$L,
    VOTUpdatedAt: VOTUpdatedAt$L,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$L,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$L,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$L,
    VOTNewAudioPlayer: VOTNewAudioPlayer$L,
    VOTUseNewModel: VOTUseNewModel$L,
    TranslationDelayed: TranslationDelayed$L,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$L,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$L,
    VOTBugReport: VOTBugReport$L,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$L,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$L,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$L,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$L,
    VOTTranslatedBy: VOTTranslatedBy$L,
    VOTStreamNotAvailable: VOTStreamNotAvailable$L,
    VOTTranslationTextService: VOTTranslationTextService$L,
    VOTNotAffectToVoice: VOTNotAffectToVoice$L,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$L,
    showVideoVolumeSlider: showVideoVolumeSlider$L,
    hotkeysSettings: hotkeysSettings$L,
    None: None$L,
    VOTUseLivelyVoice: VOTUseLivelyVoice$L,
    miscSettings: miscSettings$L,
    services: services$L,
    aboutExtension: aboutExtension$L,
    appearance: appearance$L,
    buttonPosition: buttonPosition$L,
    position: position$L,
    secs: secs$L,
    autoHideButtonDelay: autoHideButtonDelay$L,
    notFound: notFound$L,
    minButtonPositionContainer: minButtonPositionContainer$L,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$L,
    PressTheKeyCombination: PressTheKeyCombination$L,
    VOTUseAudioDownload: VOTUseAudioDownload$L,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$L,
    VOTAccountRequired: VOTAccountRequired$L,
    VOTMyAccount: VOTMyAccount$L,
    VOTLogin: VOTLogin$L,
    VOTLogout: VOTLogout$L,
    VOTRefresh: VOTRefresh$L,
    VOTYandexToken: VOTYandexToken$L,
    VOTYandexTokenInfo: VOTYandexTokenInfo$L,
    VOTLoginViaToken: VOTLoginViaToken$L,
    subtitlesSmartLayout: subtitlesSmartLayout$L,
    smartDucking: smartDucking$L,
    VOTAutoSubtitles: VOTAutoSubtitles$L,
    VOTSubtitlesFont: VOTSubtitlesFont$L
  };
  const recommended$K = "soovitatav";
  const translateVideo$K = "Tõlgi video";
  const disableTranslate$K = "Lülitama";
  const translationSettings$K = "Tõlke Seaded";
  const subtitlesSettings$K = "Subtiitrite Seaded";
  const resetSettings$K = "Lähtesta seaded";
  const videoBeingTranslated$K = "Video on tõlkimisel";
  const videoLanguage$K = "Video keel";
  const translationLanguage$K = "Tõlkekeel";
  const translationTake$K = "Tõlge võtab";
  const translationTakeMoreThanHour$K = "Tõlge võtab rohkem kui tund";
  const translationTakeAboutMinute$K = "Tõlge võtab aega umbes minut";
  const translationTakeFewMinutes$K = "Tõlge võtab paar minutit";
  const translationTakeApproximatelyMinutes$K = "Tõlge võtab umbes {0} minutit";
  const translationTakeApproximatelyMinute$K = "Tõlge võtab umbes {0} minutit";
  const requestTranslationFailed$K = "Videotõlke taotlemine nurjus";
  const audioNotReceived$K = "Audio link ei saanud";
  const audioFormatNotSupported$K = "Helivormingut ei toetata";
  const VOTAutoTranslate$K = "Tõlgi avatud";
  const VOTDontTranslateYourLang$K = "Ära tõlgi minu keelest";
  const VOTVolume$K = "Video Maht:";
  const VOTVolumeTranslation$K = "Tõlke maht:";
  const VOTAutoSetVolume$K = "Vähendage video helitugevust";
  const VOTShowVideoSlider$K = "Video helitugevuse liugur";
  const VOTSyncVolume$K = "Lingi tõlge ja video Maht";
  const VOTDisableFromYourLang$K = "Olete keelanud video tõlkimise oma keelde";
  const VOTVideoIsTooLong$K = "Video on liiga pikk";
  const VOTNoVideoIDFound$K = "Video ID-d ei leitud";
  const VOTSubtitles$K = "Subtiitrid";
  const VOTSubtitlesDisabled$K = "Puudega";
  const VOTSubtitlesMaxLength$K = "Subtiitrid max pikkus";
  const VOTHighlightWords$K = "Tõstke esile sõnad";
  const VOTTranslatedFrom$K = "tõlgitud";
  const VOTAutogenerated$K = "autogenereeritud";
  const VOTSettings$K = "VOT Seaded";
  const VOTMenuLanguage$K = "Menüü keel";
  const VOTAuthors$K = "Kirjanik";
  const VOTVersion$K = "Versioon";
  const VOTLoader$K = "Loader";
  const VOTBrowser$K = "Veebibrauser";
  const VOTShowPiPButton$K = "PIP-nupu näitamine";
  const langs$K = { "auto": "Auto", "af": "Afrikaani", "ak": "Akan", "sq": "Albaania", "am": "Amhari", "ar": "Araabia", "hy": "Armeenia", "as": "Assami", "ay": "Aymara", "az": "Aserbaidžaani", "bn": "Bangla", "eu": "Baski", "be": "Valgevene", "bho": "Bhojpuri", "bs": "Bosnia", "bg": "Bulgaaria", "my": "Birma", "ca": "Katalaani", "ceb": "Cebuano", "zh": "Hiina", "zh-Hans": "Hiina (Lihtsustatud)", "zh-Hant": "Hiina (Traditsiooniline)", "co": "Korsika", "hr": "Horvaatia", "cs": "Tšehhi", "da": "Taani", "dv": "Divehi", "nl": "Hollandi", "en": "Inglise", "eo": "Esperanto", "et": "Eesti", "ee": "Eveeni", "fil": "Filipiinide", "fi": "Soome", "fr": "Prantsuse", "gl": "Galicia", "lg": "Ganda", "ka": "Gruusia", "de": "Saksa", "el": "Kreeka", "gn": "Guarani", "gu": "Gujarati", "ht": "Haiti Kreooli", "ha": "Hausa", "haw": "Hawaii", "iw": "Heebrea", "hi": "Hindi", "hmn": "Hmongi", "hu": "Ungari", "is": "Islandi", "ig": "Igbo", "id": "Indoneesia", "ga": "Iiri", "it": "Itaalia", "ja": "Jaapani", "jv": "Jaava", "kn": "Kannada", "kk": "Kasahstani", "km": "Khmeeri", "rw": "Kinyarwanda", "ko": "Korea", "kri": "Krio", "ku": "Kurdi", "ky": "Kõrgõzstan", "lo": "Lao", "la": "Ladina", "lv": "Läti", "ln": "Lingala", "lt": "Leedu", "lb": "Luksemburgi", "mk": "Makedoonia", "mg": "Madagaskari", "ms": "Malai", "ml": "Malajalami", "mt": "Malta", "mi": "Maoori", "mr": "Marathi", "mn": "Mongoolia", "ne": "Nepali", "nso": "Põhja-Sotho", "no": "Norra", "ny": "Nyanja", "or": "Odia", "om": "Oromo", "ps": "Puštu", "fa": "Pärsia", "pl": "Poola", "pt": "Portugali", "pa": "Punjabi", "qu": "Ketšua", "ro": "Rumeenia", "ru": "Vene", "sm": "Samoa", "sa": "Sanskriti", "gd": "gaeli", "sr": "Serbia", "sn": "Shona", "sd": "Sindhi", "si": "Sinhala", "sk": "Slovaki", "sl": "Sloveenia", "so": "Somaalia", "st": "Lõuna-Sotho", "es": "Hispaania", "su": "Sundanese", "sw": "Suahiili", "sv": "Rootsi", "tg": "Tadžiki", "ta": "Tamil", "tt": "Tatari", "te": "Telugu", "th": "Tai", "ti": "Tigrinya", "ts": "Tsonga", "tr": "Türgi", "tk": "Türkmeeni", "uk": "Ukraina", "ur": "Urdu", "ug": "Uiguuri", "uz": "Usbeki", "vi": "Vietnami", "cy": "Kõmri", "fy": "Lääne-Friisi", "xh": "Xhosa", "yi": "Jidiš", "yo": "Joruba", "zu": "Zulu" };
  const streamNoConnectionToServer$K = "Serveriga pole ühendust";
  const searchField$K = "Otsing...";
  const VOTTranslateAPIErrors$K = "Tõlgi vead API-st";
  const VOTDetectService$K = "Keele tuvastamise teenus";
  const VOTProxyWorkerHost$K = "Sisestage puhverserveri töötaja aadress";
  const VOTM3u8ProxyHost$K = "Sisestage m3u8 puhverserveri töötaja aadress";
  const proxySettings$K = "Puhverserveri Seaded";
  const translationTakeApproximatelyMinute2$K = "Tõlge võtab umbes {0} minutit";
  const VOTAudioBooster$K = "Laiendatud tõlke mahu suurenemine";
  const VOTSubtitlesDesign$K = "Subtiitrite kujundus";
  const VOTSubtitlesFontSize$K = "Subtiitrite fondi suurus";
  const VOTSubtitlesOpacity$K = "Subtiitrite tausta läbipaistvus";
  const VOTSubtitlesDownloadFormat$K = "Subtiitrite allalaadimise vorming";
  const VOTDownloadWithName$K = "Laadige alla video nimega failid";
  const VOTUpdateLocaleFiles$K = "Uuenda lokaliseerimisfaile";
  const VOTLocaleHash$K = "Locale hash";
  const VOTUpdatedAt$K = "Uuendatud kell";
  const VOTNeedWebAudioAPI$K = "Selle lubamiseks peab teil olema Veebiheli API";
  const VOTMediaCSPEnabledOnSite$K = "Meedia CSP on lubatud sellel saidil";
  const VOTOnlyBypassMediaCSP$K = "Kasutage seda ainult mööda meedia CSP";
  const VOTNewAudioPlayer$K = "Kasutage uut helipleierit";
  const VOTUseNewModel$K = "Mõne video jaoks kasutage Yandexi häälte eksperimentaalset variatsiooni";
  const TranslationDelayed$K = "Tõlge on veidi hilinenud";
  const VOTTranslationCompletedNotify$K = "{0} tõlge on lõpetatud!";
  const VOTSendNotifyOnComplete$K = "Saada teade, et video on tõlgitud";
  const VOTBugReport$K = "Teata veast";
  const VOTTranslateProxyDisabled$K = "Puudega";
  const VOTTranslateProxyEnabled$K = "Võimaldama";
  const VOTTranslateProxyEverything$K = "Puhverserver kõik";
  const VOTTranslateProxyStatus$K = "Puhverserveri režiim";
  const VOTTranslatedBy$K = "Tõlkinud {0}";
  const VOTStreamNotAvailable$K = "Tõlgi voog pole saadaval";
  const VOTTranslationTextService$K = "Teksti tõlketeenus";
  const VOTNotAffectToVoice$K = "Ei mõjuta teksti tõlkimist häälega";
  const DontTranslateSelectedLanguages$K = "Ärge tõlkige valitud keeltest";
  const showVideoVolumeSlider$K = "Video helitugevuse liuguri kuvamine";
  const hotkeysSettings$K = "Kiirklahvide Seaded";
  const None$K = "Ükski";
  const VOTUseLivelyVoice$K = "Kasutage elavaid hääli. Kõlarid kõlavad nagu kohalikud venelased.";
  const miscSettings$K = "Muud seaded";
  const services$K = { "yandexbrowser": "Yandexi Brauser", "msedge": "Microsoft Edge", "rust-server": "Rust Server" };
  const aboutExtension$K = "Laienduse kohta";
  const appearance$K = "Välimus";
  const buttonPosition$K = "Nupp positsiooni mängija";
  const position$K = { "left": "Vasak", "right": "Õigus", "top": "Top", "default": "Vaikimisi" };
  const secs$K = "sekundid";
  const autoHideButtonDelay$K = "Viivitus enne tõlkimisnupu peitmist";
  const notFound$K = "ei leitud";
  const minButtonPositionContainer$K = "Nupu asend muutub ainult mängijates, mis on suuremad kui 600 pikslit.";
  const VOTTranslateProxyStatusDefault$K = "Puhverserveri täielik keelamine teie riigis võib laienduse katkestada";
  const PressTheKeyCombination$K = "Vajutage klahvikombinatsiooni...";
  const VOTUseAudioDownload$K = "Kasutage heli allalaadimist";
  const VOTUseAudioDownloadWarning$K = "Heli allalaadimise keelamine võib mõjutada laienduse funktsionaalsust";
  const VOTAccountRequired$K = "Selle funktsiooni kasutamiseks peate sisse logima";
  const VOTMyAccount$K = "Minu konto";
  const VOTLogin$K = "Login";
  const VOTLogout$K = "Logout";
  const VOTRefresh$K = "Värskendamiseks";
  const VOTYandexToken$K = "Sisestage Yandex OAuthi märk";
  const VOTYandexTokenInfo$K = "Sellel väljal saate konto märgi käsitsi seadistada. Pange tähele, et me ei kontrolli selle kehtivust enne tõlkimistaotluse saatmist";
  const VOTLoginViaToken$K = "Logi sisse tokeni kaudu";
  const subtitlesSmartLayout$K = "Nutikas subtiitrite paigutus";
  const smartDucking$K = "Adaptiivne maht";
  const VOTAutoSubtitles$K = "Subtiitrid avatud";
  const VOTSubtitlesFont$K = "Subtiitrite font";
  const __vite_glob_0_15 = {
    recommended: recommended$K,
    translateVideo: translateVideo$K,
    disableTranslate: disableTranslate$K,
    translationSettings: translationSettings$K,
    subtitlesSettings: subtitlesSettings$K,
    resetSettings: resetSettings$K,
    videoBeingTranslated: videoBeingTranslated$K,
    videoLanguage: videoLanguage$K,
    translationLanguage: translationLanguage$K,
    translationTake: translationTake$K,
    translationTakeMoreThanHour: translationTakeMoreThanHour$K,
    translationTakeAboutMinute: translationTakeAboutMinute$K,
    translationTakeFewMinutes: translationTakeFewMinutes$K,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$K,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$K,
    requestTranslationFailed: requestTranslationFailed$K,
    audioNotReceived: audioNotReceived$K,
    audioFormatNotSupported: audioFormatNotSupported$K,
    VOTAutoTranslate: VOTAutoTranslate$K,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$K,
    VOTVolume: VOTVolume$K,
    VOTVolumeTranslation: VOTVolumeTranslation$K,
    VOTAutoSetVolume: VOTAutoSetVolume$K,
    VOTShowVideoSlider: VOTShowVideoSlider$K,
    VOTSyncVolume: VOTSyncVolume$K,
    VOTDisableFromYourLang: VOTDisableFromYourLang$K,
    VOTVideoIsTooLong: VOTVideoIsTooLong$K,
    VOTNoVideoIDFound: VOTNoVideoIDFound$K,
    VOTSubtitles: VOTSubtitles$K,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$K,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$K,
    VOTHighlightWords: VOTHighlightWords$K,
    VOTTranslatedFrom: VOTTranslatedFrom$K,
    VOTAutogenerated: VOTAutogenerated$K,
    VOTSettings: VOTSettings$K,
    VOTMenuLanguage: VOTMenuLanguage$K,
    VOTAuthors: VOTAuthors$K,
    VOTVersion: VOTVersion$K,
    VOTLoader: VOTLoader$K,
    VOTBrowser: VOTBrowser$K,
    VOTShowPiPButton: VOTShowPiPButton$K,
    langs: langs$K,
    streamNoConnectionToServer: streamNoConnectionToServer$K,
    searchField: searchField$K,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$K,
    VOTDetectService: VOTDetectService$K,
    VOTProxyWorkerHost: VOTProxyWorkerHost$K,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$K,
    proxySettings: proxySettings$K,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$K,
    VOTAudioBooster: VOTAudioBooster$K,
    VOTSubtitlesDesign: VOTSubtitlesDesign$K,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$K,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$K,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$K,
    VOTDownloadWithName: VOTDownloadWithName$K,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$K,
    VOTLocaleHash: VOTLocaleHash$K,
    VOTUpdatedAt: VOTUpdatedAt$K,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$K,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$K,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$K,
    VOTNewAudioPlayer: VOTNewAudioPlayer$K,
    VOTUseNewModel: VOTUseNewModel$K,
    TranslationDelayed: TranslationDelayed$K,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$K,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$K,
    VOTBugReport: VOTBugReport$K,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$K,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$K,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$K,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$K,
    VOTTranslatedBy: VOTTranslatedBy$K,
    VOTStreamNotAvailable: VOTStreamNotAvailable$K,
    VOTTranslationTextService: VOTTranslationTextService$K,
    VOTNotAffectToVoice: VOTNotAffectToVoice$K,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$K,
    showVideoVolumeSlider: showVideoVolumeSlider$K,
    hotkeysSettings: hotkeysSettings$K,
    None: None$K,
    VOTUseLivelyVoice: VOTUseLivelyVoice$K,
    miscSettings: miscSettings$K,
    services: services$K,
    aboutExtension: aboutExtension$K,
    appearance: appearance$K,
    buttonPosition: buttonPosition$K,
    position: position$K,
    secs: secs$K,
    autoHideButtonDelay: autoHideButtonDelay$K,
    notFound: notFound$K,
    minButtonPositionContainer: minButtonPositionContainer$K,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$K,
    PressTheKeyCombination: PressTheKeyCombination$K,
    VOTUseAudioDownload: VOTUseAudioDownload$K,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$K,
    VOTAccountRequired: VOTAccountRequired$K,
    VOTMyAccount: VOTMyAccount$K,
    VOTLogin: VOTLogin$K,
    VOTLogout: VOTLogout$K,
    VOTRefresh: VOTRefresh$K,
    VOTYandexToken: VOTYandexToken$K,
    VOTYandexTokenInfo: VOTYandexTokenInfo$K,
    VOTLoginViaToken: VOTLoginViaToken$K,
    subtitlesSmartLayout: subtitlesSmartLayout$K,
    smartDucking: smartDucking$K,
    VOTAutoSubtitles: VOTAutoSubtitles$K,
    VOTSubtitlesFont: VOTSubtitlesFont$K
  };
  const recommended$J = "gomendatua";
  const translateVideo$J = "Itzuli bideoa";
  const disableTranslate$J = "Itzali";
  const translationSettings$J = "Itzulpenaren ezarpenak";
  const subtitlesSettings$J = "Azpitituluen ezarpenak";
  const resetSettings$J = "Berrezarri ezarpenak";
  const videoBeingTranslated$J = "Bideoa itzultzen ari dira";
  const videoLanguage$J = "Bideo hizkuntza";
  const translationLanguage$J = "Itzulpen hizkuntza";
  const translationTake$J = "Itzulpenak";
  const translationTakeMoreThanHour$J = "Itzulpenak ordubete baino gehiago iraungo du";
  const translationTakeAboutMinute$J = "Itzulpenak minutu bat iraungo du";
  const translationTakeFewMinutes$J = "Itzulpenak minutu batzuk iraungo du";
  const translationTakeApproximatelyMinutes$J = "Itzulpenak gutxi gorabehera {0} minutu hartuko ditu";
  const translationTakeApproximatelyMinute$J = "Itzulpenak gutxi gorabehera {0} minutu hartuko ditu";
  const requestTranslationFailed$J = "Huts egin du bideoaren itzulpena eskatzean";
  const audioNotReceived$J = "Audio esteka ez da jaso";
  const audioFormatNotSupported$J = "Audio formatua ez dago onartuta";
  const VOTAutoTranslate$J = "Itzuli irekita";
  const VOTDontTranslateYourLang$J = "Ez itzuli nire hizkuntzatik";
  const VOTVolume$J = "Bideoaren bolumena:";
  const VOTVolumeTranslation$J = "Itzulpen bolumena:";
  const VOTAutoSetVolume$J = "Murriztu bideo-bolumena";
  const VOTShowVideoSlider$J = "Bideo-bolumenaren graduatzailea";
  const VOTSyncVolume$J = "Esteka itzulpena eta bideo bolumena";
  const VOTDisableFromYourLang$J = "Bideoaren itzulpena desgaitu duzu zure hizkuntzan";
  const VOTVideoIsTooLong$J = "Bideoa luzeegia da";
  const VOTNoVideoIDFound$J = "Ez da bideorik aurkitu";
  const VOTSubtitles$J = "Azpitituluak";
  const VOTSubtitlesDisabled$J = "Desgaituta";
  const VOTSubtitlesMaxLength$J = "Azpitituluak gehienezko luzera";
  const VOTHighlightWords$J = "Nabarmendu hitzak";
  const VOTTranslatedFrom$J = "itzulpena:";
  const VOTAutogenerated$J = "autogeneratua";
  const VOTSettings$J = "BOTOAREN Ezarpenak";
  const VOTMenuLanguage$J = "Menu hizkuntza";
  const VOTAuthors$J = "Egileak";
  const VOTVersion$J = "Bertsioa";
  const VOTLoader$J = "Kargatzailea";
  const VOTBrowser$J = "Arakatzailea";
  const VOTShowPiPButton$J = "Erakutsi PiP botoia";
  const langs$J = { "auto": "Automatikoa", "af": "Afrikaans", "ak": "Akan izena", "sq": "Albaniera", "am": "Amhariera", "ar": "Arabiera", "hy": "Armeniera", "as": "Assamera", "ay": "Aimara izena", "az": "Azerbaijango", "bn": "Bangla", "eu": "Euskara", "be": "Belarusiera", "bho": "Bhojpuri izena", "bs": "Bosniera", "bg": "Bulgariera", "my": "Birmaniera", "ca": "Katalana", "ceb": "Zebuanoa", "zh": "Txinera", "zh-Hans": "Txinera (Sinplifikatua)", "zh-Hant": "Txinera (Tradizionala)", "co": "Korsikera", "hr": "Kroaziera", "cs": "Txekiera", "da": "Daniera", "dv": "Divehi izena", "nl": "Holandera", "en": "Ingelesa", "eo": "Esperantoa", "et": "Estoniera", "ee": "Ardiak", "fil": "Filipinetako", "fi": "Finlandiera", "fr": "Frantsesa", "gl": "Galiziera", "lg": "Ganda izena", "ka": "Georgiera", "de": "Alemaniera", "el": "Greziera", "gn": "Guarani izena", "gu": "Gujaratiera", "ht": "Haitiko Kreola", "ha": "Hausa izena", "haw": "Hawaiarra", "iw": "Hebreera", "hi": "Hindiera", "hmn": "Hmongak", "hu": "Hungariarra", "is": "Islandiera", "ig": "Igboak", "id": "Indonesieraz", "ga": "Irlandarra", "it": "Italiera", "ja": "Japoniera", "jv": "Javanera", "kn": "Kannada izena", "kk": "Kazakheraz", "km": "Khmerra", "rw": "Kinyarwanda izena", "ko": "Koreera", "kri": "Krio, ez egin hori.", "ku": "Kurduak", "ky": "Kirgizistan", "lo": "Lao, ez egin hori.", "la": "Latina", "lv": "Letoniera", "ln": "Lingala izena", "lt": "Lituaniera", "lb": "Luxenburgo", "mk": "Mazedoniera", "mg": "Madagaskarra", "ms": "Malaikarra", "ml": "Malayalam, ez egin hori.", "mt": "Maltako", "mi": "Maoriak", "mr": "Marathi izena", "mn": "Mongoliarra", "ne": "Nepaldarra", "nso": "Iparraldeko Sotho", "no": "Norvegiarra", "ny": "Nyanja izena", "or": "Odia, ez egin hori.", "om": "Oromo, ez egin hori.", "ps": "Pashto izena", "fa": "Persiera", "pl": "Poloniarra", "pt": "Portugesa", "pa": "Punjabiera", "qu": "Kexua", "ro": "Errumaniera", "ru": "Errusiera", "sm": "Samoarra", "sa": "Sanskritera", "gd": "Eskoziako Gaelera", "sr": "Serbiarra", "sn": "Shona izena", "sd": "Sindhi izena", "si": "Sinhala izena", "sk": "Eslovakiera", "sl": "Esloveniera", "so": "Somaliera", "st": "Hego Sotho", "es": "Espainiera", "su": "Sundanese izena", "sw": "Swahili izena", "sv": "Suediera", "tg": "Tadjikera", "ta": "Tamileraz", "tt": "Tatarra", "te": "Teluguak", "th": "Tailandiera", "ti": "Tigrina", "ts": "Tsonga izena", "tr": "Turkiera", "tk": "Turkmenak", "uk": "Ukrainarra", "ur": "Urdua", "ug": "Uigurrek", "uz": "Zbekera", "vi": "Vietnamdarra", "cy": "Galesera", "fy": "Mendebaldeko Frisiera", "xh": "Xhosa izena", "yi": "Jidizea", "yo": "Joruba izena", "zu": "Zuluak" };
  const streamNoConnectionToServer$J = "Ez dago konexiorik zerbitzariarekin";
  const searchField$J = "Bilatu...";
  const VOTTranslateAPIErrors$J = "Itzuli ERROREAK APITIK";
  const VOTDetectService$J = "Hizkuntza detektatzeko zerbitzua";
  const VOTProxyWorkerHost$J = "Sartu ordezko langilearen helbidea";
  const VOTM3u8ProxyHost$J = "Sartu m3u8 ordezkariaren helbidea";
  const proxySettings$J = "Ordezko Ezarpenak";
  const translationTakeApproximatelyMinute2$J = "Itzulpenak gutxi gorabehera {0} minutu hartuko ditu";
  const VOTAudioBooster$J = "Luzatu itzulpen bolumena handitzea";
  const VOTSubtitlesDesign$J = "Azpitituluen diseinua";
  const VOTSubtitlesFontSize$J = "Azpitituluen letra-tamaina";
  const VOTSubtitlesOpacity$J = "Azpitituluen atzeko planoaren gardentasuna";
  const VOTSubtitlesDownloadFormat$J = "Azpitituluak deskargatzeko formatua";
  const VOTDownloadWithName$J = "Deskargatu bideoaren izena duten fitxategiak";
  const VOTUpdateLocaleFiles$J = "Eguneratu lokalizazio-fitxategiak";
  const VOTLocaleHash$J = "Lokaleko hash-a";
  const VOTUpdatedAt$J = "etan eguneratua";
  const VOTNeedWebAudioAPI$J = "Hau gaitzeko, Audio API Bat Eduki Behar duzu";
  const VOTMediaCSPEnabledOnSite$J = "Multimedia KSP gune honetan gaituta dago";
  const VOTOnlyBypassMediaCSP$J = "Erabili Media ksp saihesteko bakarrik";
  const VOTNewAudioPlayer$J = "Erabili audio-erreproduzitzaile berria";
  const VOTUseNewModel$J = "Erabili Yandex ahotsen aldaera esperimentala bideo batzuetarako";
  const TranslationDelayed$J = "Itzulpena atzeratu egin da";
  const VOTTranslationCompletedNotify$J = "{0} (r) en itzulpena osatu da!";
  const VOTSendNotifyOnComplete$J = "Bidali bideoa itzuli dela jakinarazpena";
  const VOTBugReport$J = "Errore baten berri eman";
  const VOTTranslateProxyDisabled$J = "Desgaituta";
  const VOTTranslateProxyEnabled$J = "Gaituta";
  const VOTTranslateProxyEverything$J = "Ordezko dena";
  const VOTTranslateProxyStatus$J = "Ordezko modua";
  const VOTTranslatedBy$J = "Itzulpena: {0}";
  const VOTStreamNotAvailable$J = "Translate korrontea ez dago erabilgarri";
  const VOTTranslationTextService$J = "Testu itzulpen zerbitzua";
  const VOTNotAffectToVoice$J = "Ez du eraginik testuaren itzulpenean";
  const DontTranslateSelectedLanguages$J = "Ez itzuli hautatutako hizkuntzetatik";
  const showVideoVolumeSlider$J = "Bistaratu bideo-bolumenaren graduatzailea";
  const hotkeysSettings$J = "Laster-teklen ezarpenak";
  const None$J = "Bat ere ez";
  const VOTUseLivelyVoice$J = "Erabili ahots biziak. Hizlariek errusiar natiboak dirudite.";
  const miscSettings$J = "Misc ezarpenak";
  const services$J = { "yandexbrowser": "Yandex Arakatzailea", "msedge": "Microsoft Ertza", "rust-server": "Herdoilketa Zerbitzaria" };
  const aboutExtension$J = "Luzapenari buruz";
  const appearance$J = "Itxura";
  const buttonPosition$J = "Jokalariaren botoiaren posizioa";
  const position$J = { "left": "Ezkerrera", "right": "Ezta", "top": "Goian", "default": "Lehenetsia" };
  const secs$J = "segundoak";
  const autoHideButtonDelay$J = "Atzerapena itzultzeko botoia ezkutatu aurretik";
  const notFound$J = "ez da aurkitu";
  const minButtonPositionContainer$J = "Botoiaren posizioa 600 pixel baino handiagoak diren jokalarietan bakarrik aldatzen da.";
  const VOTTranslateProxyStatusDefault$J = "Zure herrialdean ordezkaritza erabat desgaitzeak luzapena hautsi dezake";
  const PressTheKeyCombination$J = "Sakatu tekla konbinazioa...";
  const VOTUseAudioDownload$J = "Erabili audio deskarga";
  const VOTUseAudioDownloadWarning$J = "Audio deskargak desgaitzeak luzapenaren funtzionaltasunean eragina izan dezake";
  const VOTAccountRequired$J = "Saioa hasi behar duzu ezaugarri hau erabiltzeko";
  const VOTMyAccount$J = "Nire kontua";
  const VOTLogin$J = "Saioa hasi";
  const VOTLogout$J = "Saioa amaitzea";
  const VOTRefresh$J = "Freskatu";
  const VOTYandexToken$J = "Sartu Yandex Oautheko Tokenera";
  const VOTYandexTokenInfo$J = "Kontu-tokena eskuz ezar dezakezu eremu honetan. Kontuan izan ez dugula bere baliozkotasuna egiaztatzen itzulpen eskaera bat bidali aurretik";
  const VOTLoginViaToken$J = "Saioa hasi token bidez";
  const subtitlesSmartLayout$J = "Azpitituluen diseinu adimenduna";
  const smartDucking$J = "Bolumen egokitzailea";
  const VOTAutoSubtitles$J = "Azpitituluak irekita";
  const VOTSubtitlesFont$J = "Azpitituluaren letra-tipoa";
  const __vite_glob_0_16 = {
    recommended: recommended$J,
    translateVideo: translateVideo$J,
    disableTranslate: disableTranslate$J,
    translationSettings: translationSettings$J,
    subtitlesSettings: subtitlesSettings$J,
    resetSettings: resetSettings$J,
    videoBeingTranslated: videoBeingTranslated$J,
    videoLanguage: videoLanguage$J,
    translationLanguage: translationLanguage$J,
    translationTake: translationTake$J,
    translationTakeMoreThanHour: translationTakeMoreThanHour$J,
    translationTakeAboutMinute: translationTakeAboutMinute$J,
    translationTakeFewMinutes: translationTakeFewMinutes$J,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$J,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$J,
    requestTranslationFailed: requestTranslationFailed$J,
    audioNotReceived: audioNotReceived$J,
    audioFormatNotSupported: audioFormatNotSupported$J,
    VOTAutoTranslate: VOTAutoTranslate$J,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$J,
    VOTVolume: VOTVolume$J,
    VOTVolumeTranslation: VOTVolumeTranslation$J,
    VOTAutoSetVolume: VOTAutoSetVolume$J,
    VOTShowVideoSlider: VOTShowVideoSlider$J,
    VOTSyncVolume: VOTSyncVolume$J,
    VOTDisableFromYourLang: VOTDisableFromYourLang$J,
    VOTVideoIsTooLong: VOTVideoIsTooLong$J,
    VOTNoVideoIDFound: VOTNoVideoIDFound$J,
    VOTSubtitles: VOTSubtitles$J,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$J,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$J,
    VOTHighlightWords: VOTHighlightWords$J,
    VOTTranslatedFrom: VOTTranslatedFrom$J,
    VOTAutogenerated: VOTAutogenerated$J,
    VOTSettings: VOTSettings$J,
    VOTMenuLanguage: VOTMenuLanguage$J,
    VOTAuthors: VOTAuthors$J,
    VOTVersion: VOTVersion$J,
    VOTLoader: VOTLoader$J,
    VOTBrowser: VOTBrowser$J,
    VOTShowPiPButton: VOTShowPiPButton$J,
    langs: langs$J,
    streamNoConnectionToServer: streamNoConnectionToServer$J,
    searchField: searchField$J,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$J,
    VOTDetectService: VOTDetectService$J,
    VOTProxyWorkerHost: VOTProxyWorkerHost$J,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$J,
    proxySettings: proxySettings$J,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$J,
    VOTAudioBooster: VOTAudioBooster$J,
    VOTSubtitlesDesign: VOTSubtitlesDesign$J,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$J,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$J,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$J,
    VOTDownloadWithName: VOTDownloadWithName$J,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$J,
    VOTLocaleHash: VOTLocaleHash$J,
    VOTUpdatedAt: VOTUpdatedAt$J,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$J,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$J,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$J,
    VOTNewAudioPlayer: VOTNewAudioPlayer$J,
    VOTUseNewModel: VOTUseNewModel$J,
    TranslationDelayed: TranslationDelayed$J,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$J,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$J,
    VOTBugReport: VOTBugReport$J,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$J,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$J,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$J,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$J,
    VOTTranslatedBy: VOTTranslatedBy$J,
    VOTStreamNotAvailable: VOTStreamNotAvailable$J,
    VOTTranslationTextService: VOTTranslationTextService$J,
    VOTNotAffectToVoice: VOTNotAffectToVoice$J,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$J,
    showVideoVolumeSlider: showVideoVolumeSlider$J,
    hotkeysSettings: hotkeysSettings$J,
    None: None$J,
    VOTUseLivelyVoice: VOTUseLivelyVoice$J,
    miscSettings: miscSettings$J,
    services: services$J,
    aboutExtension: aboutExtension$J,
    appearance: appearance$J,
    buttonPosition: buttonPosition$J,
    position: position$J,
    secs: secs$J,
    autoHideButtonDelay: autoHideButtonDelay$J,
    notFound: notFound$J,
    minButtonPositionContainer: minButtonPositionContainer$J,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$J,
    PressTheKeyCombination: PressTheKeyCombination$J,
    VOTUseAudioDownload: VOTUseAudioDownload$J,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$J,
    VOTAccountRequired: VOTAccountRequired$J,
    VOTMyAccount: VOTMyAccount$J,
    VOTLogin: VOTLogin$J,
    VOTLogout: VOTLogout$J,
    VOTRefresh: VOTRefresh$J,
    VOTYandexToken: VOTYandexToken$J,
    VOTYandexTokenInfo: VOTYandexTokenInfo$J,
    VOTLoginViaToken: VOTLoginViaToken$J,
    subtitlesSmartLayout: subtitlesSmartLayout$J,
    smartDucking: smartDucking$J,
    VOTAutoSubtitles: VOTAutoSubtitles$J,
    VOTSubtitlesFont: VOTSubtitlesFont$J
  };
  const recommended$I = "توصیه شده";
  const translateVideo$I = "ترجمه ویدیو";
  const disableTranslate$I = "غیرفعال کردن";
  const translationSettings$I = "تنظیمات ترجمه";
  const subtitlesSettings$I = "تنظیمات زیرنویس";
  const resetSettings$I = "تنظیمات را بازنشانی کنید";
  const videoBeingTranslated$I = "ویدیو در حال ترجمه است";
  const videoLanguage$I = "زبان ویدیو";
  const translationLanguage$I = "زبان ترجمه";
  const translationTake$I = "ترجمه زمان می برد";
  const translationTakeMoreThanHour$I = "ترجمه بیشتر از یک ساعت طول می کشد";
  const translationTakeAboutMinute$I = "ترجمه حدود یک دقیقه طول می کشد";
  const translationTakeFewMinutes$I = "ترجمه چند دقیقه طول می کشد";
  const translationTakeApproximatelyMinutes$I = "ترجمه تقریباً {0} دقیقه طول می کشد";
  const translationTakeApproximatelyMinute$I = "ترجمه تقریباً {0} دقیقه طول می کشد";
  const requestTranslationFailed$I = "امکان درخواست ترجمه ویدیو وجود ندارد";
  const audioNotReceived$I = "پیدا نشدن پیوند صوتی";
  const audioFormatNotSupported$I = "قالب صوتی پشتیبانی نمی شود";
  const VOTAutoTranslate$I = "ترجمه خودکار";
  const VOTDontTranslateYourLang$I = "از زبان مادری خود ترجمه نکنید";
  const VOTVolume$I = "حجم ویدیو:";
  const VOTVolumeTranslation$I = "حجم ترجمه:";
  const VOTAutoSetVolume$I = "کاهش حجم ویدیو به";
  const VOTShowVideoSlider$I = "نمایش لغزنده ویدیو";
  const VOTSyncVolume$I = "هماهنگ کردن حجم ترجمه و ویدیو";
  const VOTDisableFromYourLang$I = "ترجمه ویدیو را از زبان خود غیرفعال کرده اید";
  const VOTVideoIsTooLong$I = "ویدیو بیشتر از حد طولانی است";
  const VOTNoVideoIDFound$I = "هیچ شناسه ویدیویی پیدا نشد";
  const VOTSubtitles$I = "زیرنویس";
  const VOTSubtitlesDisabled$I = "غیرفعال شده است";
  const VOTSubtitlesMaxLength$I = "حداکثر طول زیرنویس";
  const VOTHighlightWords$I = "روشن کردن واژه ها";
  const VOTTranslatedFrom$I = "ترجمه شده از";
  const VOTAutogenerated$I = "تولید شده به صورت خودکار";
  const VOTSettings$I = "تنظیمات VOT";
  const VOTMenuLanguage$I = "زبان منو";
  const VOTAuthors$I = "نویسندگان";
  const VOTVersion$I = "نسخه";
  const VOTLoader$I = "لودر";
  const VOTBrowser$I = "مرورگر";
  const VOTShowPiPButton$I = "نمایش دکمه PiP";
  const langs$I = { "auto": "بطور خودکار", "az": "آذربایجانی", "ay": "آیمارا", "ak": "آکان", "sq": "آلبانیایی", "am": "آمهری", "en": "انگلیسی", "ar": "عربی", "hy": "ارمنی", "as": "آسامی", "af": "آفریکانس", "eu": "باسکی", "be": "بلاروسی", "bn": "بنگالی", "my": "برمه‌ای", "bg": "بلغاری", "bs": "بوسنیایی", "bho": "بھوجپوری", "cy": "ویلزی", "hu": "مجارستانی", "vi": "ویتنامی", "haw": "هاوایی", "ht": "هائیتیایی", "gl": "گالیسی", "lg": "گاندا", "el": "یونانی", "ka": "گرجی", "gn": "گوارانی", "gu": "گجراتی", "gd": "گیلی اسکاتلندی", "da": "دنمارکی", "fy": "فریزی غربی", "zu": "زولو", "iw": "عبری", "ig": "ایگبو", "yi": "یدیش", "id": "اندونزیایی", "ga": "آیرلندی", "is": "ایسلندی", "es": "اسپانیایی", "it": "ایتالیایی", "yo": "یوروبا", "kk": "قزاقی", "kn": "کانارا", "ca": "کاتالان", "qu": "کچوا", "rw": "کینیارواندا", "ky": "قرقیزی", "zh": "چینی", "zh-Hant": "چینی سنتی (تقلیدی)", "zh-Hans": "چینی ساده شده (نئوشده)", "ko": "کره ای", "co": "کرسی", "xh": "خوسا", "ku": "کردی", "km": "خمری", "lo": "لاوی", "la": "لاتین", "lv": "لتونی", "ln": "لینگالا", "lt": "لیتوانیایی", "lb": "لوکزامبورگ", "mk": "مقدونی", "mg": "مدغاسکری", "ms": "مالایی", "ml": "مالایالامی", "dv": "مالدیوی", "mt": "مالتی", "mi": "مائوری", "mr": "مراتی", "mn": "مغولی", "de": "آلمانی", "ne": "نپالی", "nl": "هلندی", "no": "نروژی", "ny": "نیانجا", "or": "اوریا", "om": "اورومو", "pa": "پنجابی", "fa": "فارسی", "pl": "لهستانی", "pt": "پرتغالی", "ps": "پشتو", "ro": "رومانیایی", "ru": "روسی", "sm": "ساموآیی", "sa": "سانسکریت", "ceb": "سبوانو", "nso": "سوتو شمالی", "sr": "صربی", "si": "سینهالی", "sd": "سندی", "sk": "اسلواکی", "sl": "اسلوونیایی", "so": "سومالیایی", "sw": "سواحلی", "su": "سوندایی", "tg": "تاجیکی", "th": "تایلندی", "ta": "تامیلی", "tt": "تاتاری", "te": "تلوگویی", "ti": "تیگرینیایی", "ts": "تسونگایی", "tr": "ترکی استانبولی", "tk": "ترکمنی", "uz": "اوزبکی", "ug": "اویغوری", "uk": "اوکراینی", "ur": "اردو", "fil": "فیلیپینی", "fi": "فنلاندی", "fr": "فرانسوی", "ha": "هوسایی", "hi": "هندی", "hmn": "همونگ", "hr": "کرواتی", "cs": "چکی", "sv": "سوئدی", "sn": "شونا", "ee": "ایوی", "eo": "اسپرانتو", "et": "استونیایی", "st": "سوتویی جنوبی", "jv": "جاوه ای", "ja": "ژاپنی", "kri": "کری" };
  const streamNoConnectionToServer$I = "هیچ ارتباطی با سرور وجود ندارد";
  const searchField$I = "جستجو کن..";
  const VOTTranslateAPIErrors$I = "ترجمه خطاها از API";
  const VOTDetectService$I = "سرویس تشخیص زبان";
  const VOTProxyWorkerHost$I = "آدرس کارگر پروکسی را وارد کنید";
  const VOTM3u8ProxyHost$I = "آدرس کارگر پروکسی m3u8 را وارد کنید";
  const proxySettings$I = "تنظیمات پروکسی";
  const translationTakeApproximatelyMinute2$I = "ترجمه تقریبا {0} دقیقه طول خواهد کشید";
  const VOTAudioBooster$I = "افزایش حجم ترجمه گسترده";
  const VOTSubtitlesDesign$I = "طراحی زیرنویس";
  const VOTSubtitlesFontSize$I = "اندازه فونت زیرنویس";
  const VOTSubtitlesOpacity$I = "شفافیت پس زمینه زیرنویس";
  const VOTSubtitlesDownloadFormat$I = "فرمت دانلود زیرنویس";
  const VOTDownloadWithName$I = "دانلود فایل با نام تصویری";
  const VOTUpdateLocaleFiles$I = "به روز رسانی فایل های محلی سازی";
  const VOTLocaleHash$I = "هش محلی";
  const VOTUpdatedAt$I = "به روز رسانی شده در";
  const VOTNeedWebAudioAPI$I = "برای فعال کردن این ، شما باید یک API صوتی وب داشته باشید";
  const VOTMediaCSPEnabledOnSite$I = "Media CSP در این سایت فعال شده است";
  const VOTOnlyBypassMediaCSP$I = "از آن فقط برای دور زدن Csp رسانه استفاده کنید";
  const VOTNewAudioPlayer$I = "از پخش کننده صوتی جدید استفاده کنید";
  const VOTUseNewModel$I = "برای برخی از فیلم ها از یک تغییر تجربی از صداهای Yandex استفاده کنید";
  const TranslationDelayed$I = "ترجمه کمی به تاخیر افتاده است";
  const VOTTranslationCompletedNotify$I = "ترجمه در مورد {0} تکمیل شده است!";
  const VOTSendNotifyOnComplete$I = "ارسال یک اطلاعیه که ویدیو ترجمه شده است";
  const VOTBugReport$I = "گزارش یک اشکال";
  const VOTTranslateProxyDisabled$I = "معلول";
  const VOTTranslateProxyEnabled$I = "فعال شده";
  const VOTTranslateProxyEverything$I = "پروکسی همه چیز";
  const VOTTranslateProxyStatus$I = "حالت پروکسی";
  const VOTTranslatedBy$I = "ترجمه شده توسط {0}";
  const VOTStreamNotAvailable$I = "Translate stream در دسترس نیست";
  const VOTTranslationTextService$I = "سرویس ترجمه متون";
  const VOTNotAffectToVoice$I = "بر ترجمه متن در صدا تاثیر نمی گذارد";
  const DontTranslateSelectedLanguages$I = "از زبان های انتخاب شده ترجمه نکنید";
  const showVideoVolumeSlider$I = "نمایش اسلایدر حجم فیلم";
  const hotkeysSettings$I = "تنظیمات کلید های میانبر";
  const None$I = "هیچ";
  const VOTUseLivelyVoice$I = "از صداهای پر جنب و جوش استفاده کنید. گویندگان مانند روس های بومی صدا می کنند.";
  const miscSettings$I = "تنظیمات Misc";
  const services$I = { "yandexbrowser": "مرورگر Yandex", "msedge": "مایکروسافت اج", "rust-server": "سرور زنگ" };
  const aboutExtension$I = "درباره تمدید";
  const appearance$I = "ظاهر";
  const buttonPosition$I = "موقعیت دکمه در بازیکن";
  const position$I = { "left": "چپ", "right": "درسته ؟ ", "top": "بالا", "default": "پیش فرض" };
  const secs$I = "ثانیه";
  const autoHideButtonDelay$I = "تاخیر قبل از پنهان کردن دکمه ترجمه";
  const notFound$I = "یافت نشد";
  const minButtonPositionContainer$I = "موقعیت دکمه فقط در بازیکنان بزرگتر از 600 پیکسل تغییر می کند.";
  const VOTTranslateProxyStatusDefault$I = "غیرفعال کردن کامل پروکسی در کشور شما ممکن است تمدید را بشکند";
  const PressTheKeyCombination$I = "کلید ترکیبی را فشار دهید...";
  const VOTUseAudioDownload$I = "استفاده از دانلود صوتی";
  const VOTUseAudioDownloadWarning$I = "غیرفعال کردن دانلود های صوتی ممکن است بر عملکرد افزونه تأثیر بگذارد";
  const VOTAccountRequired$I = "برای استفاده از این ویژگی باید وارد سیستم شوید";
  const VOTMyAccount$I = "حساب کاربری من";
  const VOTLogin$I = "ورود به سایت";
  const VOTLogout$I = "خروج از سیستم";
  const VOTRefresh$I = "تازه کردن";
  const VOTYandexToken$I = "توکن Yandex OAuth را وارد کنید";
  const VOTYandexTokenInfo$I = "شما می توانید توکن حساب را به صورت دستی در این فیلد تنظیم کنید. لطفا توجه داشته باشید که ما اعتبار آن را قبل از ارسال درخواست ترجمه بررسی نمی کنیم";
  const VOTLoginViaToken$I = "ورود از طریق توکن";
  const subtitlesSmartLayout$I = "طرح زیرنویس هوشمند";
  const smartDucking$I = "حجم سازگاری";
  const VOTAutoSubtitles$I = "زیرنویس در open";
  const VOTSubtitlesFont$I = "فونت زیرنویس";
  const __vite_glob_0_17 = {
    recommended: recommended$I,
    translateVideo: translateVideo$I,
    disableTranslate: disableTranslate$I,
    translationSettings: translationSettings$I,
    subtitlesSettings: subtitlesSettings$I,
    resetSettings: resetSettings$I,
    videoBeingTranslated: videoBeingTranslated$I,
    videoLanguage: videoLanguage$I,
    translationLanguage: translationLanguage$I,
    translationTake: translationTake$I,
    translationTakeMoreThanHour: translationTakeMoreThanHour$I,
    translationTakeAboutMinute: translationTakeAboutMinute$I,
    translationTakeFewMinutes: translationTakeFewMinutes$I,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$I,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$I,
    requestTranslationFailed: requestTranslationFailed$I,
    audioNotReceived: audioNotReceived$I,
    audioFormatNotSupported: audioFormatNotSupported$I,
    VOTAutoTranslate: VOTAutoTranslate$I,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$I,
    VOTVolume: VOTVolume$I,
    VOTVolumeTranslation: VOTVolumeTranslation$I,
    VOTAutoSetVolume: VOTAutoSetVolume$I,
    VOTShowVideoSlider: VOTShowVideoSlider$I,
    VOTSyncVolume: VOTSyncVolume$I,
    VOTDisableFromYourLang: VOTDisableFromYourLang$I,
    VOTVideoIsTooLong: VOTVideoIsTooLong$I,
    VOTNoVideoIDFound: VOTNoVideoIDFound$I,
    VOTSubtitles: VOTSubtitles$I,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$I,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$I,
    VOTHighlightWords: VOTHighlightWords$I,
    VOTTranslatedFrom: VOTTranslatedFrom$I,
    VOTAutogenerated: VOTAutogenerated$I,
    VOTSettings: VOTSettings$I,
    VOTMenuLanguage: VOTMenuLanguage$I,
    VOTAuthors: VOTAuthors$I,
    VOTVersion: VOTVersion$I,
    VOTLoader: VOTLoader$I,
    VOTBrowser: VOTBrowser$I,
    VOTShowPiPButton: VOTShowPiPButton$I,
    langs: langs$I,
    streamNoConnectionToServer: streamNoConnectionToServer$I,
    searchField: searchField$I,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$I,
    VOTDetectService: VOTDetectService$I,
    VOTProxyWorkerHost: VOTProxyWorkerHost$I,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$I,
    proxySettings: proxySettings$I,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$I,
    VOTAudioBooster: VOTAudioBooster$I,
    VOTSubtitlesDesign: VOTSubtitlesDesign$I,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$I,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$I,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$I,
    VOTDownloadWithName: VOTDownloadWithName$I,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$I,
    VOTLocaleHash: VOTLocaleHash$I,
    VOTUpdatedAt: VOTUpdatedAt$I,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$I,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$I,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$I,
    VOTNewAudioPlayer: VOTNewAudioPlayer$I,
    VOTUseNewModel: VOTUseNewModel$I,
    TranslationDelayed: TranslationDelayed$I,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$I,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$I,
    VOTBugReport: VOTBugReport$I,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$I,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$I,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$I,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$I,
    VOTTranslatedBy: VOTTranslatedBy$I,
    VOTStreamNotAvailable: VOTStreamNotAvailable$I,
    VOTTranslationTextService: VOTTranslationTextService$I,
    VOTNotAffectToVoice: VOTNotAffectToVoice$I,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$I,
    showVideoVolumeSlider: showVideoVolumeSlider$I,
    hotkeysSettings: hotkeysSettings$I,
    None: None$I,
    VOTUseLivelyVoice: VOTUseLivelyVoice$I,
    miscSettings: miscSettings$I,
    services: services$I,
    aboutExtension: aboutExtension$I,
    appearance: appearance$I,
    buttonPosition: buttonPosition$I,
    position: position$I,
    secs: secs$I,
    autoHideButtonDelay: autoHideButtonDelay$I,
    notFound: notFound$I,
    minButtonPositionContainer: minButtonPositionContainer$I,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$I,
    PressTheKeyCombination: PressTheKeyCombination$I,
    VOTUseAudioDownload: VOTUseAudioDownload$I,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$I,
    VOTAccountRequired: VOTAccountRequired$I,
    VOTMyAccount: VOTMyAccount$I,
    VOTLogin: VOTLogin$I,
    VOTLogout: VOTLogout$I,
    VOTRefresh: VOTRefresh$I,
    VOTYandexToken: VOTYandexToken$I,
    VOTYandexTokenInfo: VOTYandexTokenInfo$I,
    VOTLoginViaToken: VOTLoginViaToken$I,
    subtitlesSmartLayout: subtitlesSmartLayout$I,
    smartDucking: smartDucking$I,
    VOTAutoSubtitles: VOTAutoSubtitles$I,
    VOTSubtitlesFont: VOTSubtitlesFont$I
  };
  const recommended$H = "suositella";
  const translateVideo$H = "Käännä video";
  const disableTranslate$H = "Sammuttaa";
  const translationSettings$H = "Käännösasetukset";
  const subtitlesSettings$H = "Tekstityksen asetukset";
  const resetSettings$H = "Nollaa asetukset";
  const videoBeingTranslated$H = "Videota käännetään parhaillaan";
  const videoLanguage$H = "Videokieli";
  const translationLanguage$H = "Käännöskieli";
  const translationTake$H = "Käännös vie";
  const translationTakeMoreThanHour$H = "Käännös kestää yli tunnin";
  const translationTakeAboutMinute$H = "Käännös kestää noin minuutin";
  const translationTakeFewMinutes$H = "Käännös vie muutaman minuutin";
  const translationTakeApproximatelyMinutes$H = "Käännös vie noin {0} minuuttia";
  const translationTakeApproximatelyMinute$H = "Käännös vie noin {0} minuuttia";
  const requestTranslationFailed$H = "Videon käännöksen pyyntö epäonnistui";
  const audioNotReceived$H = "Äänilinkkiä ei vastaanotettu";
  const audioFormatNotSupported$H = "Äänimuotoa ei tueta";
  const VOTAutoTranslate$H = "Käännä auki";
  const VOTDontTranslateYourLang$H = "Älä käännä kielestäni";
  const VOTVolume$H = "Videon äänenvoimakkuus:";
  const VOTVolumeTranslation$H = "Käännösmäärä:";
  const VOTAutoSetVolume$H = "Vähennä videon äänenvoimakkuutta";
  const VOTShowVideoSlider$H = "Videon äänenvoimakkuuden liukusäädin";
  const VOTSyncVolume$H = "Linkin käännös ja videon äänenvoimakkuus";
  const VOTDisableFromYourLang$H = "Olet poistanut videon käännöksen käytöstä omalla kielelläsi";
  const VOTVideoIsTooLong$H = "Video on liian pitkä";
  const VOTNoVideoIDFound$H = "VIDEOTUNNISTETTA ei löytynyt";
  const VOTSubtitles$H = "Tekstitykset";
  const VOTSubtitlesDisabled$H = "Käytöstä";
  const VOTSubtitlesMaxLength$H = "Tekstitykset max length";
  const VOTHighlightWords$H = "Korosta sanoja";
  const VOTTranslatedFrom$H = "suomennos:";
  const VOTAutogenerated$H = "autogeneroitu";
  const VOTSettings$H = "VOT-Asetukset";
  const VOTMenuLanguage$H = "Valikon kieli";
  const VOTAuthors$H = "Tekijä";
  const VOTVersion$H = "Versio";
  const VOTLoader$H = "Loader";
  const VOTBrowser$H = "Selain";
  const VOTShowPiPButton$H = "Näytä piip-painike";
  const langs$H = { "auto": "Auto", "af": "Afrikaans", "ak": "Akan", "sq": "Albania", "am": "Amharic", "ar": "Arabia", "hy": "Armenia", "as": "Assamilainen", "ay": "Aymara", "az": "Azerbaidžanin", "bn": "Bangla", "eu": "Baski", "be": "Valko", "bho": "Bhojpuri", "bs": "Bosnia", "bg": "Bulgaria", "my": "Burma", "ca": "Katalaani", "ceb": "Cebuano", "zh": "Kiina", "zh-Hans": "Kiina (Yksinkertaistettu)", "zh-Hant": "Kiina (Perinteinen)", "co": "Korsika", "hr": "Kroatia", "cs": "Tšekki", "da": "Tanska", "dv": "Divehi", "nl": "Hollanti", "en": "Englanti", "eo": "Esperanto", "et": "Viro", "ee": "Uuhi", "fil": "Filipino", "fi": "Suomalainen", "fr": "Ranska", "gl": "Galicia", "lg": "Ganda", "ka": "Georgia", "de": "Saksa", "el": "Kreikka", "gn": "Guarani", "gu": "Gudžarati", "ht": "Haitin Kreoli", "ha": "Hausa", "haw": "Havaiji", "iw": "Heprean", "hi": "Hindi", "hmn": "Hmong", "hu": "Unkari", "is": "Islanti", "ig": "Igbo", "id": "Indonesia", "ga": "Irlantilaisen", "it": "Italia", "ja": "Japani", "jv": "Jaavalainen", "kn": "Kannada", "kk": "Kazakstan", "km": "Khmer", "rw": "Kinyarwanda", "ko": "Korea", "kri": "Krio", "ku": "Kurdi", "ky": "Kirgisia", "lo": "Lao", "la": "Latinalainen", "lv": "Latvia", "ln": "Lingala", "lt": "Liettua", "lb": "Luxemburg", "mk": "Makedonia", "mg": "Madagaskar", "ms": "Malaji", "ml": "Malajalam", "mt": "Malta", "mi": "Māori", "mr": "Marathi", "mn": "Mongolia", "ne": "Nepali", "nso": "Pohjois-Sotho", "no": "Norja", "ny": "Njanja", "or": "Odia", "om": "Oromo", "ps": "Pašto", "fa": "Persia", "pl": "Puola", "pt": "Portugali", "pa": "Punjabi", "qu": "Ketšua", "ro": "Romania", "ru": "Venäjä", "sm": "Samoa", "sa": "Sanskrit", "gd": "Skotlantilainen Gaeli", "sr": "Serbia", "sn": "Shona", "sd": "Sindhi", "si": "Sinhala", "sk": "Slovakia", "sl": "Sloveenin", "so": "Somali", "st": "Etelä-Sotho", "es": "Espanjan", "su": "Sundanese", "sw": "Swahili", "sv": "Ruotsalainen", "tg": "Tadžikki", "ta": "Tamili", "tt": "Tatar", "te": "Telugu", "th": "Thai", "ti": "Tigrinja", "ts": "Tsonga", "tr": "Turkkilainen", "tk": "Turkmeenit", "uk": "Ukraina", "ur": "Urdu", "ug": "Uiguuri", "uz": "Uzbekistan", "vi": "Vietamilainen", "cy": "Walesin", "fy": "Länsifriisi", "xh": "Xhosa", "yi": "Jiddiš", "yo": "Joruba", "zu": "Zulu" };
  const streamNoConnectionToServer$H = "Palvelimeen ei ole yhteyttä";
  const searchField$H = "Etsiä...";
  const VOTTranslateAPIErrors$H = "Käännä virheet API: sta";
  const VOTDetectService$H = "Kielen tunnistuspalvelu";
  const VOTProxyWorkerHost$H = "Anna valtakirjan työntekijän osoite";
  const VOTM3u8ProxyHost$H = "Anna M3U8-valtakirjan työntekijän osoite";
  const proxySettings$H = "Välityspalvelimen Asetukset";
  const translationTakeApproximatelyMinute2$H = "Käännös kestää noin {0} minuuttia";
  const VOTAudioBooster$H = "Laajennettu käännösmäärä kasvaa";
  const VOTSubtitlesDesign$H = "Tekstitysten suunnittelu";
  const VOTSubtitlesFontSize$H = "Tekstityksen kirjasinkoko";
  const VOTSubtitlesOpacity$H = "Tekstityksen Taustan läpinäkyvyys";
  const VOTSubtitlesDownloadFormat$H = "Tekstityksen lataamisen muoto";
  const VOTDownloadWithName$H = "Lataa tiedostoja videon nimellä";
  const VOTUpdateLocaleFiles$H = "Päivitä lokalisointitiedostot";
  const VOTLocaleHash$H = "Paikallinen hash";
  const VOTUpdatedAt$H = "Päivitetty klo";
  const VOTNeedWebAudioAPI$H = "Jotta voit ottaa tämän käyttöön, sinulla on oltava Web Audio API";
  const VOTMediaCSPEnabledOnSite$H = "Media CSP on käytössä tällä sivustolla";
  const VOTOnlyBypassMediaCSP$H = "Käytä sitä vain Media CSP: n ohittamiseen";
  const VOTNewAudioPlayer$H = "Käytä uutta soittinta";
  const VOTUseNewModel$H = "Käytä kokeellista muunnelmaa Yandex-äänistä joissakin videoissa";
  const TranslationDelayed$H = "Käännös viivästyy hieman";
  const VOTTranslationCompletedNotify$H = "Kohteen {0} käännös on valmis!";
  const VOTSendNotifyOnComplete$H = "Lähetä ilmoitus siitä, että video on käännetty";
  const VOTBugReport$H = "Ilmoita virheestä";
  const VOTTranslateProxyDisabled$H = "Käytöstä";
  const VOTTranslateProxyEnabled$H = "Käyttöön";
  const VOTTranslateProxyEverything$H = "Proxy kaikki";
  const VOTTranslateProxyStatus$H = "Välityspalvelin";
  const VOTTranslatedBy$H = "Kääntäjä {0}";
  const VOTStreamNotAvailable$H = "Translate stream ei ole käytettävissä";
  const VOTTranslationTextService$H = "Tekstin käännöspalvelu";
  const VOTNotAffectToVoice$H = "Ei vaikuta tekstin kääntämiseen voice Overissa";
  const DontTranslateSelectedLanguages$H = "Älä käännä valituista kielistä";
  const showVideoVolumeSlider$H = "Näytä videon äänenvoimakkuuden liukusäädin";
  const hotkeysSettings$H = "Pikanäppäinten asetukset";
  const None$H = "Kukaan";
  const VOTUseLivelyVoice$H = "Käytä eloisia ääniä. Puhujat kuulostavat syntyperäisiltä venäläisiltä.";
  const miscSettings$H = "Muut asetukset";
  const services$H = { "yandexbrowser": "Yandex-Selain", "msedge": "Microsoft Edge", "rust-server": "Rust-Palvelin" };
  const aboutExtension$H = "Tietoja laajennuksesta";
  const appearance$H = "Ulkonäkö";
  const buttonPosition$H = "Painikkeen sijainti soittimessa";
  const position$H = { "left": "Jättää", "right": "Oikea", "top": "Yläosa", "default": "Oletusarvo" };
  const secs$H = "sekuntia";
  const autoHideButtonDelay$H = "Viive ennen kääntämispainikkeen piilottamista";
  const notFound$H = "ei löytynyt";
  const minButtonPositionContainer$H = "Painikkeen sijainti muuttuu vain yli 600 pikselin pelaajilla.";
  const VOTTranslateProxyStatusDefault$H = "Välityspalvelimen poistaminen kokonaan käytöstä maassasi voi rikkoa laajennuksen";
  const PressTheKeyCombination$H = "Paina näppäinyhdistelmää...";
  const VOTUseAudioDownload$H = "Käytä äänen lataamista";
  const VOTUseAudioDownloadWarning$H = "Äänilatausten poistaminen käytöstä voi vaikuttaa laajennuksen toimivuuteen";
  const VOTAccountRequired$H = "Sinun on kirjauduttava sisään käyttääksesi tätä ominaisuutta";
  const VOTMyAccount$H = "Oma tili";
  const VOTLogin$H = "Kirjautuminen";
  const VOTLogout$H = "Logout";
  const VOTRefresh$H = "Päivitä";
  const VOTYandexToken$H = "Syötä Yandex OAuth-Token";
  const VOTYandexTokenInfo$H = "Voit asettaa tilin tunnuksen manuaalisesti tässä kentässä. Huomaa, että emme tarkista sen voimassaoloa ennen käännöspyynnön lähettämistä";
  const VOTLoginViaToken$H = "Kirjaudu Tokenin kautta";
  const subtitlesSmartLayout$H = "Älykäs tekstityksen asettelu";
  const smartDucking$H = "Mukautuva äänenvoimakkuus";
  const VOTAutoSubtitles$H = "Tekstitys auki";
  const VOTSubtitlesFont$H = "Tekstityksen kirjasin";
  const __vite_glob_0_18 = {
    recommended: recommended$H,
    translateVideo: translateVideo$H,
    disableTranslate: disableTranslate$H,
    translationSettings: translationSettings$H,
    subtitlesSettings: subtitlesSettings$H,
    resetSettings: resetSettings$H,
    videoBeingTranslated: videoBeingTranslated$H,
    videoLanguage: videoLanguage$H,
    translationLanguage: translationLanguage$H,
    translationTake: translationTake$H,
    translationTakeMoreThanHour: translationTakeMoreThanHour$H,
    translationTakeAboutMinute: translationTakeAboutMinute$H,
    translationTakeFewMinutes: translationTakeFewMinutes$H,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$H,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$H,
    requestTranslationFailed: requestTranslationFailed$H,
    audioNotReceived: audioNotReceived$H,
    audioFormatNotSupported: audioFormatNotSupported$H,
    VOTAutoTranslate: VOTAutoTranslate$H,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$H,
    VOTVolume: VOTVolume$H,
    VOTVolumeTranslation: VOTVolumeTranslation$H,
    VOTAutoSetVolume: VOTAutoSetVolume$H,
    VOTShowVideoSlider: VOTShowVideoSlider$H,
    VOTSyncVolume: VOTSyncVolume$H,
    VOTDisableFromYourLang: VOTDisableFromYourLang$H,
    VOTVideoIsTooLong: VOTVideoIsTooLong$H,
    VOTNoVideoIDFound: VOTNoVideoIDFound$H,
    VOTSubtitles: VOTSubtitles$H,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$H,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$H,
    VOTHighlightWords: VOTHighlightWords$H,
    VOTTranslatedFrom: VOTTranslatedFrom$H,
    VOTAutogenerated: VOTAutogenerated$H,
    VOTSettings: VOTSettings$H,
    VOTMenuLanguage: VOTMenuLanguage$H,
    VOTAuthors: VOTAuthors$H,
    VOTVersion: VOTVersion$H,
    VOTLoader: VOTLoader$H,
    VOTBrowser: VOTBrowser$H,
    VOTShowPiPButton: VOTShowPiPButton$H,
    langs: langs$H,
    streamNoConnectionToServer: streamNoConnectionToServer$H,
    searchField: searchField$H,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$H,
    VOTDetectService: VOTDetectService$H,
    VOTProxyWorkerHost: VOTProxyWorkerHost$H,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$H,
    proxySettings: proxySettings$H,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$H,
    VOTAudioBooster: VOTAudioBooster$H,
    VOTSubtitlesDesign: VOTSubtitlesDesign$H,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$H,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$H,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$H,
    VOTDownloadWithName: VOTDownloadWithName$H,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$H,
    VOTLocaleHash: VOTLocaleHash$H,
    VOTUpdatedAt: VOTUpdatedAt$H,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$H,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$H,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$H,
    VOTNewAudioPlayer: VOTNewAudioPlayer$H,
    VOTUseNewModel: VOTUseNewModel$H,
    TranslationDelayed: TranslationDelayed$H,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$H,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$H,
    VOTBugReport: VOTBugReport$H,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$H,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$H,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$H,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$H,
    VOTTranslatedBy: VOTTranslatedBy$H,
    VOTStreamNotAvailable: VOTStreamNotAvailable$H,
    VOTTranslationTextService: VOTTranslationTextService$H,
    VOTNotAffectToVoice: VOTNotAffectToVoice$H,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$H,
    showVideoVolumeSlider: showVideoVolumeSlider$H,
    hotkeysSettings: hotkeysSettings$H,
    None: None$H,
    VOTUseLivelyVoice: VOTUseLivelyVoice$H,
    miscSettings: miscSettings$H,
    services: services$H,
    aboutExtension: aboutExtension$H,
    appearance: appearance$H,
    buttonPosition: buttonPosition$H,
    position: position$H,
    secs: secs$H,
    autoHideButtonDelay: autoHideButtonDelay$H,
    notFound: notFound$H,
    minButtonPositionContainer: minButtonPositionContainer$H,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$H,
    PressTheKeyCombination: PressTheKeyCombination$H,
    VOTUseAudioDownload: VOTUseAudioDownload$H,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$H,
    VOTAccountRequired: VOTAccountRequired$H,
    VOTMyAccount: VOTMyAccount$H,
    VOTLogin: VOTLogin$H,
    VOTLogout: VOTLogout$H,
    VOTRefresh: VOTRefresh$H,
    VOTYandexToken: VOTYandexToken$H,
    VOTYandexTokenInfo: VOTYandexTokenInfo$H,
    VOTLoginViaToken: VOTLoginViaToken$H,
    subtitlesSmartLayout: subtitlesSmartLayout$H,
    smartDucking: smartDucking$H,
    VOTAutoSubtitles: VOTAutoSubtitles$H,
    VOTSubtitlesFont: VOTSubtitlesFont$H
  };
  const recommended$G = "recommande";
  const translateVideo$G = "Traduire la vidéo";
  const disableTranslate$G = "Désactiver";
  const translationSettings$G = "Paramètres de traduction";
  const subtitlesSettings$G = "Paramètres des sous-titres";
  const resetSettings$G = "Réinitialiser les paramètres";
  const videoBeingTranslated$G = "La vidéo est en cours de traduction";
  const videoLanguage$G = "Langue vidéo";
  const translationLanguage$G = "Langue cible";
  const translationTake$G = "La traduction prendra";
  const translationTakeMoreThanHour$G = "La traduction prendra plus d'une heure";
  const translationTakeAboutMinute$G = "La traduction prendra environ une minute";
  const translationTakeFewMinutes$G = "La traduction prendra quelques minutes";
  const translationTakeApproximatelyMinutes$G = "La traduction prendra environ {0} minutes";
  const translationTakeApproximatelyMinute$G = "La traduction prendra environ {0} minutes";
  const requestTranslationFailed$G = "Impossible de demander la traduction de la vidéo";
  const audioNotReceived$G = "Lien audio non reçu";
  const audioFormatNotSupported$G = "Format audio non pris en charge";
  const VOTAutoTranslate$G = "Traduire à l'ouverture";
  const VOTDontTranslateYourLang$G = "Ne pas traduire à partir de votre langue";
  const VOTVolume$G = "Volume vidéo:";
  const VOTVolumeTranslation$G = "Volume de traduction:";
  const VOTAutoSetVolume$G = "Réduisez le volume vidéo à";
  const VOTShowVideoSlider$G = "Curseur de volume vidéo";
  const VOTSyncVolume$G = "Lier le volume de la traduction et de la vidéo";
  const VOTDisableFromYourLang$G = "Vous avez désactivé la traduction de la vidéo dans votre langue";
  const VOTVideoIsTooLong$G = "La vidéo est trop longue";
  const VOTNoVideoIDFound$G = "ID vidéo introuvable";
  const VOTSubtitles$G = "Les sous-titres";
  const VOTSubtitlesDisabled$G = "Désactivé";
  const VOTSubtitlesMaxLength$G = "Longueur max des sous-titres";
  const VOTHighlightWords$G = "Mettre en surbrillance les mots";
  const VOTTranslatedFrom$G = "traduit de";
  const VOTAutogenerated$G = "genere automatiquement";
  const VOTSettings$G = "VOT Paramètres";
  const VOTMenuLanguage$G = "Langue des menus";
  const VOTAuthors$G = "Auteurs";
  const VOTVersion$G = "Version";
  const VOTLoader$G = "Chargeur";
  const VOTBrowser$G = "Navigateur";
  const VOTShowPiPButton$G = "Afficher le bouton PiP";
  const langs$G = { "auto": "Automatiquement", "af": "Afrikaans", "ak": "Akan", "sq": "Albanais", "de": "Allemand", "am": "Amharique", "en": "Anglais", "ar": "Arabe", "hy": "Arménien", "as": "Assamais", "ay": "Aymara", "az": "Azerbaïdjanais", "eu": "Basque", "bn": "Bengali", "bho": "Bhodjpouri", "be": "Biélorusse", "my": "Birman", "bs": "Bosniaque", "bg": "Bulgare", "ca": "Catalan", "ceb": "Cebuano", "ny": "Chewa", "zh": "Chinois", "zh-Hans": "Chinois (Simplifié)", "zh-Hant": "Chinois (Traditionnel)", "si": "Cingalais", "ko": "Coréen", "co": "Corse", "ht": "Créole Haïtien", "hr": "Croate", "da": "Danois", "es": "Espagnol", "eo": "Espéranto", "et": "Estonien", "ee": "Éwé", "fil": "Filipino", "fi": "Finnois", "fr": "Français", "fy": "Frison Occidental", "gd": "Gaélique Écossais", "gl": "Galicien", "cy": "Gallois", "lg": "Ganda", "ka": "Géorgien", "gu": "Goudjarati", "el": "Grec", "gn": "Guarani", "ha": "Haoussa", "haw": "Hawaïen", "iw": "Hébreu", "hi": "Hindi", "hmn": "Hmong", "hu": "Hongrois", "ig": "Igbo", "id": "Indonésien", "ga": "Irlandais", "is": "Islandais", "it": "Italien", "ja": "Japonais", "jv": "Javanais", "kn": "Kannada", "kk": "Kazakh", "km": "Khmer", "rw": "Kinyarwanda", "ky": "Kirghize", "kri": "Krio", "ku": "Kurde", "lo": "Lao", "la": "Latin", "lv": "Letton", "ln": "Lingala", "lt": "Lituanien", "lb": "Luxembourgeois", "mk": "Macédonien", "ms": "Malais", "ml": "Malayalam", "dv": "Maldivien", "mg": "Malgache", "mt": "Maltais", "mi": "Maori", "mr": "Marathi", "mn": "Mongol", "nl": "Néerlandais", "ne": "Népalais", "no": "Norvégien", "or": "Odia", "om": "Oromo", "ug": "Ouïghour", "ur": "Ourdou", "uz": "Ouzbek", "ps": "Pachto", "pa": "Pendjabi", "fa": "Persan", "pl": "Polonais", "pt": "Portugais", "qu": "Quechua", "ro": "Roumain", "ru": "Russe", "sm": "Samoan", "sa": "Sanskrit", "sr": "Serbe", "sn": "Shona", "sd": "Sindhi", "sk": "Slovaque", "sl": "Slovène", "so": "Somali", "nso": "Sotho Du Nord", "st": "Sotho Du Sud", "su": "Soundanais", "sv": "Suédois", "sw": "Swahili", "tg": "Tadjik", "ta": "Tamoul", "tt": "Tatar", "cs": "Tchèque", "te": "Télougou", "th": "Thaï", "ti": "Tigrigna", "ts": "Tsonga", "tr": "Turc", "tk": "Turkmène", "uk": "Ukrainien", "vi": "Vietnamien", "xh": "Xhosa", "yi": "Yiddish", "yo": "Yoruba", "zu": "Zoulou" };
  const streamNoConnectionToServer$G = "Il n'y a pas de connexion au serveur";
  const searchField$G = "Chercher...";
  const VOTTranslateAPIErrors$G = "Traduire les erreurs de l'API";
  const VOTDetectService$G = "Service de détection de la langue";
  const VOTProxyWorkerHost$G = "Entrez l'adresse du travailleur mandataire";
  const VOTM3u8ProxyHost$G = "Entrez l'adresse du travailleur proxy m3u8";
  const proxySettings$G = "Paramètres du Proxy";
  const translationTakeApproximatelyMinute2$G = "La traduction prendra environ {0} minutes";
  const VOTAudioBooster$G = "Augmentation du volume de traduction étendu";
  const VOTSubtitlesDesign$G = "Conception des sous-titres";
  const VOTSubtitlesFontSize$G = "Taille de la police des sous-titres";
  const VOTSubtitlesOpacity$G = "Transparence de l'arrière-plan des sous-titres";
  const VOTSubtitlesDownloadFormat$G = "Le format de téléchargement des sous-titres";
  const VOTDownloadWithName$G = "Télécharger des fichiers avec le nom de la vidéo";
  const VOTUpdateLocaleFiles$G = "Mettre à jour les fichiers de localisation";
  const VOTLocaleHash$G = "Hachage des paramètres régionaux";
  const VOTUpdatedAt$G = "Mis à jour à 16h40";
  const VOTNeedWebAudioAPI$G = "Pour activer cela, vous devez disposer d'une API audio Web";
  const VOTMediaCSPEnabledOnSite$G = "Media CSP est activé sur ce site";
  const VOTOnlyBypassMediaCSP$G = "Utilisez - le uniquement pour contourner le CSP multimédia";
  const VOTNewAudioPlayer$G = "Utilisez le nouveau lecteur audio";
  const VOTUseNewModel$G = "Utilisez une variante expérimentale des voix Yandex pour certaines vidéos";
  const TranslationDelayed$G = "La traduction est légèrement retardée";
  const VOTTranslationCompletedNotify$G = "La traduction sur le {0} est terminée!";
  const VOTSendNotifyOnComplete$G = "Envoyer une notification indiquant que la vidéo a été traduite";
  const VOTBugReport$G = "Signaler un bug";
  const VOTTranslateProxyDisabled$G = "Désactivé";
  const VOTTranslateProxyEnabled$G = "Activé";
  const VOTTranslateProxyEverything$G = "Proxy tout";
  const VOTTranslateProxyStatus$G = "Mode de proxy";
  const VOTTranslatedBy$G = "Traduit par {0}";
  const VOTStreamNotAvailable$G = "Le flux de traduction n'est pas disponible";
  const VOTTranslationTextService$G = "Service de traduction de texte";
  const VOTNotAffectToVoice$G = "N'affecte pas la traduction du texte en voix off";
  const DontTranslateSelectedLanguages$G = "Ne traduisez pas à partir des langues sélectionnées";
  const showVideoVolumeSlider$G = "Afficher le curseur de volume vidéo";
  const hotkeysSettings$G = "Paramètres des raccourcis clavier";
  const None$G = "Néant";
  const VOTUseLivelyVoice$G = "Utilisez des voix vives. Les haut-parleurs sonnent comme des Russes natifs.";
  const miscSettings$G = "Paramètres divers";
  const services$G = { "yandexbrowser": "Navigateur Yandex", "msedge": "Microsoft Bord", "rust-server": "Serveur Rust" };
  const aboutExtension$G = "À propos de l'extension";
  const appearance$G = "Apparence";
  const buttonPosition$G = "Position des boutons dans le lecteur";
  const position$G = { "left": "Gauche", "right": "Droite", "top": "Haut", "default": "Par Défaut" };
  const secs$G = "secs";
  const autoHideButtonDelay$G = "Délai avant de masquer le bouton traduire";
  const notFound$G = "non trouvé";
  const minButtonPositionContainer$G = "La position du bouton ne change que dans les lecteurs de plus de 600 pixels.";
  const VOTTranslateProxyStatusDefault$G = "La désactivation complète du proxy dans votre pays peut casser l'extension";
  const PressTheKeyCombination$G = "Appuyez sur la combinaison de touches...";
  const VOTUseAudioDownload$G = "Utiliser le téléchargement audio";
  const VOTUseAudioDownloadWarning$G = "La désactivation des téléchargements audio peut affecter la fonctionnalité de l'extension";
  const VOTAccountRequired$G = "Vous devez vous connecter pour utiliser cette fonctionnalité";
  const VOTMyAccount$G = "Mon compte";
  const VOTLogin$G = "Connexion";
  const VOTLogout$G = "Déconnexion";
  const VOTRefresh$G = "Rafraîchir";
  const VOTYandexToken$G = "Entrez le jeton Yandex OAuth";
  const VOTYandexTokenInfo$G = "Vous pouvez définir manuellement le jeton de compte dans ce champ. Veuillez noter que nous ne vérifions pas sa validité avant d'envoyer une demande de traduction";
  const VOTLoginViaToken$G = "Connexion via un jeton";
  const subtitlesSmartLayout$G = "Disposition intelligente des sous-titres";
  const smartDucking$G = "Volume adaptatif";
  const VOTAutoSubtitles$G = "Sous-titres à l'ouverture";
  const VOTSubtitlesFont$G = "Police des sous-titres";
  const __vite_glob_0_19 = {
    recommended: recommended$G,
    translateVideo: translateVideo$G,
    disableTranslate: disableTranslate$G,
    translationSettings: translationSettings$G,
    subtitlesSettings: subtitlesSettings$G,
    resetSettings: resetSettings$G,
    videoBeingTranslated: videoBeingTranslated$G,
    videoLanguage: videoLanguage$G,
    translationLanguage: translationLanguage$G,
    translationTake: translationTake$G,
    translationTakeMoreThanHour: translationTakeMoreThanHour$G,
    translationTakeAboutMinute: translationTakeAboutMinute$G,
    translationTakeFewMinutes: translationTakeFewMinutes$G,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$G,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$G,
    requestTranslationFailed: requestTranslationFailed$G,
    audioNotReceived: audioNotReceived$G,
    audioFormatNotSupported: audioFormatNotSupported$G,
    VOTAutoTranslate: VOTAutoTranslate$G,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$G,
    VOTVolume: VOTVolume$G,
    VOTVolumeTranslation: VOTVolumeTranslation$G,
    VOTAutoSetVolume: VOTAutoSetVolume$G,
    VOTShowVideoSlider: VOTShowVideoSlider$G,
    VOTSyncVolume: VOTSyncVolume$G,
    VOTDisableFromYourLang: VOTDisableFromYourLang$G,
    VOTVideoIsTooLong: VOTVideoIsTooLong$G,
    VOTNoVideoIDFound: VOTNoVideoIDFound$G,
    VOTSubtitles: VOTSubtitles$G,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$G,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$G,
    VOTHighlightWords: VOTHighlightWords$G,
    VOTTranslatedFrom: VOTTranslatedFrom$G,
    VOTAutogenerated: VOTAutogenerated$G,
    VOTSettings: VOTSettings$G,
    VOTMenuLanguage: VOTMenuLanguage$G,
    VOTAuthors: VOTAuthors$G,
    VOTVersion: VOTVersion$G,
    VOTLoader: VOTLoader$G,
    VOTBrowser: VOTBrowser$G,
    VOTShowPiPButton: VOTShowPiPButton$G,
    langs: langs$G,
    streamNoConnectionToServer: streamNoConnectionToServer$G,
    searchField: searchField$G,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$G,
    VOTDetectService: VOTDetectService$G,
    VOTProxyWorkerHost: VOTProxyWorkerHost$G,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$G,
    proxySettings: proxySettings$G,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$G,
    VOTAudioBooster: VOTAudioBooster$G,
    VOTSubtitlesDesign: VOTSubtitlesDesign$G,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$G,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$G,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$G,
    VOTDownloadWithName: VOTDownloadWithName$G,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$G,
    VOTLocaleHash: VOTLocaleHash$G,
    VOTUpdatedAt: VOTUpdatedAt$G,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$G,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$G,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$G,
    VOTNewAudioPlayer: VOTNewAudioPlayer$G,
    VOTUseNewModel: VOTUseNewModel$G,
    TranslationDelayed: TranslationDelayed$G,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$G,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$G,
    VOTBugReport: VOTBugReport$G,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$G,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$G,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$G,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$G,
    VOTTranslatedBy: VOTTranslatedBy$G,
    VOTStreamNotAvailable: VOTStreamNotAvailable$G,
    VOTTranslationTextService: VOTTranslationTextService$G,
    VOTNotAffectToVoice: VOTNotAffectToVoice$G,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$G,
    showVideoVolumeSlider: showVideoVolumeSlider$G,
    hotkeysSettings: hotkeysSettings$G,
    None: None$G,
    VOTUseLivelyVoice: VOTUseLivelyVoice$G,
    miscSettings: miscSettings$G,
    services: services$G,
    aboutExtension: aboutExtension$G,
    appearance: appearance$G,
    buttonPosition: buttonPosition$G,
    position: position$G,
    secs: secs$G,
    autoHideButtonDelay: autoHideButtonDelay$G,
    notFound: notFound$G,
    minButtonPositionContainer: minButtonPositionContainer$G,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$G,
    PressTheKeyCombination: PressTheKeyCombination$G,
    VOTUseAudioDownload: VOTUseAudioDownload$G,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$G,
    VOTAccountRequired: VOTAccountRequired$G,
    VOTMyAccount: VOTMyAccount$G,
    VOTLogin: VOTLogin$G,
    VOTLogout: VOTLogout$G,
    VOTRefresh: VOTRefresh$G,
    VOTYandexToken: VOTYandexToken$G,
    VOTYandexTokenInfo: VOTYandexTokenInfo$G,
    VOTLoginViaToken: VOTLoginViaToken$G,
    subtitlesSmartLayout: subtitlesSmartLayout$G,
    smartDucking: smartDucking$G,
    VOTAutoSubtitles: VOTAutoSubtitles$G,
    VOTSubtitlesFont: VOTSubtitlesFont$G
  };
  const recommended$F = "recomendado";
  const translateVideo$F = "Traducir vídeo";
  const disableTranslate$F = "Apaga";
  const translationSettings$F = "Configuración da tradución";
  const subtitlesSettings$F = "Configuración dos subtítulos";
  const resetSettings$F = "Reiniciar a configuración";
  const videoBeingTranslated$F = "O vídeo está a ser traducido";
  const videoLanguage$F = "Linguaxe de vídeo";
  const translationLanguage$F = "Lingua de tradución";
  const translationTake$F = "A tradución levará";
  const translationTakeMoreThanHour$F = "A tradución tardará máis dunha hora";
  const translationTakeAboutMinute$F = "A tradución tardará un minuto";
  const translationTakeFewMinutes$F = "A tradución tardará uns minutos";
  const translationTakeApproximatelyMinutes$F = "A tradución levará aproximadamente {0} minutos";
  const translationTakeApproximatelyMinute$F = "A tradución levará aproximadamente {0} minutos";
  const requestTranslationFailed$F = "Non foi posíbel solicitar a tradución de vídeo";
  const audioNotReceived$F = "Enlace de Audio non recibido";
  const audioFormatNotSupported$F = "O formato de audio non é compatible";
  const VOTAutoTranslate$F = "Traducir en aberto";
  const VOTDontTranslateYourLang$F = "Non traducir do meu idioma";
  const VOTVolume$F = "Volume de vídeo:";
  const VOTVolumeTranslation$F = "Volume de tradución:";
  const VOTAutoSetVolume$F = "Reducir o volume de vídeo a";
  const VOTShowVideoSlider$F = "Deslizador de volume de vídeo";
  const VOTSyncVolume$F = "Enlace tradución e volume de vídeo";
  const VOTDisableFromYourLang$F = "Desactivou a tradución do vídeo no seu idioma";
  const VOTVideoIsTooLong$F = "O vídeo é demasiado longo";
  const VOTNoVideoIDFound$F = "Non se atopou NINGUNHA IDENTIFICACIÓN de vídeo";
  const VOTSubtitles$F = "Subtítulos";
  const VOTSubtitlesDisabled$F = "Desactivado";
  const VOTSubtitlesMaxLength$F = "Subtítulos lonxitude máxima";
  const VOTHighlightWords$F = "Destacar palabras";
  const VOTTranslatedFrom$F = "traducido de";
  const VOTAutogenerated$F = "autogenerado";
  const VOTSettings$F = "Configuración DO VOTO";
  const VOTMenuLanguage$F = "Lingua do menú";
  const VOTAuthors$F = "Autores";
  const VOTVersion$F = "Versión";
  const VOTLoader$F = "Cargador";
  const VOTBrowser$F = "Navegador";
  const VOTShowPiPButton$F = "Mostrar o botón pip";
  const langs$F = { "auto": "Auto", "af": "Afrikaans", "ak": "Acan", "sq": "Albanés", "am": "Amárico", "ar": "Árabe", "hy": "Armenio", "as": "Assamés", "ay": "Aimara", "az": "Azerbaixán", "bn": "Bangla", "eu": "Vasco", "be": "Bielorruso", "bho": "Bhojpuri", "bs": "Bosnia", "bg": "Búlgaro", "my": "Birmanés", "ca": "Catalán", "ceb": "Cebuano", "zh": "Chinés", "zh-Hans": "Chinés (Simplificado)", "zh-Hant": "Chinés (Tradicional)", "co": "Córcega", "hr": "Croata", "cs": "Checo", "da": "Danés", "dv": "Divehi", "nl": "Holandés", "en": "Inglés", "eo": "Esperanto", "et": "Estonio", "ee": "Ovella", "fil": "Filipino", "fi": "Finlandés", "fr": "Francés", "gl": "Galego", "lg": "Ganda", "ka": "Xeorxiano", "de": "Alemán", "el": "Grego", "gn": "Guaraní", "gu": "Gujarati", "ht": "Criollo Haitiano", "ha": "Hausa", "haw": "Hawaiano", "iw": "Hebreo", "hi": "Hindi", "hmn": "Hmong", "hu": "Húngaro", "is": "Islandés", "ig": "Igbo", "id": "Indonesio", "ga": "Irlandés", "it": "Italiano", "ja": "Xaponés", "jv": "Xavanés", "kn": "Canada", "kk": "Kazaco", "km": "Khmer", "rw": "Kinyarwanda", "ko": "Coreano", "kri": "Crio", "ku": "Curdo", "ky": "Kirguiz", "lo": "Lao", "la": "Latín", "lv": "Letón", "ln": "Lingua", "lt": "Lituano", "lb": "Luxemburgués", "mk": "Macedonio", "mg": "Malgaxe", "ms": "Malaio", "ml": "Malayalam", "mt": "Maltés", "mi": "Maorí", "mr": "Marathi", "mn": "Mongol", "ne": "Nepalí", "nso": "Sotho Do Norte", "no": "Noruegués", "ny": "Nyanxa", "or": "Odia", "om": "Oromo", "ps": "Pashtú", "fa": "Persa", "pl": "Polaco", "pt": "Portugués", "pa": "Punxabi", "qu": "Quechua", "ro": "Rumano", "ru": "Ruso", "sm": "Samoano", "sa": "Sánscrito", "gd": "Gaélico Escocés", "sr": "Serbio", "sn": "Shona", "sd": "Sindhi", "si": "Cingala", "sk": "Eslovaco", "sl": "Esloveno", "so": "Somalí", "st": "Sotho Do Sur", "es": "Español", "su": "Sundanés", "sw": "Suahili", "sv": "Sueco", "tg": "Tajikistán", "ta": "Tamil", "tt": "Tártaro", "te": "Telugu", "th": "Tailandés", "ti": "Tigrinya", "ts": "Tsonga", "tr": "Turco", "tk": "Turcomanos", "uk": "Ucraniano", "ur": "Urdu", "ug": "Uigur", "uz": "Uzbeko", "vi": "Vietnamita", "cy": "Galés", "fy": "Frisia Occidental", "xh": "Xosa", "yi": "Iídiche", "yo": "Iorubá", "zu": "Zulú" };
  const streamNoConnectionToServer$F = "Non hai conexión co servidor";
  const searchField$F = "Busca...";
  const VOTTranslateAPIErrors$F = "Traducir erros DA API";
  const VOTDetectService$F = "Servizo de detección de idiomas";
  const VOTProxyWorkerHost$F = "Introduza o enderezo proxy worker";
  const VOTM3u8ProxyHost$F = "Introduza o enderezo do traballador proxy m3u8";
  const proxySettings$F = "Configuración Do Proxy";
  const translationTakeApproximatelyMinute2$F = "A tradución levará aproximadamente {0} minutos";
  const VOTAudioBooster$F = "Aumento do volume de tradución estendido";
  const VOTSubtitlesDesign$F = "Deseño de subtítulos";
  const VOTSubtitlesFontSize$F = "Tamaño da letra dos subtítulos";
  const VOTSubtitlesOpacity$F = "Transparencia do fondo do subtítulo";
  const VOTSubtitlesDownloadFormat$F = "O formato para descargar subtítulos";
  const VOTDownloadWithName$F = "Descargar ficheiros co nome do vídeo";
  const VOTUpdateLocaleFiles$F = "Actualizar os ficheiros de localización";
  const VOTLocaleHash$F = "Hash local";
  const VOTUpdatedAt$F = "Actualizado ás";
  const VOTNeedWebAudioAPI$F = "Para activar isto, debes ter UNHA API De Audio Web";
  const VOTMediaCSPEnabledOnSite$F = "Os medios de COMUNICACIÓN csp están habilitados neste sitio";
  const VOTOnlyBypassMediaCSP$F = "Usalo só para evitar A MEDIA CSP";
  const VOTNewAudioPlayer$F = "Use o novo reproductor de audio";
  const VOTUseNewModel$F = "Use unha variación experimental De voces De Yandex para algúns vídeos";
  const TranslationDelayed$F = "A tradución está un pouco atrasada";
  const VOTTranslationCompletedNotify$F = "A tradución do {0} foi completada!";
  const VOTSendNotifyOnComplete$F = "Enviar unha notificación de que o vídeo foi traducido";
  const VOTBugReport$F = "Informar dun erro";
  const VOTTranslateProxyDisabled$F = "Desactivado";
  const VOTTranslateProxyEnabled$F = "Activado";
  const VOTTranslateProxyEverything$F = "Proxy todo";
  const VOTTranslateProxyStatus$F = "Modo de proxy";
  const VOTTranslatedBy$F = "Traducido por {0}";
  const VOTStreamNotAvailable$F = "O fluxo de tradución non está dispoñible";
  const VOTTranslationTextService$F = "Servizo de tradución de texto";
  const VOTNotAffectToVoice$F = "Non afecta á tradución de textos en voz alta";
  const DontTranslateSelectedLanguages$F = "Non traducir idiomas seleccionados";
  const showVideoVolumeSlider$F = "Mostrar o deslizador de volume de vídeo";
  const hotkeysSettings$F = "Configuración de teclas quentes";
  const None$F = "Ningunha";
  const VOTUseLivelyVoice$F = "Usa voces vivas. Os falantes soan Como Rusos nativos.";
  const miscSettings$F = "Configuración do Misc";
  const services$F = { "yandexbrowser": "Navegador Yandex", "msedge": "Microsoft Edge", "rust-server": "Servidor Rust" };
  const aboutExtension$F = "Sobre a extensión";
  const appearance$F = "Aparencia";
  const buttonPosition$F = "Posición do botón no xogador";
  const position$F = { "left": "Esquerda", "right": "Dereita", "top": "Cima", "default": "Predeterminado" };
  const secs$F = "secs";
  const autoHideButtonDelay$F = "Atraso antes de ocultar o botón traducir";
  const notFound$F = "non atopado";
  const minButtonPositionContainer$F = "A posición do botón só cambia en xogadores maiores de 600 píxeles.";
  const VOTTranslateProxyStatusDefault$F = "Desactivar completamente a representación no seu país pode romper a extensión";
  const PressTheKeyCombination$F = "Preme na combinación de teclas...";
  const VOTUseAudioDownload$F = "Usar a descarga de audio";
  const VOTUseAudioDownloadWarning$F = "A desactivación das descargas de audio pode afectar á funcionalidade da extensión";
  const VOTAccountRequired$F = "Necesitas iniciar sesión para usar esta característica";
  const VOTMyAccount$F = "A miña conta";
  const VOTLogin$F = "Login";
  const VOTLogout$F = "Logout";
  const VOTRefresh$F = "Actualizar";
  const VOTYandexToken$F = "Introduza O Token Yandex OAuth";
  const VOTYandexTokenInfo$F = "Podes configurar manualmente o token da conta neste campo. Teña en conta que non comprobamos a súa validez antes de enviar unha solicitude de tradución";
  const VOTLoginViaToken$F = "Iniciar sesión a través do token";
  const subtitlesSmartLayout$F = "Deseño de subtítulos intelixentes";
  const smartDucking$F = "Volume adaptativo";
  const VOTAutoSubtitles$F = "Subtítulos en open";
  const VOTSubtitlesFont$F = "Fonte de subtítulos";
  const __vite_glob_0_20 = {
    recommended: recommended$F,
    translateVideo: translateVideo$F,
    disableTranslate: disableTranslate$F,
    translationSettings: translationSettings$F,
    subtitlesSettings: subtitlesSettings$F,
    resetSettings: resetSettings$F,
    videoBeingTranslated: videoBeingTranslated$F,
    videoLanguage: videoLanguage$F,
    translationLanguage: translationLanguage$F,
    translationTake: translationTake$F,
    translationTakeMoreThanHour: translationTakeMoreThanHour$F,
    translationTakeAboutMinute: translationTakeAboutMinute$F,
    translationTakeFewMinutes: translationTakeFewMinutes$F,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$F,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$F,
    requestTranslationFailed: requestTranslationFailed$F,
    audioNotReceived: audioNotReceived$F,
    audioFormatNotSupported: audioFormatNotSupported$F,
    VOTAutoTranslate: VOTAutoTranslate$F,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$F,
    VOTVolume: VOTVolume$F,
    VOTVolumeTranslation: VOTVolumeTranslation$F,
    VOTAutoSetVolume: VOTAutoSetVolume$F,
    VOTShowVideoSlider: VOTShowVideoSlider$F,
    VOTSyncVolume: VOTSyncVolume$F,
    VOTDisableFromYourLang: VOTDisableFromYourLang$F,
    VOTVideoIsTooLong: VOTVideoIsTooLong$F,
    VOTNoVideoIDFound: VOTNoVideoIDFound$F,
    VOTSubtitles: VOTSubtitles$F,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$F,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$F,
    VOTHighlightWords: VOTHighlightWords$F,
    VOTTranslatedFrom: VOTTranslatedFrom$F,
    VOTAutogenerated: VOTAutogenerated$F,
    VOTSettings: VOTSettings$F,
    VOTMenuLanguage: VOTMenuLanguage$F,
    VOTAuthors: VOTAuthors$F,
    VOTVersion: VOTVersion$F,
    VOTLoader: VOTLoader$F,
    VOTBrowser: VOTBrowser$F,
    VOTShowPiPButton: VOTShowPiPButton$F,
    langs: langs$F,
    streamNoConnectionToServer: streamNoConnectionToServer$F,
    searchField: searchField$F,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$F,
    VOTDetectService: VOTDetectService$F,
    VOTProxyWorkerHost: VOTProxyWorkerHost$F,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$F,
    proxySettings: proxySettings$F,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$F,
    VOTAudioBooster: VOTAudioBooster$F,
    VOTSubtitlesDesign: VOTSubtitlesDesign$F,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$F,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$F,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$F,
    VOTDownloadWithName: VOTDownloadWithName$F,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$F,
    VOTLocaleHash: VOTLocaleHash$F,
    VOTUpdatedAt: VOTUpdatedAt$F,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$F,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$F,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$F,
    VOTNewAudioPlayer: VOTNewAudioPlayer$F,
    VOTUseNewModel: VOTUseNewModel$F,
    TranslationDelayed: TranslationDelayed$F,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$F,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$F,
    VOTBugReport: VOTBugReport$F,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$F,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$F,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$F,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$F,
    VOTTranslatedBy: VOTTranslatedBy$F,
    VOTStreamNotAvailable: VOTStreamNotAvailable$F,
    VOTTranslationTextService: VOTTranslationTextService$F,
    VOTNotAffectToVoice: VOTNotAffectToVoice$F,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$F,
    showVideoVolumeSlider: showVideoVolumeSlider$F,
    hotkeysSettings: hotkeysSettings$F,
    None: None$F,
    VOTUseLivelyVoice: VOTUseLivelyVoice$F,
    miscSettings: miscSettings$F,
    services: services$F,
    aboutExtension: aboutExtension$F,
    appearance: appearance$F,
    buttonPosition: buttonPosition$F,
    position: position$F,
    secs: secs$F,
    autoHideButtonDelay: autoHideButtonDelay$F,
    notFound: notFound$F,
    minButtonPositionContainer: minButtonPositionContainer$F,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$F,
    PressTheKeyCombination: PressTheKeyCombination$F,
    VOTUseAudioDownload: VOTUseAudioDownload$F,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$F,
    VOTAccountRequired: VOTAccountRequired$F,
    VOTMyAccount: VOTMyAccount$F,
    VOTLogin: VOTLogin$F,
    VOTLogout: VOTLogout$F,
    VOTRefresh: VOTRefresh$F,
    VOTYandexToken: VOTYandexToken$F,
    VOTYandexTokenInfo: VOTYandexTokenInfo$F,
    VOTLoginViaToken: VOTLoginViaToken$F,
    subtitlesSmartLayout: subtitlesSmartLayout$F,
    smartDucking: smartDucking$F,
    VOTAutoSubtitles: VOTAutoSubtitles$F,
    VOTSubtitlesFont: VOTSubtitlesFont$F
  };
  const recommended$E = "अनुशंसित";
  const translateVideo$E = "वीडियो अनुवाद करें";
  const disableTranslate$E = "बंद करें";
  const translationSettings$E = "अनुवाद सेटिंग्स";
  const subtitlesSettings$E = "उपशीर्षक सेटिंग्स";
  const resetSettings$E = "सेटिंग्स रीसेट करें";
  const videoBeingTranslated$E = "वीडियो अनुवाद किया जा रहा है";
  const videoLanguage$E = "वीडियो भाषा";
  const translationLanguage$E = "अनुवाद भाषा";
  const translationTake$E = "अनुवाद करने में समय लगेगा";
  const translationTakeMoreThanHour$E = "अनुवाद एक घंटे से अधिक समय लगेगा";
  const translationTakeAboutMinute$E = "अनुवाद लगभग एक मिनट लगेगा";
  const translationTakeFewMinutes$E = "अनुवाद कुछ मिनट लगेगा";
  const translationTakeApproximatelyMinutes$E = "{0} मिनटों के बारे में अनुवाद लगेगा";
  const translationTakeApproximatelyMinute$E = "{0} मिनट के बारे में अनुवाद लगेगा";
  const requestTranslationFailed$E = "वीडियो अनुवाद का अनुरोध विफल रहा";
  const audioNotReceived$E = "ऑडियो का लिंक प्राप्त नहीं हुआ";
  const audioFormatNotSupported$E = "ऑडियो फ़ॉर्मेट समर्थित नहीं है";
  const VOTAutoTranslate$E = "खोलने पर अनुवाद करें";
  const VOTDontTranslateYourLang$E = "अपनी भाषा से अनुवाद न करें";
  const VOTVolume$E = "वीडियो वॉल्यूम:";
  const VOTVolumeTranslation$E = "अनुवाद की मात्रा:";
  const VOTAutoSetVolume$E = "वीडियो वॉल्यूम कम करें";
  const VOTShowVideoSlider$E = "वीडियो स्लाइडर दिखाएं";
  const VOTSyncVolume$E = "अनुवाद वॉल्यूम और वीडियो वॉल्यूम को संयुक्त करें";
  const VOTDisableFromYourLang$E = "आपने अपनी भाषा में वीडियो अनुवाद बंद कर दिया है";
  const VOTVideoIsTooLong$E = "वीडियो बहुत लंबा है";
  const VOTNoVideoIDFound$E = "वीडियो आईडी नहीं मिली";
  const VOTSubtitles$E = "सबटाइटल्स";
  const VOTSubtitlesDisabled$E = "बंद हैं";
  const VOTSubtitlesMaxLength$E = "सबटाइटल की अधिकतम लंबाई";
  const VOTHighlightWords$E = "शब्दों को हाइलाइट करें";
  const VOTTranslatedFrom$E = "से अनुवाद किया गया";
  const VOTAutogenerated$E = "स्वचालित रूप से उत्पन्न हुआ";
  const VOTSettings$E = "VOT समायोजन";
  const VOTMenuLanguage$E = "मेनू भाषा";
  const VOTAuthors$E = "लेखक";
  const VOTVersion$E = "संस्करण";
  const VOTLoader$E = "लोडर";
  const VOTBrowser$E = "ब्राउज़र";
  const VOTShowPiPButton$E = "PiP बटन दिखाएँ";
  const langs$E = { "auto": "खुद ब खुद", "az": "अज़रबैजानी", "ay": "आयमारा", "ak": "आकान", "sq": "अल्बानियाई", "am": "अम्हारिक", "en": "अंग्रेज़ी", "ar": "अरबी", "hy": "अर्मेनियाई", "as": "असमिया", "af": "अफ्रिकांस", "eu": "बास्क", "be": "बेलारूसी", "bn": "बंगाली", "my": "बर्मी", "bg": "बुल्गारियाई", "bs": "बोस्नियाई", "bho": "भोजपुरी", "cy": "वेल्श", "hu": "हंगेरियाई", "vi": "वियतनामी", "haw": "हवाईयन", "ht": "हैतियन", "gl": "गैलिशियन", "lg": "गांडा", "el": "यूनानी", "ka": "जॉर्जियाई", "gn": "गुआरनी", "gu": "गुजराती", "gd": "स्कॉट्स गायेक", "da": "डेनमार्की", "fy": "पश्चिमी फ्रिजियन", "zu": "ज़ुलु", "iw": "हिब्रू", "ig": "इग्बो", "yi": "यिद्दिश", "id": "इंडोनेशियाई", "ga": "आइरिश", "is": "आइसलैंडिक", "es": "स्पेनिश", "it": "इतालवी", "yo": "योरूबा", "kk": "कज़ाखी", "kn": "कन्नड़", "ca": "कातलान", "qu": "केचुआ", "rw": "किन्यारवांडा", "ky": "किर्गिज़", "zh": "चीनी", "zh-Hant": "चीनी (पारंपरिक)", "zh-Hans": "चीनी (सरलीकृत)", "ko": "कोरियाई", "co": "कोर्सीकन", "xh": "कोसा", "ku": "कुर्दिश", "km": "खमेर", "lo": "लाओ", "la": "लैटिन", "lv": "लात्वियाई", "ln": "लिंगाला", "lt": "लिथुएनियाई", "lb": "लुक्समबर्गी", "mk": "मैसेडोनियाई", "mg": "मालागासी", "ms": "मलय", "ml": "मलयालम", "dv": "दिवेही", "mt": "माल्टीज", "mi": "माओरी", "mr": "मराठी", "mn": "मंगोलियाई", "de": "जर्मन", "ne": "नेपाली", "nl": "डच", "no": "नोर्वेजियाई", "ny": "न्यान्जा", "or": "उड़िया", "om": "ओरोमो", "pa": "पंजाबी", "fa": "फ़ारसी", "pl": "पोलिश", "pt": "पुर्तगाली", "ps": "पश्तो", "ro": "रोमानियाई", "ru": "रूसी", "sm": "सामोअन", "sa": "संस्कृत", "ceb": "सेबुआनो", "nso": "उत्तरी सोथो", "sr": "सर्बियाई", "si": "सिंहली", "sd": "सिंधी", "sk": "स्लोवाक", "sl": "स्लोवेनियाई", "so": "सोमाली", "sw": "स्वाहिली", "su": "सुडानी", "tg": "ताजिक", "th": "थाई", "ta": "तमिल", "tt": "तातार", "te": "तेलुगु", "ti": "तिग्रिन्या", "ts": "टोंगा", "tr": "तुर्की", "tk": "तुर्कमेन", "uz": "उज़्बेक", "ug": "उयघुर", "uk": "यूक्रेनियाई", "ur": "उर्दू", "fil": "फिलिपीनो", "fi": "फ़िनिश", "fr": "फ्रेंच", "ha": "हौसा", "hi": "हिंदी", "hmn": "हमॉंग", "hr": "क्रोएशियाई", "cs": "चेक", "sv": "स्वीडिश", "sn": "शोना", "ee": "इवे", "eo": "एस्पेरान्तो", "et": "एस्टोनियाई", "st": "दक्षिण सोथो", "jv": "जावानीज़", "ja": "जापानी", "kri": "क्री" };
  const streamNoConnectionToServer$E = "सर्वर से कोई संबंध नहीं है";
  const searchField$E = "खोज...";
  const VOTTranslateAPIErrors$E = "एपीआई से त्रुटियों का अनुवाद करें";
  const VOTDetectService$E = "भाषा पहचान सेवा";
  const VOTProxyWorkerHost$E = "प्रॉक्सी कार्यकर्ता पता दर्ज करें";
  const VOTM3u8ProxyHost$E = "एम 3 यू 8 प्रॉक्सी कार्यकर्ता का पता दर्ज करें";
  const proxySettings$E = "प्रॉक्सी सेटिंग्स";
  const translationTakeApproximatelyMinute2$E = "अनुवाद में लगभग {0} मिनट लगेंगे";
  const VOTAudioBooster$E = "विस्तारित अनुवाद की मात्रा में वृद्धि";
  const VOTSubtitlesDesign$E = "उपशीर्षक डिजाइन";
  const VOTSubtitlesFontSize$E = "उपशीर्षक का फ़ॉन्ट आकार";
  const VOTSubtitlesOpacity$E = "उपशीर्षक पृष्ठभूमि की पारदर्शिता";
  const VOTSubtitlesDownloadFormat$E = "उपशीर्षक डाउनलोड करने का प्रारूप";
  const VOTDownloadWithName$E = "वीडियो नाम के साथ फ़ाइलें डाउनलोड करें";
  const VOTUpdateLocaleFiles$E = "स्थानीयकरण फ़ाइलें अपडेट करें";
  const VOTLocaleHash$E = "लोकेल हैश";
  const VOTUpdatedAt$E = "पर अपडेट किया गया";
  const VOTNeedWebAudioAPI$E = "इसे सक्षम करने के लिए, आपके पास एक वेब ऑडियो एपीआई होना चाहिए";
  const VOTMediaCSPEnabledOnSite$E = "मीडिया सीएसपी इस साइट पर सक्षम है";
  const VOTOnlyBypassMediaCSP$E = "इसका उपयोग केवल मीडिया सीएसपी को दरकिनार करने के लिए करें";
  const VOTNewAudioPlayer$E = "नए ऑडियो प्लेयर का उपयोग करें";
  const VOTUseNewModel$E = "कुछ वीडियो के लिए यांडेक्स आवाजों की प्रयोगात्मक भिन्नता का उपयोग करें";
  const TranslationDelayed$E = "अनुवाद में थोड़ी देरी हो रही है";
  const VOTTranslationCompletedNotify$E = "पर अनुवाद {0} पूरा हो गया है!";
  const VOTSendNotifyOnComplete$E = "एक अधिसूचना भेजें कि वीडियो का अनुवाद किया गया है";
  const VOTBugReport$E = "बग की रिपोर्ट करें";
  const VOTTranslateProxyDisabled$E = "विकलांग";
  const VOTTranslateProxyEnabled$E = "सक्षम किया गया";
  const VOTTranslateProxyEverything$E = "प्रॉक्सी सब कुछ";
  const VOTTranslateProxyStatus$E = "प्रॉक्सी मोड";
  const VOTTranslatedBy$E = "{0}द्वारा अनुवादित";
  const VOTStreamNotAvailable$E = "अनुवाद स्ट्रीम उपलब्ध नहीं है";
  const VOTTranslationTextService$E = "पाठ अनुवाद सेवा";
  const VOTNotAffectToVoice$E = "आवाज पर पाठ के अनुवाद को प्रभावित नहीं करता है";
  const DontTranslateSelectedLanguages$E = "चयनित भाषाओं से अनुवाद न करें";
  const showVideoVolumeSlider$E = "वीडियो वॉल्यूम स्लाइडर प्रदर्शित करें";
  const hotkeysSettings$E = "हॉटकी सेटिंग्स";
  const None$E = "कोई नहीं";
  const VOTUseLivelyVoice$E = "जीवंत आवाजों का प्रयोग करें ।  वक्ताओं देशी रूसी की तरह लगते हैं । ";
  const miscSettings$E = "विविध सेटिंग्स";
  const services$E = { "yandexbrowser": "यांडेक्स ब्राउज़र", "msedge": "माइक्रोसॉफ्ट एज", "rust-server": "रस्ट सर्वर" };
  const aboutExtension$E = "विस्तार के बारे में";
  const appearance$E = "सूरत";
  const buttonPosition$E = "खिलाड़ी में बटन की स्थिति";
  const position$E = { "left": "वाम", "right": "सही", "top": "शीर्ष", "default": "डिफ़ॉल्ट" };
  const secs$E = "एसईसीएस";
  const autoHideButtonDelay$E = "अनुवाद बटन छिपाने से पहले देरी";
  const notFound$E = "नहीं मिला";
  const minButtonPositionContainer$E = "बटन की स्थिति केवल 600 पिक्सेल से बड़े खिलाड़ियों में बदलती है । ";
  const VOTTranslateProxyStatusDefault$E = "अपने देश में प्रॉक्सी को पूरी तरह से अक्षम करने से एक्सटेंशन टूट सकता है";
  const PressTheKeyCombination$E = "कुंजी संयोजन दबाएं।..";
  const VOTUseAudioDownload$E = "ऑडियो डाउनलोड का उपयोग करें";
  const VOTUseAudioDownloadWarning$E = "ऑडियो डाउनलोड अक्षम करने से एक्सटेंशन की कार्यक्षमता प्रभावित हो सकती है";
  const VOTAccountRequired$E = "इस सुविधा का उपयोग करने के लिए आपको लॉग इन करना होगा";
  const VOTMyAccount$E = "मेरा खाता";
  const VOTLogin$E = "लॉगिन करें";
  const VOTLogout$E = "लॉगआउट";
  const VOTRefresh$E = "ताज़ा करें";
  const VOTYandexToken$E = "यांडेक्स ओथ टोकन दर्ज करें";
  const VOTYandexTokenInfo$E = "आप इस फ़ील्ड में खाता टोकन मैन्युअल रूप से सेट कर सकते हैं ।  कृपया ध्यान दें कि अनुवाद अनुरोध भेजने से पहले हम इसकी वैधता की जांच नहीं करते हैं";
  const VOTLoginViaToken$E = "टोकन के माध्यम से लॉगिन करें";
  const subtitlesSmartLayout$E = "स्मार्ट उपशीर्षक लेआउट";
  const smartDucking$E = "अनुकूली मात्रा";
  const VOTAutoSubtitles$E = "उपशीर्षक पर खुला";
  const VOTSubtitlesFont$E = "उपशीर्षक फ़ॉन्ट";
  const __vite_glob_0_21 = {
    recommended: recommended$E,
    translateVideo: translateVideo$E,
    disableTranslate: disableTranslate$E,
    translationSettings: translationSettings$E,
    subtitlesSettings: subtitlesSettings$E,
    resetSettings: resetSettings$E,
    videoBeingTranslated: videoBeingTranslated$E,
    videoLanguage: videoLanguage$E,
    translationLanguage: translationLanguage$E,
    translationTake: translationTake$E,
    translationTakeMoreThanHour: translationTakeMoreThanHour$E,
    translationTakeAboutMinute: translationTakeAboutMinute$E,
    translationTakeFewMinutes: translationTakeFewMinutes$E,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$E,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$E,
    requestTranslationFailed: requestTranslationFailed$E,
    audioNotReceived: audioNotReceived$E,
    audioFormatNotSupported: audioFormatNotSupported$E,
    VOTAutoTranslate: VOTAutoTranslate$E,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$E,
    VOTVolume: VOTVolume$E,
    VOTVolumeTranslation: VOTVolumeTranslation$E,
    VOTAutoSetVolume: VOTAutoSetVolume$E,
    VOTShowVideoSlider: VOTShowVideoSlider$E,
    VOTSyncVolume: VOTSyncVolume$E,
    VOTDisableFromYourLang: VOTDisableFromYourLang$E,
    VOTVideoIsTooLong: VOTVideoIsTooLong$E,
    VOTNoVideoIDFound: VOTNoVideoIDFound$E,
    VOTSubtitles: VOTSubtitles$E,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$E,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$E,
    VOTHighlightWords: VOTHighlightWords$E,
    VOTTranslatedFrom: VOTTranslatedFrom$E,
    VOTAutogenerated: VOTAutogenerated$E,
    VOTSettings: VOTSettings$E,
    VOTMenuLanguage: VOTMenuLanguage$E,
    VOTAuthors: VOTAuthors$E,
    VOTVersion: VOTVersion$E,
    VOTLoader: VOTLoader$E,
    VOTBrowser: VOTBrowser$E,
    VOTShowPiPButton: VOTShowPiPButton$E,
    langs: langs$E,
    streamNoConnectionToServer: streamNoConnectionToServer$E,
    searchField: searchField$E,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$E,
    VOTDetectService: VOTDetectService$E,
    VOTProxyWorkerHost: VOTProxyWorkerHost$E,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$E,
    proxySettings: proxySettings$E,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$E,
    VOTAudioBooster: VOTAudioBooster$E,
    VOTSubtitlesDesign: VOTSubtitlesDesign$E,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$E,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$E,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$E,
    VOTDownloadWithName: VOTDownloadWithName$E,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$E,
    VOTLocaleHash: VOTLocaleHash$E,
    VOTUpdatedAt: VOTUpdatedAt$E,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$E,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$E,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$E,
    VOTNewAudioPlayer: VOTNewAudioPlayer$E,
    VOTUseNewModel: VOTUseNewModel$E,
    TranslationDelayed: TranslationDelayed$E,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$E,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$E,
    VOTBugReport: VOTBugReport$E,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$E,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$E,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$E,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$E,
    VOTTranslatedBy: VOTTranslatedBy$E,
    VOTStreamNotAvailable: VOTStreamNotAvailable$E,
    VOTTranslationTextService: VOTTranslationTextService$E,
    VOTNotAffectToVoice: VOTNotAffectToVoice$E,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$E,
    showVideoVolumeSlider: showVideoVolumeSlider$E,
    hotkeysSettings: hotkeysSettings$E,
    None: None$E,
    VOTUseLivelyVoice: VOTUseLivelyVoice$E,
    miscSettings: miscSettings$E,
    services: services$E,
    aboutExtension: aboutExtension$E,
    appearance: appearance$E,
    buttonPosition: buttonPosition$E,
    position: position$E,
    secs: secs$E,
    autoHideButtonDelay: autoHideButtonDelay$E,
    notFound: notFound$E,
    minButtonPositionContainer: minButtonPositionContainer$E,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$E,
    PressTheKeyCombination: PressTheKeyCombination$E,
    VOTUseAudioDownload: VOTUseAudioDownload$E,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$E,
    VOTAccountRequired: VOTAccountRequired$E,
    VOTMyAccount: VOTMyAccount$E,
    VOTLogin: VOTLogin$E,
    VOTLogout: VOTLogout$E,
    VOTRefresh: VOTRefresh$E,
    VOTYandexToken: VOTYandexToken$E,
    VOTYandexTokenInfo: VOTYandexTokenInfo$E,
    VOTLoginViaToken: VOTLoginViaToken$E,
    subtitlesSmartLayout: subtitlesSmartLayout$E,
    smartDucking: smartDucking$E,
    VOTAutoSubtitles: VOTAutoSubtitles$E,
    VOTSubtitlesFont: VOTSubtitlesFont$E
  };
  const recommended$D = "preporučeno";
  const translateVideo$D = "Prevedi video";
  const disableTranslate$D = "Isključi";
  const translationSettings$D = "Postavke prijevoda";
  const subtitlesSettings$D = "Postavke titlova";
  const resetSettings$D = "Vraćanje na tvorničke postavke";
  const videoBeingTranslated$D = "Video preveden";
  const videoLanguage$D = "Jezik video";
  const translationLanguage$D = "Jezik prijevoda";
  const translationTake$D = "Prijevod će potrajati";
  const translationTakeMoreThanHour$D = "Prijenos će trajati više od sat vremena";
  const translationTakeAboutMinute$D = "Prijenos će trajati oko minutu";
  const translationTakeFewMinutes$D = "Prijenos će trajati nekoliko minuta";
  const translationTakeApproximatelyMinutes$D = "Prijevod će trajati približno {0} minuta";
  const translationTakeApproximatelyMinute$D = "Prijevod će trajati približno {0} minuta";
  const requestTranslationFailed$D = "Nije moguće zatražiti prijevod videozapisa";
  const audioNotReceived$D = "Audio veza nije primljena";
  const audioFormatNotSupported$D = "Audio format nije podržan";
  const VOTAutoTranslate$D = "Prevedi na otvoreno";
  const VOTDontTranslateYourLang$D = "Nemojte prevoditi s mog jezika";
  const VOTVolume$D = "Glasnoća videozapisa:";
  const VOTVolumeTranslation$D = "Opseg prijenosa:";
  const VOTAutoSetVolume$D = "Smanjite glasnoću videozapisa na";
  const VOTShowVideoSlider$D = "Klizač glasnoće videozapisa";
  const VOTSyncVolume$D = "Prijevod veza i opseg videozapisa";
  const VOTDisableFromYourLang$D = "Onemogućili ste prijevod videozapisa na svoj jezik";
  const VOTVideoIsTooLong$D = "Video je predug";
  const VOTNoVideoIDFound$D = "ID videozapisa nije pronađen";
  const VOTSubtitles$D = "Titlovi";
  const VOTSubtitlesDisabled$D = "Nesposoban za rad";
  const VOTSubtitlesMaxLength$D = "Maksimalna duljina titlova";
  const VOTHighlightWords$D = "Istaknite riječi";
  const VOTTranslatedFrom$D = "prevedeno s";
  const VOTAutogenerated$D = "automatski generirano";
  const VOTSettings$D = "Postavke svjetski vot";
  const VOTMenuLanguage$D = "Jezik izbornika";
  const VOTAuthors$D = "Autori";
  const VOTVersion$D = "Verzija";
  const VOTLoader$D = "Utovarivač";
  const VOTBrowser$D = "Preglednik";
  const VOTShowPiPButton$D = "Gumb Prikaži isprekidanu crtu";
  const langs$D = { "auto": "Auto", "af": "Afrikaans", "ak": "Akan", "sq": "Albanski", "am": "Amharski", "ar": "Arapski jezik", "hy": "Armenski", "as": "Asamski", "ay": "Ajara", "az": "Azerbajdžanski", "bn": "Bangla", "eu": "Baskijski", "be": "Bjeloruski", "bho": "Bhojpuri", "bs": "Bosanski", "bg": "Bugarski", "my": "Burmanski", "ca": "Katalonski", "ceb": "Cebuano", "zh": "Kineski", "zh-Hans": "Kineski (pojednostavljeni)", "zh-Hant": "Kineski (tradicionalni)", "co": "Korzikanac", "hr": "Hrvatski", "cs": "Češki", "da": "Danski", "dv": "Divehi", "nl": "Nizozemski", "en": "Engleski", "eo": "Esperanto", "et": "Estonski", "ee": "Ovca", "fil": "Filipinac", "fi": "Finski", "fr": "Francuski", "gl": "Galicijski", "lg": "Ganda, molim te.", "ka": "Gruzijski", "de": "Njemački", "el": "Grčki", "gn": "Guarani", "gu": "Gujarati", "ht": "Haićanski kreol", "ha": "Hausa, molim te.", "haw": "Havajski", "iw": "Židov", "hi": "Hindski", "hmn": "Hmong", "hu": "Mađarski", "is": "Islandski", "ig": "Igbo, molim te.", "id": "Indonezijski", "ga": "Irski", "it": "Talijanski", "ja": "Japanski", "jv": "Javanski", "kn": "Kannada", "kk": "Kazački", "km": "Khmerski", "rw": "Kinjaruanda", "ko": "Korejski", "kri": "Krio, molim te.", "ku": "Kurdski", "ky": "Kirgiz", "lo": "Laoski", "la": "Latinski", "lv": "Latvijski", "ln": "Lingala", "lt": "Litvanski", "lb": "Luksemburški", "mk": "Makedonski", "mg": "Malagaški", "ms": "Malajski", "ml": "Malajalam", "mt": "Malteški", "mi": "Maori", "mr": "Marathi", "mn": "Mongolski", "ne": "Nepalski", "nso": "Sjeverni Soto", "no": "Norveški jezik", "ny": "Njanja", "or": "Odia", "om": "Oromo, molim te.", "ps": "Pushtu", "fa": "Perzijski", "pl": "Poljski", "pt": "Portugalski", "pa": "Punjabi", "qu": "Kečua", "ro": "Rumunjski", "ru": "Ruski", "sm": "Samoanski", "sa": "Sanskrt", "gd": "Škotski galski", "sr": "Srpski", "sn": "Šona", "sd": "Sindhi", "si": "Sinhaleški", "sk": "Slovački", "sl": "Slovenski", "so": "Somalijski", "st": "Južni Soto", "es": "Španjolski", "su": "Sundanski", "sw": "Svahili", "sv": "Švedski", "tg": "Tadžikistanski", "ta": "Tamilski", "tt": "Tatarski", "te": "Telugu", "th": "Tajlandski", "ti": "Tigrinja", "ts": "Tsonga", "tr": "Turski", "tk": "Turkmenski", "uk": "Ukrajinski", "ur": "Urdu", "ug": "Ujgurski", "uz": "Uzbčki", "vi": "Vijetnamski", "cy": "Velški", "fy": "Zapadnofrizijski", "xh": "Kosa", "yi": "Jidiš", "yo": "Joruba", "zu": "Zuluski" };
  const streamNoConnectionToServer$D = "Nema veze s poslužiteljem";
  const searchField$D = "Traži...";
  const VOTTranslateAPIErrors$D = "Prevođenje pogrešaka iz";
  const VOTDetectService$D = "Usluga definiranja jezika";
  const VOTProxyWorkerHost$D = "Unesite radnu adresu opunomoćenika";
  const VOTM3u8ProxyHost$D = "Unesite adresu radnog opunomoćenika od 93 do 8";
  const proxySettings$D = "Postavke opunomoćenika";
  const translationTakeApproximatelyMinute2$D = "Prijevod će trajati približno {0} minuta";
  const VOTAudioBooster$D = "Povećanje opsega proširenog prijevoda";
  const VOTSubtitlesDesign$D = "Dizajn titlova";
  const VOTSubtitlesFontSize$D = "Veličina fonta titla";
  const VOTSubtitlesOpacity$D = "Transparentnost pozadine titlova";
  const VOTSubtitlesDownloadFormat$D = "Format za preuzimanje titlova";
  const VOTDownloadWithName$D = "Prenesite datoteke s nazivom videozapisa";
  const VOTUpdateLocaleFiles$D = "Ažurirajte datoteke lokalizacije";
  const VOTLocaleHash$D = "Lokalni hash";
  const VOTUpdatedAt$D = "Ažurirano u";
  const VOTNeedWebAudioAPI$D = "Da biste to omogućili, morate imati";
  const VOTMediaCSPEnabledOnSite$D = "Na ovoj je stranici omogućen";
  const VOTOnlyBypassMediaCSP$D = "Koristite ga samo za zaobilaženje";
  const VOTNewAudioPlayer$D = "Koristite novi audio uređaj";
  const VOTUseNewModel$D = "Koristite eksperimentalnu opciju za neke videozapise";
  const TranslationDelayed$D = "Prijevod je malo odgođen";
  const VOTTranslationCompletedNotify$D = "Prijevod na {0} dovršen!";
  const VOTSendNotifyOnComplete$D = "Pošaljite obavijest da je videozapis preveden";
  const VOTBugReport$D = "Prijavi pogrešku";
  const VOTTranslateProxyDisabled$D = "Nesposoban za rad";
  const VOTTranslateProxyEnabled$D = "Omogućeno";
  const VOTTranslateProxyEverything$D = "Punomoćnik za sve";
  const VOTTranslateProxyStatus$D = "Način proksiranja";
  const VOTTranslatedBy$D = "Prevedeno {0}";
  const VOTStreamNotAvailable$D = "Prijenos prijevoda nije dostupan";
  const VOTTranslationTextService$D = "Usluge prevođenja teksta";
  const VOTNotAffectToVoice$D = "Ne utječe na prijevod teksta prilikom zvučenja";
  const DontTranslateSelectedLanguages$D = "Nemojte prevoditi s odabranih jezika";
  const showVideoVolumeSlider$D = "Prikažite klizač glasnoće videozapisa";
  const hotkeysSettings$D = "Postavke prečaca";
  const None$D = "Nitko";
  const VOTUseLivelyVoice$D = "Koristite glasove uživo. Govornici govore kao autohtoni Rusi.";
  const miscSettings$D = "Različite postavke";
  const services$D = { "yandexbrowser": "Jandeks Preglednik", "msedge": "Microsoft Rub", "rust-server": "Server Rust" };
  const aboutExtension$D = "O proširenju";
  const appearance$D = "Izgled";
  const buttonPosition$D = "Položaj gumba u uređaju za reprodukciju";
  const position$D = { "left": "Lijevo", "right": "Ispravno", "top": "Gornji", "default": "Zadano" };
  const secs$D = "sek";
  const autoHideButtonDelay$D = "Zadržite prije skrivanja gumba za prijevod";
  const notFound$D = "nije pronađeno";
  const minButtonPositionContainer$D = "Položaj gumba mijenja se samo na igračima većim od 600 piksela.";
  const VOTTranslateProxyStatusDefault$D = "Potpuno onemogućavanje opunomoćenika u vašoj zemlji može dovesti do kvara proširenja";
  const PressTheKeyCombination$D = "Pritisnite kombinaciju tipki...";
  const VOTUseAudioDownload$D = "Koristite preuzimanje zvuka";
  const VOTUseAudioDownloadWarning$D = "Onemogućavanje preuzimanja zvuka može utjecati na funkcionalnost proširenja";
  const VOTAccountRequired$D = "Morate se prijaviti da biste koristili ovu značajku";
  const VOTMyAccount$D = "Moj račun";
  const VOTLogin$D = "Prijaviti se";
  const VOTLogout$D = "Odjava";
  const VOTRefresh$D = "Osvježiti";
  const VOTYandexToken$D = "Unesite IDB-Token Jandeksa";
  const VOTYandexTokenInfo$D = "U ovom polju možete ručno odrediti token računa. Imajte na umu da ne provjeravamo njegovu valjanost prije slanja zahtjeva za prijenos";
  const VOTLoginViaToken$D = "Prijavite se pomoću tokena";
  const subtitlesSmartLayout$D = "Promišljeni izgled titlova";
  const smartDucking$D = "Prilagodljiva glasnoća";
  const VOTAutoSubtitles$D = "Titlovi prilikom otvaranja";
  const VOTSubtitlesFont$D = "Font titlova";
  const __vite_glob_0_22 = {
    recommended: recommended$D,
    translateVideo: translateVideo$D,
    disableTranslate: disableTranslate$D,
    translationSettings: translationSettings$D,
    subtitlesSettings: subtitlesSettings$D,
    resetSettings: resetSettings$D,
    videoBeingTranslated: videoBeingTranslated$D,
    videoLanguage: videoLanguage$D,
    translationLanguage: translationLanguage$D,
    translationTake: translationTake$D,
    translationTakeMoreThanHour: translationTakeMoreThanHour$D,
    translationTakeAboutMinute: translationTakeAboutMinute$D,
    translationTakeFewMinutes: translationTakeFewMinutes$D,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$D,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$D,
    requestTranslationFailed: requestTranslationFailed$D,
    audioNotReceived: audioNotReceived$D,
    audioFormatNotSupported: audioFormatNotSupported$D,
    VOTAutoTranslate: VOTAutoTranslate$D,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$D,
    VOTVolume: VOTVolume$D,
    VOTVolumeTranslation: VOTVolumeTranslation$D,
    VOTAutoSetVolume: VOTAutoSetVolume$D,
    VOTShowVideoSlider: VOTShowVideoSlider$D,
    VOTSyncVolume: VOTSyncVolume$D,
    VOTDisableFromYourLang: VOTDisableFromYourLang$D,
    VOTVideoIsTooLong: VOTVideoIsTooLong$D,
    VOTNoVideoIDFound: VOTNoVideoIDFound$D,
    VOTSubtitles: VOTSubtitles$D,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$D,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$D,
    VOTHighlightWords: VOTHighlightWords$D,
    VOTTranslatedFrom: VOTTranslatedFrom$D,
    VOTAutogenerated: VOTAutogenerated$D,
    VOTSettings: VOTSettings$D,
    VOTMenuLanguage: VOTMenuLanguage$D,
    VOTAuthors: VOTAuthors$D,
    VOTVersion: VOTVersion$D,
    VOTLoader: VOTLoader$D,
    VOTBrowser: VOTBrowser$D,
    VOTShowPiPButton: VOTShowPiPButton$D,
    langs: langs$D,
    streamNoConnectionToServer: streamNoConnectionToServer$D,
    searchField: searchField$D,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$D,
    VOTDetectService: VOTDetectService$D,
    VOTProxyWorkerHost: VOTProxyWorkerHost$D,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$D,
    proxySettings: proxySettings$D,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$D,
    VOTAudioBooster: VOTAudioBooster$D,
    VOTSubtitlesDesign: VOTSubtitlesDesign$D,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$D,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$D,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$D,
    VOTDownloadWithName: VOTDownloadWithName$D,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$D,
    VOTLocaleHash: VOTLocaleHash$D,
    VOTUpdatedAt: VOTUpdatedAt$D,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$D,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$D,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$D,
    VOTNewAudioPlayer: VOTNewAudioPlayer$D,
    VOTUseNewModel: VOTUseNewModel$D,
    TranslationDelayed: TranslationDelayed$D,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$D,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$D,
    VOTBugReport: VOTBugReport$D,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$D,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$D,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$D,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$D,
    VOTTranslatedBy: VOTTranslatedBy$D,
    VOTStreamNotAvailable: VOTStreamNotAvailable$D,
    VOTTranslationTextService: VOTTranslationTextService$D,
    VOTNotAffectToVoice: VOTNotAffectToVoice$D,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$D,
    showVideoVolumeSlider: showVideoVolumeSlider$D,
    hotkeysSettings: hotkeysSettings$D,
    None: None$D,
    VOTUseLivelyVoice: VOTUseLivelyVoice$D,
    miscSettings: miscSettings$D,
    services: services$D,
    aboutExtension: aboutExtension$D,
    appearance: appearance$D,
    buttonPosition: buttonPosition$D,
    position: position$D,
    secs: secs$D,
    autoHideButtonDelay: autoHideButtonDelay$D,
    notFound: notFound$D,
    minButtonPositionContainer: minButtonPositionContainer$D,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$D,
    PressTheKeyCombination: PressTheKeyCombination$D,
    VOTUseAudioDownload: VOTUseAudioDownload$D,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$D,
    VOTAccountRequired: VOTAccountRequired$D,
    VOTMyAccount: VOTMyAccount$D,
    VOTLogin: VOTLogin$D,
    VOTLogout: VOTLogout$D,
    VOTRefresh: VOTRefresh$D,
    VOTYandexToken: VOTYandexToken$D,
    VOTYandexTokenInfo: VOTYandexTokenInfo$D,
    VOTLoginViaToken: VOTLoginViaToken$D,
    subtitlesSmartLayout: subtitlesSmartLayout$D,
    smartDucking: smartDucking$D,
    VOTAutoSubtitles: VOTAutoSubtitles$D,
    VOTSubtitlesFont: VOTSubtitlesFont$D
  };
  const recommended$C = "ajánlott";
  const translateVideo$C = "Videó fordítása";
  const disableTranslate$C = "Kapcsolja ki";
  const translationSettings$C = "Fordítási beállítások";
  const subtitlesSettings$C = "Feliratok beállításai";
  const resetSettings$C = "Beállítások visszaállítása";
  const videoBeingTranslated$C = "A videó fordítása folyamatban van";
  const videoLanguage$C = "Videó nyelve";
  const translationLanguage$C = "Fordítás nyelve";
  const translationTake$C = "A fordítás lesz";
  const translationTakeMoreThanHour$C = "A fordítás több mint egy órát vesz igénybe";
  const translationTakeAboutMinute$C = "A fordítás körülbelül egy percet vesz igénybe";
  const translationTakeFewMinutes$C = "A fordítás néhány percet vesz igénybe";
  const translationTakeApproximatelyMinutes$C = "A fordítás körülbelül {0} percet vesz igénybe";
  const translationTakeApproximatelyMinute$C = "A fordítás körülbelül {0} percet vesz igénybe";
  const requestTranslationFailed$C = "Nem sikerült a videó fordítását kérni";
  const audioNotReceived$C = "Az Audio link nem érkezett meg";
  const audioFormatNotSupported$C = "Az audio formátum nem támogatott";
  const VOTAutoTranslate$C = "Fordítás nyitva";
  const VOTDontTranslateYourLang$C = "Ne fordítson az én nyelvemről";
  const VOTVolume$C = "Videó kötet:";
  const VOTVolumeTranslation$C = "Fordítási kötet:";
  const VOTAutoSetVolume$C = "Csökkentse a videó hangerejét";
  const VOTShowVideoSlider$C = "Videó hangerő csúszka";
  const VOTSyncVolume$C = "Link fordítás és videó kötet";
  const VOTDisableFromYourLang$C = "Letiltotta a videó fordítását az Ön nyelvén";
  const VOTVideoIsTooLong$C = "A videó túl hosszú";
  const VOTNoVideoIDFound$C = "Nem található video ID";
  const VOTSubtitles$C = "Feliratok";
  const VOTSubtitlesDisabled$C = "Mozgáskorlátozottak";
  const VOTSubtitlesMaxLength$C = "Feliratok maximális hossza";
  const VOTHighlightWords$C = "Jelölje ki a szavakat";
  const VOTTranslatedFrom$C = "fordította:";
  const VOTAutogenerated$C = "autogenerált";
  const VOTSettings$C = "VOT Beállítások";
  const VOTMenuLanguage$C = "Menü nyelve";
  const VOTAuthors$C = "Szerzők";
  const VOTVersion$C = "Verzió";
  const VOTLoader$C = "Rakodó";
  const VOTBrowser$C = "Böngésző";
  const VOTShowPiPButton$C = "PiP gomb megjelenítése";
  const langs$C = { "auto": "Autó", "af": "Afrikaans", "ak": "Akan", "sq": "Albán", "am": "Amhara", "ar": "Arab", "hy": "Örmény", "as": "Asszámi", "ay": "Aymara", "az": "Azerbajdzsáni", "bn": "Bangla", "eu": "Baszk", "be": "Belorusz", "bho": "Bhojpuri", "bs": "Bosnyák", "bg": "Bolgár", "my": "Burmai", "ca": "Katalán", "ceb": "Cebuanóban", "zh": "Kínai", "zh-Hans": "Kínai (Egyszerűsített)", "zh-Hant": "Kínai (Hagyományos)", "co": "Korzikai", "hr": "Horvát", "cs": "Cseh", "da": "Dán", "dv": "Divehi", "nl": "Magyar", "en": "Magyar", "eo": "Eszperantó", "et": "Észt", "ee": "Anyajuh", "fil": "Fülöp-szigeteki", "fi": "Finn", "fr": "Francia", "gl": "Galíciai", "lg": "Ganda", "ka": "Grúz", "de": "Német", "el": "Görög", "gn": "Guarani", "gu": "Gudzsaráti", "ht": "Haiti Kreol", "ha": "Hausa", "haw": "Hawaii", "iw": "Héber", "hi": "Hindi", "hmn": "Hmong", "hu": "Magyar", "is": "Izlandi", "ig": "Igbót", "id": "Indonéz", "ga": "Ír", "it": "Olasz", "ja": "Japán", "jv": "Jávai", "kn": "Kannada", "kk": "Kazah", "km": "Khmer", "rw": "Kinyarwanda", "ko": "Koreai", "kri": "Krio", "ku": "Kurd", "ky": "Kirgiz", "lo": "Laosz", "la": "Latin", "lv": "Lett", "ln": "Lingala", "lt": "Litván", "lb": "Luxemburgi", "mk": "Macedón", "mg": "Madagaszkári", "ms": "Maláj", "ml": "Malayalam", "mt": "Máltai", "mi": "M Enterprises", "mr": "Marathi", "mn": "Mongol", "ne": "Nepáli", "nso": "Northern Sotho", "no": "Norvég", "ny": "Nyanja", "or": "Odia", "om": "Oromo", "ps": "Pastu", "fa": "Perzsa", "pl": "Lengyel", "pt": "Portugál", "pa": "Pandzsábi", "qu": "Kecsua", "ro": "Román", "ru": "Orosz", "sm": "Szamoai", "sa": "Szanszkrit", "gd": "Skót Gael", "sr": "Szerb", "sn": "Sona", "sd": "Szindhi", "si": "Szingaléz", "sk": "Szlovák", "sl": "Szlovén", "so": "Szomáliai", "st": "Southern Sotho", "es": "Spanyol", "su": "Sundanese", "sw": "Szuahéli", "sv": "Svéd", "tg": "Tádzsik", "ta": "Tamil", "tt": "Tatár", "te": "Telugu", "th": "Thai", "ti": "Tigrinya", "ts": "Conga", "tr": "Török", "tk": "Türkmén", "uk": "Ukrán", "ur": "Urdu", "ug": "Ujgur", "uz": "Üzbég", "vi": "Vietnámi", "cy": "Walesi", "fy": "Nyugati Fríz", "xh": "Hszosza", "yi": "Jiddis", "yo": "Joruba", "zu": "Zulu" };
  const streamNoConnectionToServer$C = "Nincs kapcsolat a szerverrel";
  const searchField$C = "Keresés...";
  const VOTTranslateAPIErrors$C = "Hibák lefordítása az API - ból";
  const VOTDetectService$C = "Nyelvfelismerő szolgáltatás";
  const VOTProxyWorkerHost$C = "Adja meg a proxy munkavállaló címét";
  const VOTM3u8ProxyHost$C = "Írja be az m3u8 proxy munkás címét";
  const proxySettings$C = "Proxy Beállítások";
  const translationTakeApproximatelyMinute2$C = "A fordítás körülbelül {0} percet vesz igénybe";
  const VOTAudioBooster$C = "Bővített fordítási volumen növekedés";
  const VOTSubtitlesDesign$C = "Feliratok tervezése";
  const VOTSubtitlesFontSize$C = "A feliratok betűmérete";
  const VOTSubtitlesOpacity$C = "A felirat hátterének átláthatósága";
  const VOTSubtitlesDownloadFormat$C = "A feliratok letöltésének formátuma";
  const VOTDownloadWithName$C = "Töltsön le fájlokat a videó nevével";
  const VOTUpdateLocaleFiles$C = "Lokalizációs fájlok frissítése";
  const VOTLocaleHash$C = "Locale hash";
  const VOTUpdatedAt$C = "Frissítve";
  const VOTNeedWebAudioAPI$C = "Ennek engedélyezéséhez rendelkeznie kell egy Web Audio API-val";
  const VOTMediaCSPEnabledOnSite$C = "A Media CSP engedélyezve van ezen a webhelyen";
  const VOTOnlyBypassMediaCSP$C = "Csak a Media CSP megkerülésére használja";
  const VOTNewAudioPlayer$C = "Az új audiolejátszó használata";
  const VOTUseNewModel$C = "Használja a Yandex voices kísérleti változatát néhány videóhoz";
  const TranslationDelayed$C = "A fordítás kissé késik";
  const VOTTranslationCompletedNotify$C = "A fordítás a {0} befejeződött!";
  const VOTSendNotifyOnComplete$C = "Küldjön értesítést arról, hogy a videót lefordították";
  const VOTBugReport$C = "Hibabejelentés";
  const VOTTranslateProxyDisabled$C = "Mozgáskorlátozottak";
  const VOTTranslateProxyEnabled$C = "Engedélyezett";
  const VOTTranslateProxyEverything$C = "Proxy mindent";
  const VOTTranslateProxyStatus$C = "Proxy mód";
  const VOTTranslatedBy$C = "Fordította: {0}";
  const VOTStreamNotAvailable$C = "A stream fordítása nem érhető el";
  const VOTTranslationTextService$C = "Szövegfordítási szolgáltatás";
  const VOTNotAffectToVoice$C = "Nem befolyásolja a szöveg fordítását a voice over alkalmazásban";
  const DontTranslateSelectedLanguages$C = "Ne fordítson a kiválasztott nyelvekről";
  const showVideoVolumeSlider$C = "A videó hangerő csúszkájának megjelenítése";
  const hotkeysSettings$C = "Gyorsbillentyűk beállításai";
  const None$C = "Nincs";
  const VOTUseLivelyVoice$C = "Használjon élénk hangokat. A hangszórók úgy hangzik, mint a natív oroszok.";
  const miscSettings$C = "Egyéb beállítások";
  const services$C = { "yandexbrowser": "Yandex Browser", "msedge": "Microsoft Edge", "rust-server": "Rust Server" };
  const aboutExtension$C = "A kiterjesztésről";
  const appearance$C = "Megjelenés";
  const buttonPosition$C = "Gomb pozíció a lejátszóban";
  const position$C = { "left": "Balra", "right": "Jobb", "top": "Felső", "default": "Alapértelmezett" };
  const secs$C = "secs";
  const autoHideButtonDelay$C = "Késleltetés a Fordítás gomb elrejtése előtt";
  const notFound$C = "nem található";
  const minButtonPositionContainer$C = "A gomb pozíciója csak a 600 pixelnél nagyobb játékosoknál változik.";
  const VOTTranslateProxyStatusDefault$C = "A proxy teljes letiltása az Ön országában megszakíthatja a kiterjesztést";
  const PressTheKeyCombination$C = "Nyomja meg a billentyűkombinációt...";
  const VOTUseAudioDownload$C = "Használja az audio letöltést";
  const VOTUseAudioDownloadWarning$C = "A hangletöltések letiltása befolyásolhatja a kiterjesztés működését";
  const VOTAccountRequired$C = "A funkció használatához be kell jelentkeznie";
  const VOTMyAccount$C = "Saját fiók";
  const VOTLogin$C = "Bejelentkezés";
  const VOTLogout$C = "Kijelentkezés";
  const VOTRefresh$C = "Frissítés";
  const VOTYandexToken$C = "Adja meg a Yandex OAuth Tokent";
  const VOTYandexTokenInfo$C = "Ebben a mezőben manuálisan beállíthatja a fiók tokent. Felhívjuk figyelmét, hogy a fordítási kérelem elküldése előtt nem ellenőrizzük annak érvényességét";
  const VOTLoginViaToken$C = "Bejelentkezés tokenen keresztül";
  const subtitlesSmartLayout$C = "Intelligens felirat elrendezés";
  const smartDucking$C = "Adaptív kötet";
  const VOTAutoSubtitles$C = "Feliratok nyitva";
  const VOTSubtitlesFont$C = "Felirat betűtípus";
  const __vite_glob_0_23 = {
    recommended: recommended$C,
    translateVideo: translateVideo$C,
    disableTranslate: disableTranslate$C,
    translationSettings: translationSettings$C,
    subtitlesSettings: subtitlesSettings$C,
    resetSettings: resetSettings$C,
    videoBeingTranslated: videoBeingTranslated$C,
    videoLanguage: videoLanguage$C,
    translationLanguage: translationLanguage$C,
    translationTake: translationTake$C,
    translationTakeMoreThanHour: translationTakeMoreThanHour$C,
    translationTakeAboutMinute: translationTakeAboutMinute$C,
    translationTakeFewMinutes: translationTakeFewMinutes$C,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$C,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$C,
    requestTranslationFailed: requestTranslationFailed$C,
    audioNotReceived: audioNotReceived$C,
    audioFormatNotSupported: audioFormatNotSupported$C,
    VOTAutoTranslate: VOTAutoTranslate$C,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$C,
    VOTVolume: VOTVolume$C,
    VOTVolumeTranslation: VOTVolumeTranslation$C,
    VOTAutoSetVolume: VOTAutoSetVolume$C,
    VOTShowVideoSlider: VOTShowVideoSlider$C,
    VOTSyncVolume: VOTSyncVolume$C,
    VOTDisableFromYourLang: VOTDisableFromYourLang$C,
    VOTVideoIsTooLong: VOTVideoIsTooLong$C,
    VOTNoVideoIDFound: VOTNoVideoIDFound$C,
    VOTSubtitles: VOTSubtitles$C,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$C,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$C,
    VOTHighlightWords: VOTHighlightWords$C,
    VOTTranslatedFrom: VOTTranslatedFrom$C,
    VOTAutogenerated: VOTAutogenerated$C,
    VOTSettings: VOTSettings$C,
    VOTMenuLanguage: VOTMenuLanguage$C,
    VOTAuthors: VOTAuthors$C,
    VOTVersion: VOTVersion$C,
    VOTLoader: VOTLoader$C,
    VOTBrowser: VOTBrowser$C,
    VOTShowPiPButton: VOTShowPiPButton$C,
    langs: langs$C,
    streamNoConnectionToServer: streamNoConnectionToServer$C,
    searchField: searchField$C,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$C,
    VOTDetectService: VOTDetectService$C,
    VOTProxyWorkerHost: VOTProxyWorkerHost$C,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$C,
    proxySettings: proxySettings$C,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$C,
    VOTAudioBooster: VOTAudioBooster$C,
    VOTSubtitlesDesign: VOTSubtitlesDesign$C,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$C,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$C,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$C,
    VOTDownloadWithName: VOTDownloadWithName$C,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$C,
    VOTLocaleHash: VOTLocaleHash$C,
    VOTUpdatedAt: VOTUpdatedAt$C,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$C,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$C,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$C,
    VOTNewAudioPlayer: VOTNewAudioPlayer$C,
    VOTUseNewModel: VOTUseNewModel$C,
    TranslationDelayed: TranslationDelayed$C,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$C,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$C,
    VOTBugReport: VOTBugReport$C,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$C,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$C,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$C,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$C,
    VOTTranslatedBy: VOTTranslatedBy$C,
    VOTStreamNotAvailable: VOTStreamNotAvailable$C,
    VOTTranslationTextService: VOTTranslationTextService$C,
    VOTNotAffectToVoice: VOTNotAffectToVoice$C,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$C,
    showVideoVolumeSlider: showVideoVolumeSlider$C,
    hotkeysSettings: hotkeysSettings$C,
    None: None$C,
    VOTUseLivelyVoice: VOTUseLivelyVoice$C,
    miscSettings: miscSettings$C,
    services: services$C,
    aboutExtension: aboutExtension$C,
    appearance: appearance$C,
    buttonPosition: buttonPosition$C,
    position: position$C,
    secs: secs$C,
    autoHideButtonDelay: autoHideButtonDelay$C,
    notFound: notFound$C,
    minButtonPositionContainer: minButtonPositionContainer$C,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$C,
    PressTheKeyCombination: PressTheKeyCombination$C,
    VOTUseAudioDownload: VOTUseAudioDownload$C,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$C,
    VOTAccountRequired: VOTAccountRequired$C,
    VOTMyAccount: VOTMyAccount$C,
    VOTLogin: VOTLogin$C,
    VOTLogout: VOTLogout$C,
    VOTRefresh: VOTRefresh$C,
    VOTYandexToken: VOTYandexToken$C,
    VOTYandexTokenInfo: VOTYandexTokenInfo$C,
    VOTLoginViaToken: VOTLoginViaToken$C,
    subtitlesSmartLayout: subtitlesSmartLayout$C,
    smartDucking: smartDucking$C,
    VOTAutoSubtitles: VOTAutoSubtitles$C,
    VOTSubtitlesFont: VOTSubtitlesFont$C
  };
  const recommended$B = "առաջարկվող";
  const translateVideo$B = "Թարգմանեք տեսանյութը";
  const disableTranslate$B = "Անջատել";
  const translationSettings$B = "Թարգմանության կարգավորումներ";
  const subtitlesSettings$B = "Ենթագրերի կարգավորումներ";
  const resetSettings$B = "Գործարանային վերականգնում";
  const videoBeingTranslated$B = "Տեսանյութը թարգմանվում է";
  const videoLanguage$B = "Տեսանյութի լեզուն";
  const translationLanguage$B = "Թարգմանության լեզու";
  const translationTake$B = "Թարգմանությունը կտեւի";
  const translationTakeMoreThanHour$B = "Փոխանցումը կտևի ավելի քան մեկ ժամ";
  const translationTakeAboutMinute$B = "Փոխանցումը կտևի մոտ մեկ րոպե";
  const translationTakeFewMinutes$B = "Փոխանցումը կտևի մի քանի րոպե";
  const translationTakeApproximatelyMinutes$B = "Թարգմանությունը կտեւի մոտավորապես {0} րոպե";
  const translationTakeApproximatelyMinute$B = "Թարգմանությունը կտեւի մոտավորապես {0} րոպե";
  const requestTranslationFailed$B = "Չհաջողվեց պահանջել տեսանյութի թարգմանություն";
  const audioNotReceived$B = "Աուդիո հաղորդակցություն չի ստացվել";
  const audioFormatNotSupported$B = "Աուդիո Ձևաչափը չի ապահովվում";
  const VOTAutoTranslate$B = "Թարգմանել բաց";
  const VOTDontTranslateYourLang$B = "Մի թարգմանիր իմ լեզվից";
  const VOTVolume$B = "Տեսանյութի ծավալը:";
  const VOTVolumeTranslation$B = "Փոխանցման ծավալը:";
  const VOTAutoSetVolume$B = "Նվազեցրեք տեսանյութի ծավալը";
  const VOTShowVideoSlider$B = "Տեսանյութի ծավալի սլայդեր";
  const VOTSyncVolume$B = "Հղումների թարգմանություն և տեսանյութերի ծավալ";
  const VOTDisableFromYourLang$B = "Դուք անջատել եք տեսանյութի թարգմանությունը Ձեր լեզվով";
  const VOTVideoIsTooLong$B = "Տեսանյութը չափազանց երկար է";
  const VOTNoVideoIDFound$B = "Տեսանյութի ID չի գտնվել";
  const VOTSubtitles$B = "Ենթագրեր";
  const VOTSubtitlesDisabled$B = "Անաշխատունակ";
  const VOTSubtitlesMaxLength$B = "Ենթագրերի առավելագույն երկարությունը";
  const VOTHighlightWords$B = "Ընդգծիր բառերը";
  const VOTTranslatedFrom$B = "թարգմանված է";
  const VOTAutogenerated$B = "Ավտոմատ գեներացվել";
  const VOTSettings$B = "VOT կարգավորումներ";
  const VOTMenuLanguage$B = "Մենու language";
  const VOTAuthors$B = "Հեղինակներ";
  const VOTVersion$B = "Տարբերակ";
  const VOTLoader$B = "Բեռնիչ";
  const VOTBrowser$B = "Բրաուզեր";
  const VOTShowPiPButton$B = "Ցուցադրել կետավոր կոճակը";
  const langs$B = { "auto": "Ավտո", "af": "Աֆրիկաանս", "ak": "Ական", "sq": "Ալբաներեն", "am": "Ամհարերեն", "ar": "Արաբերեն լեզու", "hy": "Հայկական", "as": "Ասսամերեն", "ay": "Այմարա", "az": "Ադրբեջաներեն", "bn": "Բանգլա", "eu": "Բասկերեն", "be": "Բելառուսերեն", "bho": "Բհոջպուրի", "bs": "Բոսնիերեն", "bg": "Բուլղարերեն", "my": "Բիրմաներեն", "ca": "Կատալաներեն", "ceb": "Սեբուանո", "zh": "Չինական", "zh-Hans": "Չինարեն (պարզեցված)", "zh-Hant": "Չինարեն (Ավանդական)", "co": "Կորսիկյան", "hr": "Խորվաթերեն", "cs": "Չեխերեն", "da": "Դանիերեն", "dv": "Դիվեհի", "nl": "Հոլանդերեն", "en": "Անգլերեն", "eo": "Էսպերանտո", "et": "Էստոնական", "ee": "Ոչխար", "fil": "Ֆիլիպինցի", "fi": "Ֆիններեն", "fr": "Ֆրանսերեն", "gl": "Գալիսիաներեն", "lg": "Գանդա", "ka": "Վրացերեն", "de": "Գերմաներեն", "el": "Հունական", "gn": "Գուարանի", "gu": "Գուջարաթի", "ht": "Հաիթիական կրեոլ", "ha": "Հաուսա", "haw": "Հավայաներեն", "iw": "Հրեա", "hi": "Հինդի", "hmn": "Հմոնգ", "hu": "Հունգարերեն", "is": "Իսլանդերեն", "ig": "Իգբո", "id": "Ինդոնեզերեն", "ga": "Իռլանդերեն", "it": "Իտալերեն", "ja": "Ճապոներեն", "jv": "Ճավայերեն", "kn": "Կաննադա", "kk": "Ղազախ", "km": "Քմերական", "rw": "Քինյարվանդա", "ko": "Կորեերեն", "kri": "Կրիո", "ku": "Քրդî", "ky": "Կիրգիզ", "lo": "Լաոսերեն", "la": "Լատիներեն", "lv": "Լատվերեն", "ln": "Լինգալա", "lt": "Լիտվերեն", "lb": "Լյուքսեմբուրգերեն", "mk": "Մակեդոներեն", "mg": "Մալագասյ", "ms": "Մալայերեն", "ml": "Մալայալամ", "mt": "Մալթերեն", "mi": "Մաորի", "mr": "Մարաթի", "mn": "Մոնղոլական", "ne": "Նեպալերեն", "nso": "Հյուսիսային Սոտո", "no": "Նորվեգերեն լեզու", "ny": "Նյանջա", "or": "Օդիա", "om": "Օրոմո", "ps": "Փուշթու", "fa": "Պարսկերեն", "pl": "Պոլսկին", "pt": "Պորտուգվêս", "pa": "Փանջաբի", "qu": "Կեչուա", "ro": "Ռումիներեն", "ru": "Հայերեն", "sm": "Սամոացի", "sa": "Սանսկրիտ", "gd": "Շոտլանդական գաելերեն", "sr": "Սերբերեն", "sn": "Շոնա", "sd": "Սինդհի", "si": "Սինհալերեն", "sk": "Սլովակ", "sl": "Սլովեներեն", "so": "Սոմալի", "st": "Հարավային Սոտո", "es": "Էսպաñոլ", "su": "Սունդանսկի", "sw": "Սուահիլի", "sv": "Շվեդերեն", "tg": "Տաջիկերեն", "ta": "Թամիլերեն", "tt": "Թաթարերեն", "te": "Թելուգու", "th": "Թայերեն", "ti": "Տիգրինյա", "ts": "Ցոնգա", "tr": "Թուրքերեն", "tk": "Թուրքմենական", "uk": "Ուկրաիներեն", "ur": "Ուրդու", "ug": "Ույղուրերեն", "uz": "Ուզբեկստան", "vi": "Վիետնամերեն", "cy": "Ուելսերեն", "fy": "Արևմտաֆրիզերեն", "xh": "Հյուս", "yi": "Իդիշ", "yo": "Յորուբա", "zu": "Զուլուսերեն" };
  const streamNoConnectionToServer$B = "Սերվերի միացում չկա";
  const searchField$B = "Որոնում..";
  const VOTTranslateAPIErrors$B = "API-ից սխալների փոխանցում";
  const VOTDetectService$B = "Լեզվի սահմանման ծառայություն";
  const VOTProxyWorkerHost$B = "Մուտքագրեք վստահված սերվերի աշխատանքային հասցեն";
  const VOTM3u8ProxyHost$B = "Մուտքագրեք աշխատանքային m3u8 վստահված անձի հասցեն";
  const proxySettings$B = "Վստահված սերվերի կարգավորումներ";
  const translationTakeApproximatelyMinute2$B = "Թարգմանությունը կտեւի մոտավորապես {0} րոպե";
  const VOTAudioBooster$B = "Ընդլայնված թարգմանության ծավալի ավելացում";
  const VOTSubtitlesDesign$B = "Ենթագրերի ձևավորում";
  const VOTSubtitlesFontSize$B = "Ենթագրերի տառատեսակի չափը";
  const VOTSubtitlesOpacity$B = "Ենթագրերի ֆոնի թափանցիկություն";
  const VOTSubtitlesDownloadFormat$B = "Ձևաչափ ՝ ենթագրերը ներբեռնելու համար";
  const VOTDownloadWithName$B = "Վերբեռնեք ֆայլեր տեսանյութի անունով";
  const VOTUpdateLocaleFiles$B = "Թարմացրեք տեղայնացման ֆայլերը";
  const VOTLocaleHash$B = "Հաշ locales";
  const VOTUpdatedAt$B = "Թարմացվել է";
  const VOTNeedWebAudioAPI$B = "Սա միացնելու համար դուք պետք է ունենաք վեբ աուդիո API";
  const VOTMediaCSPEnabledOnSite$B = "Media CSP-ն միացված է այս կայքում";
  const VOTOnlyBypassMediaCSP$B = "Օգտագործեք այն միայն Media CSP-ն շրջանցելու համար";
  const VOTNewAudioPlayer$B = "Օգտագործեք նոր աուդիո նվագարկիչ";
  const VOTUseNewModel$B = "Որոշ տեսանյութերի համար օգտագործեք Yandex voices-ի փորձնական տարբերակը";
  const TranslationDelayed$B = "Թարգմանությունը Մի փոքր ուշանում է";
  const VOTTranslationCompletedNotify$B = "Թարգմանությունը {0} ավարտված է:";
  const VOTSendNotifyOnComplete$B = "Ուղարկեք ծանուցում այն մասին, որ տեսանյութը թարգմանվել է";
  const VOTBugReport$B = "Հաղորդել սխալի մասին";
  const VOTTranslateProxyDisabled$B = "Անաշխատունակ";
  const VOTTranslateProxyEnabled$B = "Ներառված է";
  const VOTTranslateProxyEverything$B = "Վստահված սերվեր ամեն ինչի համար";
  const VOTTranslateProxyStatus$B = "Պրոքսի ռեժիմ";
  const VOTTranslatedBy$B = "Թարգմանված {0}";
  const VOTStreamNotAvailable$B = "Թարգմանության հեռարձակումը հասանելի չէ";
  const VOTTranslationTextService$B = "Տեքստերի թարգմանության ծառայություններ";
  const VOTNotAffectToVoice$B = "Հնչյունավորելիս չի ազդում տեքստի թարգմանության վրա";
  const DontTranslateSelectedLanguages$B = "Մի թարգմանեք ընտրված լեզուներից";
  const showVideoVolumeSlider$B = "Displayուցադրել տեսանյութի ծավալի սահիչը";
  const hotkeysSettings$B = "Թեժ ստեղնաշարի կարգավորումներ";
  const None$B = "Ոչ ոք";
  const VOTUseLivelyVoice$B = "Օգտագործեք կենդանի ձայներ: Խոսողները խոսում են բնիկ ռուսների պես:";
  const miscSettings$B = "Տարբեր պարամետրեր";
  const services$B = { "yandexbrowser": "Yandex Browser", "msedge": "Microsoft Edge", "rust-server": "Rust Սերվեր" };
  const aboutExtension$B = "Ընդլայնման մասին";
  const appearance$B = "Արտաքին տեսք";
  const buttonPosition$B = "Կոճակի դիրքը նվագարկիչում";
  const position$B = { "left": "Ձախ", "right": "Ճիշտ", "top": "Գագաթ", "default": "Լռելյայն" };
  const secs$B = "Սեկ";
  const autoHideButtonDelay$B = "Հետաձգեք թարգմանության կոճակը թաքցնելուց առաջ";
  const notFound$B = "չի գտնվել";
  const minButtonPositionContainer$B = "Կոճակի դիրքը փոխվում է միայն 600 պիքսելից ավելի մեծ նվագարկիչների վրա:";
  const VOTTranslateProxyStatusDefault$B = "Ձեր երկրում վստահված անձի սերվերի ամբողջական անջատումը կարող է հանգեցնել ընդլայնման կոտրմանը";
  const PressTheKeyCombination$B = "Սեղմեք ստեղնաշարի համադրությունը:..";
  const VOTUseAudioDownload$B = "Օգտագործեք աուդիո ներբեռնում";
  const VOTUseAudioDownloadWarning$B = "Աուդիո բեռնման անջատումը կարող է ազդել ընդլայնման ֆունկցիոնալության վրա";
  const VOTAccountRequired$B = "Այս գործառույթն օգտագործելու համար հարկավոր է մուտք գործել համակարգ";
  const VOTMyAccount$B = "Իմ հաշիվը";
  const VOTLogin$B = "Մուտք գործել";
  const VOTLogout$B = "Ելք";
  const VOTRefresh$B = "Թարմացնել";
  const VOTYandexToken$B = "Մուտքագրեք OAuth-Yandex Token";
  const VOTYandexTokenInfo$B = "Այս դաշտում կարող եք ձեռքով նշել հաշվի նշանը: Խնդրում ենք նկատի ունենալ, որ մենք չենք ստուգում դրա վավերականությունը նախքան փոխանցման հարցում ուղարկելը";
  const VOTLoginViaToken$B = "Մուտք գործեք նշանի հետ";
  const subtitlesSmartLayout$B = "Մտածված ենթագրերի դասավորություն";
  const smartDucking$B = "Հարմարվողական ծավալ";
  const VOTAutoSubtitles$B = "Ենթագրերը բացելիս";
  const VOTSubtitlesFont$B = "Ենթագրերի տառատեսակ";
  const __vite_glob_0_24 = {
    recommended: recommended$B,
    translateVideo: translateVideo$B,
    disableTranslate: disableTranslate$B,
    translationSettings: translationSettings$B,
    subtitlesSettings: subtitlesSettings$B,
    resetSettings: resetSettings$B,
    videoBeingTranslated: videoBeingTranslated$B,
    videoLanguage: videoLanguage$B,
    translationLanguage: translationLanguage$B,
    translationTake: translationTake$B,
    translationTakeMoreThanHour: translationTakeMoreThanHour$B,
    translationTakeAboutMinute: translationTakeAboutMinute$B,
    translationTakeFewMinutes: translationTakeFewMinutes$B,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$B,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$B,
    requestTranslationFailed: requestTranslationFailed$B,
    audioNotReceived: audioNotReceived$B,
    audioFormatNotSupported: audioFormatNotSupported$B,
    VOTAutoTranslate: VOTAutoTranslate$B,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$B,
    VOTVolume: VOTVolume$B,
    VOTVolumeTranslation: VOTVolumeTranslation$B,
    VOTAutoSetVolume: VOTAutoSetVolume$B,
    VOTShowVideoSlider: VOTShowVideoSlider$B,
    VOTSyncVolume: VOTSyncVolume$B,
    VOTDisableFromYourLang: VOTDisableFromYourLang$B,
    VOTVideoIsTooLong: VOTVideoIsTooLong$B,
    VOTNoVideoIDFound: VOTNoVideoIDFound$B,
    VOTSubtitles: VOTSubtitles$B,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$B,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$B,
    VOTHighlightWords: VOTHighlightWords$B,
    VOTTranslatedFrom: VOTTranslatedFrom$B,
    VOTAutogenerated: VOTAutogenerated$B,
    VOTSettings: VOTSettings$B,
    VOTMenuLanguage: VOTMenuLanguage$B,
    VOTAuthors: VOTAuthors$B,
    VOTVersion: VOTVersion$B,
    VOTLoader: VOTLoader$B,
    VOTBrowser: VOTBrowser$B,
    VOTShowPiPButton: VOTShowPiPButton$B,
    langs: langs$B,
    streamNoConnectionToServer: streamNoConnectionToServer$B,
    searchField: searchField$B,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$B,
    VOTDetectService: VOTDetectService$B,
    VOTProxyWorkerHost: VOTProxyWorkerHost$B,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$B,
    proxySettings: proxySettings$B,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$B,
    VOTAudioBooster: VOTAudioBooster$B,
    VOTSubtitlesDesign: VOTSubtitlesDesign$B,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$B,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$B,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$B,
    VOTDownloadWithName: VOTDownloadWithName$B,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$B,
    VOTLocaleHash: VOTLocaleHash$B,
    VOTUpdatedAt: VOTUpdatedAt$B,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$B,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$B,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$B,
    VOTNewAudioPlayer: VOTNewAudioPlayer$B,
    VOTUseNewModel: VOTUseNewModel$B,
    TranslationDelayed: TranslationDelayed$B,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$B,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$B,
    VOTBugReport: VOTBugReport$B,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$B,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$B,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$B,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$B,
    VOTTranslatedBy: VOTTranslatedBy$B,
    VOTStreamNotAvailable: VOTStreamNotAvailable$B,
    VOTTranslationTextService: VOTTranslationTextService$B,
    VOTNotAffectToVoice: VOTNotAffectToVoice$B,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$B,
    showVideoVolumeSlider: showVideoVolumeSlider$B,
    hotkeysSettings: hotkeysSettings$B,
    None: None$B,
    VOTUseLivelyVoice: VOTUseLivelyVoice$B,
    miscSettings: miscSettings$B,
    services: services$B,
    aboutExtension: aboutExtension$B,
    appearance: appearance$B,
    buttonPosition: buttonPosition$B,
    position: position$B,
    secs: secs$B,
    autoHideButtonDelay: autoHideButtonDelay$B,
    notFound: notFound$B,
    minButtonPositionContainer: minButtonPositionContainer$B,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$B,
    PressTheKeyCombination: PressTheKeyCombination$B,
    VOTUseAudioDownload: VOTUseAudioDownload$B,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$B,
    VOTAccountRequired: VOTAccountRequired$B,
    VOTMyAccount: VOTMyAccount$B,
    VOTLogin: VOTLogin$B,
    VOTLogout: VOTLogout$B,
    VOTRefresh: VOTRefresh$B,
    VOTYandexToken: VOTYandexToken$B,
    VOTYandexTokenInfo: VOTYandexTokenInfo$B,
    VOTLoginViaToken: VOTLoginViaToken$B,
    subtitlesSmartLayout: subtitlesSmartLayout$B,
    smartDucking: smartDucking$B,
    VOTAutoSubtitles: VOTAutoSubtitles$B,
    VOTSubtitlesFont: VOTSubtitlesFont$B
  };
  const recommended$A = "direkomendasikan";
  const translateVideo$A = "Terjemahkan video";
  const disableTranslate$A = "Nonaktifkan";
  const translationSettings$A = "Pengaturan terjemahan";
  const subtitlesSettings$A = "Pengaturan subtitle";
  const resetSettings$A = "Mengatur ulang pengaturan";
  const videoBeingTranslated$A = "Video sedang diterjemahkan";
  const videoLanguage$A = "Bahasa video";
  const translationLanguage$A = "Bahasa terjemahan";
  const translationTake$A = "Terjemahan akan memakan waktu";
  const translationTakeMoreThanHour$A = "Terjemahan akan memakan waktu lebih dari satu jam";
  const translationTakeAboutMinute$A = "Terjemahan akan memakan waktu sekitar satu menit";
  const translationTakeFewMinutes$A = "Terjemahan akan memakan waktu beberapa menit";
  const translationTakeApproximatelyMinutes$A = "Terjemahan akan memakan waktu sekitar {0} menit";
  const translationTakeApproximatelyMinute$A = "Terjemahan akan memakan waktu sekitar {0} menit";
  const requestTranslationFailed$A = "Tidak dapat meminta terjemahan video";
  const audioNotReceived$A = "Tidak ada tautan audio yang diterima";
  const audioFormatNotSupported$A = "Format audio tidak didukung";
  const VOTAutoTranslate$A = "Terjemahkan saat membuka";
  const VOTDontTranslateYourLang$A = "Jangan terjemahkan dari bahasa asli Anda";
  const VOTVolume$A = "Volume video:";
  const VOTVolumeTranslation$A = "Volume terjemahan:";
  const VOTAutoSetVolume$A = "Kurangi volume video menjadi";
  const VOTShowVideoSlider$A = "Slider volume video";
  const VOTSyncVolume$A = "Menghubungkan volume terjemahan dan video";
  const VOTDisableFromYourLang$A = "Anda telah mematikan terjemahan video dalam bahasa Anda";
  const VOTVideoIsTooLong$A = "Video terlalu panjang";
  const VOTNoVideoIDFound$A = "Tidak ditemukan ID video";
  const VOTSubtitles$A = "Sub judul";
  const VOTSubtitlesDisabled$A = "Dinonaktifkan";
  const VOTSubtitlesMaxLength$A = "Panjang maksimum sub judul";
  const VOTHighlightWords$A = "Menggarisbawahi kata-kata";
  const VOTTranslatedFrom$A = "diterjemahkan dari";
  const VOTAutogenerated$A = "dibuat secara otomatis";
  const VOTSettings$A = "Pengaturan VOT";
  const VOTMenuLanguage$A = "Bahasa menu";
  const VOTAuthors$A = "Penulis";
  const VOTVersion$A = "Versi";
  const VOTLoader$A = "Pemuat";
  const VOTBrowser$A = "Peramban";
  const VOTShowPiPButton$A = "Tampilkan tombol PiP";
  const langs$A = { "auto": "Secara otomatis", "az": "Azerbaijan", "ay": "Aymara", "ak": "Akan", "sq": "Albania", "am": "Amharic", "en": "Inggris", "ar": "Arab", "hy": "Armenia", "as": "Assam", "af": "Afrikaans", "eu": "Basque", "be": "Belarusia", "bn": "Bengali", "my": "Burmese", "bg": "Bulgaria", "bs": "Bosnia", "bho": "Bhojpuri", "cy": "Welsh", "hu": "Hungaria", "vi": "Vietnam", "haw": "Hawaiian", "ht": "Haiti", "gl": "Galician", "lg": "Ganda", "el": "Yunani", "ka": "Georgia", "gn": "Guarani", "gu": "Gujarati", "gd": "Scots Gaelic", "da": "Denmark", "fy": "Frisia Barat", "zu": "Zulu", "iw": "Ibrani", "ig": "Igbo", "yi": "Yiddish", "id": "Indonesia", "ga": "Irlandia", "is": "Islandia", "es": "Spanyol", "it": "Italia", "yo": "Yoruba", "kk": "Kazakhstan", "kn": "Kannada", "ca": "Katalan", "qu": "Quechua", "rw": "Kinyarwanda", "ky": "Kirgistan", "zh": "Mandarin", "zh-Hant": "Traditional Mandarin", "zh-Hans": "Simplified Mandarin", "ko": "Korean", "co": "Corsica", "xh": "Xhosa", "ku": "Kurdish", "km": "Khmer", "lo": "Laos", "la": "Latin", "lv": "Latvia", "ln": "Lingala", "lt": "Lithuania", "lb": "Luxembourg", "mk": "Macedonia", "mg": "Malagasy", "ms": "Malay", "ml": "Malayalam", "dv": "Maldives", "mt": "Malta", "mi": "Maori", "mr": "Marathi", "mn": "Mongolia", "de": "Jerman", "ne": "Nepal", "nl": "Belanda", "no": "Norwegia", "ny": "Nyanja", "or": "Oriya", "om": "Oromo", "pa": "Punjabi", "fa": "Persia", "pl": "Polandia", "pt": "Portugis", "ps": "Pashto", "ro": "Rumania", "ru": "Rusia", "sm": "Samoa", "sa": "Sanskrit", "ceb": "Cebuano", "nso": "Northern Sotho", "sr": "Serbia", "si": "Sinhala", "sd": "Sindhi", "sk": "Slovakia", "sl": "Slovenia", "so": "Somalia", "sw": "Swahili", "su": "Sunda", "tg": "Tajikistan", "th": "Thailand", "ta": "Tamil", "tt": "Tatarstan", "te": "Telugu", "ti": "Tigrinya", "ts": "Tsonga", "tr": "Turki", "tk": "Turkmenistan", "uz": "Uzbekistan", "ug": "Uighur", "uk": "Ukraina", "ur": "Urdu", "fil": "Filipino", "fi": "Finlandia", "fr": "Prancis", "ha": "Hausa", "hi": "Hindi", "hmn": "Hmong", "hr": "Kroasia", "cs": "Ceko", "sv": "Swedia", "sn": "Shona", "ee": "Ewe", "eo": "Esperanto", "et": "Estonia", "st": "Southern Sotho", "jv": "Jawa", "ja": "Jepang", "kri": "Kri" };
  const streamNoConnectionToServer$A = "Tidak ada koneksi ke server";
  const searchField$A = "Cari...";
  const VOTTranslateAPIErrors$A = "Terjemahkan kesalahan dari API";
  const VOTDetectService$A = "Layanan deteksi bahasa";
  const VOTProxyWorkerHost$A = "Masukkan alamat pekerja proxy";
  const VOTM3u8ProxyHost$A = "Masukkan alamat pekerja proksi m3u8";
  const proxySettings$A = "Pengaturan Proksi";
  const translationTakeApproximatelyMinute2$A = "Terjemahan akan memakan waktu sekitar {0} menit";
  const VOTAudioBooster$A = "Peningkatan volume terjemahan yang diperpanjang";
  const VOTSubtitlesDesign$A = "Desain subtitle";
  const VOTSubtitlesFontSize$A = "Ukuran font subtitle";
  const VOTSubtitlesOpacity$A = "Transparansi latar belakang subtitle";
  const VOTSubtitlesDownloadFormat$A = "Format untuk mengunduh subtitle";
  const VOTDownloadWithName$A = "Unduh file dengan nama video";
  const VOTUpdateLocaleFiles$A = "Perbarui file pelokalan";
  const VOTLocaleHash$A = "Hash lokal";
  const VOTUpdatedAt$A = "Diperbarui pukul";
  const VOTNeedWebAudioAPI$A = "Untuk mengaktifkan ini, Anda harus memiliki Web Audio API";
  const VOTMediaCSPEnabledOnSite$A = "Media CSP diaktifkan di situs ini";
  const VOTOnlyBypassMediaCSP$A = "Gunakan hanya untuk melewati Media CSP";
  const VOTNewAudioPlayer$A = "Gunakan pemutar audio baru";
  const VOTUseNewModel$A = "Gunakan variasi eksperimental suara Yandex untuk beberapa video";
  const TranslationDelayed$A = "Terjemahannya sedikit tertunda";
  const VOTTranslationCompletedNotify$A = "Terjemahan pada {0} telah selesai!";
  const VOTSendNotifyOnComplete$A = "Kirim pemberitahuan bahwa video telah diterjemahkan";
  const VOTBugReport$A = "Laporkan bug";
  const VOTTranslateProxyDisabled$A = "Dinonaktifkan";
  const VOTTranslateProxyEnabled$A = "Diaktifkan";
  const VOTTranslateProxyEverything$A = "Proksi semuanya";
  const VOTTranslateProxyStatus$A = "Mode proxy";
  const VOTTranslatedBy$A = "Diterjemahkan oleh {0}";
  const VOTStreamNotAvailable$A = "Aliran terjemahan tidak tersedia";
  const VOTTranslationTextService$A = "Layanan penerjemahan teks";
  const VOTNotAffectToVoice$A = "Tidak memengaruhi terjemahan teks dalam sulih suara";
  const DontTranslateSelectedLanguages$A = "Jangan menerjemahkan dari bahasa yang dipilih";
  const showVideoVolumeSlider$A = "Tampilkan penggeser volume video";
  const hotkeysSettings$A = "Pengaturan hotkey";
  const None$A = "Tidak ada";
  const VOTUseLivelyVoice$A = "Gunakan suara yang hidup. Pembicara terdengar seperti orang Rusia asli.";
  const miscSettings$A = "Pengaturan lain-lain";
  const services$A = { "yandexbrowser": "Browser Yandex", "msedge": "Microsoft Edge", "rust-server": "Rust Server" };
  const aboutExtension$A = "Tentang ekstensi";
  const appearance$A = "Penampilan";
  const buttonPosition$A = "Posisi tombol di pemutar";
  const position$A = { "left": "Kiri", "right": "Benar kan", "top": "Atas", "default": "Standar" };
  const secs$A = "detik";
  const autoHideButtonDelay$A = "Tunda sebelum menyembunyikan tombol terjemahkan";
  const notFound$A = "tidak ditemukan";
  const minButtonPositionContainer$A = "Posisi tombol hanya berubah pada pemutar yang lebih besar dari 600 piksel.";
  const VOTTranslateProxyStatusDefault$A = "Menonaktifkan proxy sepenuhnya di negara Anda dapat merusak ekstensi";
  const PressTheKeyCombination$A = "Tekan kombinasi tombol...";
  const VOTUseAudioDownload$A = "Gunakan pengunduhan audio";
  const VOTUseAudioDownloadWarning$A = "Menonaktifkan unduhan audio dapat memengaruhi fungsionalitas ekstensi";
  const VOTAccountRequired$A = "Anda harus masuk untuk menggunakan fitur ini";
  const VOTMyAccount$A = "Akun saya";
  const VOTLogin$A = "Login";
  const VOTLogout$A = "Logout";
  const VOTRefresh$A = "Menyegarkan";
  const VOTYandexToken$A = "Masukkan Token OAuth Yandex";
  const VOTYandexTokenInfo$A = "Anda dapat mengatur token akun secara manual di bidang ini. Harap perhatikan bahwa kami tidak memeriksa validitasnya sebelum mengirim permintaan terjemahan";
  const VOTLoginViaToken$A = "Login melalui token";
  const subtitlesSmartLayout$A = "Tata letak subtitle cerdas";
  const smartDucking$A = "Volume adaptif";
  const VOTAutoSubtitles$A = "Subtitle terbuka";
  const VOTSubtitlesFont$A = "Font teks terjemahan";
  const __vite_glob_0_25 = {
    recommended: recommended$A,
    translateVideo: translateVideo$A,
    disableTranslate: disableTranslate$A,
    translationSettings: translationSettings$A,
    subtitlesSettings: subtitlesSettings$A,
    resetSettings: resetSettings$A,
    videoBeingTranslated: videoBeingTranslated$A,
    videoLanguage: videoLanguage$A,
    translationLanguage: translationLanguage$A,
    translationTake: translationTake$A,
    translationTakeMoreThanHour: translationTakeMoreThanHour$A,
    translationTakeAboutMinute: translationTakeAboutMinute$A,
    translationTakeFewMinutes: translationTakeFewMinutes$A,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$A,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$A,
    requestTranslationFailed: requestTranslationFailed$A,
    audioNotReceived: audioNotReceived$A,
    audioFormatNotSupported: audioFormatNotSupported$A,
    VOTAutoTranslate: VOTAutoTranslate$A,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$A,
    VOTVolume: VOTVolume$A,
    VOTVolumeTranslation: VOTVolumeTranslation$A,
    VOTAutoSetVolume: VOTAutoSetVolume$A,
    VOTShowVideoSlider: VOTShowVideoSlider$A,
    VOTSyncVolume: VOTSyncVolume$A,
    VOTDisableFromYourLang: VOTDisableFromYourLang$A,
    VOTVideoIsTooLong: VOTVideoIsTooLong$A,
    VOTNoVideoIDFound: VOTNoVideoIDFound$A,
    VOTSubtitles: VOTSubtitles$A,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$A,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$A,
    VOTHighlightWords: VOTHighlightWords$A,
    VOTTranslatedFrom: VOTTranslatedFrom$A,
    VOTAutogenerated: VOTAutogenerated$A,
    VOTSettings: VOTSettings$A,
    VOTMenuLanguage: VOTMenuLanguage$A,
    VOTAuthors: VOTAuthors$A,
    VOTVersion: VOTVersion$A,
    VOTLoader: VOTLoader$A,
    VOTBrowser: VOTBrowser$A,
    VOTShowPiPButton: VOTShowPiPButton$A,
    langs: langs$A,
    streamNoConnectionToServer: streamNoConnectionToServer$A,
    searchField: searchField$A,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$A,
    VOTDetectService: VOTDetectService$A,
    VOTProxyWorkerHost: VOTProxyWorkerHost$A,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$A,
    proxySettings: proxySettings$A,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$A,
    VOTAudioBooster: VOTAudioBooster$A,
    VOTSubtitlesDesign: VOTSubtitlesDesign$A,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$A,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$A,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$A,
    VOTDownloadWithName: VOTDownloadWithName$A,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$A,
    VOTLocaleHash: VOTLocaleHash$A,
    VOTUpdatedAt: VOTUpdatedAt$A,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$A,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$A,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$A,
    VOTNewAudioPlayer: VOTNewAudioPlayer$A,
    VOTUseNewModel: VOTUseNewModel$A,
    TranslationDelayed: TranslationDelayed$A,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$A,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$A,
    VOTBugReport: VOTBugReport$A,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$A,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$A,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$A,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$A,
    VOTTranslatedBy: VOTTranslatedBy$A,
    VOTStreamNotAvailable: VOTStreamNotAvailable$A,
    VOTTranslationTextService: VOTTranslationTextService$A,
    VOTNotAffectToVoice: VOTNotAffectToVoice$A,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$A,
    showVideoVolumeSlider: showVideoVolumeSlider$A,
    hotkeysSettings: hotkeysSettings$A,
    None: None$A,
    VOTUseLivelyVoice: VOTUseLivelyVoice$A,
    miscSettings: miscSettings$A,
    services: services$A,
    aboutExtension: aboutExtension$A,
    appearance: appearance$A,
    buttonPosition: buttonPosition$A,
    position: position$A,
    secs: secs$A,
    autoHideButtonDelay: autoHideButtonDelay$A,
    notFound: notFound$A,
    minButtonPositionContainer: minButtonPositionContainer$A,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$A,
    PressTheKeyCombination: PressTheKeyCombination$A,
    VOTUseAudioDownload: VOTUseAudioDownload$A,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$A,
    VOTAccountRequired: VOTAccountRequired$A,
    VOTMyAccount: VOTMyAccount$A,
    VOTLogin: VOTLogin$A,
    VOTLogout: VOTLogout$A,
    VOTRefresh: VOTRefresh$A,
    VOTYandexToken: VOTYandexToken$A,
    VOTYandexTokenInfo: VOTYandexTokenInfo$A,
    VOTLoginViaToken: VOTLoginViaToken$A,
    subtitlesSmartLayout: subtitlesSmartLayout$A,
    smartDucking: smartDucking$A,
    VOTAutoSubtitles: VOTAutoSubtitles$A,
    VOTSubtitlesFont: VOTSubtitlesFont$A
  };
  const recommended$z = "consigliato";
  const translateVideo$z = "Traduci video";
  const disableTranslate$z = "Spegnere";
  const translationSettings$z = "Impostazioni di traduzione";
  const subtitlesSettings$z = "Impostazioni sottotitoli";
  const resetSettings$z = "Ripristina impostazioni";
  const videoBeingTranslated$z = "Il video è in fase di traduzione";
  const videoLanguage$z = "Lingua video";
  const translationLanguage$z = "Lingua di traduzione";
  const translationTake$z = "La traduzione avrà";
  const translationTakeMoreThanHour$z = "La traduzione richiederà più di un'ora";
  const translationTakeAboutMinute$z = "La traduzione richiederà circa un minuto";
  const translationTakeFewMinutes$z = "La traduzione richiederà alcuni minuti";
  const translationTakeApproximatelyMinutes$z = "La traduzione richiederà circa {0} minuti";
  const translationTakeApproximatelyMinute$z = "La traduzione richiederà circa {0} minuti";
  const requestTranslationFailed$z = "Richiesta di traduzione video non riuscita";
  const audioNotReceived$z = "Collegamento audio non ricevuto";
  const audioFormatNotSupported$z = "Il formato audio non è supportato";
  const VOTAutoTranslate$z = "Traduci su apri";
  const VOTDontTranslateYourLang$z = "Non tradurre dalla mia lingua";
  const VOTVolume$z = "Volume video:";
  const VOTVolumeTranslation$z = "Volume di traduzione:";
  const VOTAutoSetVolume$z = "Ridurre il volume del video a";
  const VOTShowVideoSlider$z = "Cursore volume video";
  const VOTSyncVolume$z = "Traduzione link e volume video";
  const VOTDisableFromYourLang$z = "Hai disabilitato la traduzione del video nella tua lingua";
  const VOTVideoIsTooLong$z = "Il video è troppo lungo";
  const VOTNoVideoIDFound$z = "Nessun ID video trovato";
  const VOTSubtitles$z = "Sottotitolo";
  const VOTSubtitlesDisabled$z = "Disabilita";
  const VOTSubtitlesMaxLength$z = "Sottotitoli lunghezza massima";
  const VOTHighlightWords$z = "Evidenziare le parole";
  const VOTTranslatedFrom$z = "tradotto da";
  const VOTAutogenerated$z = "autogenerato";
  const VOTSettings$z = "Impostazioni VOT";
  const VOTMenuLanguage$z = "Lingua del menu";
  const VOTAuthors$z = "Autrici";
  const VOTVersion$z = "Versione";
  const VOTLoader$z = "Loader";
  const VOTBrowser$z = "Browser";
  const VOTShowPiPButton$z = "Mostra pulsante PiP";
  const langs$z = { "auto": "Auto", "af": "Afrikaans", "ak": "Akan", "sq": "Albanese", "am": "Amarico", "ar": "Arabo", "hy": "Armeno", "as": "Assamese", "ay": "Aymara", "az": "Azero", "bn": "Bangla", "eu": "Basco", "be": "Bielorusso", "bho": "Bhojpuri", "bs": "Bosniaca", "bg": "Bulgaro", "my": "Birmano", "ca": "Catalano", "ceb": "Cebuano", "zh": "Cinese", "zh-Hans": "Cinese (semplificato)", "zh-Hant": "Cinese (tradizionale)", "co": "Corso", "hr": "Croato", "cs": "Ceca", "da": "Danese", "dv": "Divehi", "nl": "Olandese", "en": "Inglese", "eo": "Esperanto", "et": "Estone", "ee": "Pecora", "fil": "Filippino", "fi": "Finlandese", "fr": "Francese", "gl": "Galiziana", "lg": "Ganda", "ka": "Georgiano", "de": "Tedesco", "el": "Greco", "gn": "Guarani", "gu": "Gujarati", "ht": "Creolo haitiano", "ha": "Hausa", "haw": "Hawaiano", "iw": "Ebraico", "hi": "Hindi", "hmn": "Hmong", "hu": "Ungherese", "is": "Islandese", "ig": "Igbo", "id": "Indonesiano", "ga": "Irlandese", "it": "Italiano", "ja": "Giapponese", "jv": "Giavanese", "kn": "Kannada", "kk": "Kazako", "km": "Khmer", "rw": "Kinyarwanda", "ko": "Coreano", "kri": "Krio", "ku": "Curdo", "ky": "Kirghizistan", "lo": "Kip", "la": "Latino", "lv": "Lettone", "ln": "Lingala", "lt": "Lituano", "lb": "Lussemburghese", "mk": "Macedone", "mg": "Malgascio", "ms": "Malay", "ml": "Malayalam", "mt": "Maltese", "mi": "Maori", "mr": "Marathi", "mn": "Mongolo", "ne": "Nepalesi", "nso": "Sotho settentrionale", "no": "Norvegese", "ny": "Nyanja", "or": "Odia", "om": "Oromo", "ps": "Pashto", "fa": "Persiano", "pl": "Polacco", "pt": "Portoghese", "pa": "Punjabi", "qu": "Quechua", "ro": "Rumeno", "ru": "Russo", "sm": "Samoana", "sa": "Sanscrito", "gd": "Gaelico scozzese", "sr": "Serbo", "sn": "Shona", "sd": "Sindhi", "si": "Singalese", "sk": "Slovacco", "sl": "Sloveno", "so": "Somalo", "st": "Southern Sotho", "es": "Spagnolo", "su": "Sundanese", "sw": "Swahili", "sv": "Svedese", "tg": "Tagiko", "ta": "Tamil", "tt": "Tartaro", "te": "Telugu", "th": "Thai", "ti": "Tigrino", "ts": "Tsonga", "tr": "Turchia", "tk": "Turkmeno", "uk": "Ucraino", "ur": "Urdu", "ug": "Uigura", "uz": "Uzbeko", "vi": "Vietnamita", "cy": "Gallese", "fy": "Frisone occidentale", "xh": "Xhosa", "yi": "Yiddish", "yo": "Yoruba", "zu": "Zulù" };
  const streamNoConnectionToServer$z = "Non c'è connessione al server";
  const searchField$z = "Ricerca...";
  const VOTTranslateAPIErrors$z = "Tradurre gli errori dall'API";
  const VOTDetectService$z = "Servizio di rilevamento della lingua";
  const VOTProxyWorkerHost$z = "Immettere l'indirizzo del lavoratore proxy";
  const VOTM3u8ProxyHost$z = "Immettere l'indirizzo del proxy worker m3u8";
  const proxySettings$z = "Impostazioni proxy";
  const translationTakeApproximatelyMinute2$z = "La traduzione richiederà circa {0} minuti";
  const VOTAudioBooster$z = "Aumento del volume di traduzione esteso";
  const VOTSubtitlesDesign$z = "Sottotitoli design";
  const VOTSubtitlesFontSize$z = "Dimensione del carattere dei sottotitoli";
  const VOTSubtitlesOpacity$z = "Trasparenza dello sfondo dei sottotitoli";
  const VOTSubtitlesDownloadFormat$z = "Il formato per scaricare i sottotitoli";
  const VOTDownloadWithName$z = "Scarica i file con il nome del video";
  const VOTUpdateLocaleFiles$z = "Aggiornare i file di localizzazione";
  const VOTLocaleHash$z = "Locale hash";
  const VOTUpdatedAt$z = "Aggiornato alle";
  const VOTNeedWebAudioAPI$z = "Per abilitare questo, è necessario disporre di un'API audio Web";
  const VOTMediaCSPEnabledOnSite$z = "Media CSP è abilitato su questo sito";
  const VOTOnlyBypassMediaCSP$z = "Usalo solo per bypassare i media CSP";
  const VOTNewAudioPlayer$z = "Utilizzare il nuovo lettore audio";
  const VOTUseNewModel$z = "Utilizzare una variazione sperimentale di voci Yandex per alcuni video";
  const TranslationDelayed$z = "La traduzione è leggermente ritardata";
  const VOTTranslationCompletedNotify$z = "La traduzione sul {0} è stata completata!";
  const VOTSendNotifyOnComplete$z = "Invia una notifica che il video è stato tradotto";
  const VOTBugReport$z = "Segnala un bug";
  const VOTTranslateProxyDisabled$z = "Disabilita";
  const VOTTranslateProxyEnabled$z = "Abilitato";
  const VOTTranslateProxyEverything$z = "Proxy tutto";
  const VOTTranslateProxyStatus$z = "Modalità proxy";
  const VOTTranslatedBy$z = "Tradotto da {0}";
  const VOTStreamNotAvailable$z = "Translate stream non è disponibile";
  const VOTTranslationTextService$z = "Servizio traduzione testi";
  const VOTNotAffectToVoice$z = "Non influisce sulla traduzione del testo in voice over";
  const DontTranslateSelectedLanguages$z = "Non tradurre da lingue selezionate";
  const showVideoVolumeSlider$z = "Visualizzare il cursore del volume video";
  const hotkeysSettings$z = "Impostazioni tasti di scelta rapida";
  const None$z = "Nessuno";
  const VOTUseLivelyVoice$z = "Usa voci vivaci. Gli altoparlanti suonano come russi nativi.";
  const miscSettings$z = "Varie impostazioni";
  const services$z = { "yandexbrowser": "Yandex Browser", "msedge": "Microsoft Edge", "rust-server": "Rust Server" };
  const aboutExtension$z = "A proposito di estensione";
  const appearance$z = "Aspetto";
  const buttonPosition$z = "Posizione del pulsante nel lettore";
  const position$z = { "left": "Sinistra", "right": "Diritto", "top": "Cima", "default": "Predefinito" };
  const secs$z = "sec";
  const autoHideButtonDelay$z = "Ritardare prima di nascondere il pulsante traduci";
  const notFound$z = "non trovato";
  const minButtonPositionContainer$z = "La posizione del pulsante cambia solo nei giocatori più grandi di 600 pixel.";
  const VOTTranslateProxyStatusDefault$z = "La disattivazione completa del proxy nel tuo paese potrebbe interrompere l'estensione";
  const PressTheKeyCombination$z = "Premere la combinazione di tasti...";
  const VOTUseAudioDownload$z = "Usa il download audio";
  const VOTUseAudioDownloadWarning$z = "La disattivazione dei download audio può influire sulla funzionalità dell'estensione";
  const VOTAccountRequired$z = "È necessario effettuare il login per utilizzare questa funzione";
  const VOTMyAccount$z = "Il mio account";
  const VOTLogin$z = "Login";
  const VOTLogout$z = "Esci";
  const VOTRefresh$z = "Aggiornare";
  const VOTYandexToken$z = "Inserisci il token Yandex OAuth";
  const VOTYandexTokenInfo$z = "È possibile impostare manualmente il token dell'account in questo campo. Si prega di notare che non controlliamo la sua validità prima di inviare una richiesta di traduzione";
  const VOTLoginViaToken$z = "Accedi tramite token";
  const subtitlesSmartLayout$z = "Layout intelligente dei sottotitoli";
  const smartDucking$z = "Volume adattivo";
  const VOTAutoSubtitles$z = "Sottotitoli su open";
  const VOTSubtitlesFont$z = "Carattere sottotitoli";
  const __vite_glob_0_26 = {
    recommended: recommended$z,
    translateVideo: translateVideo$z,
    disableTranslate: disableTranslate$z,
    translationSettings: translationSettings$z,
    subtitlesSettings: subtitlesSettings$z,
    resetSettings: resetSettings$z,
    videoBeingTranslated: videoBeingTranslated$z,
    videoLanguage: videoLanguage$z,
    translationLanguage: translationLanguage$z,
    translationTake: translationTake$z,
    translationTakeMoreThanHour: translationTakeMoreThanHour$z,
    translationTakeAboutMinute: translationTakeAboutMinute$z,
    translationTakeFewMinutes: translationTakeFewMinutes$z,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$z,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$z,
    requestTranslationFailed: requestTranslationFailed$z,
    audioNotReceived: audioNotReceived$z,
    audioFormatNotSupported: audioFormatNotSupported$z,
    VOTAutoTranslate: VOTAutoTranslate$z,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$z,
    VOTVolume: VOTVolume$z,
    VOTVolumeTranslation: VOTVolumeTranslation$z,
    VOTAutoSetVolume: VOTAutoSetVolume$z,
    VOTShowVideoSlider: VOTShowVideoSlider$z,
    VOTSyncVolume: VOTSyncVolume$z,
    VOTDisableFromYourLang: VOTDisableFromYourLang$z,
    VOTVideoIsTooLong: VOTVideoIsTooLong$z,
    VOTNoVideoIDFound: VOTNoVideoIDFound$z,
    VOTSubtitles: VOTSubtitles$z,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$z,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$z,
    VOTHighlightWords: VOTHighlightWords$z,
    VOTTranslatedFrom: VOTTranslatedFrom$z,
    VOTAutogenerated: VOTAutogenerated$z,
    VOTSettings: VOTSettings$z,
    VOTMenuLanguage: VOTMenuLanguage$z,
    VOTAuthors: VOTAuthors$z,
    VOTVersion: VOTVersion$z,
    VOTLoader: VOTLoader$z,
    VOTBrowser: VOTBrowser$z,
    VOTShowPiPButton: VOTShowPiPButton$z,
    langs: langs$z,
    streamNoConnectionToServer: streamNoConnectionToServer$z,
    searchField: searchField$z,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$z,
    VOTDetectService: VOTDetectService$z,
    VOTProxyWorkerHost: VOTProxyWorkerHost$z,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$z,
    proxySettings: proxySettings$z,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$z,
    VOTAudioBooster: VOTAudioBooster$z,
    VOTSubtitlesDesign: VOTSubtitlesDesign$z,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$z,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$z,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$z,
    VOTDownloadWithName: VOTDownloadWithName$z,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$z,
    VOTLocaleHash: VOTLocaleHash$z,
    VOTUpdatedAt: VOTUpdatedAt$z,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$z,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$z,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$z,
    VOTNewAudioPlayer: VOTNewAudioPlayer$z,
    VOTUseNewModel: VOTUseNewModel$z,
    TranslationDelayed: TranslationDelayed$z,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$z,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$z,
    VOTBugReport: VOTBugReport$z,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$z,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$z,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$z,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$z,
    VOTTranslatedBy: VOTTranslatedBy$z,
    VOTStreamNotAvailable: VOTStreamNotAvailable$z,
    VOTTranslationTextService: VOTTranslationTextService$z,
    VOTNotAffectToVoice: VOTNotAffectToVoice$z,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$z,
    showVideoVolumeSlider: showVideoVolumeSlider$z,
    hotkeysSettings: hotkeysSettings$z,
    None: None$z,
    VOTUseLivelyVoice: VOTUseLivelyVoice$z,
    miscSettings: miscSettings$z,
    services: services$z,
    aboutExtension: aboutExtension$z,
    appearance: appearance$z,
    buttonPosition: buttonPosition$z,
    position: position$z,
    secs: secs$z,
    autoHideButtonDelay: autoHideButtonDelay$z,
    notFound: notFound$z,
    minButtonPositionContainer: minButtonPositionContainer$z,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$z,
    PressTheKeyCombination: PressTheKeyCombination$z,
    VOTUseAudioDownload: VOTUseAudioDownload$z,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$z,
    VOTAccountRequired: VOTAccountRequired$z,
    VOTMyAccount: VOTMyAccount$z,
    VOTLogin: VOTLogin$z,
    VOTLogout: VOTLogout$z,
    VOTRefresh: VOTRefresh$z,
    VOTYandexToken: VOTYandexToken$z,
    VOTYandexTokenInfo: VOTYandexTokenInfo$z,
    VOTLoginViaToken: VOTLoginViaToken$z,
    subtitlesSmartLayout: subtitlesSmartLayout$z,
    smartDucking: smartDucking$z,
    VOTAutoSubtitles: VOTAutoSubtitles$z,
    VOTSubtitlesFont: VOTSubtitlesFont$z
  };
  const recommended$y = "おすすめ";
  const translateVideo$y = "動画を翻訳する";
  const disableTranslate$y = "オフにする";
  const translationSettings$y = "翻訳設定";
  const subtitlesSettings$y = "字幕設定";
  const resetSettings$y = "設定をリセットする";
  const videoBeingTranslated$y = "動画が翻訳されています";
  const videoLanguage$y = "動画の言語";
  const translationLanguage$y = "翻訳言語";
  const translationTake$y = "翻訳にかかる時間は";
  const translationTakeMoreThanHour$y = "翻訳には1時間以上かかります";
  const translationTakeAboutMinute$y = "翻訳には約1分かかります";
  const translationTakeFewMinutes$y = "翻訳には数分かかります";
  const translationTakeApproximatelyMinutes$y = "翻訳には約{0}分かかります";
  const translationTakeApproximatelyMinute$y = "翻訳には約{0}分かかります";
  const requestTranslationFailed$y = "動画の翻訳リクエストに失敗しました";
  const audioNotReceived$y = "音声リンクが受信されませんでした";
  const audioFormatNotSupported$y = "音声形式がサポートされていません";
  const VOTAutoTranslate$y = "開いたときに翻訳する";
  const VOTDontTranslateYourLang$y = "自分の言語からは翻訳しない";
  const VOTVolume$y = "ビデオの音量:";
  const VOTVolumeTranslation$y = "翻訳量:";
  const VOTAutoSetVolume$y = "ビデオの音量を以下のように減らします。";
  const VOTShowVideoSlider$y = "動画の音量スライダー";
  const VOTSyncVolume$y = "バインド翻訳とビデオボリューム";
  const VOTDisableFromYourLang$y = "あなたの言語での動画の翻訳を無効にしました";
  const VOTVideoIsTooLong$y = "動画が長すぎます";
  const VOTNoVideoIDFound$y = "動画IDが見つかりませんでした";
  const VOTSubtitles$y = "字幕";
  const VOTSubtitlesDisabled$y = "無効";
  const VOTSubtitlesMaxLength$y = "字幕の最大長";
  const VOTHighlightWords$y = "単語を強調表示する";
  const VOTTranslatedFrom$y = "から翻訳";
  const VOTAutogenerated$y = "自動生成された";
  const VOTSettings$y = "VOT設定";
  const VOTMenuLanguage$y = "メニュー言語";
  const VOTAuthors$y = "著者";
  const VOTVersion$y = "バージョン";
  const VOTLoader$y = "ローダ";
  const VOTBrowser$y = "ブラウザ";
  const VOTShowPiPButton$y = "PiP ボタンを表示";
  const langs$y = { "auto": "自動的に", "is": "アイスランド語", "ay": "アイマラ語", "ga": "アイルランド語", "ak": "アカン語", "az": "アゼルバイジャン語", "as": "アッサム語", "af": "アフリカーンス語", "am": "アムハラ語", "ar": "アラビア語", "sq": "アルバニア語", "hy": "アルメニア語", "it": "イタリア語", "yi": "イディッシュ語", "ig": "イボ語", "id": "インドネシア語", "ug": "ウイグル語", "cy": "ウェールズ語", "uk": "ウクライナ語", "uz": "ウズベク語", "ur": "ウルドゥー語", "ee": "エウェ語", "et": "エストニア語", "eo": "エスペラント語", "or": "オディア語", "nl": "オランダ語", "om": "オロモ語", "kk": "カザフ語", "ca": "カタロニア語", "gl": "ガリシア語", "lg": "ガンダ語", "kn": "カンナダ語", "rw": "キニアルワンダ語", "el": "ギリシャ語", "ky": "キルギス語", "gn": "グアラニー語", "gu": "グジャラート語", "km": "クメール語", "kri": "クリオ語", "ku": "クルド語", "hr": "クロアチア語", "qu": "ケチュア語", "xh": "コサ語", "co": "コルシカ語", "sm": "サモア語", "sa": "サンスクリット語", "jv": "ジャワ語", "ka": "ジョージア語", "sn": "ショナ語", "sd": "シンド語", "si": "シンハラ語", "sv": "スウェーデン語", "zu": "ズールー語", "gd": "スコットランド・ゲール語", "es": "スペイン語", "sk": "スロバキア語", "sl": "スロベニア語", "sw": "スワヒリ語", "su": "スンダ語", "ceb": "セブアノ語", "sr": "セルビア語", "so": "ソマリ語", "th": "タイ語", "tg": "タジク語", "tt": "タタール語", "ta": "タミル語", "cs": "チェコ語", "ts": "ツォンガ語", "ti": "ティグリニア語", "dv": "ディベヒ語", "te": "テルグ語", "da": "デンマーク語", "de": "ドイツ語", "tk": "トルクメン語", "tr": "トルコ語", "ny": "ニャンジャ語", "ne": "ネパール語", "no": "ノルウェー語", "ht": "ハイチ・クレオール語", "ha": "ハウサ語", "ps": "パシュトゥー語", "eu": "バスク語", "haw": "ハワイ語", "hu": "ハンガリー語", "pa": "パンジャブ語", "hi": "ヒンディー語", "fil": "フィリピノ語", "fi": "フィンランド語", "hmn": "フモン語", "fr": "フランス語", "bg": "ブルガリア語", "vi": "ベトナム語", "iw": "ヘブライ語", "be": "ベラルーシ語", "fa": "ペルシア語", "bn": "ベンガル語", "bho": "ボージュプリー語", "pl": "ポーランド語", "bs": "ボスニア語", "pt": "ポルトガル語", "mi": "マオリ語", "mk": "マケドニア語", "mg": "マダガスカル語", "mr": "マラーティー語", "ml": "マラヤーラム語", "mt": "マルタ語", "ms": "マレー語", "my": "ミャンマー語", "mn": "モンゴル語", "yo": "ヨルバ語", "lo": "ラオ語", "la": "ラテン語", "lv": "ラトビア語", "lt": "リトアニア語", "ln": "リンガラ語", "ro": "ルーマニア語", "lb": "ルクセンブルク語", "ru": "ロシア語", "en": "英語", "ko": "韓国語", "fy": "西フリジア語", "zh": "中国語", "zh-Hans": "中国語 (簡体字)", "zh-Hant": "中国語 (繁体字)", "st": "南部ソト語", "ja": "日本語", "nso": "北部ソト語" };
  const streamNoConnectionToServer$y = "サーバーへの接続がありません";
  const searchField$y = "検索。..";
  const VOTTranslateAPIErrors$y = "APIからのエラーの翻訳";
  const VOTDetectService$y = "言語検出サービス";
  const VOTProxyWorkerHost$y = "プロキシワーカーアドレスを入力します";
  const VOTM3u8ProxyHost$y = "M3u8プロキシワーカーのアドレスを入力します";
  const proxySettings$y = "プロキシ設定";
  const translationTakeApproximatelyMinute2$y = "翻訳には約{0}分かかります";
  const VOTAudioBooster$y = "翻訳量の増加を拡大しました";
  const VOTSubtitlesDesign$y = "字幕デザイン";
  const VOTSubtitlesFontSize$y = "字幕のフォントサイズ";
  const VOTSubtitlesOpacity$y = "字幕の背景の透明度";
  const VOTSubtitlesDownloadFormat$y = "字幕をダウンロードするためのフォーマット";
  const VOTDownloadWithName$y = "ビデオ名のファイルをダウンロードする";
  const VOTUpdateLocaleFiles$y = "ローカリゼーションファイルの更新";
  const VOTLocaleHash$y = "ロケールハッシュ";
  const VOTUpdatedAt$y = "更新";
  const VOTNeedWebAudioAPI$y = "これを有効にするには、Web Audio APIが必要です";
  const VOTMediaCSPEnabledOnSite$y = "メディアCSPはこのサイトで有効になっています";
  const VOTOnlyBypassMediaCSP$y = "メディアCSPをバイパスするためにのみ使用します";
  const VOTNewAudioPlayer$y = "新しいオーディオプレーヤーを使用する";
  const VOTUseNewModel$y = "いくつかのビデオのためのYandexの声の実験的なバリエーションを使用してください";
  const TranslationDelayed$y = "翻訳が少し遅れています";
  const VOTTranslationCompletedNotify$y = "{0}の翻訳が完了しました！";
  const VOTSendNotifyOnComplete$y = "動画が翻訳されたことを通知する";
  const VOTBugReport$y = "バグを報告する";
  const VOTTranslateProxyDisabled$y = "無効";
  const VOTTranslateProxyEnabled$y = "有効になっている";
  const VOTTranslateProxyEverything$y = "すべてをプロキシ";
  const VOTTranslateProxyStatus$y = "プロキシモード";
  const VOTTranslatedBy$y = "{0}によって翻訳されました";
  const VOTStreamNotAvailable$y = "翻訳ストリームは利用できません";
  const VOTTranslationTextService$y = "テキスト翻訳サービス";
  const VOTNotAffectToVoice$y = "ボイスオーバーのテキストの翻訳には影響しません";
  const DontTranslateSelectedLanguages$y = "選択した言語から翻訳しない";
  const showVideoVolumeSlider$y = "ビデオの音量スライダを表示する";
  const hotkeysSettings$y = "ホットキーの設定";
  const None$y = "なし";
  const VOTUseLivelyVoice$y = "生き生きとした声を使う。 スピーカーはネイティブロシア人のように聞こえます。";
  const miscSettings$y = "その他の設定";
  const services$y = { "yandexbrowser": "Yandexブラウザ", "msedge": "マイクロソフトエッジ", "rust-server": "Rustサーバー" };
  const aboutExtension$y = "延長について";
  const appearance$y = "外観";
  const buttonPosition$y = "プレーヤー内のボタン位置";
  const position$y = { "left": "左", "right": "右", "top": "トップ", "default": "デフォルト" };
  const secs$y = "secs";
  const autoHideButtonDelay$y = "翻訳ボタンを非表示にする前に遅延します";
  const notFound$y = "見つかりません";
  const minButtonPositionContainer$y = "ボタンの位置は、600ピクセルを超えるプレーヤーでのみ変更されます。";
  const VOTTranslateProxyStatusDefault$y = "あなたの国でプロキシを完全に無効にすると、拡張機能が壊れる可能性があります";
  const PressTheKeyCombination$y = "キーの組み合わせを押します。..";
  const VOTUseAudioDownload$y = "オーディオのダウンロードを使用する";
  const VOTUseAudioDownloadWarning$y = "オーディオのダウンロードを無効にすると、拡張機能の機能に影響を与える可能性があります";
  const VOTAccountRequired$y = "この機能を使用するにはログインする必要があります";
  const VOTMyAccount$y = "マイアカウント";
  const VOTLogin$y = "ログイン";
  const VOTLogout$y = "ログアウト";
  const VOTRefresh$y = "リフレッシュ";
  const VOTYandexToken$y = "Yandex OAuthトークンを入力します";
  const VOTYandexTokenInfo$y = "このフィールドでは、アカウントトークンを手動で設定できます。 翻訳リクエストを送信する前にその有効性を確認しないことに注意してください";
  const VOTLoginViaToken$y = "トークンを介したログイン";
  const subtitlesSmartLayout$y = "スマート字幕レイアウト";
  const smartDucking$y = "適応ボリューム";
  const VOTAutoSubtitles$y = "オープン時の字幕";
  const VOTSubtitlesFont$y = "字幕フォント";
  const __vite_glob_0_27 = {
    recommended: recommended$y,
    translateVideo: translateVideo$y,
    disableTranslate: disableTranslate$y,
    translationSettings: translationSettings$y,
    subtitlesSettings: subtitlesSettings$y,
    resetSettings: resetSettings$y,
    videoBeingTranslated: videoBeingTranslated$y,
    videoLanguage: videoLanguage$y,
    translationLanguage: translationLanguage$y,
    translationTake: translationTake$y,
    translationTakeMoreThanHour: translationTakeMoreThanHour$y,
    translationTakeAboutMinute: translationTakeAboutMinute$y,
    translationTakeFewMinutes: translationTakeFewMinutes$y,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$y,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$y,
    requestTranslationFailed: requestTranslationFailed$y,
    audioNotReceived: audioNotReceived$y,
    audioFormatNotSupported: audioFormatNotSupported$y,
    VOTAutoTranslate: VOTAutoTranslate$y,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$y,
    VOTVolume: VOTVolume$y,
    VOTVolumeTranslation: VOTVolumeTranslation$y,
    VOTAutoSetVolume: VOTAutoSetVolume$y,
    VOTShowVideoSlider: VOTShowVideoSlider$y,
    VOTSyncVolume: VOTSyncVolume$y,
    VOTDisableFromYourLang: VOTDisableFromYourLang$y,
    VOTVideoIsTooLong: VOTVideoIsTooLong$y,
    VOTNoVideoIDFound: VOTNoVideoIDFound$y,
    VOTSubtitles: VOTSubtitles$y,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$y,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$y,
    VOTHighlightWords: VOTHighlightWords$y,
    VOTTranslatedFrom: VOTTranslatedFrom$y,
    VOTAutogenerated: VOTAutogenerated$y,
    VOTSettings: VOTSettings$y,
    VOTMenuLanguage: VOTMenuLanguage$y,
    VOTAuthors: VOTAuthors$y,
    VOTVersion: VOTVersion$y,
    VOTLoader: VOTLoader$y,
    VOTBrowser: VOTBrowser$y,
    VOTShowPiPButton: VOTShowPiPButton$y,
    langs: langs$y,
    streamNoConnectionToServer: streamNoConnectionToServer$y,
    searchField: searchField$y,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$y,
    VOTDetectService: VOTDetectService$y,
    VOTProxyWorkerHost: VOTProxyWorkerHost$y,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$y,
    proxySettings: proxySettings$y,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$y,
    VOTAudioBooster: VOTAudioBooster$y,
    VOTSubtitlesDesign: VOTSubtitlesDesign$y,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$y,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$y,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$y,
    VOTDownloadWithName: VOTDownloadWithName$y,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$y,
    VOTLocaleHash: VOTLocaleHash$y,
    VOTUpdatedAt: VOTUpdatedAt$y,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$y,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$y,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$y,
    VOTNewAudioPlayer: VOTNewAudioPlayer$y,
    VOTUseNewModel: VOTUseNewModel$y,
    TranslationDelayed: TranslationDelayed$y,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$y,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$y,
    VOTBugReport: VOTBugReport$y,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$y,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$y,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$y,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$y,
    VOTTranslatedBy: VOTTranslatedBy$y,
    VOTStreamNotAvailable: VOTStreamNotAvailable$y,
    VOTTranslationTextService: VOTTranslationTextService$y,
    VOTNotAffectToVoice: VOTNotAffectToVoice$y,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$y,
    showVideoVolumeSlider: showVideoVolumeSlider$y,
    hotkeysSettings: hotkeysSettings$y,
    None: None$y,
    VOTUseLivelyVoice: VOTUseLivelyVoice$y,
    miscSettings: miscSettings$y,
    services: services$y,
    aboutExtension: aboutExtension$y,
    appearance: appearance$y,
    buttonPosition: buttonPosition$y,
    position: position$y,
    secs: secs$y,
    autoHideButtonDelay: autoHideButtonDelay$y,
    notFound: notFound$y,
    minButtonPositionContainer: minButtonPositionContainer$y,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$y,
    PressTheKeyCombination: PressTheKeyCombination$y,
    VOTUseAudioDownload: VOTUseAudioDownload$y,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$y,
    VOTAccountRequired: VOTAccountRequired$y,
    VOTMyAccount: VOTMyAccount$y,
    VOTLogin: VOTLogin$y,
    VOTLogout: VOTLogout$y,
    VOTRefresh: VOTRefresh$y,
    VOTYandexToken: VOTYandexToken$y,
    VOTYandexTokenInfo: VOTYandexTokenInfo$y,
    VOTLoginViaToken: VOTLoginViaToken$y,
    subtitlesSmartLayout: subtitlesSmartLayout$y,
    smartDucking: smartDucking$y,
    VOTAutoSubtitles: VOTAutoSubtitles$y,
    VOTSubtitlesFont: VOTSubtitlesFont$y
  };
  const recommended$x = "disaranake";
  const translateVideo$x = "Translate vidéo";
  const disableTranslate$x = "Patèni";
  const translationSettings$x = "Setelan terjemahan";
  const subtitlesSettings$x = "Subtitles setelan";
  const resetSettings$x = "Setelan Reset";
  const videoBeingTranslated$x = "Vidéo iki lagi diterjemahaké";
  const videoLanguage$x = "Basa Video";
  const translationLanguage$x = "Basa terjemahan";
  const translationTake$x = "Terjemahan bakal njupuk";
  const translationTakeMoreThanHour$x = "Terjemahan bakal njupuk luwih saka sak jam";
  const translationTakeAboutMinute$x = "Terjemahan bakal njupuk kira kira menit";
  const translationTakeFewMinutes$x = "Terjemahan bakal njupuk sawetara menit";
  const translationTakeApproximatelyMinutes$x = "Terjemahan bakal njupuk kira-kira {0} menit";
  const translationTakeApproximatelyMinute$x = "Terjemahan bakal njupuk kira-kira {0} menit";
  const requestTranslationFailed$x = "Gagal kanggo njaluk terjemahan video";
  const audioNotReceived$x = "Link Audio ora ditampa";
  const audioFormatNotSupported$x = "Format audio ora didhukung";
  const VOTAutoTranslate$x = "Nerjemahake ing mbukak";
  const VOTDontTranslateYourLang$x = "Aja nerjemahké saka basanku";
  const VOTVolume$x = "Jilid Video:";
  const VOTVolumeTranslation$x = "Jilid terjemahan:";
  const VOTAutoSetVolume$x = "Ngurangi volume video kanggo";
  const VOTShowVideoSlider$x = "Geser volume Video";
  const VOTSyncVolume$x = "Terjemahan Link lan volume video";
  const VOTDisableFromYourLang$x = "Sampeyan wis mateni terjemahan video ing basa sampeyan";
  const VOTVideoIsTooLong$x = "Videonya panjang banget";
  const VOTNoVideoIDFound$x = "Ora ana ID video sing ditemokake";
  const VOTSubtitles$x = "Subtitle";
  const VOTSubtitlesDisabled$x = "Cacat";
  const VOTSubtitlesMaxLength$x = "Subtitle dawa maksimal";
  const VOTHighlightWords$x = "Sorotan tembung";
  const VOTTranslatedFrom$x = "diterjemahaké saka";
  const VOTAutogenerated$x = "autogénérasi";
  const VOTSettings$x = "Setelan VOT";
  const VOTMenuLanguage$x = "Basa Menu";
  const VOTAuthors$x = "Panganggit";
  const VOTVersion$x = "Versi";
  const VOTLoader$x = "Pangisi";
  const VOTBrowser$x = "Browser";
  const VOTShowPiPButton$x = "Gambar tombol PiP";
  const langs$x = { "auto": "Otomatis", "af": "Basa afrika", "ak": "Akan", "sq": "Basa albania", "am": "Basa amhar", "ar": "Basa arab", "hy": "Basa arménia", "as": "Basa assam", "ay": "Aymara", "az": "Basa azerbaijan", "bn": "Bangla", "eu": "Basa Basque", "be": "Basa bélarus", "bho": "Bhojpuri", "bs": "Basa bosnia", "bg": "Basa bulgaria", "my": "Basa burma", "ca": "Basa katalan", "ceb": "Cébuano", "zh": "Cina", "zh-Hans": "Cina (Disederhanakake)", "zh-Hant": "Cina (Tradisional)", "co": "Korsika", "hr": "Basa kroasia", "cs": "Basa ceko", "da": "Basa denmark", "dv": "Nyilem", "nl": "Basa walanda", "en": "Basa inggris", "eo": "Basa Esperanto", "et": "Basa estonia", "ee": "Wedhus", "fil": "Basa filipina", "fi": "Basa finlandia", "fr": "Basa prancis", "gl": "Basa galicia", "lg": "Ganda", "ka": "Basa géorgia", "de": "Basa jerman", "el": "Yunani", "gn": "Guarani", "gu": "Basa Gujarati", "ht": "Basa Kréol Haiti", "ha": "Basa Hausa", "haw": "Basa hawaii", "iw": "Basa ibrani", "hi": "Basa Hindi", "hmn": "Hmong", "hu": "Basa hongaria", "is": "Basa islandia", "ig": "Gbo (ing basa inggris)", "id": "Indonésia", "ga": "Basa irlandia", "it": "Basa italia", "ja": "Basa jepang", "jv": "Basa jawa", "kn": "Basa Kannada", "kk": "Basa Kazakh", "km": "Khmèr", "rw": "Kinyarwanda", "ko": "Basa koréa", "kri": "Krio", "ku": "Basa kurdi", "ky": "Kirgiz", "lo": "Laos", "la": "Latin", "lv": "Basa latvia", "ln": "Lingal", "lt": "Basa lithuania", "lb": "Basa luksemburg", "mk": "Basa makédonia", "mg": "Basa madagaskar", "ms": "Malayu", "ml": "Malayalam", "mt": "Basa malta", "mi": "Basa Māori", "mr": "Basa Marathi", "mn": "Basa mongol", "ne": "Basa nepal", "nso": "Sotho Lor", "no": "Basa norwegia", "ny": "Nyanja", "or": "Odia", "om": "Oromo", "ps": "Basa Pashto", "fa": "Basa pèrsi", "pl": "Basa polandia", "pt": "Basa portugis", "pa": "Basa Punjabi", "qu": "Basa Quechua", "ro": "Basa rumania", "ru": "Basa rusia", "sm": "Basa samoa", "sa": "Basa sansekerta", "gd": "Basa Gaelik Skotlandia", "sr": "Basa serbia", "sn": "Shona", "sd": "Sindhi", "si": "Sinhala", "sk": "Basa slowak", "sl": "Basa slovenia", "so": "Basa Somali", "st": "Sotho Kidul", "es": "Basa spanyol", "su": "Basa sunda", "sw": "Swahili", "sv": "Basa swédia", "tg": "Basa Tajik", "ta": "Basa Tamil", "tt": "Tatar", "te": "Basa jawa", "th": "Basa Thai", "ti": "Tigrinya", "ts": "Tsonga", "tr": "Basa turki", "tk": "Wong Turkmen", "uk": "Basa ukraina", "ur": "Urdu", "ug": "Uyghur", "uz": "Basa uzbèk", "vi": "Basa vietnam", "cy": "Basa wales", "fy": "Frisian Kulon", "xh": "Xhosa", "yi": "Basa Yiddish", "yo": "Basa Yoruba", "zu": "Zulu" };
  const streamNoConnectionToServer$x = "Ora ana sambungan menyang server";
  const searchField$x = "Nggolèki...";
  const VOTTranslateAPIErrors$x = "Translate kasalahan SAKA API";
  const VOTDetectService$x = "Layanan deteksi basa";
  const VOTProxyWorkerHost$x = "Ketik alamat buruh proxy";
  const VOTM3u8ProxyHost$x = "Pilih alamat m3u8 pekerja proxy";
  const proxySettings$x = "Setelan Proxy";
  const translationTakeApproximatelyMinute2$x = "Terjemahan bakal njupuk kira-kira {0} menit";
  const VOTAudioBooster$x = "Tambah volume terjemahan sing ditambahi";
  const VOTSubtitlesDesign$x = "Subtitle desain";
  const VOTSubtitlesFontSize$x = "Ukuran Font saka subtitles";
  const VOTSubtitlesOpacity$x = "Transparansi latar mburi subtitle";
  const VOTSubtitlesDownloadFormat$x = "Format kanggo ngundhuh subtitles";
  const VOTDownloadWithName$x = "Ngundhuh file kanthi jeneng video";
  const VOTUpdateLocaleFiles$x = "Nganyari file lokalisasi";
  const VOTLocaleHash$x = "Hash lokal";
  const VOTUpdatedAt$x = "Dianyari jam";
  const VOTNeedWebAudioAPI$x = "Kanggo ngaktifake IKI, SAMPEYAN kudu DUWE API Audio Web";
  const VOTMediaCSPEnabledOnSite$x = "Media CSP diaktifake ing situs iki";
  const VOTOnlyBypassMediaCSP$x = "Gunakna Mung Kanggo Ngliwati MEDIA CSP";
  const VOTNewAudioPlayer$x = "Gunakake pamuter audio anyar";
  const VOTUseNewModel$x = "Gunakake variasi eksperimen Swara Yandex kanggo sawetara video";
  const TranslationDelayed$x = "Terjemahan rada telat";
  const VOTTranslationCompletedNotify$x = "Terjemahan ing {0} wis rampung!";
  const VOTSendNotifyOnComplete$x = "Kirimi kabar sing video wis diterjemahaké";
  const VOTBugReport$x = "Laporan bug";
  const VOTTranslateProxyDisabled$x = "Cacat";
  const VOTTranslateProxyEnabled$x = "Diaktifake";
  const VOTTranslateProxyEverything$x = "Proxy kabèh";
  const VOTTranslateProxyStatus$x = "Mode proxy";
  const VOTTranslatedBy$x = "Dijarwakake dening {0}";
  const VOTStreamNotAvailable$x = "Translate stream ora kasedhiya";
  const VOTTranslationTextService$x = "Layanan terjemahan teks";
  const VOTNotAffectToVoice$x = "Ora mengaruhi terjemahan teks ing swara liwat";
  const DontTranslateSelectedLanguages$x = "Aja nerjemahake saka basa sing dipilih";
  const showVideoVolumeSlider$x = "Tampilan geser volume video";
  const hotkeysSettings$x = "Setelan Hotkeys";
  const None$x = "Ora ana";
  const VOTUseLivelyVoice$x = "Gunakna swara sing nyenengké. Para pamicara kaya wong Rusia asli.";
  const miscSettings$x = "Setelan Misc";
  const services$x = { "yandexbrowser": "Browser Yandex", "msedge": "Microsoft Pinggiran", "rust-server": "Server Karat" };
  const aboutExtension$x = "Babagan ekstensi";
  const appearance$x = "Tampilan";
  const buttonPosition$x = "Posisi tombol ing pamuter";
  const position$x = { "left": "Kiwa", "right": "Ya wis", "top": "Ndhuwur", "default": "Gagal" };
  const secs$x = "sèk";
  const autoHideButtonDelay$x = "Tundha sadurunge ndhelikake tombol translate";
  const notFound$x = "ora ditemokaké";
  const minButtonPositionContainer$x = "Posisi tombol mung owah-owahan ing pemain luwih saka 600 piksel.";
  const VOTTranslateProxyStatusDefault$x = "Kanthi mateni proxying ing negara sampeyan bisa ngilangi ekstensi kasebut";
  const PressTheKeyCombination$x = "Pencet kombinasi tombol...";
  const VOTUseAudioDownload$x = "Gunakake audio download";
  const VOTUseAudioDownloadWarning$x = "Mateni download audio bisa mengaruhi fungsi ekstensi";
  const VOTAccountRequired$x = "Sampeyan kudu mlebu kanggo nggunakake fitur iki";
  const VOTMyAccount$x = "Akunku";
  const VOTLogin$x = "Login";
  const VOTLogout$x = "Logout";
  const VOTRefresh$x = "Nganyari";
  const VOTYandexToken$x = "Mlebu Ing Yandex Token OAuth";
  const VOTYandexTokenInfo$x = "Sampeyan bisa kanthi manual nyetel akun token ing lapangan iki. Elinga yen kita ora mriksa validitas sadurunge ngirim panjaluk terjemahan";
  const VOTLoginViaToken$x = "Mlebu liwat token";
  const subtitlesSmartLayout$x = "Tata letak subtitle pinter";
  const smartDucking$x = "Volume adaptif";
  const VOTAutoSubtitles$x = "Subtitle ing mbukak";
  const VOTSubtitlesFont$x = "Font Subtitle";
  const __vite_glob_0_28 = {
    recommended: recommended$x,
    translateVideo: translateVideo$x,
    disableTranslate: disableTranslate$x,
    translationSettings: translationSettings$x,
    subtitlesSettings: subtitlesSettings$x,
    resetSettings: resetSettings$x,
    videoBeingTranslated: videoBeingTranslated$x,
    videoLanguage: videoLanguage$x,
    translationLanguage: translationLanguage$x,
    translationTake: translationTake$x,
    translationTakeMoreThanHour: translationTakeMoreThanHour$x,
    translationTakeAboutMinute: translationTakeAboutMinute$x,
    translationTakeFewMinutes: translationTakeFewMinutes$x,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$x,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$x,
    requestTranslationFailed: requestTranslationFailed$x,
    audioNotReceived: audioNotReceived$x,
    audioFormatNotSupported: audioFormatNotSupported$x,
    VOTAutoTranslate: VOTAutoTranslate$x,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$x,
    VOTVolume: VOTVolume$x,
    VOTVolumeTranslation: VOTVolumeTranslation$x,
    VOTAutoSetVolume: VOTAutoSetVolume$x,
    VOTShowVideoSlider: VOTShowVideoSlider$x,
    VOTSyncVolume: VOTSyncVolume$x,
    VOTDisableFromYourLang: VOTDisableFromYourLang$x,
    VOTVideoIsTooLong: VOTVideoIsTooLong$x,
    VOTNoVideoIDFound: VOTNoVideoIDFound$x,
    VOTSubtitles: VOTSubtitles$x,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$x,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$x,
    VOTHighlightWords: VOTHighlightWords$x,
    VOTTranslatedFrom: VOTTranslatedFrom$x,
    VOTAutogenerated: VOTAutogenerated$x,
    VOTSettings: VOTSettings$x,
    VOTMenuLanguage: VOTMenuLanguage$x,
    VOTAuthors: VOTAuthors$x,
    VOTVersion: VOTVersion$x,
    VOTLoader: VOTLoader$x,
    VOTBrowser: VOTBrowser$x,
    VOTShowPiPButton: VOTShowPiPButton$x,
    langs: langs$x,
    streamNoConnectionToServer: streamNoConnectionToServer$x,
    searchField: searchField$x,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$x,
    VOTDetectService: VOTDetectService$x,
    VOTProxyWorkerHost: VOTProxyWorkerHost$x,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$x,
    proxySettings: proxySettings$x,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$x,
    VOTAudioBooster: VOTAudioBooster$x,
    VOTSubtitlesDesign: VOTSubtitlesDesign$x,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$x,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$x,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$x,
    VOTDownloadWithName: VOTDownloadWithName$x,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$x,
    VOTLocaleHash: VOTLocaleHash$x,
    VOTUpdatedAt: VOTUpdatedAt$x,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$x,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$x,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$x,
    VOTNewAudioPlayer: VOTNewAudioPlayer$x,
    VOTUseNewModel: VOTUseNewModel$x,
    TranslationDelayed: TranslationDelayed$x,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$x,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$x,
    VOTBugReport: VOTBugReport$x,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$x,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$x,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$x,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$x,
    VOTTranslatedBy: VOTTranslatedBy$x,
    VOTStreamNotAvailable: VOTStreamNotAvailable$x,
    VOTTranslationTextService: VOTTranslationTextService$x,
    VOTNotAffectToVoice: VOTNotAffectToVoice$x,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$x,
    showVideoVolumeSlider: showVideoVolumeSlider$x,
    hotkeysSettings: hotkeysSettings$x,
    None: None$x,
    VOTUseLivelyVoice: VOTUseLivelyVoice$x,
    miscSettings: miscSettings$x,
    services: services$x,
    aboutExtension: aboutExtension$x,
    appearance: appearance$x,
    buttonPosition: buttonPosition$x,
    position: position$x,
    secs: secs$x,
    autoHideButtonDelay: autoHideButtonDelay$x,
    notFound: notFound$x,
    minButtonPositionContainer: minButtonPositionContainer$x,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$x,
    PressTheKeyCombination: PressTheKeyCombination$x,
    VOTUseAudioDownload: VOTUseAudioDownload$x,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$x,
    VOTAccountRequired: VOTAccountRequired$x,
    VOTMyAccount: VOTMyAccount$x,
    VOTLogin: VOTLogin$x,
    VOTLogout: VOTLogout$x,
    VOTRefresh: VOTRefresh$x,
    VOTYandexToken: VOTYandexToken$x,
    VOTYandexTokenInfo: VOTYandexTokenInfo$x,
    VOTLoginViaToken: VOTLoginViaToken$x,
    subtitlesSmartLayout: subtitlesSmartLayout$x,
    smartDucking: smartDucking$x,
    VOTAutoSubtitles: VOTAutoSubtitles$x,
    VOTSubtitlesFont: VOTSubtitlesFont$x
  };
  const recommended$w = "ұсынылады";
  const translateVideo$w = "Бейнені аудару";
  const disableTranslate$w = "Өшіру";
  const translationSettings$w = "Аударма параметрлері";
  const subtitlesSettings$w = "Субтитр параметрлері";
  const resetSettings$w = "Параметрлерді қайта орнату";
  const videoBeingTranslated$w = "Бейнеленіп жатқан бейне";
  const videoLanguage$w = "Бейненің тілі";
  const translationLanguage$w = "Аударма тілі";
  const translationTake$w = "Аударма шығару уақыты";
  const translationTakeMoreThanHour$w = "Аударма бір сағатқа немесе оны шығару үшін да көп уақыт салады";
  const translationTakeAboutMinute$w = "Аударма бір мүндегі уақытты шығарады";
  const translationTakeFewMinutes$w = "Аударма бірнеше мүндегі уақытты шығарады";
  const translationTakeApproximatelyMinutes$w = "Аударма қазірғі түрде {0} минутқа жағынан аударылатын болады";
  const translationTakeApproximatelyMinute$w = "Аударма қазірғі түрде {0} минутқа жағынан аударылатын болады";
  const requestTranslationFailed$w = "Бейнені аудару талап етуден кейін сәтсіз аяқталды";
  const audioNotReceived$w = "Аудиоға сілтеме алынған жоқ";
  const audioFormatNotSupported$w = "Аудио пішімі қолдау көрсетмейді";
  const VOTAutoTranslate$w = "Ашылада переводдау";
  const VOTDontTranslateYourLang$w = "Өз тіліңнен таратпау";
  const VOTVolume$w = "Бейне көлемі:";
  const VOTVolumeTranslation$w = "Аударма көлемі:";
  const VOTAutoSetVolume$w = "Бейне дыбыс деңгейін төмендетіңіз";
  const VOTShowVideoSlider$w = "Бейненің дыбыссын көрсету";
  const VOTSyncVolume$w = "Аударма дыбысын бейненің дыбысымен біріктіру";
  const VOTDisableFromYourLang$w = "Сіз тіліңізде бейненің аудармасын өшірдіңіз";
  const VOTVideoIsTooLong$w = "Бейне тым ұзын";
  const VOTNoVideoIDFound$w = "Бейненің ID-сі табылмады";
  const VOTSubtitles$w = "Субтитрлер";
  const VOTSubtitlesDisabled$w = "Өшіру";
  const VOTSubtitlesMaxLength$w = "Субтитрдердің максималды ұзындығы";
  const VOTHighlightWords$w = "Сөздерді жарықтау";
  const VOTTranslatedFrom$w = "аудармаға";
  const VOTAutogenerated$w = "автоматты ұқсас жасалған";
  const VOTSettings$w = "VOT параметрлері";
  const VOTMenuLanguage$w = "Мәзір тілі";
  const VOTAuthors$w = "Авторлар";
  const VOTVersion$w = "Нұсқа";
  const VOTLoader$w = "Жүктеуші";
  const VOTBrowser$w = "Браузер";
  const VOTShowPiPButton$w = "PiP түймесін көрсету";
  const langs$w = { "auto": "Автоматты түрде", "az": "Әзербайжан", "ay": "Аймара", "ak": "Акан", "sq": "Албан", "am": "Амхар", "en": "Ағылшын", "ar": "Араб", "hy": "Армян", "as": "Ассам", "af": "Африкаанс", "eu": "Баск", "be": "Беларус", "bn": "Бенгал", "my": "Бирман", "bg": "Болгар", "bs": "Босния", "bho": "Бходжпури", "cy": "Валлий", "hu": "Венгр", "vi": "Вьетнам", "haw": "Гавай", "ht": "Гаити", "gl": "Галисия", "lg": "Ганда", "el": "Грек", "ka": "Грузин", "gn": "Гуарани", "gu": "Гуджарати", "gd": "Шотландия гэль", "da": "Дания", "fy": "Батыс фриз", "zu": "Зулу", "iw": "Иврит", "ig": "Игбо", "yi": "Идиш", "id": "Индонезия", "ga": "Ирланд", "is": "Исланд", "es": "Испан", "it": "Итальян", "yo": "Йоруба", "kk": "Қазақ", "kn": "Каннада", "ca": "Каталан", "qu": "Кечуа", "rw": "Киньяруанда", "ky": "Қырғыз", "zh": "Қытай", "zh-Hant": "Қытай (Салт) ", "zh-Hans": "Қытай (Қарапайым)", "ko": "Корей", "co": "Корсика", "xh": "Коса", "ku": "Курд", "km": "Кхмер", "lo": "Лаос", "la": "Латын", "lv": "Латыш", "ln": "Лингала", "lt": "Литва", "lb": "Люксембург", "mk": "Македон", "mg": "Малагаси", "ms": "Малай", "ml": "Малаялам", "dv": "Мальдив", "mt": "Мальта", "mi": "Маори", "mr": "Маратхи", "mn": "Моңғол", "de": "Неміс", "ne": "Непал", "nl": "Голланд", "no": "Норвег", "ny": "Чиньянджа", "or": "Ория", "om": "Оромо", "pa": "Пенджаб", "fa": "Парсы", "pl": "Поляк", "pt": "Португал", "ps": "Пушту", "ro": "Румын", "ru": "Орыс", "sm": "Самоа", "sa": "Санскрит", "ceb": "Себуано", "nso": "Солтүстік Сото", "sr": "Серб", "si": "Сингал", "sd": "Синдхи", "sk": "Словак", "sl": "Словен", "so": "Сомали", "sw": "Суахили", "su": "Сундан", "tg": "Тәжік", "th": "Тай", "ta": "Тамил", "tt": "Татар", "te": "Телугу", "ti": "Тигринья", "ts": "Тсонга", "tr": "Түрік", "tk": "Түрікмен", "uz": "Өзбек", "ug": "Үйғұр", "uk": "Украин", "ur": "Урду", "fil": "Филиппин", "fi": "Фин", "fr": "Француз", "ha": "Хауса", "hi": "Ҳинд", "hmn": "Хмонг", "hr": "Хорват", "cs": "Чех", "sv": "Швед", "sn": "Шона", "ee": "Эбе", "eo": "Эсперанто", "et": "Эстон", "st": "Оңтүстік Сото", "jv": "Ява", "ja": "Жапон", "kri": "Кри" };
  const streamNoConnectionToServer$w = "Серверге қосылу жоқ";
  const searchField$w = "Іздеу...";
  const VOTTranslateAPIErrors$w = "API-ден қателерді аудару";
  const VOTDetectService$w = "Тілдерді анықтау қызметі";
  const VOTProxyWorkerHost$w = "Прокси-сервердің жұмыс мекенжайын енгізіңіз";
  const VOTM3u8ProxyHost$w = "M3u8 жұмыс прокси мекенжайын енгізіңіз";
  const proxySettings$w = "Прокси параметрлері";
  const translationTakeApproximatelyMinute2$w = "Аударма шамамен {0} минутты алады";
  const VOTAudioBooster$w = "Аударма көлемінің ұлғаюы";
  const VOTSubtitlesDesign$w = "Субтитрлер дизайны";
  const VOTSubtitlesFontSize$w = "Субтитрлердің қаріп өлшемі";
  const VOTSubtitlesOpacity$w = "Субтитр фонының мөлдірлігі";
  const VOTSubtitlesDownloadFormat$w = "Субтитрлерді жүктеу форматы";
  const VOTDownloadWithName$w = "Бейне атауы бар файлдарды жүктеңіз";
  const VOTUpdateLocaleFiles$w = "Локализация файлдарын жаңарту";
  const VOTLocaleHash$w = "Жергілікті хэш";
  const VOTUpdatedAt$w = "жаңартылды";
  const VOTNeedWebAudioAPI$w = "Мұны қосу Үшін Сізде Web Audio API БОЛУЫ КЕРЕК";
  const VOTMediaCSPEnabledOnSite$w = "Бұл сайтта медиа CSP қосылған";
  const VOTOnlyBypassMediaCSP$w = "Оны Тек Csp Медиасын айналып өту үшін пайдаланыңыз";
  const VOTNewAudioPlayer$w = "Жаңа аудио ойнатқышты пайдаланыңыз";
  const VOTUseNewModel$w = "Кейбір бейнелер Үшін яндекс дауыстарының эксперименттік нұсқасын пайдаланыңыз";
  const TranslationDelayed$w = "Аударма сәл кешіктірілді";
  const VOTTranslationCompletedNotify$w = "{0} бойынша аударма аяқталды!";
  const VOTSendNotifyOnComplete$w = "Бейне аударылғаны туралы хабарлама жіберіңіз";
  const VOTBugReport$w = "Қате туралы хабарлау";
  const VOTTranslateProxyDisabled$w = "Мүгедек";
  const VOTTranslateProxyEnabled$w = "Қосылған";
  const VOTTranslateProxyEverything$w = "Барлығын прокси арқылы жіберіңіз";
  const VOTTranslateProxyStatus$w = "Прокси режимі";
  const VOTTranslatedBy$w = "Аударған {0}";
  const VOTStreamNotAvailable$w = "Трансляция ағыны қолжетімді емес";
  const VOTTranslationTextService$w = "Мәтінді аудару қызметі";
  const VOTNotAffectToVoice$w = "Мәтіннің дауыспен аударылуына әсер етпейді";
  const DontTranslateSelectedLanguages$w = "Таңдалған тілдерден аудармаңыз";
  const showVideoVolumeSlider$w = "Бейне дыбыс деңгейі сырғытпасын көрсету";
  const hotkeysSettings$w = "Жылдам пернелердің параметрлері";
  const None$w = "Жоқ";
  const VOTUseLivelyVoice$w = "Жанды дауыстарды қолданыңыз. Спикерлер жергілікті Орыстар сияқты естіледі.";
  const miscSettings$w = "Misc параметрлері";
  const services$w = { "yandexbrowser": "Яндекс Браузері", "msedge": "Microsoft Edge", "rust-server": "Тот Сервері" };
  const aboutExtension$w = "Ұзарту туралы";
  const appearance$w = "Сыртқы түрі";
  const buttonPosition$w = "Ойнатқыштағы түйменің орны";
  const position$w = { "left": "Солға", "right": "Дұрыс", "top": "Жоғары", "default": "Әдепкі" };
  const secs$w = "әкк";
  const autoHideButtonDelay$w = "Аудару түймесін жасырмас бұрын кідірту";
  const notFound$w = "табылмады";
  const minButtonPositionContainer$w = "Түйменің орны тек 600 пиксельден асатын ойнатқыштарда өзгереді.";
  const VOTTranslateProxyStatusDefault$w = "Сіздің елде прокси-серверді толығымен өшіру кеңейтімді бұзуы мүмкін";
  const PressTheKeyCombination$w = "Пернелер тіркесімін басыңыз...";
  const VOTUseAudioDownload$w = "Аудио жүктеуді пайдаланыңыз";
  const VOTUseAudioDownloadWarning$w = "Аудио жүктеулерді өшіру кеңейтімнің жұмысына әсер етуі мүмкін";
  const VOTAccountRequired$w = "Бұл мүмкіндікті пайдалану үшін жүйеге кіру керек";
  const VOTMyAccount$w = "Менің аккаунтым";
  const VOTLogin$w = "Кіру";
  const VOTLogout$w = "Шығу";
  const VOTRefresh$w = "Жаңарту";
  const VOTYandexToken$w = "Yandex OAuth Таңбалауышын енгізіңіз";
  const VOTYandexTokenInfo$w = "Осы өрісте тіркелгі таңбалауышын қолмен орнатуға болады. Аударма сұрауын жібермес бұрын оның жарамдылығын тексермейтінімізді ескеріңіз";
  const VOTLoginViaToken$w = "Токен арқылы кіру";
  const subtitlesSmartLayout$w = "Ақылды субтитр орналасуы";
  const smartDucking$w = "Адаптивті көлем";
  const VOTAutoSubtitles$w = "Ашық субтитрлер";
  const VOTSubtitlesFont$w = "Субтитр қаріпі";
  const __vite_glob_0_29 = {
    recommended: recommended$w,
    translateVideo: translateVideo$w,
    disableTranslate: disableTranslate$w,
    translationSettings: translationSettings$w,
    subtitlesSettings: subtitlesSettings$w,
    resetSettings: resetSettings$w,
    videoBeingTranslated: videoBeingTranslated$w,
    videoLanguage: videoLanguage$w,
    translationLanguage: translationLanguage$w,
    translationTake: translationTake$w,
    translationTakeMoreThanHour: translationTakeMoreThanHour$w,
    translationTakeAboutMinute: translationTakeAboutMinute$w,
    translationTakeFewMinutes: translationTakeFewMinutes$w,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$w,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$w,
    requestTranslationFailed: requestTranslationFailed$w,
    audioNotReceived: audioNotReceived$w,
    audioFormatNotSupported: audioFormatNotSupported$w,
    VOTAutoTranslate: VOTAutoTranslate$w,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$w,
    VOTVolume: VOTVolume$w,
    VOTVolumeTranslation: VOTVolumeTranslation$w,
    VOTAutoSetVolume: VOTAutoSetVolume$w,
    VOTShowVideoSlider: VOTShowVideoSlider$w,
    VOTSyncVolume: VOTSyncVolume$w,
    VOTDisableFromYourLang: VOTDisableFromYourLang$w,
    VOTVideoIsTooLong: VOTVideoIsTooLong$w,
    VOTNoVideoIDFound: VOTNoVideoIDFound$w,
    VOTSubtitles: VOTSubtitles$w,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$w,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$w,
    VOTHighlightWords: VOTHighlightWords$w,
    VOTTranslatedFrom: VOTTranslatedFrom$w,
    VOTAutogenerated: VOTAutogenerated$w,
    VOTSettings: VOTSettings$w,
    VOTMenuLanguage: VOTMenuLanguage$w,
    VOTAuthors: VOTAuthors$w,
    VOTVersion: VOTVersion$w,
    VOTLoader: VOTLoader$w,
    VOTBrowser: VOTBrowser$w,
    VOTShowPiPButton: VOTShowPiPButton$w,
    langs: langs$w,
    streamNoConnectionToServer: streamNoConnectionToServer$w,
    searchField: searchField$w,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$w,
    VOTDetectService: VOTDetectService$w,
    VOTProxyWorkerHost: VOTProxyWorkerHost$w,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$w,
    proxySettings: proxySettings$w,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$w,
    VOTAudioBooster: VOTAudioBooster$w,
    VOTSubtitlesDesign: VOTSubtitlesDesign$w,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$w,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$w,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$w,
    VOTDownloadWithName: VOTDownloadWithName$w,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$w,
    VOTLocaleHash: VOTLocaleHash$w,
    VOTUpdatedAt: VOTUpdatedAt$w,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$w,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$w,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$w,
    VOTNewAudioPlayer: VOTNewAudioPlayer$w,
    VOTUseNewModel: VOTUseNewModel$w,
    TranslationDelayed: TranslationDelayed$w,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$w,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$w,
    VOTBugReport: VOTBugReport$w,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$w,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$w,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$w,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$w,
    VOTTranslatedBy: VOTTranslatedBy$w,
    VOTStreamNotAvailable: VOTStreamNotAvailable$w,
    VOTTranslationTextService: VOTTranslationTextService$w,
    VOTNotAffectToVoice: VOTNotAffectToVoice$w,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$w,
    showVideoVolumeSlider: showVideoVolumeSlider$w,
    hotkeysSettings: hotkeysSettings$w,
    None: None$w,
    VOTUseLivelyVoice: VOTUseLivelyVoice$w,
    miscSettings: miscSettings$w,
    services: services$w,
    aboutExtension: aboutExtension$w,
    appearance: appearance$w,
    buttonPosition: buttonPosition$w,
    position: position$w,
    secs: secs$w,
    autoHideButtonDelay: autoHideButtonDelay$w,
    notFound: notFound$w,
    minButtonPositionContainer: minButtonPositionContainer$w,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$w,
    PressTheKeyCombination: PressTheKeyCombination$w,
    VOTUseAudioDownload: VOTUseAudioDownload$w,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$w,
    VOTAccountRequired: VOTAccountRequired$w,
    VOTMyAccount: VOTMyAccount$w,
    VOTLogin: VOTLogin$w,
    VOTLogout: VOTLogout$w,
    VOTRefresh: VOTRefresh$w,
    VOTYandexToken: VOTYandexToken$w,
    VOTYandexTokenInfo: VOTYandexTokenInfo$w,
    VOTLoginViaToken: VOTLoginViaToken$w,
    subtitlesSmartLayout: subtitlesSmartLayout$w,
    smartDucking: smartDucking$w,
    VOTAutoSubtitles: VOTAutoSubtitles$w,
    VOTSubtitlesFont: VOTSubtitlesFont$w
  };
  const recommended$v = "បានផ្ដល់ជាអនុសាសន៍";
  const translateVideo$v = "បកប្រែវីដេអូ";
  const disableTranslate$v = "បិទ";
  const translationSettings$v = "ការកំណត់ការបកប្រែ";
  const subtitlesSettings$v = "ការកំណត់ចំណងជើងរង";
  const resetSettings$v = "កំណត់ការកំណត់ឡើងវិញ";
  const videoBeingTranslated$v = "វីដេអូនេះត្រូវបានបកប្រែ";
  const videoLanguage$v = "ភាសាវីដេអូ";
  const translationLanguage$v = "ភាសាបកប្រែ";
  const translationTake$v = "ការបកប្រែនឹងយក";
  const translationTakeMoreThanHour$v = "ការបកប្រែនេះនឹងចំណាយពេលច្រើនជាងមួយម៉ោង";
  const translationTakeAboutMinute$v = "ការបកប្រែនេះនឹងចំណាយពេលប្រហែលមួយនាទី";
  const translationTakeFewMinutes$v = "ការបកប្រែនេះនឹងចំណាយពេលពីរបីនាទី";
  const translationTakeApproximatelyMinutes$v = "ការបកប្រែនេះនឹងចំណាយពេលប្រមាណ{0}នាទី";
  const translationTakeApproximatelyMinute$v = "ការបកប្រែនេះនឹងចំណាយពេលប្រមាណ{0}នាទី";
  const requestTranslationFailed$v = "បរាជ័យក្នុងការស្នើសុំបកប្រែវីដេអូ";
  const audioNotReceived$v = "មិនបានទទួលតំណអូឌីយ៉ូ";
  const audioFormatNotSupported$v = "ទ្រង់ទ្រាយអូឌីយ៉ូមិនត្រូវបានគាំទ្រទេ";
  const VOTAutoTranslate$v = "បកប្រែបើក";
  const VOTDontTranslateYourLang$v = "កុំបកប្រែពីភាសារបស់ខ្ញុំ";
  const VOTVolume$v = "កម្រិតសំឡេងវីដេអូ:";
  const VOTVolumeTranslation$v = "កម្រិតសំឡេងបកប្រែ:";
  const VOTAutoSetVolume$v = "កាត់បន្ថយកម្រិតសំឡេងវីដេអូ";
  const VOTShowVideoSlider$v = "គ្រាប់រំកិលកម្រិតសំឡេងវីដេអូ";
  const VOTSyncVolume$v = "តំណបកប្រែនិងភាគវីដេអូ";
  const VOTDisableFromYourLang$v = "អ្នកបានបិទការបកប្រែវីដេអូជាភាសារបស់អ្នក";
  const VOTVideoIsTooLong$v = "វីដេអូយូរពេក";
  const VOTNoVideoIDFound$v = "រកមិនឃើញលេខសម្គាល់វីដេអូ";
  const VOTSubtitles$v = "ចំណងជើងរង";
  const VOTSubtitlesDisabled$v = "មិនអនុញ្ញាត";
  const VOTSubtitlesMaxLength$v = "ចំណងជើងរងប្រវែងអតិបរមា";
  const VOTHighlightWords$v = "បន្លិចពាក្យ";
  const VOTTranslatedFrom$v = "បកប្រែពី";
  const VOTAutogenerated$v = "បានបង្កើតស្វ័យប្រវត្តិ";
  const VOTSettings$v = "ការកំណត់ការបោះឆ្នោត";
  const VOTMenuLanguage$v = "ម៉ឺនុយភាសា";
  const VOTAuthors$v = "អ្នកនិពន្ធ";
  const VOTVersion$v = "កំណែ";
  const VOTLoader$v = "កម្មវិធី";
  const VOTBrowser$v = "កម្មវិធីរុករក";
  const VOTShowPiPButton$v = "បង្ហាញប៊ូតុង PiP";
  const langs$v = { "auto": "ស្វ័យប្រវត្តិ", "af": "Afrikaans", "ak": "Akan", "sq": "អាល់បានី", "am": "Amharic", "ar": "អារ៉ាប់", "hy": "អាមេនីname", "as": "អាសាម៉េសname", "ay": "Aymara", "az": "អាហ្សែបែហ្សង់name", "bn": "បង់ក្លាដែស", "eu": "បាស្កេ", "be": "បេឡារុស", "bho": "Bhojpuri", "bs": "បូស្ន៊ី", "bg": "ប៊ុលហ្គារី", "my": "ភូមា", "ca": "កាតាឡាន", "ceb": "សេប៊ូណូ", "zh": "ចិន", "zh-Hans": "ភាសាចិន(អក្សរកាត់)", "zh-Hant": "ចិន(ប្រពៃណី)", "co": "Corsican", "hr": "ក្រូអាត", "cs": "ឆែក", "da": "ដាណឺម៉ាក", "dv": "Divehi", "nl": "ហូឡង់", "en": "ភាសាអង់គ្លេស", "eo": "Esperanto", "et": "អេស្តូនី", "ee": "ញ៉ាំ", "fil": "ហ្វីលីពីន", "fi": "ហ្វាំងឡង់", "fr": "បារាំង", "gl": "ហ្គាលីស្យាន", "lg": "Ganda", "ka": "ហ្សកហ្ស៊ី", "de": "អាល្លឺម៉ង់", "el": "ក្រិក", "gn": "Guarani", "gu": "ហ្កុយ៉ារាទីname", "ht": "ហៃទីក្រែល", "ha": "Hausa", "haw": "ហាវ៉ៃ", "iw": "ភាសាហេប្រឺ", "hi": "ហិណ្ឌូ", "hmn": "ម៉ុង", "hu": "ហុងគ្រី", "is": "អ៊ីស្លង់", "ig": "អ៊ីបូ", "id": "ឥណ្ឌូនេស៊ី", "ga": "អៀរឡង់", "it": "អ៊ីតាលី", "ja": "ភាសាជប៉ុន", "jv": "ជ្វា", "kn": "កាណាដា", "kk": "កាហ្សាក់ស្ថាន", "km": "ភាសាខ្មែរ", "rw": "Kinyarwanda", "ko": "ភាសាកូរ៉េ", "kri": "Krio", "ku": "ភាសាខ្មែរ", "ky": "Kyrgyz", "lo": "ឡាវ", "la": "ឡាតាំង", "lv": "ឡាតវីយ៉ា", "ln": "Lingala", "lt": "លីទុយអានី", "lb": "លុចហ្សំបួរ", "mk": "ម៉ាសេដូនី", "mg": "ម៉ាឡាហ្កាស៊ី", "ms": "ម៉ាឡេ", "ml": "ម៉ាឡាយ៉ាឡាម", "mt": "ម៉ាល់តា", "mi": "ភាសាខ្មែរ", "mr": "Marathi", "mn": "ម៉ុងហ្គោលី", "ne": "នេប៉ាល់", "nso": "Sotho ភាគខាងជើង", "no": "ន័រវែស", "ny": "Nyanja", "or": "Odia", "om": "Oromo", "ps": "Pashto", "fa": "ពែក", "pl": "ប៉ូឡូញ", "pt": "ព័រទុយហ្គាល់", "pa": "ខេត្ត Punjabi", "qu": "Quechua", "ro": "រ៉ូម៉ានី", "ru": "រុស្ស៊ី", "sm": "សំនួរញឹកញាប់", "sa": "សំស្ក្រឹត", "gd": "អ៊ីម៉ែល:<url>", "sr": "ស៊ែប៊ី", "sn": "ស៊ូណា", "sd": "ភាសាខ្មែរ", "si": "ស៊ីនហាឡា", "sk": "ស្លូវ៉ាគី", "sl": "ស្លូវេនី", "so": "សូម៉ាលី", "st": "Sotho ភាគខាងត្បូង", "es": "ភាសាអេស្ប៉ាញ", "su": "ស៊ុនស្រីពេជ្រ", "sw": "ស្វាហ៊ីលី", "sv": "ស៊ុយអែដ", "tg": "តាស៊ីក្លូ", "ta": "តាមីលname", "tt": "Tatar", "te": "សំនួរញឹកញាប់", "th": "ថៃ", "ti": "Tigrinya", "ts": "Tsonga បាន", "tr": "ទួរគី", "tk": "Turkmen", "uk": "អ៊ុយក្រែន", "ur": "អ៊ូឌូ", "ug": "អៃហឺ", "uz": "អ៊ូបេគ", "vi": "វៀតណាម", "cy": "វែល", "fy": "ហ្វ្រីសៀនខាងលិច", "xh": "សាពូនមីដាដា", "yi": "អ៊ីដឌី", "yo": "អ៊ីម៉ែល", "zu": "ស៊ុយលូ" };
  const streamNoConnectionToServer$v = "មិនមានការតភ្ជាប់ទៅម៉ាស៊ីនបម្រើទេ";
  const searchField$v = "ស្វែងរក...";
  const VOTTranslateAPIErrors$v = "បកប្រែកំហុសពី API";
  const VOTDetectService$v = "សេវារកឃើញភាសា";
  const VOTProxyWorkerHost$v = "បញ្ចូលអាសយដ្ឋានកម្មករប្រូកស៊ី";
  const VOTM3u8ProxyHost$v = "បញ្ចូលអាសយដ្ឋានរបស់កម្មករប្រូកស៊ី m3u8";
  const proxySettings$v = "ការកំណត់ប្រូកស៊ី";
  const translationTakeApproximatelyMinute2$v = "ការបកប្រែនេះនឹងចំណាយពេលប្រមាណ{0}នាទី";
  const VOTAudioBooster$v = "បង្កើនកម្រិតសំឡេងបកប្រែដែលបានពង្រីក";
  const VOTSubtitlesDesign$v = "រចនាអក្សររត់ពីក្រោម";
  const VOTSubtitlesFontSize$v = "ទំហំពុម្ពអក្សរនៃចំណងជើងរង";
  const VOTSubtitlesOpacity$v = "តម្លាភាពនៃផ្ទៃខាងក្រោយចំណងជើងរង";
  const VOTSubtitlesDownloadFormat$v = "ទ្រង់ទ្រាយសម្រាប់ទាញយកចំណងជើងរង";
  const VOTDownloadWithName$v = "ទាញយកឯកសារដែលមានឈ្មោះវីដេអូ";
  const VOTUpdateLocaleFiles$v = "ធ្វើឲ្យឯកសារមូលដ្ឋានីយកម្មទាន់សម័យ";
  const VOTLocaleHash$v = "សញ្ញាមូលដ្ឋាន";
  const VOTUpdatedAt$v = "ធ្វើឱ្យទាន់សម័យនៅ";
  const VOTNeedWebAudioAPI$v = "ដើម្បីអនុញ្ញាតនេះ,អ្នកត្រូវតែមានបណ្តាញ Api របស់អូឌីយ៉ូ";
  const VOTMediaCSPEnabledOnSite$v = "ប្រព័ន្ធផ្សព្វផ្សាយ CSP ត្រូវបានបើកនៅលើតំបន់បណ្ដាញនេះ";
  const VOTOnlyBypassMediaCSP$v = "ប្រើវាសម្រាប់តែឆ្លងកាត់ប្រព័ន្ធផ្សព្វផ្សាយ CSP";
  const VOTNewAudioPlayer$v = "ប្រើកម្មវិធីចាក់អូឌីយ៉ូថ្មី";
  const VOTUseNewModel$v = "ប្រើបំរែបំរួលពិសោធន៍នៃសំឡេង Yandex សម្រាប់វីដេអូមួយចំនួន";
  const TranslationDelayed$v = "ការបកប្រែនេះត្រូវបានពន្យារពេលបន្តិច";
  const VOTTranslationCompletedNotify$v = "ការបកប្រែនៅលើនេះ{0}ត្រូវបានបញ្ចប់!";
  const VOTSendNotifyOnComplete$v = "ផ្ញើការជូនដំណឹងថាវីដេអូនេះត្រូវបានបកប្រែ";
  const VOTBugReport$v = "រាយការណ៍កំហុស";
  const VOTTranslateProxyDisabled$v = "មិនអនុញ្ញាត";
  const VOTTranslateProxyEnabled$v = "បានអនុញ្ញាត";
  const VOTTranslateProxyEverything$v = "អ្វីគ្រប់យ៉ាងប្រូកស៊ី";
  const VOTTranslateProxyStatus$v = "របៀបប្រូកស៊ី";
  const VOTTranslatedBy$v = "ប្រែសម្រួលដោយ{0}";
  const VOTStreamNotAvailable$v = "បកប្រែស្ទ្រីមគឺមិនអាចប្រើបាន";
  const VOTTranslationTextService$v = "សេវាបកប្រែអត្ថបទ";
  const VOTNotAffectToVoice$v = "មិនប៉ះពាល់ដល់ការបកប្រែអត្ថបទជាសំឡេងជាង";
  const DontTranslateSelectedLanguages$v = "កុំបកប្រែពីភាសាដែលបានជ្រើស";
  const showVideoVolumeSlider$v = "បង្ហាញគ្រាប់រំកិលកម្រិតសំឡេងវីដេអូ";
  const hotkeysSettings$v = "ការកំណត់គ្រាប់ចុចចង";
  const None$v = "គ្មាន";
  const VOTUseLivelyVoice$v = "ប្រើសំឡេងយ៉ាងរស់រវើក។ វាគ្មិនសំឡេងដូចជាជនជាតិដើមរុស្ស៊ី.";
  const miscSettings$v = "ការកំណត់ផ្សេងៗ";
  const services$v = { "yandexbrowser": "កម្មវិធីរុករក Yandex", "msedge": "ក្រុមហ៊ុន Microsoft គែម", "rust-server": "ម៉ាស៊ីនបម្រើច្រែះ" };
  const aboutExtension$v = "អំពីផ្នែកបន្ថែម";
  const appearance$v = "រូបរាង";
  const buttonPosition$v = "ប៊ូតុងទីតាំងនៅក្នុងការលេង";
  const position$v = { "left": "ឆ្វេង", "right": "ស្តាំ", "top": "កំពូល", "default": "លំនាំដើម" };
  const secs$v = "វិ.";
  const autoHideButtonDelay$v = "ពន្យារពេលមុនពេលលាក់ប៊ូតុងបកប្រែ";
  const notFound$v = "រកមិនឃើញ";
  const minButtonPositionContainer$v = "ទីតាំងប៊ូតុងនេះបានផ្លាស់ប្តូរតែនៅក្នុងអ្នកលេងដែលមានទំហំធំជាង 600 ភិចសែល.";
  const VOTTranslateProxyStatusDefault$v = "បិទទាំងស្រុងប្រូកស៊ីនៅក្នុងប្រទេសរបស់អ្នកអាចបំបែកផ្នែកបន្ថែម";
  const PressTheKeyCombination$v = "ចុចបន្សំគ្រាប់ចុច។..";
  const VOTUseAudioDownload$v = "ប្រើអូឌីយ៉ូទាញយក";
  const VOTUseAudioDownloadWarning$v = "បិទការទាញយកអូឌីយ៉ូអាចប៉ះពាល់ដល់មុខងាររបស់ផ្នែកបន្ថែម";
  const VOTAccountRequired$v = "អ្នកត្រូវការដើម្បីចូលក្នុងការប្រើលក្ខណៈពិសេសនេះ";
  const VOTMyAccount$v = "គណនីរបស់ខ្ញុ";
  const VOTLogin$v = "ចូល";
  const VOTLogout$v = "ចេញ";
  const VOTRefresh$v = "ធ្វើឲ្យស្រស់";
  const VOTYandexToken$v = "បញ្ចូលសញ្ញា Yandex OAuth";
  const VOTYandexTokenInfo$v = "អ្នកអាចកំណត់សញ្ញាគណនីនៅក្នុងវាលនេះដោយដៃ. សូមចំណាំថាយើងមិនបានពិនិត្យមើលសុពលភាពរបស់ខ្លួនមុនពេលផ្ញើសំណើបកប្រែ";
  const VOTLoginViaToken$v = "ចូលតាមរយៈ token";
  const subtitlesSmartLayout$v = "ប្លង់ចំណងជើងរងរបស់ Smart";
  const smartDucking$v = "កម្រិតសំឡេងសម្របសម្រួល";
  const VOTAutoSubtitles$v = "បើកចំណងជើងរង";
  const VOTSubtitlesFont$v = "ពុម្ពអក្សរចំណងជើងរង";
  const __vite_glob_0_30 = {
    recommended: recommended$v,
    translateVideo: translateVideo$v,
    disableTranslate: disableTranslate$v,
    translationSettings: translationSettings$v,
    subtitlesSettings: subtitlesSettings$v,
    resetSettings: resetSettings$v,
    videoBeingTranslated: videoBeingTranslated$v,
    videoLanguage: videoLanguage$v,
    translationLanguage: translationLanguage$v,
    translationTake: translationTake$v,
    translationTakeMoreThanHour: translationTakeMoreThanHour$v,
    translationTakeAboutMinute: translationTakeAboutMinute$v,
    translationTakeFewMinutes: translationTakeFewMinutes$v,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$v,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$v,
    requestTranslationFailed: requestTranslationFailed$v,
    audioNotReceived: audioNotReceived$v,
    audioFormatNotSupported: audioFormatNotSupported$v,
    VOTAutoTranslate: VOTAutoTranslate$v,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$v,
    VOTVolume: VOTVolume$v,
    VOTVolumeTranslation: VOTVolumeTranslation$v,
    VOTAutoSetVolume: VOTAutoSetVolume$v,
    VOTShowVideoSlider: VOTShowVideoSlider$v,
    VOTSyncVolume: VOTSyncVolume$v,
    VOTDisableFromYourLang: VOTDisableFromYourLang$v,
    VOTVideoIsTooLong: VOTVideoIsTooLong$v,
    VOTNoVideoIDFound: VOTNoVideoIDFound$v,
    VOTSubtitles: VOTSubtitles$v,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$v,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$v,
    VOTHighlightWords: VOTHighlightWords$v,
    VOTTranslatedFrom: VOTTranslatedFrom$v,
    VOTAutogenerated: VOTAutogenerated$v,
    VOTSettings: VOTSettings$v,
    VOTMenuLanguage: VOTMenuLanguage$v,
    VOTAuthors: VOTAuthors$v,
    VOTVersion: VOTVersion$v,
    VOTLoader: VOTLoader$v,
    VOTBrowser: VOTBrowser$v,
    VOTShowPiPButton: VOTShowPiPButton$v,
    langs: langs$v,
    streamNoConnectionToServer: streamNoConnectionToServer$v,
    searchField: searchField$v,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$v,
    VOTDetectService: VOTDetectService$v,
    VOTProxyWorkerHost: VOTProxyWorkerHost$v,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$v,
    proxySettings: proxySettings$v,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$v,
    VOTAudioBooster: VOTAudioBooster$v,
    VOTSubtitlesDesign: VOTSubtitlesDesign$v,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$v,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$v,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$v,
    VOTDownloadWithName: VOTDownloadWithName$v,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$v,
    VOTLocaleHash: VOTLocaleHash$v,
    VOTUpdatedAt: VOTUpdatedAt$v,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$v,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$v,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$v,
    VOTNewAudioPlayer: VOTNewAudioPlayer$v,
    VOTUseNewModel: VOTUseNewModel$v,
    TranslationDelayed: TranslationDelayed$v,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$v,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$v,
    VOTBugReport: VOTBugReport$v,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$v,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$v,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$v,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$v,
    VOTTranslatedBy: VOTTranslatedBy$v,
    VOTStreamNotAvailable: VOTStreamNotAvailable$v,
    VOTTranslationTextService: VOTTranslationTextService$v,
    VOTNotAffectToVoice: VOTNotAffectToVoice$v,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$v,
    showVideoVolumeSlider: showVideoVolumeSlider$v,
    hotkeysSettings: hotkeysSettings$v,
    None: None$v,
    VOTUseLivelyVoice: VOTUseLivelyVoice$v,
    miscSettings: miscSettings$v,
    services: services$v,
    aboutExtension: aboutExtension$v,
    appearance: appearance$v,
    buttonPosition: buttonPosition$v,
    position: position$v,
    secs: secs$v,
    autoHideButtonDelay: autoHideButtonDelay$v,
    notFound: notFound$v,
    minButtonPositionContainer: minButtonPositionContainer$v,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$v,
    PressTheKeyCombination: PressTheKeyCombination$v,
    VOTUseAudioDownload: VOTUseAudioDownload$v,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$v,
    VOTAccountRequired: VOTAccountRequired$v,
    VOTMyAccount: VOTMyAccount$v,
    VOTLogin: VOTLogin$v,
    VOTLogout: VOTLogout$v,
    VOTRefresh: VOTRefresh$v,
    VOTYandexToken: VOTYandexToken$v,
    VOTYandexTokenInfo: VOTYandexTokenInfo$v,
    VOTLoginViaToken: VOTLoginViaToken$v,
    subtitlesSmartLayout: subtitlesSmartLayout$v,
    smartDucking: smartDucking$v,
    VOTAutoSubtitles: VOTAutoSubtitles$v,
    VOTSubtitlesFont: VOTSubtitlesFont$v
  };
  const recommended$u = "ಶಿಫಾರಸು ಮಾಡಲಾಗಿದೆ";
  const translateVideo$u = "ವೀಡಿಯೊ ಭಾಷಾಂತರಿಸಿ";
  const disableTranslate$u = "ಆಫ್ ಮಾಡಿ";
  const translationSettings$u = "ಅನುವಾದ ಸೆಟ್ಟಿಂಗ್ಗಳು";
  const subtitlesSettings$u = "ಉಪಶೀರ್ಷಿಕೆಗಳು ಸೆಟ್ಟಿಂಗ್ಗಳು";
  const resetSettings$u = "ಸೆಟ್ಟಿಂಗ್ಗಳನ್ನು ಮರುಹೊಂದಿಸಿ";
  const videoBeingTranslated$u = "ವೀಡಿಯೊ ಅನುವಾದ";
  const videoLanguage$u = "ವೀಡಿಯೊ ಭಾಷೆ";
  const translationLanguage$u = "ಅನುವಾದ ಭಾಷೆ";
  const translationTake$u = "ಅನುವಾದ ತೆಗೆದುಕೊಳ್ಳುತ್ತದೆ";
  const translationTakeMoreThanHour$u = "ಭಾಷಾಂತರವು ಒಂದು ಗಂಟೆಗಿಂತ ಹೆಚ್ಚು ಸಮಯ ತೆಗೆದುಕೊಳ್ಳುತ್ತದೆ.";
  const translationTakeAboutMinute$u = "ಅನುವಾದ ಸುಮಾರು ಒಂದು ನಿಮಿಷ ತೆಗೆದುಕೊಳ್ಳುತ್ತದೆ";
  const translationTakeFewMinutes$u = "ಅನುವಾದ ಕೆಲವು ನಿಮಿಷಗಳನ್ನು ತೆಗೆದುಕೊಳ್ಳುತ್ತದೆ";
  const translationTakeApproximatelyMinutes$u = "ಅನುವಾದವು ಸರಿಸುಮಾರು {0} ನಿಮಿಷಗಳನ್ನು ತೆಗೆದುಕೊಳ್ಳುತ್ತದೆ";
  const translationTakeApproximatelyMinute$u = "ಅನುವಾದವು ಸರಿಸುಮಾರು {0} ನಿಮಿಷಗಳನ್ನು ತೆಗೆದುಕೊಳ್ಳುತ್ತದೆ";
  const requestTranslationFailed$u = "ವೀಡಿಯೊ ಅನುವಾದವನ್ನು ಕೋರಲು ವಿಫಲಗೊಂಡಿದೆ";
  const audioNotReceived$u = "ಆಡಿಯೋ ಲಿಂಕ್ ಬಂದಿಲ್ಲ";
  const audioFormatNotSupported$u = "ಆಡಿಯೋ ಸ್ವರೂಪ ಬೆಂಬಲವಿಲ್ಲ";
  const VOTAutoTranslate$u = "ತೆರೆದ ಮೇಲೆ ಭಾಷಾಂತರಿಸಿ";
  const VOTDontTranslateYourLang$u = "'ನನ್ನ ಭಾಷೆಯಿಂದ ಭಾಷಾಂತರಿಸಬೇಡಿ'";
  const VOTVolume$u = "ವೀಡಿಯೊ ಪರಿಮಾಣ:";
  const VOTVolumeTranslation$u = "ಅನುವಾದ ಸಂಪುಟ:";
  const VOTAutoSetVolume$u = "ವೀಡಿಯೊ ಪರಿಮಾಣವನ್ನು ಕಡಿಮೆ ಮಾಡಿ";
  const VOTShowVideoSlider$u = "ವೀಡಿಯೊ ಪರಿಮಾಣ ಸ್ಲೈಡರ್";
  const VOTSyncVolume$u = "ಲಿಂಕ್ ಅನುವಾದ ಮತ್ತು ವೀಡಿಯೊ ಪರಿಮಾಣ";
  const VOTDisableFromYourLang$u = "ನಿಮ್ಮ ಭಾಷೆಯಲ್ಲಿ ವೀಡಿಯೊದ ಅನುವಾದವನ್ನು ನೀವು ನಿಷ್ಕ್ರಿಯಗೊಳಿಸಿದ್ದೀರಿ";
  const VOTVideoIsTooLong$u = "ವೀಡಿಯೊ ತುಂಬಾ ಉದ್ದವಾಗಿದೆ";
  const VOTNoVideoIDFound$u = "ವೀಡಿಯೊ ID ಕಂಡುಬಂದಿಲ್ಲ";
  const VOTSubtitles$u = "ಉಪಶೀರ್ಷಿಕೆಗಳು";
  const VOTSubtitlesDisabled$u = "ನಿಷ್ಕ್ರಿಯಗೊಳಿಸಲಾಗಿದೆ";
  const VOTSubtitlesMaxLength$u = "ಉಪಶೀರ್ಷಿಕೆಗಳು ಗರಿಷ್ಠ ಉದ್ದ";
  const VOTHighlightWords$u = "ಪದಗಳನ್ನು ಹೈಲೈಟ್ ಮಾಡಿ";
  const VOTTranslatedFrom$u = "ಇಂದ ಅನುವಾದಿಸಲಾಗಿದೆ";
  const VOTAutogenerated$u = "ಆಟೋಜೆನೆರೇಟೆಡ್";
  const VOTSettings$u = "ಮತದಾನ ಸೆಟ್ಟಿಂಗ್ಗಳು";
  const VOTMenuLanguage$u = "ಮೆನು ಭಾಷೆ";
  const VOTAuthors$u = "ಲೇಖಕರು";
  const VOTVersion$u = "ಆವೃತ್ತಿ";
  const VOTLoader$u = "ಲೋಡರ್";
  const VOTBrowser$u = "ಬ್ರೌಸರ್";
  const VOTShowPiPButton$u = "ಪಿಪ್ ಬಟನ್ ತೋರಿಸಿ";
  const langs$u = { "auto": "ಆಟೋ", "af": "ಆಫ್ರಿಕಾನ್ಸ್", "ak": "Akan", "sq": "ಅಲ್ಬೇನಿಯನ್", "am": "ಅಮ್ಹಾರಿಕ್", "ar": "ಅರೇಬಿಕ್", "hy": "ಅರ್ಮೇನಿಯನ್", "as": "ಅಸ್ಸಾಮಿ", "ay": "Aymara", "az": "ಅಜರ್ಬೈಜಾನಿ", "bn": "ಬಾಂಗ್ಲಾ", "eu": "ಬಾಸ್ಕ್", "be": "ಬೆಲರೂಸಿಯನ್", "bho": "ಭೋಜ್ಪುರಿ", "bs": "ಬೋಸ್ನಿಯನ್", "bg": "ಬಲ್ಗೇರಿಯ", "my": "ಬರ್ಮೀಸ್", "ca": "ಕೆಟಲಾನ್", "ceb": "Cebuano", "zh": "ಚೀನೀ", "zh-Hans": "ಚೈನೀಸ್ (ಸರಳೀಕೃತ)", "zh-Hant": "ಚೀನೀ (ಸಾಂಪ್ರದಾಯಿಕ)", "co": "Corsican", "hr": "ಕ್ರೊಯೇಷಿಯಾದ", "cs": "ಜೆಕ್", "da": "ಡ್ಯಾನಿಶ್", "dv": "Divehi", "nl": "ಡಚ್", "en": "ಕನ್ನಡ", "eo": "ಎಸ್ಪೆರಾಂಟೊ", "et": "ಎಸ್ಟೋನಿಯನ್", "ee": "ಈವ್", "fil": "ಫಿಲಿಪಿನೋ", "fi": "ಫಿನ್ನಿಷ್", "fr": "ಫ್ರೆಂಚ್", "gl": "ಗ್ಯಾಲಿಶಿಯನ್", "lg": "ಕನ್ನಡ", "ka": "ಜಾರ್ಜಿಯನ್", "de": "ಜರ್ಮನ್", "el": "ಗ್ರೀಕ್", "gn": "ಗೌರಾನಿ", "gu": "ಗುಜರಾತಿ", "ht": "ಹೈಟಿಯನ್ ಕ್ರಿಯೋಲ್", "ha": "Hausa", "haw": "ಹವಾಯಿಯನ್", "iw": "ಹೀಬ್ರೂ", "hi": "ಹಿಂದಿ", "hmn": "Hmong", "hu": "ಹಂಗೇರಿಯನ್", "is": "ಐಸ್ಲ್ಯಾಂಡ್", "ig": "Igbo", "id": "ಇಂಡೋನೇಷಿಯನ್", "ga": "ಐರಿಷ್", "it": "ಇಟಾಲಿಯನ್", "ja": "ಜಪಾನೀಸ್", "jv": "ಜಾವಾನೀಸ್", "kn": "ಕನ್ನಡ", "kk": "ಕಝಕ್", "km": "ಖಮೇರ್", "rw": "Kinyarwanda", "ko": "ಕೊರಿಯನ್", "kri": "Krio", "ku": "ಕುರ್ದಿಷ್", "ky": "ಕಿರ್ಗಿಜ್", "lo": "ಲಾವೋ", "la": "ಲ್ಯಾಟಿನ್", "lv": "ಲಟ್ವಿಯನ್", "ln": "ಕನ್ನಡ", "lt": "ಲಿಥುವೇನಿಯನ್", "lb": "ಲಕ್ಸೆಂಬರ್ಗ್", "mk": "ಮೆಸಿಡೋನಿಯನ್", "mg": "ಮಲಗಾಸಿ", "ms": "ಮಲಯಾಳಂ", "ml": "ಮಲಯಾಳಂ", "mt": "ಮಾಲ್ಟೀಸ್", "mi": "Māori", "mr": "ಮರಾಠಿ", "mn": "ಮಂಗೋಲಿಯನ್", "ne": "ನೇಪಾಳಿ", "nso": "ಉತ್ತರ ಸೋಥೋ", "no": "ನಾರ್ವೇಜಿಯನ್", "ny": "Nyanja", "or": "ಒಡಿಯಾ", "om": "ಒರೊಮೊ", "ps": "Pashto", "fa": "ಪರ್ಷಿಯನ್", "pl": "ಪೋಲಿಷ್", "pt": "ಪೋರ್ಚುಗೀಸ್", "pa": "ಪಂಜಾಬಿ", "qu": "ಕ್ವೆಚುವಾ", "ro": "ರೊಮೇನಿಯನ್", "ru": "ರಷ್ಯನ್", "sm": "ಸಮೋವನ್", "sa": "ಸಂಸ್ಕೃತ", "gd": "ಸ್ಕಾಟಿಷ್ ಗೇಲಿಕ್", "sr": "ಸರ್ಬಿಯನ್", "sn": "ಶೋನಾ", "sd": "ಸಿಂಧಿ", "si": "ಸಿಂಹಳ", "sk": "ಸ್ಲೋವಾಕ್", "sl": "ಸ್ಲೊವೇನಿಯನ್", "so": "ಸೊಮಾಲಿ", "st": "ದಕ್ಷಿಣ ಸೋಥೋ", "es": "ಸ್ಪ್ಯಾನಿಷ್", "su": "Sundanese", "sw": "ಸ್ವಹಿಲಿ", "sv": "ಸ್ವೀಡಿಷ್", "tg": "ತಾಜಿಕ್", "ta": "ಕನ್ನಡ", "tt": "ಟಾಟರ್", "te": "ಕನ್ನಡ", "th": "ಥಾಯ್", "ti": "Tigrinya", "ts": "Tsonga", "tr": "ಟರ್ಕಿಶ್", "tk": "ತುರ್ಕಮೆನ್", "uk": "ಕನ್ನಡ", "ur": "ಉರ್ದು", "ug": "ಉಯಿಘರ್", "uz": "ಉಜ್ಬೇಕ್", "vi": "ವಿಯೆಟ್ನಾಮೀಸ್", "cy": "ವೆಲ್ಷ್", "fy": "ಪಶ್ಚಿಮ ಫ್ರಿಸಿಯನ್", "xh": "Xhosa", "yi": "Yiddish", "yo": "ಯೊರುಬಾ", "zu": "ಜುಲು" };
  const streamNoConnectionToServer$u = "ಸರ್ವರ್ಗೆ ಸಂಪರ್ಕವಿಲ್ಲ";
  const searchField$u = "ಹುಡುಕಿ...";
  const VOTTranslateAPIErrors$u = "API ನಿಂದ ದೋಷಗಳನ್ನು ಭಾಷಾಂತರಿಸಿ";
  const VOTDetectService$u = "ಭಾಷಾ ಪತ್ತೆ ಸೇವೆ";
  const VOTProxyWorkerHost$u = "ಪ್ರಾಕ್ಸಿ ಕಾರ್ಮಿಕರ ವಿಳಾಸವನ್ನು ನಮೂದಿಸಿ";
  const VOTM3u8ProxyHost$u = "M3u8 ಪ್ರಾಕ್ಸಿ ಕೆಲಸಗಾರನ ವಿಳಾಸವನ್ನು ನಮೂದಿಸಿ";
  const proxySettings$u = "ಪ್ರಾಕ್ಸಿ ಸೆಟ್ಟಿಂಗ್ಗಳು";
  const translationTakeApproximatelyMinute2$u = "ಅನುವಾದವು ಸರಿಸುಮಾರು {0} ನಿಮಿಷಗಳನ್ನು ತೆಗೆದುಕೊಳ್ಳುತ್ತದೆ";
  const VOTAudioBooster$u = "ವಿಸ್ತೃತ ಅನುವಾದ ಪರಿಮಾಣ ಹೆಚ್ಚಳ";
  const VOTSubtitlesDesign$u = "ಉಪಶೀರ್ಷಿಕೆಗಳು ವಿನ್ಯಾಸ";
  const VOTSubtitlesFontSize$u = "ಉಪಶೀರ್ಷಿಕೆಗಳ ಫಾಂಟ್ ಗಾತ್ರ";
  const VOTSubtitlesOpacity$u = "ಉಪಶೀರ್ಷಿಕೆ ಹಿನ್ನೆಲೆ ಪಾರದರ್ಶಕತೆ";
  const VOTSubtitlesDownloadFormat$u = "ಉಪಶೀರ್ಷಿಕೆಗಳನ್ನು ಡೌನ್ಲೋಡ್ ಮಾಡುವ ಸ್ವರೂಪ";
  const VOTDownloadWithName$u = "ವೀಡಿಯೊ ಹೆಸರಿನೊಂದಿಗೆ ಫೈಲ್ಗಳನ್ನು ಡೌನ್ಲೋಡ್ ಮಾಡಿ";
  const VOTUpdateLocaleFiles$u = "ಅಪ್ಡೇಟ್ ಸ್ಥಳೀಕರಣ ಕಡತಗಳನ್ನು";
  const VOTLocaleHash$u = "ಲೊಕೇಲ್ ಹ್ಯಾಶ್";
  const VOTUpdatedAt$u = "ಕ್ಕೆ ನವೀಕರಿಸಲಾಗಿದೆ";
  const VOTNeedWebAudioAPI$u = "ಇದನ್ನು ಸಕ್ರಿಯಗೊಳಿಸಲು, ನೀವು ವೆಬ್ ಆಡಿಯೊ API ಅನ್ನು ಹೊಂದಿರಬೇಕು";
  const VOTMediaCSPEnabledOnSite$u = "ಈ ಸೈಟ್ನಲ್ಲಿ ಮೀಡಿಯಾ CSP ಅನ್ನು ಸಕ್ರಿಯಗೊಳಿಸಲಾಗಿದೆ";
  const VOTOnlyBypassMediaCSP$u = "ಮಾಧ್ಯಮ ಸಿಎಸ್ಪಿ ಬೈಪಾಸ್ ಮಾಡಲು ಮಾತ್ರ ಇದನ್ನು ಬಳಸಿ";
  const VOTNewAudioPlayer$u = "ಹೊಸ ಆಡಿಯೋ ಪ್ಲೇಯರ್ ಬಳಸಿ";
  const VOTUseNewModel$u = "ಕೆಲವು ವೀಡಿಯೊಗಳಿಗಾಗಿ ಯಾಂಡೆಕ್ಸ್ ಧ್ವನಿಗಳ ಪ್ರಾಯೋಗಿಕ ವ್ಯತ್ಯಾಸವನ್ನು ಬಳಸಿ";
  const TranslationDelayed$u = "ಅನುವಾದ ಸ್ವಲ್ಪ ವಿಳಂಬ";
  const VOTTranslationCompletedNotify$u = "{0} ನಲ್ಲಿನ ಅನುವಾದ ಪೂರ್ಣಗೊಂಡಿದೆ!";
  const VOTSendNotifyOnComplete$u = "ವೀಡಿಯೊ ಅನುವಾದಿಸಲಾಗಿದೆ ಎಂದು ಅಧಿಸೂಚನೆಯನ್ನು ಕಳುಹಿಸಿ";
  const VOTBugReport$u = "ದೋಷವನ್ನು ವರದಿ ಮಾಡಿ";
  const VOTTranslateProxyDisabled$u = "ನಿಷ್ಕ್ರಿಯಗೊಳಿಸಲಾಗಿದೆ";
  const VOTTranslateProxyEnabled$u = "ಸಕ್ರಿಯಗೊಳಿಸಲಾಗಿದೆ";
  const VOTTranslateProxyEverything$u = "ಪ್ರಾಕ್ಸಿ ಎಲ್ಲವೂ";
  const VOTTranslateProxyStatus$u = "ಪ್ರಾಕ್ಸಿ ಮೋಡ್";
  const VOTTranslatedBy$u = "ಅನುವಾದ {0}";
  const VOTStreamNotAvailable$u = "ಅನುವಾದ ಸ್ಟ್ರೀಮ್ ಲಭ್ಯವಿಲ್ಲ";
  const VOTTranslationTextService$u = "ಪಠ್ಯ ಅನುವಾದ ಸೇವೆ";
  const VOTNotAffectToVoice$u = "Voice over ನಲ್ಲಿ ಪಠ್ಯದ ಅನುವಾದದ ಮೇಲೆ ಪರಿಣಾಮ ಬೀರುವುದಿಲ್ಲ";
  const DontTranslateSelectedLanguages$u = "'ಆಯ್ದ ಭಾಷೆಗಳಿಂದ ಭಾಷಾಂತರ ಮಾಡಬೇಡಿ'";
  const showVideoVolumeSlider$u = "ವೀಡಿಯೊ ಪರಿಮಾಣ ಸ್ಲೈಡರ್ ಪ್ರದರ್ಶಿಸಿ";
  const hotkeysSettings$u = "ಹಾಟ್ಕೀ ಸೆಟ್ಟಿಂಗ್ಗಳು";
  const None$u = "ಯಾವುದೂ ಇಲ್ಲ";
  const VOTUseLivelyVoice$u = "ಉತ್ಸಾಹಭರಿತ ಧ್ವನಿಗಳನ್ನು ಬಳಸಿ. ಸ್ಪೀಕರ್ಗಳು ಸ್ಥಳೀಯ ರಷ್ಯನ್ನರಂತೆ ಧ್ವನಿಸುತ್ತದೆ.";
  const miscSettings$u = "Misc ಸೆಟ್ಟಿಂಗ್ಗಳು";
  const services$u = { "yandexbrowser": "Yandex ಬ್ರೌಸರ್", "msedge": "ಮೈಕ್ರೋಸಾಫ್ಟ್ ಎಡ್ಜ್", "rust-server": "ರಸ್ಟ್ ಸರ್ವರ್" };
  const aboutExtension$u = "ವಿಸ್ತರಣೆ ಬಗ್ಗೆ";
  const appearance$u = "ಗೋಚರತೆ";
  const buttonPosition$u = "ಆಟಗಾರ ಬಟನ್ ಸ್ಥಾನ";
  const position$u = { "left": "ಎಡ", "right": "ಬಲ", "top": "ಟಾಪ್", "default": "ಪೂರ್ವನಿಯೋಜಿತ" };
  const secs$u = "ಸೆಕೆಂಡುಗಳು";
  const autoHideButtonDelay$u = "ಅನುವಾದ ಬಟನ್ ಮರೆಮಾಚುವ ಮೊದಲು ವಿಳಂಬ";
  const notFound$u = "ಕಂಡುಬಂದಿಲ್ಲ";
  const minButtonPositionContainer$u = "ಬಟನ್ ಸ್ಥಾನವು 600 ಪಿಕ್ಸೆಲ್ಗಳಿಗಿಂತ ದೊಡ್ಡದಾದ ಆಟಗಾರರಲ್ಲಿ ಮಾತ್ರ ಬದಲಾಗುತ್ತದೆ.";
  const VOTTranslateProxyStatusDefault$u = "ನಿಮ್ಮ ದೇಶದಲ್ಲಿ ಪ್ರಾಕ್ಸಿಂಗ್ ಅನ್ನು ಸಂಪೂರ್ಣವಾಗಿ ನಿಷ್ಕ್ರಿಯಗೊಳಿಸುವುದರಿಂದ ವಿಸ್ತರಣೆಯನ್ನು ಮುರಿಯಬಹುದು";
  const PressTheKeyCombination$u = "ಕೀ ಸಂಯೋಜನೆಯನ್ನು ಒತ್ತಿರಿ...";
  const VOTUseAudioDownload$u = "ಆಡಿಯೋ ಡೌನ್ಲೋಡ್ ಬಳಸಿ";
  const VOTUseAudioDownloadWarning$u = "ಆಡಿಯೊ ಡೌನ್ಲೋಡ್ಗಳನ್ನು ನಿಷ್ಕ್ರಿಯಗೊಳಿಸುವುದರಿಂದ ವಿಸ್ತರಣೆಯ ಕಾರ್ಯಚಟುವಟಿಕೆಯ ಮೇಲೆ ಪರಿಣಾಮ ಬೀರಬಹುದು";
  const VOTAccountRequired$u = "ಈ ವೈಶಿಷ್ಟ್ಯವನ್ನು ಬಳಸಲು ನೀವು ಲಾಗ್ ಇನ್ ಮಾಡಬೇಕಾಗುತ್ತದೆ";
  const VOTMyAccount$u = "ನನ್ನ ಖಾತೆ";
  const VOTLogin$u = "ಲಾಗಿನ್ ಮಾಡಿ";
  const VOTLogout$u = "ಲಾಗ್ಔಟ್";
  const VOTRefresh$u = "ರಿಫ್ರೆಶ್";
  const VOTYandexToken$u = "Yandex OAuth ಟೋಕನ್ ಅನ್ನು ನಮೂದಿಸಿ";
  const VOTYandexTokenInfo$u = "ಈ ಕ್ಷೇತ್ರದಲ್ಲಿ ನೀವು ಖಾತೆ ಟೋಕನ್ ಅನ್ನು ಹಸ್ತಚಾಲಿತವಾಗಿ ಹೊಂದಿಸಬಹುದು. ಅನುವಾದ ವಿನಂತಿಯನ್ನು ಕಳುಹಿಸುವ ಮೊದಲು ನಾವು ಅದರ ಸಿಂಧುತ್ವವನ್ನು ಪರಿಶೀಲಿಸುವುದಿಲ್ಲ ಎಂಬುದನ್ನು ದಯವಿಟ್ಟು ಗಮನಿಸಿ";
  const VOTLoginViaToken$u = "ಟೋಕನ್ ಮೂಲಕ ಲಾಗಿನ್ ಮಾಡಿ";
  const subtitlesSmartLayout$u = "ಸ್ಮಾರ್ಟ್ ಉಪಶೀರ್ಷಿಕೆ ಲೇಔಟ್";
  const smartDucking$u = "ಹೊಂದಾಣಿಕೆಯ ಪರಿಮಾಣ";
  const VOTAutoSubtitles$u = "ತೆರೆಯ ಮೇಲೆ ಉಪಶೀರ್ಷಿಕೆಗಳು";
  const VOTSubtitlesFont$u = "ಉಪಶೀರ್ಷಿಕೆ ಫಾಂಟ್";
  const __vite_glob_0_31 = {
    recommended: recommended$u,
    translateVideo: translateVideo$u,
    disableTranslate: disableTranslate$u,
    translationSettings: translationSettings$u,
    subtitlesSettings: subtitlesSettings$u,
    resetSettings: resetSettings$u,
    videoBeingTranslated: videoBeingTranslated$u,
    videoLanguage: videoLanguage$u,
    translationLanguage: translationLanguage$u,
    translationTake: translationTake$u,
    translationTakeMoreThanHour: translationTakeMoreThanHour$u,
    translationTakeAboutMinute: translationTakeAboutMinute$u,
    translationTakeFewMinutes: translationTakeFewMinutes$u,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$u,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$u,
    requestTranslationFailed: requestTranslationFailed$u,
    audioNotReceived: audioNotReceived$u,
    audioFormatNotSupported: audioFormatNotSupported$u,
    VOTAutoTranslate: VOTAutoTranslate$u,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$u,
    VOTVolume: VOTVolume$u,
    VOTVolumeTranslation: VOTVolumeTranslation$u,
    VOTAutoSetVolume: VOTAutoSetVolume$u,
    VOTShowVideoSlider: VOTShowVideoSlider$u,
    VOTSyncVolume: VOTSyncVolume$u,
    VOTDisableFromYourLang: VOTDisableFromYourLang$u,
    VOTVideoIsTooLong: VOTVideoIsTooLong$u,
    VOTNoVideoIDFound: VOTNoVideoIDFound$u,
    VOTSubtitles: VOTSubtitles$u,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$u,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$u,
    VOTHighlightWords: VOTHighlightWords$u,
    VOTTranslatedFrom: VOTTranslatedFrom$u,
    VOTAutogenerated: VOTAutogenerated$u,
    VOTSettings: VOTSettings$u,
    VOTMenuLanguage: VOTMenuLanguage$u,
    VOTAuthors: VOTAuthors$u,
    VOTVersion: VOTVersion$u,
    VOTLoader: VOTLoader$u,
    VOTBrowser: VOTBrowser$u,
    VOTShowPiPButton: VOTShowPiPButton$u,
    langs: langs$u,
    streamNoConnectionToServer: streamNoConnectionToServer$u,
    searchField: searchField$u,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$u,
    VOTDetectService: VOTDetectService$u,
    VOTProxyWorkerHost: VOTProxyWorkerHost$u,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$u,
    proxySettings: proxySettings$u,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$u,
    VOTAudioBooster: VOTAudioBooster$u,
    VOTSubtitlesDesign: VOTSubtitlesDesign$u,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$u,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$u,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$u,
    VOTDownloadWithName: VOTDownloadWithName$u,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$u,
    VOTLocaleHash: VOTLocaleHash$u,
    VOTUpdatedAt: VOTUpdatedAt$u,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$u,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$u,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$u,
    VOTNewAudioPlayer: VOTNewAudioPlayer$u,
    VOTUseNewModel: VOTUseNewModel$u,
    TranslationDelayed: TranslationDelayed$u,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$u,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$u,
    VOTBugReport: VOTBugReport$u,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$u,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$u,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$u,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$u,
    VOTTranslatedBy: VOTTranslatedBy$u,
    VOTStreamNotAvailable: VOTStreamNotAvailable$u,
    VOTTranslationTextService: VOTTranslationTextService$u,
    VOTNotAffectToVoice: VOTNotAffectToVoice$u,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$u,
    showVideoVolumeSlider: showVideoVolumeSlider$u,
    hotkeysSettings: hotkeysSettings$u,
    None: None$u,
    VOTUseLivelyVoice: VOTUseLivelyVoice$u,
    miscSettings: miscSettings$u,
    services: services$u,
    aboutExtension: aboutExtension$u,
    appearance: appearance$u,
    buttonPosition: buttonPosition$u,
    position: position$u,
    secs: secs$u,
    autoHideButtonDelay: autoHideButtonDelay$u,
    notFound: notFound$u,
    minButtonPositionContainer: minButtonPositionContainer$u,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$u,
    PressTheKeyCombination: PressTheKeyCombination$u,
    VOTUseAudioDownload: VOTUseAudioDownload$u,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$u,
    VOTAccountRequired: VOTAccountRequired$u,
    VOTMyAccount: VOTMyAccount$u,
    VOTLogin: VOTLogin$u,
    VOTLogout: VOTLogout$u,
    VOTRefresh: VOTRefresh$u,
    VOTYandexToken: VOTYandexToken$u,
    VOTYandexTokenInfo: VOTYandexTokenInfo$u,
    VOTLoginViaToken: VOTLoginViaToken$u,
    subtitlesSmartLayout: subtitlesSmartLayout$u,
    smartDucking: smartDucking$u,
    VOTAutoSubtitles: VOTAutoSubtitles$u,
    VOTSubtitlesFont: VOTSubtitlesFont$u
  };
  const recommended$t = "추천";
  const translateVideo$t = "비디오 번역";
  const disableTranslate$t = "끄기";
  const translationSettings$t = "번역 설정";
  const subtitlesSettings$t = "자막 설정";
  const resetSettings$t = "설정 초기화";
  const videoBeingTranslated$t = "비디오가 번역되고 있습니다";
  const videoLanguage$t = "비디오 언어";
  const translationLanguage$t = "번역 언어";
  const translationTake$t = "번역에 걸리는 시간";
  const translationTakeMoreThanHour$t = "번역에 1시간 이상 걸립니다";
  const translationTakeAboutMinute$t = "번역에 약 1분 걸립니다";
  const translationTakeFewMinutes$t = "번역에 몇 분 걸립니다";
  const translationTakeApproximatelyMinutes$t = "번역에 약 {0}분 걸립니다";
  const translationTakeApproximatelyMinute$t = "번역에 약 {0}분 걸립니다";
  const requestTranslationFailed$t = "비디오 번역 요청 실패";
  const audioNotReceived$t = "오디오 링크를 받지 못했습니다";
  const audioFormatNotSupported$t = "오디오 형식이 지원되지 않습니다";
  const VOTAutoTranslate$t = "열 때 번역하기";
  const VOTDontTranslateYourLang$t = "내 언어에서 번역하지 않기";
  const VOTVolume$t = "비디오 볼륨:";
  const VOTVolumeTranslation$t = "번역 볼륨:";
  const VOTAutoSetVolume$t = "비디오 볼륨 줄이기";
  const VOTShowVideoSlider$t = "비디오 볼륨 슬라이더";
  const VOTSyncVolume$t = "번역과 비디오 볼륨 연동하기";
  const VOTDisableFromYourLang$t = "내 언어로 된 비디오의 번역을 비활성화했습니다";
  const VOTVideoIsTooLong$t = "비디오가 너무 깁니다";
  const VOTNoVideoIDFound$t = "비디오 ID를 찾을 수 없습니다";
  const VOTSubtitles$t = "자막";
  const VOTSubtitlesDisabled$t = "장애가 있는";
  const VOTSubtitlesMaxLength$t = "자막 최대 길이";
  const VOTHighlightWords$t = "강조 단어";
  const VOTTranslatedFrom$t = "에서 번역";
  const VOTAutogenerated$t = "자동 생성";
  const VOTSettings$t = "VOT 설정";
  const VOTMenuLanguage$t = "메뉴 언어";
  const VOTAuthors$t = "저자";
  const VOTVersion$t = "버전";
  const VOTLoader$t = "짐을 싣는 사람";
  const VOTBrowser$t = "브라우저";
  const VOTShowPiPButton$t = "PiP 버튼 표시";
  const langs$t = { "auto": "자동으로", "lg": "간다어", "gl": "갈리시아어", "gn": "과라니어", "gu": "구자라트어", "el": "그리스어", "st": "남부 소토어", "ny": "냔자어", "nl": "네덜란드어", "ne": "네팔어", "no": "노르웨이어", "da": "덴마크어", "de": "독일어", "dv": "디베히어", "lo": "라오어", "lv": "라트비아어", "la": "라틴어", "ru": "러시아어", "ro": "루마니아어", "lb": "룩셈부르크어", "rw": "르완다어", "lt": "리투아니아어", "ln": "링갈라어", "mr": "마라티어", "mi": "마오리어", "mk": "마케도니아어", "mg": "말라가시어", "ml": "말라얄람어", "ms": "말레이어", "mt": "몰타어", "mn": "몽골어", "eu": "바스크어", "my": "버마어", "vi": "베트남어", "be": "벨라루스어", "bn": "벵골어", "bs": "보스니아어", "nso": "북부 소토어", "bg": "불가리아어", "sm": "사모아어", "sa": "산스크리트어", "fy": "서부 프리지아어", "sr": "세르비아어", "ceb": "세부아노어", "so": "소말리아어", "sn": "쇼나어", "su": "순다어", "sw": "스와힐리어", "sv": "스웨덴어", "gd": "스코틀랜드 게일어", "es": "스페인어", "sk": "슬로바키아어", "sl": "슬로베니아어", "sd": "신디어", "si": "싱할라어", "ar": "아랍어", "hy": "아르메니아어", "as": "아삼어", "ay": "아이마라어", "is": "아이슬란드어", "ht": "아이티어", "ga": "아일랜드어", "az": "아제르바이잔어", "ak": "아칸어", "af": "아프리칸스어", "sq": "알바니아어", "am": "암하라어", "et": "에스토니아어", "eo": "에스페란토어", "ee": "에웨어", "en": "영어", "om": "오로모어", "or": "오리야어", "yo": "요루바어", "ur": "우르두어", "uz": "우즈베크어", "uk": "우크라이나어", "cy": "웨일스어", "ug": "위구르어", "ig": "이그보어", "yi": "이디시어", "it": "이탈리아어", "id": "인도네시아어", "ja": "일본어", "jv": "자바어", "ka": "조지아어", "zu": "줄루어", "zh": "중국어", "zh-Hans": "중국어(간체)", "zh-Hant": "중국어(번체)", "cs": "체코어", "ts": "총가어", "kk": "카자흐어", "ca": "카탈로니아어", "kn": "칸나다어", "qu": "케추아어", "co": "코르시카어", "xh": "코사어", "ku": "쿠르드어", "hr": "크로아티아어", "km": "크메르어", "ky": "키르기스어", "ta": "타밀어", "tg": "타지크어", "tt": "타타르어", "th": "태국어", "tr": "터키어", "te": "텔루구어", "tk": "투르크멘어", "ti": "티그리냐어", "ps": "파슈토어", "pa": "펀잡어", "fa": "페르시아어", "pt": "포르투갈어", "pl": "폴란드어", "fr": "프랑스어", "fi": "핀란드어", "fil": "필리핀어", "haw": "하와이어", "ha": "하우사어", "ko": "한국어", "hu": "헝가리어", "bho": "호즈푸리어", "hmn": "히몸어", "iw": "히브리어", "hi": "힌디어", "kri": "Kri" };
  const streamNoConnectionToServer$t = "서버에 연결이 없습니다";
  const searchField$t = "검색...";
  const VOTTranslateAPIErrors$t = "번역 오류";
  const VOTDetectService$t = "언어 탐지 서비스";
  const VOTProxyWorkerHost$t = "프록시 작업자 주소를 입력합니다";
  const VOTM3u8ProxyHost$t = "프록시 작업자의 주소를 입력합니다";
  const proxySettings$t = "프록시 설정";
  const translationTakeApproximatelyMinute2$t = "번역은 약{0}분이 소요됩니다";
  const VOTAudioBooster$t = "확장 번역 볼륨 증가";
  const VOTSubtitlesDesign$t = "자막 디자인";
  const VOTSubtitlesFontSize$t = "자막의 글꼴 크기";
  const VOTSubtitlesOpacity$t = "자막 배경의 투명도";
  const VOTSubtitlesDownloadFormat$t = "자막 다운로드 형식";
  const VOTDownloadWithName$t = "비디오 이름을 가진 파일 다운로드";
  const VOTUpdateLocaleFiles$t = "현지화 파일 업데이트";
  const VOTLocaleHash$t = "로캘 해시";
  const VOTUpdatedAt$t = "에 업데이트 됨";
  const VOTNeedWebAudioAPI$t = "이 기능을 사용하려면 웹 오디오가 있어야 합니다.";
  const VOTMediaCSPEnabledOnSite$t = "이 사이트에서 사용할 수 있습니다";
  const VOTOnlyBypassMediaCSP$t = "미디어 우회 용으로만 사용";
  const VOTNewAudioPlayer$t = "새 오디오 플레이어 사용";
  const VOTUseNewModel$t = "일부 동영상에는 얀덱스 목소리의 실험적 변형을 사용합니다";
  const TranslationDelayed$t = "번역이 약간 지연되었습니다.";
  const VOTTranslationCompletedNotify$t = "{0}에 대한 번역이 완료되었습니다!";
  const VOTSendNotifyOnComplete$t = "동영상이 번역되었다는 알림 보내기";
  const VOTBugReport$t = "버그 신고";
  const VOTTranslateProxyDisabled$t = "사용 안 함";
  const VOTTranslateProxyEnabled$t = "사용";
  const VOTTranslateProxyEverything$t = "프록시 모든 것";
  const VOTTranslateProxyStatus$t = "프록시 모드";
  const VOTTranslatedBy$t = "번역:{0}";
  const VOTStreamNotAvailable$t = "번역 스트림을 사용할 수 없습니다";
  const VOTTranslationTextService$t = "텍스트 번역 서비스";
  const VOTNotAffectToVoice$t = "음성 해설 텍스트의 번역에 영향을 미치지 않습니다";
  const DontTranslateSelectedLanguages$t = "선택한 언어로 번역하지 마십시오";
  const showVideoVolumeSlider$t = "비디오 볼륨 슬라이더 표시";
  const hotkeysSettings$t = "단축키 설정";
  const None$t = "없지";
  const VOTUseLivelyVoice$t = "활기찬 목소리를 사용하십시오. 스피커는 네이티브 러시아인 같은 소리.";
  const miscSettings$t = "기타 설정";
  const services$t = { "yandexbrowser": "브라우저", "msedge": "마이크로소프트 엣지", "rust-server": "녹 서버" };
  const aboutExtension$t = "확장 정보";
  const appearance$t = "외관";
  const buttonPosition$t = "플레이어의 버튼 위치";
  const position$t = { "left": "왼쪽", "right": "오른쪽", "top": "상단", "default": "기본값" };
  const secs$t = "초";
  const autoHideButtonDelay$t = "번역 버튼을 숨기기 전에 지연";
  const notFound$t = "찾을 수 없음";
  const minButtonPositionContainer$t = "버튼 위치는 600 픽셀보다 큰 플레이어에서만 변경됩니다.";
  const VOTTranslateProxyStatusDefault$t = "귀하의 국가에서 프록시를 완전히 비활성화하면 확장 프로그램이 중단 될 수 있습니다";
  const PressTheKeyCombination$t = "키 조합을 누릅니다...";
  const VOTUseAudioDownload$t = "오디오 다운로드 사용";
  const VOTUseAudioDownloadWarning$t = "오디오 다운로드를 비활성화하면 확장 프로그램의 기능에 영향을 줄 수 있습니다";
  const VOTAccountRequired$t = "이 기능을 사용하려면 로그인해야 합니다";
  const VOTMyAccount$t = "내 계정";
  const VOTLogin$t = "로그인";
  const VOTLogout$t = "로그아웃";
  const VOTRefresh$t = "새로 고침";
  const VOTYandexToken$t = "토큰을 입력합니다.";
  const VOTYandexTokenInfo$t = "이 필드에서 계정 토큰을 수동으로 설정할 수 있습니다. 번역 요청을 보내기 전에 유효성을 확인하지 않습니다";
  const VOTLoginViaToken$t = "토큰을 통해 로그인";
  const subtitlesSmartLayout$t = "스마트 자막 레이아웃";
  const smartDucking$t = "적응 형 볼륨";
  const VOTAutoSubtitles$t = "자막 열기";
  const VOTSubtitlesFont$t = "자막 글꼴";
  const __vite_glob_0_32 = {
    recommended: recommended$t,
    translateVideo: translateVideo$t,
    disableTranslate: disableTranslate$t,
    translationSettings: translationSettings$t,
    subtitlesSettings: subtitlesSettings$t,
    resetSettings: resetSettings$t,
    videoBeingTranslated: videoBeingTranslated$t,
    videoLanguage: videoLanguage$t,
    translationLanguage: translationLanguage$t,
    translationTake: translationTake$t,
    translationTakeMoreThanHour: translationTakeMoreThanHour$t,
    translationTakeAboutMinute: translationTakeAboutMinute$t,
    translationTakeFewMinutes: translationTakeFewMinutes$t,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$t,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$t,
    requestTranslationFailed: requestTranslationFailed$t,
    audioNotReceived: audioNotReceived$t,
    audioFormatNotSupported: audioFormatNotSupported$t,
    VOTAutoTranslate: VOTAutoTranslate$t,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$t,
    VOTVolume: VOTVolume$t,
    VOTVolumeTranslation: VOTVolumeTranslation$t,
    VOTAutoSetVolume: VOTAutoSetVolume$t,
    VOTShowVideoSlider: VOTShowVideoSlider$t,
    VOTSyncVolume: VOTSyncVolume$t,
    VOTDisableFromYourLang: VOTDisableFromYourLang$t,
    VOTVideoIsTooLong: VOTVideoIsTooLong$t,
    VOTNoVideoIDFound: VOTNoVideoIDFound$t,
    VOTSubtitles: VOTSubtitles$t,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$t,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$t,
    VOTHighlightWords: VOTHighlightWords$t,
    VOTTranslatedFrom: VOTTranslatedFrom$t,
    VOTAutogenerated: VOTAutogenerated$t,
    VOTSettings: VOTSettings$t,
    VOTMenuLanguage: VOTMenuLanguage$t,
    VOTAuthors: VOTAuthors$t,
    VOTVersion: VOTVersion$t,
    VOTLoader: VOTLoader$t,
    VOTBrowser: VOTBrowser$t,
    VOTShowPiPButton: VOTShowPiPButton$t,
    langs: langs$t,
    streamNoConnectionToServer: streamNoConnectionToServer$t,
    searchField: searchField$t,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$t,
    VOTDetectService: VOTDetectService$t,
    VOTProxyWorkerHost: VOTProxyWorkerHost$t,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$t,
    proxySettings: proxySettings$t,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$t,
    VOTAudioBooster: VOTAudioBooster$t,
    VOTSubtitlesDesign: VOTSubtitlesDesign$t,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$t,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$t,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$t,
    VOTDownloadWithName: VOTDownloadWithName$t,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$t,
    VOTLocaleHash: VOTLocaleHash$t,
    VOTUpdatedAt: VOTUpdatedAt$t,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$t,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$t,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$t,
    VOTNewAudioPlayer: VOTNewAudioPlayer$t,
    VOTUseNewModel: VOTUseNewModel$t,
    TranslationDelayed: TranslationDelayed$t,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$t,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$t,
    VOTBugReport: VOTBugReport$t,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$t,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$t,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$t,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$t,
    VOTTranslatedBy: VOTTranslatedBy$t,
    VOTStreamNotAvailable: VOTStreamNotAvailable$t,
    VOTTranslationTextService: VOTTranslationTextService$t,
    VOTNotAffectToVoice: VOTNotAffectToVoice$t,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$t,
    showVideoVolumeSlider: showVideoVolumeSlider$t,
    hotkeysSettings: hotkeysSettings$t,
    None: None$t,
    VOTUseLivelyVoice: VOTUseLivelyVoice$t,
    miscSettings: miscSettings$t,
    services: services$t,
    aboutExtension: aboutExtension$t,
    appearance: appearance$t,
    buttonPosition: buttonPosition$t,
    position: position$t,
    secs: secs$t,
    autoHideButtonDelay: autoHideButtonDelay$t,
    notFound: notFound$t,
    minButtonPositionContainer: minButtonPositionContainer$t,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$t,
    PressTheKeyCombination: PressTheKeyCombination$t,
    VOTUseAudioDownload: VOTUseAudioDownload$t,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$t,
    VOTAccountRequired: VOTAccountRequired$t,
    VOTMyAccount: VOTMyAccount$t,
    VOTLogin: VOTLogin$t,
    VOTLogout: VOTLogout$t,
    VOTRefresh: VOTRefresh$t,
    VOTYandexToken: VOTYandexToken$t,
    VOTYandexTokenInfo: VOTYandexTokenInfo$t,
    VOTLoginViaToken: VOTLoginViaToken$t,
    subtitlesSmartLayout: subtitlesSmartLayout$t,
    smartDucking: smartDucking$t,
    VOTAutoSubtitles: VOTAutoSubtitles$t,
    VOTSubtitlesFont: VOTSubtitlesFont$t
  };
  const recommended$s = "ແນະນໍາ";
  const translateVideo$s = "ແປວິດີໂອ";
  const disableTranslate$s = "ປິດ";
  const translationSettings$s = "ການຕັ້ງຄ່າການແປພາສາ";
  const subtitlesSettings$s = "ການຕັ້ງຄ່າຄໍາບັນຍາຍ";
  const resetSettings$s = "ຕັ້ງຄ່າການຕັ້ງຄ່າໃໝ່";
  const videoBeingTranslated$s = "ວິດີໂອໄດ້ຖືກແປ";
  const videoLanguage$s = "ພາສາວິດີໂອ";
  const translationLanguage$s = "ການແປພາສາພາສາ";
  const translationTake$s = "ການແປພາສາຈະໃຊ້ເວລາ";
  const translationTakeMoreThanHour$s = "ການແປພາສາຈະໃຊ້ເວລາຫຼາຍກວ່າຫນຶ່ງຊົ່ວໂມງ";
  const translationTakeAboutMinute$s = "ການແປພາສາຈະໃຊ້ເວລາປະມານຫນຶ່ງນາທີ";
  const translationTakeFewMinutes$s = "ການແປພາສາຈະໃຊ້ເວລາສອງສາມນາທີ";
  const translationTakeApproximatelyMinutes$s = "ການແປພາສາຈະໃຊ້ເວລາປະມານ{0}ນາທີ";
  const translationTakeApproximatelyMinute$s = "ການແປພາສາຈະໃຊ້ເວລາປະມານ{0}ນາທີ";
  const requestTranslationFailed$s = "ລົ້ມເຫລວໃນການຮ້ອງຂໍການແປພາສາວິດີໂອ";
  const audioNotReceived$s = "ການເຊື່ອມຕໍ່ສຽງບໍ່ໄດ້ຮັບ";
  const audioFormatNotSupported$s = "ຮູບແບບສຽງບໍ່ໄດ້ຮັບການສະໜັບສະໜູນ";
  const VOTAutoTranslate$s = "ແປພາສາເປີດ";
  const VOTDontTranslateYourLang$s = "ຢ່າແປຈາກພາສາຂອງຂ້ອຍ";
  const VOTVolume$s = "ປະລິມານວິດີໂອ:";
  const VOTVolumeTranslation$s = "ປະລິມານການແປພາສາ:";
  const VOTAutoSetVolume$s = "ຫຼຸດຜ່ອນປະລິມານວິດີໂອໃຫ້";
  const VOTShowVideoSlider$s = "ເລື່ອນປະລິມານວິດີໂອ";
  const VOTSyncVolume$s = "ການແປພາສາເຊື່ອມຕໍ່ແລະປະລິມານວິດີໂອ";
  const VOTDisableFromYourLang$s = "ທ່ານໄດ້ປິດການແປພາສາຂອງວິດີໂອໃນພາສາຂອງທ່ານ";
  const VOTVideoIsTooLong$s = "ວິດີໂອຍາວເກີນໄປ";
  const VOTNoVideoIDFound$s = "ບໍ່ພົບ ID ວິດີໂອ";
  const VOTSubtitles$s = "ຄໍາບັນຍາຍ";
  const VOTSubtitlesDisabled$s = "ພິການ";
  const VOTSubtitlesMaxLength$s = "ຄໍາບັນຍາຍຄວາມຍາວສູງສຸດ";
  const VOTHighlightWords$s = "ເນັ້ນຄຳເວົ້າ";
  const VOTTranslatedFrom$s = "ແປຈາກ";
  const VOTAutogenerated$s = "ອັດຕະໂນມັດ";
  const VOTSettings$s = "ການຕັ້ງຄ່າ VOT";
  const VOTMenuLanguage$s = "ພາສາເມນູ";
  const VOTAuthors$s = "ຜູ້ຂຽນ";
  const VOTVersion$s = "ລຸ້ນ";
  const VOTLoader$s = "Loader";
  const VOTBrowser$s = "ຕົວທ່ອງເວັບ";
  const VOTShowPiPButton$s = "ສະແດງປຸ່ມ PiP";
  const langs$s = { "auto": "ອັດຕະໂນມັດ", "af": "ອາຟຣິກາ", "ak": "Akan", "sq": "ອານບານີ", "am": "Amharic", "ar": "ພາສາອາຣັບ", "hy": "ອາເມເນຍ", "as": "ພາສາສະວາຮິລີ", "ay": "ອາຣ໌ແອັຟເອ-ລາວ", "az": "ອາເຊີໄບຈານ", "bn": "ບັງກະລາເທດ", "eu": "Basque", "be": "ເບລາຣູດ", "bho": "Bhojpuri", "bs": "ບອສເນຍ", "bg": "ບຸນກາຣີ", "my": "ພະມ້າ", "ca": "ກາຕາ", "ceb": "Cebuano", "zh": "ຈີນ", "zh-Hans": "ພາສາຈີນ(Simplified)", "zh-Hant": "ພາສາຈີນ(ແບບດັ້ງເດີມ)", "co": "ພາສາລາວ", "hr": "ໂຄຣເອເຊຍ", "cs": "ພາສາເຊັກໂກ", "da": "ເດັນມາກ", "dv": "Divehi", "nl": "ໂຮນລັງ", "en": "ພາສາອັງກິດ", "eo": "ພາສາເອສເປຣັງໂຕ", "et": "ເອສໂຕເນຍ", "ee": "Ewe", "fil": "ຟີລິບປິນ", "fi": "ຟິນແລນ", "fr": "ພາສາຝຣັ່ງ", "gl": "ກາລິດ", "lg": "Ganda", "ka": "ຈໍເຈຍ", "de": "ເຢຍລະມັນ", "el": "ກເຣັກ", "gn": "ພາສາລາວ", "gu": "ກົວລາລຳເປີ", "ht": "ເຮຕີ Creole", "ha": "Hausa", "haw": "ຮາວາຍ", "iw": "ຍິວ", "hi": "ຮິນດູ", "hmn": "ມົ້ງ", "hu": "ຮັງກາຣີ", "is": "ປະເທດໄອສແລນ", "ig": "ອີເມວຂອງທ່ານ", "id": "ພາສາອິນໂດເນເຊຍ", "ga": "ໄອແລນ", "it": "ອິຕາລີ", "ja": "ພາສາຍີ່ປຸ່ນ", "jv": "ພາສາຈາວາ", "kn": "Kannada", "kk": "ຄາຊັກສະຖານ", "km": "ພາສາຂະແມ", "rw": "Kinyarwanda", "ko": "ພາສາເກົາຫຼີ", "kri": "Krio", "ku": "ພາສາເຄີດ", "ky": "Kyrgyz", "lo": "ລາວ", "la": "ມາດຕະຖານ", "lv": "ລັດເວຍ", "ln": "ພາສາລາວ", "lt": "ປະເທດລິທົວເນຍ", "lb": "ລັກເຊມເບີກ", "mk": "ມາເຊໂດນີ", "mg": "ມາລາກາຊີ", "ms": "ມາເລເຊຍ", "ml": "ມາເລເຊຍ", "mt": "Maltese", "mi": "ພາສາລາວ", "mr": "ມາຣາທອນ", "mn": "ມົງໂກລີ", "ne": "ເນປານ", "nso": "ພາກເຫນືອ Sotho", "no": "ນໍເວ", "ny": "Nyanja", "or": "Odia", "om": "ພາສາລາວ", "ps": "Pashto", "fa": "ເປີເຊຍ", "pl": "ໂປໂລຍ", "pt": "ພາສາປອກຕຸຍການ", "pa": "Punjabi", "qu": "Quechua", "ro": "ໂຣມາເນຍ", "ru": "ພາສາລັດເຊຍ", "sm": "ຊາມູເອນ", "sa": "ພາສາສັນສະກິດ", "gd": "ສະກັອດແລນ Gaelic", "sr": "ເຊີເບຍ", "sn": "Shona", "sd": "Sindhi", "si": "Sinhala", "sk": "ສະໂລວາເກຍ", "sl": "ສະໂລເວເນຍ", "so": "ໂຊມາເລຍ", "st": "ພາກໃຕ້ Sotho", "es": "ແອສປາໂຍນ", "su": "Sundanese", "sw": "ພາສາສະວາຮິລີ", "sv": "ຊູແອັດ", "tg": "Tajikistan", "ta": "ທະມິນ", "tt": "ທາຕາ", "te": "ໂທລະສັບ", "th": "ໄທ", "ti": "Tigrinya", "ts": "ແຂວງຊຽງຂວາງ", "tr": "ພາສາຕຸລະກີ", "tk": "ຕວກກີ", "uk": "ອູແກຣນ", "ur": "Urdu", "ug": "Uyghur", "uz": "Uzbek", "vi": "ຫວຽດນາມ", "cy": "ແວວ", "fy": "Frisian ຕາເວັນຕົກ", "xh": "Xhosa", "yi": "Yiddish", "yo": "Yoruba", "zu": "ຊູລູ" };
  const streamNoConnectionToServer$s = "ບໍ່ມີການເຊື່ອມຕໍ່ກັບ server";
  const searchField$s = "ຄົ້ນຫາ...";
  const VOTTranslateAPIErrors$s = "ແປຂໍ້ຜິດພາດຈາກ API";
  const VOTDetectService$s = "ບໍລິການກວດຈັບພາສາ";
  const VOTProxyWorkerHost$s = "ກະລຸນາໃສ່ທີ່ຢູ່ພະນັກງານຕົວແທນ";
  const VOTM3u8ProxyHost$s = "ໃສ່ທີ່ຢູ່ຂອງພະນັກງານຕົວແທນ m3u8";
  const proxySettings$s = "ການຕັ້ງຄ່າຕົວແທນ";
  const translationTakeApproximatelyMinute2$s = "ການແປພາສາຈະໃຊ້ເວລາປະມານ{0}ນາທີ";
  const VOTAudioBooster$s = "ການແປພາສາຂະຫຍາຍປະລິມານເພີ່ມຂຶ້ນ";
  const VOTSubtitlesDesign$s = "ການອອກແບບຄໍາບັນຍາຍ";
  const VOTSubtitlesFontSize$s = "ຂະໜາດຕົວອັກສອນຂອງຄຳບັນຍາຍ";
  const VOTSubtitlesOpacity$s = "ຄວາມໂປ່ງໃສຂອງຄວາມເປັນມາ subtitle ໄດ້";
  const VOTSubtitlesDownloadFormat$s = "ຮູບແບບສໍາລັບການດາວໂຫຼດຄໍາບັນຍາຍ";
  const VOTDownloadWithName$s = "ດາວນ໌ໂຫລດໄຟລ໌ທີ່ມີຊື່ວິດີໂອໄດ້";
  const VOTUpdateLocaleFiles$s = "ອັບເດດໄຟລ໌ທ້ອງຖິ່ນ";
  const VOTLocaleHash$s = "Hash ທ້ອງຖິ່ນ";
  const VOTUpdatedAt$s = "ອັບເດດເວລາ";
  const VOTNeedWebAudioAPI$s = "ເພື່ອເປີດໃຊ້ສິ່ງນີ້,ທ່ານຕ້ອງມີ Web Audio API";
  const VOTMediaCSPEnabledOnSite$s = "Media CSP ຖືກເປີດໃຊ້ໃນເວັບໄຊທ໌ນີ້";
  const VOTOnlyBypassMediaCSP$s = "ໃຊ້ມັນພຽງແຕ່ສໍາລັບການ bypassing ສື່ CSP";
  const VOTNewAudioPlayer$s = "ໃຊ້ເຄື່ອງຫຼີ້ນສຽງໃໝ່";
  const VOTUseNewModel$s = "ໃຊ້ການປ່ຽນແປງການທົດລອງຂອງ yandex voices ສໍາລັບບາງວິດີໂອ";
  const TranslationDelayed$s = "ການແປພາສາແມ່ນຊັກຊ້າເລັກນ້ອຍ";
  const VOTTranslationCompletedNotify$s = "ການແປພາສາໃນ{0}ໄດ້ສໍາເລັດແລ້ວ!";
  const VOTSendNotifyOnComplete$s = "ສົ່ງແຈ້ງການວ່າວິດີໂອໄດ້ຮັບການແປ";
  const VOTBugReport$s = "ລາຍງານຂໍ້ບົກພ່ອງ";
  const VOTTranslateProxyDisabled$s = "ພິການ";
  const VOTTranslateProxyEnabled$s = "ເປີດໃຊ້ງານ";
  const VOTTranslateProxyEverything$s = "ຕົວແທນທຸກສິ່ງທຸກຢ່າງ";
  const VOTTranslateProxyStatus$s = "ຮູບແບບຕົວແທນ";
  const VOTTranslatedBy$s = "ແປໂດຍ{0}";
  const VOTStreamNotAvailable$s = "ການແປພາສານ້ໍາແມ່ນບໍ່ມີ";
  const VOTTranslationTextService$s = "ບໍລິການແປຂໍ້ຄວາມ";
  const VOTNotAffectToVoice$s = "ບໍ່ມີຜົນກະທົບການແປພາສາຂອງຂໍ້ຄວາມໃນສຽງໃນໄລຍະ";
  const DontTranslateSelectedLanguages$s = "ບໍ່ໄດ້ແປພາສາຈາກພາສາທີ່ເລືອກ";
  const showVideoVolumeSlider$s = "ສະແດງແຖບເລື່ອນປະລິມານວິດີໂອ";
  const hotkeysSettings$s = "ການຕັ້ງຄ່າ Hotkeys";
  const None$s = "ບໍ່ມີ";
  const VOTUseLivelyVoice$s = "ໃຊ້ສຽງທີ່ມີຊີວິດຊີວາ. ລໍາໂພງສຽງຄ້າຍຄືຊາວລັດເຊຍພື້ນເມືອງ.";
  const miscSettings$s = "ການຕັ້ງຄ່າ Misc";
  const services$s = { "yandexbrowser": "ຕົວທ່ອງເວັບ Yandex", "msedge": "Microsoft Edge", "rust-server": "ເຄື່ອງແມ່ຂ່າຍ Rust" };
  const aboutExtension$s = "ກ່ຽວກັບການຂະຫຍາຍ";
  const appearance$s = "ຮູບລັກສະນະ";
  const buttonPosition$s = "ຕໍາແຫນ່ງປຸ່ມໃນເຄື່ອງຫຼີ້ນ";
  const position$s = { "left": "ຊ້າຍ", "right": "ຖືກຕ້ອງ", "top": "ທາງເທີງ", "default": "ຄ່າເລີ່ມຕົ້ນ" };
  const secs$s = "secs";
  const autoHideButtonDelay$s = "ການຊັກຊ້າກ່ອນທີ່ຈະເຊື່ອງປຸ່ມແປພາສາ";
  const notFound$s = "ບໍ່ພົບ";
  const minButtonPositionContainer$s = "ຕໍາແຫນ່ງປຸ່ມພຽງແຕ່ປ່ຽນແປງໃນເຄື່ອງຫຼີ້ນທີ່ໃຫຍ່ກວ່າ 600 ພິກະເຊນ.";
  const VOTTranslateProxyStatusDefault$s = "ການປິດການ proxying ຢ່າງສົມບູນໃນປະເທດຂອງທ່ານອາດຈະທໍາລາຍການຂະຫຍາຍ";
  const PressTheKeyCombination$s = "ກົດປຸ່ມການປະສົມປະສານ...";
  const VOTUseAudioDownload$s = "ໃຊ້ການດາວໂຫລດສຽງ";
  const VOTUseAudioDownloadWarning$s = "ການປິດການດາວໂຫຼດສຽງອາດຈະມີຜົນກະທົບການທໍາງານຂອງການຂະຫຍາຍ";
  const VOTAccountRequired$s = "ທ່ານຈໍາເປັນຕ້ອງເຂົ້າໃນການນໍາໃຊ້ຄຸນນະສົມບັດນີ້";
  const VOTMyAccount$s = "ບັນຊີຂອງຂ້ອຍ";
  const VOTLogin$s = "ເຂົ້າສູ່ລະບົບ";
  const VOTLogout$s = "ເຂົ້າສູ່ລະບົບ";
  const VOTRefresh$s = "ສົດຊື່ນ";
  const VOTYandexToken$s = "ກະລຸນາໃສ່ສັນຍາລັກ Yandex OAuth";
  const VOTYandexTokenInfo$s = "ທ່ານສາມາດກໍານົດຄູ່ມືບັນຊີ token ໃນພາກສະຫນາມນີ້. ກະລຸນາສັງເກດວ່າພວກເຮົາບໍ່ໄດ້ກວດສອບຄວາມຖືກຕ້ອງຂອງຕົນກ່ອນທີ່ຈະສົ່ງຄໍາຮ້ອງຂໍການແປພາສາ";
  const VOTLoginViaToken$s = "ເຂົ້າສູ່ລະບົບຜ່ານ token";
  const subtitlesSmartLayout$s = "ຮູບແບບຄໍາບັນຍາຍອັດສະລິຍະ";
  const smartDucking$s = "ປະລິມານການປັບຕົວ";
  const VOTAutoSubtitles$s = "ຄໍາບັນຍາຍເປີດ";
  const VOTSubtitlesFont$s = "ຕົວອັກສອນບັນຍາຍ";
  const __vite_glob_0_33 = {
    recommended: recommended$s,
    translateVideo: translateVideo$s,
    disableTranslate: disableTranslate$s,
    translationSettings: translationSettings$s,
    subtitlesSettings: subtitlesSettings$s,
    resetSettings: resetSettings$s,
    videoBeingTranslated: videoBeingTranslated$s,
    videoLanguage: videoLanguage$s,
    translationLanguage: translationLanguage$s,
    translationTake: translationTake$s,
    translationTakeMoreThanHour: translationTakeMoreThanHour$s,
    translationTakeAboutMinute: translationTakeAboutMinute$s,
    translationTakeFewMinutes: translationTakeFewMinutes$s,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$s,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$s,
    requestTranslationFailed: requestTranslationFailed$s,
    audioNotReceived: audioNotReceived$s,
    audioFormatNotSupported: audioFormatNotSupported$s,
    VOTAutoTranslate: VOTAutoTranslate$s,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$s,
    VOTVolume: VOTVolume$s,
    VOTVolumeTranslation: VOTVolumeTranslation$s,
    VOTAutoSetVolume: VOTAutoSetVolume$s,
    VOTShowVideoSlider: VOTShowVideoSlider$s,
    VOTSyncVolume: VOTSyncVolume$s,
    VOTDisableFromYourLang: VOTDisableFromYourLang$s,
    VOTVideoIsTooLong: VOTVideoIsTooLong$s,
    VOTNoVideoIDFound: VOTNoVideoIDFound$s,
    VOTSubtitles: VOTSubtitles$s,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$s,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$s,
    VOTHighlightWords: VOTHighlightWords$s,
    VOTTranslatedFrom: VOTTranslatedFrom$s,
    VOTAutogenerated: VOTAutogenerated$s,
    VOTSettings: VOTSettings$s,
    VOTMenuLanguage: VOTMenuLanguage$s,
    VOTAuthors: VOTAuthors$s,
    VOTVersion: VOTVersion$s,
    VOTLoader: VOTLoader$s,
    VOTBrowser: VOTBrowser$s,
    VOTShowPiPButton: VOTShowPiPButton$s,
    langs: langs$s,
    streamNoConnectionToServer: streamNoConnectionToServer$s,
    searchField: searchField$s,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$s,
    VOTDetectService: VOTDetectService$s,
    VOTProxyWorkerHost: VOTProxyWorkerHost$s,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$s,
    proxySettings: proxySettings$s,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$s,
    VOTAudioBooster: VOTAudioBooster$s,
    VOTSubtitlesDesign: VOTSubtitlesDesign$s,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$s,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$s,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$s,
    VOTDownloadWithName: VOTDownloadWithName$s,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$s,
    VOTLocaleHash: VOTLocaleHash$s,
    VOTUpdatedAt: VOTUpdatedAt$s,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$s,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$s,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$s,
    VOTNewAudioPlayer: VOTNewAudioPlayer$s,
    VOTUseNewModel: VOTUseNewModel$s,
    TranslationDelayed: TranslationDelayed$s,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$s,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$s,
    VOTBugReport: VOTBugReport$s,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$s,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$s,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$s,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$s,
    VOTTranslatedBy: VOTTranslatedBy$s,
    VOTStreamNotAvailable: VOTStreamNotAvailable$s,
    VOTTranslationTextService: VOTTranslationTextService$s,
    VOTNotAffectToVoice: VOTNotAffectToVoice$s,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$s,
    showVideoVolumeSlider: showVideoVolumeSlider$s,
    hotkeysSettings: hotkeysSettings$s,
    None: None$s,
    VOTUseLivelyVoice: VOTUseLivelyVoice$s,
    miscSettings: miscSettings$s,
    services: services$s,
    aboutExtension: aboutExtension$s,
    appearance: appearance$s,
    buttonPosition: buttonPosition$s,
    position: position$s,
    secs: secs$s,
    autoHideButtonDelay: autoHideButtonDelay$s,
    notFound: notFound$s,
    minButtonPositionContainer: minButtonPositionContainer$s,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$s,
    PressTheKeyCombination: PressTheKeyCombination$s,
    VOTUseAudioDownload: VOTUseAudioDownload$s,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$s,
    VOTAccountRequired: VOTAccountRequired$s,
    VOTMyAccount: VOTMyAccount$s,
    VOTLogin: VOTLogin$s,
    VOTLogout: VOTLogout$s,
    VOTRefresh: VOTRefresh$s,
    VOTYandexToken: VOTYandexToken$s,
    VOTYandexTokenInfo: VOTYandexTokenInfo$s,
    VOTLoginViaToken: VOTLoginViaToken$s,
    subtitlesSmartLayout: subtitlesSmartLayout$s,
    smartDucking: smartDucking$s,
    VOTAutoSubtitles: VOTAutoSubtitles$s,
    VOTSubtitlesFont: VOTSubtitlesFont$s
  };
  const recommended$r = "препорачано";
  const translateVideo$r = "Преведи видео";
  const disableTranslate$r = "Исклучи";
  const translationSettings$r = "Поставки за превод";
  const subtitlesSettings$r = "Поставки за преводи";
  const resetSettings$r = "Поставки за ресетирање";
  const videoBeingTranslated$r = "Видеото се преведува";
  const videoLanguage$r = "Видео јазик";
  const translationLanguage$r = "Преведувачки јазик";
  const translationTake$r = "Преводот ќе трае";
  const translationTakeMoreThanHour$r = "Преводот ќе трае повеќе од еден час";
  const translationTakeAboutMinute$r = "Преводот ќе трае околу една минута";
  const translationTakeFewMinutes$r = "Преводот ќе трае неколку минути";
  const translationTakeApproximatelyMinutes$r = "Преводот ќе трае приближно {0} минути";
  const translationTakeApproximatelyMinute$r = "Преводот ќе трае приближно {0} минути";
  const requestTranslationFailed$r = "Не успеа да побара видео превод";
  const audioNotReceived$r = "Аудио врската не е примена";
  const audioFormatNotSupported$r = "Аудио форматот не е поддржан";
  const VOTAutoTranslate$r = "Преведи на отворено";
  const VOTDontTranslateYourLang$r = "Не преведувајте од мојот јазик";
  const VOTVolume$r = "Видео волумен:";
  const VOTVolumeTranslation$r = "Волумен на превод:";
  const VOTAutoSetVolume$r = "Намалете ја јачината на видеото на";
  const VOTShowVideoSlider$r = "Лизгач за јачина на звук на видео";
  const VOTSyncVolume$r = "Линк превод и видео волумен";
  const VOTDisableFromYourLang$r = "Го оневозможивте преводот на видеото на вашиот јазик";
  const VOTVideoIsTooLong$r = "Видеото е предолго";
  const VOTNoVideoIDFound$r = "Не е пронајдено ВИДЕО";
  const VOTSubtitles$r = "Преводи";
  const VOTSubtitlesDisabled$r = "Оневозможено";
  const VOTSubtitlesMaxLength$r = "Преводи максимална должина";
  const VOTHighlightWords$r = "Означете зборови";
  const VOTTranslatedFrom$r = "превод од";
  const VOTAutogenerated$r = "автогенерирани";
  const VOTSettings$r = "Поставки ЗА ГЛАСАЊЕ";
  const VOTMenuLanguage$r = "Јазик на менито";
  const VOTAuthors$r = "Автори";
  const VOTVersion$r = "Верзија";
  const VOTLoader$r = "Натоварувач";
  const VOTBrowser$r = "Прелистувач";
  const VOTShowPiPButton$r = "Прикажи пип копче";
  const langs$r = { "auto": "Авто", "af": "Африканс", "ak": "Акан", "sq": "Албански", "am": "Амхарски", "ar": "Arпски", "hy": "Armенски", "as": "Асамски", "ay": "Ајмара", "az": "Азербејџански", "bn": "Бангла", "eu": "Баскија", "be": "Белоруски", "bho": "Бојпури", "bs": "Босански", "bg": "Бугарски", "my": "Бурмански", "ca": "Каталонски", "ceb": "Кебуано", "zh": "Кинески", "zh-Hans": "Кинески (Поедноставен)", "zh-Hant": "Кинески (Традиционален)", "co": "Корзикански", "hr": "Хрватски", "cs": "Чешка", "da": "Дански", "dv": "Дивехи", "nl": "Холандски", "en": "Англиски", "eo": "Есперанто", "et": "Estонски", "ee": "Ове", "fil": "Филипински", "fi": "Фински", "fr": "Француски", "gl": "Галициски", "lg": "Ганда", "ka": "Грузиски", "de": "Германски", "el": "Грчки", "gn": "Гуарани", "gu": "Гуџарати", "ht": "Хаити Креолски", "ha": "Хауса", "haw": "Хавајски", "iw": "Хебрејски", "hi": "Хинди", "hmn": "Хмонг", "hu": "Унгарски", "is": "Исландски", "ig": "Игбо", "id": "Индонезиски", "ga": "Ирски", "it": "Италијански", "ja": "Јапонски", "jv": "Јавански", "kn": "Канада", "kk": "Казахстански", "km": "Кмер", "rw": "Кинјарванда", "ko": "Корејски", "kri": "Крио", "ku": "Курдски", "ky": "Киргиски", "lo": "Лао", "la": "Латински", "lv": "Латвиски", "ln": "Лингала", "lt": "Литвански", "lb": "Луксембуршки", "mk": "Македонски", "mg": "Малгашки", "ms": "Малајски", "ml": "Малајалам", "mt": "Малтешки", "mi": "Маори", "mr": "Марати", "mn": "Монголски", "ne": "Непалски", "nso": "Северен Сото", "no": "Норвешки", "ny": "Нјања", "or": "Одија", "om": "Оромо", "ps": "Пашту", "fa": "Персиски", "pl": "Полски", "pt": "Португалски", "pa": "Пенџаби", "qu": "Кечуански", "ro": "Романски", "ru": "Руски", "sm": "Самоа", "sa": "Санскрит", "gd": "Шкотски Галски", "sr": "Српски", "sn": "Шона", "sd": "Синди", "si": "Синхала", "sk": "Словачки", "sl": "Словенечки", "so": "Сомалиски", "st": "Јужен Сото", "es": "Шпански", "su": "Сундски", "sw": "Свахили", "sv": "Шведски", "tg": "Таџикистански", "ta": "Тамилски", "tt": "Татарски", "te": "Телугу", "th": "Тајландски", "ti": "Тигриња", "ts": "Цонга", "tr": "Турски", "tk": "Туркменски", "uk": "Украински", "ur": "Урду", "ug": "Ујгур", "uz": "Узбекистански", "vi": "Виетнамски", "cy": "Велшкиот", "fy": "Западнофризиски", "xh": "Џоса", "yi": "Јидиш", "yo": "Јоруба", "zu": "Зулу" };
  const streamNoConnectionToServer$r = "Нема врска со серверот";
  const searchField$r = "Барај...";
  const VOTTranslateAPIErrors$r = "Преведи грешки ОД АПИ";
  const VOTDetectService$r = "Служба за откривање јазик";
  const VOTProxyWorkerHost$r = "Внесете ја адресата на прокси-работникот";
  const VOTM3u8ProxyHost$r = "Внесете ја адресата на прокси-работникот м3у8";
  const proxySettings$r = "Поставки За Прокси";
  const translationTakeApproximatelyMinute2$r = "Преводот ќе трае приближно {0} минути";
  const VOTAudioBooster$r = "Зголемен обем на превод";
  const VOTSubtitlesDesign$r = "Преводи дизајн";
  const VOTSubtitlesFontSize$r = "Големина на фонт на преводи";
  const VOTSubtitlesOpacity$r = "Транспарентност на позадината на титлот";
  const VOTSubtitlesDownloadFormat$r = "Форматот за преземање преводи";
  const VOTDownloadWithName$r = "Преземете датотеки со името на видеото";
  const VOTUpdateLocaleFiles$r = "Ажурирајте ги датотеките за локализација";
  const VOTLocaleHash$r = "Локал хаш";
  const VOTUpdatedAt$r = "Ажурирано во";
  const VOTNeedWebAudioAPI$r = "За да го овозможите ОВА, мора да имате ВЕБ АУДИО АПИ";
  const VOTMediaCSPEnabledOnSite$r = "Медиумите CSP се овозможени на оваа страница";
  const VOTOnlyBypassMediaCSP$r = "Користете го само За Заобиколување На МЕДИУМИТЕ ЦСП";
  const VOTNewAudioPlayer$r = "Користете го новиот аудио плеер";
  const VOTUseNewModel$r = "Користете експериментална варијација На гласови На Јандекс за некои видеа";
  const TranslationDelayed$r = "Преводот е малку одложен";
  const VOTTranslationCompletedNotify$r = "Преводот на {0} е завршен!";
  const VOTSendNotifyOnComplete$r = "Испратете известување дека видеото е преведено";
  const VOTBugReport$r = "Пријави грешка";
  const VOTTranslateProxyDisabled$r = "Оневозможено";
  const VOTTranslateProxyEnabled$r = "Овозможено";
  const VOTTranslateProxyEverything$r = "Прокси се";
  const VOTTranslateProxyStatus$r = "Режим на прокси";
  const VOTTranslatedBy$r = "Превод од {0}";
  const VOTStreamNotAvailable$r = "Преведи поток не е достапен";
  const VOTTranslationTextService$r = "Услуга за превод на текст";
  const VOTNotAffectToVoice$r = "Не влијае на преводот на текстот во гласот";
  const DontTranslateSelectedLanguages$r = "Не преведувајте од избрани јазици";
  const showVideoVolumeSlider$r = "Прикажете го лизгачот за јачина на звук на видеото";
  const hotkeysSettings$r = "Поставки за клучни копчиња";
  const None$r = "Никој";
  const VOTUseLivelyVoice$r = "Користете живи гласови. Звучниците звучат како мајчин Руси.";
  const miscSettings$r = "Погрешни поставки";
  const services$r = { "yandexbrowser": "Јандекс Прелистувач", "msedge": "Мајкрософт Еџ", "rust-server": "Рѓа Сервер" };
  const aboutExtension$r = "За продолжување";
  const appearance$r = "Изглед";
  const buttonPosition$r = "Позиција на копчето во плеерот";
  const position$r = { "left": "Лево", "right": "Така", "top": "Врвот", "default": "Стандардно" };
  const secs$r = "секс";
  const autoHideButtonDelay$r = "Одложување пред да го скриете копчето преведи";
  const notFound$r = "не е пронајден";
  const minButtonPositionContainer$r = "Позицијата на копчето се менува само кај играчите поголеми од 600 пиксели.";
  const VOTTranslateProxyStatusDefault$r = "Целосно оневозможување на прокси во вашата земја може да го скрши продолжувањето";
  const PressTheKeyCombination$r = "Притиснете ја комбинацијата на копчиња...";
  const VOTUseAudioDownload$r = "Користете аудио преземање";
  const VOTUseAudioDownloadWarning$r = "Оневозможувањето на аудио преземањата може да влијае на функционалноста на наставката";
  const VOTAccountRequired$r = "Треба да се најавите за да ја користите оваа функција";
  const VOTMyAccount$r = "Мојата сметка";
  const VOTLogin$r = "Најава";
  const VOTLogout$r = "Одјавување";
  const VOTRefresh$r = "Освежи";
  const VOTYandexToken$r = "Внесете Го Токенот Yandex OAuth";
  const VOTYandexTokenInfo$r = "Можете рачно да го поставите токенот на сметката во оваа област. Ве молиме имајте предвид дека не ја проверуваме неговата валидност пред да испратиме барање за превод";
  const VOTLoginViaToken$r = "Најава преку токен";
  const subtitlesSmartLayout$r = "Паметен поднаслов распоред";
  const smartDucking$r = "Адаптивен волумен";
  const VOTAutoSubtitles$r = "Преводи на отворено";
  const VOTSubtitlesFont$r = "Титл фонт";
  const __vite_glob_0_34 = {
    recommended: recommended$r,
    translateVideo: translateVideo$r,
    disableTranslate: disableTranslate$r,
    translationSettings: translationSettings$r,
    subtitlesSettings: subtitlesSettings$r,
    resetSettings: resetSettings$r,
    videoBeingTranslated: videoBeingTranslated$r,
    videoLanguage: videoLanguage$r,
    translationLanguage: translationLanguage$r,
    translationTake: translationTake$r,
    translationTakeMoreThanHour: translationTakeMoreThanHour$r,
    translationTakeAboutMinute: translationTakeAboutMinute$r,
    translationTakeFewMinutes: translationTakeFewMinutes$r,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$r,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$r,
    requestTranslationFailed: requestTranslationFailed$r,
    audioNotReceived: audioNotReceived$r,
    audioFormatNotSupported: audioFormatNotSupported$r,
    VOTAutoTranslate: VOTAutoTranslate$r,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$r,
    VOTVolume: VOTVolume$r,
    VOTVolumeTranslation: VOTVolumeTranslation$r,
    VOTAutoSetVolume: VOTAutoSetVolume$r,
    VOTShowVideoSlider: VOTShowVideoSlider$r,
    VOTSyncVolume: VOTSyncVolume$r,
    VOTDisableFromYourLang: VOTDisableFromYourLang$r,
    VOTVideoIsTooLong: VOTVideoIsTooLong$r,
    VOTNoVideoIDFound: VOTNoVideoIDFound$r,
    VOTSubtitles: VOTSubtitles$r,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$r,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$r,
    VOTHighlightWords: VOTHighlightWords$r,
    VOTTranslatedFrom: VOTTranslatedFrom$r,
    VOTAutogenerated: VOTAutogenerated$r,
    VOTSettings: VOTSettings$r,
    VOTMenuLanguage: VOTMenuLanguage$r,
    VOTAuthors: VOTAuthors$r,
    VOTVersion: VOTVersion$r,
    VOTLoader: VOTLoader$r,
    VOTBrowser: VOTBrowser$r,
    VOTShowPiPButton: VOTShowPiPButton$r,
    langs: langs$r,
    streamNoConnectionToServer: streamNoConnectionToServer$r,
    searchField: searchField$r,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$r,
    VOTDetectService: VOTDetectService$r,
    VOTProxyWorkerHost: VOTProxyWorkerHost$r,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$r,
    proxySettings: proxySettings$r,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$r,
    VOTAudioBooster: VOTAudioBooster$r,
    VOTSubtitlesDesign: VOTSubtitlesDesign$r,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$r,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$r,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$r,
    VOTDownloadWithName: VOTDownloadWithName$r,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$r,
    VOTLocaleHash: VOTLocaleHash$r,
    VOTUpdatedAt: VOTUpdatedAt$r,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$r,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$r,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$r,
    VOTNewAudioPlayer: VOTNewAudioPlayer$r,
    VOTUseNewModel: VOTUseNewModel$r,
    TranslationDelayed: TranslationDelayed$r,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$r,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$r,
    VOTBugReport: VOTBugReport$r,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$r,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$r,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$r,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$r,
    VOTTranslatedBy: VOTTranslatedBy$r,
    VOTStreamNotAvailable: VOTStreamNotAvailable$r,
    VOTTranslationTextService: VOTTranslationTextService$r,
    VOTNotAffectToVoice: VOTNotAffectToVoice$r,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$r,
    showVideoVolumeSlider: showVideoVolumeSlider$r,
    hotkeysSettings: hotkeysSettings$r,
    None: None$r,
    VOTUseLivelyVoice: VOTUseLivelyVoice$r,
    miscSettings: miscSettings$r,
    services: services$r,
    aboutExtension: aboutExtension$r,
    appearance: appearance$r,
    buttonPosition: buttonPosition$r,
    position: position$r,
    secs: secs$r,
    autoHideButtonDelay: autoHideButtonDelay$r,
    notFound: notFound$r,
    minButtonPositionContainer: minButtonPositionContainer$r,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$r,
    PressTheKeyCombination: PressTheKeyCombination$r,
    VOTUseAudioDownload: VOTUseAudioDownload$r,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$r,
    VOTAccountRequired: VOTAccountRequired$r,
    VOTMyAccount: VOTMyAccount$r,
    VOTLogin: VOTLogin$r,
    VOTLogout: VOTLogout$r,
    VOTRefresh: VOTRefresh$r,
    VOTYandexToken: VOTYandexToken$r,
    VOTYandexTokenInfo: VOTYandexTokenInfo$r,
    VOTLoginViaToken: VOTLoginViaToken$r,
    subtitlesSmartLayout: subtitlesSmartLayout$r,
    smartDucking: smartDucking$r,
    VOTAutoSubtitles: VOTAutoSubtitles$r,
    VOTSubtitlesFont: VOTSubtitlesFont$r
  };
  const recommended$q = "ശുപാർശ";
  const translateVideo$q = "വീഡിയോ പരിഭാഷപ്പെടുത്തുക";
  const disableTranslate$q = "ഓഫ് ചെയ്യുക";
  const translationSettings$q = "വിവർത്തന ക്രമീകരണങ്ങൾ";
  const subtitlesSettings$q = "സബ്ടൈറ്റിലുകൾ ക്രമീകരണങ്ങൾ";
  const resetSettings$q = "ക്രമീകരണങ്ങൾ പുനഃസജ്ജമാക്കുക";
  const videoBeingTranslated$q = "വീഡിയോ പരിഭാഷപ്പെടുത്തുന്നു";
  const videoLanguage$q = "വീഡിയോ ഭാഷ";
  const translationLanguage$q = "വിവർത്തന ഭാഷ";
  const translationTake$q = "വിവർത്തനം ചെയ്യും";
  const translationTakeMoreThanHour$q = "വിവർത്തനം ഒരു മണിക്കൂറിലധികം എടുക്കും.";
  const translationTakeAboutMinute$q = "വിവർത്തനം ഒരു മിനിറ്റ് എടുക്കും.";
  const translationTakeFewMinutes$q = "വിവർത്തനം ഏതാനും മിനിറ്റ് എടുക്കും.";
  const translationTakeApproximatelyMinutes$q = "പരിഭാഷ ഏകദേശം {0} മിനിറ്റ് എടുക്കും";
  const translationTakeApproximatelyMinute$q = "പരിഭാഷ ഏകദേശം {0} മിനിറ്റ് എടുക്കും";
  const requestTranslationFailed$q = "വീഡിയോ പരിഭാഷ ആവശ്യപ്പെടുന്നതില് പരാജയം";
  const audioNotReceived$q = "ഓഡിയോ ലിങ്ക് ലഭ്യമല്ല";
  const audioFormatNotSupported$q = "ഓഡിയോ ഫോർമാറ്റ് പിന്തുണയ്ക്കുന്നില്ല";
  const VOTAutoTranslate$q = "ഓപ്പണിൽ വിവർത്തനം ചെയ്യുക";
  const VOTDontTranslateYourLang$q = "എന്റെ ഭാഷയിൽ നിന്ന് വിവർത്തനം ചെയ്യരുത്";
  const VOTVolume$q = "വീഡിയോ വോള്യം:";
  const VOTVolumeTranslation$q = "പരിഭാഷാ വോള്യം:";
  const VOTAutoSetVolume$q = "വീഡിയോ വോളിയം കുറയ്ക്കുക";
  const VOTShowVideoSlider$q = "വീഡിയോ വോള്യം സ്ലൈഡർ";
  const VOTSyncVolume$q = "ലിങ്ക് പരിഭാഷയും വീഡിയോ വോളിയവും";
  const VOTDisableFromYourLang$q = "നിങ്ങളുടെ ഭാഷയിലെ വീഡിയോയുടെ വിവർത്തനം നിങ്ങൾ അപ്രാപ്തമാക്കി";
  const VOTVideoIsTooLong$q = "വീഡിയോ വളരെ ദൈർഘ്യമേറിയതാണ്";
  const VOTNoVideoIDFound$q = "വീഡിയോ ഐഡി ലഭ്യമല്ല";
  const VOTSubtitles$q = "സബ്ടൈറ്റിലുകൾ";
  const VOTSubtitlesDisabled$q = "Disabled";
  const VOTSubtitlesMaxLength$q = "സബ്ടൈറ്റിലുകൾ പരമാവധി നീളം";
  const VOTHighlightWords$q = "വാക്കുകള് ഹൈലൈറ്റ് ചെയ്യുക";
  const VOTTranslatedFrom$q = "വിവർത്തനം ചെയ്തത്";
  const VOTAutogenerated$q = "ഓട്ടോമേറ്റഡ്";
  const VOTSettings$q = "വോട്ട് ക്രമീകരണങ്ങൾ";
  const VOTMenuLanguage$q = "മെനു ഭാഷ";
  const VOTAuthors$q = "രചയിതാക്കൾ";
  const VOTVersion$q = "പതിപ്പ്";
  const VOTLoader$q = "ലോഡർ";
  const VOTBrowser$q = "ബ്രൗസര്";
  const VOTShowPiPButton$q = "പിപ് ബട്ടണ് കാണിക്കുക";
  const langs$q = { "auto": "ഓട്ടോ", "af": "ആഫ്രിക്കൻ", "ak": "അഹമ്മദാബാദ്", "sq": "അൽബേനിയൻ", "am": "അംഹാരിക്", "ar": "അറബി", "hy": "അർമേനിയൻ", "as": "അസമീസ്", "ay": "ആമസോണ്", "az": "അസർബൈജാനി", "bn": "ബംഗ്ലാ", "eu": "ബാസ്ക്", "be": "ബെലാറസിയൻ", "bho": "ഭോജ്പുരി", "bs": "ബോസ്നിയൻ", "bg": "ബൾഗേറിയ", "my": "ബർമീസ്", "ca": "കാറ്റലോണിയ", "ceb": "സെബുവാനോ", "zh": "ചൈനീസ്", "zh-Hans": "ചൈനീസ് (ലളിതമാക്കിയത്)", "zh-Hant": "ചൈനീസ് (പരമ്പരാഗത)", "co": "കോർസിക്കൻ", "hr": "ക്രൊയേഷ്യൻ", "cs": "ചെക്ക്", "da": "ഡാനിഷ്", "dv": "ദിവേഹി", "nl": "ഡച്ച്", "en": "ഇംഗ്ലീഷ്", "eo": "എസ്പെരാന്റോ", "et": "എസ്റ്റോണിയൻ", "ee": "ഈവേ", "fil": "ഫിലിപ്പിനോ", "fi": "ഫിന്നിഷ്", "fr": "ഫ്രഞ്ച്", "gl": "ഗാലിഷ്യൻ", "lg": "ഗാന്ധിജി", "ka": "ജോർജിയൻ", "de": "ജർമ്മൻ", "el": "ഗ്രീക്ക്", "gn": "ഗുരാനി", "gu": "ഗുജറാത്തി", "ht": "ഹെയ്തിൻ ക്രിയോൾ", "ha": "ഹൗസ", "haw": "ഹവായിയൻ", "iw": "ഹീബ്രു", "hi": "ഹിന്ദി", "hmn": "ഹ്മൊന്ഗ്", "hu": "ഹംഗേറിയൻ", "is": "ഐസ്ലാൻഡിക്", "ig": "ഇഗ്ബോ", "id": "ഇന്തോനേഷ്യൻ", "ga": "ഐറിഷ്", "it": "ഇറ്റാലിയൻ", "ja": "ജാപ്പനീസ്", "jv": "ജാവനീസ്", "kn": "കന്നഡ", "kk": "കസാഖ്", "km": "ഖമർ", "rw": "കിംയര്വാന്ഡ", "ko": "കൊറിയൻ", "kri": "ക്രിയോ", "ku": "കുർദിഷ്", "ky": "കിർഗിസ്", "lo": "ലാവോ", "la": "ലാറ്റിൻ", "lv": "ലാത്വിയൻ", "ln": "മലയാളം", "lt": "ലിത്വാനിയൻ", "lb": "ലക്സംബർഗ്", "mk": "മാസിഡോണിയൻ", "mg": "മാലഗാസി", "ms": "മലയാളം", "ml": "മലയാളം", "mt": "മാൾട്ടീസ്", "mi": "മാവോറി", "mr": "മറാത്തി", "mn": "മംഗോളിയൻ", "ne": "നേപ്പാളി", "nso": "വടക്കൻ സോത്തോ", "no": "നോർവീ", "ny": "നയൻജ", "or": "ഒഡിയ", "om": "ഒറോമോ", "ps": "പഷ്തോ", "fa": "പേർഷ്യൻ", "pl": "പോളിഷ്", "pt": "പോർച്ചുഗീസ്", "pa": "പഞ്ചാബി", "qu": "ക്യൂച്ചുവ", "ro": "റൊമാനിയൻ", "ru": "റഷ്യൻ", "sm": "സമോവൻ", "sa": "സംസ്കൃതം", "gd": "സ്കോട്ടിഷ് ഗാലിക്", "sr": "സെർബിയൻ", "sn": "ഷോണ", "sd": "സിന്ധി", "si": "സിംഹള", "sk": "സ്ലോവാക്", "sl": "സ്ലോവേനിയൻ", "so": "സൊമാലി", "st": "സതേൺ സോത്തോ", "es": "സ്പാനിഷ്", "su": "സുന്ദനീസ്", "sw": "സ്വാഹിലി", "sv": "സ്വീഡിഷ്", "tg": "താജിക്", "ta": "തമിഴ്", "tt": "ടാറ്റർ", "te": "മലയാളം", "th": "തായ്", "ti": "ടിഗ്രിനിയ", "ts": "ടൊവിനോ തോമസ്", "tr": "ടർക്കിഷ്", "tk": "തുര്ക്മെന്", "uk": "ഉക്രേനിയൻ", "ur": "ഉർദു", "ug": "ഉയിഗൂർ", "uz": "ഉസ്ബെക്", "vi": "വിയറ്റ്നാമീസ്", "cy": "വെൽഷ്", "fy": "പടിഞ്ഞാറൻ ഫ്രീസിയൻ", "xh": "ജോസഫ്", "yi": "യിദ്ദിഷ്", "yo": "യോരുബ", "zu": "സുലു" };
  const streamNoConnectionToServer$q = "സെർവറുമായി ബന്ധമില്ല";
  const searchField$q = "തിരയൂ...";
  const VOTTranslateAPIErrors$q = "എപിഐയിൽ നിന്ന് പിശകുകൾ വിവർത്തനം ചെയ്യുക";
  const VOTDetectService$q = "ഭാഷാ കണ്ടെത്തൽ സേവനം";
  const VOTProxyWorkerHost$q = "പ്രോക്സി തൊഴിലാളിയുടെ വിലാസം നല്കുക";
  const VOTM3u8ProxyHost$q = "എം 3 യു 8 പ്രോക്സി തൊഴിലാളിയുടെ വിലാസം നൽകുക";
  const proxySettings$q = "പ്രോക്സി ക്രമീകരണങ്ങൾ";
  const translationTakeApproximatelyMinute2$q = "പരിഭാഷ ഏകദേശം {0} മിനിറ്റ് എടുക്കും";
  const VOTAudioBooster$q = "വിപുലീകരിച്ച പരിഭാഷ വോള്യം വർദ്ധിപ്പിക്കാൻ";
  const VOTSubtitlesDesign$q = "സബ്ടൈറ്റിലുകൾ ഡിസൈൻ";
  const VOTSubtitlesFontSize$q = "സബ്ടൈറ്റിലുകളുടെ അക്ഷരസഞ്ചയം";
  const VOTSubtitlesOpacity$q = "സബ്ടൈറ്റിൽ പശ്ചാത്തലത്തിന്റെ സുതാര്യത";
  const VOTSubtitlesDownloadFormat$q = "സബ്ടൈറ്റിലുകൾ ഡൌൺലോഡ് ചെയ്യുന്നതിനുള്ള ഫോർമാറ്റ്";
  const VOTDownloadWithName$q = "വീഡിയോ പേര് ഉപയോഗിച്ച് ഫയലുകൾ ഡൗൺലോഡ്";
  const VOTUpdateLocaleFiles$q = "അപ്ഡേറ്റ് localization ഫയലുകൾ";
  const VOTLocaleHash$q = "പ്രാദേശിക ഹാഷ്";
  const VOTUpdatedAt$q = "അപ്ഡേറ്റ്";
  const VOTNeedWebAudioAPI$q = "ഇത് ചെയ്യുന്നതിന്, നിങ്ങൾക്ക് ഒരു വെബ് ഓഡിയോ എപിഐ ഉണ്ടായിരിക്കണം.";
  const VOTMediaCSPEnabledOnSite$q = "മീഡിയ സിഎസ്പി ഈ സൈറ്റിൽ പ്രാപ്തമാക്കിയിരിക്കുന്നു";
  const VOTOnlyBypassMediaCSP$q = "മീഡിയ സി. എസ്. പിയെ മറികടക്കാന് മാത്രം ഉപയോഗിക്കുക";
  const VOTNewAudioPlayer$q = "പുതിയ ഓഡിയോ പ്ലെയർ ഉപയോഗിക്കുക";
  const VOTUseNewModel$q = "ചില വീഡിയോകൾക്കായി യാൻഡെക്സ് ശബ്ദങ്ങളുടെ ഒരു പരീക്ഷണാത്മക വ്യതിയാനം ഉപയോഗിക്കുക";
  const TranslationDelayed$q = "പരിഭാഷ അല്പം വൈകി";
  const VOTTranslationCompletedNotify$q = "വിവർത്തനം {0} പൂർത്തിയായി!";
  const VOTSendNotifyOnComplete$q = "വീഡിയോ പരിഭാഷപ്പെടുത്തിയതായി അറിയിപ്പ് അയയ്ക്കുക";
  const VOTBugReport$q = "ഒരു ബഗ് റിപ്പോർട്ട് ചെയ്യുക";
  const VOTTranslateProxyDisabled$q = "Disabled";
  const VOTTranslateProxyEnabled$q = "പ്രാപ്തമാക്കി";
  const VOTTranslateProxyEverything$q = "പ്രോക്സി എല്ലാം";
  const VOTTranslateProxyStatus$q = "പ്രോക്സി മോഡ്";
  const VOTTranslatedBy$q = "വിവർത്തനം {0}";
  const VOTStreamNotAvailable$q = "വിവർത്തനം സ്ട്രീം ലഭ്യമല്ല";
  const VOTTranslationTextService$q = "ടെക്സ്റ്റ് വിവർത്തന സേവനം";
  const VOTNotAffectToVoice$q = "വോയ്സ് ഓവർ ടെക്സ്റ്റിന്റെ പരിഭാഷയെ ബാധിക്കില്ല";
  const DontTranslateSelectedLanguages$q = "തിരഞ്ഞെടുത്ത ഭാഷകളിൽ നിന്ന് വിവർത്തനം ചെയ്യരുത്";
  const showVideoVolumeSlider$q = "വീഡിയോ വോള്യം സ്ലൈഡര് കാണിക്കുക";
  const hotkeysSettings$q = "ഹോട്ട്കീകൾ ക്രമീകരണങ്ങൾ";
  const None$q = "ഒന്നുമില്ല";
  const VOTUseLivelyVoice$q = "സജീവമായ ശബ്ദങ്ങൾ ഉപയോഗിക്കുക. സ്പീക്കറുകൾ റഷ്യൻ സ്വദേശികളെപ്പോലെയാണ്.";
  const miscSettings$q = "എംസിസി ക്രമീകരണങ്ങൾ";
  const services$q = { "yandexbrowser": "യാൻഡെക്സ് ബ്രൗസർ", "msedge": "മൈക്രോസോഫ്റ്റ്", "rust-server": "റസ്റ്റ് സെർവർ" };
  const aboutExtension$q = "വിപുലീകരണത്തെക്കുറിച്ച്";
  const appearance$q = "രൂപം";
  const buttonPosition$q = "പ്ലെയറിലെ ബട്ടണ് സ്ഥാനം";
  const position$q = { "left": "ഇടത്", "right": "ശരിയാണ്", "top": "മുകളിൽ", "default": "സ്ഥിരസ്ഥിതി" };
  const secs$q = "സെക്കന്റുകൾ";
  const autoHideButtonDelay$q = "വിവർത്തനം ബട്ടൺ മറയ്ക്കുന്നതിന് മുമ്പ് കാലതാമസം";
  const notFound$q = "കണ്ടെത്തിയില്ല";
  const minButtonPositionContainer$q = "ബട്ടൺ സ്ഥാനം 600 പിക്സലുകളേക്കാൾ വലുപ്പമുള്ള കളിക്കാരിൽ മാത്രം മാറ്റുന്നു.";
  const VOTTranslateProxyStatusDefault$q = "നിങ്ങളുടെ രാജ്യത്ത് പ്രോക്സിംഗ് പൂർണ്ണമായും അപ്രാപ്തമാക്കുന്നത് വിപുലീകരണം തകർത്തേക്കാം";
  const PressTheKeyCombination$q = "കീ കോമ്പിനേഷൻ അമർത്തുക...";
  const VOTUseAudioDownload$q = "Use audio download";
  const VOTUseAudioDownloadWarning$q = "ഓഡിയോ ഡൌൺലോഡുകൾ അപ്രാപ്തമാക്കുന്നത് വിപുലീകരണത്തിന്റെ പ്രവർത്തനത്തെ ബാധിച്ചേക്കാം";
  const VOTAccountRequired$q = "ഈ സവിശേഷത ഉപയോഗിക്കുന്നതിന് നിങ്ങൾ ലോഗിൻ ചെയ്യേണ്ടതുണ്ട്";
  const VOTMyAccount$q = "എന്റെ അക്കൗണ്ട്";
  const VOTLogin$q = "ലോഗിൻ ചെയ്യുക";
  const VOTLogout$q = "പുറത്തുകടക്കുക";
  const VOTRefresh$q = "പുതുക്കുക";
  const VOTYandexToken$q = "യാൻഡെക്സ് ഒഒത് ടോക്കൺ നൽകുക";
  const VOTYandexTokenInfo$q = "ഈ ഫീൽഡിൽ നിങ്ങൾക്ക് അക്കൌണ്ട് ടോക്കൺ സ്വമേധയാ സജ്ജമാക്കാൻ കഴിയും. ഒരു വിവർത്തന അഭ്യർത്ഥന അയയ്ക്കുന്നതിന് മുമ്പ് ഞങ്ങൾ അതിന്റെ സാധുത പരിശോധിക്കുന്നില്ലെന്ന് ദയവായി ശ്രദ്ധിക്കുക";
  const VOTLoginViaToken$q = "ടോക്കൺ വഴി ലോഗിൻ ചെയ്യുക";
  const subtitlesSmartLayout$q = "സ്മാർട്ട് സബ്ടൈറ്റിൽ ലേഔട്ട്";
  const smartDucking$q = "അഡാപ്റ്റീവ് വോള്യം";
  const VOTAutoSubtitles$q = "സബ്ടൈറ്റിലുകൾ ഓപ്പൺ";
  const VOTSubtitlesFont$q = "സബ്ടൈറ്റില് അക്ഷരസഞ്ചയം";
  const __vite_glob_0_35 = {
    recommended: recommended$q,
    translateVideo: translateVideo$q,
    disableTranslate: disableTranslate$q,
    translationSettings: translationSettings$q,
    subtitlesSettings: subtitlesSettings$q,
    resetSettings: resetSettings$q,
    videoBeingTranslated: videoBeingTranslated$q,
    videoLanguage: videoLanguage$q,
    translationLanguage: translationLanguage$q,
    translationTake: translationTake$q,
    translationTakeMoreThanHour: translationTakeMoreThanHour$q,
    translationTakeAboutMinute: translationTakeAboutMinute$q,
    translationTakeFewMinutes: translationTakeFewMinutes$q,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$q,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$q,
    requestTranslationFailed: requestTranslationFailed$q,
    audioNotReceived: audioNotReceived$q,
    audioFormatNotSupported: audioFormatNotSupported$q,
    VOTAutoTranslate: VOTAutoTranslate$q,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$q,
    VOTVolume: VOTVolume$q,
    VOTVolumeTranslation: VOTVolumeTranslation$q,
    VOTAutoSetVolume: VOTAutoSetVolume$q,
    VOTShowVideoSlider: VOTShowVideoSlider$q,
    VOTSyncVolume: VOTSyncVolume$q,
    VOTDisableFromYourLang: VOTDisableFromYourLang$q,
    VOTVideoIsTooLong: VOTVideoIsTooLong$q,
    VOTNoVideoIDFound: VOTNoVideoIDFound$q,
    VOTSubtitles: VOTSubtitles$q,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$q,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$q,
    VOTHighlightWords: VOTHighlightWords$q,
    VOTTranslatedFrom: VOTTranslatedFrom$q,
    VOTAutogenerated: VOTAutogenerated$q,
    VOTSettings: VOTSettings$q,
    VOTMenuLanguage: VOTMenuLanguage$q,
    VOTAuthors: VOTAuthors$q,
    VOTVersion: VOTVersion$q,
    VOTLoader: VOTLoader$q,
    VOTBrowser: VOTBrowser$q,
    VOTShowPiPButton: VOTShowPiPButton$q,
    langs: langs$q,
    streamNoConnectionToServer: streamNoConnectionToServer$q,
    searchField: searchField$q,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$q,
    VOTDetectService: VOTDetectService$q,
    VOTProxyWorkerHost: VOTProxyWorkerHost$q,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$q,
    proxySettings: proxySettings$q,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$q,
    VOTAudioBooster: VOTAudioBooster$q,
    VOTSubtitlesDesign: VOTSubtitlesDesign$q,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$q,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$q,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$q,
    VOTDownloadWithName: VOTDownloadWithName$q,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$q,
    VOTLocaleHash: VOTLocaleHash$q,
    VOTUpdatedAt: VOTUpdatedAt$q,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$q,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$q,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$q,
    VOTNewAudioPlayer: VOTNewAudioPlayer$q,
    VOTUseNewModel: VOTUseNewModel$q,
    TranslationDelayed: TranslationDelayed$q,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$q,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$q,
    VOTBugReport: VOTBugReport$q,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$q,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$q,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$q,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$q,
    VOTTranslatedBy: VOTTranslatedBy$q,
    VOTStreamNotAvailable: VOTStreamNotAvailable$q,
    VOTTranslationTextService: VOTTranslationTextService$q,
    VOTNotAffectToVoice: VOTNotAffectToVoice$q,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$q,
    showVideoVolumeSlider: showVideoVolumeSlider$q,
    hotkeysSettings: hotkeysSettings$q,
    None: None$q,
    VOTUseLivelyVoice: VOTUseLivelyVoice$q,
    miscSettings: miscSettings$q,
    services: services$q,
    aboutExtension: aboutExtension$q,
    appearance: appearance$q,
    buttonPosition: buttonPosition$q,
    position: position$q,
    secs: secs$q,
    autoHideButtonDelay: autoHideButtonDelay$q,
    notFound: notFound$q,
    minButtonPositionContainer: minButtonPositionContainer$q,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$q,
    PressTheKeyCombination: PressTheKeyCombination$q,
    VOTUseAudioDownload: VOTUseAudioDownload$q,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$q,
    VOTAccountRequired: VOTAccountRequired$q,
    VOTMyAccount: VOTMyAccount$q,
    VOTLogin: VOTLogin$q,
    VOTLogout: VOTLogout$q,
    VOTRefresh: VOTRefresh$q,
    VOTYandexToken: VOTYandexToken$q,
    VOTYandexTokenInfo: VOTYandexTokenInfo$q,
    VOTLoginViaToken: VOTLoginViaToken$q,
    subtitlesSmartLayout: subtitlesSmartLayout$q,
    smartDucking: smartDucking$q,
    VOTAutoSubtitles: VOTAutoSubtitles$q,
    VOTSubtitlesFont: VOTSubtitlesFont$q
  };
  const recommended$p = "Зөвлөмж болгож байна";
  const translateVideo$p = "Видео орчуулах";
  const disableTranslate$p = "Унтраа";
  const translationSettings$p = "Орчуулгын тохиргоо";
  const subtitlesSettings$p = "Хадмалтай тохиргоо";
  const resetSettings$p = "Тохиргоог дахин тохируулах";
  const videoBeingTranslated$p = "Видеог орчуулан хүргэж байна";
  const videoLanguage$p = "Видео хэл";
  const translationLanguage$p = "Орчуулгын хэл";
  const translationTake$p = "Орчуулга авна";
  const translationTakeMoreThanHour$p = "Орчуулга нь нэг цагаас илүү хугацаа шаардагдана";
  const translationTakeAboutMinute$p = "Орчуулга нь нэг минут орчим болно";
  const translationTakeFewMinutes$p = "Орчуулга нь хэдэн минут болно";
  const translationTakeApproximatelyMinutes$p = "Орчуулга нь ойролцоогоор {0} минут болно";
  const translationTakeApproximatelyMinute$p = "Орчуулга нь ойролцоогоор {0} минут болно";
  const requestTranslationFailed$p = "Видео орчуулга хийх хүсэлт гаргаагүй";
  const audioNotReceived$p = "Аудио холбоос хүлээн аваагүй";
  const audioFormatNotSupported$p = "Аудио форматыг дэмждэггүй";
  const VOTAutoTranslate$p = "Нээлттэй дээр орчуулах";
  const VOTDontTranslateYourLang$p = "Миний хэлнээс битгий орчуулаарай";
  const VOTVolume$p = "Видео хэмжээ:";
  const VOTVolumeTranslation$p = "Орчуулгын хэмжээ:";
  const VOTAutoSetVolume$p = "Видео хэмжээг багасгах";
  const VOTShowVideoSlider$p = "Видео дууны гулсагч";
  const VOTSyncVolume$p = "Холбоос орчуулга, Видео хэмжээ";
  const VOTDisableFromYourLang$p = "Та өөрийн хэл дээр видео Орчуулгыг идэвхгүй байна";
  const VOTVideoIsTooLong$p = "Видео хэтэрхий урт байна";
  const VOTNoVideoIDFound$p = "Видео ID олдсонгүй";
  const VOTSubtitles$p = "Хадмалтай";
  const VOTSubtitlesDisabled$p = "Хөгжлийн бэрхшээлтэй";
  const VOTSubtitlesMaxLength$p = "Хадмалтай Макс урт";
  const VOTHighlightWords$p = "Онцлох үгс";
  const VOTTranslatedFrom$p = "орчуулсан нь";
  const VOTAutogenerated$p = "автогенжүүлсэн";
  const VOTSettings$p = "VOT Тохиргоо";
  const VOTMenuLanguage$p = "Цэсийн хэл";
  const VOTAuthors$p = "Зохиогчид";
  const VOTVersion$p = "Хувилбар";
  const VOTLoader$p = "Ачигч";
  const VOTBrowser$p = "Хөтөч";
  const VOTShowPiPButton$p = "PiP товчийг харуулах";
  const langs$p = { "auto": "Авто", "af": "Африкчууд", "ak": "Акан", "sq": "Албани", "am": "Амхарик", "ar": "Араб хэл", "hy": "Армян", "as": "Ассам", "ay": "Аймара", "az": "Азербайджан", "bn": "Бангла", "eu": "Баск", "be": "Беларусийн", "bho": "Бхожпури", "bs": "Боснийн", "bg": "Болгарын", "my": "Бирмийн", "ca": "Каталан", "ceb": "Сэбү", "zh": "Хятад хэл", "zh-Hans": "Хятад Хэл (Хялбаршуулсан)", "zh-Hant": "Хятад (Уламжлалт)", "co": "Корсикан", "hr": "Хорватын", "cs": "Чех", "da": "Данийн", "dv": "Дивехи", "nl": "Голланд", "en": "Англи хэл", "eo": "Эсперанто", "et": "Эстон", "ee": "Унэ", "fil": "Филиппин хэл", "fi": "Финлянд", "fr": "Франц хэл", "gl": "Галисиан", "lg": "Ганда", "ka": "Гүржийн", "de": "Герман хэл", "el": "Грек", "gn": "Гуарани", "gu": "Гужарати", "ht": "Гайтийн Креол", "ha": "Хауса", "haw": "Хавайн", "iw": "Еврей хэл", "hi": "Хинди", "hmn": "Хмонг", "hu": "Унгар хэл", "is": "Исландын", "ig": "Игбо", "id": "Индонезийн", "ga": "Ирландчууд", "it": "Италийн", "ja": "Япон хэл", "jv": "Ява", "kn": "Каннада", "kk": "Казах", "km": "Кхмер", "rw": "Кинярванда", "ko": "Солонгос хэл", "kri": "Крио", "ku": "Курд", "ky": "Киргиз", "lo": "Лао", "la": "Латин хэл", "lv": "Латвийн", "ln": "Лингала", "lt": "Литвийн", "lb": "Люксембургиш", "mk": "Македон", "mg": "Малагасы", "ms": "Малай", "ml": "Малаялам", "mt": "Мальта", "mi": "Мүүн", "mr": "Марати", "mn": "Монгол хэл", "ne": "Балбын", "nso": "Хойд Сото", "no": "Норвегийн", "ny": "Нямжав", "or": "Одия", "om": "Оромо", "ps": "Пашто", "fa": "Перс хэл", "pl": "Польш", "pt": "Португали", "pa": "Пунжаби", "qu": "Кечуа", "ro": "Румын", "ru": "Орос хэл", "sm": "Самоан", "sa": "Санскрит", "gd": "Шотландын Кельт", "sr": "Сербийн", "sn": "Шона", "sd": "Синдхи", "si": "Синхала", "sk": "Словак", "sl": "Словени", "so": "Сомали", "st": "Өмнөд Сото", "es": "Испани хэл", "su": "Сунданез", "sw": "Свахили", "sv": "Шведийн", "tg": "Тажик", "ta": "Тамил", "tt": "Татар", "te": "Тэлугу", "th": "Тай", "ti": "Тигриня", "ts": "Цонга", "tr": "Туркийн", "tk": "Туркмен", "uk": "Украины", "ur": "Урду", "ug": "Уйгур", "uz": "Узбек", "vi": "Вьетнам", "cy": "Уэльс", "fy": "Баруун Фрисиан", "xh": "Схоса", "yi": "Идиш", "yo": "Еруба", "zu": "Зулу" };
  const streamNoConnectionToServer$p = "Сервертэй холбогдох боломжгүй";
  const searchField$p = "Хайх...";
  const VOTTranslateAPIErrors$p = "API - аас алдаа орчуулах";
  const VOTDetectService$p = "Хэл илрүүлэх үйлчилгээ";
  const VOTProxyWorkerHost$p = "Прокси ажилчдын хаягийг оруулна уу";
  const VOTM3u8ProxyHost$p = "M3u8 прокси ажилтны хаягийг оруулна уу";
  const proxySettings$p = "Прокси Тохиргоо";
  const translationTakeApproximatelyMinute2$p = "Орчуулга нь ойролцоогоор {0} минут болно";
  const VOTAudioBooster$p = "Өргөтгөсөн орчуулгын хэмжээ нэмэгдэх";
  const VOTSubtitlesDesign$p = "Хадмалтай дизайн";
  const VOTSubtitlesFontSize$p = "Хадмалтай үсгийн хэмжээ";
  const VOTSubtitlesOpacity$p = "Subtitle background-ийн ил тод байдал";
  const VOTSubtitlesDownloadFormat$p = "Хадмал орчуулга татаж авах формат";
  const VOTDownloadWithName$p = "Видео нэртэй файлуудыг татаж авах";
  const VOTUpdateLocaleFiles$p = "Локалчлалын файлуудыг шинэчлэх";
  const VOTLocaleHash$p = "Локал хэш";
  const VOTUpdatedAt$p = "цагт шинэчлэгдсэн";
  const VOTNeedWebAudioAPI$p = "Үүнийг идэвхжүүлэхийн тулд та вэб аудио API-тэй байх естой";
  const VOTMediaCSPEnabledOnSite$p = "Хэвлэл мэдээллийн CSP энэ сайт дээр идэвхжүүлсэн байна";
  const VOTOnlyBypassMediaCSP$p = "Зөвхөн хэвлэл мэдээллийн ОНХХ алгасан үүнийг ашиглах";
  const VOTNewAudioPlayer$p = "Шинэ аудио тоглуулагч ашиглах";
  const VOTUseNewModel$p = "Зарим видеонуудад Yandex-ийн дуу хоолойны туршилтын хувилбарыг ашиглана уу";
  const TranslationDelayed$p = "Орчуулга нь бага зэрэг хойшлогдож байна";
  const VOTTranslationCompletedNotify$p = "Орчуулга дууссан байна!";
  const VOTSendNotifyOnComplete$p = "Видеог орчуулсан тухай мэдэгдэл илгээх";
  const VOTBugReport$p = "Алдааг мэдээлэх";
  const VOTTranslateProxyDisabled$p = "Хөгжлийн бэрхшээлтэй";
  const VOTTranslateProxyEnabled$p = "Идэвхжсэн";
  const VOTTranslateProxyEverything$p = "Прокси бүх зүйл";
  const VOTTranslateProxyStatus$p = "Proxying горим";
  const VOTTranslatedBy$p = "Орчуулагдсан: {0}";
  const VOTStreamNotAvailable$p = "Translate stream байхгүй байна";
  const VOTTranslationTextService$p = "Текст Орчуулгын үйлчилгээ";
  const VOTNotAffectToVoice$p = "Нүүр хуудас гадаад хэл дээр орчуулга хийхгүй байх";
  const DontTranslateSelectedLanguages$p = "Сонгосон хэлнээс орчуулж болохгүй";
  const showVideoVolumeSlider$p = "Видео дууны гулсагчийг харуулах";
  const hotkeysSettings$p = "Hotkeys тохиргоо";
  const None$p = "Байхгүй";
  const VOTUseLivelyVoice$p = "Амьд дуу хоолойг ашигла. Чанга яригч нь уугуул оросууд шиг сонсогддог.";
  const miscSettings$p = "Misc тохиргоо";
  const services$p = { "yandexbrowser": "Yandex Хөтөч", "msedge": "Microsoft Edge", "rust-server": "Зэв Сервер" };
  const aboutExtension$p = "Сунгах тухай";
  const appearance$p = "Гадаад төрх";
  const buttonPosition$p = "Тоглогч дахь товчлуурын байрлал";
  const position$p = { "left": "Зүүн", "right": "Зөв", "top": "Дээд", "default": "Анхдагч" };
  const secs$p = "секц";
  const autoHideButtonDelay$p = "Орчуулах товчийг нуухаасаа өмнө хойшлуул";
  const notFound$p = "олдоогүй";
  const minButtonPositionContainer$p = "Товчлуурын байрлал нь зөвхөн 600 пикселээс том тоглогчдод л өөрчлөгддөг.";
  const VOTTranslateProxyStatusDefault$p = "Танай улсад proxying-ийг бүрэн идэвхгүй болгох нь өргөтгөлийг эвдэж болзошгүй юм";
  const PressTheKeyCombination$p = "Товчлуурын хослолыг дарна уу...";
  const VOTUseAudioDownload$p = "Аудио татаж ашиглах";
  const VOTUseAudioDownloadWarning$p = "Аудио татан авалтыг идэвхгүй болгох нь өргөтгөлийн үйл ажиллагаанд нөлөөлж болзошгүй юм";
  const VOTAccountRequired$p = "Энэ функцийг ашиглахын тулд та нэвтрэх хэрэгтэй";
  const VOTMyAccount$p = "Миний данс";
  const VOTLogin$p = "Нэвтрэх";
  const VOTLogout$p = "Нэвтрэх";
  const VOTRefresh$p = "Сэргээх";
  const VOTYandexToken$p = "Yandex OAuth токен оруулна уу";
  const VOTYandexTokenInfo$p = "Та энэ талбарт дансны түлхүүрийг гараар тохируулж болно. Орчуулах хүсэлт илгээхээс өмнө бид түүний хүчинтэй эсэхийг шалгахгүй болохыг анхаарна уу";
  const VOTLoginViaToken$p = "Токеноор нэвтрэх";
  const subtitlesSmartLayout$p = "Smart subtitle зохион байгуулалт";
  const smartDucking$p = "Дасан зохицох хэмжээ";
  const VOTAutoSubtitles$p = "Нээлттэй дээр хадмалтай";
  const VOTSubtitlesFont$p = "Хадмал үсгийн фонт";
  const __vite_glob_0_36 = {
    recommended: recommended$p,
    translateVideo: translateVideo$p,
    disableTranslate: disableTranslate$p,
    translationSettings: translationSettings$p,
    subtitlesSettings: subtitlesSettings$p,
    resetSettings: resetSettings$p,
    videoBeingTranslated: videoBeingTranslated$p,
    videoLanguage: videoLanguage$p,
    translationLanguage: translationLanguage$p,
    translationTake: translationTake$p,
    translationTakeMoreThanHour: translationTakeMoreThanHour$p,
    translationTakeAboutMinute: translationTakeAboutMinute$p,
    translationTakeFewMinutes: translationTakeFewMinutes$p,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$p,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$p,
    requestTranslationFailed: requestTranslationFailed$p,
    audioNotReceived: audioNotReceived$p,
    audioFormatNotSupported: audioFormatNotSupported$p,
    VOTAutoTranslate: VOTAutoTranslate$p,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$p,
    VOTVolume: VOTVolume$p,
    VOTVolumeTranslation: VOTVolumeTranslation$p,
    VOTAutoSetVolume: VOTAutoSetVolume$p,
    VOTShowVideoSlider: VOTShowVideoSlider$p,
    VOTSyncVolume: VOTSyncVolume$p,
    VOTDisableFromYourLang: VOTDisableFromYourLang$p,
    VOTVideoIsTooLong: VOTVideoIsTooLong$p,
    VOTNoVideoIDFound: VOTNoVideoIDFound$p,
    VOTSubtitles: VOTSubtitles$p,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$p,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$p,
    VOTHighlightWords: VOTHighlightWords$p,
    VOTTranslatedFrom: VOTTranslatedFrom$p,
    VOTAutogenerated: VOTAutogenerated$p,
    VOTSettings: VOTSettings$p,
    VOTMenuLanguage: VOTMenuLanguage$p,
    VOTAuthors: VOTAuthors$p,
    VOTVersion: VOTVersion$p,
    VOTLoader: VOTLoader$p,
    VOTBrowser: VOTBrowser$p,
    VOTShowPiPButton: VOTShowPiPButton$p,
    langs: langs$p,
    streamNoConnectionToServer: streamNoConnectionToServer$p,
    searchField: searchField$p,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$p,
    VOTDetectService: VOTDetectService$p,
    VOTProxyWorkerHost: VOTProxyWorkerHost$p,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$p,
    proxySettings: proxySettings$p,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$p,
    VOTAudioBooster: VOTAudioBooster$p,
    VOTSubtitlesDesign: VOTSubtitlesDesign$p,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$p,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$p,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$p,
    VOTDownloadWithName: VOTDownloadWithName$p,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$p,
    VOTLocaleHash: VOTLocaleHash$p,
    VOTUpdatedAt: VOTUpdatedAt$p,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$p,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$p,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$p,
    VOTNewAudioPlayer: VOTNewAudioPlayer$p,
    VOTUseNewModel: VOTUseNewModel$p,
    TranslationDelayed: TranslationDelayed$p,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$p,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$p,
    VOTBugReport: VOTBugReport$p,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$p,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$p,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$p,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$p,
    VOTTranslatedBy: VOTTranslatedBy$p,
    VOTStreamNotAvailable: VOTStreamNotAvailable$p,
    VOTTranslationTextService: VOTTranslationTextService$p,
    VOTNotAffectToVoice: VOTNotAffectToVoice$p,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$p,
    showVideoVolumeSlider: showVideoVolumeSlider$p,
    hotkeysSettings: hotkeysSettings$p,
    None: None$p,
    VOTUseLivelyVoice: VOTUseLivelyVoice$p,
    miscSettings: miscSettings$p,
    services: services$p,
    aboutExtension: aboutExtension$p,
    appearance: appearance$p,
    buttonPosition: buttonPosition$p,
    position: position$p,
    secs: secs$p,
    autoHideButtonDelay: autoHideButtonDelay$p,
    notFound: notFound$p,
    minButtonPositionContainer: minButtonPositionContainer$p,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$p,
    PressTheKeyCombination: PressTheKeyCombination$p,
    VOTUseAudioDownload: VOTUseAudioDownload$p,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$p,
    VOTAccountRequired: VOTAccountRequired$p,
    VOTMyAccount: VOTMyAccount$p,
    VOTLogin: VOTLogin$p,
    VOTLogout: VOTLogout$p,
    VOTRefresh: VOTRefresh$p,
    VOTYandexToken: VOTYandexToken$p,
    VOTYandexTokenInfo: VOTYandexTokenInfo$p,
    VOTLoginViaToken: VOTLoginViaToken$p,
    subtitlesSmartLayout: subtitlesSmartLayout$p,
    smartDucking: smartDucking$p,
    VOTAutoSubtitles: VOTAutoSubtitles$p,
    VOTSubtitlesFont: VOTSubtitlesFont$p
  };
  const recommended$o = "disyorkan";
  const translateVideo$o = "Terjemahkan video";
  const disableTranslate$o = "Matikan";
  const translationSettings$o = "Tetapan terjemahan";
  const subtitlesSettings$o = "Tetapan sari kata";
  const resetSettings$o = "Tetap semula tetapan";
  const videoBeingTranslated$o = "Video sedang diterjemahkan";
  const videoLanguage$o = "Bahasa video";
  const translationLanguage$o = "Bahasa terjemahan";
  const translationTake$o = "Terjemahan akan mengambil masa";
  const translationTakeMoreThanHour$o = "Terjemahan akan mengambil masa lebih daripada satu jam";
  const translationTakeAboutMinute$o = "Terjemahan akan mengambil masa selama sekitar satu minit";
  const translationTakeFewMinutes$o = "Terjemahan akan mengambil masa beberapa minit";
  const translationTakeApproximatelyMinutes$o = "Terjemahan akan mengambil masa selama kira-kira {0} minit";
  const translationTakeApproximatelyMinute$o = "Terjemahan akan mengambil masa selama kira-kira {0} minit";
  const requestTranslationFailed$o = "Gagal meminta terjemahan video";
  const audioNotReceived$o = "Link audio tidak diterima";
  const audioFormatNotSupported$o = "Format audio tidak disokong";
  const VOTAutoTranslate$o = "Terjemahkan semasa pembukaan";
  const VOTDontTranslateYourLang$o = "Jangan terjemahkan dari bahasa asal anda";
  const VOTVolume$o = "Kelantangan Video:";
  const VOTVolumeTranslation$o = "Jumlah terjemahan:";
  const VOTAutoSetVolume$o = "Kurangkan kelantangan video kepada";
  const VOTShowVideoSlider$o = "Papar slider kelantangan video";
  const VOTSyncVolume$o = "Segerakan kelantangan terjemahan dan video";
  const VOTDisableFromYourLang$o = "Anda telah mematikan terjemahan video dalam bahasa anda";
  const VOTVideoIsTooLong$o = "Video terlalu panjang";
  const VOTNoVideoIDFound$o = "Tidak ditemui ID video";
  const VOTSubtitles$o = "Sari kata";
  const VOTSubtitlesDisabled$o = "Dimatikan";
  const VOTSubtitlesMaxLength$o = "Panjang maksimum sari kata";
  const VOTHighlightWords$o = "Serlahkan perkataan";
  const VOTTranslatedFrom$o = "diterjemahkan dari";
  const VOTAutogenerated$o = "dijana secara automatik";
  const VOTSettings$o = "Tetapan VOT";
  const VOTMenuLanguage$o = "Bahasa menu";
  const VOTAuthors$o = "Pengarang";
  const VOTVersion$o = "Versi";
  const VOTLoader$o = "Pemuat";
  const VOTBrowser$o = "Pelayar";
  const VOTShowPiPButton$o = "Tunjukkan butang PiP";
  const langs$o = { "auto": "Secara automatik", "az": "Azerbaijan", "ay": "Aymara", "ak": "Akan", "sq": "Albania", "am": "Amharic", "en": "Inggeris", "ar": "Arab", "hy": "Armenia", "as": "Assam", "af": "Afrikaans", "eu": "Basque", "be": "Belarusia", "bn": "Bengali", "my": "Burmese", "bg": "Bulgaria", "bs": "Bosnia", "bho": "Bhojpuri", "cy": "Wales", "hu": "Hungary", "vi": "Vietnam", "haw": "Hawaii", "ht": "Haiti", "gl": "Galicia", "lg": "Ganda", "el": "Greek", "ka": "Georgia", "gn": "Guaraní", "gu": "Gujarati", "gd": "Scottish Gaelic", "da": "Denmark", "fy": "Frisia Barat", "zu": "Zulu", "iw": "Ibrani", "ig": "Igbo", "yi": "Yahudi", "id": "Indonesia", "ga": "Ireland", "is": "Icelandic", "es": "Sepanyol", "it": "Itali", "yo": "Yoruba", "kk": "Kazakhstan", "kn": "Kannada", "ca": "Catalonia", "qu": "Quechua", "rw": "Kinyarwanda", "ky": "Kyrgyzstan", "zh": "Cina", "zh-Hant": "Cina (Tradisional)", "zh-Hans": "Cina (Mudah)", "ko": "Korea", "co": "Corsica", "xh": "Xhosa", "ku": "Kurdistan", "km": "Khmer", "lo": "Laos", "la": "Latin", "lv": "Latvia", "ln": "Lingala", "lt": "Lithuania", "lb": "Luxembourg", "mk": "Macedonia", "mg": "Madagascar", "ms": "Melayu", "ml": "Malayalam", "dv": "Maldives", "mt": "Malta", "mi": "Maori", "mr": "Marathi", "mn": "Mongolia", "de": "Jerman", "ne": "Nepal", "nl": "Belanda", "no": "Norway", "ny": "Nyanja", "or": "Oriya", "om": "Oromo", "pa": "Punjabi", "fa": "Parsi", "pl": "Poland", "pt": "Portugis", "ps": "Pashto", "ro": "Romania", "ru": "Rusia", "sm": "Samoan", "sa": "Sanskrit", "ceb": "Cebuano", "nso": "Soto Utara", "sr": "Serbia", "si": "Sinhala", "sd": "Sindhi", "sk": "Slovakia", "sl": "Slovenia", "so": "Somali", "sw": "Swahili", "su": "Sundan", "tg": "Tajikistan", "th": "Thai", "ta": "Tamil", "tt": "Tatar", "te": "Telugu", "ti": "Tigrinya", "ts": "Tsonga", "tr": "Turki", "tk": "Turkmenistan", "uz": "Uzbekistan", "ug": "Uighur", "uk": "Ukraine", "ur": "Urdu", "fil": "Filipina", "fi": "Finland", "fr": "Perancis", "ha": "Hausa", "hi": "Hindi", "hmn": "Hmong", "hr": "Croatia", "cs": "Czech", "sv": "Swedish", "sn": "Shona", "ee": "Ewe", "eo": "Esperanto", "et": "Estonia", "st": "Soto Selatan", "jv": "Jawa", "ja": "Jepun", "kri": "Kri" };
  const streamNoConnectionToServer$o = "Tiada sambungan ke pelayan";
  const searchField$o = "Cari...";
  const VOTTranslateAPIErrors$o = "Terjemahkan ralat dari API";
  const VOTDetectService$o = "Perkhidmatan pengesanan bahasa";
  const VOTProxyWorkerHost$o = "Masukkan alamat pekerja proksi";
  const VOTM3u8ProxyHost$o = "Masukkan alamat pekerja proksi m3u8";
  const proxySettings$o = "Tetapan Proksi";
  const translationTakeApproximatelyMinute2$o = "Terjemahan akan mengambil masa lebih kurang {0} minit";
  const VOTAudioBooster$o = "Peningkatan jumlah terjemahan lanjutan";
  const VOTSubtitlesDesign$o = "Reka bentuk sarikata";
  const VOTSubtitlesFontSize$o = "Saiz fon sari kata";
  const VOTSubtitlesOpacity$o = "Ketelusan latar belakang sari kata";
  const VOTSubtitlesDownloadFormat$o = "Format untuk muat turun sari kata";
  const VOTDownloadWithName$o = "Muat turun fail dengan nama video";
  const VOTUpdateLocaleFiles$o = "Kemas kini fail penyetempatan";
  const VOTLocaleHash$o = "Hash tempatan";
  const VOTUpdatedAt$o = "Dikemaskini pada";
  const VOTNeedWebAudioAPI$o = "Untuk membolehkan ini, anda mesti mempunyai API Audio web";
  const VOTMediaCSPEnabledOnSite$o = "Media CSP diaktifkan di laman web ini";
  const VOTOnlyBypassMediaCSP$o = "Gunakan ia hanya untuk memintas media CSP";
  const VOTNewAudioPlayer$o = "Gunakan pemain audio baru";
  const VOTUseNewModel$o = "Gunakan variasi percubaan suara Yandex untuk beberapa video";
  const TranslationDelayed$o = "Terjemahan sedikit ditangguhkan";
  const VOTTranslationCompletedNotify$o = "Terjemahan pada {0} telah selesai!";
  const VOTSendNotifyOnComplete$o = "Hantar pemberitahuan bahawa video telah diterjemahkan";
  const VOTBugReport$o = "Laporkan pepijat";
  const VOTTranslateProxyDisabled$o = "Dimatikan";
  const VOTTranslateProxyEnabled$o = "Diaktifkan";
  const VOTTranslateProxyEverything$o = "Proksi semuanya";
  const VOTTranslateProxyStatus$o = "Mod proksi";
  const VOTTranslatedBy$o = "Diterjemahkan oleh {0}";
  const VOTStreamNotAvailable$o = "Aliran Terjemah tidak tersedia";
  const VOTTranslationTextService$o = "Perkhidmatan terjemahan teks";
  const VOTNotAffectToVoice$o = "Tidak menjejaskan terjemahan teks dalam voice over";
  const DontTranslateSelectedLanguages$o = "Jangan terjemahkan daripada bahasa terpilih";
  const showVideoVolumeSlider$o = "Papar penggelongsor volum video";
  const hotkeysSettings$o = "Tetapan Hotkeys";
  const None$o = "Tiada";
  const VOTUseLivelyVoice$o = "Gunakan suara yang meriah. Pembesar suara terdengar seperti orang asli Rusia.";
  const miscSettings$o = "Tetapan lain-lain";
  const services$o = { "yandexbrowser": "Pelayar Yandex", "msedge": "Microsoft Edge", "rust-server": "Pelayan Rust" };
  const aboutExtension$o = "Mengenai sambungan";
  const appearance$o = "Penampilan";
  const buttonPosition$o = "Kedudukan butang dalam pemain";
  const position$o = { "left": "Kiri", "right": "Kanan", "top": "Atas", "default": "Default" };
  const secs$o = "secs";
  const autoHideButtonDelay$o = "Tunda sebelum menyembunyikan butang Terjemah";
  const notFound$o = "tidak dijumpai";
  const minButtonPositionContainer$o = "Kedudukan butang hanya berubah pada pemain yang lebih besar daripada 600 piksel.";
  const VOTTranslateProxyStatusDefault$o = "Melumpuhkan proksi sepenuhnya di negara anda boleh mematahkan pelanjutan";
  const PressTheKeyCombination$o = "Tekan kombinasi kekunci...";
  const VOTUseAudioDownload$o = "Gunakan Muat turun audio";
  const VOTUseAudioDownloadWarning$o = "Melumpuhkan muat turun audio boleh menjejaskan fungsi sambungan";
  const VOTAccountRequired$o = "Anda perlu log masuk untuk menggunakan ciri ini";
  const VOTMyAccount$o = "Akaun saya";
  const VOTLogin$o = "Logmasuk";
  const VOTLogout$o = "Log keluar";
  const VOTRefresh$o = "Segarkan semula";
  const VOTYandexToken$o = "MASUKKAN Token Yandex OAuth";
  const VOTYandexTokenInfo$o = "Anda boleh menetapkan token akaun secara manual dalam medan ini. Sila ambil perhatian bahawa kami tidak menyemak kesahihannya sebelum menghantar permintaan Terjemah";
  const VOTLoginViaToken$o = "Log masuk melalui token";
  const subtitlesSmartLayout$o = "Susun atur sari kata pintar";
  const smartDucking$o = "Isipadu adaptif";
  const VOTAutoSubtitles$o = "Subtitle di buka";
  const VOTSubtitlesFont$o = "Font subtajuk";
  const __vite_glob_0_37 = {
    recommended: recommended$o,
    translateVideo: translateVideo$o,
    disableTranslate: disableTranslate$o,
    translationSettings: translationSettings$o,
    subtitlesSettings: subtitlesSettings$o,
    resetSettings: resetSettings$o,
    videoBeingTranslated: videoBeingTranslated$o,
    videoLanguage: videoLanguage$o,
    translationLanguage: translationLanguage$o,
    translationTake: translationTake$o,
    translationTakeMoreThanHour: translationTakeMoreThanHour$o,
    translationTakeAboutMinute: translationTakeAboutMinute$o,
    translationTakeFewMinutes: translationTakeFewMinutes$o,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$o,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$o,
    requestTranslationFailed: requestTranslationFailed$o,
    audioNotReceived: audioNotReceived$o,
    audioFormatNotSupported: audioFormatNotSupported$o,
    VOTAutoTranslate: VOTAutoTranslate$o,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$o,
    VOTVolume: VOTVolume$o,
    VOTVolumeTranslation: VOTVolumeTranslation$o,
    VOTAutoSetVolume: VOTAutoSetVolume$o,
    VOTShowVideoSlider: VOTShowVideoSlider$o,
    VOTSyncVolume: VOTSyncVolume$o,
    VOTDisableFromYourLang: VOTDisableFromYourLang$o,
    VOTVideoIsTooLong: VOTVideoIsTooLong$o,
    VOTNoVideoIDFound: VOTNoVideoIDFound$o,
    VOTSubtitles: VOTSubtitles$o,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$o,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$o,
    VOTHighlightWords: VOTHighlightWords$o,
    VOTTranslatedFrom: VOTTranslatedFrom$o,
    VOTAutogenerated: VOTAutogenerated$o,
    VOTSettings: VOTSettings$o,
    VOTMenuLanguage: VOTMenuLanguage$o,
    VOTAuthors: VOTAuthors$o,
    VOTVersion: VOTVersion$o,
    VOTLoader: VOTLoader$o,
    VOTBrowser: VOTBrowser$o,
    VOTShowPiPButton: VOTShowPiPButton$o,
    langs: langs$o,
    streamNoConnectionToServer: streamNoConnectionToServer$o,
    searchField: searchField$o,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$o,
    VOTDetectService: VOTDetectService$o,
    VOTProxyWorkerHost: VOTProxyWorkerHost$o,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$o,
    proxySettings: proxySettings$o,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$o,
    VOTAudioBooster: VOTAudioBooster$o,
    VOTSubtitlesDesign: VOTSubtitlesDesign$o,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$o,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$o,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$o,
    VOTDownloadWithName: VOTDownloadWithName$o,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$o,
    VOTLocaleHash: VOTLocaleHash$o,
    VOTUpdatedAt: VOTUpdatedAt$o,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$o,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$o,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$o,
    VOTNewAudioPlayer: VOTNewAudioPlayer$o,
    VOTUseNewModel: VOTUseNewModel$o,
    TranslationDelayed: TranslationDelayed$o,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$o,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$o,
    VOTBugReport: VOTBugReport$o,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$o,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$o,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$o,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$o,
    VOTTranslatedBy: VOTTranslatedBy$o,
    VOTStreamNotAvailable: VOTStreamNotAvailable$o,
    VOTTranslationTextService: VOTTranslationTextService$o,
    VOTNotAffectToVoice: VOTNotAffectToVoice$o,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$o,
    showVideoVolumeSlider: showVideoVolumeSlider$o,
    hotkeysSettings: hotkeysSettings$o,
    None: None$o,
    VOTUseLivelyVoice: VOTUseLivelyVoice$o,
    miscSettings: miscSettings$o,
    services: services$o,
    aboutExtension: aboutExtension$o,
    appearance: appearance$o,
    buttonPosition: buttonPosition$o,
    position: position$o,
    secs: secs$o,
    autoHideButtonDelay: autoHideButtonDelay$o,
    notFound: notFound$o,
    minButtonPositionContainer: minButtonPositionContainer$o,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$o,
    PressTheKeyCombination: PressTheKeyCombination$o,
    VOTUseAudioDownload: VOTUseAudioDownload$o,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$o,
    VOTAccountRequired: VOTAccountRequired$o,
    VOTMyAccount: VOTMyAccount$o,
    VOTLogin: VOTLogin$o,
    VOTLogout: VOTLogout$o,
    VOTRefresh: VOTRefresh$o,
    VOTYandexToken: VOTYandexToken$o,
    VOTYandexTokenInfo: VOTYandexTokenInfo$o,
    VOTLoginViaToken: VOTLoginViaToken$o,
    subtitlesSmartLayout: subtitlesSmartLayout$o,
    smartDucking: smartDucking$o,
    VOTAutoSubtitles: VOTAutoSubtitles$o,
    VOTSubtitlesFont: VOTSubtitlesFont$o
  };
  const recommended$n = "rakkomandat";
  const translateVideo$n = "Ittraduċi video";
  const disableTranslate$n = "Itfi";
  const translationSettings$n = "Settings tat-traduzzjoni";
  const subtitlesSettings$n = "Sottotitoli settings";
  const resetSettings$n = "Irrisettja l-issettjar";
  const videoBeingTranslated$n = "Il-filmat qed jiġi tradott";
  const videoLanguage$n = "Lingwa tal-vidjo";
  const translationLanguage$n = "Lingwa tat-traduzzjoni";
  const translationTake$n = "It-traduzzjoni se tieħu";
  const translationTakeMoreThanHour$n = "It traduzzjoni tieħu aktar minn siegħa";
  const translationTakeAboutMinute$n = "It traduzzjoni tieħu madwar minuta";
  const translationTakeFewMinutes$n = "It traduzzjoni tieħu ftit minuti";
  const translationTakeApproximatelyMinutes$n = "It-traduzzjoni tieħu madwar {0} minuti";
  const translationTakeApproximatelyMinute$n = "It-traduzzjoni tieħu madwar {0} minuti";
  const requestTranslationFailed$n = "Naqas milli jitlob traduzzjoni bil-vidjo";
  const audioNotReceived$n = "Link awdjo mhux riċevut";
  const audioFormatNotSupported$n = "Il-format tal-awdjo mhuwiex appoġġjat";
  const VOTAutoTranslate$n = "Ittraduċi fuq miftuħ";
  const VOTDontTranslateYourLang$n = "Tittraduċix mil lingwa tiegħi";
  const VOTVolume$n = "Volum tal-vidjo:";
  const VOTVolumeTranslation$n = "Volum tat-traduzzjoni:";
  const VOTAutoSetVolume$n = "Naqqas il-volum tal-vidjo għal";
  const VOTShowVideoSlider$n = "Slider tal-volum tal-vidjo";
  const VOTSyncVolume$n = "Traduzzjoni tal-Link u volum tal-vidjow";
  const VOTDisableFromYourLang$n = "Int iddiżattivajt it traduzzjoni tal vidjo fil lingwa tiegħek";
  const VOTVideoIsTooLong$n = "Video huwa twil wisq";
  const VOTNoVideoIDFound$n = "L-EBDA id tal-vidjo ma nstab";
  const VOTSubtitles$n = "Sottotitoli";
  const VOTSubtitlesDisabled$n = "Diżattivat";
  const VOTSubtitlesMaxLength$n = "Sottotitoli tul massimu";
  const VOTHighlightWords$n = "Jenfasizzaw kliem";
  const VOTTranslatedFrom$n = "tradotti minn";
  const VOTAutogenerated$n = "awtoġenerat";
  const VOTSettings$n = "Settings TAL-VOT";
  const VOTMenuLanguage$n = "Lingwa tal-Menu";
  const VOTAuthors$n = "Awturi";
  const VOTVersion$n = "Verżjoni";
  const VOTLoader$n = "Loader";
  const VOTBrowser$n = "Browser";
  const VOTShowPiPButton$n = "Button l-buttuna PiP";
  const langs$n = { "auto": "Auto", "af": "Afrikani", "ak": "Akan", "sq": "Albaniż", "am": "Amariku", "ar": "Għarbi", "hy": "Armenjan", "as": "Assamese", "ay": "Aymara", "az": "L-ażerbajġan", "bn": "Bangla", "eu": "Bask", "be": "Belarus", "bho": "Bhojpuri", "bs": "Bosnijan", "bg": "Bulgaru", "my": "Burmiż", "ca": "Il-katalan", "ceb": "Ċebuano", "zh": "Ċiniż", "zh-Hans": "Ċiniż (Simplifikat)", "zh-Hant": "Ċiniż (Tradizzjonali)", "co": "Korsiku", "hr": "Kroat", "cs": "Ċek", "da": "Daniż", "dv": "Divehi", "nl": "Olandiż", "en": "Ingliż", "eo": "Esperanto", "et": "Estonjan", "ee": "Nagħaġ", "fil": "Filippin", "fi": "Finlandiż", "fr": "Franċiż", "gl": "Galizjan", "lg": "Ganda", "ka": "Ġorġjan", "de": "Ġermaniż", "el": "Grieg", "gn": "Guarani", "gu": "Guġarati", "ht": "Krejol Ħaiti", "ha": "Hausa", "haw": "Ħawajjan", "iw": "Ebrajk", "hi": "Ħindi", "hmn": "Hmong", "hu": "Ungeriż", "is": "L-islandiż", "ig": "Igbo", "id": "Indoneżjan", "ga": "Irlandiż", "it": "Taljan", "ja": "Ġappuniż", "jv": "Ġavaniż", "kn": "Kannada", "kk": "Il-każak", "km": "Khmer", "rw": "Kinyarwanda", "ko": "Korean", "kri": "Krio", "ku": "Kurdi", "ky": "Il-kirgiż", "lo": "Lao", "la": "Latin", "lv": "Latvjan", "ln": "Lingala", "lt": "Litwan", "lb": "Il-lussemburgiż", "mk": "Maċedonjan", "mg": "Madagaskar", "ms": "Malajan", "ml": "Il-malajalam", "mt": "Malti", "mi": "Mġġori", "mr": "Marati", "mn": "Mongoljan", "ne": "In-nepal", "nso": "Soto Tat-Tramuntana", "no": "Norveġiż", "ny": "Nyanja", "or": "Odia", "om": "Oromo", "ps": "Pashto", "fa": "Persjan", "pl": "Pollakk", "pt": "Portugiż", "pa": "Punġabi", "qu": "Quechua", "ro": "Rumen", "ru": "Russu", "sm": "Samoan", "sa": "Sanskrit", "gd": "Galliku Skoċċiż", "sr": "Serb", "sn": "Shona", "sd": "Sindhi", "si": "Sinhala", "sk": "Slovakk", "sl": "Sloven", "so": "Is-somalja", "st": "Soto Tan-Nofsinhar", "es": "Spanjol", "su": "Sundaniż", "sw": "Swaħili", "sv": "Svediż", "tg": "Taġik", "ta": "Tamil", "tt": "Tatar", "te": "Telugu", "th": "Tajlandiż", "ti": "Tigrinya", "ts": "Tsonga", "tr": "Tork", "tk": "Turkmeni", "uk": "Ukraina", "ur": "Urdu", "ug": "Uigur", "uz": "L-użbek", "vi": "Vjetnamiż", "cy": "Welsh", "fy": "Frisian Tal-Punent", "xh": "Xhosa", "yi": "Jiddix", "yo": "Joruba", "zu": "Żulu" };
  const streamNoConnectionToServer$n = "M'hemm l ebda konnessjoni mas server";
  const searchField$n = "Fittex...";
  const VOTTranslateAPIErrors$n = "Ittraduċi żbalji mill-API";
  const VOTDetectService$n = "Servizz ta'skoperta tal-lingwa";
  const VOTProxyWorkerHost$n = "Daħħal l indirizz tal ħaddiem tal prokura";
  const VOTM3u8ProxyHost$n = "Daħħal l indirizz tal ħaddiem tal prokura m3u8";
  const proxySettings$n = "Settings Tal-Prokura";
  const translationTakeApproximatelyMinute2$n = "It-traduzzjoni tieħu madwar {0} minuti";
  const VOTAudioBooster$n = "Żieda estiża fil-volum tat-traduzzjoni";
  const VOTSubtitlesDesign$n = "Disinn tas-sottotitoli";
  const VOTSubtitlesFontSize$n = "Daqs tat-tipa tas-sottotitoli";
  const VOTSubtitlesOpacity$n = "Trasparenza tal-isfond tas-sottotitolu";
  const VOTSubtitlesDownloadFormat$n = "Il-format għat-tniżżil ta'sottotitoli";
  const VOTDownloadWithName$n = "Niżżel fajls bl-isem tal-vidjo";
  const VOTUpdateLocaleFiles$n = "Aġġorna l-fajls tal-lokalizzazzjoni";
  const VOTLocaleHash$n = "Hash lokali";
  const VOTUpdatedAt$n = "Aġġornat fis-";
  const VOTNeedWebAudioAPI$n = "Biex tippermetti dan, irid ikollok Api Awdjo Tal - Web";
  const VOTMediaCSPEnabledOnSite$n = "Il-PSK tal-midja huwa attivat fuq dan is-sit";
  const VOTOnlyBypassMediaCSP$n = "Użaha biss Biex tevita L-PSK Tal-Midja";
  const VOTNewAudioPlayer$n = "Uża l-plejer tal-awdjo l-ġdid";
  const VOTUseNewModel$n = "Uża varjazzjoni sperimentali ta'vuċijiet Yandex għal xi vidjows";
  const TranslationDelayed$n = "It traduzzjoni hija kemmxejn ittardjata";
  const VOTTranslationCompletedNotify$n = "It-traduzzjoni fuq il - {0} tlestiet!";
  const VOTSendNotifyOnComplete$n = "Ibgħat notifika li l-video ġie tradott";
  const VOTBugReport$n = "Irrapporta bug";
  const VOTTranslateProxyDisabled$n = "Diżattivat";
  const VOTTranslateProxyEnabled$n = "Attivat";
  const VOTTranslateProxyEverything$n = "Prokura kollox";
  const VOTTranslateProxyStatus$n = "Mod ta'prokura";
  const VOTTranslatedBy$n = "Tradott minn {0}";
  const VOTStreamNotAvailable$n = "Ittraduċi nixxiegħa mhix disponibbli";
  const VOTTranslationTextService$n = "Servizz ta'traduzzjoni ta'test";
  const VOTNotAffectToVoice$n = "Ma jaffettwax it-traduzzjoni tat-test bil-vuċi fuq";
  const DontTranslateSelectedLanguages$n = "Tittraduċix minn lingwi magħżula";
  const showVideoVolumeSlider$n = "Ay s-slider tal-volum tal-vidjo";
  const hotkeysSettings$n = "Settings tal-Hotkeys";
  const None$n = "Xejn";
  const VOTUseLivelyVoice$n = "Uża vuċijiet vivaċi. Il-kelliema jinstemgħu bħal Russi indiġeni.";
  const miscSettings$n = "Settings Misc";
  const services$n = { "yandexbrowser": "Browser Yandex", "msedge": "Microsoft Edge", "rust-server": "Server Tas-Sadid" };
  const aboutExtension$n = "Dwar l-estensjoni";
  const appearance$n = "Dehra";
  const buttonPosition$n = "Pożizzjoni tal-buttuna fil-plejer";
  const position$n = { "left": "Xellug", "right": "Dritt", "top": "Fuq", "default": "Inadempjenza" };
  const secs$n = "seks";
  const autoHideButtonDelay$n = "Dewmien qabel ma taħbi l-buttuna ittraduċi";
  const notFound$n = "ma nstabx";
  const minButtonPositionContainer$n = "Il-pożizzjoni tal-buttuna tinbidel biss f'plejers akbar minn 600 pixel.";
  const VOTTranslateProxyStatusDefault$n = "Inkapaċitazzjoni kompletament prokura f'pajjiżek tista'tkisser l estensjoni";
  const PressTheKeyCombination$n = "Agħfas il-kombinazzjoni taċ-ċavetta...";
  const VOTUseAudioDownload$n = "Uża tniżżil awdjo";
  const VOTUseAudioDownloadWarning$n = "Id diżattivazzjoni tat tniżżil tal awdjo tista'taffettwa l funzjonalità tal estensjoni";
  const VOTAccountRequired$n = "Trid tidħol biex tuża din il karatteristika";
  const VOTMyAccount$n = "Il-kont tiegħi";
  const VOTLogin$n = "Login";
  const VOTLogout$n = "Logout";
  const VOTRefresh$n = "Aġġorna";
  const VOTYandexToken$n = "Daħħal It-Token Yandex OAuth";
  const VOTYandexTokenInfo$n = "Tista'tissettja manwalment it-token tal-kont f'dan il-qasam. Jekk jogħġbok innota li aħna ma niċċekkjawx il-validità tagħha qabel ma nibagħtu talba ta'traduzzjoni";
  const VOTLoginViaToken$n = "Idħol permezz tat-token";
  const subtitlesSmartLayout$n = "Tqassim ta'sottotitolu intelliġenti";
  const smartDucking$n = "Volum adattiv";
  const VOTAutoSubtitles$n = "Sottotitoli fuq miftuħa";
  const VOTSubtitlesFont$n = "Font tas-sottotitolu";
  const __vite_glob_0_38 = {
    recommended: recommended$n,
    translateVideo: translateVideo$n,
    disableTranslate: disableTranslate$n,
    translationSettings: translationSettings$n,
    subtitlesSettings: subtitlesSettings$n,
    resetSettings: resetSettings$n,
    videoBeingTranslated: videoBeingTranslated$n,
    videoLanguage: videoLanguage$n,
    translationLanguage: translationLanguage$n,
    translationTake: translationTake$n,
    translationTakeMoreThanHour: translationTakeMoreThanHour$n,
    translationTakeAboutMinute: translationTakeAboutMinute$n,
    translationTakeFewMinutes: translationTakeFewMinutes$n,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$n,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$n,
    requestTranslationFailed: requestTranslationFailed$n,
    audioNotReceived: audioNotReceived$n,
    audioFormatNotSupported: audioFormatNotSupported$n,
    VOTAutoTranslate: VOTAutoTranslate$n,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$n,
    VOTVolume: VOTVolume$n,
    VOTVolumeTranslation: VOTVolumeTranslation$n,
    VOTAutoSetVolume: VOTAutoSetVolume$n,
    VOTShowVideoSlider: VOTShowVideoSlider$n,
    VOTSyncVolume: VOTSyncVolume$n,
    VOTDisableFromYourLang: VOTDisableFromYourLang$n,
    VOTVideoIsTooLong: VOTVideoIsTooLong$n,
    VOTNoVideoIDFound: VOTNoVideoIDFound$n,
    VOTSubtitles: VOTSubtitles$n,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$n,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$n,
    VOTHighlightWords: VOTHighlightWords$n,
    VOTTranslatedFrom: VOTTranslatedFrom$n,
    VOTAutogenerated: VOTAutogenerated$n,
    VOTSettings: VOTSettings$n,
    VOTMenuLanguage: VOTMenuLanguage$n,
    VOTAuthors: VOTAuthors$n,
    VOTVersion: VOTVersion$n,
    VOTLoader: VOTLoader$n,
    VOTBrowser: VOTBrowser$n,
    VOTShowPiPButton: VOTShowPiPButton$n,
    langs: langs$n,
    streamNoConnectionToServer: streamNoConnectionToServer$n,
    searchField: searchField$n,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$n,
    VOTDetectService: VOTDetectService$n,
    VOTProxyWorkerHost: VOTProxyWorkerHost$n,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$n,
    proxySettings: proxySettings$n,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$n,
    VOTAudioBooster: VOTAudioBooster$n,
    VOTSubtitlesDesign: VOTSubtitlesDesign$n,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$n,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$n,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$n,
    VOTDownloadWithName: VOTDownloadWithName$n,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$n,
    VOTLocaleHash: VOTLocaleHash$n,
    VOTUpdatedAt: VOTUpdatedAt$n,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$n,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$n,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$n,
    VOTNewAudioPlayer: VOTNewAudioPlayer$n,
    VOTUseNewModel: VOTUseNewModel$n,
    TranslationDelayed: TranslationDelayed$n,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$n,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$n,
    VOTBugReport: VOTBugReport$n,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$n,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$n,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$n,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$n,
    VOTTranslatedBy: VOTTranslatedBy$n,
    VOTStreamNotAvailable: VOTStreamNotAvailable$n,
    VOTTranslationTextService: VOTTranslationTextService$n,
    VOTNotAffectToVoice: VOTNotAffectToVoice$n,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$n,
    showVideoVolumeSlider: showVideoVolumeSlider$n,
    hotkeysSettings: hotkeysSettings$n,
    None: None$n,
    VOTUseLivelyVoice: VOTUseLivelyVoice$n,
    miscSettings: miscSettings$n,
    services: services$n,
    aboutExtension: aboutExtension$n,
    appearance: appearance$n,
    buttonPosition: buttonPosition$n,
    position: position$n,
    secs: secs$n,
    autoHideButtonDelay: autoHideButtonDelay$n,
    notFound: notFound$n,
    minButtonPositionContainer: minButtonPositionContainer$n,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$n,
    PressTheKeyCombination: PressTheKeyCombination$n,
    VOTUseAudioDownload: VOTUseAudioDownload$n,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$n,
    VOTAccountRequired: VOTAccountRequired$n,
    VOTMyAccount: VOTMyAccount$n,
    VOTLogin: VOTLogin$n,
    VOTLogout: VOTLogout$n,
    VOTRefresh: VOTRefresh$n,
    VOTYandexToken: VOTYandexToken$n,
    VOTYandexTokenInfo: VOTYandexTokenInfo$n,
    VOTLoginViaToken: VOTLoginViaToken$n,
    subtitlesSmartLayout: subtitlesSmartLayout$n,
    smartDucking: smartDucking$n,
    VOTAutoSubtitles: VOTAutoSubtitles$n,
    VOTSubtitlesFont: VOTSubtitlesFont$n
  };
  const recommended$m = "အကြံပြုထားသည်";
  const translateVideo$m = "ဗီဒီယိုဘာသာပြန်";
  const disableTranslate$m = "ပိတ်လိုက်ပါ";
  const translationSettings$m = "ဘာသာပြန် setting များ";
  const subtitlesSettings$m = "စာတန်းထိုး setting များ";
  const resetSettings$m = "ပြန်လည်ချိန်ညှိခြင်း";
  const videoBeingTranslated$m = "ဗီဒီယိုကိုဘာသာပြန်နေတာပါ။";
  const videoLanguage$m = "ဗီဒီယိုဘာသာစကား";
  const translationLanguage$m = "ဘာသာပြန်ဘာသာစကား";
  const translationTake$m = "ဘာသာပြန်ဆိုမှုမှာ";
  const translationTakeMoreThanHour$m = "ဘာသာပြန်ဆိုမှုကတစ်နာရီကျော်ကြာလိမ့်မယ်။";
  const translationTakeAboutMinute$m = "ဘာသာပြန်ဆိုမှုကတစ်မိနစ်လောက်ကြာလိမ့်မယ်။";
  const translationTakeFewMinutes$m = "ဘာသာပြန်ဖို့မိနစ်အနည်းငယ်ကြာလိမ့်မယ်။";
  const translationTakeApproximatelyMinutes$m = "ဘာသာပြန်ဆိုမှုသည်{0}မိနစ်ခန့်ကြာလိမ့်မည်။";
  const translationTakeApproximatelyMinute$m = "ဘာသာပြန်ဆိုမှုသည်{0}မိနစ်ခန့်ကြာလိမ့်မည်။";
  const requestTranslationFailed$m = "ဗီဒီယိုဘာသာပြန်မှုကိုတောင်းဆိုရန်ပျက်ကွက်";
  const audioNotReceived$m = "အသံလင့်ခ်ကိုမရရှိသေးပါ။";
  const audioFormatNotSupported$m = "အသံပုံစံကိုမထောက်ပံ့ပါ။";
  const VOTAutoTranslate$m = "ပွင့်လင်းအပေါ်ဘာသာပြန်ဆို";
  const VOTDontTranslateYourLang$m = "ငါ့ဘာသာစကားကနေဘာသာပြန်မနေနဲ့။";
  const VOTVolume$m = "ဗီဒီယိုအခွေ:";
  const VOTVolumeTranslation$m = "ဘာသာပြန်စာအုပ်:";
  const VOTAutoSetVolume$m = "ဗီဒီယိုပမာဏကို";
  const VOTShowVideoSlider$m = "ဗီဒီယိုပမာဏဆလိုက်";
  const VOTSyncVolume$m = "လင့်ခ်ဘာသာပြန်နှင့်ဗီဒီယို volume ကို";
  const VOTDisableFromYourLang$m = "သင်၏ဘာသာစကားတွင်ဗီဒီယို၏ဘာသာပြန်မှုကိုပိတ်ထားသည်။";
  const VOTVideoIsTooLong$m = "ဗီဒီယိုကရှည်လွန်းတယ်။";
  const VOTNoVideoIDFound$m = "ဗီဒီယိုအမှတ်အသားမတွေ့ရ";
  const VOTSubtitles$m = "စာတန်းထိုး";
  const VOTSubtitlesDisabled$m = "မသန်စွမ်း";
  const VOTSubtitlesMaxLength$m = "စာတန်းထိုး max length";
  const VOTHighlightWords$m = "Highlight စကားလုံးများ";
  const VOTTranslatedFrom$m = "ဘာသာပြန်သည်";
  const VOTAutogenerated$m = "အလိုအလျောက်ထုတ်လုပ်ထားသော";
  const VOTSettings$m = "မဲပေးမှု Setting များ";
  const VOTMenuLanguage$m = "မီနူးဘာသာစကား";
  const VOTAuthors$m = "စာရေးဆရာများ";
  const VOTVersion$m = "ဗားရှင်း";
  const VOTLoader$m = "Loader ကို";
  const VOTBrowser$m = "ရှာဖွေရေးကိရိယာ";
  const VOTShowPiPButton$m = "Pip ခလုတ်ကိုပြပါ";
  const langs$m = { "auto": "အော်တို", "af": "အာဖရိကန်", "ak": "အာကန်", "sq": "အယ်လ်ဘားနီးယား", "am": "အမ်ဟာရစ်", "ar": "အာရပ်", "hy": "အာမေးနီးယား", "as": "အာသံ", "ay": "အိုင်မာရာ", "az": "အာဇာနည်", "bn": "ဘင်္ဂလား", "eu": "ဘတ်စကစ်", "be": "ဘီလာရုစ်", "bho": "ဘိုဂျ်ပူရီ", "bs": "ဘော့စနီးယား", "bg": "ဘူဂေးရီးယား", "my": "မြန်မာ", "ca": "ကက်တလန်", "ceb": "ဆီဘူအာနို", "zh": "တရုတ်", "zh-Hans": "တရုတ်(ရိုးရှင်းသည်)", "zh-Hant": "တရုတ်(ရိုးရာ)", "co": "ကော်စကီးယား", "hr": "ခရိုအေးရှား", "cs": "ချက်နိုင်ငံ", "da": "ဒိန်းမတ်", "dv": "ဒေဗီဟီ", "nl": "ဒတ်ချ်", "en": "အင်္ဂလိပ်", "eo": "အီစပရင်တို", "et": "အက်စတိုးနီးယား", "ee": "သိုးမ", "fil": "ဖိလစ်ပိုင်", "fi": "ဖင်လန်", "fr": "ပြင်သစ်", "gl": "ဂါလီစီယန်း", "lg": "ဂန်ဒါ", "ka": "ဂျော်ဂျီယန်", "de": "ဂျာမန်", "el": "ဂရိ", "gn": "ဂွာရန်နီ", "gu": "ဂူဂျာရတ်တီ", "ht": "ဟေတီက Creole", "ha": "ဟော်ဆာ", "haw": "ဟွာဝေး", "iw": "ဟေဗြဲ", "hi": "ဟင်ဒီ", "hmn": "ဟမ်မောင်", "hu": "ဟန်ဂေရီ", "is": "အိုက်စလန်", "ig": "အီဂိုဘို", "id": "အင်ဒိုနီးရှား", "ga": "အိုင်ယာလန်", "it": "အီတလီ", "ja": "ဂျပန်", "jv": "ဂျာဗား", "kn": "ကန်နာဒါ", "kk": "ကာဇက်စတန်", "km": "ခမာ", "rw": "ကင်ယာရဝမ်ဒါ", "ko": "ကိုရီးယား", "kri": "ခရီယို", "ku": "ကာ့ဒ်", "ky": "ကတ်ဂ်စ်", "lo": "လာအို", "la": "လက်တင်", "lv": "လတ်ဗီးယား", "ln": "လင်ဂါလာ", "lt": "လစ်ဗျား", "lb": "လစ်ဇင်ဘတ်", "mk": "မက်ဆီဒိုးနီးယား", "mg": "မာလာဂါစီ", "ms": "မလေး", "ml": "မလေးရှား", "mt": "မယ်လ်တာ", "mi": "မော်ရီ", "mr": "မာရသီ", "mn": "မွန်ဂိုလီးယား", "ne": "နီပေါ", "nso": "မြောက်ပိုင်း Sotho", "no": "နော်ဝေ", "ny": "နယန်ဂျာ", "or": "အိုဒီယာ", "om": "အိုရိုမို", "ps": "ပါရှတို", "fa": "ပါရှား", "pl": "ပိုလန်", "pt": "ပေါ်တူဂီ", "pa": "ပန်ဂျပ်ဘီ", "qu": "ကေကျူးအာ", "ro": "ရိုမေးနီးယန်း", "ru": "ရုရှား", "sm": "ဆာမိုအာ", "sa": "ဆန်စကရစ်", "gd": "စကော့တလန်ဂေးလစ်", "sr": "ဆားဘီးယား", "sn": "ရှိုနာ", "sd": "ဆိန္ဒီ", "si": "စင်္ဟာလာ", "sk": "စလိုဗက်ကီးယား", "sl": "စလိုဗေးနီးယား", "so": "ဆိုမာလီ", "st": "တောင်ပိုင်း Sotho", "es": "စပိန်", "su": "ဆွန်ဒန်", "sw": "ဆွာဟီလီ", "sv": "ဆွီဒင်", "tg": "တာဂျစ်", "ta": "တာမွေ", "tt": "တာတာ", "te": "တဲလူဂူ", "th": "ထိုင်း", "ti": "တိဂရီနီယာ", "ts": "တွန်ဂါ", "tr": "တူရကီ", "tk": "တူရကီမန်", "uk": "ယူကရိန်း", "ur": "အူဒူ", "ug": "ဥဂ်ုးလူမ်ိဳးမ်ား", "uz": "ဥဇဘက်", "vi": "ဗီယက်နမ်", "cy": "ဝေလ့စ်", "fy": "အနောက်ဖရစ်ရှန်", "xh": "ခိုဆာ", "yi": "ဂျစ်ဒစ်", "yo": "ယိုရူးဘား", "zu": "ဇူလူး" };
  const streamNoConnectionToServer$m = "ဆာဗာနှင့်ချိတ်ဆက်မှုမရှိပါ။";
  const searchField$m = "ရှာဖွေပါ။..";
  const VOTTranslateAPIErrors$m = "API မှအမှားများကိုဘာသာပြန်ပါ";
  const VOTDetectService$m = "ဘာသာစကားရှာဖွေရေးဝန်ဆောင်မှု";
  const VOTProxyWorkerHost$m = "Proxy worker လိပ်စာကိုရိုက်ထည့်ပါ";
  const VOTM3u8ProxyHost$m = "M3u8 proxy worker၏လိပ်စာကိုရိုက်ထည့်ပါ";
  const proxySettings$m = "Proxy Setting များ";
  const translationTakeApproximatelyMinute2$m = "ဘာသာပြန်ဆိုမှုသည်{0}မိနစ်ခန့်ကြာလိမ့်မည်။";
  const VOTAudioBooster$m = "ဘာသာပြန်မှုပမာဏတိုးမြှင့်";
  const VOTSubtitlesDesign$m = "စာတန်းထိုးဒီဇိုင်း";
  const VOTSubtitlesFontSize$m = "စာတန်းထိုးများ၏စာလုံးအရွယ်အစား";
  const VOTSubtitlesOpacity$m = "စာတန်းထိုးနောက်ခံ၏ပွင့်လင်းမြင်သာမှု";
  const VOTSubtitlesDownloadFormat$m = "စာတန်းထိုးများကိုဒေါင်းလုဒ်လုပ်ရန်ပုံစံ";
  const VOTDownloadWithName$m = "ဗီဒီယိုအမည်ပါဖိုင်များကိုဒေါင်းလုပ်ဆွဲပါ";
  const VOTUpdateLocaleFiles$m = "ဒေသတွင်းဖိုင်များကို Update လုပ်ပါ";
  const VOTLocaleHash$m = "ဒေသတွင်းဟက်ရှ်";
  const VOTUpdatedAt$m = "၁၆:၄၀ တွင်အတည်ပြု";
  const VOTNeedWebAudioAPI$m = "ဒါကိုဖွင့်ဖို့ Web Audio API တစ်ခုရှိရပါမယ်။";
  const VOTMediaCSPEnabledOnSite$m = "ဤဆိုက်တွင်မီဒီယာ CSP ကိုခွင့်ပြုထားသည်။";
  const VOTOnlyBypassMediaCSP$m = "မီဒီယာ CSP ကိုရှောင်လွှဲဖို့ပဲသုံးပါ။";
  const VOTNewAudioPlayer$m = "အသံဖွင့်စက်အသစ်ကိုအသုံးပြုပါ";
  const VOTUseNewModel$m = "ဗီဒီယိုအချို့အတွက် Yandex အသံများ၏စမ်းသပ်ပြောင်းလဲမှုတစ်ခုကိုအသုံးပြုပါ။";
  const TranslationDelayed$m = "ဘာသာပြန်မှုအနည်းငယ်နှောင့်နှေးပါတယ်။";
  const VOTTranslationCompletedNotify$m = "{0}မှာဘာသာပြန်ပြီးပါပြီ။";
  const VOTSendNotifyOnComplete$m = "ဗီဒီယိုကိုဘာသာပြန်ထားကြောင်းအသိပေးချက်ပို့ပါ။";
  const VOTBugReport$m = "အမှားတစ်ခုကိုအစီရင်ခံပါ";
  const VOTTranslateProxyDisabled$m = "မသန်စွမ်း";
  const VOTTranslateProxyEnabled$m = "ခွင့်ပြုထားသည်";
  const VOTTranslateProxyEverything$m = "Proxy အရာရာတိုင်း";
  const VOTTranslateProxyStatus$m = "Proxying mode ကို";
  const VOTTranslatedBy$m = "{0}မှဘာသာပြန်ထားသည်";
  const VOTStreamNotAvailable$m = "Translate stream ကိုမသုံးနိုင်ပါ။";
  const VOTTranslationTextService$m = "စာသားဘာသာပြန်ဝန်ဆောင်မှု";
  const VOTNotAffectToVoice$m = "Voice over တွင်စာသားဘာသာပြန်မှုကိုမသက်ရောက်စေပါ။";
  const DontTranslateSelectedLanguages$m = "ရွေးချယ်ထားသောဘာသာစကားများမှဘာသာပြန်၍မရပါ။";
  const showVideoVolumeSlider$m = "ဗီဒီယို volume slider ကိုပြသပါ";
  const hotkeysSettings$m = "Hotkeys setting များ";
  const None$m = "ဘာမှမရှိဘူး။";
  const VOTUseLivelyVoice$m = "အသက်ဝင်တဲ့အသံတွေသုံးပါ။ ရုရှားလိုအသံထွက်တယ်။";
  const miscSettings$m = "Misc setting များ";
  const services$m = { "yandexbrowser": "Yandex ဘရောက်ဇာ", "msedge": "Microsoft Edge ကို", "rust-server": "Rust ဆာဗာ" };
  const aboutExtension$m = "Extension အကြောင်း";
  const appearance$m = "အသွင်အပြင်";
  const buttonPosition$m = "ခလုတ်အနေအထားကို player ထဲမှာ";
  const position$m = { "left": "ဘယ်ဘက်", "right": "ဟုတ်တယ်နော်။", "top": "ထိပ်ဆုံး", "default": "မူလသတ်မှတ်ချက်" };
  const secs$m = "စက္ကန့်";
  const autoHideButtonDelay$m = "ဘာသာပြန်ခလုတ်ကိုမဖုံးကွယ်မီနှောင့်နှေးခြင်း";
  const notFound$m = "မတွေ့ရ";
  const minButtonPositionContainer$m = "ခလုတ်အနေအထားက 600 pixel ထက်ကြီးတဲ့ကစားသမားတွေမှာပဲပြောင်းလဲတယ်။";
  const VOTTranslateProxyStatusDefault$m = "သင့်နိုင်ငံတွင် proxying ကိုလုံးဝပိတ်ပင်ခြင်းသည်သက်တမ်းတိုးခြင်းကိုချိုးဖောက်နိုင်သည်။";
  const PressTheKeyCombination$m = "ခလုတ်ပေါင်းစပ်မှုကိုနှိပ်ပါ။..";
  const VOTUseAudioDownload$m = "အသံ download ကိုသုံးပါ";
  const VOTUseAudioDownloadWarning$m = "အသံဒေါင်းလုပ်များကိုပိတ်ထားခြင်းသည် extension၏လုပ်ဆောင်ချက်များကိုသက်ရောက်နိုင်သည်။";
  const VOTAccountRequired$m = "ဤလုပ်ဆောင်ချက်ကိုအသုံးပြုရန်သင်ဝင်ရောက်ရန်လိုအပ်သည်။";
  const VOTMyAccount$m = "ငါ့အကောင့်";
  const VOTLogin$m = "ဝင်ရောက်ပါ";
  const VOTLogout$m = "Logout";
  const VOTRefresh$m = "Refresh";
  const VOTYandexToken$m = "Yandex OAuth Token ကိုထည့်ပါ";
  const VOTYandexTokenInfo$m = "ဒီကွင်းမှာအကောင့် token ကိုလက်နဲ့သတ်မှတ်နိုင်ပါတယ်။ ကျေးဇူးပြု၍မှတ်ထားရန်မှာဘာသာပြန်တောင်းဆိုမှုမပို့မီ၎င်း၏မှန်ကန်မှုကိုကျွန်ုပ်တို့စစ်ဆေးခြင်းမရှိပါ။";
  const VOTLoginViaToken$m = "Token မှတဆင့်ဝင်ရောက်ပါ";
  const subtitlesSmartLayout$m = "စမတ်စာတန်းထိုး layout ကို";
  const smartDucking$m = "အလိုက်သင့်ပြောင်းလဲနိုင်သောပမာဏ";
  const VOTAutoSubtitles$m = "Open မှာစာတန်းထိုးထားတယ်။";
  const VOTSubtitlesFont$m = "စာတန်းထိုးစာလုံး";
  const __vite_glob_0_39 = {
    recommended: recommended$m,
    translateVideo: translateVideo$m,
    disableTranslate: disableTranslate$m,
    translationSettings: translationSettings$m,
    subtitlesSettings: subtitlesSettings$m,
    resetSettings: resetSettings$m,
    videoBeingTranslated: videoBeingTranslated$m,
    videoLanguage: videoLanguage$m,
    translationLanguage: translationLanguage$m,
    translationTake: translationTake$m,
    translationTakeMoreThanHour: translationTakeMoreThanHour$m,
    translationTakeAboutMinute: translationTakeAboutMinute$m,
    translationTakeFewMinutes: translationTakeFewMinutes$m,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$m,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$m,
    requestTranslationFailed: requestTranslationFailed$m,
    audioNotReceived: audioNotReceived$m,
    audioFormatNotSupported: audioFormatNotSupported$m,
    VOTAutoTranslate: VOTAutoTranslate$m,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$m,
    VOTVolume: VOTVolume$m,
    VOTVolumeTranslation: VOTVolumeTranslation$m,
    VOTAutoSetVolume: VOTAutoSetVolume$m,
    VOTShowVideoSlider: VOTShowVideoSlider$m,
    VOTSyncVolume: VOTSyncVolume$m,
    VOTDisableFromYourLang: VOTDisableFromYourLang$m,
    VOTVideoIsTooLong: VOTVideoIsTooLong$m,
    VOTNoVideoIDFound: VOTNoVideoIDFound$m,
    VOTSubtitles: VOTSubtitles$m,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$m,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$m,
    VOTHighlightWords: VOTHighlightWords$m,
    VOTTranslatedFrom: VOTTranslatedFrom$m,
    VOTAutogenerated: VOTAutogenerated$m,
    VOTSettings: VOTSettings$m,
    VOTMenuLanguage: VOTMenuLanguage$m,
    VOTAuthors: VOTAuthors$m,
    VOTVersion: VOTVersion$m,
    VOTLoader: VOTLoader$m,
    VOTBrowser: VOTBrowser$m,
    VOTShowPiPButton: VOTShowPiPButton$m,
    langs: langs$m,
    streamNoConnectionToServer: streamNoConnectionToServer$m,
    searchField: searchField$m,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$m,
    VOTDetectService: VOTDetectService$m,
    VOTProxyWorkerHost: VOTProxyWorkerHost$m,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$m,
    proxySettings: proxySettings$m,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$m,
    VOTAudioBooster: VOTAudioBooster$m,
    VOTSubtitlesDesign: VOTSubtitlesDesign$m,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$m,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$m,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$m,
    VOTDownloadWithName: VOTDownloadWithName$m,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$m,
    VOTLocaleHash: VOTLocaleHash$m,
    VOTUpdatedAt: VOTUpdatedAt$m,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$m,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$m,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$m,
    VOTNewAudioPlayer: VOTNewAudioPlayer$m,
    VOTUseNewModel: VOTUseNewModel$m,
    TranslationDelayed: TranslationDelayed$m,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$m,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$m,
    VOTBugReport: VOTBugReport$m,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$m,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$m,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$m,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$m,
    VOTTranslatedBy: VOTTranslatedBy$m,
    VOTStreamNotAvailable: VOTStreamNotAvailable$m,
    VOTTranslationTextService: VOTTranslationTextService$m,
    VOTNotAffectToVoice: VOTNotAffectToVoice$m,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$m,
    showVideoVolumeSlider: showVideoVolumeSlider$m,
    hotkeysSettings: hotkeysSettings$m,
    None: None$m,
    VOTUseLivelyVoice: VOTUseLivelyVoice$m,
    miscSettings: miscSettings$m,
    services: services$m,
    aboutExtension: aboutExtension$m,
    appearance: appearance$m,
    buttonPosition: buttonPosition$m,
    position: position$m,
    secs: secs$m,
    autoHideButtonDelay: autoHideButtonDelay$m,
    notFound: notFound$m,
    minButtonPositionContainer: minButtonPositionContainer$m,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$m,
    PressTheKeyCombination: PressTheKeyCombination$m,
    VOTUseAudioDownload: VOTUseAudioDownload$m,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$m,
    VOTAccountRequired: VOTAccountRequired$m,
    VOTMyAccount: VOTMyAccount$m,
    VOTLogin: VOTLogin$m,
    VOTLogout: VOTLogout$m,
    VOTRefresh: VOTRefresh$m,
    VOTYandexToken: VOTYandexToken$m,
    VOTYandexTokenInfo: VOTYandexTokenInfo$m,
    VOTLoginViaToken: VOTLoginViaToken$m,
    subtitlesSmartLayout: subtitlesSmartLayout$m,
    smartDucking: smartDucking$m,
    VOTAutoSubtitles: VOTAutoSubtitles$m,
    VOTSubtitlesFont: VOTSubtitlesFont$m
  };
  const recommended$l = "सिफारिस गरिएको";
  const translateVideo$l = "भिडियो अनुवाद गर्नुहोस्";
  const disableTranslate$l = "बन्द गर्नुहोस्";
  const translationSettings$l = "अनुवाद सेटिङ";
  const subtitlesSettings$l = "उपशीर्षक सेटिङ";
  const resetSettings$l = "सेटिङ रिसेट गर्नुहोस्";
  const videoBeingTranslated$l = "भिडियो अनुवाद";
  const videoLanguage$l = "भिडियो भाषा";
  const translationLanguage$l = "अनुवाद भाषा";
  const translationTake$l = "अनुवाद हुनेछ";
  const translationTakeMoreThanHour$l = "अनुवाद एक घण्टा भन्दा बढी समय लाग्नेछ";
  const translationTakeAboutMinute$l = "अनुवाद लगभग एक मिनेट हुनेछ";
  const translationTakeFewMinutes$l = "अनुवाद केही मिनेट हुनेछ";
  const translationTakeApproximatelyMinutes$l = "अनुवाद लगभग {0} मिनेट लाग्नेछ";
  const translationTakeApproximatelyMinute$l = "अनुवाद लगभग {0} मिनेट लाग्नेछ";
  const requestTranslationFailed$l = "भिडियो अनुवाद अनुरोध गर्न असफल भयो";
  const audioNotReceived$l = "अडियो लिङ्क प्राप्त भएन";
  const audioFormatNotSupported$l = "अडियो ढाँचा समर्थित छैन";
  const VOTAutoTranslate$l = "खुला मा अनुवाद गर्नुहोस्";
  const VOTDontTranslateYourLang$l = "मेरो भाषाबाट अनुवाद नगर्नुहोस्";
  const VOTVolume$l = "भिडियो भोल्युम:";
  const VOTVolumeTranslation$l = "अनुवादको मात्रा:";
  const VOTAutoSetVolume$l = "भिडियो भोल्युम घटाउनुहोस्";
  const VOTShowVideoSlider$l = "भिडियो भोल्युम स्लाइडर";
  const VOTSyncVolume$l = "लिङ्क अनुवाद र भिडियो भोल्युम";
  const VOTDisableFromYourLang$l = "तपाईँले आफ्नो भाषामा भिडियोको अनुवाद अक्षम पार्नुभएको छ";
  const VOTVideoIsTooLong$l = "भिडियो धेरै लामो छ";
  const VOTNoVideoIDFound$l = "भिडियो आईडी फेला परेन";
  const VOTSubtitles$l = "उपशीर्षकहरू";
  const VOTSubtitlesDisabled$l = "अक्षम पारिएको";
  const VOTSubtitlesMaxLength$l = "उपशीर्षक अधिकतम लम्बाइ";
  const VOTHighlightWords$l = "शब्दहरू हाइलाइट गर्नुहोस्";
  const VOTTranslatedFrom$l = "अनुवादित";
  const VOTAutogenerated$l = "स्वतः उत्पन्न";
  const VOTSettings$l = "भोट सेटिङ";
  const VOTMenuLanguage$l = "मेनु भाषा";
  const VOTAuthors$l = "लेखकहरू";
  const VOTVersion$l = "संस्करण";
  const VOTLoader$l = "लोडर";
  const VOTBrowser$l = "ब्राउजर";
  const VOTShowPiPButton$l = "पिप बटन देखाउनुहोस्";
  const langs$l = { "auto": "स्वत:", "af": "अफ्रिकान्स", "ak": "अकान", "sq": "अल्बानियाली", "am": "अम्हारी", "ar": "अरबी", "hy": "अर्मेनियाली", "as": "असमिया", "ay": "अायमरा", "az": "अजरबैजानी", "bn": "बङ्गाली", "eu": "बास्क", "be": "बेलारूसी", "bho": "भोजपुरी", "bs": "बोस्नियाली", "bg": "बुल्गारियाली", "my": "बर्मेली", "ca": "क्याटालान", "ceb": "सेबुआनो", "zh": "चिनियाँ", "zh-Hans": "चिनियाँ (सरलीकृत)", "zh-Hant": "चिनियाँ (परम्परागत)", "co": "कोर्सिकन", "hr": "क्रोएसियाली", "cs": "चेक", "da": "डेनमार्क", "dv": "दिवेही", "nl": "डच भाषा", "en": "अङ्ग्रेजी", "eo": "एस्पेरान्टो", "et": "एस्टोनियाली", "ee": "भेडा", "fil": "फिलिपिनो", "fi": "फिनल्यान्ड", "fr": "फ्रान्सेली", "gl": "गालिसियन", "lg": "गान्डा", "ka": "जर्जियाली", "de": "जर्मन भाषा", "el": "ग्रीक", "gn": "गुराँई", "gu": "गुजराती", "ht": "हाइटी क्रियोल", "ha": "हाउसा", "haw": "हवाईयन", "iw": "हिब्रू", "hi": "हिन्दी", "hmn": "ह्मोङ", "hu": "हंगेरी", "is": "आइसल्याण्ड", "ig": "इग्बो", "id": "इन्डोनेसियाली", "ga": "आइरिस", "it": "इटालियन", "ja": "जापानी", "jv": "जाभा", "kn": "कन्नड", "kk": "कजाख", "km": "खमेर", "rw": "किन्नारवाण्डा", "ko": "कोरियाली", "kri": "क्रिओ", "ku": "कुर्दिस", "ky": "किर्गिज", "lo": "लाओ", "la": "ल्याटिन", "lv": "लातवियाली", "ln": "लिङ्गाला", "lt": "लिथुआनियाली", "lb": "लक्जमबर्ग", "mk": "म्यासेडोनियाली", "mg": "मालागासी", "ms": "मलेसियाली", "ml": "मलयालम", "mt": "माल्टाली", "mi": "माओरी", "mr": "मराठी", "mn": "मंगोलियाली", "ne": "नेपाली", "nso": "उत्तरी सोथो", "no": "नर्वेजियन", "ny": "न्यानजा", "or": "ओडिया", "om": "ओरोमो", "ps": "पश्तो", "fa": "फारसी", "pl": "पोलिश", "pt": "पोर्चुगिज", "pa": "पञ्जाबी", "qu": "क्वेचुआ", "ro": "रोमानियाली", "ru": "रुसी भाषा", "sm": "सामोआ", "sa": "संस्कृत", "gd": "स्कटिश गेलिक", "sr": "सर्बियाली", "sn": "शोन", "sd": "सिन्धी", "si": "सिङ्गाली", "sk": "स्लोभाक", "sl": "स्लोभेनियाली", "so": "सोमाली", "st": "दक्षिणी सोथो", "es": "स्पेनी", "su": "सुन्दानी", "sw": "स्वाहिली", "sv": "स्वीडिश", "tg": "ताजिक", "ta": "तमिल", "tt": "टाटार", "te": "तेलुगु", "th": "थाई", "ti": "टिग्रिनिया", "ts": "त्सोङ्गा", "tr": "टर्की", "tk": "तुर्कमेन", "uk": "युक्रेनी", "ur": "उर्दू", "ug": "उइघुर", "uz": "उज्बेक", "vi": "भियतनामी", "cy": "वेल्श", "fy": "पश्चिमी फ्रिजियन", "xh": "खोसा", "yi": "यडिश", "yo": "योरुबा", "zu": "जुलु" };
  const streamNoConnectionToServer$l = "सर्भरसँग कुनै सम्बन्ध छैन";
  const searchField$l = "खोजी गर्नुहोस्।..";
  const VOTTranslateAPIErrors$l = "एपीआईबाट त्रुटिहरू अनुवाद गर्नुहोस्";
  const VOTDetectService$l = "भाषा पत्ता लगाउने सेवा";
  const VOTProxyWorkerHost$l = "प्रोक्सी कार्यकर्ताको ठेगाना प्रविष्ट गर्नुहोस्";
  const VOTM3u8ProxyHost$l = "M3u8 प्रोक्सी कार्यकर्ताको ठेगाना प्रविष्ट गर्नुहोस्";
  const proxySettings$l = "प्रोक्सी सेटिङ";
  const translationTakeApproximatelyMinute2$l = "अनुवाद लगभग {0} मिनेट लाग्नेछ";
  const VOTAudioBooster$l = "विस्तारित अनुवाद मात्रा वृद्धि";
  const VOTSubtitlesDesign$l = "उपशीर्षक डिजाइन";
  const VOTSubtitlesFontSize$l = "उपशीर्षकहरूको फन्ट साइज";
  const VOTSubtitlesOpacity$l = "उपशीर्षक पृष्ठभूमिको पारदर्शिता";
  const VOTSubtitlesDownloadFormat$l = "उपशीर्षक डाउनलोड गर्नका लागि ढाँचा";
  const VOTDownloadWithName$l = "भिडियो नाम सहित फाइलहरू डाउनलोड गर्नुहोस्";
  const VOTUpdateLocaleFiles$l = "स्थानीयकरण फाइलहरू अद्यावधिक गर्नुहोस्";
  const VOTLocaleHash$l = "स्थानीय ह्यास";
  const VOTUpdatedAt$l = "मा अपडेट गरिएको";
  const VOTNeedWebAudioAPI$l = "यो सक्षम गर्न, तपाईं एक वेब अडियो एपीआई हुनुपर्छ";
  const VOTMediaCSPEnabledOnSite$l = "यस साइटमा मिडिया सीएसपी सक्षम छ";
  const VOTOnlyBypassMediaCSP$l = "यसलाई केवल मिडिया सीएसपीलाई बाइपास गर्नका लागि प्रयोग गर्नुहोस्";
  const VOTNewAudioPlayer$l = "नयाँ अडियो प्लेयर प्रयोग गर्नुहोस्";
  const VOTUseNewModel$l = "केही भिडियोहरूको लागि यान्डेक्स आवाजहरूको प्रयोगात्मक भिन्नता प्रयोग गर्नुहोस्";
  const TranslationDelayed$l = "अनुवाद केही ढिलाइ भएको छ";
  const VOTTranslationCompletedNotify$l = "{0} मा अनुवाद पूरा भएको छ!";
  const VOTSendNotifyOnComplete$l = "भिडियो अनुवाद गरिएको सूचना पठाउनुहोस्";
  const VOTBugReport$l = "बग रिपोर्ट गर्नुहोस्";
  const VOTTranslateProxyDisabled$l = "अक्षम पारिएको";
  const VOTTranslateProxyEnabled$l = "सक्षम पारिएको";
  const VOTTranslateProxyEverything$l = "प्रोक्सी सबै कुरा";
  const VOTTranslateProxyStatus$l = "प्रोक्सी मोड";
  const VOTTranslatedBy$l = "{0}द्वारा अनुवाद गरिएको";
  const VOTStreamNotAvailable$l = "अनुवाद प्रवाह उपलब्ध छैन";
  const VOTTranslationTextService$l = "पाठ अनुवाद सेवा";
  const VOTNotAffectToVoice$l = "आवाज माथि पाठ को अनुवाद असर गर्दैन";
  const DontTranslateSelectedLanguages$l = "चयन गरिएका भाषाहरूबाट अनुवाद नगर्नुहोस्";
  const showVideoVolumeSlider$l = "भिडियो भोल्युम स्लाइडर प्रदर्शन गर्नुहोस्";
  const hotkeysSettings$l = "हटकी सेटिङ";
  const None$l = "कुनै पनि";
  const VOTUseLivelyVoice$l = "जीवन्त आवाज प्रयोग गर्नुहोस् ।  स्पिकरहरू मूल निवासी रुसीहरू जस्तै सुनिन्छन् । ";
  const miscSettings$l = "विविध सेटिङ";
  const services$l = { "yandexbrowser": "यान्डेक्स ब्राउजर", "msedge": "माइक्रोसफ्ट एज", "rust-server": "रस्ट सर्भर" };
  const aboutExtension$l = "विस्तारको बारेमा";
  const appearance$l = "उपस्थिति";
  const buttonPosition$l = "प्लेयरमा बटन स्थिति";
  const position$l = { "left": "बायाँ", "right": "दायाँ", "top": "माथि", "default": "पूर्वनिर्धारित" };
  const secs$l = "सेकेन्ड";
  const autoHideButtonDelay$l = "अनुवाद बटन लुकाउनु अघि ढिलाइ";
  const notFound$l = "फेला परेन";
  const minButtonPositionContainer$l = "बटनको स्थिति केवल 600 पिक्सेल भन्दा ठूला खेलाडीहरूमा परिवर्तन हुन्छ । ";
  const VOTTranslateProxyStatusDefault$l = "पूर्णतया आफ्नो देश मा प्रोक्सी अक्षम विस्तार तोड्न सक्छ";
  const PressTheKeyCombination$l = "कुञ्जी संयोजन थिच्नुहोस्।..";
  const VOTUseAudioDownload$l = "अडियो डाउनलोड प्रयोग गर्नुहोस्";
  const VOTUseAudioDownloadWarning$l = "अडियो डाउनलोडहरू असक्षम पार्दा एक्सटेन्सनको कार्यक्षमतामा असर पर्न सक्छ";
  const VOTAccountRequired$l = "यो सुविधा प्रयोग गर्न तपाईँले लग इन गर्नु आवश्यक छ";
  const VOTMyAccount$l = "मेरो खाता";
  const VOTLogin$l = "लगइन गर्नुहोस्";
  const VOTLogout$l = "लगआउट";
  const VOTRefresh$l = "ताजा पार्नुहोस्";
  const VOTYandexToken$l = "यान्डेक्स ओआथ टोकन प्रविष्ट गर्नुहोस्";
  const VOTYandexTokenInfo$l = "तपाईं यस क्षेत्रमा खाता टोकन म्यानुअल रूपमा सेट गर्न सक्नुहुन्छ ।  कृपया ध्यान दिनुहोस् कि हामी अनुवाद अनुरोध पठाउनु अघि यसको वैधता जाँच गर्दैनौं";
  const VOTLoginViaToken$l = "टोकन मार्फत लगइन गर्नुहोस् । ";
  const subtitlesSmartLayout$l = "स्मार्ट उपशीर्षक लेआउट";
  const smartDucking$l = "अनुकूलनशील मात्रा";
  const VOTAutoSubtitles$l = "उपशीर्षकहरू खोल्नुहोस्";
  const VOTSubtitlesFont$l = "उपशीर्षक फन्ट";
  const __vite_glob_0_40 = {
    recommended: recommended$l,
    translateVideo: translateVideo$l,
    disableTranslate: disableTranslate$l,
    translationSettings: translationSettings$l,
    subtitlesSettings: subtitlesSettings$l,
    resetSettings: resetSettings$l,
    videoBeingTranslated: videoBeingTranslated$l,
    videoLanguage: videoLanguage$l,
    translationLanguage: translationLanguage$l,
    translationTake: translationTake$l,
    translationTakeMoreThanHour: translationTakeMoreThanHour$l,
    translationTakeAboutMinute: translationTakeAboutMinute$l,
    translationTakeFewMinutes: translationTakeFewMinutes$l,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$l,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$l,
    requestTranslationFailed: requestTranslationFailed$l,
    audioNotReceived: audioNotReceived$l,
    audioFormatNotSupported: audioFormatNotSupported$l,
    VOTAutoTranslate: VOTAutoTranslate$l,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$l,
    VOTVolume: VOTVolume$l,
    VOTVolumeTranslation: VOTVolumeTranslation$l,
    VOTAutoSetVolume: VOTAutoSetVolume$l,
    VOTShowVideoSlider: VOTShowVideoSlider$l,
    VOTSyncVolume: VOTSyncVolume$l,
    VOTDisableFromYourLang: VOTDisableFromYourLang$l,
    VOTVideoIsTooLong: VOTVideoIsTooLong$l,
    VOTNoVideoIDFound: VOTNoVideoIDFound$l,
    VOTSubtitles: VOTSubtitles$l,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$l,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$l,
    VOTHighlightWords: VOTHighlightWords$l,
    VOTTranslatedFrom: VOTTranslatedFrom$l,
    VOTAutogenerated: VOTAutogenerated$l,
    VOTSettings: VOTSettings$l,
    VOTMenuLanguage: VOTMenuLanguage$l,
    VOTAuthors: VOTAuthors$l,
    VOTVersion: VOTVersion$l,
    VOTLoader: VOTLoader$l,
    VOTBrowser: VOTBrowser$l,
    VOTShowPiPButton: VOTShowPiPButton$l,
    langs: langs$l,
    streamNoConnectionToServer: streamNoConnectionToServer$l,
    searchField: searchField$l,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$l,
    VOTDetectService: VOTDetectService$l,
    VOTProxyWorkerHost: VOTProxyWorkerHost$l,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$l,
    proxySettings: proxySettings$l,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$l,
    VOTAudioBooster: VOTAudioBooster$l,
    VOTSubtitlesDesign: VOTSubtitlesDesign$l,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$l,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$l,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$l,
    VOTDownloadWithName: VOTDownloadWithName$l,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$l,
    VOTLocaleHash: VOTLocaleHash$l,
    VOTUpdatedAt: VOTUpdatedAt$l,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$l,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$l,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$l,
    VOTNewAudioPlayer: VOTNewAudioPlayer$l,
    VOTUseNewModel: VOTUseNewModel$l,
    TranslationDelayed: TranslationDelayed$l,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$l,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$l,
    VOTBugReport: VOTBugReport$l,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$l,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$l,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$l,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$l,
    VOTTranslatedBy: VOTTranslatedBy$l,
    VOTStreamNotAvailable: VOTStreamNotAvailable$l,
    VOTTranslationTextService: VOTTranslationTextService$l,
    VOTNotAffectToVoice: VOTNotAffectToVoice$l,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$l,
    showVideoVolumeSlider: showVideoVolumeSlider$l,
    hotkeysSettings: hotkeysSettings$l,
    None: None$l,
    VOTUseLivelyVoice: VOTUseLivelyVoice$l,
    miscSettings: miscSettings$l,
    services: services$l,
    aboutExtension: aboutExtension$l,
    appearance: appearance$l,
    buttonPosition: buttonPosition$l,
    position: position$l,
    secs: secs$l,
    autoHideButtonDelay: autoHideButtonDelay$l,
    notFound: notFound$l,
    minButtonPositionContainer: minButtonPositionContainer$l,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$l,
    PressTheKeyCombination: PressTheKeyCombination$l,
    VOTUseAudioDownload: VOTUseAudioDownload$l,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$l,
    VOTAccountRequired: VOTAccountRequired$l,
    VOTMyAccount: VOTMyAccount$l,
    VOTLogin: VOTLogin$l,
    VOTLogout: VOTLogout$l,
    VOTRefresh: VOTRefresh$l,
    VOTYandexToken: VOTYandexToken$l,
    VOTYandexTokenInfo: VOTYandexTokenInfo$l,
    VOTLoginViaToken: VOTLoginViaToken$l,
    subtitlesSmartLayout: subtitlesSmartLayout$l,
    smartDucking: smartDucking$l,
    VOTAutoSubtitles: VOTAutoSubtitles$l,
    VOTSubtitlesFont: VOTSubtitlesFont$l
  };
  const recommended$k = "aanbevolen";
  const translateVideo$k = "Video vertalen";
  const disableTranslate$k = "Uitgeschakeld";
  const translationSettings$k = "Vertaal instellingen";
  const subtitlesSettings$k = "Instellingen voor ondertitels";
  const resetSettings$k = "Instellingen resetten";
  const videoBeingTranslated$k = "De video wordt vertaald";
  const videoLanguage$k = "Video taal";
  const translationLanguage$k = "Vertaling taal";
  const translationTake$k = "De vertaling zal";
  const translationTakeMoreThanHour$k = "De vertaling duurt meer dan een uur";
  const translationTakeAboutMinute$k = "De vertaling duurt ongeveer een minuut";
  const translationTakeFewMinutes$k = "De vertaling duurt enkele minuten";
  const translationTakeApproximatelyMinutes$k = "De vertaling duurt ongeveer {0} minuten";
  const translationTakeApproximatelyMinute$k = "De vertaling duurt ongeveer {0} minuten";
  const requestTranslationFailed$k = "Verzoek om videovertaling is mislukt";
  const audioNotReceived$k = "Audioverbinding niet ontvangen";
  const audioFormatNotSupported$k = "Het audioformaat wordt niet ondersteund";
  const VOTAutoTranslate$k = "Vertalen op open";
  const VOTDontTranslateYourLang$k = "Niet vertalen vanuit mijn taal";
  const VOTVolume$k = "Video volume:";
  const VOTVolumeTranslation$k = "Vertaling volume:";
  const VOTAutoSetVolume$k = "Verminder het videovolume naar";
  const VOTShowVideoSlider$k = "Video volume slider";
  const VOTSyncVolume$k = "Linkvertaling en videovolume";
  const VOTDisableFromYourLang$k = "U hebt de vertaling van de video in uw taal uitgeschakeld";
  const VOTVideoIsTooLong$k = "Video is te lang";
  const VOTNoVideoIDFound$k = "Geen video-ID gevonden";
  const VOTSubtitles$k = "Ondertitels";
  const VOTSubtitlesDisabled$k = "Gehandicapten";
  const VOTSubtitlesMaxLength$k = "Ondertitels max lengte";
  const VOTHighlightWords$k = "Woorden markeren";
  const VOTTranslatedFrom$k = "vertaald uit";
  const VOTAutogenerated$k = "autogenerated";
  const VOTSettings$k = "Vot Instellingen";
  const VOTMenuLanguage$k = "Menu taal";
  const VOTAuthors$k = "Auteur";
  const VOTVersion$k = "Versie";
  const VOTLoader$k = "Loader";
  const VOTBrowser$k = "Browser";
  const VOTShowPiPButton$k = "Toon PiP knop";
  const langs$k = { "auto": "Auto", "af": "Afrikaans", "ak": "Akan", "sq": "Albanees", "am": "Amhaars", "ar": "Arabisch", "hy": "Armeense", "as": "Assamese", "ay": "Aymara", "az": "Azerbeidzjaanse", "bn": "Bangla", "eu": "Baskische", "be": "Belarussische", "bho": "Bhojpuri", "bs": "Bosnisch", "bg": "Bulgaarse", "my": "Birmese", "ca": "Catalaanse", "ceb": "Cebuano", "zh": "Chinees", "zh-Hans": "Chinees (Vereenvoudigd)", "zh-Hant": "Chinees (Traditioneel)", "co": "Corsica", "hr": "Kroatisch", "cs": "Tsjechisch", "da": "Deens", "dv": "Divehi", "nl": "Nederlands", "en": "Engels", "eo": "Esperanto", "et": "Estlandse", "ee": "Ooi", "fil": "Filippijnse", "fi": "Fins", "fr": "Frans", "gl": "Galicische", "lg": "Ganda", "ka": "Georgisch", "de": "Duits", "el": "Griekse", "gn": "Guarani", "gu": "Gujarati", "ht": "Haïtiaans Creools", "ha": "Hausa", "haw": "Hawaiiaanse", "iw": "Hebreeuws", "hi": "Hindi", "hmn": "Hmong", "hu": "Hongaarse", "is": "IJslands", "ig": "Igbo", "id": "Indonesisch", "ga": "Iers", "it": "Italiaans", "ja": "Japans", "jv": "Javaanse", "kn": "Kannada", "kk": "Kazachse", "km": "Khmer", "rw": "Kinyarwanda", "ko": "Koreaanse", "kri": "Krio", "ku": "Koerdisch", "ky": "Kirgizische", "lo": "Laotiaanse", "la": "Latijn", "lv": "Letse", "ln": "Lingala", "lt": "Litouwse", "lb": "Luxemburgse", "mk": "Macedoniër", "mg": "Madagascar", "ms": "Maleis", "ml": "Malayalam", "mt": "Malta", "mi": "Maori", "mr": "Marathi", "mn": "Mongools", "ne": "Nepalese", "nso": "Noord-Sotho", "no": "Noors", "ny": "Nyanja", "or": "Odia", "om": "Oromo", "ps": "Pashto", "fa": "Perzisch", "pl": "Pools", "pt": "Portugees", "pa": "Punjabi", "qu": "Quechua", "ro": "Roemeense", "ru": "Russisch", "sm": "Samoa", "sa": "Sanskriet", "gd": "Schots Gaelic", "sr": "Servische", "sn": "Shona", "sd": "Sindhi", "si": "Sinhala", "sk": "Slowaakse", "sl": "Sloveense", "so": "Somalisch", "st": "Zuid-Sotho", "es": "Spaans", "su": "Sundanese", "sw": "Swahili", "sv": "Zweeds", "tg": "Tadzjiekse", "ta": "Tamil", "tt": "Tataren", "te": "Telugu", "th": "Thais", "ti": "Tigrinya", "ts": "Tsonga", "tr": "Turks", "tk": "Turkmeense", "uk": "Oekraïense", "ur": "Urdu", "ug": "Oeigoerse", "uz": "Oezbeekse", "vi": "Vietnamees", "cy": "Welshe", "fy": "West-Fries", "xh": "Xhosa", "yi": "Jiddisch", "yo": "Yoruba", "zu": "Zulu" };
  const streamNoConnectionToServer$k = "Er is geen verbinding met de server";
  const searchField$k = "Zoeken...";
  const VOTTranslateAPIErrors$k = "Vertaal fouten uit de API";
  const VOTDetectService$k = "Language detection service";
  const VOTProxyWorkerHost$k = "Voer het proxy worker-adres in";
  const VOTM3u8ProxyHost$k = "Voer het adres van de m3u8 proxy worker in";
  const proxySettings$k = "Proxy-Instellingen";
  const translationTakeApproximatelyMinute2$k = "De vertaling duurt ongeveer {0} minuten";
  const VOTAudioBooster$k = "Uitbreiding van het vertaalvolume";
  const VOTSubtitlesDesign$k = "Ondertitels ontwerp";
  const VOTSubtitlesFontSize$k = "Lettergrootte van ondertitels";
  const VOTSubtitlesOpacity$k = "Transparantie van de ondertitelingsachtergrond";
  const VOTSubtitlesDownloadFormat$k = "Het formaat voor het downloaden van ondertitels";
  const VOTDownloadWithName$k = "Download bestanden met de naam van de video";
  const VOTUpdateLocaleFiles$k = "Lokalisatiebestanden bijwerken";
  const VOTLocaleHash$k = "Lokale hash";
  const VOTUpdatedAt$k = "Bijgewerkt op";
  const VOTNeedWebAudioAPI$k = "Om dit in te schakelen, moet u een Web Audio API hebben";
  const VOTMediaCSPEnabledOnSite$k = "Media CSP is ingeschakeld op deze site";
  const VOTOnlyBypassMediaCSP$k = "Gebruik het alleen voor het omzeilen van Media CSP";
  const VOTNewAudioPlayer$k = "De nieuwe audiospeler gebruiken";
  const VOTUseNewModel$k = "Gebruik een experimentele variant van Yandex voices voor sommige video ' s";
  const TranslationDelayed$k = "De vertaling is iets vertraagd";
  const VOTTranslationCompletedNotify$k = "De vertaling op {0} is voltooid!";
  const VOTSendNotifyOnComplete$k = "Stuur een melding dat de video is vertaald";
  const VOTBugReport$k = "Meld een bug";
  const VOTTranslateProxyDisabled$k = "Gehandicapten";
  const VOTTranslateProxyEnabled$k = "Ingeschakeld";
  const VOTTranslateProxyEverything$k = "Proxy alles";
  const VOTTranslateProxyStatus$k = "Proxy-modus";
  const VOTTranslatedBy$k = "Vertaald door {0}";
  const VOTStreamNotAvailable$k = "Translate stream is niet beschikbaar";
  const VOTTranslationTextService$k = "Tekst vertaaldienst";
  const VOTNotAffectToVoice$k = "Heeft geen invloed op de vertaling van tekst in voice over";
  const DontTranslateSelectedLanguages$k = "Niet vertalen vanuit geselecteerde talen";
  const showVideoVolumeSlider$k = "De schuifregelaar videovolume weergeven";
  const hotkeysSettings$k = "Sneltoetsen instellingen";
  const None$k = "Niemand";
  const VOTUseLivelyVoice$k = "Gebruik levendige stemmen. Sprekers klinken als inheemse Russen.";
  const miscSettings$k = "Overige instellingen";
  const services$k = { "yandexbrowser": "Yandex Browser", "msedge": "Microsoft Edge", "rust-server": "Rust Server" };
  const aboutExtension$k = "Over extensie";
  const appearance$k = "Uiterlijk";
  const buttonPosition$k = "Knoppositie in de speler";
  const position$k = { "left": "Links", "right": "Recht", "top": "Top", "default": "Standaard" };
  const secs$k = "secs";
  const autoHideButtonDelay$k = "Vertraging voordat u de vertaal knop verbergt";
  const notFound$k = "niet gevonden";
  const minButtonPositionContainer$k = "De positie van de knop verandert alleen bij spelers groter dan 600 pixels.";
  const VOTTranslateProxyStatusDefault$k = "Het volledig uitschakelen van proxy ' s in uw land kan de extensie breken";
  const PressTheKeyCombination$k = "Druk op de toetsencombinatie...";
  const VOTUseAudioDownload$k = "Audio downloaden gebruiken";
  const VOTUseAudioDownloadWarning$k = "Het uitschakelen van audiodownloads kan de functionaliteit van de extensie beïnvloeden";
  const VOTAccountRequired$k = "U moet inloggen om deze functie te gebruiken";
  const VOTMyAccount$k = "Mijn account";
  const VOTLogin$k = "Login";
  const VOTLogout$k = "Afmelden";
  const VOTRefresh$k = "Vernieuwen";
  const VOTYandexToken$k = "Voer de Yandex OAuth Token in";
  const VOTYandexTokenInfo$k = "U kunt het account-token handmatig instellen in dit veld. Houd er rekening mee dat we de geldigheid ervan niet controleren voordat we een vertaal verzoek sturen";
  const VOTLoginViaToken$k = "Inloggen via token";
  const subtitlesSmartLayout$k = "Smart subtitle layout";
  const smartDucking$k = "Adaptief volume";
  const VOTAutoSubtitles$k = "Ondertitels op open";
  const VOTSubtitlesFont$k = "Ondertiteling lettertype";
  const __vite_glob_0_41 = {
    recommended: recommended$k,
    translateVideo: translateVideo$k,
    disableTranslate: disableTranslate$k,
    translationSettings: translationSettings$k,
    subtitlesSettings: subtitlesSettings$k,
    resetSettings: resetSettings$k,
    videoBeingTranslated: videoBeingTranslated$k,
    videoLanguage: videoLanguage$k,
    translationLanguage: translationLanguage$k,
    translationTake: translationTake$k,
    translationTakeMoreThanHour: translationTakeMoreThanHour$k,
    translationTakeAboutMinute: translationTakeAboutMinute$k,
    translationTakeFewMinutes: translationTakeFewMinutes$k,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$k,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$k,
    requestTranslationFailed: requestTranslationFailed$k,
    audioNotReceived: audioNotReceived$k,
    audioFormatNotSupported: audioFormatNotSupported$k,
    VOTAutoTranslate: VOTAutoTranslate$k,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$k,
    VOTVolume: VOTVolume$k,
    VOTVolumeTranslation: VOTVolumeTranslation$k,
    VOTAutoSetVolume: VOTAutoSetVolume$k,
    VOTShowVideoSlider: VOTShowVideoSlider$k,
    VOTSyncVolume: VOTSyncVolume$k,
    VOTDisableFromYourLang: VOTDisableFromYourLang$k,
    VOTVideoIsTooLong: VOTVideoIsTooLong$k,
    VOTNoVideoIDFound: VOTNoVideoIDFound$k,
    VOTSubtitles: VOTSubtitles$k,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$k,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$k,
    VOTHighlightWords: VOTHighlightWords$k,
    VOTTranslatedFrom: VOTTranslatedFrom$k,
    VOTAutogenerated: VOTAutogenerated$k,
    VOTSettings: VOTSettings$k,
    VOTMenuLanguage: VOTMenuLanguage$k,
    VOTAuthors: VOTAuthors$k,
    VOTVersion: VOTVersion$k,
    VOTLoader: VOTLoader$k,
    VOTBrowser: VOTBrowser$k,
    VOTShowPiPButton: VOTShowPiPButton$k,
    langs: langs$k,
    streamNoConnectionToServer: streamNoConnectionToServer$k,
    searchField: searchField$k,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$k,
    VOTDetectService: VOTDetectService$k,
    VOTProxyWorkerHost: VOTProxyWorkerHost$k,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$k,
    proxySettings: proxySettings$k,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$k,
    VOTAudioBooster: VOTAudioBooster$k,
    VOTSubtitlesDesign: VOTSubtitlesDesign$k,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$k,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$k,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$k,
    VOTDownloadWithName: VOTDownloadWithName$k,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$k,
    VOTLocaleHash: VOTLocaleHash$k,
    VOTUpdatedAt: VOTUpdatedAt$k,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$k,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$k,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$k,
    VOTNewAudioPlayer: VOTNewAudioPlayer$k,
    VOTUseNewModel: VOTUseNewModel$k,
    TranslationDelayed: TranslationDelayed$k,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$k,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$k,
    VOTBugReport: VOTBugReport$k,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$k,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$k,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$k,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$k,
    VOTTranslatedBy: VOTTranslatedBy$k,
    VOTStreamNotAvailable: VOTStreamNotAvailable$k,
    VOTTranslationTextService: VOTTranslationTextService$k,
    VOTNotAffectToVoice: VOTNotAffectToVoice$k,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$k,
    showVideoVolumeSlider: showVideoVolumeSlider$k,
    hotkeysSettings: hotkeysSettings$k,
    None: None$k,
    VOTUseLivelyVoice: VOTUseLivelyVoice$k,
    miscSettings: miscSettings$k,
    services: services$k,
    aboutExtension: aboutExtension$k,
    appearance: appearance$k,
    buttonPosition: buttonPosition$k,
    position: position$k,
    secs: secs$k,
    autoHideButtonDelay: autoHideButtonDelay$k,
    notFound: notFound$k,
    minButtonPositionContainer: minButtonPositionContainer$k,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$k,
    PressTheKeyCombination: PressTheKeyCombination$k,
    VOTUseAudioDownload: VOTUseAudioDownload$k,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$k,
    VOTAccountRequired: VOTAccountRequired$k,
    VOTMyAccount: VOTMyAccount$k,
    VOTLogin: VOTLogin$k,
    VOTLogout: VOTLogout$k,
    VOTRefresh: VOTRefresh$k,
    VOTYandexToken: VOTYandexToken$k,
    VOTYandexTokenInfo: VOTYandexTokenInfo$k,
    VOTLoginViaToken: VOTLoginViaToken$k,
    subtitlesSmartLayout: subtitlesSmartLayout$k,
    smartDucking: smartDucking$k,
    VOTAutoSubtitles: VOTAutoSubtitles$k,
    VOTSubtitlesFont: VOTSubtitlesFont$k
  };
  const recommended$j = "ਸਿਫਾਰਸ਼ੀ";
  const translateVideo$j = "ਵੀਡੀਓ ਅਨੁਵਾਦ ਕਰੋ";
  const disableTranslate$j = "ਬੰਦ ਕਰੋ";
  const translationSettings$j = "ਅਨੁਵਾਦ ਸੈਟਿੰਗ";
  const subtitlesSettings$j = "ਉਪਸਿਰਲੇਖ ਸੈਟਿੰਗ";
  const resetSettings$j = "ਸੈਟਿੰਗ ਮੁੜ-ਸੈੱਟ";
  const videoBeingTranslated$j = "ਵੀਡੀਓ ਅਨੁਵਾਦ ਕੀਤਾ ਜਾ ਰਿਹਾ ਹੈ";
  const videoLanguage$j = "ਵੀਡੀਓ ਭਾਸ਼ਾ";
  const translationLanguage$j = "ਅਨੁਵਾਦ ਭਾਸ਼ਾ";
  const translationTake$j = "ਅਨੁਵਾਦ ਕਰੇਗਾ";
  const translationTakeMoreThanHour$j = "ਅਨੁਵਾਦ ਵਿੱਚ ਇੱਕ ਘੰਟੇ ਤੋਂ ਵੱਧ ਸਮਾਂ ਲੱਗੇਗਾ";
  const translationTakeAboutMinute$j = "ਅਨੁਵਾਦ ਲਗਭਗ ਇੱਕ ਮਿੰਟ ਲਵੇਗਾ";
  const translationTakeFewMinutes$j = "ਅਨੁਵਾਦ ਕੁਝ ਮਿੰਟ ਲਵੇਗਾ";
  const translationTakeApproximatelyMinutes$j = "ਅਨੁਵਾਦ ਲਗਭਗ {0} ਮਿੰਟ ਲਵੇਗਾ";
  const translationTakeApproximatelyMinute$j = "ਅਨੁਵਾਦ ਲਗਭਗ {0} ਮਿੰਟ ਲਵੇਗਾ";
  const requestTranslationFailed$j = "ਵੀਡੀਓ ਅਨੁਵਾਦ ਲਈ ਬੇਨਤੀ ਕਰਨ ਲਈ ਫੇਲ੍ਹ ਹੈ";
  const audioNotReceived$j = "ਆਡੀਓ ਲਿੰਕ ਨਹੀਂ ਮਿਲਿਆ";
  const audioFormatNotSupported$j = "ਆਡੀਓ ਫਾਰਮੈਟ ਸਹਾਇਕ ਨਹੀਂ ਹੈ";
  const VOTAutoTranslate$j = "ਓਪਨ ਉੱਤੇ ਅਨੁਵਾਦ ਕਰੋ";
  const VOTDontTranslateYourLang$j = "ਮੇਰੀ ਭਾਸ਼ਾ ਤੋਂ ਅਨੁਵਾਦ ਨਾ ਕਰੋ";
  const VOTVolume$j = "ਵੀਡੀਓ ਵਾਲੀਅਮ:";
  const VOTVolumeTranslation$j = "ਅਨੁਵਾਦ ਵਾਲੀਅਮ:";
  const VOTAutoSetVolume$j = "ਵੀਡੀਓ ਵਾਲੀਅਮ ਘਟਾਓ";
  const VOTShowVideoSlider$j = "ਵੀਡੀਓ ਵਾਲੀਅਮ ਸਲਾਈਡਰ";
  const VOTSyncVolume$j = "ਲਿੰਕ ਅਨੁਵਾਦ ਅਤੇ ਵੀਡੀਓ ਵਾਲੀਅਮ";
  const VOTDisableFromYourLang$j = "ਤੁਸੀਂ ਆਪਣੀ ਭਾਸ਼ਾ ਵਿੱਚ ਵੀਡੀਓ ਦਾ ਅਨੁਵਾਦ ਅਯੋਗ ਕਰ ਦਿੱਤਾ ਹੈ";
  const VOTVideoIsTooLong$j = "ਵੀਡੀਓ ਬਹੁਤ ਲੰਮਾ ਹੈ";
  const VOTNoVideoIDFound$j = "ਵਿਡੀਓ ਆਈਡੀ ਨਹੀਂ ਮਿਲੀ";
  const VOTSubtitles$j = "ਉਪਸਿਰਲੇਖ";
  const VOTSubtitlesDisabled$j = "ਆਯੋਗ";
  const VOTSubtitlesMaxLength$j = "ਉਪਸਿਰਲੇਖ ਅਧਿਕਤਮ ਲੰਬਾਈ";
  const VOTHighlightWords$j = "ਉਚਾਰਨ ਸ਼ਬਦ";
  const VOTTranslatedFrom$j = "ਤੋਂ ਅਨੁਵਾਦ ਕੀਤਾ";
  const VOTAutogenerated$j = "ਸਵੈ-ਉਤਪੰਨ";
  const VOTSettings$j = "ਵੋਟ ਸੈਟਿੰਗ";
  const VOTMenuLanguage$j = "ਮੇਨੂ ਭਾਸ਼ਾ";
  const VOTAuthors$j = "ਲੇਖਕ";
  const VOTVersion$j = "ਵਰਜਨ";
  const VOTLoader$j = "ਲੋਡਰ";
  const VOTBrowser$j = "ਬਰਾਊਜ਼ਰ";
  const VOTShowPiPButton$j = "ਪਾਈਪ ਬਟਨ ਵੇਖੋ";
  const langs$j = { "auto": "ਆਟੋ", "af": "ਅਫ਼ਰੀਕਾਂਸ", "ak": "ਅਕਾਨ", "sq": "ਅਲਬਾਨੀਆਈ", "am": "ਅਮਹਾਰੀ", "ar": "ਅਰਬੀ", "hy": "ਅਰਮੀਨੀਆਈ", "as": "ਅਸਾਮੀ", "ay": "ਅਯਮਾਰਾ", "az": "ਅਜ਼ਰਬਾਈਜਾਨੀ", "bn": "ਬੰਗਲਾ", "eu": "ਬਾਸਕ", "be": "ਬੇਲਾਰੂਸੀ", "bho": "ਭੋਜਪੁਰੀ", "bs": "ਬੋਸਨੀਆਈ", "bg": "ਬੁਲਗਾਰੀਆਈ", "my": "ਬਰਮੀਜ਼", "ca": "ਕੈਟਲਨ", "ceb": "ਸੇਬੂਆਨੋ", "zh": "ਚੀਨੀ", "zh-Hans": "ਚੀਨੀ (ਸਰਲ)", "zh-Hant": "ਚੀਨੀ (ਰਵਾਇਤੀ)", "co": "ਕੋਰਸਿਕਨ", "hr": "ਕ੍ਰੋਏਸ਼ੀਆਈ", "cs": "ਚੈੱਕ", "da": "ਡੈਨਿਸ਼", "dv": "ਦਿਵੇਹੀ", "nl": "ਡੱਚ", "en": "ਅੰਗਰੇਜ਼ੀ", "eo": "ਐਸਪੇਰੈਂਟੋ", "et": "ਐਸਟੋਨੀਆਈ", "ee": "ਭੇਡ", "fil": "ਫਿਲੀਪੀਨੋ", "fi": "ਫ਼ਿਨਲੈਂਡੀ", "fr": "ਫ਼ਰਾਂਸੀਸੀ", "gl": "ਗਾਲੀਸੀਆਈ", "lg": "ਗਾਂਡਾ", "ka": "ਜਾਰਜੀਅਨ", "de": "ਜਰਮਨ", "el": "ਯੂਨਾਨੀ", "gn": "ਗੁਆਰਾਨੀ", "gu": "ਗੁਜਰਾਤੀ", "ht": "ਹੈਤੀਆਈ ਕ੍ਰੀਓਲ", "ha": "ਹੁਆਸਾ", "haw": "ਹਵਾਈ", "iw": "ਇਬਰਾਨੀ", "hi": "ਹਿੰਦੀ", "hmn": "ਹਮੋਂਗ", "hu": "ਹੰਗਰੀਆਈ", "is": "ਆਈਸਲੈਂਡੀ", "ig": "ਇਗਬੋ", "id": "ਇੰਡੋਨੇਸ਼ੀਆਈ", "ga": "ਆਇਰਿਸ਼", "it": "ਇਤਾਲਵੀ", "ja": "ਜਾਪਾਨੀ", "jv": "ਜਾਵਾਨੀ", "kn": "ਕੰਨੜ", "kk": "ਕਜ਼ਾਖ", "km": "ਖਮੇਰ", "rw": "ਕਿਨਯਾਰਵਾਂਡਾ", "ko": "ਕੋਰੀਆਈ", "kri": "ਕ੍ਰਿਓ", "ku": "ਕੁਰਦੀ", "ky": "ਕਿਰਗਿਜ਼", "lo": "ਲਾਓ", "la": "ਲਾਤੀਨੀ", "lv": "ਲਾਤਵੀਆਈ", "ln": "ਲਿੰਗਲਾ", "lt": "ਲਿਥੁਆਨੀ", "lb": "ਲਕਸਮਬਰਗ", "mk": "ਮਕਦੂਨੀ", "mg": "ਮਲਗਾਸੀ", "ms": "ਮਲਾਇ", "ml": "ਮਲਿਆਲਮ", "mt": "ਮਾਲਟੀਜ਼", "mi": "ਮਾਓਰੀ", "mr": "ਮਰਾਠੀ", "mn": "ਮੰਗੋਲੀਆਈ", "ne": "ਨੇਪਾਲੀ", "nso": "ਉੱਤਰੀ ਸੋਥੋ", "no": "ਨਾਰਵੇਈ", "ny": "ਨਯੰਜਾ", "or": "ਓਡੀਆ", "om": "ਓਰੋਮੋ", "ps": "ਪਸ਼ਤੋ", "fa": "ਫ਼ਾਰਸੀ", "pl": "ਪੋਲਿਸ਼", "pt": "ਪੁਰਤਗਾਲੀ", "pa": "ਪੰਜਾਬੀ ਭਾਸ਼ਾ", "qu": "ਕਿਚੂਆ", "ro": "ਰੋਮਾਨੀਆਈ", "ru": "ਰੂਸੀ", "sm": "ਸਮੋਆਨ", "sa": "ਸੰਸਕ੍ਰਿਤ", "gd": "ਸਕਾਟਿਸ਼ ਗੈਲਿਕ", "sr": "ਸਰਬੀਆਈ", "sn": "ਸ਼ੋਨਾ", "sd": "ਸਿੰਧੀ", "si": "ਸਿੰਹਾਲੀ", "sk": "ਸਲੋਵਾਕੀ", "sl": "ਸਲੋਵੇਨੀ", "so": "ਸੋਮਾਲੀ", "st": "ਦੱਖਣੀ ਸੋਥੋ", "es": "ਸਪੇਨੀ", "su": "ਸੁਨਦੇਸੀ", "sw": "ਸਵਾਹਿਲੀ", "sv": "ਸਵੀਡਿਸ਼", "tg": "ਤਾਜਿਕ", "ta": "ਤਾਮਿਲ", "tt": "ਤਾਤਾਰ", "te": "ਤੇਲਗੂ", "th": "ਥਾਈ", "ti": "ਟਿਗਰੀਨੀਆ", "ts": "ਟੋਂਗਾ", "tr": "ਤੁਰਕੀ", "tk": "ਤੁਰਕਮੇਨ", "uk": "ਯੂਕਰੇਨੀ", "ur": "ਉਰਦੂ", "ug": "ਉਈਗਰ", "uz": "ਉਜ਼ਬੇਕ", "vi": "ਵੀਅਤਨਾਮੀ", "cy": "ਵੈਲਸ਼", "fy": "ਪੱਛਮੀ ਫ੍ਰਿਸੀਅਨ", "xh": "ਜ਼ੋਸਾ", "yi": "ਯਿੱਦਿਸ਼", "yo": "ਯੋਰੂਬਾ", "zu": "ਜ਼ੂਲੂ" };
  const streamNoConnectionToServer$j = "ਸਰਵਰ ਨਾਲ ਕੋਈ ਕੁਨੈਕਸ਼ਨ ਨਹੀਂ";
  const searchField$j = "ਖੋਜ ਕਰੋ...";
  const VOTTranslateAPIErrors$j = "ਏਪੀਆਈ ਤੋਂ ਗਲਤੀਆਂ ਅਨੁਵਾਦ ਕਰੋ";
  const VOTDetectService$j = "ਭਾਸ਼ਾ ਖੋਜ ਸੇਵਾ";
  const VOTProxyWorkerHost$j = "ਪਰਾਕਸੀ ਵਰਕਰ ਐਡਰੈੱਸ ਦਿਓ";
  const VOTM3u8ProxyHost$j = "ਐਮ 3 ਯੂ 8 ਪ੍ਰੌਕਸੀ ਵਰਕਰ ਦਾ ਪਤਾ ਦਰਜ ਕਰੋ";
  const proxySettings$j = "ਪਰਾਕਸੀ ਸੈਟਿੰਗ";
  const translationTakeApproximatelyMinute2$j = "ਅਨੁਵਾਦ ਲਗਭਗ {0} ਮਿੰਟ ਲਵੇਗਾ";
  const VOTAudioBooster$j = "ਵਿਸਤ੍ਰਿਤ ਅਨੁਵਾਦ ਵਾਲੀਅਮ ਵਾਧਾ";
  const VOTSubtitlesDesign$j = "ਉਪਸਿਰਲੇਖ ਡਿਜ਼ਾਈਨ";
  const VOTSubtitlesFontSize$j = "ਉਪਸਿਰਲੇਖਾਂ ਦਾ ਫੋਂਟ ਸਾਈਜ਼";
  const VOTSubtitlesOpacity$j = "ਉਪਸਿਰਲੇਖ ਪਿਛੋਕੜ ਦੀ ਪਾਰਦਰਸ਼ਤਾ";
  const VOTSubtitlesDownloadFormat$j = "ਉਪਸਿਰਲੇਖ ਡਾਊਨਲੋਡ ਕਰਨ ਲਈ ਫਾਰਮੈਟ";
  const VOTDownloadWithName$j = "ਵੀਡੀਓ ਨਾਂ ਨਾਲ ਫਾਇਲਾਂ ਡਾਊਨਲੋਡ ਕਰੋ";
  const VOTUpdateLocaleFiles$j = "ਸਥਾਨੀਕਰਨ ਫਾਇਲਾਂ ਅੱਪਡੇਟ";
  const VOTLocaleHash$j = "ਲੋਕੇਲ ਹੈਸ਼";
  const VOTUpdatedAt$j = "ਵਜੇ ਅਪਡੇਟ ਕੀਤਾ ਗਿਆ";
  const VOTNeedWebAudioAPI$j = "ਇਸ ਨੂੰ ਯੋਗ ਕਰਨ ਲਈ, ਤੁਹਾਡੇ ਕੋਲ ਇੱਕ ਵੈਬ ਆਡੀਓ ਏਪੀਆਈ ਹੋਣਾ ਚਾਹੀਦਾ ਹੈ";
  const VOTMediaCSPEnabledOnSite$j = "ਮੀਡੀਆ ਸੀਐਸਪੀ ਇਸ ਸਾਈਟ ਤੇ ਸਮਰੱਥ ਹੈ";
  const VOTOnlyBypassMediaCSP$j = "ਇਸ ਨੂੰ ਸਿਰਫ ਮੀਡੀਆ ਸੀਐਸਪੀ ਨੂੰ ਬਾਈਪਾਸ ਕਰਨ ਲਈ ਵਰਤੋ";
  const VOTNewAudioPlayer$j = "ਨਵਾਂ ਆਡੀਓ ਪਲੇਅਰ ਵਰਤੋਂ";
  const VOTUseNewModel$j = "ਕੁਝ ਵੀਡੀਓਜ਼ ਲਈ ਯਾਂਡੇਕਸ ਆਵਾਜ਼ਾਂ ਦੀ ਪ੍ਰਯੋਗਾਤਮਕ ਪਰਿਵਰਤਨ ਦੀ ਵਰਤੋਂ ਕਰੋ";
  const TranslationDelayed$j = "ਅਨੁਵਾਦ ਥੋੜ੍ਹਾ ਦੇਰੀ ਨਾਲ ਹੋਇਆ ਹੈ";
  const VOTTranslationCompletedNotify$j = "{0} ਉੱਤੇ ਅਨੁਵਾਦ ਪੂਰਾ ਹੋ ਗਿਆ ਹੈ!";
  const VOTSendNotifyOnComplete$j = "ਇੱਕ ਨੋਟੀਫਿਕੇਸ਼ਨ ਭੇਜੋ ਕਿ ਵੀਡੀਓ ਦਾ ਅਨੁਵਾਦ ਕੀਤਾ ਗਿਆ ਹੈ";
  const VOTBugReport$j = "ਬੱਗ ਰਿਪੋਰਟ ਕਰੋ";
  const VOTTranslateProxyDisabled$j = "ਆਯੋਗ";
  const VOTTranslateProxyEnabled$j = "ਯੋਗ";
  const VOTTranslateProxyEverything$j = "ਪਰਾਕਸੀ ਸਭ ਕੁਝ";
  const VOTTranslateProxyStatus$j = "ਪ੍ਰੌਕਸੀ ਮੋਡ";
  const VOTTranslatedBy$j = "{0}ਦੁਆਰਾ ਅਨੁਵਾਦ ਕੀਤਾ";
  const VOTStreamNotAvailable$j = "ਅਨੁਵਾਦ ਸਟਰੀਮ ਉਪਲੱਬਧ ਨਹੀਂ ਹੈ";
  const VOTTranslationTextService$j = "ਪਾਠ ਅਨੁਵਾਦ ਸੇਵਾ";
  const VOTNotAffectToVoice$j = "ਵੋਇਸ ਓਵਰ ਵਿੱਚ ਟੈਕਸਟ ਦੇ ਅਨੁਵਾਦ ਨੂੰ ਪ੍ਰਭਾਵਤ ਨਹੀਂ ਕਰਦਾ";
  const DontTranslateSelectedLanguages$j = "ਚੁਣੀਆਂ ਭਾਸ਼ਾਵਾਂ ਤੋਂ ਅਨੁਵਾਦ ਨਾ ਕਰੋ";
  const showVideoVolumeSlider$j = "ਵਿਡੀਓ ਵਾਲੀਅਮ ਸਲਾਈਡਰ ਵੇਖਾਓ";
  const hotkeysSettings$j = "ਹੌਟਕੀਜ਼ ਸੈਟਿੰਗ";
  const None$j = "ਕੋਈ ਨਹੀਂ";
  const VOTUseLivelyVoice$j = "ਜੀਵੰਤ ਆਵਾਜ਼ ਵਰਤੋ. ਬੋਲਣ ਵਾਲੇ ਮੂਲ ਰੂਸੀ ਵਰਗੇ ਆਵਾਜ਼.";
  const miscSettings$j = "ਫੁਟਕਲ ਸੈਟਿੰਗ";
  const services$j = { "yandexbrowser": "ਯਾਂਡੇਕਸ ਬਰਾਊਜ਼ਰ", "msedge": "ਮਾਈਕਰੋਸਾਫਟ ਐਜ", "rust-server": "ਰਸਟ ਸਰਵਰ" };
  const aboutExtension$j = "ਐਕਸਟੈਂਸ਼ਨ ਬਾਰੇ";
  const appearance$j = "ਦਿੱਖ";
  const buttonPosition$j = "ਪਲੇਅਰ ਵਿੱਚ ਬਟਨ ਸਥਿਤੀ";
  const position$j = { "left": "ਖੱਬੇ", "right": "ਸਹੀ", "top": "ਸਿਖਰ", "default": "ਡਿਫਾਲਟ" };
  const secs$j = "ਸੈਕਿੰਡ";
  const autoHideButtonDelay$j = "ਅਨੁਵਾਦ ਬਟਨ ਨੂੰ ਲੁਕਾਉਣ ਤੋਂ ਪਹਿਲਾਂ ਦੇਰੀ";
  const notFound$j = "ਨਹੀਂ ਲੱਭਿਆ";
  const minButtonPositionContainer$j = "ਬਟਨ ਦੀ ਸਥਿਤੀ ਸਿਰਫ 600 ਪਿਕਸਲ ਤੋਂ ਵੱਡੇ ਖਿਡਾਰੀਆਂ ਵਿੱਚ ਬਦਲਦੀ ਹੈ.";
  const VOTTranslateProxyStatusDefault$j = "ਤੁਹਾਡੇ ਦੇਸ਼ ਵਿੱਚ ਪ੍ਰੌਕਸੀ ਨੂੰ ਪੂਰੀ ਤਰ੍ਹਾਂ ਅਯੋਗ ਕਰਨਾ ਐਕਸਟੈਂਸ਼ਨ ਨੂੰ ਤੋੜ ਸਕਦਾ ਹੈ";
  const PressTheKeyCombination$j = "ਕੁੰਜੀ ਸੰਜੋਗ ਦਬਾਓ...";
  const VOTUseAudioDownload$j = "ਆਡੀਓ ਡਾਊਨਲੋਡ ਵਰਤੋਂ";
  const VOTUseAudioDownloadWarning$j = "ਆਡੀਓ ਡਾਉਨਲੋਡਸ ਨੂੰ ਅਯੋਗ ਕਰਨਾ ਐਕਸਟੈਂਸ਼ਨ ਦੀ ਕਾਰਜਸ਼ੀਲਤਾ ਨੂੰ ਪ੍ਰਭਾਵਤ ਕਰ ਸਕਦਾ ਹੈ";
  const VOTAccountRequired$j = "ਇਸ ਫੀਚਰ ਨੂੰ ਵਰਤਣ ਲਈ ਤੁਹਾਨੂੰ ਲਾਗਇਨ ਕਰਨ ਦੀ ਲੋੜ ਹੈ";
  const VOTMyAccount$j = "ਮੇਰਾ ਖਾਤਾ";
  const VOTLogin$j = "ਲਾਗਇਨ";
  const VOTLogout$j = "ਲਾਗਆਉਟ";
  const VOTRefresh$j = "ਤਾਜ਼ਾ ਕਰੋ";
  const VOTYandexToken$j = "ਯਾਂਡੇਕਸ ਓਏਥ ਟੋਕਨ ਦਾਖਲ ਕਰੋ";
  const VOTYandexTokenInfo$j = "ਤੁਹਾਨੂੰ ਇਸ ਖੇਤਰ ਵਿੱਚ ਦਸਤੀ ਖਾਤੇ ਟੋਕਨ ਸੈੱਟ ਕਰ ਸਕਦੇ ਹੋ. ਕਿਰਪਾ ਕਰਕੇ ਨੋਟ ਕਰੋ ਕਿ ਅਸੀਂ ਅਨੁਵਾਦ ਬੇਨਤੀ ਭੇਜਣ ਤੋਂ ਪਹਿਲਾਂ ਇਸਦੀ ਵੈਧਤਾ ਦੀ ਜਾਂਚ ਨਹੀਂ ਕਰਦੇ";
  const VOTLoginViaToken$j = "ਟੋਕਨ ਰਾਹੀਂ ਲਾਗਇਨ";
  const subtitlesSmartLayout$j = "ਸਮਾਰਟ ਉਪਸਿਰਲੇਖ ਲੇਆਉਟ";
  const smartDucking$j = "ਅਨੁਕੂਲ ਵਾਲੀਅਮ";
  const VOTAutoSubtitles$j = "ਓਪਨ ' ਤੇ ਉਪਸਿਰਲੇਖ";
  const VOTSubtitlesFont$j = "ਉਪਸਿਰਲੇਖ ਫੋਂਟ";
  const __vite_glob_0_42 = {
    recommended: recommended$j,
    translateVideo: translateVideo$j,
    disableTranslate: disableTranslate$j,
    translationSettings: translationSettings$j,
    subtitlesSettings: subtitlesSettings$j,
    resetSettings: resetSettings$j,
    videoBeingTranslated: videoBeingTranslated$j,
    videoLanguage: videoLanguage$j,
    translationLanguage: translationLanguage$j,
    translationTake: translationTake$j,
    translationTakeMoreThanHour: translationTakeMoreThanHour$j,
    translationTakeAboutMinute: translationTakeAboutMinute$j,
    translationTakeFewMinutes: translationTakeFewMinutes$j,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$j,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$j,
    requestTranslationFailed: requestTranslationFailed$j,
    audioNotReceived: audioNotReceived$j,
    audioFormatNotSupported: audioFormatNotSupported$j,
    VOTAutoTranslate: VOTAutoTranslate$j,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$j,
    VOTVolume: VOTVolume$j,
    VOTVolumeTranslation: VOTVolumeTranslation$j,
    VOTAutoSetVolume: VOTAutoSetVolume$j,
    VOTShowVideoSlider: VOTShowVideoSlider$j,
    VOTSyncVolume: VOTSyncVolume$j,
    VOTDisableFromYourLang: VOTDisableFromYourLang$j,
    VOTVideoIsTooLong: VOTVideoIsTooLong$j,
    VOTNoVideoIDFound: VOTNoVideoIDFound$j,
    VOTSubtitles: VOTSubtitles$j,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$j,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$j,
    VOTHighlightWords: VOTHighlightWords$j,
    VOTTranslatedFrom: VOTTranslatedFrom$j,
    VOTAutogenerated: VOTAutogenerated$j,
    VOTSettings: VOTSettings$j,
    VOTMenuLanguage: VOTMenuLanguage$j,
    VOTAuthors: VOTAuthors$j,
    VOTVersion: VOTVersion$j,
    VOTLoader: VOTLoader$j,
    VOTBrowser: VOTBrowser$j,
    VOTShowPiPButton: VOTShowPiPButton$j,
    langs: langs$j,
    streamNoConnectionToServer: streamNoConnectionToServer$j,
    searchField: searchField$j,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$j,
    VOTDetectService: VOTDetectService$j,
    VOTProxyWorkerHost: VOTProxyWorkerHost$j,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$j,
    proxySettings: proxySettings$j,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$j,
    VOTAudioBooster: VOTAudioBooster$j,
    VOTSubtitlesDesign: VOTSubtitlesDesign$j,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$j,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$j,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$j,
    VOTDownloadWithName: VOTDownloadWithName$j,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$j,
    VOTLocaleHash: VOTLocaleHash$j,
    VOTUpdatedAt: VOTUpdatedAt$j,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$j,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$j,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$j,
    VOTNewAudioPlayer: VOTNewAudioPlayer$j,
    VOTUseNewModel: VOTUseNewModel$j,
    TranslationDelayed: TranslationDelayed$j,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$j,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$j,
    VOTBugReport: VOTBugReport$j,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$j,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$j,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$j,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$j,
    VOTTranslatedBy: VOTTranslatedBy$j,
    VOTStreamNotAvailable: VOTStreamNotAvailable$j,
    VOTTranslationTextService: VOTTranslationTextService$j,
    VOTNotAffectToVoice: VOTNotAffectToVoice$j,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$j,
    showVideoVolumeSlider: showVideoVolumeSlider$j,
    hotkeysSettings: hotkeysSettings$j,
    None: None$j,
    VOTUseLivelyVoice: VOTUseLivelyVoice$j,
    miscSettings: miscSettings$j,
    services: services$j,
    aboutExtension: aboutExtension$j,
    appearance: appearance$j,
    buttonPosition: buttonPosition$j,
    position: position$j,
    secs: secs$j,
    autoHideButtonDelay: autoHideButtonDelay$j,
    notFound: notFound$j,
    minButtonPositionContainer: minButtonPositionContainer$j,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$j,
    PressTheKeyCombination: PressTheKeyCombination$j,
    VOTUseAudioDownload: VOTUseAudioDownload$j,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$j,
    VOTAccountRequired: VOTAccountRequired$j,
    VOTMyAccount: VOTMyAccount$j,
    VOTLogin: VOTLogin$j,
    VOTLogout: VOTLogout$j,
    VOTRefresh: VOTRefresh$j,
    VOTYandexToken: VOTYandexToken$j,
    VOTYandexTokenInfo: VOTYandexTokenInfo$j,
    VOTLoginViaToken: VOTLoginViaToken$j,
    subtitlesSmartLayout: subtitlesSmartLayout$j,
    smartDucking: smartDucking$j,
    VOTAutoSubtitles: VOTAutoSubtitles$j,
    VOTSubtitlesFont: VOTSubtitlesFont$j
  };
  const recommended$i = "zalecane";
  const translateVideo$i = "Przetłumacz wideo";
  const disableTranslate$i = "Wyłącz";
  const translationSettings$i = "Ustawienia tłumaczenia";
  const subtitlesSettings$i = "Ustawienia napisów";
  const resetSettings$i = "Zresetuj ustawienia";
  const videoBeingTranslated$i = "Film jest tłumaczony";
  const videoLanguage$i = "Język wideo";
  const translationLanguage$i = "Język tłumaczenia";
  const translationTake$i = "Tłumaczenie zajmie";
  const translationTakeMoreThanHour$i = "Tłumaczenie zajmie ponad godzinę";
  const translationTakeAboutMinute$i = "Tłumaczenie zajmie około minuty";
  const translationTakeFewMinutes$i = "Tłumaczenie zajmie kilka minut";
  const translationTakeApproximatelyMinutes$i = "Tłumaczenie zajmie około {0} minut";
  const translationTakeApproximatelyMinute$i = "Tłumaczenie zajmie około {0} minut";
  const requestTranslationFailed$i = "Nie udało się poprosić o tłumaczenie wideo";
  const audioNotReceived$i = "Łącze Audio nie zostało odebrane";
  const audioFormatNotSupported$i = "Format audio nie jest obsługiwany";
  const VOTAutoTranslate$i = "Translate on open";
  const VOTDontTranslateYourLang$i = "Nie tłumacz z mojego języka";
  const VOTVolume$i = "Głośność wideo:";
  const VOTVolumeTranslation$i = "Objętość tłumaczenia:";
  const VOTAutoSetVolume$i = "Zmniejsz głośność wideo do";
  const VOTShowVideoSlider$i = "Suwak głośności wideo";
  const VOTSyncVolume$i = "Tłumaczenie linków i objętość wideo";
  const VOTDisableFromYourLang$i = "Wyłączyłeś tłumaczenie wideo na swój język";
  const VOTVideoIsTooLong$i = "Wideo jest za długie";
  const VOTNoVideoIDFound$i = "Nie znaleziono identyfikatora wideo";
  const VOTSubtitles$i = "Napisy";
  const VOTSubtitlesDisabled$i = "Niepełnosprawni";
  const VOTSubtitlesMaxLength$i = "Napisy max długość";
  const VOTHighlightWords$i = "Wyróżnij słowa";
  const VOTTranslatedFrom$i = "przetłumaczone z";
  const VOTAutogenerated$i = "autogenerated";
  const VOTSettings$i = "Ustawienia VOT";
  const VOTMenuLanguage$i = "Język Menu";
  const VOTAuthors$i = "Autorzy";
  const VOTVersion$i = "Wersja";
  const VOTLoader$i = "Ładowarka";
  const VOTBrowser$i = "Przeglądarka";
  const VOTShowPiPButton$i = "Pokaż przycisk PiP";
  const langs$i = { "auto": "Auto", "af": "Afrikaans", "ak": "Akan", "sq": "Albański", "am": "Amharski", "ar": "Arabski", "hy": "Ormiański", "as": "Asamski", "ay": "Aymara", "az": "Azerbejdżański", "bn": "Bangla", "eu": "Baskijski", "be": "Białoruski", "bho": "Bhojpuri", "bs": "Bośniacki", "bg": "Bułgarski", "my": "Birmański", "ca": "Kataloński", "ceb": "Cebuano", "zh": "Chiński", "zh-Hans": "Chiński (Uproszczony)", "zh-Hant": "Chiński (Tradycyjny)", "co": "Korsykański", "hr": "Chorwacki", "cs": "Czeski", "da": "Duński", "dv": "Divehi", "nl": "Holenderski", "en": "Polski", "eo": "Esperanto", "et": "Estoński", "ee": "Ewe", "fil": "Filipino", "fi": "Fiński", "fr": "Francuski", "gl": "Galicyjski", "lg": "Ganda", "ka": "Gruziński", "de": "Niemiecki", "el": "Grecki", "gn": "Guarani", "gu": "Gudżarati", "ht": "Kreolski Haitański", "ha": "Hausa", "haw": "Hawajski", "iw": "Hebrajski", "hi": "Hindi", "hmn": "Hmong", "hu": "Węgierski", "is": "Islandzki", "ig": "Igbo", "id": "Indonezyjski", "ga": "Irlandzki", "it": "Włoski", "ja": "Japoński", "jv": "Jawajski", "kn": "Kannada", "kk": "Kazachski", "km": "Khmerski", "rw": "Kinyarwanda", "ko": "Koreański", "kri": "Krio", "ku": "Kurdyjski", "ky": "Kirgiski", "lo": "Lao", "la": "Łacina", "lv": "Łotewski", "ln": "Lingala", "lt": "Litewski", "lb": "Luksemburski", "mk": "Macedoński", "mg": "Madagaskaru", "ms": "Malajski", "ml": "Malajalam", "mt": "Maltański", "mi": "Maorysów", "mr": "Marathi", "mn": "Mongolski", "ne": "Nepalski", "nso": "Northern Sotho", "no": "Norweski", "ny": "Nyanja", "or": "Odia", "om": "Oromo", "ps": "Paszto", "fa": "Perski", "pl": "Polskie", "pt": "Portugalski", "pa": "Pendżabski", "qu": "Keczua", "ro": "Rumuński", "ru": "Rosyjski", "sm": "Samoa", "sa": "Sanskryt", "gd": "Szkocki Gaelicki", "sr": "Serbski", "sn": "Shona", "sd": "Sindhi", "si": "Syngaleskim", "sk": "Słowacki", "sl": "Słoweński", "so": "Somalijski", "st": "Southern Sotho", "es": "Hiszpański", "su": "Sundanese", "sw": "Suahili", "sv": "Szwedzki", "tg": "Tadżycki", "ta": "Tamilski", "tt": "Tatar", "te": "Telugu", "th": "Tajski", "ti": "Tigrinya", "ts": "Tsonga", "tr": "Turecki", "tk": "Turkmen", "uk": "Ukraiński", "ur": "Urdu", "ug": "Ujgurów", "uz": "Uzbecki", "vi": "Wietnamski", "cy": "Walijski", "fy": "Zachodniofryzyjski", "xh": "Xhosa", "yi": "Jidysz", "yo": "Joruba", "zu": "Zulu" };
  const streamNoConnectionToServer$i = "Nie ma połączenia z serwerem";
  const searchField$i = "Szukaj...";
  const VOTTranslateAPIErrors$i = "Przetłumacz błędy z API";
  const VOTDetectService$i = "Usługa wykrywania języka";
  const VOTProxyWorkerHost$i = "Wprowadź adres pracownika proxy";
  const VOTM3u8ProxyHost$i = "Wprowadź adres pracownika proxy m3u8";
  const proxySettings$i = "Ustawienia Proxy";
  const translationTakeApproximatelyMinute2$i = "Tłumaczenie zajmie około {0} minut";
  const VOTAudioBooster$i = "Rozszerzony wzrost objętości tłumaczenia";
  const VOTSubtitlesDesign$i = "Projektowanie napisów";
  const VOTSubtitlesFontSize$i = "Rozmiar czcionki napisów";
  const VOTSubtitlesOpacity$i = "Przezroczystość tła napisów";
  const VOTSubtitlesDownloadFormat$i = "Format pobierania napisów";
  const VOTDownloadWithName$i = "Pobierz pliki z nazwą wideo";
  const VOTUpdateLocaleFiles$i = "Aktualizuj pliki lokalizacyjne";
  const VOTLocaleHash$i = "Locale hash";
  const VOTUpdatedAt$i = "Aktualizacja:";
  const VOTNeedWebAudioAPI$i = "Aby to włączyć, musisz mieć Web Audio API";
  const VOTMediaCSPEnabledOnSite$i = "Media CSP jest włączony na tej stronie";
  const VOTOnlyBypassMediaCSP$i = "Używaj go tylko do omijania mediów CSP";
  const VOTNewAudioPlayer$i = "Użyj nowego odtwarzacza audio";
  const VOTUseNewModel$i = "Użyj eksperymentalnej odmiany głosów Yandex dla niektórych filmów";
  const TranslationDelayed$i = "Tłumaczenie jest nieco opóźnione";
  const VOTTranslationCompletedNotify$i = "Tłumaczenie na {0} zostało zakończone!";
  const VOTSendNotifyOnComplete$i = "Wyślij powiadomienie, że film został przetłumaczony";
  const VOTBugReport$i = "Zgłoś błąd";
  const VOTTranslateProxyDisabled$i = "Niepełnosprawni";
  const VOTTranslateProxyEnabled$i = "Włączone";
  const VOTTranslateProxyEverything$i = "Proxy wszystko";
  const VOTTranslateProxyStatus$i = "Tryb proxy";
  const VOTTranslatedBy$i = "Tłumaczenie {0}";
  const VOTStreamNotAvailable$i = "Translate stream nie jest dostępny";
  const VOTTranslationTextService$i = "Usługa tłumaczenia tekstów";
  const VOTNotAffectToVoice$i = "Nie wpływa na tłumaczenie tekstu w voice over";
  const DontTranslateSelectedLanguages$i = "Nie tłumacz z wybranych języków";
  const showVideoVolumeSlider$i = "Wyświetl suwak głośności wideo";
  const hotkeysSettings$i = "Ustawienia skrótów klawiszowych";
  const None$i = "Brak";
  const VOTUseLivelyVoice$i = "Używaj żywych głosów. Głośniki brzmią jak rodzimi Rosjanie.";
  const miscSettings$i = "Różne ustawienia";
  const services$i = { "yandexbrowser": "Yandex Browser", "msedge": "Microsoft Edge", "rust-server": "Rust Server" };
  const aboutExtension$i = "O rozszerzeniu";
  const appearance$i = "Wygląd";
  const buttonPosition$i = "Pozycja przycisku w odtwarzaczu";
  const position$i = { "left": "Lewo", "right": "Prawo", "top": "Top", "default": "Default" };
  const secs$i = "secs";
  const autoHideButtonDelay$i = "Opóźnienie przed ukryciem przycisku tłumaczenia";
  const notFound$i = "nie znaleziono";
  const minButtonPositionContainer$i = "Pozycja przycisku zmienia się tylko u graczy większych niż 600 pikseli.";
  const VOTTranslateProxyStatusDefault$i = "Całkowite wyłączenie proxy w Twoim kraju może przerwać rozszerzenie";
  const PressTheKeyCombination$i = "Naciśnij kombinację klawiszy...";
  const VOTUseAudioDownload$i = "Użyj pobierania audio";
  const VOTUseAudioDownloadWarning$i = "Wyłączenie pobierania dźwięku może wpłynąć na funkcjonalność rozszerzenia";
  const VOTAccountRequired$i = "Aby korzystać z tej funkcji, musisz się zalogować";
  const VOTMyAccount$i = "Moje konto";
  const VOTLogin$i = "Login";
  const VOTLogout$i = "Wyloguj się";
  const VOTRefresh$i = "Odśwież";
  const VOTYandexToken$i = "Wprowadź Token Yandex OAuth";
  const VOTYandexTokenInfo$i = "W tym polu możesz ręcznie ustawić token konta. Należy pamiętać, że nie sprawdzamy jego ważności przed wysłaniem żądania tłumaczenia";
  const VOTLoginViaToken$i = "Zaloguj się przez token";
  const subtitlesSmartLayout$i = "Inteligentny układ napisów";
  const smartDucking$i = "Adaptacyjna objętość";
  const VOTAutoSubtitles$i = "Napisy na open";
  const VOTSubtitlesFont$i = "Czcionka napisów";
  const __vite_glob_0_43 = {
    recommended: recommended$i,
    translateVideo: translateVideo$i,
    disableTranslate: disableTranslate$i,
    translationSettings: translationSettings$i,
    subtitlesSettings: subtitlesSettings$i,
    resetSettings: resetSettings$i,
    videoBeingTranslated: videoBeingTranslated$i,
    videoLanguage: videoLanguage$i,
    translationLanguage: translationLanguage$i,
    translationTake: translationTake$i,
    translationTakeMoreThanHour: translationTakeMoreThanHour$i,
    translationTakeAboutMinute: translationTakeAboutMinute$i,
    translationTakeFewMinutes: translationTakeFewMinutes$i,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$i,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$i,
    requestTranslationFailed: requestTranslationFailed$i,
    audioNotReceived: audioNotReceived$i,
    audioFormatNotSupported: audioFormatNotSupported$i,
    VOTAutoTranslate: VOTAutoTranslate$i,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$i,
    VOTVolume: VOTVolume$i,
    VOTVolumeTranslation: VOTVolumeTranslation$i,
    VOTAutoSetVolume: VOTAutoSetVolume$i,
    VOTShowVideoSlider: VOTShowVideoSlider$i,
    VOTSyncVolume: VOTSyncVolume$i,
    VOTDisableFromYourLang: VOTDisableFromYourLang$i,
    VOTVideoIsTooLong: VOTVideoIsTooLong$i,
    VOTNoVideoIDFound: VOTNoVideoIDFound$i,
    VOTSubtitles: VOTSubtitles$i,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$i,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$i,
    VOTHighlightWords: VOTHighlightWords$i,
    VOTTranslatedFrom: VOTTranslatedFrom$i,
    VOTAutogenerated: VOTAutogenerated$i,
    VOTSettings: VOTSettings$i,
    VOTMenuLanguage: VOTMenuLanguage$i,
    VOTAuthors: VOTAuthors$i,
    VOTVersion: VOTVersion$i,
    VOTLoader: VOTLoader$i,
    VOTBrowser: VOTBrowser$i,
    VOTShowPiPButton: VOTShowPiPButton$i,
    langs: langs$i,
    streamNoConnectionToServer: streamNoConnectionToServer$i,
    searchField: searchField$i,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$i,
    VOTDetectService: VOTDetectService$i,
    VOTProxyWorkerHost: VOTProxyWorkerHost$i,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$i,
    proxySettings: proxySettings$i,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$i,
    VOTAudioBooster: VOTAudioBooster$i,
    VOTSubtitlesDesign: VOTSubtitlesDesign$i,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$i,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$i,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$i,
    VOTDownloadWithName: VOTDownloadWithName$i,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$i,
    VOTLocaleHash: VOTLocaleHash$i,
    VOTUpdatedAt: VOTUpdatedAt$i,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$i,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$i,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$i,
    VOTNewAudioPlayer: VOTNewAudioPlayer$i,
    VOTUseNewModel: VOTUseNewModel$i,
    TranslationDelayed: TranslationDelayed$i,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$i,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$i,
    VOTBugReport: VOTBugReport$i,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$i,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$i,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$i,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$i,
    VOTTranslatedBy: VOTTranslatedBy$i,
    VOTStreamNotAvailable: VOTStreamNotAvailable$i,
    VOTTranslationTextService: VOTTranslationTextService$i,
    VOTNotAffectToVoice: VOTNotAffectToVoice$i,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$i,
    showVideoVolumeSlider: showVideoVolumeSlider$i,
    hotkeysSettings: hotkeysSettings$i,
    None: None$i,
    VOTUseLivelyVoice: VOTUseLivelyVoice$i,
    miscSettings: miscSettings$i,
    services: services$i,
    aboutExtension: aboutExtension$i,
    appearance: appearance$i,
    buttonPosition: buttonPosition$i,
    position: position$i,
    secs: secs$i,
    autoHideButtonDelay: autoHideButtonDelay$i,
    notFound: notFound$i,
    minButtonPositionContainer: minButtonPositionContainer$i,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$i,
    PressTheKeyCombination: PressTheKeyCombination$i,
    VOTUseAudioDownload: VOTUseAudioDownload$i,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$i,
    VOTAccountRequired: VOTAccountRequired$i,
    VOTMyAccount: VOTMyAccount$i,
    VOTLogin: VOTLogin$i,
    VOTLogout: VOTLogout$i,
    VOTRefresh: VOTRefresh$i,
    VOTYandexToken: VOTYandexToken$i,
    VOTYandexTokenInfo: VOTYandexTokenInfo$i,
    VOTLoginViaToken: VOTLoginViaToken$i,
    subtitlesSmartLayout: subtitlesSmartLayout$i,
    smartDucking: smartDucking$i,
    VOTAutoSubtitles: VOTAutoSubtitles$i,
    VOTSubtitlesFont: VOTSubtitlesFont$i
  };
  const recommended$h = "recomendado";
  const translateVideo$h = "Traduzir vídeo";
  const disableTranslate$h = "Desativar";
  const translationSettings$h = "Configurações de tradução";
  const subtitlesSettings$h = "Configurações de legendas";
  const resetSettings$h = "Redefinir configurações";
  const videoBeingTranslated$h = "Vídeo sendo traduzido";
  const videoLanguage$h = "Idioma do vídeo";
  const translationLanguage$h = "Idioma de tradução";
  const translationTake$h = "A tradução levará";
  const translationTakeMoreThanHour$h = "A tradução levará mais de uma hora";
  const translationTakeAboutMinute$h = "A tradução levará cerca de um minuto";
  const translationTakeFewMinutes$h = "A tradução levará alguns minutos";
  const translationTakeApproximatelyMinutes$h = "A tradução levará aproximadamente {0} minutos";
  const translationTakeApproximatelyMinute$h = "A tradução levará aproximadamente {0} minuto";
  const requestTranslationFailed$h = "Falha ao solicitar a tradução do vídeo";
  const audioNotReceived$h = "Link de áudio não recebido";
  const audioFormatNotSupported$h = "Formato de áudio não suportado";
  const VOTAutoTranslate$h = "Traduzir automaticamente ao abrir";
  const VOTDontTranslateYourLang$h = "Não traduzir do seu idioma nativo";
  const VOTVolume$h = "Volume do vídeo:";
  const VOTVolumeTranslation$h = "Volume de tradução:";
  const VOTAutoSetVolume$h = "Reduza o volume do vídeo para";
  const VOTShowVideoSlider$h = "Mostrar controle de volume do vídeo";
  const VOTSyncVolume$h = "Sincronizar volume da tradução e do vídeo";
  const VOTDisableFromYourLang$h = "Você desativou a tradução de vídeo em seu idioma";
  const VOTVideoIsTooLong$h = "O vídeo é muito longo";
  const VOTNoVideoIDFound$h = "Nenhum ID de vídeo encontrado";
  const VOTSubtitles$h = "Legendas";
  const VOTSubtitlesDisabled$h = "Desativado";
  const VOTSubtitlesMaxLength$h = "Comprimento máximo das legendas";
  const VOTHighlightWords$h = "Destacar palavras";
  const VOTTranslatedFrom$h = "Traduzido do";
  const VOTAutogenerated$h = "Gerado automaticamente";
  const VOTSettings$h = "VOT Configurações";
  const VOTMenuLanguage$h = "Idioma do menu";
  const VOTAuthors$h = "Autores";
  const VOTVersion$h = "Versão";
  const VOTLoader$h = "Carregador";
  const VOTBrowser$h = "Navegador";
  const VOTShowPiPButton$h = "Mostrar botão PiP";
  const langs$h = { "auto": "Automaticamente", "az": "Azerbaijano", "ay": "Aymara", "ak": "Akan", "sq": "Albanês", "am": "Amárico", "en": "Inglês", "ar": "Árabe", "hy": "Armênio", "as": "Assamês", "af": "Afrikaans", "eu": "Basco", "be": "Bielorrusso", "bn": "Bengali", "my": "Birmanês", "bg": "Búlgaro", "bs": "Bósnio", "bho": "Bhojpuri", "cy": "Galês", "hu": "Húngaro", "vi": "Vietnamita", "haw": "Havaiano", "ht": "Haitiano", "gl": "Galego", "lg": "Ganda", "el": "Grego", "ka": "Georgiano", "gn": "Guarani", "gu": "Gujarati", "gd": "Gaélico escocês", "da": "Dinamarquês", "fy": "Frisão ocidental", "zu": "Zulu", "iw": "Hebraico", "ig": "Igbo", "yi": "Yiddish", "id": "Indonésio", "ga": "Irlandês", "is": "Islandês", "es": "Espanhol", "it": "Italiano", "yo": "Iorubá", "kk": "Cazaque", "kn": "Canarês", "ca": "Catalão", "qu": "Quéchua", "rw": "Kinyarwanda", "ky": "Quirguiz", "zh": "Chinês", "zh-Hant": "Chinês tradicional", "zh-Hans": "Chinês simplificado", "ko": "Coreano", "co": "Córsico", "xh": "Xhosa", "ku": "Curdo", "km": "Khmer", "lo": "Lao", "la": "Latim", "lv": "Letão", "ln": "Lingala", "lt": "Lituano", "lb": "Luxemburguês", "mk": "Macedônio", "mg": "Malgaxe", "ms": "Malaio", "ml": "Malaiala", "dv": "Maldívio", "mt": "Maltês", "mi": "Maori", "mr": "Marata", "mn": "Mongol", "de": "Alemão", "ne": "Nepalês", "nl": "Holandês", "no": "Norueguês", "ny": "Chichewa", "or": "Oriá", "om": "Oromo", "pa": "Punjabi", "fa": "Persa", "pl": "Polonês", "pt": "Português", "ps": "Pashto", "ro": "Romeno", "ru": "Russo", "sm": "Samoano", "sa": "Sânscrito", "ceb": "Cebuano", "nso": "Soto do Norte", "sr": "Sérvio", "si": "Cingalês", "sd": "Sindi", "sk": "Eslovaco", "sl": "Esloveno", "so": "Somali", "sw": "Suaíli", "su": "Sundanês", "tg": "Tajique", "th": "Tailandês", "ta": "Tâmil", "tt": "Tártaro", "te": "Telugu", "ti": "Tigrínia", "ts": "Tsonga", "tr": "Turco", "tk": "Turcomeno", "uz": "Uzbeque", "ug": "Uigur", "uk": "Ucraniano", "ur": "Urdu", "fi": "Finlandês", "fr": "Francês", "ha": "Hausa", "hi": "Hindi", "hmn": "Hmong", "hr": "Croata", "cs": "Tcheco", "sv": "Sueco", "sn": "Shona", "ee": "Ewe", "eo": "Esperanto", "et": "Estoniano", "st": "Soto do Sul", "jv": "Javanês", "ja": "Japonês", "kri": "Crioulo" };
  const streamNoConnectionToServer$h = "Não há conexão com o servidor";
  const searchField$h = "Procura...";
  const VOTTranslateAPIErrors$h = "Traduzir erros da API";
  const VOTDetectService$h = "Serviço de detecção de idioma";
  const VOTProxyWorkerHost$h = "Insira o endereço do proxy worker";
  const VOTM3u8ProxyHost$h = "Insira o endereço do trabalhador proxy m3u8";
  const proxySettings$h = "Definições De Proxy";
  const translationTakeApproximatelyMinute2$h = "A tradução demora cerca de {0} minutos";
  const VOTAudioBooster$h = "Aumento do volume de tradução alargado";
  const VOTSubtitlesDesign$h = "Design de Legendas";
  const VOTSubtitlesFontSize$h = "Tamanho da fonte das legendas";
  const VOTSubtitlesOpacity$h = "Transparência do fundo do subtítulo";
  const VOTSubtitlesDownloadFormat$h = "O formato para baixar legendas";
  const VOTDownloadWithName$h = "Baixar arquivos com o nome do vídeo";
  const VOTUpdateLocaleFiles$h = "Actualizar ficheiros de localização";
  const VOTLocaleHash$h = "Hash de localidade";
  const VOTUpdatedAt$h = "Actualizado às";
  const VOTNeedWebAudioAPI$h = "Para habilitar isso, você deve ter uma API de áudio da Web";
  const VOTMediaCSPEnabledOnSite$h = "O Media CSP está ativado neste site";
  const VOTOnlyBypassMediaCSP$h = "Use-o apenas para ignorar o CSP de mídia";
  const VOTNewAudioPlayer$h = "Utilizar o novo leitor de áudio";
  const VOTUseNewModel$h = "Use uma variação experimental de vozes Yandex para alguns vídeos";
  const TranslationDelayed$h = "A tradução está ligeiramente atrasada";
  const VOTTranslationCompletedNotify$h = "A tradução no {0} foi concluída!";
  const VOTSendNotifyOnComplete$h = "Enviar uma notificação de que o vídeo foi traduzido";
  const VOTBugReport$h = "Reportar um erro";
  const VOTTranslateProxyDisabled$h = "Deficientes";
  const VOTTranslateProxyEnabled$h = "Activado";
  const VOTTranslateProxyEverything$h = "Proxy tudo";
  const VOTTranslateProxyStatus$h = "Modo Proxy";
  const VOTTranslatedBy$h = "Traduzido por {0}";
  const VOTStreamNotAvailable$h = "Traduzir stream não está disponível";
  const VOTTranslationTextService$h = "Serviço de tradução de textos";
  const VOTNotAffectToVoice$h = "Não afeta a tradução de texto em voice over";
  const DontTranslateSelectedLanguages$h = "Não traduza de idiomas selecionados";
  const showVideoVolumeSlider$h = "Exibir o controle deslizante de volume do vídeo";
  const hotkeysSettings$h = "Definições das teclas de atalho";
  const None$h = "Nenhum";
  const VOTUseLivelyVoice$h = "Use vozes vivas. Os falantes soam como russos nativos.";
  const miscSettings$h = "Configurações diversas";
  const services$h = { "yandexbrowser": "Yandex Browser", "msedge": "Microsoft Edge", "rust-server": "Servidor Rust" };
  const aboutExtension$h = "Sobre a extensão";
  const appearance$h = "Aparência";
  const buttonPosition$h = "Posição do botão no leitor";
  const position$h = { "left": "Esquerda", "right": "Direito", "top": "Top", "default": "Defeito" };
  const secs$h = "secs";
  const autoHideButtonDelay$h = "Atraso antes de ocultar o botão Traduzir";
  const notFound$h = "não encontrado";
  const minButtonPositionContainer$h = "A posição do botão só muda em Jogadores com mais de 600 píxeis.";
  const VOTTranslateProxyStatusDefault$h = "Desativar completamente o proxy no seu país pode quebrar a extensão";
  const PressTheKeyCombination$h = "Prima a combinação de teclas...";
  const VOTUseAudioDownload$h = "Utilizar a transferência de áudio";
  const VOTUseAudioDownloadWarning$h = "A desativação de downloads de áudio pode afetar a funcionalidade da extensão";
  const VOTAccountRequired$h = "Você precisa fazer login para usar esse recurso";
  const VOTMyAccount$h = "A minha conta";
  const VOTLogin$h = "Login";
  const VOTLogout$h = "Sair";
  const VOTRefresh$h = "Atualizar";
  const VOTYandexToken$h = "Insira o token Yandex OAuth";
  const VOTYandexTokenInfo$h = "Você pode definir manualmente o token da conta neste campo. Observe que não verificamos sua validade antes de enviar uma solicitação de tradução";
  const VOTLoginViaToken$h = "Login via token";
  const subtitlesSmartLayout$h = "Smart subtitle layout";
  const smartDucking$h = "Volume adaptativo";
  const VOTAutoSubtitles$h = "Legendas em aberto";
  const VOTSubtitlesFont$h = "Fonte do subtítulo";
  const __vite_glob_0_44 = {
    recommended: recommended$h,
    translateVideo: translateVideo$h,
    disableTranslate: disableTranslate$h,
    translationSettings: translationSettings$h,
    subtitlesSettings: subtitlesSettings$h,
    resetSettings: resetSettings$h,
    videoBeingTranslated: videoBeingTranslated$h,
    videoLanguage: videoLanguage$h,
    translationLanguage: translationLanguage$h,
    translationTake: translationTake$h,
    translationTakeMoreThanHour: translationTakeMoreThanHour$h,
    translationTakeAboutMinute: translationTakeAboutMinute$h,
    translationTakeFewMinutes: translationTakeFewMinutes$h,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$h,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$h,
    requestTranslationFailed: requestTranslationFailed$h,
    audioNotReceived: audioNotReceived$h,
    audioFormatNotSupported: audioFormatNotSupported$h,
    VOTAutoTranslate: VOTAutoTranslate$h,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$h,
    VOTVolume: VOTVolume$h,
    VOTVolumeTranslation: VOTVolumeTranslation$h,
    VOTAutoSetVolume: VOTAutoSetVolume$h,
    VOTShowVideoSlider: VOTShowVideoSlider$h,
    VOTSyncVolume: VOTSyncVolume$h,
    VOTDisableFromYourLang: VOTDisableFromYourLang$h,
    VOTVideoIsTooLong: VOTVideoIsTooLong$h,
    VOTNoVideoIDFound: VOTNoVideoIDFound$h,
    VOTSubtitles: VOTSubtitles$h,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$h,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$h,
    VOTHighlightWords: VOTHighlightWords$h,
    VOTTranslatedFrom: VOTTranslatedFrom$h,
    VOTAutogenerated: VOTAutogenerated$h,
    VOTSettings: VOTSettings$h,
    VOTMenuLanguage: VOTMenuLanguage$h,
    VOTAuthors: VOTAuthors$h,
    VOTVersion: VOTVersion$h,
    VOTLoader: VOTLoader$h,
    VOTBrowser: VOTBrowser$h,
    VOTShowPiPButton: VOTShowPiPButton$h,
    langs: langs$h,
    streamNoConnectionToServer: streamNoConnectionToServer$h,
    searchField: searchField$h,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$h,
    VOTDetectService: VOTDetectService$h,
    VOTProxyWorkerHost: VOTProxyWorkerHost$h,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$h,
    proxySettings: proxySettings$h,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$h,
    VOTAudioBooster: VOTAudioBooster$h,
    VOTSubtitlesDesign: VOTSubtitlesDesign$h,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$h,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$h,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$h,
    VOTDownloadWithName: VOTDownloadWithName$h,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$h,
    VOTLocaleHash: VOTLocaleHash$h,
    VOTUpdatedAt: VOTUpdatedAt$h,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$h,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$h,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$h,
    VOTNewAudioPlayer: VOTNewAudioPlayer$h,
    VOTUseNewModel: VOTUseNewModel$h,
    TranslationDelayed: TranslationDelayed$h,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$h,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$h,
    VOTBugReport: VOTBugReport$h,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$h,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$h,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$h,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$h,
    VOTTranslatedBy: VOTTranslatedBy$h,
    VOTStreamNotAvailable: VOTStreamNotAvailable$h,
    VOTTranslationTextService: VOTTranslationTextService$h,
    VOTNotAffectToVoice: VOTNotAffectToVoice$h,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$h,
    showVideoVolumeSlider: showVideoVolumeSlider$h,
    hotkeysSettings: hotkeysSettings$h,
    None: None$h,
    VOTUseLivelyVoice: VOTUseLivelyVoice$h,
    miscSettings: miscSettings$h,
    services: services$h,
    aboutExtension: aboutExtension$h,
    appearance: appearance$h,
    buttonPosition: buttonPosition$h,
    position: position$h,
    secs: secs$h,
    autoHideButtonDelay: autoHideButtonDelay$h,
    notFound: notFound$h,
    minButtonPositionContainer: minButtonPositionContainer$h,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$h,
    PressTheKeyCombination: PressTheKeyCombination$h,
    VOTUseAudioDownload: VOTUseAudioDownload$h,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$h,
    VOTAccountRequired: VOTAccountRequired$h,
    VOTMyAccount: VOTMyAccount$h,
    VOTLogin: VOTLogin$h,
    VOTLogout: VOTLogout$h,
    VOTRefresh: VOTRefresh$h,
    VOTYandexToken: VOTYandexToken$h,
    VOTYandexTokenInfo: VOTYandexTokenInfo$h,
    VOTLoginViaToken: VOTLoginViaToken$h,
    subtitlesSmartLayout: subtitlesSmartLayout$h,
    smartDucking: smartDucking$h,
    VOTAutoSubtitles: VOTAutoSubtitles$h,
    VOTSubtitlesFont: VOTSubtitlesFont$h
  };
  const recommended$g = "recomandat";
  const translateVideo$g = "Traduceți videoclipul";
  const disableTranslate$g = "Dezactivare";
  const translationSettings$g = "Setări de traducere";
  const subtitlesSettings$g = "Setări subtitrări";
  const resetSettings$g = "Resetați setările";
  const videoBeingTranslated$g = "Videoclipul este tradus";
  const videoLanguage$g = "Limba Video";
  const translationLanguage$g = "Limba de traducere";
  const translationTake$g = "Traducerea va lua";
  const translationTakeMoreThanHour$g = "Traducerea va dura mai mult de o oră";
  const translationTakeAboutMinute$g = "Traducerea va dura aproximativ un minut";
  const translationTakeFewMinutes$g = "Traducerea va dura câteva minute";
  const translationTakeApproximatelyMinutes$g = "Traducerea va dura aproximativ {0} minute";
  const translationTakeApproximatelyMinute$g = "Traducerea va dura aproximativ {0} minute";
  const requestTranslationFailed$g = "Nu s-a solicitat traducerea video";
  const audioNotReceived$g = "Link-ul Audio nu a fost primit";
  const audioFormatNotSupported$g = "Formatul audio nu este acceptat";
  const VOTAutoTranslate$g = "Traduceți pe deschis";
  const VOTDontTranslateYourLang$g = "Nu Traduceți din limba mea";
  const VOTVolume$g = "Volumul Video:";
  const VOTVolumeTranslation$g = "Volumul traducerii:";
  const VOTAutoSetVolume$g = "Reduceți volumul video la";
  const VOTShowVideoSlider$g = "Slider volum Video";
  const VOTSyncVolume$g = "Traducere Link și volum video";
  const VOTDisableFromYourLang$g = "Ați dezactivat traducerea videoclipului în limba dvs";
  const VOTVideoIsTooLong$g = "Videoclipul este prea lung";
  const VOTNoVideoIDFound$g = "Nu a fost găsit niciun ID video";
  const VOTSubtitles$g = "Subtitrari";
  const VOTSubtitlesDisabled$g = "Dezactivat";
  const VOTSubtitlesMaxLength$g = "Subtitrari lungime maxima";
  const VOTHighlightWords$g = "Evidențiați cuvintele";
  const VOTTranslatedFrom$g = "tradus din";
  const VOTAutogenerated$g = "autogenerate";
  const VOTSettings$g = "Setări VOT";
  const VOTMenuLanguage$g = "Limba meniului";
  const VOTAuthors$g = "Autori";
  const VOTVersion$g = "Versiune";
  const VOTLoader$g = "Încărcător";
  const VOTBrowser$g = "Navigator";
  const VOTShowPiPButton$g = "Afișează butonul PiP";
  const langs$g = { "auto": "Auto", "af": "Afrikaans", "ak": "Acan", "sq": "Albaneză", "am": "Amharică", "ar": "Arabă", "hy": "Armeană", "as": "Asameză", "ay": "Aimara", "az": "Azeră", "bn": "Bangla", "eu": "Bască", "be": "Bielorusă", "bho": "Bhojpuri", "bs": "Bosniacă", "bg": "Bulgari", "my": "Birmaneză", "ca": "Catalană", "ceb": "Cebuano", "zh": "Chineză", "zh-Hans": "Chineză (Simplificată)", "zh-Hant": "Chineză (Tradițională)", "co": "Corsica", "hr": "Croată", "cs": "Cehă", "da": "Daneză", "dv": "Divehi", "nl": "Olandeză", "en": "Engleză", "eo": "Esperanto", "et": "Estonă", "ee": "Ovine", "fil": "Filipineză", "fi": "Finlandeză", "fr": "Franceză", "gl": "Galiciană", "lg": "Ganda", "ka": "Georgiană", "de": "Germană", "el": "Greacă", "gn": "Guarani", "gu": "Gujarati", "ht": "Creola Haitiană", "ha": "Hausa", "haw": "Hawaiian", "iw": "Ebraică", "hi": "Hindi", "hmn": "Hmong", "hu": "Maghiară", "is": "Islandeză", "ig": "Igbo", "id": "Indoneziană", "ga": "Irlandeză", "it": "Italiană", "ja": "Japoneză", "jv": "Javanese", "kn": "Kannada", "kk": "Kazah", "km": "Khmerilor", "rw": "Kinyarwanda", "ko": "Coreeană", "kri": "Krio", "ku": "Kurdă", "ky": "Kirghiz", "lo": "Lao", "la": "Latină", "lv": "Letonă", "ln": "Lingala", "lt": "Lituaniană", "lb": "Luxemburgheză", "mk": "Macedoneană", "mg": "Malgașă", "ms": "Malay", "ml": "Malayalam", "mt": "Malteză", "mi": "Mrcomori", "mr": "Marathi", "mn": "Mongolă", "ne": "Nepaleză", "nso": "Sotho De Nord", "no": "Norvegiană", "ny": "Nyanja", "or": "Odia", "om": "Oromo", "ps": "Pașto", "fa": "Persană", "pl": "Poloneză", "pt": "Portugheză", "pa": "Punjabi", "qu": "Quechua", "ro": "Română", "ru": "Rusă", "sm": "Samoa", "sa": "Sanscrită", "gd": "Gaelica Scoțiană", "sr": "Sârbă", "sn": "Shona", "sd": "Sindhi", "si": "Sinhala", "sk": "Slovacă", "sl": "Slovenă", "so": "Somaleză", "st": "Sotho De Sud", "es": "Spaniolă", "su": "Sundanese", "sw": "Swahili", "sv": "Suedeză", "tg": "Tadjică", "ta": "Tamilă", "tt": "Tatar", "te": "Telugu", "th": "Tailandeză", "ti": "Tigrinya", "ts": "Tsonga", "tr": "Turcă", "tk": "Turkmenă", "uk": "Ucraineană", "ur": "Urdu", "ug": "Uiguri", "uz": "Uzbecă", "vi": "Vietnameză", "cy": "Galeză", "fy": "Friza De Vest", "xh": "Xosa", "yi": "Idiș", "yo": "Yoruba", "zu": "Zulu" };
  const streamNoConnectionToServer$g = "Nu există nicio conexiune la server";
  const searchField$g = "Căutare...";
  const VOTTranslateAPIErrors$g = "Traduceți erorile din API";
  const VOTDetectService$g = "Serviciul de detectare a limbii";
  const VOTProxyWorkerHost$g = "Introduceți adresa lucrătorului proxy";
  const VOTM3u8ProxyHost$g = "Introduceți adresa lucrătorului proxy m3u8";
  const proxySettings$g = "Setări Proxy";
  const translationTakeApproximatelyMinute2$g = "Traducerea va dura aproximativ {0} minute";
  const VOTAudioBooster$g = "Creșterea extinsă a volumului de traducere";
  const VOTSubtitlesDesign$g = "Design subtitrari";
  const VOTSubtitlesFontSize$g = "Dimensiunea fontului subtitrărilor";
  const VOTSubtitlesOpacity$g = "Transparența fundalului subtitrării";
  const VOTSubtitlesDownloadFormat$g = "Formatul pentru descărcarea subtitrărilor";
  const VOTDownloadWithName$g = "Descărcați fișiere cu numele videoclipului";
  const VOTUpdateLocaleFiles$g = "Actualizați fișierele de localizare";
  const VOTLocaleHash$g = "Hash Locale";
  const VOTUpdatedAt$g = "Actualizat la";
  const VOTNeedWebAudioAPI$g = "Pentru a activa acest lucru, trebuie să aveți un API Audio Web";
  const VOTMediaCSPEnabledOnSite$g = "Media CSP este activat pe acest site";
  const VOTOnlyBypassMediaCSP$g = "Utilizați-l numai pentru ocolirea CSP Media";
  const VOTNewAudioPlayer$g = "Utilizați noul player audio";
  const VOTUseNewModel$g = "Utilizați o variantă experimentală a vocilor Yandex pentru unele videoclipuri";
  const TranslationDelayed$g = "Traducerea este ușor întârziată";
  const VOTTranslationCompletedNotify$g = "Traducerea pe {0} a fost finalizată!";
  const VOTSendNotifyOnComplete$g = "Trimiteți o notificare că videoclipul a fost tradus";
  const VOTBugReport$g = "Raportați o eroare";
  const VOTTranslateProxyDisabled$g = "Dezactivat";
  const VOTTranslateProxyEnabled$g = "Activat";
  const VOTTranslateProxyEverything$g = "Proxy totul";
  const VOTTranslateProxyStatus$g = "Modul proxy";
  const VOTTranslatedBy$g = "Tradus de {0}";
  const VOTStreamNotAvailable$g = "Translate stream nu este disponibil";
  const VOTTranslationTextService$g = "Serviciul de traducere a textului";
  const VOTNotAffectToVoice$g = "Nu afectează traducerea textului în voice over";
  const DontTranslateSelectedLanguages$g = "Nu Traduceți din limbile selectate";
  const showVideoVolumeSlider$g = "Afișați glisorul de volum video";
  const hotkeysSettings$g = "Setări taste rapide";
  const None$g = "Niciuna";
  const VOTUseLivelyVoice$g = "Folosiți voci pline de viață. Vorbitorii sună ca rușii nativi.";
  const miscSettings$g = "Setări diverse";
  const services$g = { "yandexbrowser": "Yandex Browser", "msedge": "Microsoft Edge", "rust-server": "Rust Server" };
  const aboutExtension$g = "Despre extensie";
  const appearance$g = "Aspect";
  const buttonPosition$g = "Poziția butonului în player";
  const position$g = { "left": "Stânga", "right": "Dreapta", "top": "Sus", "default": "Implicit" };
  const secs$g = "sec";
  const autoHideButtonDelay$g = "Întârziere înainte de a ascunde butonul de traducere";
  const notFound$g = "nu a fost găsit";
  const minButtonPositionContainer$g = "Poziția butonului se schimbă doar la jucătorii mai mari de 600 de pixeli.";
  const VOTTranslateProxyStatusDefault$g = "Dezactivarea completă a proxy-ului în țara dvs. poate rupe extensia";
  const PressTheKeyCombination$g = "Apăsați combinația de taste...";
  const VOTUseAudioDownload$g = "Utilizați descărcarea audio";
  const VOTUseAudioDownloadWarning$g = "Dezactivarea descărcărilor audio poate afecta funcționalitatea extensiei";
  const VOTAccountRequired$g = "Trebuie să vă conectați pentru a utiliza această caracteristică";
  const VOTMyAccount$g = "Contul meu";
  const VOTLogin$g = "Autentificare";
  const VOTLogout$g = "Deconectare";
  const VOTRefresh$g = "Reîmprospătare";
  const VOTYandexToken$g = "Introduceți tokenul Yandex OAuth";
  const VOTYandexTokenInfo$g = "Puteți seta manual simbolul contului în acest câmp. Vă rugăm să rețineți că nu verificăm validitatea acesteia înainte de a trimite o solicitare de traducere";
  const VOTLoginViaToken$g = "Autentificare prin token";
  const subtitlesSmartLayout$g = "Aspect inteligent de subtitrare";
  const smartDucking$g = "Volum adaptiv";
  const VOTAutoSubtitles$g = "Subtitrari pe open";
  const VOTSubtitlesFont$g = "Font subtitrare";
  const __vite_glob_0_45 = {
    recommended: recommended$g,
    translateVideo: translateVideo$g,
    disableTranslate: disableTranslate$g,
    translationSettings: translationSettings$g,
    subtitlesSettings: subtitlesSettings$g,
    resetSettings: resetSettings$g,
    videoBeingTranslated: videoBeingTranslated$g,
    videoLanguage: videoLanguage$g,
    translationLanguage: translationLanguage$g,
    translationTake: translationTake$g,
    translationTakeMoreThanHour: translationTakeMoreThanHour$g,
    translationTakeAboutMinute: translationTakeAboutMinute$g,
    translationTakeFewMinutes: translationTakeFewMinutes$g,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$g,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$g,
    requestTranslationFailed: requestTranslationFailed$g,
    audioNotReceived: audioNotReceived$g,
    audioFormatNotSupported: audioFormatNotSupported$g,
    VOTAutoTranslate: VOTAutoTranslate$g,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$g,
    VOTVolume: VOTVolume$g,
    VOTVolumeTranslation: VOTVolumeTranslation$g,
    VOTAutoSetVolume: VOTAutoSetVolume$g,
    VOTShowVideoSlider: VOTShowVideoSlider$g,
    VOTSyncVolume: VOTSyncVolume$g,
    VOTDisableFromYourLang: VOTDisableFromYourLang$g,
    VOTVideoIsTooLong: VOTVideoIsTooLong$g,
    VOTNoVideoIDFound: VOTNoVideoIDFound$g,
    VOTSubtitles: VOTSubtitles$g,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$g,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$g,
    VOTHighlightWords: VOTHighlightWords$g,
    VOTTranslatedFrom: VOTTranslatedFrom$g,
    VOTAutogenerated: VOTAutogenerated$g,
    VOTSettings: VOTSettings$g,
    VOTMenuLanguage: VOTMenuLanguage$g,
    VOTAuthors: VOTAuthors$g,
    VOTVersion: VOTVersion$g,
    VOTLoader: VOTLoader$g,
    VOTBrowser: VOTBrowser$g,
    VOTShowPiPButton: VOTShowPiPButton$g,
    langs: langs$g,
    streamNoConnectionToServer: streamNoConnectionToServer$g,
    searchField: searchField$g,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$g,
    VOTDetectService: VOTDetectService$g,
    VOTProxyWorkerHost: VOTProxyWorkerHost$g,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$g,
    proxySettings: proxySettings$g,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$g,
    VOTAudioBooster: VOTAudioBooster$g,
    VOTSubtitlesDesign: VOTSubtitlesDesign$g,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$g,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$g,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$g,
    VOTDownloadWithName: VOTDownloadWithName$g,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$g,
    VOTLocaleHash: VOTLocaleHash$g,
    VOTUpdatedAt: VOTUpdatedAt$g,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$g,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$g,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$g,
    VOTNewAudioPlayer: VOTNewAudioPlayer$g,
    VOTUseNewModel: VOTUseNewModel$g,
    TranslationDelayed: TranslationDelayed$g,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$g,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$g,
    VOTBugReport: VOTBugReport$g,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$g,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$g,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$g,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$g,
    VOTTranslatedBy: VOTTranslatedBy$g,
    VOTStreamNotAvailable: VOTStreamNotAvailable$g,
    VOTTranslationTextService: VOTTranslationTextService$g,
    VOTNotAffectToVoice: VOTNotAffectToVoice$g,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$g,
    showVideoVolumeSlider: showVideoVolumeSlider$g,
    hotkeysSettings: hotkeysSettings$g,
    None: None$g,
    VOTUseLivelyVoice: VOTUseLivelyVoice$g,
    miscSettings: miscSettings$g,
    services: services$g,
    aboutExtension: aboutExtension$g,
    appearance: appearance$g,
    buttonPosition: buttonPosition$g,
    position: position$g,
    secs: secs$g,
    autoHideButtonDelay: autoHideButtonDelay$g,
    notFound: notFound$g,
    minButtonPositionContainer: minButtonPositionContainer$g,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$g,
    PressTheKeyCombination: PressTheKeyCombination$g,
    VOTUseAudioDownload: VOTUseAudioDownload$g,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$g,
    VOTAccountRequired: VOTAccountRequired$g,
    VOTMyAccount: VOTMyAccount$g,
    VOTLogin: VOTLogin$g,
    VOTLogout: VOTLogout$g,
    VOTRefresh: VOTRefresh$g,
    VOTYandexToken: VOTYandexToken$g,
    VOTYandexTokenInfo: VOTYandexTokenInfo$g,
    VOTLoginViaToken: VOTLoginViaToken$g,
    subtitlesSmartLayout: subtitlesSmartLayout$g,
    smartDucking: smartDucking$g,
    VOTAutoSubtitles: VOTAutoSubtitles$g,
    VOTSubtitlesFont: VOTSubtitlesFont$g
  };
  const recommended$f = "рекомендуется";
  const translateVideo$f = "Перевести видео";
  const disableTranslate$f = "Отключить";
  const translationSettings$f = "Настройки перевода";
  const subtitlesSettings$f = "Настройки субтитров";
  const resetSettings$f = "Сбросить настройки";
  const videoBeingTranslated$f = "Видео переводится";
  const videoLanguage$f = "Язык видео";
  const translationLanguage$f = "Язык перевода";
  const translationTake$f = "Перевод займёт";
  const translationTakeMoreThanHour$f = "Перевод займёт больше часа";
  const translationTakeAboutMinute$f = "Перевод займёт около минуты";
  const translationTakeFewMinutes$f = "Перевод займёт несколько минут";
  const translationTakeApproximatelyMinutes$f = "Перевод займёт примерно {0} минут";
  const translationTakeApproximatelyMinute$f = "Перевод займёт примерно {0} минуты";
  const requestTranslationFailed$f = "Не удалось запросить перевод видео";
  const audioNotReceived$f = "Не получена ссылка на аудио";
  const audioFormatNotSupported$f = "Формат аудио не поддерживается";
  const VOTFailedDownloadAudio = "Не удалось скачать аудио";
  const VOTStartVideoForTranslation = "Запустите видео для перевода";
  const VOTAudioNotDetected = "Аудио не обнаружено";
  const VOTAudioNotYetAvailable = "Аудио пока недоступно";
  const VOTAutoTranslate$f = "Переводить при открытии";
  const VOTAutoSubtitles$f = "Субтитры при открытии";
  const VOTDontTranslateYourLang$f = "Не переводить с родного языка";
  const VOTVolume$f = "Громкость видео:";
  const VOTVolumeTranslation$f = "Громкость перевода:";
  const VOTAutoSetVolume$f = "Уменьшать громкость видео до";
  const VOTShowVideoSlider$f = "Ползунок громкости видео";
  const VOTSyncVolume$f = "Связать громкость перевода и видео";
  const VOTDisableFromYourLang$f = "Вы отключили перевод видео на вашем языке";
  const VOTVideoIsTooLong$f = "Видео слишком длинное";
  const VOTNoVideoIDFound$f = "Не найден идентификатор (ID) видео";
  const VOTSubtitles$f = "Субтитры";
  const VOTSubtitlesDisabled$f = "Отключены";
  const VOTSubtitlesMaxLength$f = "Максимальная длина субтитров";
  const VOTHighlightWords$f = "Подсвечивать слова";
  const VOTTranslatedFrom$f = "переведено с";
  const VOTAutogenerated$f = "создано автоматически";
  const VOTSettings$f = "Настройки VOT";
  const VOTMenuLanguage$f = "Язык меню";
  const VOTAuthors$f = "Авторы";
  const VOTVersion$f = "Версия";
  const VOTLoader$f = "Загрузчик";
  const VOTBrowser$f = "Браузер";
  const VOTShowPiPButton$f = "Показывать кнопку «Картинка в картинке»";
  const langs$f = { "auto": "Авто", "az": "Азербайджанский", "ay": "Аймара", "ak": "Акан", "sq": "Албанский", "am": "Амхарский", "en": "Английский", "ar": "Арабский", "hy": "Армянский", "as": "Ассамский", "af": "Африкаанс", "eu": "Баскский", "be": "Белорусский", "bn": "Бенгальский", "my": "Бирманский", "bg": "Болгарский", "bs": "Боснийский", "bho": "Бходжпури", "cy": "Валлийский", "hu": "Венгерский", "vi": "Вьетнамский", "haw": "Гавайский", "ht": "Гаитянский", "gl": "Галисийский", "lg": "Ганда", "el": "Греческий", "ka": "Грузинский", "gn": "Гуарани", "gu": "Гуджарати", "gd": "Гэльский", "da": "Датский", "fy": "Западнофризский", "zu": "Зулу", "iw": "Иврит", "ig": "Игбо", "yi": "Идиш", "id": "Индонезийский", "ga": "Ирландский", "is": "Исландский", "es": "Испанский", "it": "Итальянский", "yo": "Йоруба", "kk": "Казахский", "kn": "Каннада", "ca": "Каталанский", "qu": "Кечуа", "rw": "Киньяруанда", "ky": "Киргизский", "zh": "Китайский", "zh-Hant": "Китайский (традиционный)", "zh-Hans": "Китайский (упрощённый)", "ko": "Корейский", "co": "Корсиканский", "xh": "Коса", "ku": "Курдский", "km": "Кхмерский", "lo": "Лаосский", "la": "Латинский", "lv": "Латышский", "ln": "Лингала", "lt": "Литовский", "lb": "Люксембургский", "mk": "Македонский", "mg": "Малагасийский", "ms": "Малайский", "ml": "Малаялам", "dv": "Мальдивский", "mt": "Мальтийский", "mi": "Маори", "mr": "Маратхи", "mn": "Монгольский", "de": "Немецкий", "ne": "Непальский", "nl": "Нидерландский", "no": "Норвежский", "ny": "Ньянджа", "or": "Ория", "om": "Оромо", "pa": "Панджаби", "fa": "Персидский", "pl": "Польский", "pt": "Португальский", "ps": "Пушту", "ro": "Румынский", "ru": "Русский", "sm": "Самоанский", "sa": "Санскрит", "ceb": "Себуано", "nso": "Северный Сото", "sr": "Сербский", "si": "Сингальский", "sd": "Синдхи", "sk": "Словацкий", "sl": "Словенский", "so": "Сомали", "sw": "Суахили", "su": "Сунданский", "tg": "Таджикский", "th": "Тайский", "ta": "Тамильский", "tt": "Татарский", "te": "Телугу", "ti": "Тигринья", "ts": "Тсонга", "tr": "Турецкий", "tk": "Туркменский", "uz": "Узбекский", "ug": "Уйгурский", "uk": "Украинский", "ur": "Урду", "fil": "Филиппинский", "fi": "Финский", "fr": "Французский", "ha": "Хауса", "hi": "Хинди", "hmn": "Хмонг", "hr": "Хорватский", "cs": "Чешский", "sv": "Шведский", "sn": "Шона", "ee": "Эве", "eo": "Эсперанто", "et": "Эстонский", "st": "Южный Сото", "jv": "Яванский", "ja": "Японский", "kri": "Кри" };
  const streamNoConnectionToServer$f = "Нет подключения к серверу";
  const searchField$f = "Поиск...";
  const VOTTranslateAPIErrors$f = "Переводить ошибки из API";
  const VOTDetectService$f = "Сервис определения языка";
  const VOTProxyWorkerHost$f = "Введите адрес worker прокси-сервера";
  const VOTM3u8ProxyHost$f = "Введите адрес m3u8 прокси-сервера";
  const proxySettings$f = "Настройки прокси-сервера";
  const translationTakeApproximatelyMinute2$f = "Перевод займёт примерно {0} минуту";
  const VOTAudioBooster$f = "Расширенное увеличение громкости перевода";
  const VOTSubtitlesDesign$f = "Оформление субтитров";
  const VOTSubtitlesFont$f = "Шрифт субтитров";
  const VOTSubtitlesFontSize$f = "Размер шрифта субтитров";
  const VOTSubtitlesOpacity$f = "Прозрачность фона субтитров";
  const VOTSubtitlesDownloadFormat$f = "Формат для загрузки субтитров";
  const VOTDownloadWithName$f = "Загружать файлы с названием видео";
  const VOTUpdateLocaleFiles$f = "Обновить файлы локализации";
  const VOTLocaleHash$f = "Хеш перевода";
  const VOTUpdatedAt$f = "Обновлено в";
  const VOTNeedWebAudioAPI$f = "Чтобы включить, у вас должен быть Web Audio API";
  const VOTMediaCSPEnabledOnSite$f = "На этом сайте включен Media CSP";
  const VOTOnlyBypassMediaCSP$f = "Использовать только для обхода Media CSP";
  const VOTNewAudioPlayer$f = "Использовать новый аудиоплеер";
  const VOTUseNewModel$f = "Использовать экспериментальный вариант голосов Yandex для некоторых видеороликов";
  const TranslationDelayed$f = "Перевод немного задерживается";
  const VOTTranslationCompletedNotify$f = "Перевод на {0} завершен!";
  const VOTSendNotifyOnComplete$f = "Отправлять уведомление о том, что видео было переведено";
  const VOTBugReport$f = "Сообщить об ошибке";
  const VOTTranslateProxyDisabled$f = "Отключено";
  const VOTTranslateProxyEnabled$f = "Включено";
  const VOTTranslateProxyEverything$f = "Проксировать всё";
  const VOTTranslateProxyStatus$f = "Режим проксирования";
  const VOTTranslatedBy$f = "Переведено {0}";
  const VOTStreamNotAvailable$f = "Трансляция перевода недоступна";
  const VOTTranslationTextService$f = "Сервис перевода текста";
  const VOTNotAffectToVoice$f = "Не влияет на перевод текста при озвучивании";
  const DontTranslateSelectedLanguages$f = "Не переводите с выбранных языков";
  const showVideoVolumeSlider$f = "Отображать ползунок громкости видео";
  const hotkeysSettings$f = "Настройки горячих клавиш";
  const None$f = "Нет";
  const VOTUseLivelyVoice$f = "Использовать живые голоса. Будто все знают русский.";
  const VOTStandardVoices = "Обычные голоса";
  const VOTStandardVoicesDescription = "Быстрая и качественная озвучка";
  const VOTLivelyVoices = "Живые голоса";
  const VOTLivelyVoicesDescription = "Максимальное сходство. Будто все знают русский";
  const VOTVoiceModeMenuTitle = "Режим озвучки";
  const VOTRailTranslateAndDub = "Перевести и озвучить";
  const VOTRailDisableTranslation = "Выключить перевод";
  const VOTRailTranslatingEta = "Переводится, ещё {0}";
  const VOTRailCompactMinute = "~1 мин";
  const VOTRailCompactMinutes = "~{0} мин";
  const VOTRailCompactMoreThanHour = ">1 ч";
  const miscSettings$f = "Другие настройки";
  const services$f = { "yandexbrowser": "Яндекс Браузер", "msedge": "Microsoft Edge", "rust-server": "Сервер на Rust" };
  const aboutExtension$f = "О расширении";
  const appearance$f = "Внешний вид";
  const buttonPosition$f = "Положение кнопки в плеере";
  const position$f = { "left": "Слева", "right": "Справа", "top": "Сверху", "default": "По умолчанию" };
  const secs$f = "сек.";
  const autoHideButtonDelay$f = "Задержка перед скрытием кнопки перевода";
  const notFound$f = "не найдено";
  const minButtonPositionContainer$f = "Положение кнопки меняется только на проигрывателях размером более 600 пикселей.";
  const VOTTranslateProxyStatusDefault$f = "Полное отключение проксирования в вашей стране может привести к поломке расширения";
  const PressTheKeyCombination$f = "Нажмите комбинацию клавиш...";
  const VOTUseAudioDownload$f = "Использовать загрузку аудио";
  const VOTUseAudioDownloadWarning$f = "Отключение загрузки аудио может нарушить работоспособность расширения";
  const VOTAccountRequired$f = "Вам необходимо авторизоваться, чтобы использовать эту функцию";
  const VOTMyAccount$f = "Мой аккаунт";
  const VOTLogin$f = "Авторизоваться";
  const VOTLogout$f = "Выйти";
  const VOTRefresh$f = "Обновить";
  const VOTYandexToken$f = "Введите OAuth-токен Яндекса";
  const VOTYandexTokenInfo$f = "Вы можете вручную указать токен учетной записи в этом поле. Пожалуйста, обратите внимание, что мы не проверяем его действительность перед отправкой запроса на перевод";
  const VOTLoginViaToken$f = "Войти с помощью токена";
  const subtitlesSmartLayout$f = "Умное расположение субтитров";
  const smartDucking$f = "Адаптивная громкость";
  const __vite_glob_0_46 = {
    recommended: recommended$f,
    translateVideo: translateVideo$f,
    disableTranslate: disableTranslate$f,
    translationSettings: translationSettings$f,
    subtitlesSettings: subtitlesSettings$f,
    resetSettings: resetSettings$f,
    videoBeingTranslated: videoBeingTranslated$f,
    videoLanguage: videoLanguage$f,
    translationLanguage: translationLanguage$f,
    translationTake: translationTake$f,
    translationTakeMoreThanHour: translationTakeMoreThanHour$f,
    translationTakeAboutMinute: translationTakeAboutMinute$f,
    translationTakeFewMinutes: translationTakeFewMinutes$f,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$f,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$f,
    requestTranslationFailed: requestTranslationFailed$f,
    audioNotReceived: audioNotReceived$f,
    audioFormatNotSupported: audioFormatNotSupported$f,
    VOTFailedDownloadAudio,
    VOTStartVideoForTranslation,
    VOTAudioNotDetected,
    VOTAudioNotYetAvailable,
    VOTAutoTranslate: VOTAutoTranslate$f,
    VOTAutoSubtitles: VOTAutoSubtitles$f,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$f,
    VOTVolume: VOTVolume$f,
    VOTVolumeTranslation: VOTVolumeTranslation$f,
    VOTAutoSetVolume: VOTAutoSetVolume$f,
    VOTShowVideoSlider: VOTShowVideoSlider$f,
    VOTSyncVolume: VOTSyncVolume$f,
    VOTDisableFromYourLang: VOTDisableFromYourLang$f,
    VOTVideoIsTooLong: VOTVideoIsTooLong$f,
    VOTNoVideoIDFound: VOTNoVideoIDFound$f,
    VOTSubtitles: VOTSubtitles$f,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$f,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$f,
    VOTHighlightWords: VOTHighlightWords$f,
    VOTTranslatedFrom: VOTTranslatedFrom$f,
    VOTAutogenerated: VOTAutogenerated$f,
    VOTSettings: VOTSettings$f,
    VOTMenuLanguage: VOTMenuLanguage$f,
    VOTAuthors: VOTAuthors$f,
    VOTVersion: VOTVersion$f,
    VOTLoader: VOTLoader$f,
    VOTBrowser: VOTBrowser$f,
    VOTShowPiPButton: VOTShowPiPButton$f,
    langs: langs$f,
    streamNoConnectionToServer: streamNoConnectionToServer$f,
    searchField: searchField$f,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$f,
    VOTDetectService: VOTDetectService$f,
    VOTProxyWorkerHost: VOTProxyWorkerHost$f,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$f,
    proxySettings: proxySettings$f,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$f,
    VOTAudioBooster: VOTAudioBooster$f,
    VOTSubtitlesDesign: VOTSubtitlesDesign$f,
    VOTSubtitlesFont: VOTSubtitlesFont$f,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$f,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$f,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$f,
    VOTDownloadWithName: VOTDownloadWithName$f,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$f,
    VOTLocaleHash: VOTLocaleHash$f,
    VOTUpdatedAt: VOTUpdatedAt$f,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$f,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$f,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$f,
    VOTNewAudioPlayer: VOTNewAudioPlayer$f,
    VOTUseNewModel: VOTUseNewModel$f,
    TranslationDelayed: TranslationDelayed$f,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$f,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$f,
    VOTBugReport: VOTBugReport$f,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$f,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$f,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$f,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$f,
    VOTTranslatedBy: VOTTranslatedBy$f,
    VOTStreamNotAvailable: VOTStreamNotAvailable$f,
    VOTTranslationTextService: VOTTranslationTextService$f,
    VOTNotAffectToVoice: VOTNotAffectToVoice$f,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$f,
    showVideoVolumeSlider: showVideoVolumeSlider$f,
    hotkeysSettings: hotkeysSettings$f,
    None: None$f,
    VOTUseLivelyVoice: VOTUseLivelyVoice$f,
    VOTStandardVoices,
    VOTStandardVoicesDescription,
    VOTLivelyVoices,
    VOTLivelyVoicesDescription,
    VOTVoiceModeMenuTitle,
    VOTRailTranslateAndDub,
    VOTRailDisableTranslation,
    VOTRailTranslatingEta,
    VOTRailCompactMinute,
    VOTRailCompactMinutes,
    VOTRailCompactMoreThanHour,
    miscSettings: miscSettings$f,
    services: services$f,
    aboutExtension: aboutExtension$f,
    appearance: appearance$f,
    buttonPosition: buttonPosition$f,
    position: position$f,
    secs: secs$f,
    autoHideButtonDelay: autoHideButtonDelay$f,
    notFound: notFound$f,
    minButtonPositionContainer: minButtonPositionContainer$f,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$f,
    PressTheKeyCombination: PressTheKeyCombination$f,
    VOTUseAudioDownload: VOTUseAudioDownload$f,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$f,
    VOTAccountRequired: VOTAccountRequired$f,
    VOTMyAccount: VOTMyAccount$f,
    VOTLogin: VOTLogin$f,
    VOTLogout: VOTLogout$f,
    VOTRefresh: VOTRefresh$f,
    VOTYandexToken: VOTYandexToken$f,
    VOTYandexTokenInfo: VOTYandexTokenInfo$f,
    VOTLoginViaToken: VOTLoginViaToken$f,
    subtitlesSmartLayout: subtitlesSmartLayout$f,
    smartDucking: smartDucking$f
  };
  const recommended$e = "නිර්දේශිත";
  const translateVideo$e = "වීඩියෝ පරිවර්තනය කරන්න";
  const disableTranslate$e = "අක් රිය කරන්න";
  const translationSettings$e = "පරිවර්තන සැකසුම්";
  const subtitlesSettings$e = "උපසිරැසි සැකසුම්";
  const resetSettings$e = "සැකසුම් නැවත සකසන්න";
  const videoBeingTranslated$e = "වීඩියෝ පරිවර්තනය කරයි";
  const videoLanguage$e = "වීඩියෝ භාෂාව";
  const translationLanguage$e = "පරිවර්තන භාෂාව";
  const translationTake$e = "පරිවර්තනය වෙයි";
  const translationTakeMoreThanHour$e = "පරිවර්තනය පැයකට වඩා වැඩි කාලයක් ගතවනු ඇත";
  const translationTakeAboutMinute$e = "පරිවර්තනය විනාඩියක් විතර යයි";
  const translationTakeFewMinutes$e = "පරිවර්තනය විනාඩි කිහිපයක් ගත වනු ඇත";
  const translationTakeApproximatelyMinutes$e = "පරිවර්තනය සඳහා ආසන්න වශයෙන් {0} විනාඩි ගතවනු ඇත";
  const translationTakeApproximatelyMinute$e = "පරිවර්තනය සඳහා ආසන්න වශයෙන් {0} විනාඩි ගතවනු ඇත";
  const requestTranslationFailed$e = "වීඩියෝ පරිවර්තනය ඉල්ලා සිටීමට අසමත්";
  const audioNotReceived$e = "ශ්රව්ය සබැඳිය ලැබී නැත";
  const audioFormatNotSupported$e = "ශ් රව් ය ආකෘතිය සහාය නොදක්වයි";
  const VOTAutoTranslate$e = "විවෘත මත පරිවර්තනය කරන්න";
  const VOTDontTranslateYourLang$e = "මගේ භාෂාවෙන් පරිවර්තනය කරන්න එපා";
  const VOTVolume$e = "වීඩියෝ පරිමාව:";
  const VOTVolumeTranslation$e = "පරිවර්තන පරිමාව:";
  const VOTAutoSetVolume$e = "වීඩියෝ පරිමාව අඩු කරන්න";
  const VOTShowVideoSlider$e = "වීඩියෝ පරිමාව ස්ලයිඩර්";
  const VOTSyncVolume$e = "සබැඳි පරිවර්තනය සහ වීඩියෝ පරිමාව";
  const VOTDisableFromYourLang$e = "ඔබ ඔබේ භාෂාවෙන් වීඩියෝ පරිවර්තනය අක්රිය කර ඇත";
  const VOTVideoIsTooLong$e = "වීඩියෝ එක දිග වැඩියි";
  const VOTNoVideoIDFound$e = "වීඩියෝ හැඳුනුම්පතක් සොයාගත නොහැකි විය";
  const VOTSubtitles$e = "උපසිරැසි";
  const VOTSubtitlesDisabled$e = "අක්රිය කර ඇත";
  const VOTSubtitlesMaxLength$e = "උපසිරැසි උපරිම දිග";
  const VOTHighlightWords$e = "වචන ඉස්මතු කරන්න";
  const VOTTranslatedFrom$e = "පරිවර්තනය කරලා තියෙන්නේ";
  const VOTAutogenerated$e = "ස්වයංක් රීයව ජනනය කරන ලද";
  const VOTSettings$e = "Vot සැකසුම්";
  const VOTMenuLanguage$e = "මෙනු භාෂාව";
  const VOTAuthors$e = "කතුවරුන්";
  const VOTVersion$e = "අනුවාදය";
  const VOTLoader$e = "බඩු පටවන යන්ත් රය";
  const VOTBrowser$e = "බ්රවුසරය";
  const VOTShowPiPButton$e = "පෙප් බොත්තම පෙන්වන්න";
  const langs$e = { "auto": "ස්වයංක්රීය", "af": "අප් රිකාන්ස්", "ak": "ආකාන්", "sq": "ඇල්බේනියානු", "am": "අමහාරි", "ar": "අරාබි", "hy": "ආර්මේනියානු", "as": "අසම්", "ay": "අයිමාරා", "az": "අසර්බයිජාන", "bn": "බංග්ලා", "eu": "බැස්කියානු", "be": "බෙලරුසියානු", "bho": "භෝජ්පුරි", "bs": "බොස්නියානු", "bg": "බල්ගේරියානු", "my": "බර්මානු", "ca": "කැටලන්", "ceb": "සෙබුවානෝ", "zh": "චීන", "zh-Hans": "චීන (සරල)", "zh-Hant": "චීන (සාම්ප්රදායික)", "co": "කොර්සිකන්", "hr": "ක් රොඒෂියානු", "cs": "චෙක්", "da": "ඩෙන්මාර්ක", "dv": "දිවේහි", "nl": "ලන්දේසි", "en": "ඉංග්රීසි", "eo": "එස්පෙරන්ටෝ", "et": "එස්තෝනියානු", "ee": "බැටළු පැටවා", "fil": "පිලිපීන", "fi": "ෆින්ලන්ත", "fr": "ප් රංශ", "gl": "ගැලීසියානු", "lg": "ගන්ඩා", "ka": "ජෝර්ජියානු", "de": "ජර්මානු", "el": "ග් රීක", "gn": "ගුරානි", "gu": "ගුජරාටි", "ht": "හයිටි ක් රෙයෝල්", "ha": "හවුසා", "haw": "හවායි", "iw": "හෙබ් රෙව්", "hi": "හින්දි", "hmn": "හ්මොන්ග්", "hu": "හංගේරියානු", "is": "අයිස්ලන්ත", "ig": "ඉග්බෝ", "id": "ඉන්දුනීසියානු", "ga": "අයිරිෂ්", "it": "ඉතාලි", "ja": "ජපන්", "jv": "ජාවා", "kn": "කන්නාඩිය", "kk": "කසකස්තානය", "km": "Khmer", "rw": "කිනියර්වාන්ඩා", "ko": "කොරියානු", "kri": "ක් රියෝ", "ku": "කුර්දි", "ky": "කිර්ගිස්", "lo": "ලාඕ", "la": "ලතින්", "lv": "ලැට්වියානු", "ln": "ලින්ගලා", "lt": "ලිතුවේනියානු", "lb": "ලක්සම්බර්ග්", "mk": "මැසිඩෝනියානු", "mg": "මැලගසි", "ms": "මැලේ", "ml": "මලයාලම්", "mt": "මෝල්ටානු", "mi": "මාඕරි", "mr": "මරාති", "mn": "මොන්ගෝලියානු", "ne": "නේපාල", "nso": "උතුරු සොතෝ", "no": "නෝර්වේ", "ny": "නන්ජා", "or": "ඔඩියා", "om": "ඔරෝමෝ", "ps": "පෂ්ටෝ", "fa": "පර්සියානු", "pl": "පෝලන්ත භාෂාව", "pt": "පෘතුගීසි", "pa": "පන්ජාබි", "qu": "කුචුවා", "ro": "රුමේනියානු", "ru": "රුසියානු", "sm": "සමෝවා", "sa": "සංස්කෘත", "gd": "ස්කොට්ලන්ත ගැලික් භාෂාව", "sr": "සර්බියානු", "sn": "ෂෝනා", "sd": "සිංහල", "si": "සිංහල", "sk": "ස්ලෝවැකියාව", "sl": "ස්ලොවේනියානු", "so": "සෝමාලියානු", "st": "දකුණු සොතෝ", "es": "ස්පාඤ්ඤ භාෂාව", "su": "සුන්දන්", "sw": "ස්වහීලි", "sv": "ස්වීඩන් භාෂාව", "tg": "තාජික", "ta": "දෙමළ", "tt": "ටාටාර්", "te": "තෙලිගු", "th": "තායි", "ti": "ටිග් රියා", "ts": "ට්සොන්ගා", "tr": "තුර්කි", "tk": "තුර්කි ජාතිකයන්", "uk": "යුක්රේනියානු", "ur": "උර්දු", "ug": "උයිගර්", "uz": "උස්බෙකිස්ථානය", "vi": "වියට්නාම", "cy": "වේල්ස්", "fy": "බටහිර ෆ් රිසියානු", "xh": "Xhosa", "yi": "යීඩිෂ්", "yo": "යෝරුබා", "zu": "සුලු" };
  const streamNoConnectionToServer$e = "සර්වර් එකට කිසිම සම්බන්ධයක් නෑ";
  const searchField$e = "හොයන්න...";
  const VOTTranslateAPIErrors$e = "API වෙතින් දෝෂ පරිවර්තනය කරන්න";
  const VOTDetectService$e = "භාෂා හඳුනාගැනීමේ සේවාව";
  const VOTProxyWorkerHost$e = "ප්රොක්සි සේවක ලිපිනය ඇතුලත් කරන්න";
  const VOTM3u8ProxyHost$e = "M3u8 ප්රොක්සි සේවකයාගේ ලිපිනය ඇතුලත් කරන්න";
  const proxySettings$e = "ප්රොක්සි සැකසුම්";
  const translationTakeApproximatelyMinute2$e = "පරිවර්තනය සඳහා ආසන්න වශයෙන් {0} විනාඩි ගතවනු ඇත";
  const VOTAudioBooster$e = "දීර්ඝ පරිවර්තන පරිමාව වැඩි කිරීම";
  const VOTSubtitlesDesign$e = "උපසිරැසි නිර්මාණය";
  const VOTSubtitlesFontSize$e = "උපසිරැසි වල අකුරු ප්රමාණය";
  const VOTSubtitlesOpacity$e = "උපසිරැසි පසුබිමේ විනිවිදභාවය";
  const VOTSubtitlesDownloadFormat$e = "උපසිරැසි බාගත කිරීම සඳහා ආකෘතිය";
  const VOTDownloadWithName$e = "වීඩියෝ නාමය සහිත ගොනු බාගත කරන්න";
  const VOTUpdateLocaleFiles$e = "දේශීයකරණ ගොනු යාවත්කාලීන කරන්න";
  const VOTLocaleHash$e = "දේශීය හැෂ්";
  const VOTUpdatedAt$e = "ට යාවත්කාලීන කරන ලදි";
  const VOTNeedWebAudioAPI$e = "මෙය සක් රීය කිරීම සඳහා, ඔබට වෙබ් ශ් රව් ය API එකක් තිබිය යුතුය";
  const VOTMediaCSPEnabledOnSite$e = "මෙම වෙබ් අඩවියේ csp මාධ් ය සක් රීය කර ඇත";
  const VOTOnlyBypassMediaCSP$e = "මාධ්ය CSP මඟ හැරීම සඳහා පමණක් එය භාවිතා කරන්න";
  const VOTNewAudioPlayer$e = "නව ශ්රව්ය වාදකය භාවිතා කරන්න";
  const VOTUseNewModel$e = "සමහර වීඩියෝ සඳහා Yandex voices හි පර්යේෂණාත්මක විචලනයක් භාවිතා කරන්න";
  const TranslationDelayed$e = "පරිවර්තනය ටිකක් ප්රමාදයි";
  const VOTTranslationCompletedNotify$e = "{0} හි පරිවර්තනය සම්පූර්ණ කර ඇත!";
  const VOTSendNotifyOnComplete$e = "වීඩියෝව පරිවර්තනය කර ඇති බවට දැනුම් දීමක් යවන්න";
  const VOTBugReport$e = "දෝෂයක් වාර්තා කරන්න";
  const VOTTranslateProxyDisabled$e = "අක්රිය කර ඇත";
  const VOTTranslateProxyEnabled$e = "සක් රීය කර ඇත";
  const VOTTranslateProxyEverything$e = "ප්රොක්සි සියල්ල";
  const VOTTranslateProxyStatus$e = "ප් රොක්සි මාදිලිය";
  const VOTTranslatedBy$e = "පරිවර්තනය {0}";
  const VOTStreamNotAvailable$e = "පරිවර්තන ධාරාව ලබා ගත නොහැක";
  const VOTTranslationTextService$e = "පෙළ පරිවර්තන සේවාව";
  const VOTNotAffectToVoice$e = "කටහඬින් පෙළ පරිවර්තනය කිරීම බලපාන්නේ නැහැ";
  const DontTranslateSelectedLanguages$e = "තෝරාගත් භාෂා වලින් පරිවර්තනය නොකරන්න";
  const showVideoVolumeSlider$e = "වීඩියෝ පරිමාව ස්ලයිඩරය පෙන්වන්න";
  const hotkeysSettings$e = "හොට්කීස් සැකසුම්";
  const None$e = "කිසිවක් නැත";
  const VOTUseLivelyVoice$e = "සජීවී හඬවල් භාවිතා කරන්න. කථිකයන් රුසියානු ජාතිකයන් වගේ.";
  const miscSettings$e = "විවිධ සැකසුම්";
  const services$e = { "yandexbrowser": "යාන්ඩෙක්ස් බ්රව්සරය", "msedge": "මයික් රොසොෆ්ට් එජ්", "rust-server": "රස්ට් සේවාදායකය" };
  const aboutExtension$e = "දිගුව ගැන";
  const appearance$e = "පෙනුම";
  const buttonPosition$e = "ක්රීඩකයා තුළ බොත්තම පිහිටීම";
  const position$e = { "left": "වමට", "right": "හරි", "top": "ඉහළ", "default": "පෙරනිමි" };
  const secs$e = "තත්පර";
  const autoHideButtonDelay$e = "පරිවර්තනය බොත්තම සැඟවීමට පෙර ප්රමාද";
  const notFound$e = "සොයාගත නොහැක";
  const minButtonPositionContainer$e = "බොත්තමේ පිහිටීම වෙනස් වෙන්නේ පික්සල් 600ට වඩා විශාල ක් රීඩකයන් තුළ පමණයි.";
  const VOTTranslateProxyStatusDefault$e = "සම්පූර්ණයෙන්ම අක්රිය ඔබගේ රටේ proxying දීර්ඝ බිඳ දැමිය හැක";
  const PressTheKeyCombination$e = "යතුරු සංයෝගය ඔබන්න...";
  const VOTUseAudioDownload$e = "ශ්රව්ය බාගත භාවිතා කරන්න";
  const VOTUseAudioDownloadWarning$e = "ශ් රව් ය බාගත කිරීම් අක් රිය කිරීම දිගු කිරීමේ ක් රියාකාරිත්වයට බලපෑම් කළ හැකිය";
  const VOTAccountRequired$e = "මෙම විශේෂාංගය භාවිතා කිරීමට ඔබ පිවිසිය යුතුය";
  const VOTMyAccount$e = "මගේ ගිණුම";
  const VOTLogin$e = "පිවිසෙන්න";
  const VOTLogout$e = "පිටවීම";
  const VOTRefresh$e = "නැවුම් කරන්න";
  const VOTYandexToken$e = "Yandex Oauth ටෝකනය ඇතුලත් කරන්න";
  const VOTYandexTokenInfo$e = "ඔබට මෙම ක්ෂේත් රයේ ගිණුම් ටෝකනය අතින් සකස් කළ හැකිය. පරිවර්තන ඉල්ලීමක් යැවීමට පෙර අපි එහි වලංගුභාවය පරීක්ෂා නොකරන බව කරුණාවෙන් සලකන්න";
  const VOTLoginViaToken$e = "Token හරහා පිවිසෙන්න";
  const subtitlesSmartLayout$e = "ස්මාර්ට් උපසිරැසි සැකැස්ම";
  const smartDucking$e = "අනුවර්තන පරිමාව";
  const VOTAutoSubtitles$e = "විවෘත උපසිරැසි";
  const VOTSubtitlesFont$e = "උපසිරැසි අකුරු";
  const __vite_glob_0_47 = {
    recommended: recommended$e,
    translateVideo: translateVideo$e,
    disableTranslate: disableTranslate$e,
    translationSettings: translationSettings$e,
    subtitlesSettings: subtitlesSettings$e,
    resetSettings: resetSettings$e,
    videoBeingTranslated: videoBeingTranslated$e,
    videoLanguage: videoLanguage$e,
    translationLanguage: translationLanguage$e,
    translationTake: translationTake$e,
    translationTakeMoreThanHour: translationTakeMoreThanHour$e,
    translationTakeAboutMinute: translationTakeAboutMinute$e,
    translationTakeFewMinutes: translationTakeFewMinutes$e,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$e,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$e,
    requestTranslationFailed: requestTranslationFailed$e,
    audioNotReceived: audioNotReceived$e,
    audioFormatNotSupported: audioFormatNotSupported$e,
    VOTAutoTranslate: VOTAutoTranslate$e,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$e,
    VOTVolume: VOTVolume$e,
    VOTVolumeTranslation: VOTVolumeTranslation$e,
    VOTAutoSetVolume: VOTAutoSetVolume$e,
    VOTShowVideoSlider: VOTShowVideoSlider$e,
    VOTSyncVolume: VOTSyncVolume$e,
    VOTDisableFromYourLang: VOTDisableFromYourLang$e,
    VOTVideoIsTooLong: VOTVideoIsTooLong$e,
    VOTNoVideoIDFound: VOTNoVideoIDFound$e,
    VOTSubtitles: VOTSubtitles$e,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$e,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$e,
    VOTHighlightWords: VOTHighlightWords$e,
    VOTTranslatedFrom: VOTTranslatedFrom$e,
    VOTAutogenerated: VOTAutogenerated$e,
    VOTSettings: VOTSettings$e,
    VOTMenuLanguage: VOTMenuLanguage$e,
    VOTAuthors: VOTAuthors$e,
    VOTVersion: VOTVersion$e,
    VOTLoader: VOTLoader$e,
    VOTBrowser: VOTBrowser$e,
    VOTShowPiPButton: VOTShowPiPButton$e,
    langs: langs$e,
    streamNoConnectionToServer: streamNoConnectionToServer$e,
    searchField: searchField$e,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$e,
    VOTDetectService: VOTDetectService$e,
    VOTProxyWorkerHost: VOTProxyWorkerHost$e,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$e,
    proxySettings: proxySettings$e,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$e,
    VOTAudioBooster: VOTAudioBooster$e,
    VOTSubtitlesDesign: VOTSubtitlesDesign$e,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$e,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$e,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$e,
    VOTDownloadWithName: VOTDownloadWithName$e,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$e,
    VOTLocaleHash: VOTLocaleHash$e,
    VOTUpdatedAt: VOTUpdatedAt$e,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$e,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$e,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$e,
    VOTNewAudioPlayer: VOTNewAudioPlayer$e,
    VOTUseNewModel: VOTUseNewModel$e,
    TranslationDelayed: TranslationDelayed$e,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$e,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$e,
    VOTBugReport: VOTBugReport$e,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$e,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$e,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$e,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$e,
    VOTTranslatedBy: VOTTranslatedBy$e,
    VOTStreamNotAvailable: VOTStreamNotAvailable$e,
    VOTTranslationTextService: VOTTranslationTextService$e,
    VOTNotAffectToVoice: VOTNotAffectToVoice$e,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$e,
    showVideoVolumeSlider: showVideoVolumeSlider$e,
    hotkeysSettings: hotkeysSettings$e,
    None: None$e,
    VOTUseLivelyVoice: VOTUseLivelyVoice$e,
    miscSettings: miscSettings$e,
    services: services$e,
    aboutExtension: aboutExtension$e,
    appearance: appearance$e,
    buttonPosition: buttonPosition$e,
    position: position$e,
    secs: secs$e,
    autoHideButtonDelay: autoHideButtonDelay$e,
    notFound: notFound$e,
    minButtonPositionContainer: minButtonPositionContainer$e,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$e,
    PressTheKeyCombination: PressTheKeyCombination$e,
    VOTUseAudioDownload: VOTUseAudioDownload$e,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$e,
    VOTAccountRequired: VOTAccountRequired$e,
    VOTMyAccount: VOTMyAccount$e,
    VOTLogin: VOTLogin$e,
    VOTLogout: VOTLogout$e,
    VOTRefresh: VOTRefresh$e,
    VOTYandexToken: VOTYandexToken$e,
    VOTYandexTokenInfo: VOTYandexTokenInfo$e,
    VOTLoginViaToken: VOTLoginViaToken$e,
    subtitlesSmartLayout: subtitlesSmartLayout$e,
    smartDucking: smartDucking$e,
    VOTAutoSubtitles: VOTAutoSubtitles$e,
    VOTSubtitlesFont: VOTSubtitlesFont$e
  };
  const recommended$d = "Odporúčaný";
  const translateVideo$d = "Preložiť video";
  const disableTranslate$d = "Vypnúť";
  const translationSettings$d = "Nastavenia prekladu";
  const subtitlesSettings$d = "Nastavenia titulkov";
  const resetSettings$d = "Obnoviť nastavenia";
  const videoBeingTranslated$d = "Video sa prekladá";
  const videoLanguage$d = "Jazyk videa";
  const translationLanguage$d = "Jazyk prekladu";
  const translationTake$d = "Preklad bude trvať";
  const translationTakeMoreThanHour$d = "Preklad bude trvať viac ako hodinu";
  const translationTakeAboutMinute$d = "Preklad bude trvať asi minútu";
  const translationTakeFewMinutes$d = "Preklad bude trvať niekoľko minút";
  const translationTakeApproximatelyMinutes$d = "Preklad bude trvať približne {0} minút";
  const translationTakeApproximatelyMinute$d = "Preklad bude trvať približne {0} minút";
  const requestTranslationFailed$d = "Nepodarilo sa požiadať o preklad videa";
  const audioNotReceived$d = "Audio odkaz nebol prijatý";
  const audioFormatNotSupported$d = "Formát zvuku nie je podporovaný";
  const VOTAutoTranslate$d = "Preložiť na open";
  const VOTDontTranslateYourLang$d = "Neprekladajte z môjho jazyka";
  const VOTVolume$d = "Hlasitosť videa:";
  const VOTVolumeTranslation$d = "Objem prekladu:";
  const VOTAutoSetVolume$d = "Znížte hlasitosť videa na";
  const VOTShowVideoSlider$d = "Posuvník hlasitosti videa";
  const VOTSyncVolume$d = "Preklad odkazu a objem videa";
  const VOTDisableFromYourLang$d = "Zakázali ste preklad videa vo vašom jazyku";
  const VOTVideoIsTooLong$d = "Video je príliš dlhé";
  const VOTNoVideoIDFound$d = "Nebolo nájdené žiadne video ID";
  const VOTSubtitles$d = "Nadpis";
  const VOTSubtitlesDisabled$d = "Vypnutý";
  const VOTSubtitlesMaxLength$d = "Maximálna dĺžka titulkov";
  const VOTHighlightWords$d = "Zvýraznite slová";
  const VOTTranslatedFrom$d = "preložené z";
  const VOTAutogenerated$d = "automatické generovanie";
  const VOTSettings$d = "Nastavenia VOT";
  const VOTMenuLanguage$d = "Hlavné menu";
  const VOTAuthors$d = "Autori";
  const VOTVersion$d = "Verzia";
  const VOTLoader$d = "Loader";
  const VOTBrowser$d = "Prehliadač";
  const VOTShowPiPButton$d = "Zobraziť tlačidlo PiP";
  const langs$d = { "auto": "Auto", "af": "Afrikánčina", "ak": "Akan", "sq": "Albánsky", "am": "Amharčina", "ar": "Arabčiny", "hy": "Arménsky", "as": "Ásámčina", "ay": "Aymara", "az": "Azerbajdžanský", "bn": "Bengálčina", "eu": "Baskičtina", "be": "Bieloruština", "bho": "Bhojpuri", "bs": "Bosniansky", "bg": "Bulharský", "my": "Barmská", "ca": "Čeština", "ceb": "Cebuano", "zh": "Čínsky", "zh-Hans": "Čínština (Zjednodušená)", "zh-Hant": "Čínština (Tradičná)", "co": "Korzický", "hr": "Chorvátsky", "cs": "Český", "da": "Dánsky", "dv": "Divehi", "nl": "Holandský", "en": "Anglický", "eo": "Esperanto", "et": "Estónsky", "ee": "Ewe", "fil": "Filipínsky", "fi": "Fínsko", "fr": "Francúzsky jazyk", "gl": "Galícijský", "lg": "Ganda", "ka": "Gruzínsky", "de": "Nemecký", "el": "Gréčtina", "gn": "Guarani", "gu": "Gujarati", "ht": "Haitskej Kreolskej", "ha": "Hausa", "haw": "Havajský", "iw": "Hebrejský", "hi": "Hindčina", "hmn": "Hmong", "hu": "Maďarský", "is": "Islandský", "ig": "Igbo", "id": "Indonézsky", "ga": "Írsky", "it": "Taliansky", "ja": "Japonský", "jv": "Javanese", "kn": "Kannada", "kk": "Kazaština", "km": "Khmer", "rw": "Kinyarwanda", "ko": "Kórejský", "kri": "Krio", "ku": "Kurdských", "ky": "Kirgizsko", "lo": "Lao", "la": "Latinský", "lv": "Lotyšský", "ln": "Lingala", "lt": "Litovský", "lb": "Luxemburčina", "mk": "Macedónsky", "mg": "Madagaskar", "ms": "Malajčina", "ml": "Malayalam", "mt": "Maltčina", "mi": "Māori", "mr": "Maráthˇ", "mn": "Mongolský", "ne": "Nepálsky", "nso": "Northern Sotho", "no": "Nórsko", "ny": "Nyanja", "or": "Odia", "om": "Oromo", "ps": "Paštčina", "fa": "Perzský", "pl": "Poľský", "pt": "Portugalský", "pa": "Punjabi", "qu": "Kečuánčina", "ro": "Rumunský", "ru": "Ruský", "sm": "Samoan", "sa": "Sanskrit", "gd": "Škótska Gaelčina", "sr": "Srbský", "sn": "Shona", "sd": "Sindhi", "si": "Sinhala", "sk": "Slovenský", "sl": "Slovinský", "so": "Somálsky", "st": "Southern Sotho", "es": "Španielsko", "su": "Prihlásiť sa", "sw": "Svahilčina", "sv": "Švédsky", "tg": "Tadžické", "ta": "Tamil", "tt": "Tatar", "te": "Telugu", "th": "Thajské", "ti": "Tigrinya", "ts": "Tsonga", "tr": "Turecký", "tk": "Turkménskych", "uk": "Ukrajinský", "ur": "Urdu", "ug": "Ujgur", "uz": "Uzbecké", "vi": "Vietnamskej", "cy": "Welsh", "fy": "Západofrízsky", "xh": "Xhosa", "yi": "Jidiš", "yo": "Yoruba", "zu": "Zulu" };
  const streamNoConnectionToServer$d = "Neexistuje žiadne pripojenie k serveru";
  const searchField$d = "Vyhľadávanie...";
  const VOTTranslateAPIErrors$d = "Preklad chýb z API";
  const VOTDetectService$d = "Služba detekcie jazykov";
  const VOTProxyWorkerHost$d = "Zadajte adresu proxy pracovníka";
  const VOTM3u8ProxyHost$d = "Zadajte adresu pracovníka proxy m3u8";
  const proxySettings$d = "Nastavenia Servera Proxy";
  const translationTakeApproximatelyMinute2$d = "Preklad bude trvať približne {0} minút";
  const VOTAudioBooster$d = "Rozšírené zvýšenie objemu prekladu";
  const VOTSubtitlesDesign$d = "Dizajn titulkov";
  const VOTSubtitlesFontSize$d = "Veľkosť písma titulkov";
  const VOTSubtitlesOpacity$d = "Transparentnosť pozadia titulkov";
  const VOTSubtitlesDownloadFormat$d = "Formát na stiahnutie titulkov";
  const VOTDownloadWithName$d = "Stiahnite si súbory s názvom videa";
  const VOTUpdateLocaleFiles$d = "Aktualizácia lokalizačných súborov";
  const VOTLocaleHash$d = "Locale hash";
  const VOTUpdatedAt$d = "Aktualizované o";
  const VOTNeedWebAudioAPI$d = "Ak to chcete povoliť, musíte mať rozhranie Web Audio API";
  const VOTMediaCSPEnabledOnSite$d = "Media CSP je povolené na tejto stránke";
  const VOTOnlyBypassMediaCSP$d = "Používajte ho iba na obídenie médií CSP";
  const VOTNewAudioPlayer$d = "Použite nový audio prehrávač";
  const VOTUseNewModel$d = "Pre niektoré videá použite experimentálnu variáciu hlasov Yandex";
  const TranslationDelayed$d = "Preklad je mierne oneskorený";
  const VOTTranslationCompletedNotify$d = "Preklad na {0} bol dokončený!";
  const VOTSendNotifyOnComplete$d = "Pošlite upozornenie, že video bolo preložené";
  const VOTBugReport$d = "Nahlásiť chybu";
  const VOTTranslateProxyDisabled$d = "Vypnutý";
  const VOTTranslateProxyEnabled$d = "Povoliť";
  const VOTTranslateProxyEverything$d = "Proxy všetko";
  const VOTTranslateProxyStatus$d = "Režim proxy";
  const VOTTranslatedBy$d = "Preklad: {0}";
  const VOTStreamNotAvailable$d = "Preklad streamu nie je k dispozícii";
  const VOTTranslationTextService$d = "Prekladateľská služba";
  const VOTNotAffectToVoice$d = "Nemá vplyv na preklad textu v aplikácii voice over";
  const DontTranslateSelectedLanguages$d = "Neprekladať z vybraných jazykov";
  const showVideoVolumeSlider$d = "Zobrazenie posúvača hlasitosti videa";
  const hotkeysSettings$d = "Nastavenia klávesových skratiek";
  const None$d = "Nikto";
  const VOTUseLivelyVoice$d = "Používajte živé hlasy. Reproduktory znejú ako rodení Rusi.";
  const miscSettings$d = "Rôzne nastavenia";
  const services$d = { "yandexbrowser": "Yandex Browser", "msedge": "Microsoft Edge", "rust-server": "Rust Server" };
  const aboutExtension$d = "O rozšírení";
  const appearance$d = "Vzhľad";
  const buttonPosition$d = "Pozícia tlačidla v prehrávači";
  const position$d = { "left": "Ľavý", "right": "Právo", "top": "Hornej", "default": "Predvolený" };
  const secs$d = "secs";
  const autoHideButtonDelay$d = "Oneskorenie pred skrytím tlačidla preložiť";
  const notFound$d = "nenájdený";
  const minButtonPositionContainer$d = "Pozícia tlačidla sa mení iba u hráčov väčších ako 600 pixelov.";
  const VOTTranslateProxyStatusDefault$d = "Úplné zakázanie proxy vo vašej krajine môže Rozšírenie prerušiť";
  const PressTheKeyCombination$d = "Stlačte kombináciu klávesov...";
  const VOTUseAudioDownload$d = "Použite stiahnutie zvuku";
  const VOTUseAudioDownloadWarning$d = "Zakázanie sťahovania zvuku môže mať vplyv na funkčnosť rozšírenia";
  const VOTAccountRequired$d = "Ak chcete používať túto funkciu, musíte sa prihlásiť";
  const VOTMyAccount$d = "Prihlásiť sa";
  const VOTLogin$d = "Prihlásenie";
  const VOTLogout$d = "Odhlásiť";
  const VOTRefresh$d = "Obnoviť";
  const VOTYandexToken$d = "Zadajte Token Yandex OAuth";
  const VOTYandexTokenInfo$d = "V tomto poli môžete manuálne nastaviť token účtu. Upozorňujeme, že pred odoslaním žiadosti o preklad nekontrolujeme jeho platnosť";
  const VOTLoginViaToken$d = "Prihlásenie cez token";
  const subtitlesSmartLayout$d = "Inteligentné rozloženie titulkov";
  const smartDucking$d = "Adaptívny objem";
  const VOTAutoSubtitles$d = "Titulky otvorené";
  const VOTSubtitlesFont$d = "Písmo titulkov";
  const __vite_glob_0_48 = {
    recommended: recommended$d,
    translateVideo: translateVideo$d,
    disableTranslate: disableTranslate$d,
    translationSettings: translationSettings$d,
    subtitlesSettings: subtitlesSettings$d,
    resetSettings: resetSettings$d,
    videoBeingTranslated: videoBeingTranslated$d,
    videoLanguage: videoLanguage$d,
    translationLanguage: translationLanguage$d,
    translationTake: translationTake$d,
    translationTakeMoreThanHour: translationTakeMoreThanHour$d,
    translationTakeAboutMinute: translationTakeAboutMinute$d,
    translationTakeFewMinutes: translationTakeFewMinutes$d,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$d,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$d,
    requestTranslationFailed: requestTranslationFailed$d,
    audioNotReceived: audioNotReceived$d,
    audioFormatNotSupported: audioFormatNotSupported$d,
    VOTAutoTranslate: VOTAutoTranslate$d,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$d,
    VOTVolume: VOTVolume$d,
    VOTVolumeTranslation: VOTVolumeTranslation$d,
    VOTAutoSetVolume: VOTAutoSetVolume$d,
    VOTShowVideoSlider: VOTShowVideoSlider$d,
    VOTSyncVolume: VOTSyncVolume$d,
    VOTDisableFromYourLang: VOTDisableFromYourLang$d,
    VOTVideoIsTooLong: VOTVideoIsTooLong$d,
    VOTNoVideoIDFound: VOTNoVideoIDFound$d,
    VOTSubtitles: VOTSubtitles$d,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$d,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$d,
    VOTHighlightWords: VOTHighlightWords$d,
    VOTTranslatedFrom: VOTTranslatedFrom$d,
    VOTAutogenerated: VOTAutogenerated$d,
    VOTSettings: VOTSettings$d,
    VOTMenuLanguage: VOTMenuLanguage$d,
    VOTAuthors: VOTAuthors$d,
    VOTVersion: VOTVersion$d,
    VOTLoader: VOTLoader$d,
    VOTBrowser: VOTBrowser$d,
    VOTShowPiPButton: VOTShowPiPButton$d,
    langs: langs$d,
    streamNoConnectionToServer: streamNoConnectionToServer$d,
    searchField: searchField$d,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$d,
    VOTDetectService: VOTDetectService$d,
    VOTProxyWorkerHost: VOTProxyWorkerHost$d,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$d,
    proxySettings: proxySettings$d,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$d,
    VOTAudioBooster: VOTAudioBooster$d,
    VOTSubtitlesDesign: VOTSubtitlesDesign$d,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$d,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$d,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$d,
    VOTDownloadWithName: VOTDownloadWithName$d,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$d,
    VOTLocaleHash: VOTLocaleHash$d,
    VOTUpdatedAt: VOTUpdatedAt$d,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$d,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$d,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$d,
    VOTNewAudioPlayer: VOTNewAudioPlayer$d,
    VOTUseNewModel: VOTUseNewModel$d,
    TranslationDelayed: TranslationDelayed$d,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$d,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$d,
    VOTBugReport: VOTBugReport$d,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$d,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$d,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$d,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$d,
    VOTTranslatedBy: VOTTranslatedBy$d,
    VOTStreamNotAvailable: VOTStreamNotAvailable$d,
    VOTTranslationTextService: VOTTranslationTextService$d,
    VOTNotAffectToVoice: VOTNotAffectToVoice$d,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$d,
    showVideoVolumeSlider: showVideoVolumeSlider$d,
    hotkeysSettings: hotkeysSettings$d,
    None: None$d,
    VOTUseLivelyVoice: VOTUseLivelyVoice$d,
    miscSettings: miscSettings$d,
    services: services$d,
    aboutExtension: aboutExtension$d,
    appearance: appearance$d,
    buttonPosition: buttonPosition$d,
    position: position$d,
    secs: secs$d,
    autoHideButtonDelay: autoHideButtonDelay$d,
    notFound: notFound$d,
    minButtonPositionContainer: minButtonPositionContainer$d,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$d,
    PressTheKeyCombination: PressTheKeyCombination$d,
    VOTUseAudioDownload: VOTUseAudioDownload$d,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$d,
    VOTAccountRequired: VOTAccountRequired$d,
    VOTMyAccount: VOTMyAccount$d,
    VOTLogin: VOTLogin$d,
    VOTLogout: VOTLogout$d,
    VOTRefresh: VOTRefresh$d,
    VOTYandexToken: VOTYandexToken$d,
    VOTYandexTokenInfo: VOTYandexTokenInfo$d,
    VOTLoginViaToken: VOTLoginViaToken$d,
    subtitlesSmartLayout: subtitlesSmartLayout$d,
    smartDucking: smartDucking$d,
    VOTAutoSubtitles: VOTAutoSubtitles$d,
    VOTSubtitlesFont: VOTSubtitlesFont$d
  };
  const recommended$c = "priporočeno";
  const translateVideo$c = "Prevedi video";
  const disableTranslate$c = "Izklopite";
  const translationSettings$c = "Nastavitve prevajanja";
  const subtitlesSettings$c = "Nastavitve podnapisov";
  const resetSettings$c = "Ponastavi nastavitve";
  const videoBeingTranslated$c = "Video se prevaja";
  const videoLanguage$c = "Video jezik";
  const translationLanguage$c = "Jezik prevajanja";
  const translationTake$c = "Prevod bo";
  const translationTakeMoreThanHour$c = "Prevod bo trajal več kot eno uro";
  const translationTakeAboutMinute$c = "Prevod bo trajal približno minuto";
  const translationTakeFewMinutes$c = "Prevod bo trajal nekaj minut";
  const translationTakeApproximatelyMinutes$c = "Prevod bo trajal približno {0} minut";
  const translationTakeApproximatelyMinute$c = "Prevod bo trajal približno {0} minut";
  const requestTranslationFailed$c = "Ni bilo mogoče zahtevati prevoda videa";
  const audioNotReceived$c = "Zvočna povezava ni prejeta";
  const audioFormatNotSupported$c = "Zvočni format ni podprt";
  const VOTAutoTranslate$c = "Prevedi na odprto";
  const VOTDontTranslateYourLang$c = "Ne prevajaj iz mojega jezika";
  const VOTVolume$c = "Glasnost videa:";
  const VOTVolumeTranslation$c = "Obseg prevoda:";
  const VOTAutoSetVolume$c = "Zmanjšajte glasnost videa na";
  const VOTShowVideoSlider$c = "Drsnik za glasnost videa";
  const VOTSyncVolume$c = "Prevajanje povezav in obseg videa";
  const VOTDisableFromYourLang$c = "Onemogočili ste prevod videoposnetka v svoj jezik";
  const VOTVideoIsTooLong$c = "Video je predolg";
  const VOTNoVideoIDFound$c = "Video ID ni bil najden";
  const VOTSubtitles$c = "Podnapisi";
  const VOTSubtitlesDisabled$c = "Onemogočeno";
  const VOTSubtitlesMaxLength$c = "Podnapisi največja dolžina";
  const VOTHighlightWords$c = "Označite besede";
  const VOTTranslatedFrom$c = "prevedeno iz";
  const VOTAutogenerated$c = "avtogenerirano";
  const VOTSettings$c = "Nastavitve VOT";
  const VOTMenuLanguage$c = "Jezik menija";
  const VOTAuthors$c = "Avtorji";
  const VOTVersion$c = "Različica";
  const VOTLoader$c = "Nakladalnik";
  const VOTBrowser$c = "Brskalnik";
  const VOTShowPiPButton$c = "Prikaži gumb PiP";
  const langs$c = { "auto": "Samodejno", "af": "Afrikaans", "ak": "Akan", "sq": "Albanski", "am": "Amharščina", "ar": "Arabščina", "hy": "Armenski", "as": "Asamski", "ay": "Ajmara", "az": "Azerbajdžanski", "bn": "Bangla", "eu": "Baskovščina", "be": "Beloruski", "bho": "Bhojpuri", "bs": "Bosanski", "bg": "Bolgarščina", "my": "Burmanski", "ca": "Katalonščina", "ceb": "Cebuano", "zh": "Kitajski", "zh-Hans": "Kitajščina (Poenostavljeno)", "zh-Hant": "Kitajski (Tradicionalni)", "co": "Korziški", "hr": "Hrvaški", "cs": "Češka", "da": "Danščina", "dv": "Divehi", "nl": "Nizozemščina", "en": "Angleščina", "eo": "Esperanto", "et": "Estonščina", "ee": "Ovce", "fil": "Filipino", "fi": "Finščina", "fr": "Francoski", "gl": "Galicijščina", "lg": "Ganda", "ka": "Gruzijski", "de": "Nemščina", "el": "Grški", "gn": "Guarani", "gu": "Gujarati", "ht": "Haitijski Kreolski Jezik", "ha": "Hausa", "haw": "Havajski", "iw": "Hebrejščina", "hi": "Hindujščina", "hmn": "Hmong", "hu": "Madžarščina", "is": "Islandski", "ig": "Igbo", "id": "Indonezijski", "ga": "Irski", "it": "Italijanski", "ja": "Japonski", "jv": "Javanski", "kn": "Kannada", "kk": "Kazahstanski", "km": "Kmerski", "rw": "Kinjarvanda", "ko": "Korejščina", "kri": "Krio", "ku": "Kurdski", "ky": "Kirgiški", "lo": "Lao", "la": "Latinsko", "lv": "Latvijski", "ln": "Lingala", "lt": "Litovski", "lb": "Luksemburški", "mk": "Makedonski", "mg": "Malgaški", "ms": "Malajski", "ml": "Malajalamščina", "mt": "Malteščina", "mi": "M", "mr": "Marathi", "mn": "Mongolski", "ne": "Nepalski", "nso": "Severni Sotho", "no": "Norveški", "ny": "Njanja", "or": "Odia", "om": "Oromo", "ps": "Paštunščina", "fa": "Perzijski", "pl": "Poljski", "pt": "Portugalščina", "pa": "Punjabi", "qu": "Kečuanski", "ro": "Romunščina", "ru": "Ruski", "sm": "Samoanski", "sa": "Sanskrit", "gd": "Škotski Gelski", "sr": "Srbski", "sn": "Shona", "sd": "Sindhi", "si": "Sinhalščina", "sk": "Slovaščina", "sl": "Slovenščina", "so": "Somalijski", "st": "Južni Sotho", "es": "Španščina", "su": "Sundanese", "sw": "Svahili", "sv": "Švedščina", "tg": "Tadžikistan", "ta": "Tamilščina", "tt": "Tatarski", "te": "Telugu", "th": "Tajska", "ti": "Tigrinja", "ts": "Tsonga", "tr": "Turški", "tk": "Turkmenski", "uk": "Ukrajinski", "ur": "Urdu", "ug": "Ujgurski", "uz": "Uzbekistanski", "vi": "Vietnamski", "cy": "Valižanski", "fy": "Zahodni Frizijski", "xh": "Ksosa", "yi": "Jidiš", "yo": "Joruba", "zu": "Zulu" };
  const streamNoConnectionToServer$c = "Ni povezave s strežnikom";
  const searchField$c = "Išči...";
  const VOTTranslateAPIErrors$c = "Prevedi napake iz API-ja";
  const VOTDetectService$c = "Storitev zaznavanja jezika";
  const VOTProxyWorkerHost$c = "Vnesite naslov pooblaščenca";
  const VOTM3u8ProxyHost$c = "Vnesite naslov pooblaščenca m3u8";
  const proxySettings$c = "Nastavitve Posrednika";
  const translationTakeApproximatelyMinute2$c = "Prevod bo trajal približno {0} minut";
  const VOTAudioBooster$c = "Povečanje obsega razširjenega prevajanja";
  const VOTSubtitlesDesign$c = "Oblikovanje podnapisov";
  const VOTSubtitlesFontSize$c = "Velikost pisave podnapisov";
  const VOTSubtitlesOpacity$c = "Preglednost ozadja podnapisov";
  const VOTSubtitlesDownloadFormat$c = "Oblika za prenos podnapisov";
  const VOTDownloadWithName$c = "Prenesite datoteke z imenom videoposnetka";
  const VOTUpdateLocaleFiles$c = "Posodobite lokalizacijske datoteke";
  const VOTLocaleHash$c = "Lokalni hash";
  const VOTUpdatedAt$c = "Posodobljeno ob";
  const VOTNeedWebAudioAPI$c = "Če želite to omogočiti, morate imeti spletni avdio API";
  const VOTMediaCSPEnabledOnSite$c = "Media CSP je omogočen na tej spletni strani";
  const VOTOnlyBypassMediaCSP$c = "Uporabite ga samo za obhod medijev CSP";
  const VOTNewAudioPlayer$c = "Uporabite nov predvajalnik zvoka";
  const VOTUseNewModel$c = "Za nekatere videoposnetke uporabite eksperimentalno različico glasov Jandeksa";
  const TranslationDelayed$c = "Prevod je nekoliko zakasnjen";
  const VOTTranslationCompletedNotify$c = "Prevod na {0} je končan!";
  const VOTSendNotifyOnComplete$c = "Pošljite obvestilo, da je bil video preveden";
  const VOTBugReport$c = "Prijavite napako";
  const VOTTranslateProxyDisabled$c = "Onemogočeno";
  const VOTTranslateProxyEnabled$c = "Omogočeno";
  const VOTTranslateProxyEverything$c = "Pooblaščenec vse";
  const VOTTranslateProxyStatus$c = "Način posredovanja";
  const VOTTranslatedBy$c = "Prevedel {0}";
  const VOTStreamNotAvailable$c = "Prevedi tok ni na voljo";
  const VOTTranslationTextService$c = "Storitev prevajanja besedila";
  const VOTNotAffectToVoice$c = "Ne vpliva na prevod besedila v voice over";
  const DontTranslateSelectedLanguages$c = "Ne prevajajte iz izbranih jezikov";
  const showVideoVolumeSlider$c = "Prikažite drsnik za glasnost videa";
  const hotkeysSettings$c = "Nastavitve bližnjičnih tipk";
  const None$c = "Brez";
  const VOTUseLivelyVoice$c = "Uporabite živahne glasove. Zvočniki zvenijo kot domači Rusi.";
  const miscSettings$c = "Različne nastavitve";
  const services$c = { "yandexbrowser": "<url>", "msedge": "Microsoft Edge", "rust-server": "Strežnik Rust" };
  const aboutExtension$c = "O razširitvi";
  const appearance$c = "Videz";
  const buttonPosition$c = "Položaj gumba v predvajalniku";
  const position$c = { "left": "Levo", "right": "Desno", "top": "Vrh", "default": "Privzeto" };
  const secs$c = "sec";
  const autoHideButtonDelay$c = "Zakasnitev, preden skrijete gumb za prevajanje";
  const notFound$c = "ni bilo mogoče najti";
  const minButtonPositionContainer$c = "Položaj gumba se spremeni samo pri predvajalnikih, večjih od 600 slikovnih pik.";
  const VOTTranslateProxyStatusDefault$c = "Popolnoma onemogočanje posredništva v vaši državi lahko prekine razširitev";
  const PressTheKeyCombination$c = "Pritisnite kombinacijo tipk...";
  const VOTUseAudioDownload$c = "Uporabite prenos zvoka";
  const VOTUseAudioDownloadWarning$c = "Onemogočanje prenosov zvoka lahko vpliva na funkcionalnost razširitve";
  const VOTAccountRequired$c = "Za uporabo te funkcije se morate prijaviti";
  const VOTMyAccount$c = "Moj Račun";
  const VOTLogin$c = "Prijava";
  const VOTLogout$c = "Odjava";
  const VOTRefresh$c = "Osveži";
  const VOTYandexToken$c = "Vnesite žeton OAuth";
  const VOTYandexTokenInfo$c = "V tem polju lahko ročno nastavite žeton računa. Upoštevajte, da pred pošiljanjem zahteve za prevajanje ne preverimo njegove veljavnosti";
  const VOTLoginViaToken$c = "Prijava prek žetona";
  const subtitlesSmartLayout$c = "Pametna postavitev podnapisov";
  const smartDucking$c = "Prilagodljiva prostornina";
  const VOTAutoSubtitles$c = "Podnapisi na odprtem";
  const VOTSubtitlesFont$c = "Pisava podnapisov";
  const __vite_glob_0_49 = {
    recommended: recommended$c,
    translateVideo: translateVideo$c,
    disableTranslate: disableTranslate$c,
    translationSettings: translationSettings$c,
    subtitlesSettings: subtitlesSettings$c,
    resetSettings: resetSettings$c,
    videoBeingTranslated: videoBeingTranslated$c,
    videoLanguage: videoLanguage$c,
    translationLanguage: translationLanguage$c,
    translationTake: translationTake$c,
    translationTakeMoreThanHour: translationTakeMoreThanHour$c,
    translationTakeAboutMinute: translationTakeAboutMinute$c,
    translationTakeFewMinutes: translationTakeFewMinutes$c,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$c,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$c,
    requestTranslationFailed: requestTranslationFailed$c,
    audioNotReceived: audioNotReceived$c,
    audioFormatNotSupported: audioFormatNotSupported$c,
    VOTAutoTranslate: VOTAutoTranslate$c,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$c,
    VOTVolume: VOTVolume$c,
    VOTVolumeTranslation: VOTVolumeTranslation$c,
    VOTAutoSetVolume: VOTAutoSetVolume$c,
    VOTShowVideoSlider: VOTShowVideoSlider$c,
    VOTSyncVolume: VOTSyncVolume$c,
    VOTDisableFromYourLang: VOTDisableFromYourLang$c,
    VOTVideoIsTooLong: VOTVideoIsTooLong$c,
    VOTNoVideoIDFound: VOTNoVideoIDFound$c,
    VOTSubtitles: VOTSubtitles$c,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$c,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$c,
    VOTHighlightWords: VOTHighlightWords$c,
    VOTTranslatedFrom: VOTTranslatedFrom$c,
    VOTAutogenerated: VOTAutogenerated$c,
    VOTSettings: VOTSettings$c,
    VOTMenuLanguage: VOTMenuLanguage$c,
    VOTAuthors: VOTAuthors$c,
    VOTVersion: VOTVersion$c,
    VOTLoader: VOTLoader$c,
    VOTBrowser: VOTBrowser$c,
    VOTShowPiPButton: VOTShowPiPButton$c,
    langs: langs$c,
    streamNoConnectionToServer: streamNoConnectionToServer$c,
    searchField: searchField$c,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$c,
    VOTDetectService: VOTDetectService$c,
    VOTProxyWorkerHost: VOTProxyWorkerHost$c,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$c,
    proxySettings: proxySettings$c,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$c,
    VOTAudioBooster: VOTAudioBooster$c,
    VOTSubtitlesDesign: VOTSubtitlesDesign$c,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$c,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$c,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$c,
    VOTDownloadWithName: VOTDownloadWithName$c,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$c,
    VOTLocaleHash: VOTLocaleHash$c,
    VOTUpdatedAt: VOTUpdatedAt$c,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$c,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$c,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$c,
    VOTNewAudioPlayer: VOTNewAudioPlayer$c,
    VOTUseNewModel: VOTUseNewModel$c,
    TranslationDelayed: TranslationDelayed$c,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$c,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$c,
    VOTBugReport: VOTBugReport$c,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$c,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$c,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$c,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$c,
    VOTTranslatedBy: VOTTranslatedBy$c,
    VOTStreamNotAvailable: VOTStreamNotAvailable$c,
    VOTTranslationTextService: VOTTranslationTextService$c,
    VOTNotAffectToVoice: VOTNotAffectToVoice$c,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$c,
    showVideoVolumeSlider: showVideoVolumeSlider$c,
    hotkeysSettings: hotkeysSettings$c,
    None: None$c,
    VOTUseLivelyVoice: VOTUseLivelyVoice$c,
    miscSettings: miscSettings$c,
    services: services$c,
    aboutExtension: aboutExtension$c,
    appearance: appearance$c,
    buttonPosition: buttonPosition$c,
    position: position$c,
    secs: secs$c,
    autoHideButtonDelay: autoHideButtonDelay$c,
    notFound: notFound$c,
    minButtonPositionContainer: minButtonPositionContainer$c,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$c,
    PressTheKeyCombination: PressTheKeyCombination$c,
    VOTUseAudioDownload: VOTUseAudioDownload$c,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$c,
    VOTAccountRequired: VOTAccountRequired$c,
    VOTMyAccount: VOTMyAccount$c,
    VOTLogin: VOTLogin$c,
    VOTLogout: VOTLogout$c,
    VOTRefresh: VOTRefresh$c,
    VOTYandexToken: VOTYandexToken$c,
    VOTYandexTokenInfo: VOTYandexTokenInfo$c,
    VOTLoginViaToken: VOTLoginViaToken$c,
    subtitlesSmartLayout: subtitlesSmartLayout$c,
    smartDucking: smartDucking$c,
    VOTAutoSubtitles: VOTAutoSubtitles$c,
    VOTSubtitlesFont: VOTSubtitlesFont$c
  };
  const recommended$b = "rekomandohet";
  const translateVideo$b = "Përktheni videon";
  const disableTranslate$b = "Fikeni";
  const translationSettings$b = "Cilësimet e përkthimit";
  const subtitlesSettings$b = "Cilësimet e titrave";
  const resetSettings$b = "Rivendosni cilësimet";
  const videoBeingTranslated$b = "Videoja po përkthehet";
  const videoLanguage$b = "Gjuha e videos";
  const translationLanguage$b = "Gjuha e përkthimit";
  const translationTake$b = "Përkthimi do të marrë";
  const translationTakeMoreThanHour$b = "Përkthimi do të zgjasë më shumë se një orë";
  const translationTakeAboutMinute$b = "Përkthimi do të zgjasë rreth një minutë";
  const translationTakeFewMinutes$b = "Përkthimi do të zgjasë disa minuta";
  const translationTakeApproximatelyMinutes$b = "Përkthimi do të zgjasë afërsisht {0} minuta";
  const translationTakeApproximatelyMinute$b = "Përkthimi do të zgjasë afërsisht {0} minuta";
  const requestTranslationFailed$b = "Nuk arriti të kërkojë përkthim video";
  const audioNotReceived$b = "Lidhja Audio nuk është marrë";
  const audioFormatNotSupported$b = "Formati audio nuk mbështetet";
  const VOTAutoTranslate$b = "Përktheni në open";
  const VOTDontTranslateYourLang$b = "Mos përktheni nga gjuha ime";
  const VOTVolume$b = "Vëllimi i videos:";
  const VOTVolumeTranslation$b = "Vëllimi i përkthimit:";
  const VOTAutoSetVolume$b = "Zvogëloni vëllimin e videos në";
  const VOTShowVideoSlider$b = "Rrëshqitësi i vëllimit të videos";
  const VOTSyncVolume$b = "Përkthimi i lidhjes dhe vëllimi i videos";
  const VOTDisableFromYourLang$b = "Ju keni çaktivizuar përkthimin e videos në gjuhën tuaj";
  const VOTVideoIsTooLong$b = "Video është shumë e gjatë";
  const VOTNoVideoIDFound$b = "Nuk u gjet ASNJË ID video";
  const VOTSubtitles$b = "Titra";
  const VOTSubtitlesDisabled$b = "Me aftësi të kufizuara";
  const VOTSubtitlesMaxLength$b = "Titra gjatësia maksimale";
  const VOTHighlightWords$b = "Theksoni fjalët";
  const VOTTranslatedFrom$b = "përkthyer nga";
  const VOTAutogenerated$b = "autogjeneruar";
  const VOTSettings$b = "Cilësimet E VOT";
  const VOTMenuLanguage$b = "Gjuha e menusë";
  const VOTAuthors$b = "Autorët";
  const VOTVersion$b = "Versioni";
  const VOTLoader$b = "Ngarkues";
  const VOTBrowser$b = "Shfletuesi";
  const VOTShowPiPButton$b = "Shfaq butonin PiP";
  const langs$b = { "auto": "Auto", "af": "Afrikaans", "ak": "Akan", "sq": "Shqiptare", "am": "Amharik", "ar": "Arabisht", "hy": "Armenisht", "as": "Assamese", "ay": "Aymara", "az": "Azerbajxhanas", "bn": "Bangla", "eu": "Baske", "be": "Bjellorusisht", "bho": "Bhojpuri", "bs": "Boshnjake", "bg": "Bullgare", "my": "Burmesisht", "ca": "Katalanisht", "ceb": "Cebuano", "zh": "Kineze", "zh-Hans": "Kineze (E Thjeshtuar)", "zh-Hant": "Kineze (Tradicionale)", "co": "Korsikan", "hr": "Kroate", "cs": "Çeki", "da": "Daneze", "dv": "Divehi", "nl": "Holandisht", "en": "Anglisht", "eo": "Esperanto", "et": "Estonisht", "ee": "Ewe", "fil": "Filipinas", "fi": "Finlandeze", "fr": "Frëngjisht", "gl": "Galician", "lg": "Ganda", "ka": "Gjeorgjian", "de": "Gjermanisht", "el": "Greke", "gn": "Guarani", "gu": "Gujarati", "ht": "Kreole Haitiane", "ha": "Hausa", "haw": "Havai", "iw": "Hebraisht", "hi": "Hindi", "hmn": "Hmong", "hu": "Hungareze", "is": "Islandeze", "ig": "Igbo", "id": "Indoneziane", "ga": "Irlandez", "it": "Italiane", "ja": "Japoneze", "jv": "Javanez", "kn": "Kannada", "kk": "Kazak", "km": "Khmer", "rw": "Kinyarwanda", "ko": "Koreane", "kri": "Krio", "ku": "Kurde", "ky": "Kirgiz", "lo": "Lao", "la": "Latinisht", "lv": "Letonisht", "ln": "Lingala", "lt": "Lituanisht", "lb": "Luksemburgisht", "mk": "Maqedonas", "mg": "Malgasi", "ms": "Malaj", "ml": "Malajalam", "mt": "Maltese", "mi": "Māori", "mr": "Marathi", "mn": "Mongol", "ne": "Nepali", "nso": "Sotho Veriore", "no": "Norvegjeze", "ny": "Nyanja", "or": "Odia", "om": "Oromo", "ps": "Pashto", "fa": "Persisht", "pl": "Polonisht", "pt": "Portugeze", "pa": "Punjabi", "qu": "Keçua", "ro": "Rumune", "ru": "Rusisht", "sm": "Samoan", "sa": "Sanskrit", "gd": "Gjuha Gaelike Skoceze", "sr": "Serbe", "sn": "Shona", "sd": "Sindhi", "si": "Sinhala", "sk": "Sllovak", "sl": "Sllovenisht", "so": "Somali", "st": "Sotho Jugor", "es": "Spanjisht", "su": "Sundanese", "sw": "Suahili", "sv": "Suedeze", "tg": "Tajik", "ta": "Tamil", "tt": "Tatar", "te": "Telugu", "th": "Thai", "ti": "Tigrinya", "ts": "Tsonga", "tr": "Turqisht", "tk": "Turkmen", "uk": "Ukrainas", "ur": "Urdu", "ug": "Ujgur", "uz": "Uzbek", "vi": "Vietnameze", "cy": "Uellsi", "fy": "Frisian Perëndimor", "xh": "Xhosa", "yi": "Jidiç", "yo": "Joruba", "zu": "Zulu" };
  const streamNoConnectionToServer$b = "Nuk ka lidhje me serverin";
  const searchField$b = "Kërko...";
  const VOTTranslateAPIErrors$b = "Përktheni gabimet NGA API";
  const VOTDetectService$b = "Shërbimi i zbulimit të gjuhës";
  const VOTProxyWorkerHost$b = "Vendosni adresën e punonjësit të përfaqësuesit";
  const VOTM3u8ProxyHost$b = "Vendosni adresën e punonjësit të përfaqësuesit m3u8";
  const proxySettings$b = "Cilësimet E Përfaqësuesit";
  const translationTakeApproximatelyMinute2$b = "Përkthimi do të zgjasë afërsisht {0} minuta";
  const VOTAudioBooster$b = "Rritja e vëllimit të përkthimit të zgjeruar";
  const VOTSubtitlesDesign$b = "Dizajni i titrave";
  const VOTSubtitlesFontSize$b = "Madhësia e shkronjave të titrave";
  const VOTSubtitlesOpacity$b = "Transparenca e sfondit të titrave";
  const VOTSubtitlesDownloadFormat$b = "Formati për shkarkimin e titrave";
  const VOTDownloadWithName$b = "Shkarkoni skedarë me emrin e videos";
  const VOTUpdateLocaleFiles$b = "Përditësoni skedarët e lokalizimit";
  const VOTLocaleHash$b = "Hash Locale";
  const VOTUpdatedAt$b = "Përditësuar në";
  const VOTNeedWebAudioAPI$b = "Për ta mundësuar këtë, duhet të keni NJË API Audio Në Internet";
  const VOTMediaCSPEnabledOnSite$b = "Media CSP është aktivizuar në këtë faqe";
  const VOTOnlyBypassMediaCSP$b = "Përdoreni atë vetëm për anashkalimin E CSP-Së Mediatike";
  const VOTNewAudioPlayer$b = "Përdorni luajtësin e ri audio";
  const VOTUseNewModel$b = "Përdorni një variant eksperimental Të Zërave Yandex për disa video";
  const TranslationDelayed$b = "Përkthimi është vonuar pak";
  const VOTTranslationCompletedNotify$b = "Përkthimi në {0} ka përfunduar!";
  const VOTSendNotifyOnComplete$b = "Dërgoni një njoftim se videoja është përkthyer";
  const VOTBugReport$b = "Raportoni një gabim";
  const VOTTranslateProxyDisabled$b = "Me aftësi të kufizuara";
  const VOTTranslateProxyEnabled$b = "Aktivizuar";
  const VOTTranslateProxyEverything$b = "Proxy gjithçka";
  const VOTTranslateProxyStatus$b = "Modaliteti i Proxying";
  const VOTTranslatedBy$b = "Përkthyer nga {0}";
  const VOTStreamNotAvailable$b = "Translate stream nuk është në dispozicion";
  const VOTTranslationTextService$b = "Shërbimi i përkthimit të tekstit";
  const VOTNotAffectToVoice$b = "Nuk ndikon në përkthimin e tekstit me zë mbi";
  const DontTranslateSelectedLanguages$b = "Mos përktheni nga gjuhët e zgjedhura";
  const showVideoVolumeSlider$b = "Shfaqni rrëshqitësin e vëllimit të videos";
  const hotkeysSettings$b = "Cilësimet e Hotkeys";
  const None$b = "Asnjë";
  const VOTUseLivelyVoice$b = "Përdorni zëra të gjallë. Folësit tingëllojnë si Rusë vendas.";
  const miscSettings$b = "Cilësimet e Misc";
  const services$b = { "yandexbrowser": "Shfletuesi Yandex", "msedge": "Microsoft Edge", "rust-server": "Server Rust" };
  const aboutExtension$b = "Rreth zgjerimit";
  const appearance$b = "Pamje";
  const buttonPosition$b = "Pozicioni i butonit në luajtës";
  const position$b = { "left": "Majtas", "right": "E drejtë", "top": "Top", "default": "Parazgjedhur" };
  const secs$b = "sek";
  const autoHideButtonDelay$b = "Vononi para se të fshehni butonin e përkthimit";
  const notFound$b = "nuk u gjet";
  const minButtonPositionContainer$b = "Pozicioni i butonit ndryshon vetëm në lojtarët më të mëdhenj se 600 piksele.";
  const VOTTranslateProxyStatusDefault$b = "Çaktivizimi i plotë i proxying në vendin tuaj mund të prishë zgjatjen";
  const PressTheKeyCombination$b = "Shtypni kombinimin e tastit...";
  const VOTUseAudioDownload$b = "Përdorni shkarkimin audio";
  const VOTUseAudioDownloadWarning$b = "Çaktivizimi i shkarkimeve audio mund të ndikojë në funksionalitetin e shtesës";
  const VOTAccountRequired$b = "Ju duhet të identifikoheni për të përdorur këtë veçori";
  const VOTMyAccount$b = "Llogaria ime";
  const VOTLogin$b = "Hyrje";
  const VOTLogout$b = "Dalja";
  const VOTRefresh$b = "Rifresko";
  const VOTYandexToken$b = "Futni Shenjën Yandex OAuth";
  const VOTYandexTokenInfo$b = "Ju mund të vendosni manualisht shenjën e llogarisë në këtë fushë. Ju lutemi vini re se ne nuk e kontrollojmë vlefshmërinë e tij përpara se të dërgojmë një kërkesë për përkthim";
  const VOTLoginViaToken$b = "Identifikohuni përmes token";
  const subtitlesSmartLayout$b = "Paraqitja e zgjuar e titrave";
  const smartDucking$b = "Vëllimi adaptiv";
  const VOTAutoSubtitles$b = "Titra në open";
  const VOTSubtitlesFont$b = "Shkronja e titrave";
  const __vite_glob_0_50 = {
    recommended: recommended$b,
    translateVideo: translateVideo$b,
    disableTranslate: disableTranslate$b,
    translationSettings: translationSettings$b,
    subtitlesSettings: subtitlesSettings$b,
    resetSettings: resetSettings$b,
    videoBeingTranslated: videoBeingTranslated$b,
    videoLanguage: videoLanguage$b,
    translationLanguage: translationLanguage$b,
    translationTake: translationTake$b,
    translationTakeMoreThanHour: translationTakeMoreThanHour$b,
    translationTakeAboutMinute: translationTakeAboutMinute$b,
    translationTakeFewMinutes: translationTakeFewMinutes$b,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$b,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$b,
    requestTranslationFailed: requestTranslationFailed$b,
    audioNotReceived: audioNotReceived$b,
    audioFormatNotSupported: audioFormatNotSupported$b,
    VOTAutoTranslate: VOTAutoTranslate$b,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$b,
    VOTVolume: VOTVolume$b,
    VOTVolumeTranslation: VOTVolumeTranslation$b,
    VOTAutoSetVolume: VOTAutoSetVolume$b,
    VOTShowVideoSlider: VOTShowVideoSlider$b,
    VOTSyncVolume: VOTSyncVolume$b,
    VOTDisableFromYourLang: VOTDisableFromYourLang$b,
    VOTVideoIsTooLong: VOTVideoIsTooLong$b,
    VOTNoVideoIDFound: VOTNoVideoIDFound$b,
    VOTSubtitles: VOTSubtitles$b,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$b,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$b,
    VOTHighlightWords: VOTHighlightWords$b,
    VOTTranslatedFrom: VOTTranslatedFrom$b,
    VOTAutogenerated: VOTAutogenerated$b,
    VOTSettings: VOTSettings$b,
    VOTMenuLanguage: VOTMenuLanguage$b,
    VOTAuthors: VOTAuthors$b,
    VOTVersion: VOTVersion$b,
    VOTLoader: VOTLoader$b,
    VOTBrowser: VOTBrowser$b,
    VOTShowPiPButton: VOTShowPiPButton$b,
    langs: langs$b,
    streamNoConnectionToServer: streamNoConnectionToServer$b,
    searchField: searchField$b,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$b,
    VOTDetectService: VOTDetectService$b,
    VOTProxyWorkerHost: VOTProxyWorkerHost$b,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$b,
    proxySettings: proxySettings$b,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$b,
    VOTAudioBooster: VOTAudioBooster$b,
    VOTSubtitlesDesign: VOTSubtitlesDesign$b,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$b,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$b,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$b,
    VOTDownloadWithName: VOTDownloadWithName$b,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$b,
    VOTLocaleHash: VOTLocaleHash$b,
    VOTUpdatedAt: VOTUpdatedAt$b,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$b,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$b,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$b,
    VOTNewAudioPlayer: VOTNewAudioPlayer$b,
    VOTUseNewModel: VOTUseNewModel$b,
    TranslationDelayed: TranslationDelayed$b,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$b,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$b,
    VOTBugReport: VOTBugReport$b,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$b,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$b,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$b,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$b,
    VOTTranslatedBy: VOTTranslatedBy$b,
    VOTStreamNotAvailable: VOTStreamNotAvailable$b,
    VOTTranslationTextService: VOTTranslationTextService$b,
    VOTNotAffectToVoice: VOTNotAffectToVoice$b,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$b,
    showVideoVolumeSlider: showVideoVolumeSlider$b,
    hotkeysSettings: hotkeysSettings$b,
    None: None$b,
    VOTUseLivelyVoice: VOTUseLivelyVoice$b,
    miscSettings: miscSettings$b,
    services: services$b,
    aboutExtension: aboutExtension$b,
    appearance: appearance$b,
    buttonPosition: buttonPosition$b,
    position: position$b,
    secs: secs$b,
    autoHideButtonDelay: autoHideButtonDelay$b,
    notFound: notFound$b,
    minButtonPositionContainer: minButtonPositionContainer$b,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$b,
    PressTheKeyCombination: PressTheKeyCombination$b,
    VOTUseAudioDownload: VOTUseAudioDownload$b,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$b,
    VOTAccountRequired: VOTAccountRequired$b,
    VOTMyAccount: VOTMyAccount$b,
    VOTLogin: VOTLogin$b,
    VOTLogout: VOTLogout$b,
    VOTRefresh: VOTRefresh$b,
    VOTYandexToken: VOTYandexToken$b,
    VOTYandexTokenInfo: VOTYandexTokenInfo$b,
    VOTLoginViaToken: VOTLoginViaToken$b,
    subtitlesSmartLayout: subtitlesSmartLayout$b,
    smartDucking: smartDucking$b,
    VOTAutoSubtitles: VOTAutoSubtitles$b,
    VOTSubtitlesFont: VOTSubtitlesFont$b
  };
  const recommended$a = "препоручено";
  const translateVideo$a = "Преведи видео";
  const disableTranslate$a = "Искључи";
  const translationSettings$a = "Подешавања превода";
  const subtitlesSettings$a = "Подешавања титлова";
  const resetSettings$a = "Ресетовање на фабричка подешавања";
  const videoBeingTranslated$a = "Видео преводи";
  const videoLanguage$a = "Видео језик";
  const translationLanguage$a = "Језик превода";
  const translationTake$a = "Трансфер ће потрајати";
  const translationTakeMoreThanHour$a = "Трансфер ће трајати више од сат времена";
  const translationTakeAboutMinute$a = "Трансфер ће трајати око минут";
  const translationTakeFewMinutes$a = "Трансфер ће трајати неколико минута";
  const translationTakeApproximatelyMinutes$a = "Превод ће трајати приближно {0} минута";
  const translationTakeApproximatelyMinute$a = "Превод ће трајати приближно {0} минута";
  const requestTranslationFailed$a = "Захтев за превод видео записа није успео";
  const audioNotReceived$a = "Аудио комуникација није примљена";
  const audioFormatNotSupported$a = "Аудио формат није подржан";
  const VOTAutoTranslate$a = "Преведи на отворено";
  const VOTDontTranslateYourLang$a = "Не преводите са мог језика";
  const VOTVolume$a = "Јачина звука видео записа:";
  const VOTVolumeTranslation$a = "Обим превода:";
  const VOTAutoSetVolume$a = "Смањите јачину видео записа на";
  const VOTShowVideoSlider$a = "Клизач за јачину звука видео записа";
  const VOTSyncVolume$a = "Превод веза и обим видео записа";
  const VOTDisableFromYourLang$a = "Онемогућили сте превођење видео записа на ваш језик";
  const VOTVideoIsTooLong$a = "Видео је предуг";
  const VOTNoVideoIDFound$a = "Видео ИД није пронађен";
  const VOTSubtitles$a = "Титлови";
  const VOTSubtitlesDisabled$a = "Неспособан за рад";
  const VOTSubtitlesMaxLength$a = "Максимална дужина титлова";
  const VOTHighlightWords$a = "Истакните речи";
  const VOTTranslatedFrom$a = "преведено са";
  const VOTAutogenerated$a = "аутоматски генерисан";
  const VOTSettings$a = "Вот подешавања";
  const VOTMenuLanguage$a = "Језик менија";
  const VOTAuthors$a = "Аутори";
  const VOTVersion$a = "Верзија";
  const VOTLoader$a = "Утоваривач";
  const VOTBrowser$a = "Прегледник";
  const VOTShowPiPButton$a = "Дугме Прикажи испрекидану тачку";
  const langs$a = { "auto": "Ауто", "af": "Африкаанс", "ak": "Акан", "sq": "Албански", "am": "Амхарски", "ar": "Арапски језик", "hy": "Јерменски", "as": "Ассамски", "ay": "Аймара", "az": "Азербејџански", "bn": "Бангла", "eu": "Баскијски", "be": "Белоруски", "bho": "Бхожпури", "bs": "Босански", "bg": "Бугарски", "my": "Бурмански", "ca": "Каталонски", "ceb": "Себуано", "zh": "Кинески", "zh-Hans": "Кинески (поједностављени)", "zh-Hant": "Кинески (традиционални)", "co": "Корсиканец", "hr": "Хрватски", "cs": "Чешки", "da": "Дански", "dv": "Дивехи", "nl": "Холандски", "en": "Енглески", "eo": "Есперанто", "et": "Естонски", "ee": "Овце", "fil": "Филипинац", "fi": "Фински", "fr": "Француски", "gl": "Галицијски", "lg": "Ганда", "ka": "Грузијски", "de": "Немачки", "el": "Грчки", "gn": "Гуарани", "gu": "Гуџарати", "ht": "Хаитијски креол", "ha": "Хауса", "haw": "Хавајски", "iw": "Јеврејин", "hi": "Хинди", "hmn": "Хмонг", "hu": "Мађарски", "is": "Исландски", "ig": "Игбо", "id": "Индонезијски", "ga": "Ирски", "it": "Италијански", "ja": "Јапански", "jv": "Јавански", "kn": "Каннада", "kk": "Казах", "km": "Кхмерски", "rw": "Кинјаруанда", "ko": "Корејски", "kri": "Крио", "ku": "Курдски", "ky": "Киргиз", "lo": "Лаоски", "la": "Латински", "lv": "Латвијски", "ln": "Лингала", "lt": "Литовски", "lb": "Луксембуршки", "mk": "Македонски", "mg": "Малагасијски", "ms": "Малајски", "ml": "Малајаламски", "mt": "Малтијски", "mi": "Маори", "mr": "Маратхи", "mn": "Монголски", "ne": "Непалски", "nso": "Северни Сото", "no": "Норвешки језик", "ny": "Нјања", "or": "Одиа", "om": "Оромо", "ps": "Пашту", "fa": "Персијски", "pl": "Пољски", "pt": "Португалски", "pa": "Панџаби", "qu": "Кечуа", "ro": "Румунски", "ru": "Руски", "sm": "Самоански", "sa": "Санскрит", "gd": "Шкотски Галски", "sr": "Српски", "sn": "Шона", "sd": "Синдхи", "si": "Сингаљски", "sk": "Словак", "sl": "Словенски", "so": "Сомалијски", "st": "Јужни Сото", "es": "Шпански", "su": "Сундански", "sw": "Свахили", "sv": "Шведски", "tg": "Таџички", "ta": "Тамилски", "tt": "Татарски", "te": "Телугу", "th": "Тајландски", "ti": "Тигриња", "ts": "Тсонга", "tr": "Турски", "tk": "Туркменски", "uk": "Украјински", "ur": "Урду", "ug": "Ујгурски", "uz": "Узбекски", "vi": "Вијетнамски", "cy": "Велшки", "fy": "Западно-Фризијски", "xh": "Коса", "yi": "Јидиш", "yo": "Јоруба", "zu": "Зулуски" };
  const streamNoConnectionToServer$a = "Нема везе са сервером";
  const searchField$a = "Претрага...";
  const VOTTranslateAPIErrors$a = "Превођење грешака из АПИ-ја";
  const VOTDetectService$a = "Услуга дефиниције језика";
  const VOTProxyWorkerHost$a = "Унесите радну адресу проки сервера";
  const VOTM3u8ProxyHost$a = "Унесите адресу радног проки сервера м3у8";
  const proxySettings$a = "Подешавања проксија";
  const translationTakeApproximatelyMinute2$a = "Превод ће трајати приближно {0} минута";
  const VOTAudioBooster$a = "Повећање обима проширеног трансфера";
  const VOTSubtitlesDesign$a = "Дизајн титлова";
  const VOTSubtitlesFontSize$a = "Величина фонта титлова";
  const VOTSubtitlesOpacity$a = "Транспарентност позадине титлова";
  const VOTSubtitlesDownloadFormat$a = "Формат за преузимање титлова";
  const VOTDownloadWithName$a = "Преузмите датотеке са именом видео записа";
  const VOTUpdateLocaleFiles$a = "Ажурирајте датотеке локализације";
  const VOTLocaleHash$a = "Хеш локали";
  const VOTUpdatedAt$a = "Ажурирано у";
  const VOTNeedWebAudioAPI$a = "Да бисте то омогућили, морате имати Веб Аудио АПИ";
  const VOTMediaCSPEnabledOnSite$a = "На овој веб локацији је омогућен Медиа ЦСП";
  const VOTOnlyBypassMediaCSP$a = "Користите га само за заобилажење Медиа ЦСП-а";
  const VOTNewAudioPlayer$a = "Користите нови аудио плејер";
  const VOTUseNewModel$a = "Користите експерименталну опцију Иандек Воицес за неке видео записе";
  const TranslationDelayed$a = "Превод мало касни";
  const VOTTranslationCompletedNotify$a = "Превод на {0} је завршен!";
  const VOTSendNotifyOnComplete$a = "Пошаљите обавештење да је видео преведен";
  const VOTBugReport$a = "Пријавите грешку";
  const VOTTranslateProxyDisabled$a = "Неспособан за рад";
  const VOTTranslateProxyEnabled$a = "Укључено";
  const VOTTranslateProxyEverything$a = "Проки сервер за све";
  const VOTTranslateProxyStatus$a = "Режим проксирања";
  const VOTTranslatedBy$a = "Преведено {0}";
  const VOTStreamNotAvailable$a = "Превод није доступан";
  const VOTTranslationTextService$a = "Услуге превођења текстова";
  const VOTNotAffectToVoice$a = "Не утиче на превођење текста у гласу";
  const DontTranslateSelectedLanguages$a = "Не преводите са изабраних језика";
  const showVideoVolumeSlider$a = "Прикажите клизач за јачину звука видео записа";
  const hotkeysSettings$a = "Подешавања интервентних тастера";
  const None$a = "Нико";
  const VOTUseLivelyVoice$a = "Користите гласове уживо. Говорници говоре као аутохтони Руси.";
  const miscSettings$a = "Различита подешавања";
  const services$a = { "yandexbrowser": "Иандек Претраживач", "msedge": "Мицрософт Ивица", "rust-server": "Руст Сервер" };
  const aboutExtension$a = "О проширењу";
  const appearance$a = "Изглед";
  const buttonPosition$a = "Положај дугмета у грамофону";
  const position$a = { "left": "Лево", "right": "Тачно", "top": "Горњи", "default": "Подразумевано" };
  const secs$a = "сек";
  const autoHideButtonDelay$a = "Кашњење пре скривања дугмета за превод";
  const notFound$a = "није пронађено";
  const minButtonPositionContainer$a = "Положај дугмета се мења само на плејерима већим од 600 пиксела.";
  const VOTTranslateProxyStatusDefault$a = "Потпуно онемогућавање проки сервера у вашој земљи може проузроковати квар проширења";
  const PressTheKeyCombination$a = "Притисните комбинацију тастера...";
  const VOTUseAudioDownload$a = "Користите аудио Довнлоад";
  const VOTUseAudioDownloadWarning$a = "Онемогућавање преузимања звука може утицати на функционалност проширења";
  const VOTAccountRequired$a = "Морате се пријавити да бисте користили ову функцију";
  const VOTMyAccount$a = "Мој рачун";
  const VOTLogin$a = "Пријавите се";
  const VOTLogout$a = "Одјава";
  const VOTRefresh$a = "Освежите";
  const VOTYandexToken$a = "Унесите ОАутх-Иандек токен";
  const VOTYandexTokenInfo$a = "У овом пољу можете ручно одредити токен рачуна. Имајте на уму да не проверавамо његову ваљаност пре него што пошаљемо захтев за трансфер";
  const VOTLoginViaToken$a = "Пријавите се помоћу токена";
  const subtitlesSmartLayout$a = "Паметан изглед титлова";
  const smartDucking$a = "Адаптивна јачина звука";
  const VOTAutoSubtitles$a = "Титлови приликом отварања";
  const VOTSubtitlesFont$a = "Фонт титлова";
  const __vite_glob_0_51 = {
    recommended: recommended$a,
    translateVideo: translateVideo$a,
    disableTranslate: disableTranslate$a,
    translationSettings: translationSettings$a,
    subtitlesSettings: subtitlesSettings$a,
    resetSettings: resetSettings$a,
    videoBeingTranslated: videoBeingTranslated$a,
    videoLanguage: videoLanguage$a,
    translationLanguage: translationLanguage$a,
    translationTake: translationTake$a,
    translationTakeMoreThanHour: translationTakeMoreThanHour$a,
    translationTakeAboutMinute: translationTakeAboutMinute$a,
    translationTakeFewMinutes: translationTakeFewMinutes$a,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$a,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$a,
    requestTranslationFailed: requestTranslationFailed$a,
    audioNotReceived: audioNotReceived$a,
    audioFormatNotSupported: audioFormatNotSupported$a,
    VOTAutoTranslate: VOTAutoTranslate$a,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$a,
    VOTVolume: VOTVolume$a,
    VOTVolumeTranslation: VOTVolumeTranslation$a,
    VOTAutoSetVolume: VOTAutoSetVolume$a,
    VOTShowVideoSlider: VOTShowVideoSlider$a,
    VOTSyncVolume: VOTSyncVolume$a,
    VOTDisableFromYourLang: VOTDisableFromYourLang$a,
    VOTVideoIsTooLong: VOTVideoIsTooLong$a,
    VOTNoVideoIDFound: VOTNoVideoIDFound$a,
    VOTSubtitles: VOTSubtitles$a,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$a,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$a,
    VOTHighlightWords: VOTHighlightWords$a,
    VOTTranslatedFrom: VOTTranslatedFrom$a,
    VOTAutogenerated: VOTAutogenerated$a,
    VOTSettings: VOTSettings$a,
    VOTMenuLanguage: VOTMenuLanguage$a,
    VOTAuthors: VOTAuthors$a,
    VOTVersion: VOTVersion$a,
    VOTLoader: VOTLoader$a,
    VOTBrowser: VOTBrowser$a,
    VOTShowPiPButton: VOTShowPiPButton$a,
    langs: langs$a,
    streamNoConnectionToServer: streamNoConnectionToServer$a,
    searchField: searchField$a,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$a,
    VOTDetectService: VOTDetectService$a,
    VOTProxyWorkerHost: VOTProxyWorkerHost$a,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$a,
    proxySettings: proxySettings$a,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$a,
    VOTAudioBooster: VOTAudioBooster$a,
    VOTSubtitlesDesign: VOTSubtitlesDesign$a,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$a,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$a,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$a,
    VOTDownloadWithName: VOTDownloadWithName$a,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$a,
    VOTLocaleHash: VOTLocaleHash$a,
    VOTUpdatedAt: VOTUpdatedAt$a,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$a,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$a,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$a,
    VOTNewAudioPlayer: VOTNewAudioPlayer$a,
    VOTUseNewModel: VOTUseNewModel$a,
    TranslationDelayed: TranslationDelayed$a,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$a,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$a,
    VOTBugReport: VOTBugReport$a,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$a,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$a,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$a,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$a,
    VOTTranslatedBy: VOTTranslatedBy$a,
    VOTStreamNotAvailable: VOTStreamNotAvailable$a,
    VOTTranslationTextService: VOTTranslationTextService$a,
    VOTNotAffectToVoice: VOTNotAffectToVoice$a,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$a,
    showVideoVolumeSlider: showVideoVolumeSlider$a,
    hotkeysSettings: hotkeysSettings$a,
    None: None$a,
    VOTUseLivelyVoice: VOTUseLivelyVoice$a,
    miscSettings: miscSettings$a,
    services: services$a,
    aboutExtension: aboutExtension$a,
    appearance: appearance$a,
    buttonPosition: buttonPosition$a,
    position: position$a,
    secs: secs$a,
    autoHideButtonDelay: autoHideButtonDelay$a,
    notFound: notFound$a,
    minButtonPositionContainer: minButtonPositionContainer$a,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$a,
    PressTheKeyCombination: PressTheKeyCombination$a,
    VOTUseAudioDownload: VOTUseAudioDownload$a,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$a,
    VOTAccountRequired: VOTAccountRequired$a,
    VOTMyAccount: VOTMyAccount$a,
    VOTLogin: VOTLogin$a,
    VOTLogout: VOTLogout$a,
    VOTRefresh: VOTRefresh$a,
    VOTYandexToken: VOTYandexToken$a,
    VOTYandexTokenInfo: VOTYandexTokenInfo$a,
    VOTLoginViaToken: VOTLoginViaToken$a,
    subtitlesSmartLayout: subtitlesSmartLayout$a,
    smartDucking: smartDucking$a,
    VOTAutoSubtitles: VOTAutoSubtitles$a,
    VOTSubtitlesFont: VOTSubtitlesFont$a
  };
  const recommended$9 = "disarankeun";
  const translateVideo$9 = "Narjamahkeun video";
  const disableTranslate$9 = "Pareuman";
  const translationSettings$9 = "Setélan tarjamahan";
  const subtitlesSettings$9 = "Setélan subjudul";
  const resetSettings$9 = "Setélan Reset";
  const videoBeingTranslated$9 = "Video ieu keur ditarjamahkeun";
  const videoLanguage$9 = "Basa vidéo";
  const translationLanguage$9 = "Basa tarjamahan";
  const translationTake$9 = "Tarjamahan bakal nyandak";
  const translationTakeMoreThanHour$9 = "Tarjamahan bakal nyandak leuwih ti sajam";
  const translationTakeAboutMinute$9 = "Tarjamahan bakal nyandak ngeunaan menit";
  const translationTakeFewMinutes$9 = "Tarjamahan bakal nyandak sababaraha menit";
  const translationTakeApproximatelyMinutes$9 = "Tarjamahan bakal nyandak kira-kira {0} menit";
  const translationTakeApproximatelyMinute$9 = "Tarjamahan bakal nyandak kira-kira {0} menit";
  const requestTranslationFailed$9 = "Gagal menta tarjamahan video";
  const audioNotReceived$9 = "Link Audio teu narima";
  const audioFormatNotSupported$9 = "Format audio teu dirojong";
  const VOTAutoTranslate$9 = "Tarjamahkeun dina buka";
  const VOTDontTranslateYourLang$9 = "Ulah narjamahkeun tina basa kuring";
  const VOTVolume$9 = "Jilid vidéo:";
  const VOTVolumeTranslation$9 = "Jilid tarjamahan:";
  const VOTAutoSetVolume$9 = "Ngurangan volume video ka";
  const VOTShowVideoSlider$9 = "Video volume geser";
  const VOTSyncVolume$9 = "Tarjamahan Link jeung volume video";
  const VOTDisableFromYourLang$9 = "Anjeun geus nganonaktipkeun tarjamahan video dina basa anjeun";
  const VOTVideoIsTooLong$9 = "Video téh panjang teuing";
  const VOTNoVideoIDFound$9 = "Teu aya video ANU kuring mendakan";
  const VOTSubtitles$9 = "Subjudul";
  const VOTSubtitlesDisabled$9 = "Cacat";
  const VOTSubtitlesMaxLength$9 = "Subjudul panjang maksimum";
  const VOTHighlightWords$9 = "Nyorot kecap";
  const VOTTranslatedFrom$9 = "ditarjamahkeun tina";
  const VOTAutogenerated$9 = "otomatis dihasilkeun";
  const VOTSettings$9 = "Setélan VOT";
  const VOTMenuLanguage$9 = "Basa Menu";
  const VOTAuthors$9 = "Pangarang";
  const VOTVersion$9 = "Versi";
  const VOTLoader$9 = "Loader";
  const VOTBrowser$9 = "Panyungsi";
  const VOTShowPiPButton$9 = "Témbongkeun tombol pip";
  const langs$9 = { "auto": "Otomatis", "af": "Basa Afrikaans", "ak": "Akan", "sq": "Basa albania", "am": "Basa Amharic", "ar": "Basa arab", "hy": "Basa arménia", "as": "Basa assam", "ay": "Aymara", "az": "Basa azerbaijan", "bn": "Basa Bangla", "eu": "Basa Basque", "be": "Bélarus", "bho": "Bhojpuri (basa inggris)", "bs": "Basa bosnia", "bg": "Basa bulgaria", "my": "Basa burma", "ca": "Basa katalan", "ceb": "Cébuano", "zh": "Cina", "zh-Hans": "Cina (Disederhanakeun)", "zh-Hant": "Cina (Tradisional)", "co": "Korsika", "hr": "Basa kroasia", "cs": "Basa céko", "da": "Basa denmark", "dv": "Divéhi", "nl": "Basa walanda", "en": "Basa inggris", "eo": "Basa espéranto", "et": "Basa esttonia", "ee": "Domba", "fil": "Basa filipina", "fi": "Basa finlandia", "fr": "Basa perancis", "gl": "Basa galicia", "lg": "Ganda", "ka": "Basa géorgia", "de": "Basa jerman", "el": "Basa yunani", "gn": "Basa Guarani", "gu": "Gujarati", "ht": "Basa Kreol Haiti", "ha": "Basa Hausa", "haw": "Basa hawaii", "iw": "Basa ibrani", "hi": "Basa Hindi", "hmn": "Hmong (basa inggris)", "hu": "Basa hungaria", "is": "Basa islandia", "ig": "Igbo (basa inggris)", "id": "Basa indonésia", "ga": "Irlandia", "it": "Basa italia", "ja": "Jepang", "jv": "Basa jawa", "kn": "Basa Kannada", "kk": "Kazakh", "km": "Khmer (basa inggris)", "rw": "Kinyarwanda", "ko": "Basa koréa", "kri": "Krio (basa inggris)", "ku": "Basa Kurdish", "ky": "Kirgiz", "lo": "Lao (basa inggris)", "la": "Basa Latin", "lv": "Basa latvia", "ln": "Lingala (basa inggris)", "lt": "Basa lituania", "lb": "Basa luksemburg", "mk": "Basa makédonia", "mg": "Basa madagaskar", "ms": "Basa melayu", "ml": "Malayalam", "mt": "Basa malta", "mi": "Basa Māori", "mr": "Basa Marathi", "mn": "Basa mongolia", "ne": "Basa nepal", "nso": "Sotho Kalér", "no": "Basa norwégia", "ny": "Nyanja", "or": "Odia (basa inggris)", "om": "Oromo (basa inggris)", "ps": "Basa Pashto", "fa": "Basa pérsia", "pl": "Basa polandia", "pt": "Basa portugis", "pa": "Basa Punjabi", "qu": "Basa Quechua", "ro": "Basa romania", "ru": "Rusia", "sm": "Basa samoa", "sa": "Basa sanskerta", "gd": "Basa Gaelik Skotlandia", "sr": "Basa sérbia", "sn": "Shona (basa inggris)", "sd": "Sindhi (basa Sindhi)", "si": "Sinhala", "sk": "Basa slowakia", "sl": "Basa slovenia", "so": "Basa somalia", "st": "Sotho Kidul", "es": "Basa spanyol", "su": "Basa sunda", "sw": "Basa Swahili", "sv": "Basa swédia", "tg": "Basa Tajik", "ta": "Basa Tamil", "tt": "Tatar", "te": "Telugu (basa inggris)", "th": "Basa thailand", "ti": "Tigrinya", "ts": "Tsonga (basa inggris)", "tr": "Basa turki", "tk": "Urang Turkmen", "uk": "Basa ukrania", "ur": "Basa Urdu", "ug": "Urghur", "uz": "Uzbék", "vi": "Urang viétnam", "cy": "Basa wales", "fy": "Frisian Kulon", "xh": "Xhosa (basa inggris)", "yi": "Basa Yiddish", "yo": "Basa Yoruba", "zu": "Basa Zulu" };
  const streamNoConnectionToServer$9 = "Teu aya sambungan ka server";
  const searchField$9 = "Néangan...";
  const VOTTranslateAPIErrors$9 = "Narjamahkeun kasalahan TINA API";
  const VOTDetectService$9 = "Jasa deteksi basa";
  const VOTProxyWorkerHost$9 = "Asupkeun alamat proxy worker";
  const VOTM3u8ProxyHost$9 = "Tuliskeun alamat worker proxy m3u8";
  const proxySettings$9 = "Setélan Proxy";
  const translationTakeApproximatelyMinute2$9 = "Tarjamahan bakal nyandak kira-kira {0} menit";
  const VOTAudioBooster$9 = "Paningkatan volume tarjamahan anu diperpanjang";
  const VOTSubtitlesDesign$9 = "Desain subtitle";
  const VOTSubtitlesFontSize$9 = "Ukuran font tina subtitles";
  const VOTSubtitlesOpacity$9 = "Transparansi tina latar subjudul";
  const VOTSubtitlesDownloadFormat$9 = "Format pikeun ngundeur terjemahan";
  const VOTDownloadWithName$9 = "Ngundeur payil jeung ngaran video";
  const VOTUpdateLocaleFiles$9 = "File lokalisasi Update";
  const VOTLocaleHash$9 = "Lokal hash";
  const VOTUpdatedAt$9 = "Diperbarui jam";
  const VOTNeedWebAudioAPI$9 = "Pikeun ngaktipkeun ieu, anjeun kudu boga API Audio Web";
  const VOTMediaCSPEnabledOnSite$9 = "Media csp diaktipkeun dina situs ieu";
  const VOTOnlyBypassMediaCSP$9 = "Ngagunakeun eta ngan Pikeun Bypassing Media Csp";
  const VOTNewAudioPlayer$9 = "Paké pamuter audio anyar";
  const VOTUseNewModel$9 = "Paké variasi ékspérimén sora Yandex pikeun sababaraha video";
  const TranslationDelayed$9 = "Tarjamahan rada ditunda";
  const VOTTranslationCompletedNotify$9 = "Tarjamahan dina {0} geus réngsé!";
  const VOTSendNotifyOnComplete$9 = "Kirimkeun bewara yén video geus ditarjamahkeun";
  const VOTBugReport$9 = "Laporan hiji bug";
  const VOTTranslateProxyDisabled$9 = "Cacat";
  const VOTTranslateProxyEnabled$9 = "Diaktipkeun";
  const VOTTranslateProxyEverything$9 = "Proxy sagalana";
  const VOTTranslateProxyStatus$9 = "Modeu proxy";
  const VOTTranslatedBy$9 = "Ditarjamahkeun ku {0}";
  const VOTStreamNotAvailable$9 = "Translate stream teu aya";
  const VOTTranslationTextService$9 = "Jasa tarjamahan téks";
  const VOTNotAffectToVoice$9 = "Teu mangaruhan tarjamahan téks dina sora leuwih";
  const DontTranslateSelectedLanguages$9 = "Ulah narjamahkeun tina basa nu dipilih";
  const showVideoVolumeSlider$9 = "Témbongkeun slaider volume video";
  const hotkeysSettings$9 = "Hotkeys setélan";
  const None$9 = "Teu aya";
  const VOTUseLivelyVoice$9 = "Ngagunakeun sora nu hirup. Nu nyarita disada Kawas urang Rusia asli.";
  const miscSettings$9 = "Setélan Misc";
  const services$9 = { "yandexbrowser": "Browser Yandex", "msedge": "Microsoft Tepi", "rust-server": "Karat Server" };
  const aboutExtension$9 = "Ngeunaan extension";
  const appearance$9 = "Penampilan";
  const buttonPosition$9 = "Posisi tombol dina pamuter";
  const position$9 = { "left": "Kénca", "right": "Katuhu", "top": "Puncak", "default": "Standar" };
  const secs$9 = "detik";
  const autoHideButtonDelay$9 = "Reureuh saméméh nyumputkeun tombol tarjamah";
  const notFound$9 = "teu kapanggih";
  const minButtonPositionContainer$9 = "Posisi tombol ngan robah dina pamaén leuwih gede ti 600 piksel.";
  const VOTTranslateProxyStatusDefault$9 = "Lengkep nganonaktipkeun proxying di nagara anjeun bisa megatkeun extension nu";
  const PressTheKeyCombination$9 = "Pencét kombinasi konci...";
  const VOTUseAudioDownload$9 = "Paké audio download";
  const VOTUseAudioDownloadWarning$9 = "Nganonaktipkeun download audio bisa mangaruhan fungsionalitas extension";
  const VOTAccountRequired$9 = "Anjeun kudu asup pikeun make fitur ieu";
  const VOTMyAccount$9 = "Akun abdi";
  const VOTLogin$9 = "Login";
  const VOTLogout$9 = "Logout";
  const VOTRefresh$9 = "Nyegerkeun";
  const VOTYandexToken$9 = "Asupkeun Token Yandex OAuth";
  const VOTYandexTokenInfo$9 = "Anjeun tiasa sacara manual nyetel token akun dina widang ieu. Punten perhatoskeun yén kami henteu mariksa validitasna sateuacan ngirim pamundut tarjamahan";
  const VOTLoginViaToken$9 = "Login ngaliwatan token";
  const subtitlesSmartLayout$9 = "Tata perenah subjudul pinter";
  const smartDucking$9 = "Volume adaptif";
  const VOTAutoSubtitles$9 = "Subjudul dina buka";
  const VOTSubtitlesFont$9 = "Font subjudul";
  const __vite_glob_0_52 = {
    recommended: recommended$9,
    translateVideo: translateVideo$9,
    disableTranslate: disableTranslate$9,
    translationSettings: translationSettings$9,
    subtitlesSettings: subtitlesSettings$9,
    resetSettings: resetSettings$9,
    videoBeingTranslated: videoBeingTranslated$9,
    videoLanguage: videoLanguage$9,
    translationLanguage: translationLanguage$9,
    translationTake: translationTake$9,
    translationTakeMoreThanHour: translationTakeMoreThanHour$9,
    translationTakeAboutMinute: translationTakeAboutMinute$9,
    translationTakeFewMinutes: translationTakeFewMinutes$9,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$9,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$9,
    requestTranslationFailed: requestTranslationFailed$9,
    audioNotReceived: audioNotReceived$9,
    audioFormatNotSupported: audioFormatNotSupported$9,
    VOTAutoTranslate: VOTAutoTranslate$9,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$9,
    VOTVolume: VOTVolume$9,
    VOTVolumeTranslation: VOTVolumeTranslation$9,
    VOTAutoSetVolume: VOTAutoSetVolume$9,
    VOTShowVideoSlider: VOTShowVideoSlider$9,
    VOTSyncVolume: VOTSyncVolume$9,
    VOTDisableFromYourLang: VOTDisableFromYourLang$9,
    VOTVideoIsTooLong: VOTVideoIsTooLong$9,
    VOTNoVideoIDFound: VOTNoVideoIDFound$9,
    VOTSubtitles: VOTSubtitles$9,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$9,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$9,
    VOTHighlightWords: VOTHighlightWords$9,
    VOTTranslatedFrom: VOTTranslatedFrom$9,
    VOTAutogenerated: VOTAutogenerated$9,
    VOTSettings: VOTSettings$9,
    VOTMenuLanguage: VOTMenuLanguage$9,
    VOTAuthors: VOTAuthors$9,
    VOTVersion: VOTVersion$9,
    VOTLoader: VOTLoader$9,
    VOTBrowser: VOTBrowser$9,
    VOTShowPiPButton: VOTShowPiPButton$9,
    langs: langs$9,
    streamNoConnectionToServer: streamNoConnectionToServer$9,
    searchField: searchField$9,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$9,
    VOTDetectService: VOTDetectService$9,
    VOTProxyWorkerHost: VOTProxyWorkerHost$9,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$9,
    proxySettings: proxySettings$9,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$9,
    VOTAudioBooster: VOTAudioBooster$9,
    VOTSubtitlesDesign: VOTSubtitlesDesign$9,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$9,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$9,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$9,
    VOTDownloadWithName: VOTDownloadWithName$9,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$9,
    VOTLocaleHash: VOTLocaleHash$9,
    VOTUpdatedAt: VOTUpdatedAt$9,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$9,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$9,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$9,
    VOTNewAudioPlayer: VOTNewAudioPlayer$9,
    VOTUseNewModel: VOTUseNewModel$9,
    TranslationDelayed: TranslationDelayed$9,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$9,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$9,
    VOTBugReport: VOTBugReport$9,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$9,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$9,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$9,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$9,
    VOTTranslatedBy: VOTTranslatedBy$9,
    VOTStreamNotAvailable: VOTStreamNotAvailable$9,
    VOTTranslationTextService: VOTTranslationTextService$9,
    VOTNotAffectToVoice: VOTNotAffectToVoice$9,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$9,
    showVideoVolumeSlider: showVideoVolumeSlider$9,
    hotkeysSettings: hotkeysSettings$9,
    None: None$9,
    VOTUseLivelyVoice: VOTUseLivelyVoice$9,
    miscSettings: miscSettings$9,
    services: services$9,
    aboutExtension: aboutExtension$9,
    appearance: appearance$9,
    buttonPosition: buttonPosition$9,
    position: position$9,
    secs: secs$9,
    autoHideButtonDelay: autoHideButtonDelay$9,
    notFound: notFound$9,
    minButtonPositionContainer: minButtonPositionContainer$9,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$9,
    PressTheKeyCombination: PressTheKeyCombination$9,
    VOTUseAudioDownload: VOTUseAudioDownload$9,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$9,
    VOTAccountRequired: VOTAccountRequired$9,
    VOTMyAccount: VOTMyAccount$9,
    VOTLogin: VOTLogin$9,
    VOTLogout: VOTLogout$9,
    VOTRefresh: VOTRefresh$9,
    VOTYandexToken: VOTYandexToken$9,
    VOTYandexTokenInfo: VOTYandexTokenInfo$9,
    VOTLoginViaToken: VOTLoginViaToken$9,
    subtitlesSmartLayout: subtitlesSmartLayout$9,
    smartDucking: smartDucking$9,
    VOTAutoSubtitles: VOTAutoSubtitles$9,
    VOTSubtitlesFont: VOTSubtitlesFont$9
  };
  const recommended$8 = "Rekommenderad";
  const translateVideo$8 = "Översätt video";
  const disableTranslate$8 = "Stänga";
  const translationSettings$8 = "Översättningsinställningar";
  const subtitlesSettings$8 = "Inställningar för undertexter";
  const resetSettings$8 = "Återställ inställningar";
  const videoBeingTranslated$8 = "Videon översätts";
  const videoLanguage$8 = "Video språk";
  const translationLanguage$8 = "Översättningsspråk";
  const translationTake$8 = "Översättningen kommer att ta";
  const translationTakeMoreThanHour$8 = "Översättningen tar mer än en timme";
  const translationTakeAboutMinute$8 = "Översättningen tar ungefär en minut";
  const translationTakeFewMinutes$8 = "Översättningen tar några minuter";
  const translationTakeApproximatelyMinutes$8 = "Översättningen tar ungefär {0} minuter";
  const translationTakeApproximatelyMinute$8 = "Översättningen tar ungefär {0} minuter";
  const requestTranslationFailed$8 = "Misslyckades med att begära videoöversättning";
  const audioNotReceived$8 = "Ljudlänk inte mottagen";
  const audioFormatNotSupported$8 = "Ljudformatet stöds inte";
  const VOTAutoTranslate$8 = "Översätt på öppna";
  const VOTDontTranslateYourLang$8 = "Översätt inte från mitt språk";
  const VOTVolume$8 = "Video volym:";
  const VOTVolumeTranslation$8 = "Översättningsvolym:";
  const VOTAutoSetVolume$8 = "Minska videovolymen till";
  const VOTShowVideoSlider$8 = "Video volym reglaget";
  const VOTSyncVolume$8 = "Länk översättning och video volym";
  const VOTDisableFromYourLang$8 = "Du har inaktiverat översättningen av videon på ditt språk";
  const VOTVideoIsTooLong$8 = "Videon är för lång";
  const VOTNoVideoIDFound$8 = "Inget video-ID hittades";
  const VOTSubtitles$8 = "Rubrik";
  const VOTSubtitlesDisabled$8 = "Inaktiverad";
  const VOTSubtitlesMaxLength$8 = "Textning max längd";
  const VOTHighlightWords$8 = "Markera ord";
  const VOTTranslatedFrom$8 = "översatt från";
  const VOTAutogenerated$8 = "autogenererad";
  const VOTSettings$8 = "VOT Inställningar";
  const VOTMenuLanguage$8 = "Menyspråk";
  const VOTAuthors$8 = "Författare";
  const VOTVersion$8 = "Version";
  const VOTLoader$8 = "Lastare";
  const VOTBrowser$8 = "Webbläsare";
  const VOTShowPiPButton$8 = "Visa pip-knapp";
  const langs$8 = { "auto": "Auto", "af": "Afrikaans", "ak": "Akan", "sq": "Albansk", "am": "Amhariska", "ar": "Arabiska", "hy": "Armenisk", "as": "Assamiska", "ay": "Aymara", "az": "Azerbajdzjanska", "bn": "Bangla", "eu": "Baskien", "be": "Vitryssland", "bho": "Bhojpuri", "bs": "Bosnisk", "bg": "Bulgariska", "my": "Burmesisk", "ca": "Katalanska", "ceb": "Cebuano", "zh": "Kinesisk", "zh-Hans": "Kinesiska (Förenklad)", "zh-Hant": "Kinesiska (Traditionell)", "co": "Korsikansk", "hr": "Kroatisk", "cs": "Tjeckisk", "da": "Dansk", "dv": "Divehi", "nl": "Nederländerna", "en": "Engelsk", "eo": "Esperanto", "et": "Estnisk", "ee": "Ewe", "fil": "Filippinska", "fi": "Finsk", "fr": "Fransk", "gl": "Galicisk", "lg": "Ganda", "ka": "Georgisk", "de": "Tysk", "el": "Grekisk", "gn": "Guarani", "gu": "Gujarati", "ht": "Haitisk Kreol", "ha": "Hausa", "haw": "Hawaiian", "iw": "Hebreisk", "hi": "Hindi", "hmn": "Hmong", "hu": "Ungersk", "is": "Isländska", "ig": "Igbo", "id": "Indonesisk", "ga": "Irländsk", "it": "Italiensk", "ja": "Japansk", "jv": "Javanesiska", "kn": "Kannada", "kk": "Kazakstan", "km": "Khmer", "rw": "Kinyarwanda", "ko": "Koreansk", "kri": "Krio", "ku": "Kurdisk", "ky": "Kirgizistan", "lo": "Lao", "la": "Latin", "lv": "Lettisk", "ln": "Lingala", "lt": "Litauiska", "lb": "Luxemburgska", "mk": "Makedonsk", "mg": "Madagaskisk", "ms": "Malaysisk", "ml": "Malayalam", "mt": "Malta", "mi": "M xxori", "mr": "Marathi", "mn": "Mongoliska", "ne": "Nepalesisk", "nso": "Norra Sotho", "no": "Norsk", "ny": "Nyanja", "or": "Odia", "om": "Oromo", "ps": "Pashto", "fa": "Persiska", "pl": "Polens", "pt": "Portugisisk", "pa": "Punjabi", "qu": "Quechua", "ro": "Rumänska", "ru": "Rysk", "sm": "Samoanska", "sa": "Sanskrit", "gd": "Skotsk Gäliska", "sr": "Serber", "sn": "Shona", "sd": "Sindhi", "si": "Sinhala", "sk": "Slovakien", "sl": "Slovenska", "so": "Somali", "st": "Södra Sotho", "es": "Spansk", "su": "Sundanese", "sw": "Swahili", "sv": "Svensk", "tg": "Tadzjikiska", "ta": "Tamil", "tt": "Tatariska", "te": "Telugu", "th": "Thailändska", "ti": "Tigrinja", "ts": "Tsonga", "tr": "Turkisk", "tk": "Turkmensk", "uk": "Ukrainsk", "ur": "Urdu", "ug": "Uiguriska", "uz": "Uzbekisk", "vi": "Vietnamesiska", "cy": "Walesisk", "fy": "Västfrisiska", "xh": "Xhosa", "yi": "Jiddish", "yo": "Yoruba", "zu": "Zulu" };
  const streamNoConnectionToServer$8 = "Det finns ingen anslutning till servern";
  const searchField$8 = "Söka...";
  const VOTTranslateAPIErrors$8 = "Översätt fel från API";
  const VOTDetectService$8 = "Språkidentifieringstjänst";
  const VOTProxyWorkerHost$8 = "Ange proxyarbetarens adress";
  const VOTM3u8ProxyHost$8 = "Ange adressen till M3U8-proxyarbetaren";
  const proxySettings$8 = "Proxyinställningar";
  const translationTakeApproximatelyMinute2$8 = "Översättningen tar ungefär {0} minuter";
  const VOTAudioBooster$8 = "Utökad volymökning av översättning";
  const VOTSubtitlesDesign$8 = "Textning design";
  const VOTSubtitlesFontSize$8 = "Teckenstorlek på undertexter";
  const VOTSubtitlesOpacity$8 = "Öppenhet i undertextens bakgrund";
  const VOTSubtitlesDownloadFormat$8 = "Formatet för nedladdning av undertexter";
  const VOTDownloadWithName$8 = "Ladda ner filer med videonamnet";
  const VOTUpdateLocaleFiles$8 = "Uppdatera lokaliseringsfiler";
  const VOTLocaleHash$8 = "Lokal hash";
  const VOTUpdatedAt$8 = "Uppdaterat klockan";
  const VOTNeedWebAudioAPI$8 = "För att aktivera detta måste du ha ETT Web Audio API";
  const VOTMediaCSPEnabledOnSite$8 = "Media CSP är aktiverat på den här webbplatsen";
  const VOTOnlyBypassMediaCSP$8 = "Använd den bara för att kringgå Media CSP";
  const VOTNewAudioPlayer$8 = "Använd den nya ljudspelaren";
  const VOTUseNewModel$8 = "Använd en experimentell variant av Yandex-Röster För vissa videor";
  const TranslationDelayed$8 = "Översättningen är något försenad";
  const VOTTranslationCompletedNotify$8 = "Översättningen på {0} är klar!";
  const VOTSendNotifyOnComplete$8 = "Skicka ett meddelande om att videon har översatts";
  const VOTBugReport$8 = "Rapportera ett fel";
  const VOTTranslateProxyDisabled$8 = "Inaktiverad";
  const VOTTranslateProxyEnabled$8 = "Aktiverad";
  const VOTTranslateProxyEverything$8 = "Proxy allt";
  const VOTTranslateProxyStatus$8 = "Proxying-läge";
  const VOTTranslatedBy$8 = "Översatt av {0}";
  const VOTStreamNotAvailable$8 = "Translate stream är inte tillgängligt";
  const VOTTranslationTextService$8 = "Textöversättningstjänst";
  const VOTNotAffectToVoice$8 = "Påverkar inte översättningen av text i voice over";
  const DontTranslateSelectedLanguages$8 = "Översätt inte från valda språk";
  const showVideoVolumeSlider$8 = "Visa reglaget för videovolym";
  const hotkeysSettings$8 = "Snabbtangenter inställningar";
  const None$8 = "Ingen";
  const VOTUseLivelyVoice$8 = "Använd livliga röster. Högtalare låter som inhemska ryssar.";
  const miscSettings$8 = "Övriga inställningar";
  const services$8 = { "yandexbrowser": "Yandex Browser", "msedge": "Microsoft Edge", "rust-server": "Rust Server" };
  const aboutExtension$8 = "Om förlängning";
  const appearance$8 = "Utseende";
  const buttonPosition$8 = "Knappens position i spelaren";
  const position$8 = { "left": "Vänster", "right": "Rättighet", "top": "Topp", "default": "Standard" };
  const secs$8 = "sek";
  const autoHideButtonDelay$8 = "Fördröjning innan du döljer Översätt-knappen";
  const notFound$8 = "hittades inte";
  const minButtonPositionContainer$8 = "Knappens position ändras bara i spelare som är större än 600 pixlar.";
  const VOTTranslateProxyStatusDefault$8 = "Helt inaktiverande proxy i ditt land kan bryta förlängningen";
  const PressTheKeyCombination$8 = "Tryck på tangentkombinationen...";
  const VOTUseAudioDownload$8 = "Använd ljudnedladdning";
  const VOTUseAudioDownloadWarning$8 = "Inaktivera ljudnedladdningar kan påverka funktionaliteten i tillägget";
  const VOTAccountRequired$8 = "Du måste logga in för att använda den här funktionen";
  const VOTMyAccount$8 = "Mitt konto";
  const VOTLogin$8 = "Logga";
  const VOTLogout$8 = "Utloggning";
  const VOTRefresh$8 = "Uppdatera";
  const VOTYandexToken$8 = "Ange Yandex OAuth-Token";
  const VOTYandexTokenInfo$8 = "Du kan ställa in kontotoken manuellt i det här fältet. Observera att vi inte kontrollerar dess giltighet innan vi skickar en översättningsförfrågan";
  const VOTLoginViaToken$8 = "Logga in via token";
  const subtitlesSmartLayout$8 = "Smart textning layout";
  const smartDucking$8 = "Adaptiv volym";
  const VOTAutoSubtitles$8 = "Undertexter på open";
  const VOTSubtitlesFont$8 = "Textning teckensnitt";
  const __vite_glob_0_53 = {
    recommended: recommended$8,
    translateVideo: translateVideo$8,
    disableTranslate: disableTranslate$8,
    translationSettings: translationSettings$8,
    subtitlesSettings: subtitlesSettings$8,
    resetSettings: resetSettings$8,
    videoBeingTranslated: videoBeingTranslated$8,
    videoLanguage: videoLanguage$8,
    translationLanguage: translationLanguage$8,
    translationTake: translationTake$8,
    translationTakeMoreThanHour: translationTakeMoreThanHour$8,
    translationTakeAboutMinute: translationTakeAboutMinute$8,
    translationTakeFewMinutes: translationTakeFewMinutes$8,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$8,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$8,
    requestTranslationFailed: requestTranslationFailed$8,
    audioNotReceived: audioNotReceived$8,
    audioFormatNotSupported: audioFormatNotSupported$8,
    VOTAutoTranslate: VOTAutoTranslate$8,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$8,
    VOTVolume: VOTVolume$8,
    VOTVolumeTranslation: VOTVolumeTranslation$8,
    VOTAutoSetVolume: VOTAutoSetVolume$8,
    VOTShowVideoSlider: VOTShowVideoSlider$8,
    VOTSyncVolume: VOTSyncVolume$8,
    VOTDisableFromYourLang: VOTDisableFromYourLang$8,
    VOTVideoIsTooLong: VOTVideoIsTooLong$8,
    VOTNoVideoIDFound: VOTNoVideoIDFound$8,
    VOTSubtitles: VOTSubtitles$8,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$8,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$8,
    VOTHighlightWords: VOTHighlightWords$8,
    VOTTranslatedFrom: VOTTranslatedFrom$8,
    VOTAutogenerated: VOTAutogenerated$8,
    VOTSettings: VOTSettings$8,
    VOTMenuLanguage: VOTMenuLanguage$8,
    VOTAuthors: VOTAuthors$8,
    VOTVersion: VOTVersion$8,
    VOTLoader: VOTLoader$8,
    VOTBrowser: VOTBrowser$8,
    VOTShowPiPButton: VOTShowPiPButton$8,
    langs: langs$8,
    streamNoConnectionToServer: streamNoConnectionToServer$8,
    searchField: searchField$8,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$8,
    VOTDetectService: VOTDetectService$8,
    VOTProxyWorkerHost: VOTProxyWorkerHost$8,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$8,
    proxySettings: proxySettings$8,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$8,
    VOTAudioBooster: VOTAudioBooster$8,
    VOTSubtitlesDesign: VOTSubtitlesDesign$8,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$8,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$8,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$8,
    VOTDownloadWithName: VOTDownloadWithName$8,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$8,
    VOTLocaleHash: VOTLocaleHash$8,
    VOTUpdatedAt: VOTUpdatedAt$8,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$8,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$8,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$8,
    VOTNewAudioPlayer: VOTNewAudioPlayer$8,
    VOTUseNewModel: VOTUseNewModel$8,
    TranslationDelayed: TranslationDelayed$8,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$8,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$8,
    VOTBugReport: VOTBugReport$8,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$8,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$8,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$8,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$8,
    VOTTranslatedBy: VOTTranslatedBy$8,
    VOTStreamNotAvailable: VOTStreamNotAvailable$8,
    VOTTranslationTextService: VOTTranslationTextService$8,
    VOTNotAffectToVoice: VOTNotAffectToVoice$8,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$8,
    showVideoVolumeSlider: showVideoVolumeSlider$8,
    hotkeysSettings: hotkeysSettings$8,
    None: None$8,
    VOTUseLivelyVoice: VOTUseLivelyVoice$8,
    miscSettings: miscSettings$8,
    services: services$8,
    aboutExtension: aboutExtension$8,
    appearance: appearance$8,
    buttonPosition: buttonPosition$8,
    position: position$8,
    secs: secs$8,
    autoHideButtonDelay: autoHideButtonDelay$8,
    notFound: notFound$8,
    minButtonPositionContainer: minButtonPositionContainer$8,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$8,
    PressTheKeyCombination: PressTheKeyCombination$8,
    VOTUseAudioDownload: VOTUseAudioDownload$8,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$8,
    VOTAccountRequired: VOTAccountRequired$8,
    VOTMyAccount: VOTMyAccount$8,
    VOTLogin: VOTLogin$8,
    VOTLogout: VOTLogout$8,
    VOTRefresh: VOTRefresh$8,
    VOTYandexToken: VOTYandexToken$8,
    VOTYandexTokenInfo: VOTYandexTokenInfo$8,
    VOTLoginViaToken: VOTLoginViaToken$8,
    subtitlesSmartLayout: subtitlesSmartLayout$8,
    smartDucking: smartDucking$8,
    VOTAutoSubtitles: VOTAutoSubtitles$8,
    VOTSubtitlesFont: VOTSubtitlesFont$8
  };
  const recommended$7 = "imependekezwa";
  const translateVideo$7 = "Tafsiri video";
  const disableTranslate$7 = "Zima";
  const translationSettings$7 = "Mipangilio ya tafsiri";
  const subtitlesSettings$7 = "Mipangilio ya manukuu";
  const resetSettings$7 = "Weka upya mipangilio";
  const videoBeingTranslated$7 = "Video hiyo inatafsiriwa";
  const videoLanguage$7 = "Lugha ya Video";
  const translationLanguage$7 = "Lugha ya tafsiri";
  const translationTake$7 = "Tafsiri itachukua";
  const translationTakeMoreThanHour$7 = "Tafsiri itachukua zaidi ya saa moja";
  const translationTakeAboutMinute$7 = "Tafsiri itachukua kama dakika";
  const translationTakeFewMinutes$7 = "Tafsiri itachukua dakika chache";
  const translationTakeApproximatelyMinutes$7 = "Tafsiri itachukua takriban {0} dakika";
  const translationTakeApproximatelyMinute$7 = "Tafsiri itachukua takriban {0} dakika";
  const requestTranslationFailed$7 = "Imeshindwa kuomba tafsiri ya video";
  const audioNotReceived$7 = "Kiungo cha sauti hakijapokelewa";
  const audioFormatNotSupported$7 = "Umbizo la sauti halitumiki";
  const VOTAutoTranslate$7 = "Tafsiri juu ya wazi";
  const VOTDontTranslateYourLang$7 = "Usitafsiri kutoka kwa lugha yangu";
  const VOTVolume$7 = "Kiasi cha Video:";
  const VOTVolumeTranslation$7 = "Kiasi cha tafsiri:";
  const VOTAutoSetVolume$7 = "Punguza sauti ya video hadi";
  const VOTShowVideoSlider$7 = "Video kiasi slider";
  const VOTSyncVolume$7 = "Tafsiri ya kiungo na kiasi cha video";
  const VOTDisableFromYourLang$7 = "Umezima tafsiri ya video katika lugha yako";
  const VOTVideoIsTooLong$7 = "Video ni ndefu sana";
  const VOTNoVideoIDFound$7 = "HAKUNA KITAMBULISHO cha video kilichopatikana";
  const VOTSubtitles$7 = "Manukuu";
  const VOTSubtitlesDisabled$7 = "Walemavu";
  const VOTSubtitlesMaxLength$7 = "Manukuu urefu wa juu";
  const VOTHighlightWords$7 = "Angazia maneno";
  const VOTTranslatedFrom$7 = "ilitafsiriwa kutoka";
  const VOTAutogenerated$7 = "autogenerated";
  const VOTSettings$7 = "Mipangilio YA KURA";
  const VOTMenuLanguage$7 = "Lugha ya menyu";
  const VOTAuthors$7 = "Waandishi";
  const VOTVersion$7 = "Toleo";
  const VOTLoader$7 = "Kipakiaji";
  const VOTBrowser$7 = "Kivinjari";
  const VOTShowPiPButton$7 = "Show kitufe Cha PiP";
  const langs$7 = { "auto": "Auto", "af": "Waafrika", "ak": "Akani", "sq": "Kialbania", "am": "Kiamhari", "ar": "Kiarabu", "hy": "Kiarmenia", "as": "Kiasami", "ay": "Aymara", "az": "Kiazerbaijani", "bn": "Bangla", "eu": "Lugha ya Basque", "be": "Kibelarusi", "bho": "Bhojpuri", "bs": "Kibosnia", "bg": "Kibulgaria", "my": "Kiburma", "ca": "Kikatalani", "ceb": "Cebuano", "zh": "Kichina", "zh-Hans": "Kichina (Kilichorahisishwa)", "zh-Hant": "Kichina (Jadi)", "co": "Korsika", "hr": "Kikroatia", "cs": "Kicheki", "da": "Kidenmaki", "dv": "Divehi", "nl": "Kiholanzi", "en": "Kiingereza", "eo": "Kiesperanto", "et": "Kiestonia", "ee": "We", "fil": "Kifilipino", "fi": "Kifini", "fr": "Kifaransa", "gl": "Kigalisia", "lg": "Ganda", "ka": "Kigeorgia", "de": "Kijerumani", "el": "Kigiriki", "gn": "Guarani", "gu": "Kigujarati", "ht": "Kikrioli Cha Haiti", "ha": "Hausa", "haw": "Kihawaii", "iw": "Kiebrania", "hi": "Kihindi", "hmn": "Hmong", "hu": "Kihungaria", "is": "Kiaislandi", "ig": "Igbo", "id": "Kiindonesia", "ga": "Kiirelandi", "it": "Kiitaliano", "ja": "Kijapani", "jv": "Kijava", "kn": "Kannada", "kk": "Kazakh", "km": "Khmer", "rw": "Kinyarwanda", "ko": "Kikorea", "kri": "Krio", "ku": "Kikurdi", "ky": "Kyrgyz", "lo": "Lao", "la": "Kilatini", "lv": "Kilatvia", "ln": "Lingala", "lt": "Kilithuania", "lb": "Lugha ya luxemburg", "mk": "Kimasedonia", "mg": "Kimalagasi", "ms": "Kimalya", "ml": "Kimalayalamu", "mt": "Kimalta", "mi": "Wamaori", "mr": "Kimarathi", "mn": "Kimongolia", "ne": "Kinepali", "nso": "Sotho Ya Kaskazini", "no": "Kinorwe", "ny": "Nyanja", "or": "Odia", "om": "Oromo", "ps": "Kipashu", "fa": "Kiajemi", "pl": "Kipolishi", "pt": "Kireno", "pa": "Kipunjabi", "qu": "Lugha ya Quechua", "ro": "Kiromania", "ru": "Kirusi", "sm": "Kisamoa", "sa": "Kisanskriti", "gd": "Kigeliki Cha Scotland", "sr": "Kiserbia", "sn": "Shona", "sd": "Sindhi", "si": "Kisinhala", "sk": "Kislovakia", "sl": "Kislovenia", "so": "Kisomali", "st": "Wasotho Wa Kusini", "es": "Kihispania", "su": "Wasundan", "sw": "Kiswahili", "sv": "Kiswidi", "tg": "Tajik", "ta": "Kitamil", "tt": "Kitatari", "te": "Kitelugu", "th": "Kithai", "ti": "Mti wa Tigrinya", "ts": "Tsonga", "tr": "Kituruki", "tk": "Waturuki", "uk": "Kiukreni", "ur": "Kiurdu", "ug": "Uyghur", "uz": "Kiuzbeki", "vi": "Kivietinamu", "cy": "Kiwelsh", "fy": "Frisian Ya Magharibi", "xh": "Kixhosa", "yi": "Kiyidish", "yo": "Wayoruba", "zu": "Kizulu" };
  const streamNoConnectionToServer$7 = "Hakuna uhusiano na server";
  const searchField$7 = "Tafuta...";
  const VOTTranslateAPIErrors$7 = "Tafsiri makosa KUTOKA API";
  const VOTDetectService$7 = "Huduma ya kugundua lugha";
  const VOTProxyWorkerHost$7 = "Ingiza anwani ya mfanyakazi wa wakala";
  const VOTM3u8ProxyHost$7 = "Ingiza anwani ya mfanyakazi wa wakala wa m3u8";
  const proxySettings$7 = "Mipangilio Ya Wakala";
  const translationTakeApproximatelyMinute2$7 = "Tafsiri itachukua takriban {0} dakika";
  const VOTAudioBooster$7 = "Kuongezeka kwa kiasi cha tafsiri";
  const VOTSubtitlesDesign$7 = "Subtitles kubuni";
  const VOTSubtitlesFontSize$7 = "Ukubwa wa fonti ya manukuu";
  const VOTSubtitlesOpacity$7 = "Uwazi wa mandharinyuma ya manukuu";
  const VOTSubtitlesDownloadFormat$7 = "Umbizo la kupakua manukuu";
  const VOTDownloadWithName$7 = "Pakua faili zilizo na jina la video";
  const VOTUpdateLocaleFiles$7 = "Sasisha faili za ujanibishaji";
  const VOTLocaleHash$7 = "Eneo hash";
  const VOTUpdatedAt$7 = "Imesasishwa saa";
  const VOTNeedWebAudioAPI$7 = "Ili kuwezesha hii, lazima uwe NA API Ya Sauti Ya Wavuti";
  const VOTMediaCSPEnabledOnSite$7 = "Media CSP imewezeshwa kwenye tovuti hii";
  const VOTOnlyBypassMediaCSP$7 = "Tumia Tu kwa kupitisha Vyombo VYA Habari CSP";
  const VOTNewAudioPlayer$7 = "Tumia kicheza sauti kipya";
  const VOTUseNewModel$7 = "Tumia tofauti ya majaribio Ya sauti Za Yandex kwa video zingine";
  const TranslationDelayed$7 = "Tafsiri imechelewa kidogo";
  const VOTTranslationCompletedNotify$7 = "Tafsiri ya {0} imekamilika!";
  const VOTSendNotifyOnComplete$7 = "Tuma arifa kwamba video imetafsiriwa";
  const VOTBugReport$7 = "Ripoti mdudu";
  const VOTTranslateProxyDisabled$7 = "Walemavu";
  const VOTTranslateProxyEnabled$7 = "Imewezeshwa";
  const VOTTranslateProxyEverything$7 = "Wakala kila kitu";
  const VOTTranslateProxyStatus$7 = "Hali ya wakala";
  const VOTTranslatedBy$7 = "Ilitafsiriwa na {0}";
  const VOTStreamNotAvailable$7 = "Tafsiri ya mkondo haipatikani";
  const VOTTranslationTextService$7 = "Huduma ya tafsiri ya maandishi";
  const VOTNotAffectToVoice$7 = "Haiathiri tafsiri ya maandishi kwa sauti juu";
  const DontTranslateSelectedLanguages$7 = "Usitafsiri kutoka kwa lugha zilizochaguliwa";
  const showVideoVolumeSlider$7 = "Display kitelezi cha sauti ya video";
  const hotkeysSettings$7 = "Mipangilio ya Hotkeys";
  const None$7 = "Hakuna";
  const VOTUseLivelyVoice$7 = "Tumia sauti za kupendeza. Wasemaji wanaonekana Kama Warusi wa asili.";
  const miscSettings$7 = "Mipangilio ya Misc";
  const services$7 = { "yandexbrowser": "Kivinjari Cha Yandex", "msedge": "Microsoft Edge", "rust-server": "Seva Ya Kutu" };
  const aboutExtension$7 = "Kuhusu ugani";
  const appearance$7 = "Kuonekana";
  const buttonPosition$7 = "Nafasi ya kifungo katika mchezaji";
  const position$7 = { "left": "Kushoto", "right": "Haki", "top": "Juu", "default": "Chaguo-msingi" };
  const secs$7 = "sekunde";
  const autoHideButtonDelay$7 = "Kuchelewa kabla ya kujificha kifungo cha kutafsiri";
  const notFound$7 = "haipatikani";
  const minButtonPositionContainer$7 = "Nafasi ya kitufe hubadilika tu kwa wachezaji wakubwa kuliko saizi 600.";
  const VOTTranslateProxyStatusDefault$7 = "Kuzima kabisa wakala katika nchi yako kunaweza kuvunja ugani";
  const PressTheKeyCombination$7 = "Bonyeza mchanganyiko muhimu...";
  const VOTUseAudioDownload$7 = "Tumia upakuaji wa sauti";
  const VOTUseAudioDownloadWarning$7 = "Kuzima upakuaji wa sauti kunaweza kuathiri utendakazi wa kiendelezi";
  const VOTAccountRequired$7 = "Unahitaji kuingia ili kutumia kipengele hiki";
  const VOTMyAccount$7 = "Akaunti yangu";
  const VOTLogin$7 = "Ingia";
  const VOTLogout$7 = "Kuingia";
  const VOTRefresh$7 = "Furahisha";
  const VOTYandexToken$7 = "Ingiza Ishara Ya Yandex OAuth";
  const VOTYandexTokenInfo$7 = "Unaweza kuweka ishara ya akaunti kwa mikono katika uwanja huu. Tafadhali kumbuka kuwa hatuangalii uhalali wake kabla ya kutuma ombi la kutafsiri";
  const VOTLoginViaToken$7 = "Ingia kupitia ishara";
  const subtitlesSmartLayout$7 = "Mpangilio wa manukuu mahiri";
  const smartDucking$7 = "Kiasi cha kubadilika";
  const VOTAutoSubtitles$7 = "Subtitles juu ya wazi";
  const VOTSubtitlesFont$7 = "Fonti ya kichwa kidogo";
  const __vite_glob_0_54 = {
    recommended: recommended$7,
    translateVideo: translateVideo$7,
    disableTranslate: disableTranslate$7,
    translationSettings: translationSettings$7,
    subtitlesSettings: subtitlesSettings$7,
    resetSettings: resetSettings$7,
    videoBeingTranslated: videoBeingTranslated$7,
    videoLanguage: videoLanguage$7,
    translationLanguage: translationLanguage$7,
    translationTake: translationTake$7,
    translationTakeMoreThanHour: translationTakeMoreThanHour$7,
    translationTakeAboutMinute: translationTakeAboutMinute$7,
    translationTakeFewMinutes: translationTakeFewMinutes$7,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$7,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$7,
    requestTranslationFailed: requestTranslationFailed$7,
    audioNotReceived: audioNotReceived$7,
    audioFormatNotSupported: audioFormatNotSupported$7,
    VOTAutoTranslate: VOTAutoTranslate$7,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$7,
    VOTVolume: VOTVolume$7,
    VOTVolumeTranslation: VOTVolumeTranslation$7,
    VOTAutoSetVolume: VOTAutoSetVolume$7,
    VOTShowVideoSlider: VOTShowVideoSlider$7,
    VOTSyncVolume: VOTSyncVolume$7,
    VOTDisableFromYourLang: VOTDisableFromYourLang$7,
    VOTVideoIsTooLong: VOTVideoIsTooLong$7,
    VOTNoVideoIDFound: VOTNoVideoIDFound$7,
    VOTSubtitles: VOTSubtitles$7,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$7,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$7,
    VOTHighlightWords: VOTHighlightWords$7,
    VOTTranslatedFrom: VOTTranslatedFrom$7,
    VOTAutogenerated: VOTAutogenerated$7,
    VOTSettings: VOTSettings$7,
    VOTMenuLanguage: VOTMenuLanguage$7,
    VOTAuthors: VOTAuthors$7,
    VOTVersion: VOTVersion$7,
    VOTLoader: VOTLoader$7,
    VOTBrowser: VOTBrowser$7,
    VOTShowPiPButton: VOTShowPiPButton$7,
    langs: langs$7,
    streamNoConnectionToServer: streamNoConnectionToServer$7,
    searchField: searchField$7,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$7,
    VOTDetectService: VOTDetectService$7,
    VOTProxyWorkerHost: VOTProxyWorkerHost$7,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$7,
    proxySettings: proxySettings$7,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$7,
    VOTAudioBooster: VOTAudioBooster$7,
    VOTSubtitlesDesign: VOTSubtitlesDesign$7,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$7,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$7,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$7,
    VOTDownloadWithName: VOTDownloadWithName$7,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$7,
    VOTLocaleHash: VOTLocaleHash$7,
    VOTUpdatedAt: VOTUpdatedAt$7,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$7,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$7,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$7,
    VOTNewAudioPlayer: VOTNewAudioPlayer$7,
    VOTUseNewModel: VOTUseNewModel$7,
    TranslationDelayed: TranslationDelayed$7,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$7,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$7,
    VOTBugReport: VOTBugReport$7,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$7,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$7,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$7,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$7,
    VOTTranslatedBy: VOTTranslatedBy$7,
    VOTStreamNotAvailable: VOTStreamNotAvailable$7,
    VOTTranslationTextService: VOTTranslationTextService$7,
    VOTNotAffectToVoice: VOTNotAffectToVoice$7,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$7,
    showVideoVolumeSlider: showVideoVolumeSlider$7,
    hotkeysSettings: hotkeysSettings$7,
    None: None$7,
    VOTUseLivelyVoice: VOTUseLivelyVoice$7,
    miscSettings: miscSettings$7,
    services: services$7,
    aboutExtension: aboutExtension$7,
    appearance: appearance$7,
    buttonPosition: buttonPosition$7,
    position: position$7,
    secs: secs$7,
    autoHideButtonDelay: autoHideButtonDelay$7,
    notFound: notFound$7,
    minButtonPositionContainer: minButtonPositionContainer$7,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$7,
    PressTheKeyCombination: PressTheKeyCombination$7,
    VOTUseAudioDownload: VOTUseAudioDownload$7,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$7,
    VOTAccountRequired: VOTAccountRequired$7,
    VOTMyAccount: VOTMyAccount$7,
    VOTLogin: VOTLogin$7,
    VOTLogout: VOTLogout$7,
    VOTRefresh: VOTRefresh$7,
    VOTYandexToken: VOTYandexToken$7,
    VOTYandexTokenInfo: VOTYandexTokenInfo$7,
    VOTLoginViaToken: VOTLoginViaToken$7,
    subtitlesSmartLayout: subtitlesSmartLayout$7,
    smartDucking: smartDucking$7,
    VOTAutoSubtitles: VOTAutoSubtitles$7,
    VOTSubtitlesFont: VOTSubtitlesFont$7
  };
  const recommended$6 = "önerilen";
  const translateVideo$6 = "Videoyu çevir";
  const disableTranslate$6 = "Kapat";
  const translationSettings$6 = "Çeviri ayarları";
  const subtitlesSettings$6 = "Altyazı ayarları";
  const resetSettings$6 = "Ayarları sıfırla";
  const videoBeingTranslated$6 = "Video çevrilir";
  const videoLanguage$6 = "Video dil";
  const translationLanguage$6 = "Çeviri dili";
  const translationTake$6 = "Çeviri şu kadar sürecek";
  const translationTakeMoreThanHour$6 = "Çeviri bir saatten daha uzun sürecek";
  const translationTakeAboutMinute$6 = "Çeviri yaklaşık bir dakika sürecek";
  const translationTakeFewMinutes$6 = "Çeviri birkaç dakika sürecek";
  const translationTakeApproximatelyMinutes$6 = "Çeviri yaklaşık {0} dakika sürecek";
  const translationTakeApproximatelyMinute$6 = "Çeviri yaklaşık {0} dakika sürecek";
  const requestTranslationFailed$6 = "Video çevirisi isteği başarısız oldu";
  const audioNotReceived$6 = "Ses bağlantısı alınamadı";
  const audioFormatNotSupported$6 = "Ses formatı desteklenmiyor";
  const VOTAutoTranslate$6 = "Açılışta çevir";
  const VOTDontTranslateYourLang$6 = "Ana dilinizden çevirme";
  const VOTVolume$6 = "Video hacmi:";
  const VOTVolumeTranslation$6 = "Çeviri hacmi:";
  const VOTAutoSetVolume$6 = "Video ses seviyesini azaltın";
  const VOTShowVideoSlider$6 = "Video ses kaydırıcısını göster";
  const VOTSyncVolume$6 = "Çeviri ve video seslerini eşitle";
  const VOTDisableFromYourLang$6 = "Video çevirisini ana dilinizden kapattınız";
  const VOTVideoIsTooLong$6 = "Video çok uzun";
  const VOTNoVideoIDFound$6 = "Video kimlik numarası bulunamadı";
  const VOTSubtitles$6 = "Altyazılar";
  const VOTSubtitlesDisabled$6 = "Kapalı";
  const VOTSubtitlesMaxLength$6 = "En fazla altyazı uzunluğu";
  const VOTHighlightWords$6 = "Kelime vurgulamayı etkinleştir";
  const VOTTranslatedFrom$6 = "Çevrilen";
  const VOTAutogenerated$6 = "Otomatik oluşturuldu";
  const VOTSettings$6 = "VOT Ayarları";
  const VOTMenuLanguage$6 = "Menü dili";
  const VOTAuthors$6 = "Yazarlar";
  const VOTVersion$6 = "Sürüm";
  const VOTLoader$6 = "Yükleyici";
  const VOTBrowser$6 = "Tarayıcı";
  const VOTShowPiPButton$6 = "PiP düğmesini göster";
  const langs$6 = { "auto": "Otomatik olarak", "az": "Azerice", "ay": "Aymara", "ak": "Akanca", "sq": "Arnavutça", "am": "Amharca", "en": "İngilizce", "ar": "Arapça", "hy": "Ermenice", "as": "Assamca", "af": "Afrikaanca", "eu": "Baskça", "be": "Beyaz Rusça", "bn": "Bengalce", "my": "Burmaca", "bg": "Bulgarca", "bs": "Boşnakça", "bho": "Bhojpuri", "cy": "Galce", "hu": "Macarca", "vi": "Vietnamca", "haw": "Hawaii dili", "ht": "Haiti Kreol dili", "gl": "Galicia dili", "lg": "Ganda dili", "el": "Yunanca", "ka": "Gürcüce", "gn": "Guaraní dili", "gu": "Gucerat dili", "gd": "İskoç Gaelcesi", "da": "Danca", "fy": "Batı Frizce", "zu": "Zuluca", "iw": "İbranice", "ig": "Igbo dili", "yi": "Yidiş", "id": "Endonezce", "ga": "İrlandaca", "is": "İzlandaca", "es": "İspanyolca", "it": "İtalyanca", "yo": "Yoruba", "kk": "Kazakça", "kn": "Kannada dili", "ca": "Katalanca", "qu": "Quechua dili", "rw": "Kinyarwanda", "ky": "Kırgızca", "zh": "Çince", "zh-Hant": "Geleneksel Çince", "zh-Hans": "Basitleştirilmiş Çince", "ko": "Korece", "co": "Korsikaca", "xh": "Kosa", "ku": "Kürtçe", "km": "Khmer", "lo": "Laoca", "la": "Latince", "lv": "Latince", "ln": "Lingala", "lt": "Litvanyaca", "lb": "Lüksemburgca", "mk": "Makedonca", "mg": "Malgaşça", "ms": "Malay dili", "ml": "Malayalam dili", "dv": "Divehi", "mt": "Maltaca", "mi": "Maori dili", "mr": "Marathi dili", "mn": "Moğolca", "de": "Almanca", "ne": "Nepalce", "nl": "Hollandaca", "no": "Norveççe", "ny": "Chichewa", "or": "Oriya dili", "om": "Oromo dili", "pa": "Pencap dili", "fa": "Farsça", "pl": "Lehçe", "pt": "Portekizce", "ps": "Peştuca", "ro": "Rumence", "ru": "Rusça", "sm": "Samoaca", "sa": "Sanskritçe", "ceb": "Cebuano", "nso": "Kuzey Sotho dili", "sr": "Sırpça", "si": "Sinhali", "sd": "Sindhice", "sk": "Slovakça", "sl": "Slovence", "so": "Somalice", "sw": "Svahili", "su": "Sundaca", "tg": "Tacikçe", "th": "Tay dili", "ta": "Tamilce", "tt": "Tatarca", "te": "Telugu dili", "ti": "Tigrinya dili", "ts": "Tsonga dili", "tr": "Türkçe", "tk": "Türkmence", "uz": "Özbekçe", "ug": "Uygurca", "uk": "Ukraynaca", "ur": "Urduca", "fil": "Filipince", "fi": "Fince", "fr": "Fransızca", "ha": "Hausa dili", "hi": "Hintçe", "hmn": "Hmong dili", "hr": "Hırvatça", "cs": "Çekçe", "sv": "İsveççe", "sn": "Shona dili", "ee": "Ewe dili", "eo": "Esperanto", "et": "Estonya dili", "st": "Güney Sotho dili", "jv": "Javanese", "ja": "Japonca", "kri": "Krio" };
  const streamNoConnectionToServer$6 = "Sunucuya bağlantı yok";
  const searchField$6 = "Aramak...";
  const VOTTranslateAPIErrors$6 = "API'DEN hataları çevir";
  const VOTDetectService$6 = "Dil algılama hizmeti";
  const VOTProxyWorkerHost$6 = "Vekil çalışan adresini girin";
  const VOTM3u8ProxyHost$6 = "M3u8 proxy çalışanının adresini girin";
  const proxySettings$6 = "Proxy Ayarları";
  const translationTakeApproximatelyMinute2$6 = "Çeviri yaklaşık {0} dakika sürecektir";
  const VOTAudioBooster$6 = "Genişletilmiş çeviri hacmi artışı";
  const VOTSubtitlesDesign$6 = "Altyazı tasarımı";
  const VOTSubtitlesFontSize$6 = "Altyazıların yazı tipi boyutu";
  const VOTSubtitlesOpacity$6 = "Altyazı arka planının şeffaflığı";
  const VOTSubtitlesDownloadFormat$6 = "Altyazı indirme biçimi";
  const VOTDownloadWithName$6 = "Video adına sahip dosyaları indirin";
  const VOTUpdateLocaleFiles$6 = "Yerelleştirme dosyalarını güncelle";
  const VOTLocaleHash$6 = "Yerel ayar karması";
  const VOTUpdatedAt$6 = "güncellendi";
  const VOTNeedWebAudioAPI$6 = "Bunu etkinleştirmek için bir Web Ses API'sine sahip olmanız gerekir";
  const VOTMediaCSPEnabledOnSite$6 = "Media CSP bu sitede etkindir";
  const VOTOnlyBypassMediaCSP$6 = "Yalnızca Medya csp'sini atlamak için kullanın";
  const VOTNewAudioPlayer$6 = "Yeni ses çaları kullanma";
  const VOTUseNewModel$6 = "Bazı videolar için Yandex seslerinin deneysel bir varyasyonunu kullanın";
  const TranslationDelayed$6 = "Çeviri biraz gecikti";
  const VOTTranslationCompletedNotify$6 = "{0}'daki çeviri tamamlandı!";
  const VOTSendNotifyOnComplete$6 = "Videonun çevrildiğine dair bir bildirim gönderin";
  const VOTBugReport$6 = "Hata bildir";
  const VOTTranslateProxyDisabled$6 = "Engelli";
  const VOTTranslateProxyEnabled$6 = "Etkin";
  const VOTTranslateProxyEverything$6 = "Proxy her şey";
  const VOTTranslateProxyStatus$6 = "Proxy modu";
  const VOTTranslatedBy$6 = "Çeviren: {0}";
  const VOTStreamNotAvailable$6 = "Çeviri akışı kullanılamıyor";
  const VOTTranslationTextService$6 = "Metin çeviri hizmeti";
  const VOTNotAffectToVoice$6 = "Sesli metnin çevirisini etkilemez";
  const DontTranslateSelectedLanguages$6 = "Seçilen dillerden çeviri yapmayın";
  const showVideoVolumeSlider$6 = "Video ses kaydırıcısını görüntüleme";
  const hotkeysSettings$6 = "Kısayol tuşları ayarları";
  const None$6 = "Hiçbiri";
  const VOTUseLivelyVoice$6 = "Canlı sesler kullanın. Konuşmacılar yerli Ruslar gibi geliyor.";
  const miscSettings$6 = "Çeşitli ayarlar";
  const services$6 = { "yandexbrowser": "Yandex Tarayıcı", "msedge": "Microsoft Edge'in", "rust-server": "Pas Sunucusu" };
  const aboutExtension$6 = "Uzantı hakkında";
  const appearance$6 = "Görünüm";
  const buttonPosition$6 = "Oynatıcıdaki düğme konumu";
  const position$6 = { "left": "Sol", "right": "Sağ", "top": "Üst", "default": "Varsayılan" };
  const secs$6 = "saniye";
  const autoHideButtonDelay$6 = "Çevir düğmesini gizlemeden önce gecikme";
  const notFound$6 = "bulunamadı";
  const minButtonPositionContainer$6 = "Düğmenin konumu yalnızca 600 pikselden büyük oynatıcılarda değişir.";
  const VOTTranslateProxyStatusDefault$6 = "Ülkenizdeki proxying'i tamamen devre dışı bırakmak uzantıyı bozabilir";
  const PressTheKeyCombination$6 = "Tuş kombinasyonuna basın...";
  const VOTUseAudioDownload$6 = "Ses indirmeyi kullan";
  const VOTUseAudioDownloadWarning$6 = "Ses indirmelerini devre dışı bırakmak uzantının işlevselliğini etkileyebilir";
  const VOTAccountRequired$6 = "Bu özelliği kullanmak için giriş yapmanız gerekir";
  const VOTMyAccount$6 = "Benim hesabım";
  const VOTLogin$6 = "Oturum aç";
  const VOTLogout$6 = "Çıkış";
  const VOTRefresh$6 = "Yenilemek";
  const VOTYandexToken$6 = "Yandex OAuth Belirtecini Girin";
  const VOTYandexTokenInfo$6 = "Bu alanda hesap belirtecini manuel olarak ayarlayabilirsiniz. Bir çeviri isteği göndermeden önce geçerliliğini kontrol etmediğimizi lütfen unutmayın";
  const VOTLoginViaToken$6 = "Token ile giriş yap";
  const subtitlesSmartLayout$6 = "Akıllı altyazı düzeni";
  const smartDucking$6 = "Uyarlanabilir hacim";
  const VOTAutoSubtitles$6 = "Altyazılar açık";
  const VOTSubtitlesFont$6 = "Altyazı yazı tipi";
  const __vite_glob_0_55 = {
    recommended: recommended$6,
    translateVideo: translateVideo$6,
    disableTranslate: disableTranslate$6,
    translationSettings: translationSettings$6,
    subtitlesSettings: subtitlesSettings$6,
    resetSettings: resetSettings$6,
    videoBeingTranslated: videoBeingTranslated$6,
    videoLanguage: videoLanguage$6,
    translationLanguage: translationLanguage$6,
    translationTake: translationTake$6,
    translationTakeMoreThanHour: translationTakeMoreThanHour$6,
    translationTakeAboutMinute: translationTakeAboutMinute$6,
    translationTakeFewMinutes: translationTakeFewMinutes$6,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$6,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$6,
    requestTranslationFailed: requestTranslationFailed$6,
    audioNotReceived: audioNotReceived$6,
    audioFormatNotSupported: audioFormatNotSupported$6,
    VOTAutoTranslate: VOTAutoTranslate$6,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$6,
    VOTVolume: VOTVolume$6,
    VOTVolumeTranslation: VOTVolumeTranslation$6,
    VOTAutoSetVolume: VOTAutoSetVolume$6,
    VOTShowVideoSlider: VOTShowVideoSlider$6,
    VOTSyncVolume: VOTSyncVolume$6,
    VOTDisableFromYourLang: VOTDisableFromYourLang$6,
    VOTVideoIsTooLong: VOTVideoIsTooLong$6,
    VOTNoVideoIDFound: VOTNoVideoIDFound$6,
    VOTSubtitles: VOTSubtitles$6,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$6,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$6,
    VOTHighlightWords: VOTHighlightWords$6,
    VOTTranslatedFrom: VOTTranslatedFrom$6,
    VOTAutogenerated: VOTAutogenerated$6,
    VOTSettings: VOTSettings$6,
    VOTMenuLanguage: VOTMenuLanguage$6,
    VOTAuthors: VOTAuthors$6,
    VOTVersion: VOTVersion$6,
    VOTLoader: VOTLoader$6,
    VOTBrowser: VOTBrowser$6,
    VOTShowPiPButton: VOTShowPiPButton$6,
    langs: langs$6,
    streamNoConnectionToServer: streamNoConnectionToServer$6,
    searchField: searchField$6,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$6,
    VOTDetectService: VOTDetectService$6,
    VOTProxyWorkerHost: VOTProxyWorkerHost$6,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$6,
    proxySettings: proxySettings$6,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$6,
    VOTAudioBooster: VOTAudioBooster$6,
    VOTSubtitlesDesign: VOTSubtitlesDesign$6,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$6,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$6,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$6,
    VOTDownloadWithName: VOTDownloadWithName$6,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$6,
    VOTLocaleHash: VOTLocaleHash$6,
    VOTUpdatedAt: VOTUpdatedAt$6,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$6,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$6,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$6,
    VOTNewAudioPlayer: VOTNewAudioPlayer$6,
    VOTUseNewModel: VOTUseNewModel$6,
    TranslationDelayed: TranslationDelayed$6,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$6,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$6,
    VOTBugReport: VOTBugReport$6,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$6,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$6,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$6,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$6,
    VOTTranslatedBy: VOTTranslatedBy$6,
    VOTStreamNotAvailable: VOTStreamNotAvailable$6,
    VOTTranslationTextService: VOTTranslationTextService$6,
    VOTNotAffectToVoice: VOTNotAffectToVoice$6,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$6,
    showVideoVolumeSlider: showVideoVolumeSlider$6,
    hotkeysSettings: hotkeysSettings$6,
    None: None$6,
    VOTUseLivelyVoice: VOTUseLivelyVoice$6,
    miscSettings: miscSettings$6,
    services: services$6,
    aboutExtension: aboutExtension$6,
    appearance: appearance$6,
    buttonPosition: buttonPosition$6,
    position: position$6,
    secs: secs$6,
    autoHideButtonDelay: autoHideButtonDelay$6,
    notFound: notFound$6,
    minButtonPositionContainer: minButtonPositionContainer$6,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$6,
    PressTheKeyCombination: PressTheKeyCombination$6,
    VOTUseAudioDownload: VOTUseAudioDownload$6,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$6,
    VOTAccountRequired: VOTAccountRequired$6,
    VOTMyAccount: VOTMyAccount$6,
    VOTLogin: VOTLogin$6,
    VOTLogout: VOTLogout$6,
    VOTRefresh: VOTRefresh$6,
    VOTYandexToken: VOTYandexToken$6,
    VOTYandexTokenInfo: VOTYandexTokenInfo$6,
    VOTLoginViaToken: VOTLoginViaToken$6,
    subtitlesSmartLayout: subtitlesSmartLayout$6,
    smartDucking: smartDucking$6,
    VOTAutoSubtitles: VOTAutoSubtitles$6,
    VOTSubtitlesFont: VOTSubtitlesFont$6
  };
  const recommended$5 = "рекомендований";
  const translateVideo$5 = "Перекласти відео";
  const disableTranslate$5 = "Вимкнути";
  const translationSettings$5 = "Налаштування перекладу";
  const subtitlesSettings$5 = "Налаштування субтитрів";
  const resetSettings$5 = "Скидання налаштувань";
  const videoBeingTranslated$5 = "Відео перекладається";
  const videoLanguage$5 = "Мова відео";
  const translationLanguage$5 = "Мова перекладу";
  const translationTake$5 = "Переклад займе";
  const translationTakeMoreThanHour$5 = "Переклад займе більше години";
  const translationTakeAboutMinute$5 = "Переклад займе близько хвилини";
  const translationTakeFewMinutes$5 = "Переклад займе кілька хвилин";
  const translationTakeApproximatelyMinutes$5 = "Переклад займе приблизно {0} хвилин";
  const translationTakeApproximatelyMinute$5 = "Переклад займе приблизно {0} хвилини";
  const requestTranslationFailed$5 = "Не вдалося запросити Переклад відео";
  const audioNotReceived$5 = "Аудіосвязь не отримана";
  const audioFormatNotSupported$5 = "Аудіоформат не підтримується";
  const VOTAutoTranslate$5 = "Перевести на відкритий";
  const VOTDontTranslateYourLang$5 = "Не перекладайте з моєї мови";
  const VOTVolume$5 = "Гучність відео:";
  const VOTVolumeTranslation$5 = "Гучність перекладу:";
  const VOTAutoSetVolume$5 = "Зменшіть гучність відео до";
  const VOTShowVideoSlider$5 = "Повзунок гучності відео";
  const VOTSyncVolume$5 = "Переклад посилань і обсяг відео";
  const VOTDisableFromYourLang$5 = "Ви вимкнули Переклад відео на вашу мову";
  const VOTVideoIsTooLong$5 = "Відео занадто довге";
  const VOTNoVideoIDFound$5 = "Ідентифікатор відео не знайдено";
  const VOTSubtitles$5 = "Субтитр";
  const VOTSubtitlesDisabled$5 = "Непрацездатний";
  const VOTSubtitlesMaxLength$5 = "Максимальна довжина субтитрів";
  const VOTHighlightWords$5 = "Виділіть слова";
  const VOTTranslatedFrom$5 = "перекладено з";
  const VOTAutogenerated$5 = "автоматично генерується";
  const VOTSettings$5 = "Налаштування VOT";
  const VOTMenuLanguage$5 = "Мова меню";
  const VOTAuthors$5 = "Автор";
  const VOTVersion$5 = "Версія";
  const VOTLoader$5 = "Навантажувач";
  const VOTBrowser$5 = "Браузер";
  const VOTShowPiPButton$5 = "Кнопка Показати пунктир";
  const langs$5 = { "auto": "Авто", "af": "Африкаанс", "ak": "Акан", "sq": "Албанський", "am": "Амхарський", "ar": "Арабська мова", "hy": "Вірменський", "as": "Ассамський", "ay": "Аймара", "az": "Азербайджанський", "bn": "Бангла", "eu": "Баскський", "be": "Білоруський", "bho": "Бходжпурі", "bs": "Боснійський", "bg": "Болгарський", "my": "Бірманський", "ca": "Каталонський", "ceb": "Себуано", "zh": "Китайський", "zh-Hans": "Китайська (спрощена)", "zh-Hant": "Китайська (традиційна)", "co": "Корсиканець", "hr": "Хорватський", "cs": "Чеський", "da": "Датський", "dv": "Дівехі", "nl": "Голландський", "en": "Англійський", "eo": "Есперанто", "et": "Естонський", "ee": "Вівця", "fil": "Філіппінець", "fi": "Фінський", "fr": "Французький", "gl": "Галісійський", "lg": "Ганда", "ka": "Грузинський", "de": "Німецький", "el": "Грецький", "gn": "Гуарані", "gu": "Гуджараті", "ht": "Гаїтянський креол", "ha": "Хауса", "haw": "Гавайський", "iw": "Єврей", "hi": "Хінді", "hmn": "Хмонг", "hu": "Угорський", "is": "Ісландський", "ig": "Ігбо", "id": "Індонезійський", "ga": "Ірландський", "it": "Італійський", "ja": "Японський", "jv": "Яванський", "kn": "Каннада", "kk": "Казах", "km": "Кхмерська", "rw": "Кіньяруанда", "ko": "Корейський", "kri": "Кріо", "ku": "Курдський", "ky": "Киргиз", "lo": "Лаоський", "la": "Латинський", "lv": "Латиський", "ln": "Лінгала", "lt": "Литовський", "lb": "Люксембурзький", "mk": "Македонський", "mg": "Малагасійський", "ms": "Малайський", "ml": "Малаялам", "mt": "Мальтійський", "mi": "Маорі", "mr": "Марати", "mn": "Монгольський", "ne": "Непальський", "nso": "Північний Сото", "no": "Норвезька мова", "ny": "Ньянджа", "or": "Одіа", "om": "Оромо", "ps": "Пушту", "fa": "Перський", "pl": "Польський", "pt": "Португальський", "pa": "Панджабі", "qu": "Кечуа", "ro": "Румунський", "ru": "Російський", "sm": "Самоанський", "sa": "Санскрит", "gd": "Шотландська гельська", "sr": "Сербський", "sn": "Шона", "sd": "Сіндхі", "si": "Сингальський", "sk": "Словак", "sl": "Словенський", "so": "Сомалійський", "st": "Південний Сото", "es": "Іспанський", "su": "Сунданський", "sw": "Суахілі", "sv": "Шведський", "tg": "Таджицький", "ta": "Тамільський", "tt": "Татарський", "te": "Телугу", "th": "Тайський", "ti": "Тигрінья", "ts": "Тсонга", "tr": "Турецький", "tk": "Туркменський", "uk": "Український", "ur": "Урду", "ug": "Уйгурський", "uz": "Узбецький", "vi": "В'єтнамський", "cy": "Валлійська", "fy": "Західно-фризька", "xh": "Коса", "yi": "Ідиш", "yo": "Йоруба", "zu": "Зулуський" };
  const streamNoConnectionToServer$5 = "Немає підключення до сервера";
  const searchField$5 = "Пошук...";
  const VOTTranslateAPIErrors$5 = "Переклад помилок з API";
  const VOTDetectService$5 = "Сервіс визначення мови";
  const VOTProxyWorkerHost$5 = "Введіть адресу worker проксі-сервера";
  const VOTM3u8ProxyHost$5 = "Введіть адресу m3u8 проксі-сервера";
  const proxySettings$5 = "Налаштування проксі-сервера";
  const translationTakeApproximatelyMinute2$5 = "Переклад займе приблизно {0} хвилину";
  const VOTAudioBooster$5 = "Збільшення обсягу розширеного перекладу";
  const VOTSubtitlesDesign$5 = "Дизайн субтитрів";
  const VOTSubtitlesFontSize$5 = "Розмір шрифту субтитрів";
  const VOTSubtitlesOpacity$5 = "Прозорість фону субтитрів";
  const VOTSubtitlesDownloadFormat$5 = "Формат для завантаження субтитрів";
  const VOTDownloadWithName$5 = "Завантажуйте файли з назвою відео";
  const VOTUpdateLocaleFiles$5 = "Оновити файли локалізації";
  const VOTLocaleHash$5 = "Хеш локалі";
  const VOTUpdatedAt$5 = "Оновлено о";
  const VOTNeedWebAudioAPI$5 = "Щоб увімкнути це, ви повинні мати Web Audio API";
  const VOTMediaCSPEnabledOnSite$5 = "На цьому сайті включено Media CSP";
  const VOTOnlyBypassMediaCSP$5 = "Використовуйте його лише для обходу Media CSP";
  const VOTNewAudioPlayer$5 = "Використовуйте новий аудіоплеєр";
  const VOTUseNewModel$5 = "Використовуйте експериментальний варіант Yandex voices для деяких відео";
  const TranslationDelayed$5 = "Переклад трохи затримується";
  const VOTTranslationCompletedNotify$5 = "Переклад на {0} завершено!";
  const VOTSendNotifyOnComplete$5 = "Надішліть повідомлення про те, що відео було перекладено";
  const VOTBugReport$5 = "Повідомити про помилку";
  const VOTTranslateProxyDisabled$5 = "Непрацездатний";
  const VOTTranslateProxyEnabled$5 = "Включений";
  const VOTTranslateProxyEverything$5 = "Проксіровать всьо";
  const VOTTranslateProxyStatus$5 = "Режим проксирования";
  const VOTTranslatedBy$5 = "Перекладено {0}";
  const VOTStreamNotAvailable$5 = "Трансляція перекладу недоступна";
  const VOTTranslationTextService$5 = "Сервіс перекладу текстів";
  const VOTNotAffectToVoice$5 = "Не впливає на переклад тексту при озвучуванні";
  const DontTranslateSelectedLanguages$5 = "Не перекладайте з вибраних мов";
  const showVideoVolumeSlider$5 = "Відобразіть повзунок гучності відео";
  const hotkeysSettings$5 = "Налаштування гарячих клавіш";
  const None$5 = "Ні";
  const VOTUseLivelyVoice$5 = "Використовуйте живі голоси. Оратори говорять як корінні росіяни.";
  const miscSettings$5 = "Різні налаштування";
  const services$5 = { "yandexbrowser": "Яндекс Браузер", "msedge": "Microsoft Edge", "rust-server": "Сервер Rust" };
  const aboutExtension$5 = "Про розширення";
  const appearance$5 = "Зовнішність";
  const buttonPosition$5 = "Положення кнопки в програвачі";
  const position$5 = { "left": "Зліва", "right": "Праворуч", "top": "Верхній", "default": "За замовчуванням" };
  const secs$5 = "січ";
  const autoHideButtonDelay$5 = "Затримайтеся, перш ніж приховувати кнопку перекладу";
  const notFound$5 = "не знайдено";
  const minButtonPositionContainer$5 = "Положення кнопки змінюється тільки на програвачах розміром більше 600 пікселів.";
  const VOTTranslateProxyStatusDefault$5 = "Повне вимкнення проксування у вашій країні може призвести до поломки розширення";
  const PressTheKeyCombination$5 = "Натисніть комбінацію клавіш...";
  const VOTUseAudioDownload$5 = "Використовуйте завантаження аудіо";
  const VOTUseAudioDownloadWarning$5 = "Вимкнення завантаження аудіо може вплинути на функціональність розширення";
  const VOTAccountRequired$5 = "Вам потрібно увійти, щоб використовувати цю функцію";
  const VOTMyAccount$5 = "Мій рахунок";
  const VOTLogin$5 = "Авторизувати";
  const VOTLogout$5 = "Вихід";
  const VOTRefresh$5 = "Оновити";
  const VOTYandexToken$5 = "Введіть OAuth-токен Яндекса";
  const VOTYandexTokenInfo$5 = "Ви можете вручну вказати маркер облікового запису в цьому полі. Будь ласка, зверніть увагу, що ми не перевіряємо його дійсність перед відправкою запиту на переклад";
  const VOTLoginViaToken$5 = "Увійдіть за допомогою токена";
  const subtitlesSmartLayout$5 = "Продуманий макет субтитрів";
  const smartDucking$5 = "Адаптивна гучність";
  const VOTAutoSubtitles$5 = "Субтитри при відкритті";
  const VOTSubtitlesFont$5 = "Шрифт субтитрів";
  const __vite_glob_0_56 = {
    recommended: recommended$5,
    translateVideo: translateVideo$5,
    disableTranslate: disableTranslate$5,
    translationSettings: translationSettings$5,
    subtitlesSettings: subtitlesSettings$5,
    resetSettings: resetSettings$5,
    videoBeingTranslated: videoBeingTranslated$5,
    videoLanguage: videoLanguage$5,
    translationLanguage: translationLanguage$5,
    translationTake: translationTake$5,
    translationTakeMoreThanHour: translationTakeMoreThanHour$5,
    translationTakeAboutMinute: translationTakeAboutMinute$5,
    translationTakeFewMinutes: translationTakeFewMinutes$5,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$5,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$5,
    requestTranslationFailed: requestTranslationFailed$5,
    audioNotReceived: audioNotReceived$5,
    audioFormatNotSupported: audioFormatNotSupported$5,
    VOTAutoTranslate: VOTAutoTranslate$5,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$5,
    VOTVolume: VOTVolume$5,
    VOTVolumeTranslation: VOTVolumeTranslation$5,
    VOTAutoSetVolume: VOTAutoSetVolume$5,
    VOTShowVideoSlider: VOTShowVideoSlider$5,
    VOTSyncVolume: VOTSyncVolume$5,
    VOTDisableFromYourLang: VOTDisableFromYourLang$5,
    VOTVideoIsTooLong: VOTVideoIsTooLong$5,
    VOTNoVideoIDFound: VOTNoVideoIDFound$5,
    VOTSubtitles: VOTSubtitles$5,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$5,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$5,
    VOTHighlightWords: VOTHighlightWords$5,
    VOTTranslatedFrom: VOTTranslatedFrom$5,
    VOTAutogenerated: VOTAutogenerated$5,
    VOTSettings: VOTSettings$5,
    VOTMenuLanguage: VOTMenuLanguage$5,
    VOTAuthors: VOTAuthors$5,
    VOTVersion: VOTVersion$5,
    VOTLoader: VOTLoader$5,
    VOTBrowser: VOTBrowser$5,
    VOTShowPiPButton: VOTShowPiPButton$5,
    langs: langs$5,
    streamNoConnectionToServer: streamNoConnectionToServer$5,
    searchField: searchField$5,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$5,
    VOTDetectService: VOTDetectService$5,
    VOTProxyWorkerHost: VOTProxyWorkerHost$5,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$5,
    proxySettings: proxySettings$5,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$5,
    VOTAudioBooster: VOTAudioBooster$5,
    VOTSubtitlesDesign: VOTSubtitlesDesign$5,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$5,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$5,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$5,
    VOTDownloadWithName: VOTDownloadWithName$5,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$5,
    VOTLocaleHash: VOTLocaleHash$5,
    VOTUpdatedAt: VOTUpdatedAt$5,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$5,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$5,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$5,
    VOTNewAudioPlayer: VOTNewAudioPlayer$5,
    VOTUseNewModel: VOTUseNewModel$5,
    TranslationDelayed: TranslationDelayed$5,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$5,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$5,
    VOTBugReport: VOTBugReport$5,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$5,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$5,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$5,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$5,
    VOTTranslatedBy: VOTTranslatedBy$5,
    VOTStreamNotAvailable: VOTStreamNotAvailable$5,
    VOTTranslationTextService: VOTTranslationTextService$5,
    VOTNotAffectToVoice: VOTNotAffectToVoice$5,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$5,
    showVideoVolumeSlider: showVideoVolumeSlider$5,
    hotkeysSettings: hotkeysSettings$5,
    None: None$5,
    VOTUseLivelyVoice: VOTUseLivelyVoice$5,
    miscSettings: miscSettings$5,
    services: services$5,
    aboutExtension: aboutExtension$5,
    appearance: appearance$5,
    buttonPosition: buttonPosition$5,
    position: position$5,
    secs: secs$5,
    autoHideButtonDelay: autoHideButtonDelay$5,
    notFound: notFound$5,
    minButtonPositionContainer: minButtonPositionContainer$5,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$5,
    PressTheKeyCombination: PressTheKeyCombination$5,
    VOTUseAudioDownload: VOTUseAudioDownload$5,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$5,
    VOTAccountRequired: VOTAccountRequired$5,
    VOTMyAccount: VOTMyAccount$5,
    VOTLogin: VOTLogin$5,
    VOTLogout: VOTLogout$5,
    VOTRefresh: VOTRefresh$5,
    VOTYandexToken: VOTYandexToken$5,
    VOTYandexTokenInfo: VOTYandexTokenInfo$5,
    VOTLoginViaToken: VOTLoginViaToken$5,
    subtitlesSmartLayout: subtitlesSmartLayout$5,
    smartDucking: smartDucking$5,
    VOTAutoSubtitles: VOTAutoSubtitles$5,
    VOTSubtitlesFont: VOTSubtitlesFont$5
  };
  const recommended$4 = "تجویز کیا";
  const translateVideo$4 = "ویڈیو ترجمہ کریں";
  const disableTranslate$4 = "برطرف کریں";
  const translationSettings$4 = "ترجمہ سیٹنگیں";
  const subtitlesSettings$4 = "سب ٹائٹلز کی ترتیبات";
  const resetSettings$4 = "ترتیب دوبارہ کریں";
  const videoBeingTranslated$4 = "ویڈیو ترجمہ کی جا رہی ہے";
  const videoLanguage$4 = "ویڈیو زبان";
  const translationLanguage$4 = "ترجمہ زبان";
  const translationTake$4 = "ترجمہ کرنا";
  const translationTakeMoreThanHour$4 = "ترجمہ کرنا ایک سے زیادہ گھنٹہ لگے گا";
  const translationTakeAboutMinute$4 = "ترجمہ لگ بھگ ایک منٹ لگے گا";
  const translationTakeFewMinutes$4 = "ترجمہ کرنے میں کچھ منٹ لگتے ہیں";
  const translationTakeApproximatelyMinutes$4 = "ترجمہ کرنا تقریباً {0} منٹ لیگا";
  const translationTakeApproximatelyMinute$4 = "ترجمہ تقریباً {0} منٹ لیگا";
  const requestTranslationFailed$4 = "ویڈیو ترجمہ کرنے میں ناکامی";
  const audioNotReceived$4 = "آڈیو کا لنک حاصل نہیں ہوا";
  const audioFormatNotSupported$4 = "آڈیو فارمیٹ کی حمایت نہیں کی جاتی";
  const VOTAutoTranslate$4 = "کھولنے پر ترجمہ کریں";
  const VOTDontTranslateYourLang$4 = "اپنی زبان سے ترجمہ نہ کریں";
  const VOTVolume$4 = "ویڈیو حجم:";
  const VOTVolumeTranslation$4 = "ترجمہ کا حجم:";
  const VOTAutoSetVolume$4 = "ویڈیو حجم کو کم کریں";
  const VOTShowVideoSlider$4 = "ویڈیو کے آواز سلائیڈر کو دکھائیں";
  const VOTSyncVolume$4 = "ترجمہ اور ویڈیو کی آواز کو ملاوٹ";
  const VOTDisableFromYourLang$4 = "آپ نے اپنی زبان میں ویڈیو ترجمہ کرنا بند کردیا ہے";
  const VOTVideoIsTooLong$4 = "ویڈیو بہت لمبی ہے";
  const VOTNoVideoIDFound$4 = "ویڈیو ID نہیں ملا";
  const VOTSubtitles$4 = "سب ٹائٹلز";
  const VOTSubtitlesDisabled$4 = "منسوخ کر دیے گئے ہیں";
  const VOTSubtitlesMaxLength$4 = "سب ٹائٹل کی زیادہ سے زیادہ لمبائی";
  const VOTHighlightWords$4 = "لفظوں کو روشن کریں";
  const VOTTranslatedFrom$4 = "سے ترجمہ";
  const VOTAutogenerated$4 = "خود کار بنایا گیا";
  const VOTSettings$4 = "VOT کی ترتیبات";
  const VOTMenuLanguage$4 = "مینو کی زبان";
  const VOTAuthors$4 = "مصنفین";
  const VOTVersion$4 = "ورژن";
  const VOTLoader$4 = "لوڈر";
  const VOTBrowser$4 = "براؤزر";
  const VOTShowPiPButton$4 = "PiP بٹن دکھائیں۔";
  const langs$4 = { "auto": "خود بخود", "az": "آذربائیجانی", "ay": "ایمارا", "ak": "اکان", "sq": "البانی", "am": "امہری", "en": "انگریزی", "ar": "عربی", "hy": "آرمینیائی", "as": "آسامی", "af": "افریقی", "eu": "باسک", "be": "بیلاروسی", "bn": "بنگالی", "my": "برمی", "bg": "بلغاریائی", "bs": "بوسنیائی", "bho": "بھوجپوری", "cy": "ویلزی", "hu": "ہنگریائی", "vi": "ویئتنامی", "haw": "ہوائی", "ht": "ہیتی", "gl": "گیلیشیائی", "lg": "گینڈا", "el": "یونانی", "ka": "جورجیائی", "gn": "گوارانی", "gu": "گجراتی", "gd": "سکاٹش گیلک", "da": "ڈنمارکی", "fy": "مغربی فریزین", "zu": "زولو", "iw": "عبرانی", "ig": "ایگبو", "yi": "یدش", "id": "انڈونیشیائی", "ga": "آئرش", "is": "آئس لینڈی", "es": "ہسپانوی", "it": "اطالوی", "yo": "یوروبا", "kk": "قزاقستانی", "kn": "کنڑ", "ca": "کیٹالان", "qu": "کیچوا", "rw": "کینیاروانڈا", "ky": "کرغیزی", "zh": "چینی", "zh-Hant": "چینی (عرفانی)", "zh-Hans": "چینی (آسان)", "ko": "کوریائی", "co": "کورسیکن", "xh": "کوسا", "ku": "کردی", "km": "خمیر", "lo": "لاوس", "la": "لاطینی", "lv": "لیٹوینی", "ln": "لنگالا", "lt": "لتھوانیائی", "lb": "لکسمبرگ", "mk": "مقدونیائی", "mg": "ملاگاسی", "ms": "ملائی", "ml": "ملیالم", "dv": "مالدیپ", "mt": "مالٹی", "mi": "مائوری", "mr": "مراٹھی", "mn": "منگولیائی", "de": "جرمن", "ne": "نیپالی", "nl": "ڈچ", "no": "ناروے", "ny": "نیانجا", "or": "وریا", "om": "آرومو", "pa": "پنجابی", "fa": "فارسی", "pl": "پولش", "pt": "پُرتگالی", "ps": "پشتو", "ro": "رومانیائی", "ru": "روسی", "sm": "ساموآن", "sa": "سنسکرت", "ceb": "سیبوانو", "nso": "شمالی سوٹو", "sr": "سربیائی", "si": "سنہالی", "sd": "سندھی", "sk": "سلوواکی", "sl": "سلووینیائی", "so": "صومالی", "sw": "سواحلی", "su": "سنڈانیز", "tg": "تاجکستانی", "th": "تھائی", "ta": "تمل", "tt": "تاتار", "te": "تیلگو", "ti": "ٹگرینیا", "ts": "ٹسونگا", "tr": "ترکی", "tk": "ترکمانی", "uz": "ازبک", "ug": "آئیغور", "uk": "یوکرینی", "ur": "اردو", "fil": "فلپائنی", "fi": "فنیش", "fr": "فرانسیسی", "ha": "ہؤسا", "hi": "ہندی", "hmn": "ہمانگ", "hr": "کروئیشین", "cs": "چیک", "sv": "سویڈش", "sn": "شونا", "ee": "ایو", "eo": "ایسپرانٹو", "et": "ایسٹونین", "st": "جنوبی سوٹو", "jv": "جاوی", "ja": "جاپانی", "kri": "کری" };
  const streamNoConnectionToServer$4 = "سرور سے کوئی تعلق نہیں ہے";
  const searchField$4 = "تلاش...";
  const VOTTranslateAPIErrors$4 = "API سے غلطیوں کا ترجمہ کریں";
  const VOTDetectService$4 = "زبان کا پتہ لگانے کی خدمت";
  const VOTProxyWorkerHost$4 = "پراکسی ورکر ایڈریس درج کریں ۔ ";
  const VOTM3u8ProxyHost$4 = "M3u8 پراکسی ورکر کا پتہ درج کریں ۔ ";
  const proxySettings$4 = "پراکسی کی ترتیبات";
  const translationTakeApproximatelyMinute2$4 = "ترجمہ میں تقریبا {0} منٹ لگیں گے";
  const VOTAudioBooster$4 = "توسیع شدہ ترجمہ حجم میں اضافہ";
  const VOTSubtitlesDesign$4 = "سب ٹائٹلز ڈیزائن";
  const VOTSubtitlesFontSize$4 = "سب ٹائٹلز کا فونٹ سائز";
  const VOTSubtitlesOpacity$4 = "ذیلی عنوان کے پس منظر کی شفافیت";
  const VOTSubtitlesDownloadFormat$4 = "سب ٹائٹلز ڈاؤن لوڈ کرنے کا فارمیٹ";
  const VOTDownloadWithName$4 = "ویڈیو نام کے ساتھ فائلیں ڈاؤن لوڈ کریں";
  const VOTUpdateLocaleFiles$4 = "لوکلائزیشن فائلوں کو اپ ڈیٹ کریں";
  const VOTLocaleHash$4 = "مقامی ہیش";
  const VOTUpdatedAt$4 = "پر اپ ڈیٹ";
  const VOTNeedWebAudioAPI$4 = "اس کو فعال کرنے کے ل you ، آپ کے پاس ویب آڈیو API ہونا ضروری ہے";
  const VOTMediaCSPEnabledOnSite$4 = "اس سائٹ پر میڈیا سی ایس پی فعال ہے";
  const VOTOnlyBypassMediaCSP$4 = "اسے صرف میڈیا CSP کو نظرانداز کرنے کے لیے استعمال کریں ۔ ";
  const VOTNewAudioPlayer$4 = "نیا آڈیو پلیئر استعمال کریں";
  const VOTUseNewModel$4 = "کچھ ویڈیوز کے لئے یاندیکس آوازوں کی تجرباتی تغیر کا استعمال کریں ۔ ";
  const TranslationDelayed$4 = "ترجمہ میں قدرے تاخیر ہوئی ہے ۔ ";
  const VOTTranslationCompletedNotify$4 = "{0} پر ترجمہ مکمل ہو گیا ہے!";
  const VOTSendNotifyOnComplete$4 = "ایک نوٹیفکیشن بھیجیں کہ ویڈیو کا ترجمہ ہوچکا ہے";
  const VOTBugReport$4 = "ایک بگ کی اطلاع دیں";
  const VOTTranslateProxyDisabled$4 = "معذور";
  const VOTTranslateProxyEnabled$4 = "فعال";
  const VOTTranslateProxyEverything$4 = "پراکسی سب کچھ";
  const VOTTranslateProxyStatus$4 = "پراکسی موڈ";
  const VOTTranslatedBy$4 = "ترجمہ: {0}";
  const VOTStreamNotAvailable$4 = "ترجمہ سٹریم دستیاب نہیں ہے";
  const VOTTranslationTextService$4 = "ٹیکسٹ ٹرانسلیشن سروس";
  const VOTNotAffectToVoice$4 = "آواز میں متن کے ترجمہ کو متاثر نہیں کرتا";
  const DontTranslateSelectedLanguages$4 = "منتخب زبانوں سے ترجمہ نہ کریں";
  const showVideoVolumeSlider$4 = "ویڈیو حجم سلائیڈر دکھائیں";
  const hotkeysSettings$4 = "ہاٹکیز کی ترتیبات";
  const None$4 = "کوئی نہیں";
  const VOTUseLivelyVoice$4 = "زندہ آوازیں استعمال کریں ۔  بولنے والے مقامی روسیوں کی طرح آواز دیتے ہیں ۔ ";
  const miscSettings$4 = "متفرق ترتیبات";
  const services$4 = { "yandexbrowser": "Yandex براؤزر", "msedge": "مائیکروسافٹ ایج", "rust-server": "زنگ سرور" };
  const aboutExtension$4 = "توسیع کے بارے میں";
  const appearance$4 = "ظاہری شکل";
  const buttonPosition$4 = "کھلاڑی میں بٹن کی پوزیشن";
  const position$4 = { "left": "بائیں", "right": "ٹھیک ہے", "top": "اوپر", "default": "ڈیفالٹ" };
  const secs$4 = "ایس ای سی ایس";
  const autoHideButtonDelay$4 = "ترجمہ کے بٹن کو چھپانے سے پہلے تاخیر";
  const notFound$4 = "نہیں ملا";
  const minButtonPositionContainer$4 = "بٹن کی پوزیشن صرف 600 پکسلز سے بڑے کھلاڑیوں میں تبدیل ہوتی ہے ۔ ";
  const VOTTranslateProxyStatusDefault$4 = "آپ کے ملک میں پراکسی کو مکمل طور پر غیر فعال کرنا توسیع کو توڑ سکتا ہے ۔ ";
  const PressTheKeyCombination$4 = "کلیدی امتزاج دبائیں۔..";
  const VOTUseAudioDownload$4 = "آڈیو ڈاؤن لوڈ کا استعمال کریں";
  const VOTUseAudioDownloadWarning$4 = "آڈیو ڈاؤن لوڈ کو غیر فعال کرنے سے توسیع کی فعالیت متاثر ہوسکتی ہے ۔ ";
  const VOTAccountRequired$4 = "اس خصوصیت کو استعمال کرنے کے لیے آپ کو لاگ ان کرنے کی ضرورت ہے ۔ ";
  const VOTMyAccount$4 = "میرا اکاؤنٹ";
  const VOTLogin$4 = "لاگ ان";
  const VOTLogout$4 = "لاگ آؤٹ";
  const VOTRefresh$4 = "ریفریش";
  const VOTYandexToken$4 = "Yandex OAuth ٹوکن درج کریں";
  const VOTYandexTokenInfo$4 = "آپ دستی طور پر اس فیلڈ میں اکاؤنٹ ٹوکن سیٹ کر سکتے ہیں ۔  براہ کرم نوٹ کریں کہ ہم ترجمہ کی درخواست بھیجنے سے پہلے اس کی صداقت کی جانچ نہیں کرتے ہیں ۔ ";
  const VOTLoginViaToken$4 = "ٹوکن کے ذریعے لاگ ان کریں";
  const subtitlesSmartLayout$4 = "اسمارٹ سب ٹائٹل لے آؤٹ";
  const smartDucking$4 = "انکولی حجم";
  const VOTAutoSubtitles$4 = "اوپن پر سب ٹائٹلز";
  const VOTSubtitlesFont$4 = "ذیلی عنوان فونٹ";
  const __vite_glob_0_57 = {
    recommended: recommended$4,
    translateVideo: translateVideo$4,
    disableTranslate: disableTranslate$4,
    translationSettings: translationSettings$4,
    subtitlesSettings: subtitlesSettings$4,
    resetSettings: resetSettings$4,
    videoBeingTranslated: videoBeingTranslated$4,
    videoLanguage: videoLanguage$4,
    translationLanguage: translationLanguage$4,
    translationTake: translationTake$4,
    translationTakeMoreThanHour: translationTakeMoreThanHour$4,
    translationTakeAboutMinute: translationTakeAboutMinute$4,
    translationTakeFewMinutes: translationTakeFewMinutes$4,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$4,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$4,
    requestTranslationFailed: requestTranslationFailed$4,
    audioNotReceived: audioNotReceived$4,
    audioFormatNotSupported: audioFormatNotSupported$4,
    VOTAutoTranslate: VOTAutoTranslate$4,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$4,
    VOTVolume: VOTVolume$4,
    VOTVolumeTranslation: VOTVolumeTranslation$4,
    VOTAutoSetVolume: VOTAutoSetVolume$4,
    VOTShowVideoSlider: VOTShowVideoSlider$4,
    VOTSyncVolume: VOTSyncVolume$4,
    VOTDisableFromYourLang: VOTDisableFromYourLang$4,
    VOTVideoIsTooLong: VOTVideoIsTooLong$4,
    VOTNoVideoIDFound: VOTNoVideoIDFound$4,
    VOTSubtitles: VOTSubtitles$4,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$4,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$4,
    VOTHighlightWords: VOTHighlightWords$4,
    VOTTranslatedFrom: VOTTranslatedFrom$4,
    VOTAutogenerated: VOTAutogenerated$4,
    VOTSettings: VOTSettings$4,
    VOTMenuLanguage: VOTMenuLanguage$4,
    VOTAuthors: VOTAuthors$4,
    VOTVersion: VOTVersion$4,
    VOTLoader: VOTLoader$4,
    VOTBrowser: VOTBrowser$4,
    VOTShowPiPButton: VOTShowPiPButton$4,
    langs: langs$4,
    streamNoConnectionToServer: streamNoConnectionToServer$4,
    searchField: searchField$4,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$4,
    VOTDetectService: VOTDetectService$4,
    VOTProxyWorkerHost: VOTProxyWorkerHost$4,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$4,
    proxySettings: proxySettings$4,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$4,
    VOTAudioBooster: VOTAudioBooster$4,
    VOTSubtitlesDesign: VOTSubtitlesDesign$4,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$4,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$4,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$4,
    VOTDownloadWithName: VOTDownloadWithName$4,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$4,
    VOTLocaleHash: VOTLocaleHash$4,
    VOTUpdatedAt: VOTUpdatedAt$4,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$4,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$4,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$4,
    VOTNewAudioPlayer: VOTNewAudioPlayer$4,
    VOTUseNewModel: VOTUseNewModel$4,
    TranslationDelayed: TranslationDelayed$4,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$4,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$4,
    VOTBugReport: VOTBugReport$4,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$4,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$4,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$4,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$4,
    VOTTranslatedBy: VOTTranslatedBy$4,
    VOTStreamNotAvailable: VOTStreamNotAvailable$4,
    VOTTranslationTextService: VOTTranslationTextService$4,
    VOTNotAffectToVoice: VOTNotAffectToVoice$4,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$4,
    showVideoVolumeSlider: showVideoVolumeSlider$4,
    hotkeysSettings: hotkeysSettings$4,
    None: None$4,
    VOTUseLivelyVoice: VOTUseLivelyVoice$4,
    miscSettings: miscSettings$4,
    services: services$4,
    aboutExtension: aboutExtension$4,
    appearance: appearance$4,
    buttonPosition: buttonPosition$4,
    position: position$4,
    secs: secs$4,
    autoHideButtonDelay: autoHideButtonDelay$4,
    notFound: notFound$4,
    minButtonPositionContainer: minButtonPositionContainer$4,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$4,
    PressTheKeyCombination: PressTheKeyCombination$4,
    VOTUseAudioDownload: VOTUseAudioDownload$4,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$4,
    VOTAccountRequired: VOTAccountRequired$4,
    VOTMyAccount: VOTMyAccount$4,
    VOTLogin: VOTLogin$4,
    VOTLogout: VOTLogout$4,
    VOTRefresh: VOTRefresh$4,
    VOTYandexToken: VOTYandexToken$4,
    VOTYandexTokenInfo: VOTYandexTokenInfo$4,
    VOTLoginViaToken: VOTLoginViaToken$4,
    subtitlesSmartLayout: subtitlesSmartLayout$4,
    smartDucking: smartDucking$4,
    VOTAutoSubtitles: VOTAutoSubtitles$4,
    VOTSubtitlesFont: VOTSubtitlesFont$4
  };
  const recommended$3 = "tavsiya";
  const translateVideo$3 = "Videoni tarjima qiling";
  const disableTranslate$3 = "O'chirish";
  const translationSettings$3 = "Tarjima sozlamalari";
  const subtitlesSettings$3 = "Subtitrlar sozlamalari";
  const resetSettings$3 = "Sozlamalarni tiklash";
  const videoBeingTranslated$3 = "Video tarjima qilinmoqda";
  const videoLanguage$3 = "Video tili";
  const translationLanguage$3 = "Tarjima tili";
  const translationTake$3 = "Tarjima oladi";
  const translationTakeMoreThanHour$3 = "Tarjima bir soatdan ko'proq vaqtni oladi";
  const translationTakeAboutMinute$3 = "Tarjima taxminan bir daqiqa davom etadi";
  const translationTakeFewMinutes$3 = "Tarjima bir necha daqiqa davom etadi";
  const translationTakeApproximatelyMinutes$3 = "Tarjima taxminan {0} daqiqa davom etadi";
  const translationTakeApproximatelyMinute$3 = "Tarjima taxminan {0} daqiqa davom etadi";
  const requestTranslationFailed$3 = "Video tarjimasini talab qilib bo'lmadi";
  const audioNotReceived$3 = "Audio havola qabul qilinmadi";
  const audioFormatNotSupported$3 = "Audio format mavjud emas";
  const VOTAutoTranslate$3 = "Ochiq tarjima qiling";
  const VOTDontTranslateYourLang$3 = "Mening tilimdan tarjima qilmang";
  const VOTVolume$3 = "Video hajmi:";
  const VOTVolumeTranslation$3 = "Tarjima hajmi:";
  const VOTAutoSetVolume$3 = "Video hajmini kamaytiring";
  const VOTShowVideoSlider$3 = "Video hajmi slayder";
  const VOTSyncVolume$3 = "Tarjima va video hajmini bog'lang";
  const VOTDisableFromYourLang$3 = "Siz videoning o'z tilingizga tarjimasini o'chirib qo'ydingiz";
  const VOTVideoIsTooLong$3 = "Video juda uzun";
  const VOTNoVideoIDFound$3 = "Video identifikatori topilmadi";
  const VOTSubtitles$3 = "Taglavhalar";
  const VOTSubtitlesDisabled$3 = "Nogiron";
  const VOTSubtitlesMaxLength$3 = "Subtitrlar maksimal uzunlik";
  const VOTHighlightWords$3 = "So'zlarni ajratib ko'rsatish";
  const VOTTranslatedFrom$3 = "tarjima qilingan";
  const VOTAutogenerated$3 = "avtogeneratsiyalangan";
  const VOTSettings$3 = "Vot sozlamalari";
  const VOTMenuLanguage$3 = "Menyu tili";
  const VOTAuthors$3 = "Mualliflar";
  const VOTVersion$3 = "Versiya";
  const VOTLoader$3 = "Yuklagich";
  const VOTBrowser$3 = "Brauzer";
  const VOTShowPiPButton$3 = "PiP tugmasini ko'rsatish";
  const langs$3 = { "auto": "Avto", "af": "Afrikaliklar", "ak": "Akanname", "sq": "Albanchaname", "am": "Amharcha", "ar": "Arabcha", "hy": "Armancha", "as": "Assamcha", "ay": "Aymara", "az": "Ozarbayjon", "bn": "Bangla", "eu": "Baskcha", "be": "Beloruschaname", "bho": "Bhojpuri", "bs": "Bosniyaname", "bg": "Bolgarchaname", "my": "Birma", "ca": "Katalancha", "ceb": "Sebuano", "zh": "Xitoy", "zh-Hans": "Xitoycha (Soddalashtirilgan)", "zh-Hant": "Xitoy (An'anaviy)", "co": "Korsikaname", "hr": "Xorvatcha", "cs": "Chexiya", "da": "Daniyacha", "dv": "Divehi", "nl": "Gollandcha", "en": "Inglizcha", "eo": "Esperanto", "et": "Estoncha", "ee": "Qo'y", "fil": "Filipiname", "fi": "Finlyandiya", "fr": "Fransuzcha", "gl": "Galichaname", "lg": "Ganda", "ka": "Gruzin", "de": "Nemis", "el": "Yunoncha", "gn": "Guarani", "gu": "Gujarati", "ht": "Gaiti Kreoli", "ha": "Hausa", "haw": "Gavayi", "iw": "Ibroniycha", "hi": "Hindcha", "hmn": "Hmong", "hu": "Vengriya", "is": "Islandcha", "ig": "Igbo", "id": "Indoneziyaname", "ga": "Irlandcha", "it": "Italiya", "ja": "Yaponcha", "jv": "Yava", "kn": "Kannada", "kk": "Qozoq", "km": "Khmer", "rw": "Kinyarvanda", "ko": "Koreyscha", "kri": "Krio", "ku": "Kurdcha", "ky": "Qirg'iz", "lo": "Laosname", "la": "Lotin", "lv": "Latviya", "ln": "Lingala", "lt": "Litva", "lb": "Lyuksemburg", "mk": "Makedoncha", "mg": "Malagasiya", "ms": "Malay", "ml": "Malayalamcha", "mt": "Malta", "mi": "Māori", "mr": "Marathi", "mn": "Mo'g'ulcha", "ne": "Nepalcha", "nso": "Shimoliy Sotho", "no": "Norvegiya", "ny": "Nyanja", "or": "Odia", "om": "Oromo", "ps": "Pashto", "fa": "Forscha", "pl": "Polsha", "pt": "Portugalcha", "pa": "Panjobcha", "qu": "Kechua", "ro": "Rumincha", "ru": "Ruscha", "sm": "Samoa", "sa": "Sanskritcha", "gd": "Shotlandiya Gaelic", "sr": "Serbchaname", "sn": "Shona", "sd": "Sindhi", "si": "Sinhala", "sk": "Slovakcha", "sl": "Slovencha", "so": "Somali", "st": "Janubiy Sotho", "es": "Ispancha", "su": "Sundancha", "sw": "Suaxili", "sv": "Shvedcha", "tg": "Tojik", "ta": "Tamilcha", "tt": "Tatarcha", "te": "Telugu", "th": "Taylandcha", "ti": "Tigrinya", "ts": "Tsonga", "tr": "Turkcha", "tk": "Turkmancha", "uk": "Ukraina", "ur": "Urdu", "ug": "Uyg'urcha", "uz": "O'zbek", "vi": "Vetnamcha", "cy": "Uels", "fy": "G'arbiy Friz", "xh": "Xosa", "yi": "Yiddishcha", "yo": "Yoruba", "zu": "Zulu" };
  const streamNoConnectionToServer$3 = "Serverga ulanish yo'q";
  const searchField$3 = "Qidirish...";
  const VOTTranslateAPIErrors$3 = "API-dan xatolarni tarjima qiling";
  const VOTDetectService$3 = "Tilni aniqlash xizmati";
  const VOTProxyWorkerHost$3 = "Proksi-server manzilini kiriting";
  const VOTM3u8ProxyHost$3 = "M3u8 proksi-serverining manzilini kiriting";
  const proxySettings$3 = "Proksi Moslamalari";
  const translationTakeApproximatelyMinute2$3 = "Tarjima taxminan {0} daqiqa davom etadi";
  const VOTAudioBooster$3 = "Kengaytirilgan tarjima hajmini oshirish";
  const VOTSubtitlesDesign$3 = "Subtitrlar dizayni";
  const VOTSubtitlesFontSize$3 = "Subtitrlarning shrift o'lchami";
  const VOTSubtitlesOpacity$3 = "Subtitr fonining shaffofligi";
  const VOTSubtitlesDownloadFormat$3 = "Subtitrlarni yuklab olish formati";
  const VOTDownloadWithName$3 = "Video nomi bilan fayllarni yuklab oling";
  const VOTUpdateLocaleFiles$3 = "Mahalliylashtirish fayllarini yangilang";
  const VOTLocaleHash$3 = "Mahalliy Xash";
  const VOTUpdatedAt$3 = "da yangilangan";
  const VOTNeedWebAudioAPI$3 = "Buni yoqish uchun sizda veb-Audio API bo'lishi kerak";
  const VOTMediaCSPEnabledOnSite$3 = "Media CSP ushbu saytda yoqilgan";
  const VOTOnlyBypassMediaCSP$3 = "Uni faqat Media CSP-ni chetlab o'tish uchun foydalaning";
  const VOTNewAudioPlayer$3 = "Yangi audio pleerdan foydalaning";
  const VOTUseNewModel$3 = "Ba'zi videolar uchun Yandex ovozlarining eksperimental variantidan foydalaning";
  const TranslationDelayed$3 = "Tarjima biroz kechiktirildi";
  const VOTTranslationCompletedNotify$3 = "{0} dagi tarjima tugallandi!";
  const VOTSendNotifyOnComplete$3 = "Video tarjima qilinganligi to'g'risida xabar yuboring";
  const VOTBugReport$3 = "Xato haqida xabar bering";
  const VOTTranslateProxyDisabled$3 = "Nogiron";
  const VOTTranslateProxyEnabled$3 = "Yoqilgan";
  const VOTTranslateProxyEverything$3 = "Proksi hamma narsa";
  const VOTTranslateProxyStatus$3 = "Proksi rejimi";
  const VOTTranslatedBy$3 = "{0}tomonidan tarjima qilingan";
  const VOTStreamNotAvailable$3 = "Translate stream mavjud emas";
  const VOTTranslationTextService$3 = "Matnni tarjima qilish xizmati";
  const VOTNotAffectToVoice$3 = "Matnning ovozli tarjimasiga ta'sir qilmaydi";
  const DontTranslateSelectedLanguages$3 = "Tanlangan tillardan tarjima qilmang";
  const showVideoVolumeSlider$3 = "Video hajmi slayderini ko'rsatish";
  const hotkeysSettings$3 = "Hotkeys sozlamalari";
  const None$3 = "Yo'q";
  const VOTUseLivelyVoice$3 = "Jonli ovozlardan foydalaning. Ma'ruzachilar ona ruslar kabi ovoz.";
  const miscSettings$3 = "Misc sozlamalari";
  const services$3 = { "yandexbrowser": "Yandex Brauzeri", "msedge": "Microsoft Edge", "rust-server": "Zang Serveri" };
  const aboutExtension$3 = "Kengaytma haqida";
  const appearance$3 = "Ko'rinishi";
  const buttonPosition$3 = "Pleyerdagi tugma holati";
  const position$3 = { "left": "Chapga", "right": "O'ng", "top": "Yuqori", "default": "Andoza" };
  const secs$3 = "sek";
  const autoHideButtonDelay$3 = "Tarjima tugmachasini yashirishdan oldin kechikish";
  const notFound$3 = "topilmadi";
  const minButtonPositionContainer$3 = "Tugma holati faqat 600 pikseldan katta o'yinchilarda o'zgaradi.";
  const VOTTranslateProxyStatusDefault$3 = "Mamlakatingizda proksi-serverni butunlay o'chirib qo'yish kengaytmani buzishi mumkin";
  const PressTheKeyCombination$3 = "Tugmalar birikmasini bosing...";
  const VOTUseAudioDownload$3 = "Audio yuklab olishdan foydalaning";
  const VOTUseAudioDownloadWarning$3 = "Ovozni yuklab olishni o'chirib qo'yish kengaytmaning ishlashiga ta'sir qilishi mumkin";
  const VOTAccountRequired$3 = "Ushbu xususiyatdan foydalanish uchun tizimga kirishingiz kerak";
  const VOTMyAccount$3 = "Mening hisobim";
  const VOTLogin$3 = "Kirish";
  const VOTLogout$3 = "Chiqish";
  const VOTRefresh$3 = "Yangilash";
  const VOTYandexToken$3 = "Yandex OAuth Tokenini kiriting";
  const VOTYandexTokenInfo$3 = "Ushbu sohada hisob belgisini qo'lda o'rnatishingiz mumkin. Iltimos, tarjima so'rovini yuborishdan oldin uning haqiqiyligini tekshirmaymiz";
  const VOTLoginViaToken$3 = "Token orqali kirish";
  const subtitlesSmartLayout$3 = "Aqlli subtitr tartibi";
  const smartDucking$3 = "Moslashuvchan hajm";
  const VOTAutoSubtitles$3 = "Ochiq subtitrlar";
  const VOTSubtitlesFont$3 = "Subtitr shrifti";
  const __vite_glob_0_58 = {
    recommended: recommended$3,
    translateVideo: translateVideo$3,
    disableTranslate: disableTranslate$3,
    translationSettings: translationSettings$3,
    subtitlesSettings: subtitlesSettings$3,
    resetSettings: resetSettings$3,
    videoBeingTranslated: videoBeingTranslated$3,
    videoLanguage: videoLanguage$3,
    translationLanguage: translationLanguage$3,
    translationTake: translationTake$3,
    translationTakeMoreThanHour: translationTakeMoreThanHour$3,
    translationTakeAboutMinute: translationTakeAboutMinute$3,
    translationTakeFewMinutes: translationTakeFewMinutes$3,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$3,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$3,
    requestTranslationFailed: requestTranslationFailed$3,
    audioNotReceived: audioNotReceived$3,
    audioFormatNotSupported: audioFormatNotSupported$3,
    VOTAutoTranslate: VOTAutoTranslate$3,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$3,
    VOTVolume: VOTVolume$3,
    VOTVolumeTranslation: VOTVolumeTranslation$3,
    VOTAutoSetVolume: VOTAutoSetVolume$3,
    VOTShowVideoSlider: VOTShowVideoSlider$3,
    VOTSyncVolume: VOTSyncVolume$3,
    VOTDisableFromYourLang: VOTDisableFromYourLang$3,
    VOTVideoIsTooLong: VOTVideoIsTooLong$3,
    VOTNoVideoIDFound: VOTNoVideoIDFound$3,
    VOTSubtitles: VOTSubtitles$3,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$3,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$3,
    VOTHighlightWords: VOTHighlightWords$3,
    VOTTranslatedFrom: VOTTranslatedFrom$3,
    VOTAutogenerated: VOTAutogenerated$3,
    VOTSettings: VOTSettings$3,
    VOTMenuLanguage: VOTMenuLanguage$3,
    VOTAuthors: VOTAuthors$3,
    VOTVersion: VOTVersion$3,
    VOTLoader: VOTLoader$3,
    VOTBrowser: VOTBrowser$3,
    VOTShowPiPButton: VOTShowPiPButton$3,
    langs: langs$3,
    streamNoConnectionToServer: streamNoConnectionToServer$3,
    searchField: searchField$3,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$3,
    VOTDetectService: VOTDetectService$3,
    VOTProxyWorkerHost: VOTProxyWorkerHost$3,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$3,
    proxySettings: proxySettings$3,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$3,
    VOTAudioBooster: VOTAudioBooster$3,
    VOTSubtitlesDesign: VOTSubtitlesDesign$3,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$3,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$3,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$3,
    VOTDownloadWithName: VOTDownloadWithName$3,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$3,
    VOTLocaleHash: VOTLocaleHash$3,
    VOTUpdatedAt: VOTUpdatedAt$3,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$3,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$3,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$3,
    VOTNewAudioPlayer: VOTNewAudioPlayer$3,
    VOTUseNewModel: VOTUseNewModel$3,
    TranslationDelayed: TranslationDelayed$3,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$3,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$3,
    VOTBugReport: VOTBugReport$3,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$3,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$3,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$3,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$3,
    VOTTranslatedBy: VOTTranslatedBy$3,
    VOTStreamNotAvailable: VOTStreamNotAvailable$3,
    VOTTranslationTextService: VOTTranslationTextService$3,
    VOTNotAffectToVoice: VOTNotAffectToVoice$3,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$3,
    showVideoVolumeSlider: showVideoVolumeSlider$3,
    hotkeysSettings: hotkeysSettings$3,
    None: None$3,
    VOTUseLivelyVoice: VOTUseLivelyVoice$3,
    miscSettings: miscSettings$3,
    services: services$3,
    aboutExtension: aboutExtension$3,
    appearance: appearance$3,
    buttonPosition: buttonPosition$3,
    position: position$3,
    secs: secs$3,
    autoHideButtonDelay: autoHideButtonDelay$3,
    notFound: notFound$3,
    minButtonPositionContainer: minButtonPositionContainer$3,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$3,
    PressTheKeyCombination: PressTheKeyCombination$3,
    VOTUseAudioDownload: VOTUseAudioDownload$3,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$3,
    VOTAccountRequired: VOTAccountRequired$3,
    VOTMyAccount: VOTMyAccount$3,
    VOTLogin: VOTLogin$3,
    VOTLogout: VOTLogout$3,
    VOTRefresh: VOTRefresh$3,
    VOTYandexToken: VOTYandexToken$3,
    VOTYandexTokenInfo: VOTYandexTokenInfo$3,
    VOTLoginViaToken: VOTLoginViaToken$3,
    subtitlesSmartLayout: subtitlesSmartLayout$3,
    smartDucking: smartDucking$3,
    VOTAutoSubtitles: VOTAutoSubtitles$3,
    VOTSubtitlesFont: VOTSubtitlesFont$3
  };
  const recommended$2 = "Được đề xuất";
  const translateVideo$2 = "Dịch video";
  const disableTranslate$2 = "Tắt";
  const translationSettings$2 = "Cài đặt dịch";
  const subtitlesSettings$2 = "Cài đặt phụ đề";
  const resetSettings$2 = "Đặt lại cài đặt";
  const videoBeingTranslated$2 = "Video đang được dịch";
  const videoLanguage$2 = "Ngôn ngữ video";
  const translationLanguage$2 = "Ngôn ngữ dịch";
  const translationTake$2 = "Dịch sẽ mất";
  const translationTakeMoreThanHour$2 = "Dịch sẽ mất hơn một giờ";
  const translationTakeAboutMinute$2 = "Dịch sẽ mất khoảng một phút";
  const translationTakeFewMinutes$2 = "Dịch sẽ mất vài phút";
  const translationTakeApproximatelyMinutes$2 = "Dịch sẽ mất khoảng {0} phút";
  const translationTakeApproximatelyMinute$2 = "Dịch sẽ mất khoảng {0} phút";
  const requestTranslationFailed$2 = "Không thể yêu cầu dịch video";
  const audioNotReceived$2 = "Không nhận được liên kết âm thanh";
  const audioFormatNotSupported$2 = "Định dạng âm thanh không được hỗ trợ";
  const VOTAutoTranslate$2 = "Tự động dịch khi mở";
  const VOTDontTranslateYourLang$2 = "Không dịch từ ngôn ngữ gốc của bạn";
  const VOTVolume$2 = "Khối lượng Video:";
  const VOTVolumeTranslation$2 = "Khối lượng dịch:";
  const VOTAutoSetVolume$2 = "Giảm âm lượng video xuống";
  const VOTShowVideoSlider$2 = "Hiển thị thanh trượt âm lượng của video";
  const VOTSyncVolume$2 = "Đồng bộ âm lượng dịch và video";
  const VOTDisableFromYourLang$2 = "Bạn đã tắt dịch video bằng ngôn ngữ của bạn";
  const VOTVideoIsTooLong$2 = "Video quá dài";
  const VOTNoVideoIDFound$2 = "Không tìm thấy ID video";
  const VOTSubtitles$2 = "Phụ đề";
  const VOTSubtitlesDisabled$2 = "Tắt";
  const VOTSubtitlesMaxLength$2 = "Độ dài tối đa của phụ đề";
  const VOTHighlightWords$2 = "Tô sáng các từ";
  const VOTTranslatedFrom$2 = "dịch từ";
  const VOTAutogenerated$2 = "tự động tạo";
  const VOTSettings$2 = "Cài đặt VOT";
  const VOTMenuLanguage$2 = "Ngôn ngữ thực đơn";
  const VOTAuthors$2 = "Tác giả";
  const VOTVersion$2 = "Phiên bản";
  const VOTLoader$2 = "Trình tải";
  const VOTBrowser$2 = "Trình duyệt";
  const VOTShowPiPButton$2 = "Hiển thị nút PiP";
  const langs$2 = { "auto": "Tự động", "az": "Azerbaijan", "ay": "Aymara", "ak": "Akan", "sq": "Albania", "am": "Amharic", "en": "Anh", "ar": "Ả Rập", "hy": "Armenia", "as": "Assam", "af": "Afrikaans", "eu": "Basque", "be": "Belarus", "bn": "Bengali", "my": "Myanmar", "bg": "Bulgaria", "bs": "Bosnia", "bho": "Bhojpuri", "cy": "Wales", "hu": "Hungary", "vi": "Việt", "haw": "Hawaii", "ht": "Creole Haiti", "gl": "Galicia", "lg": "Ganda", "el": "Hy Lạp", "ka": "Gruzia", "gn": "Guaraní", "gu": "Gujarati", "gd": "Gael Scotland", "da": "Đan Mạch", "fy": "Frisia", "zu": "Zulu", "iw": "Do Thái", "ig": "Igbo", "yi": "Yiddish", "id": "Indonesia", "ga": "Ireland", "is": "Iceland", "es": "Tây Ban Nha", "it": "Italy", "yo": "Yoruba", "kk": "Kazakhstan", "kn": "Kannada", "ca": "Catalonia", "qu": "Quechua", "rw": "Kinyarwanda", "ky": "Kyrgyzstan", "zh": "Trung Quốc", "zh-Hant": "Trung (Phồn thể)", "zh-Hans": "Trung (Giản thể)", "ko": "Hàn Quốc", "co": "Corsica", "xh": "Xhosa", "ku": "Kurd", "km": "Khmer", "lo": "Lào", "la": "Latin", "lv": "Latvia", "ln": "Lingala", "lt": "Litva", "lb": "Luxembourg", "mk": "Macedonia", "mg": "Malagasy", "ms": "Malaysia", "ml": "Malayalam", "dv": "Maldives", "mt": "Malta", "mi": "Maori", "mr": "Marathi", "mn": "Mông Cổ", "de": "Đức", "ne": "Nepal", "nl": "Hà Lan", "no": "Na Uy", "ny": "Nyanja", "or": "Oriya", "om": "Oromo", "pa": "Punjab", "fa": "Ba Tư", "pl": "Ba Lan", "pt": "Bồ Đào Nha", "ps": "Pashto", "ro": "Romania", "ru": "Nga", "sm": "Samoa", "sa": "Sanskrit", "ceb": "Cebuano", "nso": "Sotho Bắc", "sr": "Serbia", "si": "Sinhalese", "sd": "Sindhi", "sk": "Slovakia", "sl": "Slovenia", "so": "Somali", "sw": "Swahili", "su": "Sundanese", "tg": "Tajikistan", "th": "Thái", "ta": "Tamil", "tt": "Tatarstan", "te": "Telugu", "ti": "Tigrinya", "ts": "Tsonga", "tr": "Thổ Nhĩ Kỳ", "tk": "Turkmenistan", "uz": "Uzbekistan", "ug": "Uighur", "uk": "Ukraina", "ur": "Urdu", "fil": "Phillipine", "fi": "Phần Lan", "fr": "Pháp", "ha": "Hausa", "hi": "Hindi", "hmn": "H'mông", "hr": "Croatia", "cs": "Séc", "sv": "Thụy Điển", "sn": "Shona", "ee": "Ewe", "eo": "Esperanto", "et": "Estonia", "st": "Sotho Nam Phi", "jv": "Javan", "ja": "Nhật", "kri": "Kri" };
  const streamNoConnectionToServer$2 = "Không có kết nối đến máy chủ";
  const searchField$2 = "Tìm kiếm...";
  const VOTTranslateAPIErrors$2 = "Dịch lỗi TỪ API";
  const VOTDetectService$2 = "Dịch vụ phát hiện ngôn ngữ";
  const VOTProxyWorkerHost$2 = "Nhập địa chỉ công nhân proxy";
  const VOTM3u8ProxyHost$2 = "Nhập địa chỉ của nhân viên proxy m3u8";
  const proxySettings$2 = "Cài Đặt Proxy";
  const translationTakeApproximatelyMinute2$2 = "Bản dịch sẽ mất khoảng {0} phút";
  const VOTAudioBooster$2 = "Mở rộng khối lượng dịch tăng";
  const VOTSubtitlesDesign$2 = "Thiết kế phụ đề";
  const VOTSubtitlesFontSize$2 = "Kích thước phông chữ của phụ đề";
  const VOTSubtitlesOpacity$2 = "Độ trong suốt của nền phụ đề";
  const VOTSubtitlesDownloadFormat$2 = "Định dạng để tải xuống phụ đề";
  const VOTDownloadWithName$2 = "Tải xuống các tệp có tên video";
  const VOTUpdateLocaleFiles$2 = "Cập nhật tệp bản địa hóa";
  const VOTLocaleHash$2 = "Ngôn ngữ băm";
  const VOTUpdatedAt$2 = "Cập nhật lúc";
  const VOTNeedWebAudioAPI$2 = "Để kích hoạt ĐIỀU này, bạn phải có Api Âm Thanh Web";
  const VOTMediaCSPEnabledOnSite$2 = "Media CSP được kích hoạt trên trang web này";
  const VOTOnlyBypassMediaCSP$2 = "Chỉ sử dụng Nó để bỏ QUA Media Csp";
  const VOTNewAudioPlayer$2 = "Sử dụng trình phát âm thanh mới";
  const VOTUseNewModel$2 = "Sử dụng một biến thể thử nghiệm Của Giọng Nói Yandex cho một số video";
  const TranslationDelayed$2 = "Bản dịch hơi bị trì hoãn";
  const VOTTranslationCompletedNotify$2 = "Bản dịch trên {0} đã được hoàn thành!";
  const VOTSendNotifyOnComplete$2 = "Gửi thông báo rằng video đã được dịch";
  const VOTBugReport$2 = "Báo cáo lỗi";
  const VOTTranslateProxyDisabled$2 = "Vô hiệu hóa";
  const VOTTranslateProxyEnabled$2 = "Đã bật";
  const VOTTranslateProxyEverything$2 = "Proxy mọi thứ";
  const VOTTranslateProxyStatus$2 = "Chế độ proxy";
  const VOTTranslatedBy$2 = "Dịch bởi {0}";
  const VOTStreamNotAvailable$2 = "Dịch luồng không khả dụng";
  const VOTTranslationTextService$2 = "Dịch vụ dịch thuật văn bản";
  const VOTNotAffectToVoice$2 = "Không ảnh hưởng đến việc dịch văn bản bằng giọng nói";
  const DontTranslateSelectedLanguages$2 = "Không dịch từ các ngôn ngữ đã chọn";
  const showVideoVolumeSlider$2 = "Hiển thị thanh trượt âm lượng video";
  const hotkeysSettings$2 = "Cài đặt phím nóng";
  const None$2 = "Không";
  const VOTUseLivelyVoice$2 = "Sử dụng giọng nói sống động. Loa nghe giống như người nga bản địa.";
  const miscSettings$2 = "Cài đặt linh tinh";
  const services$2 = { "yandexbrowser": "Trình Duyệt Yandex", "msedge": "Microsoft Cạnh", "rust-server": "Máy Chủ Rust" };
  const aboutExtension$2 = "Về phần mở rộng";
  const appearance$2 = "Ngoại hình";
  const buttonPosition$2 = "Vị trí nút trong trình phát";
  const position$2 = { "left": "Trái", "right": "Phải không", "top": "Hàng đầu", "default": "Mặc định" };
  const secs$2 = "sk";
  const autoHideButtonDelay$2 = "Trì hoãn trước khi ẩn nút dịch";
  const notFound$2 = "không tìm thấy";
  const minButtonPositionContainer$2 = "Vị trí nút chỉ thay đổi ở những người chơi lớn hơn 600 pixel.";
  const VOTTranslateProxyStatusDefault$2 = "Vô hiệu hóa hoàn toàn proxy ở quốc gia của bạn có thể phá vỡ tiện ích mở rộng";
  const PressTheKeyCombination$2 = "Nhấn tổ hợp phím...";
  const VOTUseAudioDownload$2 = "Sử dụng tải xuống âm thanh";
  const VOTUseAudioDownloadWarning$2 = "Vô hiệu hóa tải xuống âm thanh có thể ảnh hưởng đến chức năng của tiện ích mở rộng";
  const VOTAccountRequired$2 = "Bạn cần đăng nhập để sử dụng tính năng này";
  const VOTMyAccount$2 = "Tài khoản của tôi";
  const VOTLogin$2 = "Đăng nhập";
  const VOTLogout$2 = "Đăng xuất";
  const VOTRefresh$2 = "Làm mới";
  const VOTYandexToken$2 = "Nhập Mã Thông báo Yandex OAuth";
  const VOTYandexTokenInfo$2 = "Bạn có thể đặt mã thông báo tài khoản theo cách thủ công trong trường này. Xin lưu ý rằng chúng tôi không kiểm tra tính hợp lệ của nó trước khi gửi yêu cầu dịch";
  const VOTLoginViaToken$2 = "Đăng nhập qua token";
  const subtitlesSmartLayout$2 = "Bố cục phụ đề thông minh";
  const smartDucking$2 = "Khối lượng thích ứng";
  const VOTAutoSubtitles$2 = "Phụ đề mở";
  const VOTSubtitlesFont$2 = "Phông chữ phụ đề";
  const __vite_glob_0_59 = {
    recommended: recommended$2,
    translateVideo: translateVideo$2,
    disableTranslate: disableTranslate$2,
    translationSettings: translationSettings$2,
    subtitlesSettings: subtitlesSettings$2,
    resetSettings: resetSettings$2,
    videoBeingTranslated: videoBeingTranslated$2,
    videoLanguage: videoLanguage$2,
    translationLanguage: translationLanguage$2,
    translationTake: translationTake$2,
    translationTakeMoreThanHour: translationTakeMoreThanHour$2,
    translationTakeAboutMinute: translationTakeAboutMinute$2,
    translationTakeFewMinutes: translationTakeFewMinutes$2,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$2,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$2,
    requestTranslationFailed: requestTranslationFailed$2,
    audioNotReceived: audioNotReceived$2,
    audioFormatNotSupported: audioFormatNotSupported$2,
    VOTAutoTranslate: VOTAutoTranslate$2,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$2,
    VOTVolume: VOTVolume$2,
    VOTVolumeTranslation: VOTVolumeTranslation$2,
    VOTAutoSetVolume: VOTAutoSetVolume$2,
    VOTShowVideoSlider: VOTShowVideoSlider$2,
    VOTSyncVolume: VOTSyncVolume$2,
    VOTDisableFromYourLang: VOTDisableFromYourLang$2,
    VOTVideoIsTooLong: VOTVideoIsTooLong$2,
    VOTNoVideoIDFound: VOTNoVideoIDFound$2,
    VOTSubtitles: VOTSubtitles$2,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$2,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$2,
    VOTHighlightWords: VOTHighlightWords$2,
    VOTTranslatedFrom: VOTTranslatedFrom$2,
    VOTAutogenerated: VOTAutogenerated$2,
    VOTSettings: VOTSettings$2,
    VOTMenuLanguage: VOTMenuLanguage$2,
    VOTAuthors: VOTAuthors$2,
    VOTVersion: VOTVersion$2,
    VOTLoader: VOTLoader$2,
    VOTBrowser: VOTBrowser$2,
    VOTShowPiPButton: VOTShowPiPButton$2,
    langs: langs$2,
    streamNoConnectionToServer: streamNoConnectionToServer$2,
    searchField: searchField$2,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$2,
    VOTDetectService: VOTDetectService$2,
    VOTProxyWorkerHost: VOTProxyWorkerHost$2,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$2,
    proxySettings: proxySettings$2,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$2,
    VOTAudioBooster: VOTAudioBooster$2,
    VOTSubtitlesDesign: VOTSubtitlesDesign$2,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$2,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$2,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$2,
    VOTDownloadWithName: VOTDownloadWithName$2,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$2,
    VOTLocaleHash: VOTLocaleHash$2,
    VOTUpdatedAt: VOTUpdatedAt$2,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$2,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$2,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$2,
    VOTNewAudioPlayer: VOTNewAudioPlayer$2,
    VOTUseNewModel: VOTUseNewModel$2,
    TranslationDelayed: TranslationDelayed$2,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$2,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$2,
    VOTBugReport: VOTBugReport$2,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$2,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$2,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$2,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$2,
    VOTTranslatedBy: VOTTranslatedBy$2,
    VOTStreamNotAvailable: VOTStreamNotAvailable$2,
    VOTTranslationTextService: VOTTranslationTextService$2,
    VOTNotAffectToVoice: VOTNotAffectToVoice$2,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$2,
    showVideoVolumeSlider: showVideoVolumeSlider$2,
    hotkeysSettings: hotkeysSettings$2,
    None: None$2,
    VOTUseLivelyVoice: VOTUseLivelyVoice$2,
    miscSettings: miscSettings$2,
    services: services$2,
    aboutExtension: aboutExtension$2,
    appearance: appearance$2,
    buttonPosition: buttonPosition$2,
    position: position$2,
    secs: secs$2,
    autoHideButtonDelay: autoHideButtonDelay$2,
    notFound: notFound$2,
    minButtonPositionContainer: minButtonPositionContainer$2,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$2,
    PressTheKeyCombination: PressTheKeyCombination$2,
    VOTUseAudioDownload: VOTUseAudioDownload$2,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$2,
    VOTAccountRequired: VOTAccountRequired$2,
    VOTMyAccount: VOTMyAccount$2,
    VOTLogin: VOTLogin$2,
    VOTLogout: VOTLogout$2,
    VOTRefresh: VOTRefresh$2,
    VOTYandexToken: VOTYandexToken$2,
    VOTYandexTokenInfo: VOTYandexTokenInfo$2,
    VOTLoginViaToken: VOTLoginViaToken$2,
    subtitlesSmartLayout: subtitlesSmartLayout$2,
    smartDucking: smartDucking$2,
    VOTAutoSubtitles: VOTAutoSubtitles$2,
    VOTSubtitlesFont: VOTSubtitlesFont$2
  };
  const recommended$1 = "推荐使用";
  const translateVideo$1 = "翻译视频";
  const disableTranslate$1 = "关闭";
  const translationSettings$1 = "翻译设置";
  const subtitlesSettings$1 = "字幕设置";
  const resetSettings$1 = "重置设置";
  const videoBeingTranslated$1 = "视频正在翻译中";
  const videoLanguage$1 = "视频语言";
  const translationLanguage$1 = "翻译语言";
  const translationTake$1 = "翻译将采取";
  const translationTakeMoreThanHour$1 = "翻译需要一个多小时";
  const translationTakeAboutMinute$1 = "翻译大约需要一分钟";
  const translationTakeFewMinutes$1 = "翻译需要几分钟";
  const translationTakeApproximatelyMinutes$1 = "翻译大约需要{0}分钟";
  const translationTakeApproximatelyMinute$1 = "翻译大约需要{0}分钟";
  const requestTranslationFailed$1 = "请求视频翻译失败";
  const audioNotReceived$1 = "未收到音频链接";
  const audioFormatNotSupported$1 = "不支持音频格式";
  const VOTAutoTranslate$1 = "打开翻译";
  const VOTDontTranslateYourLang$1 = "不要从我的语言翻译";
  const VOTVolume$1 = "视频量:";
  const VOTVolumeTranslation$1 = "翻译卷:";
  const VOTAutoSetVolume$1 = "将视频音量降低到";
  const VOTShowVideoSlider$1 = "视频音量滑块";
  const VOTSyncVolume$1 = "链接翻译和视频卷";
  const VOTDisableFromYourLang$1 = "您已禁用以您的语言翻译视频";
  const VOTVideoIsTooLong$1 = "视频太长";
  const VOTNoVideoIDFound$1 = "没有找到视频ID";
  const VOTSubtitles$1 = "字幕";
  const VOTSubtitlesDisabled$1 = "残疾人士";
  const VOTSubtitlesMaxLength$1 = "字幕最大长度";
  const VOTHighlightWords$1 = "突出显示单词";
  const VOTTranslatedFrom$1 = "翻译自";
  const VOTAutogenerated$1 = "自生化";
  const VOTSettings$1 = "VOT设置";
  const VOTMenuLanguage$1 = "菜单语言";
  const VOTAuthors$1 = "作者";
  const VOTVersion$1 = "版本";
  const VOTLoader$1 = "装载机/装载机";
  const VOTBrowser$1 = "浏览器";
  const VOTShowPiPButton$1 = "显示PiP按钮";
  const langs$1 = { "auto": "汽车", "af": "南非荷兰语", "ak": "阿寒", "sq": "阿尔巴尼亚语", "am": "阿姆哈拉语", "ar": "阿拉伯文", "hy": "亚美尼亚语", "as": "阿萨姆语", "ay": "艾马拉", "az": "阿塞拜疆语", "bn": "邦拉", "eu": "巴斯克", "be": "白俄罗斯语", "bho": "博杰普里", "bs": "波斯尼亚", "bg": "保加利亚语", "my": "缅甸语", "ca": "加泰罗尼亚语", "ceb": "宿务野", "zh": "中文", "zh-Hans": "中文(简体)", "zh-Hant": "中文(传统)", "co": "科西嘉", "hr": "克罗地亚语", "cs": "捷克语", "da": "丹麦语", "dv": "迪维希", "nl": "荷兰语", "en": "英语", "eo": "世界语", "et": "爱沙尼亚语", "ee": "母羊", "fil": "菲律宾人", "fi": "芬兰语", "fr": "法语", "gl": "加利西亚语", "lg": "甘达", "ka": "格鲁吉亚语", "de": "德语", "el": "希腊文", "gn": "瓜拉尼", "gu": "古吉拉特语", "ht": "海地克里奥尔语", "ha": "豪萨", "haw": "夏威夷人", "iw": "希伯来语", "hi": "印地语", "hmn": "苗族", "hu": "匈牙利语", "is": "冰岛语", "ig": "伊博", "id": "印尼语", "ga": "爱尔兰人", "it": "意大利文", "ja": "日本语", "jv": "爪哇人", "kn": "卡纳达语", "kk": "哈萨克语", "km": "高棉语", "rw": "基尼亚卢旺达", "ko": "韩国人", "kri": "克里奥", "ku": "库尔德人", "ky": "吉尔吉斯", "lo": "老挝语", "la": "拉丁文", "lv": "拉脱维亚语", "ln": "林加拉", "lt": "立陶宛语", "lb": "卢森堡语", "mk": "马其顿语", "mg": "马达加斯加语", "ms": "马来语", "ml": "马拉雅拉姆语", "mt": "马耳他语", "mi": "毛利人", "mr": "马拉地语", "mn": "蒙古语", "ne": "尼泊尔语", "nso": "北索托", "no": "挪威人", "ny": "尼亚尼亚", "or": "奥迪亚", "om": "奥罗莫", "ps": "普什图语", "fa": "波斯语", "pl": "波兰语", "pt": "葡萄牙语", "pa": "旁遮普语", "qu": "克丘亚语", "ro": "罗马尼亚语", "ru": "俄语", "sm": "萨摩亚人", "sa": "梵语", "gd": "苏格兰盖尔语", "sr": "塞尔维亚语", "sn": "绍纳", "sd": "信德", "si": "僧伽罗语", "sk": "斯洛伐克语", "sl": "斯洛文尼亚语", "so": "索马里人", "st": "南索托", "es": "西班牙语", "su": "孙丹尼斯", "sw": "斯瓦希里语", "sv": "瑞典语", "tg": "塔吉克语", "ta": "泰米尔语", "tt": "鞑靼人", "te": "泰卢固语", "th": "泰国语", "ti": "提格里尼亚", "ts": "松加", "tr": "土耳其语", "tk": "土库曼", "uk": "乌克兰语", "ur": "乌尔都语", "ug": "维吾尔", "uz": "乌兹别克语", "vi": "越南人", "cy": "威尔士语", "fy": "西弗里斯兰", "xh": "科萨", "yi": "意第绪语", "yo": "约鲁巴", "zu": "祖鲁" };
  const streamNoConnectionToServer$1 = "没有连接到服务器";
  const searchField$1 = "搜索。..";
  const VOTTranslateAPIErrors$1 = "从API翻译错误";
  const VOTDetectService$1 = "语言检测服务";
  const VOTProxyWorkerHost$1 = "输入代理工作者地址";
  const VOTM3u8ProxyHost$1 = "输入m3u8代理worker的地址";
  const proxySettings$1 = "代理设置";
  const translationTakeApproximatelyMinute2$1 = "翻译大约需要{0}分钟";
  const VOTAudioBooster$1 = "扩展翻译量增加";
  const VOTSubtitlesDesign$1 = "字幕设计";
  const VOTSubtitlesFontSize$1 = "字幕的字体大小";
  const VOTSubtitlesOpacity$1 = "字幕背景的透明度";
  const VOTSubtitlesDownloadFormat$1 = "下载字幕的格式";
  const VOTDownloadWithName$1 = "下载带有视频名称的文件";
  const VOTUpdateLocaleFiles$1 = "更新本地化文件";
  const VOTLocaleHash$1 = "区域设置哈希";
  const VOTUpdatedAt$1 = "更新于";
  const VOTNeedWebAudioAPI$1 = "要启用此功能，您必须具有Web音频API";
  const VOTMediaCSPEnabledOnSite$1 = "媒体CSP在此站点上启用";
  const VOTOnlyBypassMediaCSP$1 = "仅将其用于绕过媒体CSP";
  const VOTNewAudioPlayer$1 = "使用新的音频播放器";
  const VOTUseNewModel$1 = "在一些视频中使用Yandex声音的实验变体";
  const TranslationDelayed$1 = "翻译稍有延迟";
  const VOTTranslationCompletedNotify$1 = "{0}上的翻译已经完成！";
  const VOTSendNotifyOnComplete$1 = "发送视频已翻译的通知";
  const VOTBugReport$1 = "报告错误";
  const VOTTranslateProxyDisabled$1 = "残疾人士";
  const VOTTranslateProxyEnabled$1 = "已启用";
  const VOTTranslateProxyEverything$1 = "代理一切";
  const VOTTranslateProxyStatus$1 = "代理模式";
  const VOTTranslatedBy$1 = "翻译{0}";
  const VOTStreamNotAvailable$1 = "翻译流不可用";
  const VOTTranslationTextService$1 = "文本翻译服务";
  const VOTNotAffectToVoice$1 = "不影响语音翻译文本";
  const DontTranslateSelectedLanguages$1 = "不要从选定的语言进行翻译";
  const showVideoVolumeSlider$1 = "显示视频音量滑块";
  const hotkeysSettings$1 = "热键设置";
  const None$1 = "无";
  const VOTUseLivelyVoice$1 = "用活泼的声音。 说话的人听起来像土生土长的俄罗斯人。";
  const miscSettings$1 = "Misc设置";
  const services$1 = { "yandexbrowser": "Yandex浏览器", "msedge": "微软优势", "rust-server": "Rust服务器" };
  const aboutExtension$1 = "关于延期";
  const appearance$1 = "外观";
  const buttonPosition$1 = "播放器中的按钮位置";
  const position$1 = { "left": "左图", "right": "对", "top": "顶部", "default": "违约情况" };
  const secs$1 = "secs";
  const autoHideButtonDelay$1 = "隐藏翻译按钮之前延迟";
  const notFound$1 = "未找到";
  const minButtonPositionContainer$1 = "按钮位置仅在大于600像素的玩家中发生变化。";
  const VOTTranslateProxyStatusDefault$1 = "在您的国家/地区完全禁用代理可能会破坏扩展";
  const PressTheKeyCombination$1 = "按组合键。..";
  const VOTUseAudioDownload$1 = "使用音频下载";
  const VOTUseAudioDownloadWarning$1 = "禁用音频下载可能会影响扩展的功能";
  const VOTAccountRequired$1 = "您需要登录才能使用此功能";
  const VOTMyAccount$1 = "我的帐户";
  const VOTLogin$1 = "登入";
  const VOTLogout$1 = "登出";
  const VOTRefresh$1 = "刷新";
  const VOTYandexToken$1 = "输入Yandex OAuth令牌";
  const VOTYandexTokenInfo$1 = "您可以在此字段中手动设置帐户令牌。 请注意，在发送翻译请求之前，我们不会检查其有效性";
  const VOTLoginViaToken$1 = "通过令牌登录";
  const subtitlesSmartLayout$1 = "智能字幕布局";
  const smartDucking$1 = "自适应音量";
  const VOTAutoSubtitles$1 = "公开字幕";
  const VOTSubtitlesFont$1 = "字幕字体";
  const __vite_glob_0_60 = {
    recommended: recommended$1,
    translateVideo: translateVideo$1,
    disableTranslate: disableTranslate$1,
    translationSettings: translationSettings$1,
    subtitlesSettings: subtitlesSettings$1,
    resetSettings: resetSettings$1,
    videoBeingTranslated: videoBeingTranslated$1,
    videoLanguage: videoLanguage$1,
    translationLanguage: translationLanguage$1,
    translationTake: translationTake$1,
    translationTakeMoreThanHour: translationTakeMoreThanHour$1,
    translationTakeAboutMinute: translationTakeAboutMinute$1,
    translationTakeFewMinutes: translationTakeFewMinutes$1,
    translationTakeApproximatelyMinutes: translationTakeApproximatelyMinutes$1,
    translationTakeApproximatelyMinute: translationTakeApproximatelyMinute$1,
    requestTranslationFailed: requestTranslationFailed$1,
    audioNotReceived: audioNotReceived$1,
    audioFormatNotSupported: audioFormatNotSupported$1,
    VOTAutoTranslate: VOTAutoTranslate$1,
    VOTDontTranslateYourLang: VOTDontTranslateYourLang$1,
    VOTVolume: VOTVolume$1,
    VOTVolumeTranslation: VOTVolumeTranslation$1,
    VOTAutoSetVolume: VOTAutoSetVolume$1,
    VOTShowVideoSlider: VOTShowVideoSlider$1,
    VOTSyncVolume: VOTSyncVolume$1,
    VOTDisableFromYourLang: VOTDisableFromYourLang$1,
    VOTVideoIsTooLong: VOTVideoIsTooLong$1,
    VOTNoVideoIDFound: VOTNoVideoIDFound$1,
    VOTSubtitles: VOTSubtitles$1,
    VOTSubtitlesDisabled: VOTSubtitlesDisabled$1,
    VOTSubtitlesMaxLength: VOTSubtitlesMaxLength$1,
    VOTHighlightWords: VOTHighlightWords$1,
    VOTTranslatedFrom: VOTTranslatedFrom$1,
    VOTAutogenerated: VOTAutogenerated$1,
    VOTSettings: VOTSettings$1,
    VOTMenuLanguage: VOTMenuLanguage$1,
    VOTAuthors: VOTAuthors$1,
    VOTVersion: VOTVersion$1,
    VOTLoader: VOTLoader$1,
    VOTBrowser: VOTBrowser$1,
    VOTShowPiPButton: VOTShowPiPButton$1,
    langs: langs$1,
    streamNoConnectionToServer: streamNoConnectionToServer$1,
    searchField: searchField$1,
    VOTTranslateAPIErrors: VOTTranslateAPIErrors$1,
    VOTDetectService: VOTDetectService$1,
    VOTProxyWorkerHost: VOTProxyWorkerHost$1,
    VOTM3u8ProxyHost: VOTM3u8ProxyHost$1,
    proxySettings: proxySettings$1,
    translationTakeApproximatelyMinute2: translationTakeApproximatelyMinute2$1,
    VOTAudioBooster: VOTAudioBooster$1,
    VOTSubtitlesDesign: VOTSubtitlesDesign$1,
    VOTSubtitlesFontSize: VOTSubtitlesFontSize$1,
    VOTSubtitlesOpacity: VOTSubtitlesOpacity$1,
    VOTSubtitlesDownloadFormat: VOTSubtitlesDownloadFormat$1,
    VOTDownloadWithName: VOTDownloadWithName$1,
    VOTUpdateLocaleFiles: VOTUpdateLocaleFiles$1,
    VOTLocaleHash: VOTLocaleHash$1,
    VOTUpdatedAt: VOTUpdatedAt$1,
    VOTNeedWebAudioAPI: VOTNeedWebAudioAPI$1,
    VOTMediaCSPEnabledOnSite: VOTMediaCSPEnabledOnSite$1,
    VOTOnlyBypassMediaCSP: VOTOnlyBypassMediaCSP$1,
    VOTNewAudioPlayer: VOTNewAudioPlayer$1,
    VOTUseNewModel: VOTUseNewModel$1,
    TranslationDelayed: TranslationDelayed$1,
    VOTTranslationCompletedNotify: VOTTranslationCompletedNotify$1,
    VOTSendNotifyOnComplete: VOTSendNotifyOnComplete$1,
    VOTBugReport: VOTBugReport$1,
    VOTTranslateProxyDisabled: VOTTranslateProxyDisabled$1,
    VOTTranslateProxyEnabled: VOTTranslateProxyEnabled$1,
    VOTTranslateProxyEverything: VOTTranslateProxyEverything$1,
    VOTTranslateProxyStatus: VOTTranslateProxyStatus$1,
    VOTTranslatedBy: VOTTranslatedBy$1,
    VOTStreamNotAvailable: VOTStreamNotAvailable$1,
    VOTTranslationTextService: VOTTranslationTextService$1,
    VOTNotAffectToVoice: VOTNotAffectToVoice$1,
    DontTranslateSelectedLanguages: DontTranslateSelectedLanguages$1,
    showVideoVolumeSlider: showVideoVolumeSlider$1,
    hotkeysSettings: hotkeysSettings$1,
    None: None$1,
    VOTUseLivelyVoice: VOTUseLivelyVoice$1,
    miscSettings: miscSettings$1,
    services: services$1,
    aboutExtension: aboutExtension$1,
    appearance: appearance$1,
    buttonPosition: buttonPosition$1,
    position: position$1,
    secs: secs$1,
    autoHideButtonDelay: autoHideButtonDelay$1,
    notFound: notFound$1,
    minButtonPositionContainer: minButtonPositionContainer$1,
    VOTTranslateProxyStatusDefault: VOTTranslateProxyStatusDefault$1,
    PressTheKeyCombination: PressTheKeyCombination$1,
    VOTUseAudioDownload: VOTUseAudioDownload$1,
    VOTUseAudioDownloadWarning: VOTUseAudioDownloadWarning$1,
    VOTAccountRequired: VOTAccountRequired$1,
    VOTMyAccount: VOTMyAccount$1,
    VOTLogin: VOTLogin$1,
    VOTLogout: VOTLogout$1,
    VOTRefresh: VOTRefresh$1,
    VOTYandexToken: VOTYandexToken$1,
    VOTYandexTokenInfo: VOTYandexTokenInfo$1,
    VOTLoginViaToken: VOTLoginViaToken$1,
    subtitlesSmartLayout: subtitlesSmartLayout$1,
    smartDucking: smartDucking$1,
    VOTAutoSubtitles: VOTAutoSubtitles$1,
    VOTSubtitlesFont: VOTSubtitlesFont$1
  };
  const recommended = "kunconywa";
  const translateVideo = "Humusha ividiyo";
  const disableTranslate = "Cisha";
  const translationSettings = "Izilungiselelo zokuhumusha";
  const subtitlesSettings = "Izihlokwana izilungiselelo";
  const resetSettings = "Resela kabusha izilungiselelo";
  const videoBeingTranslated = "Le video iyahunyushwa";
  const videoLanguage = "Ulimi lwevidiyo";
  const translationLanguage = "Ulimi lokuhumusha";
  const translationTake = "Ukuhumusha kuzothatha";
  const translationTakeMoreThanHour = "Ukuhumusha kuzothatha isikhathi esingaphezu kwehora";
  const translationTakeAboutMinute = "Ukuhumusha kuzothatha cishe umzuzu";
  const translationTakeFewMinutes = "Ukuhumusha kuzothatha imizuzu embalwa";
  const translationTakeApproximatelyMinutes = "Ukuhumusha kuzothatha cishe imizuzu {0}";
  const translationTakeApproximatelyMinute = "Ukuhumusha kuzothatha cishe imizuzu {0}";
  const requestTranslationFailed = "Yehlulekile ukucela ukuhunyushwa kwevidiyo";
  const audioNotReceived = "Isixhumanisi somsindo asitholakalanga";
  const audioFormatNotSupported = "Ifomethi yomsindo ayisekelwa";
  const VOTAutoTranslate = "Ukuhumusha ku-open";
  const VOTDontTranslateYourLang = "Ungahumushi kusuka olimini lwami";
  const VOTVolume = "Ivolumu yevidiyo:";
  const VOTVolumeTranslation = "Umqulu wokuhumusha:";
  const VOTAutoSetVolume = "Nciphisa ivolumu yevidiyo ibe";
  const VOTShowVideoSlider = "Isilayidi sevidiyo";
  const VOTSyncVolume = "Isixhumanisi ukuhumusha nevidiyo ivolumu";
  const VOTDisableFromYourLang = "Ukhubaze ukuhunyushwa kwevidiyo ngolimi lwakho";
  const VOTVideoIsTooLong = "Ividiyo yinde kakhulu";
  const VOTNoVideoIDFound = "Ayikho i-id yevidiyo etholakele";
  const VOTSubtitles = "Imibhalo engezansi";
  const VOTSubtitlesDisabled = "Abakhubazekile";
  const VOTSubtitlesMaxLength = "Imibhalo engezansi ubude obukhulu";
  const VOTHighlightWords = "Gqamisa amagama";
  const VOTTranslatedFrom = "kuhunyushwe kusuka";
  const VOTAutogenerated = "okuzenzakalelayo";
  const VOTSettings = "Izilungiselelo ZOKUVOTA";
  const VOTMenuLanguage = "Ulimi lwemenyu";
  const VOTAuthors = "Ababhali";
  const VOTVersion = "Inguqulo";
  const VOTLoader = "Umthwali";
  const VOTBrowser = "Isiphequluli";
  const VOTShowPiPButton = "Bonisa inkinobho PiP";
  const langs = { "auto": "Imoto", "af": "Isibhunu", "ak": "U-Akan", "sq": "Isi-albania", "am": "Isi-Amharic", "ar": "Isi-arabhu", "hy": "Isi-armenia", "as": "Isi-Assamese", "ay": "Isi-Aymara", "az": "Isi-Azerbaijani", "bn": "Isibangla", "eu": "Isibasque", "be": "Isibelarusian", "bho": "Ibhojpuri", "bs": "Isibosnia", "bg": "Isibulgaria", "my": "Isiburma", "ca": "Isicatalan", "ceb": "Isicebuano", "zh": "Isishayina", "zh-Hans": "Isishayina (Simplified)", "zh-Hant": "Isishayina (Ngokwesiko)", "co": "Isicorsican", "hr": "Isicroatia", "cs": "Isiczech", "da": "Isidanish", "dv": "Udivehi", "nl": "Isidashi", "en": "Isingisi", "eo": "Isi-Esperanto", "et": "Isi-estonia", "ee": "Imvu", "fil": "Isifilipino", "fi": "Isifinnish", "fr": "Isifulentshi", "gl": "Isigalician", "lg": "Uganda", "ka": "Isigeorgia", "de": "Isijalimane", "el": "Isigreki", "gn": "Isiguarani", "gu": "Isigujarati", "ht": "Isicreole Sasehaiti", "ha": "Isihausa", "haw": "Isihawaii", "iw": "Isiheberu", "hi": "Isihindi", "hmn": "Isihmong", "hu": "Isihungary", "is": "Isi-iceland", "ig": "Isi-Igbo", "id": "Isi-indonesia", "ga": "Isi-Irish", "it": "Isintaliyane", "ja": "Isijapane", "jv": "Isijava", "kn": "Isikannada", "kk": "Isikazakh", "km": "Isikhmer", "rw": "Ikinyarwanda", "ko": "Isikorea", "kri": "Isikrio", "ku": "Isikurdish", "ky": "Isikyrgyz", "lo": "Isilao", "la": "Isilatini", "lv": "Isilatvia", "ln": "Isilingala", "lt": "Isilithuania", "lb": "Isiluxembourg", "mk": "Isimacedonia", "mg": "Isimalagasy", "ms": "Isimalay", "ml": "Isimalayalam", "mt": "Isimalta", "mi": "Amamaori", "mr": "Isimarathi", "mn": "Isimongolia", "ne": "Isinepali", "nso": "Isisotho Sasenyakatho", "no": "Isinorway", "ny": "Inyanja", "or": "Isi-Odia", "om": "Ama-Oromo", "ps": "Isipashto", "fa": "Isipheresiya", "pl": "Isipolish", "pt": "Isiputukezi", "pa": "Isipunjabi", "qu": "Isiquechua", "ro": "Isiromania", "ru": "Isirashiya", "sm": "Isisamoa", "sa": "Isisanskrit", "gd": "Isigaelic Sasescotland", "sr": "Isiserbia", "sn": "Isishona", "sd": "Isisindi", "si": "Isisinhala", "sk": "Isislovak", "sl": "Isislovenia", "so": "Isisomali", "st": "Isisotho Saseningizimu", "es": "Isispanishi", "su": "Isisundanese", "sw": "Isiswahili", "sv": "Isiswidi", "tg": "Isitajik", "ta": "Isitamil", "tt": "Isitatar", "te": "Isitelugu", "th": "Isithai", "ti": "Isitigrinya", "ts": "Isitsonga", "tr": "Isiturkey", "tk": "Amaturkmen", "uk": "Isi-ukraine", "ur": "Isi-Urdu", "ug": "Isi-Uyghur", "uz": "Isi-Uzbek", "vi": "Isivietnam", "cy": "Isiwelsh", "fy": "Isifrizi Sasentshonalanga", "xh": "Isixhosa", "yi": "Isiyiddish", "yo": "Isiyoruba", "zu": "Isizulu" };
  const streamNoConnectionToServer = "Akukho ukuxhumana neseva";
  const searchField = "Ukucinga...";
  const VOTTranslateAPIErrors = "Humusha amaphutha KUSUKA API";
  const VOTDetectService = "Isevisi yokuthola ulimi";
  const VOTProxyWorkerHost = "Faka ikheli proxy isisebenzi";
  const VOTM3u8ProxyHost = "Faka ikheli le-m3u8 proxy worker";
  const proxySettings = "Izilungiselelo Ze-Proxy";
  const translationTakeApproximatelyMinute2 = "Ukuhumusha kuzothatha cishe imizuzu {0}";
  const VOTAudioBooster = "Ukwanda kwevolumu yokuhumusha okwandisiwe";
  const VOTSubtitlesDesign = "Umklamo wemibhalo engezansi";
  const VOTSubtitlesFontSize = "Usayizi wefonti yemibhalo engezansi";
  const VOTSubtitlesOpacity = "Ukungafihli lutho kwesizinda sesihlokwana";
  const VOTSubtitlesDownloadFormat = "Ifomethi yokulanda imibhalo engezansi";
  const VOTDownloadWithName = "Thwebula amafayela ngegama video";
  const VOTUpdateLocaleFiles = "Buyekeza amafayela localization";
  const VOTLocaleHash = "I-hash yendawo";
  const VOTUpdatedAt = "Kubuyekezwe";
  const VOTNeedWebAudioAPI = "Ukuze ukwazi lokhu, kufanele UBE NE-WEB AUDIO API";
  const VOTMediaCSPEnabledOnSite = "I-Media csp inikwe amandla kule sayithi";
  const VOTOnlyBypassMediaCSP = "Sebenzisa kuphela Bypassing Media Csp";
  const VOTNewAudioPlayer = "Sebenzisa umdlali omusha audio";
  const VOTUseNewModel = "Sebenzisa ukuhlukahluka kokuhlola kwamazwi E Yandex kwamanye amavidiyo";
  const TranslationDelayed = "Ukuhumusha kubambezeleke kancane";
  const VOTTranslationCompletedNotify = "Ukuhunyushwa kwe - {0} sekuqediwe!";
  const VOTSendNotifyOnComplete = "Thumela isaziso sokuthi ividiyo ihunyushwe";
  const VOTBugReport = "Bika iphutha";
  const VOTTranslateProxyDisabled = "Abakhubazekile";
  const VOTTranslateProxyEnabled = "Inikwe amandla";
  const VOTTranslateProxyEverything = "Ummeleli konke";
  const VOTTranslateProxyStatus = "Imodi yokumisa";
  const VOTTranslatedBy = "Kuhunyushwe ngokuthi {0}";
  const VOTStreamNotAvailable = "Ukuhumusha stream ayitholakali";
  const VOTTranslationTextService = "Isevisi yokuhumusha umbhalo";
  const VOTNotAffectToVoice = "Akuthinti ukuhunyushwa kombhalo ngezwi";
  const DontTranslateSelectedLanguages = "Ungahumushi kusuka ezilimini ezikhethiwe";
  const showVideoVolumeSlider = "Bonisa ividiyo ivolumu slider";
  const hotkeysSettings = "Hotkeys izilungiselelo";
  const None = "Ayikho";
  const VOTUseLivelyVoice = "Sebenzisa amazwi aphilayo. Izikhulumi zizwakala Njengabantu bomdabu Baserussia.";
  const miscSettings = "Izilungiselelo ze-Misc";
  const services = { "yandexbrowser": "Isiphequluli Se-Yandex", "msedge": "Umngcele Wemicrosoft", "rust-server": "Iseva Yokugqwala" };
  const aboutExtension = "Mayelana nokwandiswa";
  const appearance = "Ukubukeka";
  const buttonPosition = "Isikhundla senkinobho kumdlali";
  const position = { "left": "Kwesobunxele", "right": "Ngakwesokudla", "top": "Phezulu", "default": "Okuzenzakalelayo" };
  const secs = "amasekhondi";
  const autoHideButtonDelay = "Ukubambezeleka ngaphambi kokufihla inkinobho yokuhumusha";
  const notFound = "ayitholakalanga";
  const minButtonPositionContainer = "Isikhundla senkinobho sishintsha kuphela kubadlali abanamaphikseli angaphezu kuka-600.";
  const VOTTranslateProxyStatusDefault = "Ukukhubaza ngokuphelele ukumela izwe lakho kungase kuphule ukunwetshwa";
  const PressTheKeyCombination = "Cindezela inhlanganisela key...";
  const VOTUseAudioDownload = "Sebenzisa audio thwebula";
  const VOTUseAudioDownloadWarning = "Ukukhubaza ukulandwa komsindo kungathinta ukusebenza kwesandiso";
  const VOTAccountRequired = "Udinga ukungena ngemvume ukuze usebenzise lesi sici";
  const VOTMyAccount = "I akhawunti yami";
  const VOTLogin = "Ukungena ngemvume";
  const VOTLogout = "Ukuphuma";
  const VOTRefresh = "Vuselela";
  const VOTYandexToken = "Faka Ithokheni Yandex OAuth";
  const VOTYandexTokenInfo = "Ungakwazi ngesandla usethe ithokheni akhawunti kule nkambu. Sicela uqaphele ukuthi asikuhloli ukuba yiqiniso kwayo ngaphambi kokuthumela isicelo sokuhumusha";
  const VOTLoginViaToken = "Ukungena ngemvume nge token";
  const subtitlesSmartLayout = "Ukuhlelwa kwesihlokwana esihlakaniphile";
  const smartDucking = "Umthamo wokuzivumelanisa nezimo";
  const VOTAutoSubtitles = "Izihlokwana ezivulekile";
  const VOTSubtitlesFont = "Ifonti yesihlokwana";
  const __vite_glob_0_61 = {
    recommended,
    translateVideo,
    disableTranslate,
    translationSettings,
    subtitlesSettings,
    resetSettings,
    videoBeingTranslated,
    videoLanguage,
    translationLanguage,
    translationTake,
    translationTakeMoreThanHour,
    translationTakeAboutMinute,
    translationTakeFewMinutes,
    translationTakeApproximatelyMinutes,
    translationTakeApproximatelyMinute,
    requestTranslationFailed,
    audioNotReceived,
    audioFormatNotSupported,
    VOTAutoTranslate,
    VOTDontTranslateYourLang,
    VOTVolume,
    VOTVolumeTranslation,
    VOTAutoSetVolume,
    VOTShowVideoSlider,
    VOTSyncVolume,
    VOTDisableFromYourLang,
    VOTVideoIsTooLong,
    VOTNoVideoIDFound,
    VOTSubtitles,
    VOTSubtitlesDisabled,
    VOTSubtitlesMaxLength,
    VOTHighlightWords,
    VOTTranslatedFrom,
    VOTAutogenerated,
    VOTSettings,
    VOTMenuLanguage,
    VOTAuthors,
    VOTVersion,
    VOTLoader,
    VOTBrowser,
    VOTShowPiPButton,
    langs,
    streamNoConnectionToServer,
    searchField,
    VOTTranslateAPIErrors,
    VOTDetectService,
    VOTProxyWorkerHost,
    VOTM3u8ProxyHost,
    proxySettings,
    translationTakeApproximatelyMinute2,
    VOTAudioBooster,
    VOTSubtitlesDesign,
    VOTSubtitlesFontSize,
    VOTSubtitlesOpacity,
    VOTSubtitlesDownloadFormat,
    VOTDownloadWithName,
    VOTUpdateLocaleFiles,
    VOTLocaleHash,
    VOTUpdatedAt,
    VOTNeedWebAudioAPI,
    VOTMediaCSPEnabledOnSite,
    VOTOnlyBypassMediaCSP,
    VOTNewAudioPlayer,
    VOTUseNewModel,
    TranslationDelayed,
    VOTTranslationCompletedNotify,
    VOTSendNotifyOnComplete,
    VOTBugReport,
    VOTTranslateProxyDisabled,
    VOTTranslateProxyEnabled,
    VOTTranslateProxyEverything,
    VOTTranslateProxyStatus,
    VOTTranslatedBy,
    VOTStreamNotAvailable,
    VOTTranslationTextService,
    VOTNotAffectToVoice,
    DontTranslateSelectedLanguages,
    showVideoVolumeSlider,
    hotkeysSettings,
    None,
    VOTUseLivelyVoice,
    miscSettings,
    services,
    aboutExtension,
    appearance,
    buttonPosition,
    position,
    secs,
    autoHideButtonDelay,
    notFound,
    minButtonPositionContainer,
    VOTTranslateProxyStatusDefault,
    PressTheKeyCombination,
    VOTUseAudioDownload,
    VOTUseAudioDownloadWarning,
    VOTAccountRequired,
    VOTMyAccount,
    VOTLogin,
    VOTLogout,
    VOTRefresh,
    VOTYandexToken,
    VOTYandexTokenInfo,
    VOTLoginViaToken,
    subtitlesSmartLayout,
    smartDucking,
    VOTAutoSubtitles,
    VOTSubtitlesFont
  };
  var define_AVAILABLE_LOCALES_default = ["auto", "en", "ru", "af", "am", "ar", "az", "bg", "bn", "bs", "ca", "cs", "cy", "da", "de", "el", "es", "et", "eu", "fa", "fi", "fr", "gl", "hi", "hr", "hu", "hy", "id", "it", "ja", "jv", "kk", "km", "kn", "ko", "lo", "mk", "ml", "mn", "ms", "mt", "my", "ne", "nl", "pa", "pl", "pt", "ro", "si", "sk", "sl", "sq", "sr", "su", "sv", "sw", "tr", "uk", "ur", "uz", "vi", "zh", "zu"];
  const LOCALE_STORAGE_KEYS = [
    "localePhrases",
    "localeLang",
    "localeHash",
    "localeVersion",
    "localeUpdatedAt",
    "localeLangOverride"
  ];
  const DEFAULT_LOCALE = toFlatObj(__vite_glob_0_13);
  const BUNDLED_LOCALE_BY_LANG = (() => {
    const modules = Object.assign({
      "./locales/af.json": __vite_glob_0_0,
      "./locales/am.json": __vite_glob_0_1,
      "./locales/ar.json": __vite_glob_0_2,
      "./locales/az.json": __vite_glob_0_3,
      "./locales/bg.json": __vite_glob_0_4,
      "./locales/bn.json": __vite_glob_0_5,
      "./locales/bs.json": __vite_glob_0_6,
      "./locales/ca.json": __vite_glob_0_7,
      "./locales/cs.json": __vite_glob_0_8,
      "./locales/cy.json": __vite_glob_0_9,
      "./locales/da.json": __vite_glob_0_10,
      "./locales/de.json": __vite_glob_0_11,
      "./locales/el.json": __vite_glob_0_12,
      "./locales/en.json": __vite_glob_0_13,
      "./locales/es.json": __vite_glob_0_14,
      "./locales/et.json": __vite_glob_0_15,
      "./locales/eu.json": __vite_glob_0_16,
      "./locales/fa.json": __vite_glob_0_17,
      "./locales/fi.json": __vite_glob_0_18,
      "./locales/fr.json": __vite_glob_0_19,
      "./locales/gl.json": __vite_glob_0_20,
      "./locales/hi.json": __vite_glob_0_21,
      "./locales/hr.json": __vite_glob_0_22,
      "./locales/hu.json": __vite_glob_0_23,
      "./locales/hy.json": __vite_glob_0_24,
      "./locales/id.json": __vite_glob_0_25,
      "./locales/it.json": __vite_glob_0_26,
      "./locales/ja.json": __vite_glob_0_27,
      "./locales/jv.json": __vite_glob_0_28,
      "./locales/kk.json": __vite_glob_0_29,
      "./locales/km.json": __vite_glob_0_30,
      "./locales/kn.json": __vite_glob_0_31,
      "./locales/ko.json": __vite_glob_0_32,
      "./locales/lo.json": __vite_glob_0_33,
      "./locales/mk.json": __vite_glob_0_34,
      "./locales/ml.json": __vite_glob_0_35,
      "./locales/mn.json": __vite_glob_0_36,
      "./locales/ms.json": __vite_glob_0_37,
      "./locales/mt.json": __vite_glob_0_38,
      "./locales/my.json": __vite_glob_0_39,
      "./locales/ne.json": __vite_glob_0_40,
      "./locales/nl.json": __vite_glob_0_41,
      "./locales/pa.json": __vite_glob_0_42,
      "./locales/pl.json": __vite_glob_0_43,
      "./locales/pt.json": __vite_glob_0_44,
      "./locales/ro.json": __vite_glob_0_45,
      "./locales/ru.json": __vite_glob_0_46,
      "./locales/si.json": __vite_glob_0_47,
      "./locales/sk.json": __vite_glob_0_48,
      "./locales/sl.json": __vite_glob_0_49,
      "./locales/sq.json": __vite_glob_0_50,
      "./locales/sr.json": __vite_glob_0_51,
      "./locales/su.json": __vite_glob_0_52,
      "./locales/sv.json": __vite_glob_0_53,
      "./locales/sw.json": __vite_glob_0_54,
      "./locales/tr.json": __vite_glob_0_55,
      "./locales/uk.json": __vite_glob_0_56,
      "./locales/ur.json": __vite_glob_0_57,
      "./locales/uz.json": __vite_glob_0_58,
      "./locales/vi.json": __vite_glob_0_59,
      "./locales/zh.json": __vite_glob_0_60,
      "./locales/zu.json": __vite_glob_0_61
    });
    const map = {};
    for (const [path, locale] of Object.entries(modules)) {
      const match = path.match(/\/([^/]+)\.json$/i);
      if (!match) continue;
      const localeCode = match[1].toLowerCase();
      if (localeCode === "en") continue;
      map[localeCode] = toFlatObj(locale);
    }
    return map;
  })();
  const repoBranch = "dev";
  const availableLocales = (() => {
    const locales = typeof define_AVAILABLE_LOCALES_default !== "undefined" && Array.isArray(define_AVAILABLE_LOCALES_default) ? define_AVAILABLE_LOCALES_default : ["en"];
    return locales.includes("auto") ? locales : ["auto", ...locales];
  })();
  function resolveRuntimeLocaleVersion(buildVersion, scriptVersion) {
    return buildVersion || scriptVersion || "unknown";
  }
  function getRuntimeLocaleVersion() {
    const buildVersion = String("1.11.5.93");
    const scriptVersion = typeof GM_info !== "undefined" ? String(GM_info?.script?.version || "") : "";
    return resolveRuntimeLocaleVersion(buildVersion, scriptVersion);
  }
  class LocalizationProvider {
lang;
locale;
    defaultLocale = DEFAULT_LOCALE;
    localesUrl = `${contentUrl}/${repoBranch}/src/localization/locales`;
    hashesUrl = `${contentUrl}/${repoBranch}/src/localization/hashes.json`;
    warnedMissingKeys = new Set();
    _langOverride = "auto";
    constructor() {
      this.lang = this.getLang();
      this.locale = {};
    }
    async init() {
      const [langOverride, phrases, storedLocaleLang] = await Promise.all([
        votStorage.get("localeLangOverride", "auto"),
        votStorage.get("localePhrases", ""),
        votStorage.get("localeLang", "")
      ]);
      this._langOverride = langOverride;
      this.lang = this.getLang();
      if (phrases && storedLocaleLang === this.lang) {
        this.setLocaleFromJsonString(phrases);
      } else {
        this.applyBundledLocale(this.lang);
      }
      return this;
    }
    get langOverride() {
      return this._langOverride;
    }
    getLang() {
      return this.langOverride !== "auto" ? this.langOverride : lang;
    }
    getAvailableLangs() {
      return [...availableLocales];
    }
    async reset() {
      await Promise.all(LOCALE_STORAGE_KEYS.map((key) => votStorage.delete(key)));
      return this;
    }
    buildUrl(baseUrl, path = "", force = false) {
      const query = force ? `?timestamp=${getTimestamp()}` : "";
      return `${baseUrl}${path}${query}`;
    }
    getBundledLocale(lang2) {
      return lang2.toLowerCase() === "en" ? DEFAULT_LOCALE : BUNDLED_LOCALE_BY_LANG[lang2.toLowerCase()] ?? null;
    }
    applyBundledLocale(lang2) {
      const bundledLocale = this.getBundledLocale(lang2);
      if (!bundledLocale) {
        this.locale = {};
        this.warnedMissingKeys.clear();
        return false;
      }
      this.locale = { ...bundledLocale };
      this.warnedMissingKeys.clear();
      return true;
    }
    async changeLang(newLang) {
      const oldLang = this.langOverride;
      if (oldLang === newLang) {
        return false;
      }
      await votStorage.set("localeLangOverride", newLang);
      this._langOverride = newLang;
      this.lang = this.getLang();
      await this.update(true);
      return true;
    }
    async checkUpdates(force = false) {
      debug.log("Check locale updates...");
      try {
        const res = await GM_fetch(this.buildUrl(this.hashesUrl, "", force), {
          forceGmXhr: true
        });
        if (!res.ok) throw res.status;
        const hashes = await res.json();
        if (!hashes || typeof hashes !== "object") {
          throw new Error("Invalid locale hashes payload");
        }
        const nextHash = hashes[this.lang];
        if (typeof nextHash !== "string" || !nextHash) {
          return false;
        }
        const currentHash = await votStorage.get("localeHash", "");
        return currentHash === nextHash ? false : nextHash;
      } catch (err) {
        console.error(
          "[VOT] [localizationProvider] Failed to get locales hash:",
          err
        );
        return null;
      }
    }
    async update(force = false) {
      const runtimeLocaleVersion = getRuntimeLocaleVersion();
      const storedLocaleVersion = await votStorage.get(
        "localeVersion",
        ""
      );
      if (!force && storedLocaleVersion === runtimeLocaleVersion) {
        return this;
      }
      const hash = await this.checkUpdates(force);
      if (hash === null) {
        this.applyBundledLocale(this.lang);
        return this;
      }
      if (!hash) {
        if (!Object.keys(this.locale).length) {
          this.applyBundledLocale(this.lang);
        }
        return this;
      }
      const timestamp = getTimestamp();
      debug.log("Updating locale...");
      try {
        const res = await GM_fetch(
          this.buildUrl(this.localesUrl, `/${this.lang}.json`, force),
          { forceGmXhr: true }
        );
        if (!res.ok) throw res.status;
        const text = await res.text();
        this.setLocaleFromJsonString(text);
        await Promise.all([
          votStorage.set("localePhrases", text),
          votStorage.set("localeHash", hash),
          votStorage.set("localeLang", this.lang),
          votStorage.set("localeVersion", runtimeLocaleVersion),
          votStorage.set("localeUpdatedAt", timestamp)
        ]);
      } catch (err) {
        console.error("[VOT] [localizationProvider] Failed to get locale:", err);
        const storedLocaleLang = await votStorage.get("localeLang", "");
        const storedLocalePhrases = await votStorage.get("localePhrases", "");
        if (storedLocalePhrases && storedLocaleLang === this.lang) {
          this.setLocaleFromJsonString(storedLocalePhrases);
        } else {
          this.applyBundledLocale(this.lang);
        }
      }
      return this;
    }
    setLocaleFromJsonString(json) {
      const trimmed = json.trim();
      if (!trimmed) {
        this.locale = {};
        this.warnedMissingKeys.clear();
        return this;
      }
      try {
        const locale = JSON.parse(trimmed);
        if (!locale || typeof locale !== "object" || Array.isArray(locale)) {
          throw new Error("Locale payload should be a JSON object");
        }
        const parsedLocale = toFlatObj(locale);
        const bundledLocale = this.getBundledLocale(this.lang);
        this.locale = bundledLocale ? { ...bundledLocale, ...parsedLocale } : parsedLocale;
      } catch (err) {
        console.error("[VOT] [localizationProvider]", err);
        this.locale = {};
      }
      this.warnedMissingKeys.clear();
      return this;
    }
    getFromLocale(locale, key, source = "locale") {
      const phrase = locale[key];
      return phrase ?? this.warnMissingKey(locale, key, source);
    }
    warnMissingKey(locale, key, source) {
      const warningKey = `${source}:${key}`;
      if (this.warnedMissingKeys.has(warningKey)) {
        return void 0;
      }
      this.warnedMissingKeys.add(warningKey);
      console.warn(
        "[VOT] [localizationProvider] locale",
        locale,
        "doesn't contain key",
        key
      );
      return void 0;
    }
    getDefault(key) {
      return this.getFromLocale(this.defaultLocale, key, "default") ?? key;
    }
    get(key) {
      return this.getFromLocale(this.locale, key) ?? this.getDefault(key);
    }
    getLangLabel(lang2) {
      const key = `langs.${lang2}`;
      if (key in this.defaultLocale) {
        const label = this.get(key);
        if (label) {
          return label;
        }
      }
      return lang2.toUpperCase();
    }
  }
  const localizationProvider = new LocalizationProvider();
  let localizationProviderReadyPromise = null;
  function ensureLocalizationProviderReady() {
    localizationProviderReadyPromise ??= localizationProvider.init();
    return localizationProviderReadyPromise;
  }
  const mainStylesInline = '.vot-button{--vot-helper-theme:var(--vot-theme-rgb,var(--vot-primary-rgb,33, 150, 243));--vot-helper-ontheme:var(--vot-ontheme-rgb,var(--vot-onprimary-rgb,255, 255, 255));box-sizing:border-box;vertical-align:middle;text-align:center;text-overflow:ellipsis;cursor:pointer;min-width:64px;height:36px;color:rgb(var(--vot-helper-ontheme));background-color:rgb(var(--vot-helper-theme));box-shadow:var(--vot-shadow-1);transition:box-shadow var(--vot-duration-medium) var(--vot-easing-standard);outline:none;font-size:14px;line-height:36px;display:inline-block;position:relative;border-radius:var(--vot-radius-s)!important;padding:0 var(--vot-space-4)!important;font-family:var(--vot-font-family,"Roboto", "Segoe UI", system-ui, sans-serif)!important;border:none!important;font-weight:500!important}.vot-button:before,.vot-button:after{content:"";opacity:0;position:absolute;inset:0;border-radius:inherit!important}.vot-button:before{background-color:rgb(var(--vot-helper-ontheme));transition:opacity var(--vot-duration-medium) var(--vot-easing-standard)}.vot-button:after{transition:opacity var(--vot-duration-slow) var(--vot-easing-standard),background-size var(--vot-duration-slow) var(--vot-easing-standard);background:radial-gradient(circle,currentColor 1%,#0000 1%) 50%/10000% 10000% no-repeat}.vot-button:hover:before{opacity:.08}.vot-button:active:after{opacity:.32;background-size:100% 100%;transition:background-size}.vot-button:hover,.vot-button:active{box-shadow:var(--vot-shadow-2)}.vot-button[disabled=true]{background-color:rgba(var(--vot-onsurface-rgb,0, 0, 0),.12);color:rgba(var(--vot-onsurface-rgb,0, 0, 0),.38);box-shadow:none;cursor:initial}.vot-button[disabled=true]:before,.vot-button[disabled=true]:after{opacity:0}.vot-outlined-button{--vot-helper-theme:var(--vot-theme-rgb,var(--vot-primary-rgb,33, 150, 243));box-sizing:border-box;vertical-align:middle;text-align:center;text-overflow:ellipsis;cursor:pointer;min-width:64px;height:36px;color:rgb(var(--vot-helper-theme));background-color:#0000;outline:none;font-size:14px;line-height:34px;display:inline-block;position:relative;border-radius:var(--vot-radius-s)!important;padding:0 var(--vot-space-4)!important;font-family:var(--vot-font-family,"Roboto", "Segoe UI", system-ui, sans-serif)!important;border:solid 1px var(--vot-border-color)!important;margin:0!important;font-weight:500!important}.vot-outlined-button:before,.vot-outlined-button:after{content:"";opacity:0;position:absolute;inset:0;border-radius:inherit!important}.vot-outlined-button:before{background-color:rgb(var(--vot-helper-theme));transition:opacity var(--vot-duration-medium) var(--vot-easing-standard)}.vot-outlined-button:after{transition:opacity var(--vot-duration-slow) var(--vot-easing-standard),background-size var(--vot-duration-slow) var(--vot-easing-standard);background:radial-gradient(circle,currentColor 1%,#0000 1%) 50%/10000% 10000% no-repeat}.vot-outlined-button:hover:before{opacity:.04}.vot-outlined-button:active:after{opacity:.16;background-size:100% 100%;transition:background-size}.vot-outlined-button[disabled=true]{color:rgba(var(--vot-onsurface-rgb,0, 0, 0),.38);cursor:initial;background-color:#0000}.vot-outlined-button[disabled=true]:before,.vot-outlined-button[disabled=true]:after{opacity:0}.vot-text-button{--vot-helper-theme:var(--vot-theme-rgb,var(--vot-primary-rgb,33, 150, 243));box-sizing:border-box;vertical-align:middle;text-align:center;text-overflow:ellipsis;cursor:pointer;min-width:64px;height:36px;color:rgb(var(--vot-helper-theme));background-color:#0000;outline:none;font-size:14px;line-height:36px;display:inline-block;position:relative;border-radius:var(--vot-radius-s)!important;padding:0 var(--vot-space-2)!important;font-family:var(--vot-font-family,"Roboto", "Segoe UI", system-ui, sans-serif)!important;border:none!important;margin:0!important;font-weight:500!important}.vot-text-button:before,.vot-text-button:after{content:"";opacity:0;position:absolute;inset:0;border-radius:inherit!important}.vot-text-button:before{background-color:rgb(var(--vot-helper-theme));transition:opacity var(--vot-duration-medium) var(--vot-easing-standard)}.vot-text-button:after{transition:opacity var(--vot-duration-slow) var(--vot-easing-standard),background-size var(--vot-duration-slow) var(--vot-easing-standard);background:radial-gradient(circle,currentColor 1%,#0000 1%) 50%/10000% 10000% no-repeat}.vot-text-button:hover:before{opacity:.04}.vot-text-button:active:after{opacity:.16;background-size:100% 100%;transition:background-size}.vot-text-button[disabled=true]{color:rgba(var(--vot-onsurface-rgb,0, 0, 0),.38);cursor:initial;background-color:#0000}.vot-text-button[disabled=true]:before,.vot-text-button[disabled=true]:after{opacity:0}.vot-icon-button{--vot-helper-onsurface:rgba(var(--vot-onsurface-rgb,0, 0, 0), .87);box-sizing:border-box;vertical-align:middle;text-align:center;text-overflow:ellipsis;cursor:pointer;width:36px;min-width:36px;height:36px;fill:var(--vot-helper-onsurface);color:var(--vot-helper-onsurface);background-color:#0000;outline:none;font-size:14px;line-height:36px;display:inline-block;position:relative;font-family:var(--vot-font-family,"Roboto", "Segoe UI", system-ui, sans-serif)!important;border:none!important;border-radius:50%!important;margin:0!important;padding:0!important;font-weight:500!important}.vot-icon-button:before,.vot-icon-button:after{content:"";opacity:0;position:absolute;inset:0;border-radius:inherit!important}.vot-icon-button:before{background-color:var(--vot-helper-onsurface);transition:opacity var(--vot-duration-medium) var(--vot-easing-standard)}.vot-icon-button:after{transition:opacity var(--vot-duration-slow) var(--vot-easing-standard),background-size var(--vot-duration-slow) var(--vot-easing-standard);background:radial-gradient(circle,currentColor 1%,#0000 1%) 50%/10000% 10000% no-repeat}.vot-icon-button:hover:before{opacity:.04}.vot-icon-button:active:after{opacity:.32;background-size:100% 100%;transition:background-size}.vot-icon-button[disabled=true]{color:rgba(var(--vot-onsurface-rgb,0, 0, 0),.38);fill:rgba(var(--vot-onsurface-rgb,0, 0, 0),.38);cursor:initial;background-color:#0000}.vot-icon-button[disabled=true]:before,.vot-icon-button[disabled=true]:after{opacity:0}.vot-icon-button svg{fill:inherit;stroke:inherit;width:24px;height:36px}.vot-hotkey{justify-content:flex-start;align-items:center;gap:var(--vot-space-3,12px);flex-wrap:wrap;display:flex}.vot-hotkey-label{word-break:break-word;max-width:80%}.vot-hotkey-button{--vot-helper-theme:var(--vot-theme-rgb,var(--vot-primary-rgb,33, 150, 243));box-sizing:border-box;vertical-align:middle;text-align:center;text-overflow:ellipsis;cursor:pointer;background-color:#0000;outline:none;width:fit-content;min-width:32px;height:fit-content;font-size:15px;line-height:1.5;display:inline-block;position:relative;border-radius:var(--vot-radius-s)!important;padding:0 var(--vot-space-2)!important;font-family:var(--vot-font-family,"Roboto", "Segoe UI", system-ui, sans-serif)!important;border:solid 1px var(--vot-border-color)!important;margin:0!important;font-weight:400!important}.vot-hotkey-button:before,.vot-hotkey-button:after{content:"";opacity:0;position:absolute;inset:0;border-radius:inherit!important}.vot-hotkey-button:before{background-color:rgb(var(--vot-helper-theme));transition:opacity var(--vot-duration-medium) var(--vot-easing-standard)}.vot-hotkey-button:after{transition:opacity var(--vot-duration-slow) var(--vot-easing-standard),background-size var(--vot-duration-slow) var(--vot-easing-standard);background:radial-gradient(circle,currentColor 1%,#0000 1%) 50%/10000% 10000% no-repeat}.vot-hotkey-button:hover:before{opacity:.04}.vot-hotkey-button:active:after{opacity:.16;background-size:100% 100%;transition:background-size}.vot-hotkey-button[data-status=active]{color:rgb(var(--vot-helper-theme))}.vot-hotkey-button[data-status=active]:before{opacity:.04}.vot-hotkey-button[disabled=true]{color:rgba(var(--vot-onsurface-rgb,0, 0, 0),.38);cursor:initial;background-color:#0000}.vot-hotkey-button[disabled=true]:before,.vot-hotkey-button[disabled=true]:after{opacity:0}.vot-textfield{display:inline-block;--vot-helper-theme:rgb(var(--vot-theme-rgb,var(--vot-primary-rgb,33, 150, 243)))!important;--vot-helper-safari1:rgba(var(--vot-onsurface-rgb,0, 0, 0), .38)!important;--vot-helper-safari2:rgba(var(--vot-onsurface-rgb,0, 0, 0), .6)!important;--vot-helper-safari3:rgba(var(--vot-onsurface-rgb,0, 0, 0), .87)!important;font-family:var(--vot-font-family,"Roboto", "Segoe UI", system-ui, sans-serif)!important;text-align:start!important;padding-top:6px!important;font-size:16px!important;line-height:1.5!important;position:relative!important}.vot-textfield>:is(input,textarea){box-sizing:border-box!important;border-style:solid!important;border-width:1px!important;border-color:transparent var(--vot-helper-safari2) var(--vot-helper-safari2)!important;width:100%!important;height:inherit!important;color:rgba(var(--vot-onsurface-rgb,0, 0, 0),.87)!important;-webkit-text-fill-color:currentColor!important;font-family:inherit!important;font-size:inherit!important;line-height:inherit!important;caret-color:var(--vot-helper-theme)!important;background-color:#0000!important;border-radius:4px!important;margin:0!important;padding:15px 13px!important;transition:border .2s,box-shadow .2s!important;box-shadow:inset 1px 0 #0000,inset -1px 0 #0000,inset 0 -1px #0000!important}.vot-textfield>:is(input,textarea):not(:focus):not(:is(.vot-show-placeholder,.vot-show-placeholer))::placeholder{color:#0000!important}.vot-textfield>:is(input,textarea):not(:focus):placeholder-shown{border-top-color:var(--vot-helper-safari2)!important}.vot-textfield>:is(input,textarea)+span{font-family:inherit;width:100%!important;max-height:100%!important;color:rgba(var(--vot-onsurface-rgb,0, 0, 0),.6)!important;cursor:text!important;pointer-events:none!important;font-size:75%!important;line-height:15px!important;transition:color .2s,font-size .2s,line-height .2s!important;display:flex!important;position:absolute!important;top:0!important;left:0!important}.vot-textfield>:is(input,textarea):not(:focus):placeholder-shown+span{font-size:inherit!important;line-height:68px!important}.vot-textfield>input+span:before,.vot-textfield>input+span:after,.vot-textfield>textarea+span:before,.vot-textfield>textarea+span:after{content:""!important;box-sizing:border-box!important;border-top:solid 1px var(--vot-helper-safari2)!important;pointer-events:none!important;min-width:10px!important;height:8px!important;margin-top:6px!important;transition:border .2s,box-shadow .2s!important;display:block!important;box-shadow:inset 0 1px #0000!important}.vot-textfield>input+span:before,.vot-textfield>textarea+span:before{border-left:1px solid #0000!important;border-radius:4px 0!important;margin-right:4px!important}.vot-textfield>input+span:after,.vot-textfield>textarea+span:after{border-right:1px solid #0000!important;border-radius:0 4px!important;flex-grow:1!important;margin-left:4px!important}.vot-textfield>input:is(.vot-show-placeholder,.vot-show-placeholer)+span:before,.vot-textfield>textarea:is(.vot-show-placeholder,.vot-show-placeholer)+span:before{margin-right:0!important}.vot-textfield>input:is(.vot-show-placeholder,.vot-show-placeholer)+span:after,.vot-textfield>textarea:is(.vot-show-placeholder,.vot-show-placeholer)+span:after{margin-left:0!important}.vot-textfield>input:not(:focus):placeholder-shown+span:before,.vot-textfield>input:not(:focus):placeholder-shown+span:after,.vot-textfield>textarea:not(:focus):placeholder-shown+span:before,.vot-textfield>textarea:not(:focus):placeholder-shown+span:after{border-top-color:#0000!important}.vot-textfield:hover>input:not(:disabled),.vot-textfield:hover>textarea:not(:disabled){border-color:transparent var(--vot-helper-safari3) var(--vot-helper-safari3)!important}.vot-textfield:hover>input:not(:disabled)+span:before,.vot-textfield:hover>input:not(:disabled)+span:after,.vot-textfield:hover>textarea:not(:disabled)+span:before,.vot-textfield:hover>textarea:not(:disabled)+span:after{border-top-color:var(--vot-helper-safari3)!important}.vot-textfield:hover>input:not(:disabled):not(:focus):placeholder-shown,.vot-textfield:hover>textarea:not(:disabled):not(:focus):placeholder-shown{border-color:var(--vot-helper-safari3)!important}.vot-textfield>input:focus,.vot-textfield>textarea:focus{border-color:transparent var(--vot-helper-theme) var(--vot-helper-theme)!important;box-shadow:inset 1px 0 var(--vot-helper-theme),inset -1px 0 var(--vot-helper-theme),inset 0 -1px var(--vot-helper-theme)!important;outline:none!important}.vot-textfield>input:focus+span,.vot-textfield>textarea:focus+span{color:var(--vot-helper-theme)!important}.vot-textfield>input:focus+span:before,.vot-textfield>input:focus+span:after,.vot-textfield>textarea:focus+span:before,.vot-textfield>textarea:focus+span:after{border-top-color:var(--vot-helper-theme)!important;box-shadow:inset 0 1px var(--vot-helper-theme)!important}.vot-textfield>input:disabled,.vot-textfield>input:disabled+span,.vot-textfield>textarea:disabled,.vot-textfield>textarea:disabled+span{border-color:transparent var(--vot-helper-safari1) var(--vot-helper-safari1)!important;color:rgba(var(--vot-onsurface-rgb,0, 0, 0),.38)!important;pointer-events:none!important}.vot-textfield>input:disabled+span:before,.vot-textfield>input:disabled+span:after,.vot-textfield>textarea:disabled+span:before,.vot-textfield>textarea:disabled+span:after,.vot-textfield>input:disabled:placeholder-shown,.vot-textfield>input:disabled:placeholder-shown+span,.vot-textfield>textarea:disabled:placeholder-shown,.vot-textfield>textarea:disabled:placeholder-shown+span{border-top-color:var(--vot-helper-safari1)!important}.vot-textfield>input:disabled:placeholder-shown+span:before,.vot-textfield>input:disabled:placeholder-shown+span:after,.vot-textfield>textarea:disabled:placeholder-shown+span:before,.vot-textfield>textarea:disabled:placeholder-shown+span:after{border-top-color:#0000!important}@media not all and (min-resolution:.001dpcm){@supports ((-webkit-appearance:none)){.vot-textfield>input,.vot-textfield>input+span,.vot-textfield>textarea,.vot-textfield>textarea+span,.vot-textfield>input+span:before,.vot-textfield>input+span:after,.vot-textfield>textarea+span:before,.vot-textfield>textarea+span:after{transition-duration:.1s!important}}}.vot-checkbox{--vot-checkbox-label-offset:30px;--vot-helper-theme:var(--vot-theme-rgb,var(--vot-primary-rgb,33, 150, 243));--vot-helper-ontheme:var(--vot-ontheme-rgb,var(--vot-onprimary-rgb,255, 255, 255));z-index:0;color:rgba(var(--vot-onsurface-rgb,0, 0, 0),.87);text-align:start;font-size:16px;line-height:1.5;display:inline-block;position:relative;font-family:var(--vot-font-family,"Roboto", "Segoe UI", system-ui, sans-serif)!important;text-transform:none!important}.vot-checkbox-sub{padding-left:var(--vot-checkbox-label-offset)!important}.vot-checkbox>input{appearance:none;z-index:10000;box-sizing:border-box;opacity:1;cursor:pointer;background:0 0;outline:none;width:18px;height:18px;transition:border-color .2s,background-color .2s;display:block;position:absolute;border:2px solid!important;border-color:rgba(var(--vot-onsurface-rgb,0, 0, 0),.6)!important;border-radius:2px!important;margin:3px 1px!important;padding:0!important}.vot-checkbox>input+span{box-sizing:border-box;width:inherit;cursor:pointer;font-family:inherit;display:inline-block;position:relative;padding-left:var(--vot-checkbox-label-offset)!important;font-weight:400!important}.vot-checkbox>input+span:before{content:"";background-color:rgb(var(--vot-onsurface-rgb,0, 0, 0));opacity:0;pointer-events:none;width:40px;height:40px;transition:opacity .3s,transform .2s;display:block;position:absolute;top:-8px;left:-10px;transform:scale(1);border-radius:50%!important}.vot-checkbox>input+span:after{content:"";z-index:10000;pointer-events:none;width:10px;height:5px;transition:border-color .2s;display:block;position:absolute;top:3px;left:1px;transform:translate(3px,4px)rotate(-45deg);box-sizing:content-box!important;border:0 solid #0000!important;border-width:0 0 2px 2px!important}.vot-checkbox>input:checked,.vot-checkbox>input:indeterminate{background-color:rgb(var(--vot-helper-theme));border-color:rgb(var(--vot-helper-theme))!important}.vot-checkbox>input:checked+span:before,.vot-checkbox>input:indeterminate+span:before{background-color:rgb(var(--vot-helper-theme))}.vot-checkbox>input:checked+span:after,.vot-checkbox>input:indeterminate+span:after{border-color:rgb(var(--vot-helper-ontheme,255, 255, 255))!important}.vot-checkbox>input:hover{box-shadow:none!important}.vot-checkbox>input:indeterminate+span:after{transform:translate(4px,3px);border-left-width:0!important}.vot-checkbox:hover>input+span:before{opacity:.04}.vot-checkbox:active>input,.vot-checkbox:active:hover>input:not(:disabled){border-color:rgb(var(--vot-helper-theme))!important}.vot-checkbox:active>input:checked{background-color:rgba(var(--vot-onsurface-rgb,0, 0, 0),.6);border-color:#0000!important}.vot-checkbox:active>input+span:before{opacity:1;transition:transform,opacity;transform:scale(0)}.vot-checkbox>input:disabled{cursor:initial;border-color:rgba(var(--vot-onsurface-rgb,0, 0, 0),.38)!important}.vot-checkbox>input:disabled:checked,.vot-checkbox>input:disabled:indeterminate{background-color:rgba(var(--vot-onsurface-rgb,0, 0, 0),.38);border-color:#0000!important}.vot-checkbox>input:disabled+span{color:rgba(var(--vot-onsurface-rgb,0, 0, 0),.38);cursor:initial}.vot-checkbox>input:disabled+span:before{opacity:0;transform:scale(0)}html.vot-keyboard-nav .vot-checkbox>input:focus-visible{box-shadow:var(--vot-focus-ring),var(--vot-focus-ring-offset)!important}@supports not selector(:focus-visible){html.vot-keyboard-nav .vot-checkbox>input:focus{box-shadow:var(--vot-focus-ring),var(--vot-focus-ring-offset)!important}}.vot-slider{flex-direction:column;gap:6px;display:flex;width:100%!important;color:rgba(var(--vot-onsurface-rgb,0, 0, 0),.87)!important;font-family:var(--vot-font-family,"Roboto", "Segoe UI", BlinkMacSystemFont, system-ui, -apple-system)!important;text-align:start!important;font-size:16px!important;line-height:1.5!important}.vot-slider>span{order:1;margin:0!important;display:block!important}.vot-slider .vot-slider-label{flex-wrap:wrap;align-items:baseline;gap:6px;width:100%;display:inline-flex}.vot-slider-label-value{font-variant-numeric:tabular-nums;margin-left:0!important;font-weight:500!important}.vot-slider .vot-slider-label-text{min-width:0}.vot-slider>input{order:2;appearance:none!important;cursor:pointer!important;background-color:#0000!important;border:none!important;width:100%!important;height:32px!important;margin:0!important;padding:0!important;display:block!important;position:relative!important;top:0!important}.vot-slider>input:hover{box-shadow:none!important}.vot-slider>input:before{content:""!important;width:calc(100% * var(--vot-progress,0))!important;background:rgb(var(--vot-primary-rgb,33, 150, 243))!important;height:2px!important;display:block!important;position:absolute!important;top:calc(50% - 1px)!important}.vot-slider>input:disabled{cursor:default!important;opacity:.38!important}.vot-slider>input:disabled+span{color:rgba(var(--vot-onsurface-rgb,0, 0, 0),.38)!important}.vot-slider>input:disabled::-webkit-slider-runnable-track{background-color:rgba(var(--vot-onsurface-rgb,0, 0, 0),.38)!important}.vot-slider>input:disabled::-moz-range-track{background-color:rgba(var(--vot-onsurface-rgb,0, 0, 0),.38)!important}.vot-slider>input:disabled::-webkit-slider-thumb{background-color:rgb(var(--vot-onsurface-rgb,0, 0, 0))!important;box-shadow:0 0 0 1px rgb(var(--vot-surface-rgb,255, 255, 255))!important;transform:scale(4)!important}.vot-slider>input:disabled::-moz-range-thumb{background-color:rgb(var(--vot-onsurface-rgb,0, 0, 0))!important;box-shadow:0 0 0 1px rgb(var(--vot-surface-rgb,255, 255, 255))!important;transform:scale(4)!important}.vot-slider>input:disabled::-moz-range-progress{background-color:rgba(var(--vot-onsurface-rgb,0, 0, 0),.87)!important}.vot-slider>input:focus{outline:none!important}.vot-slider>input::-webkit-slider-runnable-track{background-color:rgba(var(--vot-primary-rgb,33, 150, 243),.24)!important;border-radius:1px!important;width:100%!important;height:2px!important;margin:15px 0!important}.vot-slider>input::-moz-range-track{background-color:rgba(var(--vot-primary-rgb,33, 150, 243),.24)!important;border-radius:1px!important;width:100%!important;height:2px!important;margin:15px 0!important}.vot-slider>input::-webkit-slider-thumb{appearance:none!important;background-color:rgb(var(--vot-primary-rgb,33, 150, 243))!important;width:2px!important;height:2px!important;box-shadow:none!important;border:none!important;border-radius:50%!important;transition:box-shadow .2s!important;transform:scale(6)!important}.vot-slider>input::-moz-range-thumb{appearance:none!important;background-color:rgb(var(--vot-primary-rgb,33, 150, 243))!important;width:2px!important;height:2px!important;box-shadow:none!important;border:none!important;border-radius:50%!important;transition:box-shadow .2s!important;transform:scale(6)!important}.vot-slider>input::-webkit-slider-thumb{-webkit-appearance:none!important;margin:0!important}.vot-slider>input::-moz-range-progress{background-color:rgb(var(--vot-primary-rgb,33, 150, 243))!important;border-radius:1px!important;height:2px!important}.vot-slider>input:focus:not(:focus-visible)::-webkit-slider-thumb{box-shadow:none!important}.vot-slider>input:focus:not(:focus-visible)::-moz-range-thumb{box-shadow:none!important}html.vot-keyboard-nav .vot-slider>input:focus-visible::-webkit-slider-thumb{box-shadow:0 0 0 2px rgba(var(--vot-primary-rgb,33, 150, 243),.24)!important}html.vot-keyboard-nav .vot-slider>input:focus-visible::-moz-range-thumb{box-shadow:0 0 0 2px rgba(var(--vot-primary-rgb,33, 150, 243),.24)!important}@supports not selector(:focus-visible){html.vot-keyboard-nav .vot-slider>input:focus::-webkit-slider-thumb{box-shadow:0 0 0 2px rgba(var(--vot-primary-rgb,33, 150, 243),.24)!important}html.vot-keyboard-nav .vot-slider>input:focus::-moz-range-thumb{box-shadow:0 0 0 2px rgba(var(--vot-primary-rgb,33, 150, 243),.24)!important}}.vot-select{--vot-helper-theme-rgb:var(--vot-onsurface-rgb,0, 0, 0);--vot-helper-theme:rgba(var(--vot-helper-theme-rgb), .87);--vot-helper-safari1:rgba(var(--vot-onsurface-rgb,0, 0, 0), .6);--vot-helper-safari2:rgba(var(--vot-onsurface-rgb,0, 0, 0), .87);font-family:var(--vot-font-family,"Roboto", "Segoe UI", system-ui, sans-serif);text-align:start;color:var(--vot-helper-theme);fill:var(--vot-helper-theme);justify-content:space-between;align-items:center;font-size:14px;line-height:1.5;display:flex;font-weight:400!important}.vot-select-outer{cursor:pointer;justify-content:space-between;align-items:center;width:120px;max-width:120px;display:flex;border:1px solid var(--vot-helper-safari1)!important;border-radius:4px!important;padding:0 5px!important;transition:border .2s!important}.vot-select-outer:hover{border-color:var(--vot-helper-safari2)!important}.vot-select-outer[disabled=true]{opacity:.5;cursor:default}.vot-select-outer[disabled=true]:hover{border-color:var(--vot-helper-safari1)!important}.vot-select-title{text-overflow:ellipsis;white-space:nowrap;font-family:inherit;overflow:hidden}.vot-select-arrow-icon{justify-content:center;align-items:center;width:20px;height:32px;display:flex}.vot-select-arrow-icon svg{fill:inherit;stroke:inherit}.vot-select-content-list{flex-direction:column;display:flex}.vot-select-content-list .vot-select-content-item{cursor:pointer;border-radius:8px!important;padding:5px 10px!important}.vot-select-content-list .vot-select-content-item:not([inert]):hover{background-color:#2a2c31}.vot-select-content-list .vot-select-content-item[data-vot-selected=true]{color:rgb(var(--vot-primary-rgb,33, 150, 243));background-color:rgba(var(--vot-primary-rgb,33, 150, 243),.2)}.vot-select-content-list .vot-select-content-item[data-vot-selected=true]:hover{background-color:rgba(var(--vot-primary-rgb,33, 150, 243),.1)!important}.vot-select-content-list .vot-select-content-item[inert]{cursor:default;color:rgba(var(--vot-onsurface-rgb,0, 0, 0),.38)}.vot-header{color:rgba(var(--vot-helper-onsurface-rgb),.87);font-family:var(--vot-font-family,"Roboto", "Segoe UI", system-ui, sans-serif);text-align:start;line-height:1.5;font-weight:700!important}.vot-header:not(:first-child){padding-top:8px}.vot-header-level-1{font-size:2em}.vot-header-level-2{font-size:1.5em}.vot-header-level-3{font-size:1.17em}.vot-header-level-4{font-size:1em}.vot-header-level-5{font-size:.83em}.vot-header-level-6{font-size:.67em}.vot-info{color:rgba(var(--vot-helper-onsurface-rgb),.87);font-family:var(--vot-font-family,"Roboto", "Segoe UI", system-ui, sans-serif);text-align:start;-webkit-user-select:text;user-select:text;font-size:16px;line-height:1.5;display:flex}.vot-info>:not(:first-child){color:rgba(var(--vot-helper-onsurface-rgb),.5);flex:1;margin-left:8px!important}.vot-details{color:rgba(var(--vot-helper-onsurface-rgb),.87);font-family:var(--vot-font-family,"Roboto", "Segoe UI", system-ui, sans-serif);text-align:start;cursor:pointer;transition:background var(--vot-duration-medium) var(--vot-easing-standard);justify-content:space-between;align-items:center;font-size:16px;line-height:1.5;display:flex;border-radius:.5em!important;margin:-.5em!important;padding:.5em!important}.vot-details-arrow-icon{width:20px;height:32px;fill:rgba(var(--vot-helper-onsurface-rgb),.87);justify-content:center;align-items:center;display:flex;transform:scale(1.25)rotate(-90deg)}.vot-details:hover{background:rgba(var(--vot-onsurface-rgb,0, 0, 0),.06)}.vot-settings-section{border:1px solid var(--vot-border-color);border-radius:var(--vot-radius-l);padding:var(--vot-space-2);background:rgba(var(--vot-helper-onsurface-rgb),.03);flex-direction:column;display:flex}.vot-settings-section>*{margin:0!important}.vot-settings-section>*+*{margin-top:var(--vot-space-2)!important}.vot-settings-section-header{border-radius:var(--vot-radius-m);margin:0!important;padding:.45em .5em!important}.vot-settings-section-header .vot-details-arrow-icon{transition:transform var(--vot-duration-medium) var(--vot-easing-standard)}.vot-settings-section-header[data-open=true] .vot-details-arrow-icon{transform:scale(1.25)rotate(0)}.vot-settings-section-content{--vot-settings-control-width:200px;--vot-settings-row-gap:var(--vot-space-2);padding:0 var(--vot-space-1) var(--vot-space-1);flex-direction:column;display:flex}.vot-settings-section-content>*{margin:0!important}.vot-settings-section-content>*+*{margin-top:var(--vot-settings-row-gap)!important}.vot-settings-section-content>.vot-checkbox,.vot-settings-section-content>.vot-hotkey,.vot-settings-section-content>.vot-textfield,.vot-settings-section-content>.vot-select,.vot-settings-section-content>.vot-slider{padding:var(--vot-space-1);box-sizing:border-box;width:100%!important}.vot-settings-section-content>.vot-textfield{gap:var(--vot-space-1);flex-direction:column;padding-top:0!important;display:flex!important}.vot-settings-section-content>.vot-textfield>span{order:0;width:auto!important;max-height:none!important;color:rgba(var(--vot-helper-onsurface-rgb),.72)!important;cursor:default!important;pointer-events:none!important;font-size:13px!important;line-height:1.2!important;display:block!important;position:static!important}.vot-settings-section-content>.vot-textfield>span:before,.vot-settings-section-content>.vot-textfield>span:after{content:none!important;display:none!important}.vot-settings-section-content>.vot-textfield>input,.vot-settings-section-content>.vot-textfield>textarea{transition:border-color var(--vot-duration-fast) var(--vot-easing-standard),background-color var(--vot-duration-fast) var(--vot-easing-standard);order:1;width:100%!important;height:36px!important;padding:0 var(--vot-space-3)!important;border:1px solid var(--vot-border-color)!important;border-radius:var(--vot-radius-s)!important;background:rgba(var(--vot-helper-onsurface-rgb),.04)!important;color:rgba(var(--vot-helper-onsurface-rgb),.9)!important;-webkit-text-fill-color:currentColor!important;box-shadow:none!important}.vot-settings-section-content>.vot-textfield>textarea{resize:vertical;height:auto!important;min-height:84px!important;padding:var(--vot-space-2) var(--vot-space-3)!important}.vot-settings-section-content>.vot-textfield>input::placeholder,.vot-settings-section-content>.vot-textfield>textarea::placeholder{color:rgba(var(--vot-helper-onsurface-rgb),.55)!important}.vot-settings-section-content>.vot-textfield:hover>input,.vot-settings-section-content>.vot-textfield:hover>textarea{border-color:var(--vot-border-color-hover)!important}.vot-settings-section-content>.vot-textfield>input:not(:focus):placeholder-shown,.vot-settings-section-content>.vot-textfield>textarea:not(:focus):placeholder-shown{border-color:var(--vot-border-color)!important}.vot-settings-section-content>.vot-textfield>input:focus,.vot-settings-section-content>.vot-textfield>textarea:focus{border-color:rgba(var(--vot-primary-rgb),.7)!important}.vot-lang-select{--vot-helper-theme-rgb:var(--vot-onsurface-rgb,0, 0, 0);--vot-helper-theme:rgba(var(--vot-helper-theme-rgb), .87);color:var(--vot-helper-theme);fill:var(--vot-helper-theme);justify-content:space-between;align-items:center;display:flex}.vot-lang-select-icon{justify-content:center;align-items:center;width:32px;height:32px;display:flex}.vot-lang-select-icon svg{fill:inherit;stroke:inherit}.vot-segmented-button{--vot-helper-theme-rgb:var(--vot-onsurface-rgb,0, 0, 0);--vot-helper-theme:rgba(var(--vot-helper-theme-rgb), .87);-webkit-user-select:none;user-select:none;background:rgb(var(--vot-surface-rgb,255, 255, 255));color:var(--vot-helper-theme);fill:var(--vot-helper-theme);cursor:default;transition:opacity var(--vot-duration-slow) var(--vot-easing-standard);z-index:2147483647;align-items:center;font-size:16px;line-height:1.5;display:flex;position:absolute;top:5rem;left:50%;overflow:hidden;transform:translate(-50%);opacity:1!important;pointer-events:auto!important;touch-action:none!important;border:1px solid var(--vot-border-color)!important;border-radius:var(--vot-radius-s)!important;width:fit-content!important;min-width:0!important;max-width:calc(100vw - 24px)!important;height:36px!important;min-height:36px!important;max-height:none!important;box-shadow:var(--vot-shadow-1)!important;font-family:var(--vot-font-family,"Roboto", "Segoe UI", system-ui, sans-serif)!important;flex:none!important;align-self:auto!important}.vot-segmented-button.vot-segmented-button--hidden{opacity:0!important;pointer-events:none!important}.vot-segmented-button *{box-sizing:border-box!important}.vot-segmented-button .vot-separator{background:rgba(var(--vot-helper-theme-rgb),.1);width:1px;height:50%}.vot-segmented-button .vot-segment,.vot-segmented-button .vot-segment-only-icon{color:inherit;transition:background-color var(--vot-duration-fast) var(--vot-easing-standard);-webkit-tap-highlight-color:transparent;background-color:#0000;outline:none;justify-content:center;align-items:center;display:flex;position:relative;overflow:hidden;height:100%!important;padding:0 var(--vot-space-2)!important;border:none!important;flex:none!important;align-self:auto!important}.vot-segmented-button .vot-segment:focus,.vot-segmented-button .vot-segment-only-icon:focus{box-shadow:inset 0 0 0 2px var(--vot-focus-ring-color);outline:none}.vot-segmented-button .vot-segment:focus:not(:focus-visible),.vot-segmented-button .vot-segment-only-icon:focus:not(:focus-visible){box-shadow:none}.vot-segmented-button .vot-segment:before,.vot-segmented-button .vot-segment-only-icon:before,.vot-segmented-button .vot-segment:after,.vot-segmented-button .vot-segment-only-icon:after{content:"";opacity:0;position:absolute;inset:0;border-radius:inherit!important}.vot-segmented-button .vot-segment:before,.vot-segmented-button .vot-segment-only-icon:before{background-color:rgb(var(--vot-helper-theme-rgb));transition:opacity var(--vot-duration-medium) var(--vot-easing-standard)}.vot-segmented-button .vot-segment:after,.vot-segmented-button .vot-segment-only-icon:after{transition:opacity var(--vot-duration-slow) var(--vot-easing-standard),background-size var(--vot-duration-slow) var(--vot-easing-standard);background:radial-gradient(circle,currentColor 1%,#0000 1%) 50%/10000% 10000% no-repeat}.vot-segmented-button .vot-segment:hover:before,.vot-segmented-button .vot-segment-only-icon:hover:before{opacity:.04}.vot-segmented-button .vot-segment:active:after,.vot-segmented-button .vot-segment-only-icon:active:after{opacity:.16;background-size:100% 100%;transition:background-size}.vot-segmented-button .vot-segment-only-icon{min-width:36px;padding:0!important}.vot-segmented-button .vot-segment-label{white-space:nowrap;color:inherit;margin-left:var(--vot-space-2)!important;font-weight:400!important}.vot-segmented-button[data-status=success] .vot-translate-button{color:rgb(var(--vot-primary-rgb,33, 150, 243));fill:rgb(var(--vot-primary-rgb,33, 150, 243))}.vot-segmented-button[data-status=error] .vot-translate-button{color:#f28b82;fill:#f28b82}.vot-segmented-button[data-status=disabled] .vot-translate-button{color:rgba(var(--vot-helper-theme-rgb),.56);fill:rgba(var(--vot-helper-theme-rgb),.56)}.vot-segmented-button[data-loading=true] #vot-loading-icon{display:block!important}.vot-segmented-button[data-loading=true] #vot-translate-icon{display:none!important}.vot-segmented-button[data-direction=column]{flex-direction:column;height:fit-content!important;min-height:0!important}.vot-segmented-button[data-direction=column] .vot-segment-label{display:none}.vot-segmented-button[data-direction=column]>.vot-segment-only-icon,.vot-segmented-button[data-direction=column]>.vot-segment{padding:8px!important}.vot-segmented-button[data-direction=column] .vot-separator{width:50%;height:1px}.vot-segmented-button[data-position=left]{top:12.5vh;left:50px}.vot-segmented-button[data-position=right]{top:12.5vh;left:auto;right:0}.vot-segmented-button svg{width:24px;fill:inherit;stroke:inherit}@keyframes vot-rail-pulse{0%,to{box-shadow:0 0 rgba(var(--vot-primary-rgb,139, 180, 245),.16),var(--vot-shadow-1)}50%{box-shadow:0 0 0 8px rgba(var(--vot-primary-rgb,139, 180, 245),0),var(--vot-shadow-2)}}@keyframes vot-voice-wave{0%,to{transform:scaleY(.58)}50%{transform:scaleY(1)}}@keyframes vot-rail-spin{0%{transform:rotate(0)}to{transform:rotate(360deg)}}.vot-rail{--vot-helper-theme-rgb:var(--vot-onsurface-rgb,227, 227, 227);--vot-helper-theme:rgba(var(--vot-helper-theme-rgb), .92);--vot-rail-primary-rgb:var(--vot-primary-rgb,139, 180, 245);gap:var(--vot-space-2);padding:var(--vot-space-2);background:linear-gradient(180deg,rgba(var(--vot-surface-rgb,32, 33, 36),.96),rgba(var(--vot-surface-rgb,32, 33, 36),.92));color:var(--vot-helper-theme);fill:var(--vot-helper-theme);z-index:2147483647;-webkit-user-select:none;user-select:none;transition:opacity var(--vot-duration-slow) var(--vot-easing-standard),transform var(--vot-duration-medium) var(--vot-easing-standard);flex-direction:column;display:flex;position:absolute;opacity:1!important;pointer-events:auto!important;touch-action:none!important;border:1px solid var(--vot-border-color)!important;width:fit-content!important;min-width:0!important;max-width:calc(100vw - 24px)!important;height:fit-content!important;min-height:0!important;max-height:none!important;box-shadow:var(--vot-shadow-1)!important;font-family:var(--vot-font-family,"Roboto", "Segoe UI", system-ui, sans-serif)!important;border-radius:22px!important;flex:none!important;align-self:auto!important}.vot-rail.vot-rail--hidden{transform:translateY(4px)scale(.98);opacity:0!important;pointer-events:none!important}.vot-rail *{box-sizing:border-box!important}.vot-rail .vot-rail-button{color:inherit;fill:inherit;-webkit-tap-highlight-color:transparent;transition:background-color var(--vot-duration-fast) var(--vot-easing-standard),color var(--vot-duration-fast) var(--vot-easing-standard),transform var(--vot-duration-fast) var(--vot-easing-standard);background:0 0;outline:none;justify-content:center;align-items:center;display:flex;position:relative;border:none!important;border-radius:16px!important;flex:none!important;align-self:auto!important;width:42px!important;min-width:42px!important;max-width:42px!important;height:42px!important;min-height:42px!important;max-height:42px!important}.vot-rail .vot-rail-button:focus{box-shadow:inset 0 0 0 2px var(--vot-focus-ring-color);outline:none}.vot-rail .vot-rail-button:focus:not(:focus-visible){box-shadow:none}.vot-rail .vot-rail-button:before,.vot-rail .vot-rail-button:after{content:"";opacity:0;position:absolute;inset:0;border-radius:inherit!important}.vot-rail .vot-rail-button:before{background-color:rgb(var(--vot-helper-theme-rgb));transition:opacity var(--vot-duration-medium) var(--vot-easing-standard)}.vot-rail .vot-rail-button:after{transition:opacity var(--vot-duration-slow) var(--vot-easing-standard),background-size var(--vot-duration-slow) var(--vot-easing-standard);background:radial-gradient(circle,currentColor 1%,#0000 1%) 50%/10000% 10000% no-repeat}.vot-rail .vot-rail-button:hover:before{opacity:.05}.vot-rail .vot-rail-button:active:after{opacity:.18;background-size:100% 100%;transition:background-size}.vot-rail .vot-rail-button:active{transform:scale(.96)}.vot-rail .vot-rail-translate-group{background:rgba(var(--vot-helper-theme-rgb),.08);border:1px solid rgba(var(--vot-helper-theme-rgb),.08);align-items:stretch;display:flex;overflow:hidden;border-radius:18px!important;flex:none!important;align-self:auto!important}.vot-rail .vot-rail-button-primary{gap:var(--vot-space-2);border-radius:18px!important;width:46px!important;min-width:46px!important;max-width:none!important;height:46px!important;min-height:46px!important}.vot-rail .vot-rail-button-primary-action{background:0 0;border-radius:0!important}.vot-rail .vot-rail-button-chevron{border-left:1px solid rgba(var(--vot-helper-theme-rgb),.12);background:0 0;justify-content:center;align-items:center;display:flex;border-radius:0!important;width:38px!important;min-width:38px!important;max-width:38px!important;height:46px!important;min-height:46px!important}.vot-rail .vot-translate-button .vot-rail-button-icon{color:rgba(var(--vot-onsurface-rgb,227, 227, 227),.72);fill:currentColor}.vot-rail .vot-translate-button .vot-rail-button-icon--voice{display:none}.vot-rail[data-status=success][data-voice-mode=lively] .vot-translate-button .vot-rail-button-icon,.vot-rail[data-loading=true][data-voice-mode=lively] .vot-translate-button .vot-rail-button-icon{color:#ff61b6}.vot-rail .vot-voice-wave-bar{transform-box:fill-box;transform-origin:bottom}.vot-rail .vot-voice-wave-bar-1{animation-delay:-.3s}.vot-rail .vot-voice-wave-bar-2{animation-delay:-.15s}.vot-rail .vot-voice-wave-bar-3{animation-delay:-.05s}.vot-rail .vot-voice-wave-bar-4{animation-delay:-.2s}.vot-rail .vot-rail-button-label,.vot-rail .vot-rail-button-chevron{display:none}.vot-rail .vot-rail-button-label{white-space:nowrap;font-size:14px;line-height:1.2}.vot-rail[data-position=default],.vot-rail[data-position=top]{flex-direction:row;align-items:center;padding:6px;border-radius:22px!important;min-height:52px!important}.vot-rail[data-position=default] .vot-rail-button-primary,.vot-rail[data-position=top] .vot-rail-button-primary{border-radius:0!important;width:auto!important;min-width:0!important;height:40px!important;min-height:40px!important;padding:0 12px!important}.vot-rail[data-position=default] .vot-rail-button-label,.vot-rail[data-position=default] .vot-rail-button-chevron,.vot-rail[data-position=top] .vot-rail-button-label,.vot-rail[data-position=top] .vot-rail-button-chevron{align-items:center;display:flex}.vot-rail[data-position=default] .vot-rail-button-chevron,.vot-rail[data-position=top] .vot-rail-button-chevron{justify-content:center;width:36px!important;min-width:36px!important;max-width:36px!important;height:40px!important;min-height:40px!important;padding:0!important}.vot-rail[data-position=default] .vot-rail-translate-group,.vot-rail[data-position=top] .vot-rail-translate-group{border-radius:14px!important}.vot-rail[data-position=left],.vot-rail[data-position=right],.vot-rail[data-position=left] .vot-rail-translate-group,.vot-rail[data-position=right] .vot-rail-translate-group{flex-direction:column}.vot-rail[data-position=left] .vot-rail-button-primary,.vot-rail[data-position=right] .vot-rail-button-primary{width:46px!important;min-width:46px!important;max-width:46px!important;height:46px!important;min-height:46px!important;padding:0!important}.vot-rail[data-position=left] .vot-rail-button-chevron,.vot-rail[data-position=right] .vot-rail-button-chevron{border-left:none;border-top:1px solid rgba(var(--vot-helper-theme-rgb),.12);width:46px!important;min-width:46px!important;max-width:46px!important;height:24px!important;min-height:24px!important}.vot-rail[data-status=success] .vot-translate-button,.vot-rail[data-status=success] .vot-rail-button-chevron{color:rgb(var(--vot-rail-primary-rgb));fill:rgb(var(--vot-rail-primary-rgb));background:rgba(var(--vot-rail-primary-rgb),.16)}.vot-rail[data-status=success] .vot-translate-button .vot-rail-button-icon--translate{display:none}.vot-rail[data-status=success] .vot-translate-button .vot-rail-button-icon--voice{display:flex}.vot-rail[data-loading=true][data-voice-mode=standard] .vot-translate-button,.vot-rail[data-loading=true][data-voice-mode=standard] .vot-rail-button-chevron{color:rgb(var(--vot-rail-primary-rgb));fill:rgb(var(--vot-rail-primary-rgb));background:rgba(var(--vot-rail-primary-rgb),.12)}.vot-rail[data-status=success][data-voice-mode=lively] .vot-translate-button,.vot-rail[data-status=success][data-voice-mode=lively] .vot-rail-button-chevron,.vot-rail[data-loading=true][data-voice-mode=lively] .vot-translate-button,.vot-rail[data-loading=true][data-voice-mode=lively] .vot-rail-button-chevron{color:#ff61b6;fill:#ff61b6;background:#ff61b629}.vot-rail[data-status=success][data-voice-playback-state=playing] .vot-translate-button{animation:1.6s ease-in-out infinite vot-rail-pulse}.vot-rail[data-status=success][data-voice-playback-state=playing] .vot-translate-button .vot-voice-wave-bar{animation:.9s ease-in-out infinite vot-voice-wave}.vot-rail[data-status=error] .vot-translate-button,.vot-rail[data-status=error] .vot-rail-button-chevron{color:#f28b82;fill:#f28b82;background:#f28b821f}.vot-rail[data-status=disabled] .vot-translate-button,.vot-rail[data-status=disabled] .vot-rail-button-chevron{color:rgba(var(--vot-helper-theme-rgb),.56);fill:rgba(var(--vot-helper-theme-rgb),.56);background:rgba(var(--vot-helper-theme-rgb),.06)}.vot-rail[data-loading=true] #vot-loading-icon{transform-box:fill-box;transform-origin:50%;animation:.95s linear infinite vot-rail-spin;display:block!important}.vot-rail[data-loading=true] #vot-translate-icon{display:none!important}.vot-rail[data-loading=true] .vot-translate-button .vot-rail-button-icon--voice{display:none}.vot-rail svg{width:22px;height:22px;fill:inherit;stroke:inherit}.vot-voice-mode-menu{width:min(360px,100vw - 24px)}.vot-voice-mode-menu .vot-menu-body-container{flex-direction:column;gap:0;display:flex}@keyframes vot-voice-menu-spin{0%{transform:rotate(0)}to{transform:rotate(360deg)}}.vot-voice-mode-menu-item{cursor:pointer;color:rgb(var(--vot-onsurface-rgb,227, 227, 227));grid-template-columns:24px 1fr 24px;align-items:center;gap:12px;padding:14px 0;display:grid;border-radius:0!important}.vot-voice-mode-menu-item:not(:last-child){border-bottom:1px solid var(--vot-border-color)}.vot-voice-mode-menu-item[data-selected=true]{background:rgba(var(--vot-primary-rgb,139, 180, 245),.18);box-shadow:inset 0 0 0 1px rgba(var(--vot-primary-rgb,139, 180, 245),.32)}.vot-voice-mode-menu-item[data-selected=true][data-mode=standard]{background:rgba(var(--vot-primary-rgb,139, 180, 245),.2)}.vot-voice-mode-menu-item[data-selected=true][data-mode=lively]{background:#ff61b633;box-shadow:inset 0 0 0 1px #ff61b647}.vot-voice-mode-menu-item[data-disabled=true]{opacity:.6}.vot-voice-mode-menu-item-icon,.vot-voice-mode-menu-item-check{fill:currentColor;justify-content:center;align-items:center;width:24px;height:24px;display:flex}.vot-voice-mode-menu-item-icon{color:rgba(var(--vot-onsurface-rgb,227, 227, 227),.64)}.vot-voice-mode-menu-item[data-selected=true] .vot-voice-mode-menu-item-icon{color:rgb(var(--vot-primary-rgb,139, 180, 245))}.vot-voice-mode-menu-item[data-mode=lively] .vot-voice-mode-menu-item-icon{color:#ff61b6}.vot-voice-mode-menu-item[data-selected=true][data-mode=standard] .vot-voice-mode-menu-item-title,.vot-voice-mode-menu-item[data-selected=true][data-mode=standard] .vot-voice-mode-menu-item-check{color:rgb(var(--vot-primary-rgb,139, 180, 245))}.vot-voice-mode-menu-item[data-selected=true][data-mode=standard] .vot-voice-mode-menu-item-description{color:rgba(var(--vot-primary-rgb,139, 180, 245),.88)}.vot-voice-mode-menu-item[data-selected=true][data-mode=lively] .vot-voice-mode-menu-item-title,.vot-voice-mode-menu-item[data-selected=true][data-mode=lively] .vot-voice-mode-menu-item-check{color:#ff61b6}.vot-voice-mode-menu-item[data-selected=true][data-mode=lively] .vot-voice-mode-menu-item-description{color:#ff61b6e0}.vot-voice-wave-bar{transform-box:fill-box;transform-origin:bottom}.vot-voice-wave-bar-1{animation-delay:-.3s}.vot-voice-wave-bar-2{animation-delay:-.15s}.vot-voice-wave-bar-3{animation-delay:-.05s}.vot-voice-wave-bar-4{animation-delay:-.2s}.vot-voice-mode-menu-item-content{flex-direction:column;gap:4px;min-width:0;display:flex}.vot-voice-mode-menu-item-title{font-size:16px;line-height:1.35;font-weight:500!important}.vot-voice-mode-menu-item-description{color:rgba(var(--vot-onsurface-rgb,227, 227, 227),.7);font-size:14px;line-height:1.35}.vot-voice-mode-menu-item-check{color:rgb(var(--vot-primary-rgb,139, 180, 245));opacity:0;transition:opacity var(--vot-duration-fast) var(--vot-easing-standard)}.vot-voice-mode-menu-item[data-selected=true] .vot-voice-mode-menu-item-check{opacity:1}.vot-voice-mode-menu-item[data-selected=true][data-loading=true] .vot-voice-mode-menu-item-check svg{display:none}.vot-voice-mode-menu-item[data-selected=true][data-loading=true] .vot-voice-mode-menu-item-check:before{content:"";border:2px solid;border-right-color:#0000;border-radius:50%;width:16px;height:16px;animation:.9s linear infinite vot-voice-menu-spin}.vot-voice-mode-menu-item[data-selected=true][data-playback-state=playing] .vot-voice-wave-bar{animation:.9s ease-in-out infinite vot-voice-wave}.vot-tooltip{--vot-helper-theme-rgb:var(--vot-onsurface-rgb,0, 0, 0);--vot-helper-theme:rgba(var(--vot-helper-theme-rgb), .87);--vot-helper-ondialog:rgb(var(--vot-ondialog-rgb,37, 38, 40));--vot-helper-border:rgb(var(--vot-tooltip-border,69, 69, 69));-webkit-user-select:none;user-select:none;background:rgb(var(--vot-surface-rgb,255, 255, 255));color:var(--vot-helper-theme);fill:var(--vot-helper-theme);cursor:default;z-index:2147483647;opacity:0;align-items:center;width:max-content;max-width:calc(100vw - 10px);height:max-content;font-size:14px;line-height:1.5;transition:opacity .5s;display:flex;position:absolute;inset:0;overflow:hidden;box-shadow:0 1px 3px #0000001f;font-family:var(--vot-font-family,"Roboto", "Segoe UI", system-ui, sans-serif)!important;border-radius:4px!important;padding:4px 8px!important}.vot-tooltip[data-trigger=click]{-webkit-user-select:text;user-select:text}.vot-tooltip.vot-tooltip-bordered{border:1px solid var(--vot-helper-border)}.vot-tooltip *{box-sizing:border-box!important;font-family:inherit!important}.vot-menu{--vot-helper-surface-rgb:var(--vot-surface-rgb,255, 255, 255);--vot-helper-surface:rgb(var(--vot-helper-surface-rgb));--vot-helper-onsurface-rgb:var(--vot-onsurface-rgb,0, 0, 0);--vot-helper-onsurface:rgba(var(--vot-helper-onsurface-rgb), .87);--vot-settings-control-width:clamp(120px, 45%, 200px);pointer-events:auto;-webkit-user-select:none;user-select:none;background-color:var(--vot-helper-surface);color:var(--vot-helper-onsurface);cursor:default;z-index:2147483646;visibility:visible;opacity:1;transform-origin:top;width:fit-content;min-width:320px;max-width:min(90vw,560px);transition:opacity var(--vot-duration-medium) var(--vot-easing-standard),transform var(--vot-duration-medium) var(--vot-easing-standard);font-size:16px;line-height:1.5;position:absolute;top:calc(5rem + 48px);left:50%;overflow:hidden;transform:translate(-50%)scale(1);border:1px solid var(--vot-border-color)!important;border-radius:var(--vot-radius-m)!important;box-shadow:var(--vot-shadow-2)!important;font-family:var(--vot-font-family,"Roboto", "Segoe UI", system-ui, sans-serif)!important}.vot-menu.vot-menu--fixed-anchor{transform-origin:0 0;transform:none}.vot-menu *{box-sizing:border-box!important}.vot-menu[hidden]{pointer-events:none;visibility:hidden;opacity:0;transform:translate(-50%,-4px)scale(.98);display:block!important}.vot-menu.vot-menu--fixed-anchor[hidden]{transform:translateY(-4px)scale(.98)}.vot-menu-content-wrapper{min-width:320px;min-height:100px;max-height:calc(var(--vot-container-height,75vh) - (5rem + 32px + 16px) * 2);flex-direction:column;display:flex;overflow:auto}.vot-menu-header-container{flex-shrink:0;align-items:center;min-height:31px;display:flex;padding-inline-end:var(--vot-space-2)!important}.vot-menu-header-container:empty{padding:0 0 16px!important}.vot-menu-header-container>.vot-icon-button{margin-inline-end:var(--vot-space-1)!important;margin-top:var(--vot-space-1)!important}.vot-menu-title-container{font-size:inherit;text-align:start;outline:0;flex:1;display:flex;font-weight:inherit!important;margin:0!important}.vot-menu-title{flex:1;font-size:16px;line-height:1;padding:var(--vot-space-4)!important;font-weight:500!important}.vot-menu-body-container{box-sizing:border-box;gap:var(--vot-space-2);overscroll-behavior:contain;flex-direction:column;min-height:1.375rem;display:flex;overflow:auto;padding:0 var(--vot-space-4)!important;scrollbar-color:rgba(var(--vot-helper-onsurface-rgb),.1) var(--vot-helper-surface)!important}.vot-menu-body-container::-webkit-scrollbar{background:var(--vot-helper-surface)!important;width:12px!important;height:12px!important}.vot-menu-body-container::-webkit-scrollbar-track{background:var(--vot-helper-surface)!important;width:12px!important;height:12px!important}.vot-menu-body-container::-webkit-scrollbar-thumb{border-radius:1ex;background:rgba(var(--vot-helper-onsurface-rgb),.1)!important;border:5px solid var(--vot-helper-surface)!important}.vot-menu-body-container::-webkit-scrollbar-thumb:hover{border-width:3px!important}.vot-menu-body-container::-webkit-scrollbar-corner{background:var(--vot-helper-surface)!important}.vot-menu-footer-container{flex-shrink:0;justify-content:flex-end;display:flex;padding:var(--vot-space-4)!important}.vot-menu-footer-container:empty{padding:var(--vot-space-4) 0 0 0!important}.vot-menu .vot-select--labeled>.vot-select-outer{margin-left:auto}.vot-menu[data-position=left]{transform-origin:0;top:12.5vh;left:240px}.vot-menu[data-position=right]{transform-origin:100%;top:12.5vh;left:auto;right:-80px}.vot-dialog{--vot-helper-surface-rgb:var(--vot-surface-rgb,255, 255, 255);--vot-helper-surface:rgb(var(--vot-helper-surface-rgb));--vot-helper-onsurface-rgb:var(--vot-onsurface-rgb,0, 0, 0);--vot-helper-onsurface:rgba(var(--vot-helper-onsurface-rgb), .87);--vot-dialog-viewport-margin:16px;--vot-dialog-max-height:75vh;max-width:initial;max-height:initial;width:min(var(--vot-dialog-width,512px),100%);border:1px solid var(--vot-border-color);border-radius:var(--vot-radius-l);background-color:var(--vot-helper-surface);height:fit-content;color:var(--vot-helper-onsurface);box-shadow:var(--vot-shadow-2);-webkit-user-select:none;user-select:none;visibility:visible;opacity:1;transform-origin:50%;transition:opacity var(--vot-duration-medium) var(--vot-easing-standard),transform var(--vot-duration-medium) var(--vot-easing-standard);font-size:16px;line-height:1.5;display:block;position:fixed;inset-block:0;inset-inline:0;overflow:auto hidden;transform:scale(1);font-family:var(--vot-font-family,"Roboto", "Segoe UI", system-ui, sans-serif)!important;margin:auto!important;padding:0!important}[hidden]>.vot-dialog{pointer-events:none;opacity:0;transition:opacity var(--vot-duration-fast) var(--vot-easing-standard),transform var(--vot-duration-medium) var(--vot-easing-standard);transform:translateY(-4px)scale(.98)}.vot-dialog[data-vertical-align=top]{inset-block-start:var(--vot-dialog-viewport-margin);inset-block-end:auto;margin:0 auto!important}.vot-dialog-container{visibility:visible;z-index:2147483647;position:absolute}.vot-dialog-container[hidden]{pointer-events:none;visibility:hidden;display:block!important}.vot-dialog-container *{box-sizing:border-box!important}.vot-dialog-backdrop{opacity:1;background-color:#0009;transition:opacity .3s;position:fixed;inset:0}[hidden]>.vot-dialog-backdrop{pointer-events:none;opacity:0}.vot-dialog-content-wrapper{max-height:var(--vot-dialog-max-height,75vh);flex-direction:column;display:flex;overflow:auto}.vot-dialog-header-container{flex-shrink:0;align-items:flex-start;min-height:31px;display:flex}.vot-dialog-header-container:empty{padding:0 0 20px}.vot-dialog-header-container>.vot-icon-button{margin-inline-end:var(--vot-space-1)!important;margin-top:var(--vot-space-1)!important}.vot-dialog-title-container{font-size:inherit;outline:0;flex:1;display:flex;font-weight:inherit!important;margin:0!important}.vot-dialog-title{flex:1;font-size:115.385%;line-height:1;padding:var(--vot-space-5) var(--vot-space-5) var(--vot-space-4)!important;font-weight:700!important}.vot-dialog-body-container{box-sizing:border-box;gap:var(--vot-space-4);overscroll-behavior:contain;flex-direction:column;min-height:1.375rem;display:flex;overflow:auto;padding:0 var(--vot-space-5)!important;scrollbar-color:rgba(var(--vot-helper-onsurface-rgb),.1) var(--vot-helper-surface)!important}.vot-dialog-body-container::-webkit-scrollbar{background:var(--vot-helper-surface)!important;width:12px!important;height:12px!important}.vot-dialog-body-container::-webkit-scrollbar-track{background:var(--vot-helper-surface)!important;width:12px!important;height:12px!important}.vot-dialog-body-container::-webkit-scrollbar-thumb{border-radius:1ex;background:rgba(var(--vot-helper-onsurface-rgb),.1)!important;border:5px solid var(--vot-helper-surface)!important}.vot-dialog-body-container::-webkit-scrollbar-thumb:hover{border-width:3px!important}.vot-dialog-body-container::-webkit-scrollbar-corner{background:var(--vot-helper-surface)!important}.vot-dialog-footer-container{justify-content:flex-end;gap:var(--vot-space-2);flex-wrap:wrap;flex-shrink:0;display:flex;padding:var(--vot-space-4)!important}.vot-dialog-footer-container:empty{padding:var(--vot-space-5) 0 0 0!important}@media(max-width:480px){.vot-dialog-footer-container{flex-direction:column;align-items:stretch}.vot-dialog-footer-container>:is(.vot-button,.vot-outlined-button,.vot-text-button){white-space:normal;text-overflow:clip;text-align:center;justify-content:center;align-items:center;width:100%;height:auto;min-height:36px;padding:8px 16px;line-height:1.2;display:flex;overflow:visible}}.vot-inline-loader{aspect-ratio:5;--vot-loader-bg:no-repeat radial-gradient(farthest-side, rgba(var(--vot-onsurface-rgb,0, 0, 0), .38) 94%, transparent);background:var(--vot-loader-bg),var(--vot-loader-bg),var(--vot-loader-bg),var(--vot-loader-bg);background-size:20% 100%;height:8px;animation:.75s infinite alternate dotsSlide,1.5s infinite alternate dotsFlip}.vot-loader-progress{--vot-helper-theme:var(--vot-theme-rgb,var(--vot-primary-rgb,33, 150, 243));fill:none;stroke:rgb(var(--vot-helper-theme));stroke-width:2px;stroke-linecap:round;transform-origin:50%;transform:rotate(-90deg)}@keyframes dotsSlide{0%,10%{background-position:0 0,0 0,0 0,0 0}33%{background-position:0 0,33.3333% 0,33.3333% 0,33.3333% 0}66%{background-position:0 0,33.3333% 0,66.6667% 0,66.6667% 0}90%,to{background-position:0 0,33.3333% 0,66.6667% 0,100% 0}}@keyframes dotsFlip{0%,49.99%{transform:scale(1)}50%,to{transform:scale(-1)}}.vot-label{font-family:inherit;font-size:16px;line-height:1.5;display:block}.vot-label-text{display:inline}.vot-label-icon{vertical-align:text-bottom;cursor:help;justify-content:center;align-items:center;width:20px;height:20px;margin-left:4px;display:inline-flex}.vot-label-icon>svg{width:20px;height:20px;display:block}.vot-account{justify-content:space-between;align-items:center;gap:1rem;display:flex}.vot-account-container,.vot-account-wrapper,.vot-account-buttons{align-items:center;gap:1rem;display:flex}.vot-account-avatar{min-width:36px;max-width:36px;min-height:36px;max-height:36px;overflow:hidden}.vot-account-avatar-img{object-fit:cover;border-radius:50%;width:36px;height:36px}@property --vot-subtitles-opacity{syntax:"<number>";inherits:true;initial-value:.8}@property --vot-subtitles-scale-compensation{syntax:"<number>";inherits:true;initial-value:1}.vot-subtitles{--vot-subtitles-background:rgba(var(--vot-surface-rgb,46, 47, 52), var(--vot-subtitles-opacity,.8));--vot-subtitles-effective-max-width:var(--vot-subtitles-max-width,var(--vot-subtitles-smart-max-width,70vw));max-width:var(--vot-subtitles-effective-max-width);max-inline-size:var(--vot-subtitles-effective-max-width);width:max-content;background:var(--vot-subtitles-background,#2e2f34cc);inline-size:max-content;color:var(--vot-subtitles-color,#e3e3e3);pointer-events:all;touch-action:none;font-size:calc(var(--vot-subtitles-font-size,clamp(18px, var(--vot-subtitles-smart-font-preferred,2.2vw), 50px)) * var(--vot-subtitles-scale-compensation,1));-webkit-text-stroke:var(--vot-subtitles-text-stroke-width,clamp(1px, .08em, 2px)) var(--vot-subtitles-text-stroke-color,#000000eb);paint-order:stroke fill;text-shadow:var(--vot-subtitles-text-shadow,0 1px 2px #00000073, 0 2px 8px #00000040);-webkit-font-smoothing:antialiased;-moz-osx-font-smoothing:grayscale;font-synthesis:none;position:relative;--vot-subtitles-font-family:var(--vot-subtitles-font-family-custom,var(--vot-font-family,"Roboto", "Segoe UI", system-ui, sans-serif))!important;font-family:var(--vot-subtitles-font-family)!important;font-style:normal!important;font-weight:var(--vot-subtitles-font-weight,500)!important;text-transform:none!important;letter-spacing:normal!important;border-radius:.5em!important;padding:.5em .75em!important;line-height:1.25!important}.vot-subtitles,.vot-subtitles *{-webkit-text-stroke:inherit;paint-order:inherit;font-family:var(--vot-subtitles-font-family)!important}.vot-subtitles{box-sizing:border-box;-webkit-user-select:none;user-select:none;contain:layout paint;isolation:isolate;text-align:center;margin:0 auto;display:block}.vot-subtitles.vot-subtitles--clamped{overflow:hidden}@supports (line-clamp:2){.vot-subtitles.vot-subtitles--clamped{line-clamp:2}}@supports not (line-clamp:2){.vot-subtitles.vot-subtitles--clamped{-webkit-line-clamp:2;-webkit-box-orient:vertical;display:-webkit-box}}.vot-subtitles{text-wrap:balance;white-space:normal;overflow-wrap:anywhere}.vot-subtitles-widget{--vot-subtitles-anchor-width:100vw;--vot-subtitles-anchor-height:100vh;--vot-subtitles-effective-max-width:var(--vot-subtitles-max-width,var(--vot-subtitles-smart-max-width,70vw));--vot-subtitles-smart-target-width:42ch;--vot-subtitles-smart-min-width-ratio:.55;--vot-subtitles-smart-max-width-ratio:.68;--vot-subtitles-smart-font-preferred:calc(var(--vot-subtitles-anchor-height) * .0333);--vot-subtitles-smart-max-width:clamp(calc(var(--vot-subtitles-anchor-width) * var(--vot-subtitles-smart-min-width-ratio)), var(--vot-subtitles-smart-target-width), calc(var(--vot-subtitles-anchor-width) * var(--vot-subtitles-smart-max-width-ratio)));box-sizing:border-box;z-index:2147483647;--vot-subtitles-fallback-bottom-inset: calc(env(safe-area-inset-bottom,0px) + clamp(56px, 10vh, 220px) + 10px) ;left:50%;top:calc(100% - var(--vot-subtitles-fallback-bottom-inset));width:max-content;inline-size:max-content;max-width:var(--vot-subtitles-effective-max-width);max-inline-size:var(--vot-subtitles-effective-max-width);pointer-events:none;will-change:left,top,transform;max-height:100%;display:block;position:absolute;transform:translate(-50%,-100%)}.vot-subtitles-info{flex-direction:column;gap:2px;max-width:100%;display:flex;padding:6px!important}.vot-subtitles-info-service,.vot-subtitles-info-header,.vot-subtitles-info-context{overflow-wrap:anywhere;word-break:break-word;white-space:normal!important}.vot-subtitles-info-service{color:var(--vot-subtitles-context-color,#86919b);margin-bottom:8px!important;font-size:10px!important;line-height:1!important}.vot-subtitles-info-header{color:var(--vot-subtitles-header-color,#fff);margin-bottom:6px!important;font-size:20px!important;font-weight:500!important;line-height:1!important}.vot-subtitles-info-context{color:var(--vot-subtitles-context-color,#86919b);font-size:12px!important;line-height:1.2!important}.vot-subtitles span[data-vot-token="1"]{cursor:pointer;white-space:normal;overflow-wrap:inherit;word-break:normal;position:relative;font-size:inherit!important;font-family:inherit!important;font-style:inherit!important;font-weight:inherit!important;line-height:inherit!important;text-transform:inherit!important;text-decoration:none!important}.vot-subtitles span[data-vot-token="1"].passed{color:var(--vot-subtitles-passed-color,#2196f3)}.vot-subtitles span[data-vot-token="1"]:before{content:"";z-index:-1;position:absolute;inset:2px -2px;border-radius:4px!important}.vot-subtitles span[data-vot-token="1"]:hover:before{background:var(--vot-subtitles-hover-color,#ffffff8c)}.vot-subtitles span[data-vot-token="1"].selected:before{background:var(--vot-subtitles-passed-color,#2196f3)}.vot-subtitles span[data-vot-style-italic="1"]{font-style:italic!important}.vot-subtitles span[data-vot-style-bold="1"]{font-weight:700!important}.vot-subtitles span[data-vot-style-underline="1"]{text-decoration:underline!important}.vot-subtitles-layer{pointer-events:none;z-index:2147483647;contain:layout paint;width:100vw!important;height:100vh!important;position:fixed!important;inset:0!important}.vot-subtitles-guides{pointer-events:none;z-index:2147483646;position:absolute;inset:0}.vot-subtitles-guide{background:rgba(var(--vot-primary-rgb,33, 150, 243),.7);box-shadow:0 0 0 1px rgba(var(--vot-primary-rgb,33, 150, 243),.12);opacity:0;transition:opacity .12s linear;position:absolute}.vot-subtitles-guide[data-visible=true]{opacity:1}.vot-subtitles-guide--vertical{width:2px;transform:translate(-50%)}.vot-subtitles-guide--horizontal{height:2px;transform:translateY(-50%)}@media(max-aspect-ratio:1){.vot-subtitles-widget{--vot-subtitles-smart-target-width:28ch;--vot-subtitles-smart-min-width-ratio:.8;--vot-subtitles-smart-max-width-ratio:.92;--vot-subtitles-smart-font-preferred:calc(var(--vot-subtitles-anchor-height) * .0296)}}@media(min-aspect-ratio:1)and (max-aspect-ratio:7/5){.vot-subtitles-widget{--vot-subtitles-smart-target-width:32ch;--vot-subtitles-smart-min-width-ratio:.55;--vot-subtitles-smart-max-width-ratio:.9;--vot-subtitles-smart-font-preferred:calc(var(--vot-subtitles-anchor-height) * .0333)}}@media(max-width:900px)and (pointer:coarse){.vot-subtitles-widget{--vot-subtitles-fallback-bottom-inset:env(safe-area-inset-bottom,0px)}}:-webkit-any(:-webkit-full-screen .vot-subtitles-widget,:-webkit-full-screen .vot-subtitles-widget){--vot-subtitles-smart-max-width-ratio:.8}:is(:fullscreen .vot-subtitles-widget){--vot-subtitles-smart-max-width-ratio:.8}:-webkit-any(:-webkit-full-screen .vot-subtitles,:-webkit-full-screen .vot-subtitles){font-size:calc(var(--vot-subtitles-font-size,clamp(18px, var(--vot-subtitles-smart-font-preferred,2vw), 50px)) * var(--vot-subtitles-fullscreen-scale,1) * .95 * var(--vot-subtitles-scale-compensation,1))}:is(:fullscreen .vot-subtitles){font-size:calc(var(--vot-subtitles-font-size,clamp(18px, var(--vot-subtitles-smart-font-preferred,2vw), 50px)) * var(--vot-subtitles-fullscreen-scale,1) * .95 * var(--vot-subtitles-scale-compensation,1))}#vot-subtitles-info.vot-subtitles-info *{-webkit-user-select:text!important;user-select:text!important}:root{--vot-font-family:"Roboto", "Segoe UI", system-ui, sans-serif;--vot-primary-rgb:139, 180, 245;--vot-onprimary-rgb:32, 33, 36;--vot-surface-rgb:32, 33, 36;--vot-onsurface-rgb:227, 227, 227;--vot-subtitles-color:rgb(var(--vot-onsurface-rgb,227, 227, 227));--vot-subtitles-passed-color:rgb(var(--vot-primary-rgb,33, 150, 243));--vot-space-1:4px;--vot-space-2:8px;--vot-space-3:12px;--vot-space-4:16px;--vot-space-5:20px;--vot-space-6:24px;--vot-radius-xs:6px;--vot-radius-s:10px;--vot-radius-m:14px;--vot-radius-l:18px;--vot-border-color:rgba(var(--vot-onsurface-rgb,227, 227, 227), .14);--vot-border-color-hover:rgba(var(--vot-onsurface-rgb,227, 227, 227), .22);--vot-shadow-1:0 1px 2px #0000002e, 0 8px 24px #00000024;--vot-shadow-2:0 2px 4px #00000038, 0 12px 32px #00000038;--vot-duration-fast:.12s;--vot-duration-medium:.2s;--vot-duration-slow:.32s;--vot-easing-standard:cubic-bezier(.4, 0, .2, 1);--vot-focus-ring-color:rgba(var(--vot-primary-rgb,139, 180, 245), .9);--vot-focus-ring:0 0 0 2px var(--vot-focus-ring-color);--vot-focus-ring-offset:0 0 0 4px rgba(var(--vot-surface-rgb,32, 33, 36), .9)}vot-block,vot-block *{box-sizing:border-box;-webkit-tap-highlight-color:transparent}vot-block[hidden]:not(.vot-menu):not(.vot-dialog-container),vot-block [hidden]:not(.vot-menu):not(.vot-dialog-container){display:none!important}vot-block{-webkit-font-smoothing:antialiased;-moz-osx-font-smoothing:grayscale;text-rendering:optimizelegibility;-moz-text-size-adjust:100%;text-size-adjust:100%;display:block;--vot-font-family:"Roboto", "Segoe UI", system-ui, sans-serif!important;font-family:var(--vot-font-family,"Roboto", "Segoe UI", system-ui, sans-serif)!important;visibility:visible!important;font-weight:400!important}vot-block *{font-weight:inherit!important}.vot-portal-local,.vot-subtitles-widget{isolation:isolate}vot-block:focus,vot-block :focus{box-shadow:none!important;outline:none!important}html.vot-keyboard-nav vot-block:focus-visible,html.vot-keyboard-nav vot-block :focus-visible{box-shadow:var(--vot-focus-ring),var(--vot-focus-ring-offset)!important}@supports not selector(:focus-visible){html.vot-keyboard-nav vot-block:focus,html.vot-keyboard-nav vot-block :focus{box-shadow:var(--vot-focus-ring),var(--vot-focus-ring-offset)!important}}@media(prefers-reduced-motion:reduce){.vot-portal-local *,.vot-portal *,.vot-subtitles-widget *{scroll-behavior:auto!important;transition-duration:.001ms!important;animation-duration:.001ms!important;animation-iteration-count:1!important}}.vot-portal{display:inline}.vot-portal-local{z-index:2147483647;position:fixed;top:0;left:0}';
  importCSS(mainStylesInline);
  function initKeyboardNavigationMode() {
    if (globalThis.__votKeyboardNavInitialized) return;
    globalThis.__votKeyboardNavInitialized = true;
    const root = document.documentElement;
    const CLASS = "vot-keyboard-nav";
    const enable = () => root.classList.add(CLASS);
    const disable = () => root.classList.remove(CLASS);
    globalThis.addEventListener(
      "keydown",
      (e2) => {
        if (e2.key === "Tab") enable();
      },
      true
    );
    for (const evt of ["pointerdown", "mousedown", "touchstart"]) {
      globalThis.addEventListener(evt, disable, {
        capture: true,
        passive: true
      });
    }
  }
  initKeyboardNavigationMode();
  const UI = {
makeButtonLike(el, { ariaLabel } = {}) {
      el.setAttribute("role", "button");
      if (!el.hasAttribute("tabindex")) {
        el.tabIndex = 0;
      }
      const enabledTabIndex = el.tabIndex;
      const syncDisabledState = () => {
        const isDisabled = el.getAttribute("disabled") === "true";
        if (isDisabled) {
          el.setAttribute("aria-disabled", "true");
          el.tabIndex = -1;
        } else {
          el.removeAttribute("aria-disabled");
          el.tabIndex = enabledTabIndex;
        }
      };
      syncDisabledState();
      new MutationObserver(() => syncDisabledState()).observe(el, {
        attributes: true,
        attributeFilter: ["disabled"]
      });
      if (ariaLabel) {
        el.setAttribute("aria-label", ariaLabel);
      }
      el.addEventListener("keydown", (e2) => {
        const disabled = el.getAttribute("disabled") === "true" || el.getAttribute("aria-disabled") === "true";
        if (disabled) return;
        if (e2.key === "Enter" || e2.key === " ") {
          e2.preventDefault();
          el.click();
        }
      });
      return el;
    },
createEl(tag, classes = [], content = null) {
      const el = document.createElement(tag);
      if (classes.length) el.classList.add(...classes);
      if (content !== null) el.append(content);
      return el;
    },
createHeader(html, level = 4) {
      return UI.createEl(
        "vot-block",
        ["vot-header", `vot-header-level-${level}`],
        html
      );
    },
createInformation(labelHtml, valueHtml) {
      const container = UI.createEl("vot-block", ["vot-info"]);
      const header = UI.createEl("vot-block");
      D(labelHtml, header);
      const value = UI.createEl("vot-block");
      D(valueHtml, value);
      container.append(header, value);
      return { container, header, value };
    },
createButton(html) {
      const el = UI.createEl("vot-block", ["vot-button"], html);
      return UI.makeButtonLike(el);
    },
createTextButton(html) {
      const el = UI.createEl("vot-block", ["vot-text-button"], html);
      return UI.makeButtonLike(el);
    },
createOutlinedButton(html) {
      const el = UI.createEl("vot-block", ["vot-outlined-button"], html);
      return UI.makeButtonLike(el);
    },
createIconButton(templateHtml, options = {}) {
      const button2 = UI.createEl("vot-block", ["vot-icon-button"]);
      D(templateHtml, button2);
      return UI.makeButtonLike(button2, options);
    },
    createInlineLoader() {
      return UI.createEl("vot-block", ["vot-inline-loader"]);
    },
    createPortal(local = false) {
      return UI.createEl("vot-block", [`vot-portal${local ? "-local" : ""}`]);
    },
    createSubtitleInfo(word, desc, translationService) {
      const container = UI.createEl("vot-block", ["vot-subtitles-info"]);
      container.id = "vot-subtitles-info";
      const translatedWith = UI.createEl(
        "vot-block",
        ["vot-subtitles-info-service"],
        localizationProvider.get("VOTTranslatedBy").replace("{0}", translationService)
      );
      const header = UI.createEl(
        "vot-block",
        ["vot-subtitles-info-header"],
        word
      );
      const context = UI.createEl(
        "vot-block",
        ["vot-subtitles-info-context"],
        desc
      );
      container.append(translatedWith, header, context);
      return {
        container,
        translatedWith,
        header,
        context
      };
    }
  };
  class EventImpl {
    listeners = new Set();
    get size() {
      return this.listeners.size;
    }
    addListener(handler) {
      this.listeners.add(handler);
      return this;
    }
    removeListener(handler) {
      this.listeners.delete(handler);
      return this;
    }
    dispatch(...args) {
      for (const handler of this.listeners) {
        try {
          handler(...args);
        } catch (exception) {
          console.error("[VOT]", exception);
        }
      }
    }
    async dispatchAsync(...args) {
      const pending = [];
      for (const handler of this.listeners) {
        try {
          const result = handler(...args);
          if (result && typeof result.then === "function") {
            pending.push(Promise.resolve(result));
          }
        } catch (exception) {
          console.error("[VOT]", exception);
        }
      }
      if (!pending.length) {
        return;
      }
      const settled = await Promise.allSettled(pending);
      for (const item of settled) {
        if (item.status === "rejected") {
          console.error("[VOT]", item.reason);
        }
      }
    }
    clear() {
      this.listeners.clear();
    }
  }
  function addComponentEventListener(events, type, listener) {
    events[type].addListener(listener);
  }
  function removeComponentEventListener(events, type, listener) {
    events[type].removeListener(listener);
  }
  function setHiddenState(element, isHidden) {
    element.hidden = isHidden;
  }
  function getHiddenState(element) {
    return element.hidden;
  }
  class DownloadButton {
    button;
    loaderMain;
    loaderCircle;
    onClick = new EventImpl();
    events = {
      click: this.onClick
    };
    _progress = 0;
    constructor() {
      const elements = this.createElements();
      this.button = elements.button;
      this.loaderMain = elements.loaderMain;
      this.loaderCircle = elements.loaderCircle;
      this.progress = 0;
    }
    createElements() {
      const button2 = UI.createIconButton(DOWNLOAD_ICON, {
        ariaLabel: "Download translation"
      });
      const loaderMain = button2.querySelector(".vot-loader-main");
      if (!loaderMain) {
        throw new Error("[VOT] DownloadButton loader main element not found");
      }
      const loaderCircle = button2.querySelector(
        ".vot-loader-progress"
      );
      if (!loaderCircle) {
        throw new Error("[VOT] DownloadButton loader circle element not found");
      }
      button2.addEventListener("click", () => {
        this.onClick.dispatch();
      });
      return { button: button2, loaderMain, loaderCircle };
    }
    addEventListener(_type, listener) {
      addComponentEventListener(this.events, "click", listener);
      return this;
    }
    removeEventListener(_type, listener) {
      removeComponentEventListener(this.events, "click", listener);
      return this;
    }
    get progress() {
      return this._progress;
    }
    set progress(value) {
      const normalized = clampProgress(value);
      this._progress = normalized;
      const circumference = this.getCircleCircumference();
      this.loaderCircle.style.strokeDasharray = `${circumference}`;
      const offset = circumference * (1 - normalized / 100);
      this.loaderCircle.style.strokeDashoffset = `${offset}`;
      this.loaderMain.style.opacity = normalized === 0 ? "1" : "0";
      this.loaderCircle.style.opacity = normalized === 0 ? "0" : "1";
    }
    getCircleCircumference() {
      const radius = this.loaderCircle.r?.baseVal?.value ?? 0;
      return 2 * Math.PI * radius;
    }
    set hidden(isHidden) {
      setHiddenState(this.button, isHidden);
    }
    get hidden() {
      return getHiddenState(this.button);
    }
  }
  function clampProgress(value) {
    if (!Number.isFinite(value)) return 0;
    const asPercent = value < 1 ? value * 100 : value;
    return Math.max(0, Math.min(100, Math.round(asPercent)));
  }
  class Label {
    container;
    icon;
    text;
    _labelText;
    _icon;
    constructor({ labelText, icon }) {
      this._labelText = labelText;
      this._icon = icon;
      const elements = this.createElements();
      this.container = elements.container;
      this.icon = elements.icon;
      this.text = elements.text;
    }
    createElements() {
      const container = UI.createEl("vot-block", ["vot-label"]);
      const text = UI.createEl("span", ["vot-label-text"]);
      text.textContent = this._labelText;
      const icon = UI.createEl("span", ["vot-label-icon"]);
      if (this._icon) {
        D(this._icon, icon);
      } else {
        icon.hidden = true;
      }
      container.append(text, icon);
      return {
        container,
        icon,
        text
      };
    }
    set hidden(isHidden) {
      setHiddenState(this.container, isHidden);
    }
    get hidden() {
      return getHiddenState(this.container);
    }
  }
  class Dialog {
    container;
    backdrop;
    box;
    contentWrapper;
    headerContainer;
    titleContainer;
    title;
    closeButton;
    bodyContainer;
    footerContainer;
    onClose = new EventImpl();
    events = {
      close: this.onClose
    };
previouslyFocused = null;
    keydownListener;
adaptiveAlignObserver;
    adaptiveAlignRaf = null;
    handleViewportChange = () => {
      this.scheduleAdaptiveVerticalAlign();
    };
    titleId = typeof crypto !== "undefined" && "randomUUID" in crypto ? crypto.randomUUID() : `vot-dialog-title-${Math.random().toString(36).slice(2)}`;
    _titleHtml;
    _isTemp;
    constructor({ titleHtml, isTemp = false }) {
      this._titleHtml = titleHtml;
      this._isTemp = isTemp;
      const elements = this.createElements();
      this.container = elements.container;
      this.backdrop = elements.backdrop;
      this.box = elements.box;
      this.contentWrapper = elements.contentWrapper;
      this.headerContainer = elements.headerContainer;
      this.titleContainer = elements.titleContainer;
      this.title = elements.title;
      this.closeButton = elements.closeButton;
      this.bodyContainer = elements.bodyContainer;
      this.footerContainer = elements.footerContainer;
    }
    createElements() {
      const container = UI.createEl("vot-block", ["vot-dialog-container"]);
      if (this._isTemp) {
        container.classList.add("vot-dialog-temp");
      }
      container.hidden = !this._isTemp;
      container.setAttribute("aria-hidden", container.hidden ? "true" : "false");
      container.toggleAttribute("inert", container.hidden);
      const backdrop = UI.createEl("vot-block", ["vot-dialog-backdrop"]);
      const box = UI.createEl("vot-block", ["vot-dialog"]);
      box.dataset.verticalAlign = "center";
      box.setAttribute("role", "dialog");
      box.setAttribute("aria-modal", "true");
      box.tabIndex = -1;
      const contentWrapper = UI.createEl("vot-block", [
        "vot-dialog-content-wrapper"
      ]);
      const headerContainer = UI.createEl("vot-block", [
        "vot-dialog-header-container"
      ]);
      const titleContainer = UI.createEl("vot-block", [
        "vot-dialog-title-container"
      ]);
      const title = UI.createEl("vot-block", ["vot-dialog-title"]);
      title.id = this.titleId;
      title.append(this._titleHtml);
      titleContainer.appendChild(title);
      box.setAttribute("aria-labelledby", this.titleId);
      const closeButton = UI.createIconButton(CLOSE_ICON, {
        ariaLabel: "Close"
      });
      closeButton.classList.add("vot-dialog-close-button");
      backdrop.addEventListener("click", () => {
        this.close();
      });
      closeButton.addEventListener("click", () => {
        this.close();
      });
      headerContainer.append(titleContainer, closeButton);
      const bodyContainer = UI.createEl("vot-block", [
        "vot-dialog-body-container"
      ]);
      const footerContainer = UI.createEl("vot-block", [
        "vot-dialog-footer-container"
      ]);
      contentWrapper.append(headerContainer, bodyContainer, footerContainer);
      box.appendChild(contentWrapper);
      container.append(backdrop, box);
      box.addEventListener("click", (e2) => {
        e2.stopPropagation();
      });
      return {
        container,
        backdrop,
        box,
        contentWrapper,
        headerContainer,
        titleContainer,
        title,
        closeButton,
        bodyContainer,
        footerContainer
      };
    }
    addEventListener(_type, listener) {
      addComponentEventListener(this.events, "close", listener);
      return this;
    }
    removeEventListener(_type, listener) {
      removeComponentEventListener(this.events, "close", listener);
      return this;
    }
    open() {
      this.previouslyFocused ??= document.activeElement;
      this.hidden = false;
      this.attachKeydownTrap();
      this.attachAdaptiveVerticalAlign();
      queueMicrotask(() => this.focusFirst());
      return this;
    }
    remove() {
      this.detachAdaptiveVerticalAlign();
      this.detachKeydownTrap();
      this.container.remove();
      this.restoreFocus();
      this.onClose.dispatch();
      return this;
    }
    close() {
      if (this._isTemp) {
        return this.remove();
      }
      this.detachAdaptiveVerticalAlign();
      this.detachKeydownTrap();
      this.hidden = true;
      this.restoreFocus();
      this.onClose.dispatch();
      return this;
    }
    attachAdaptiveVerticalAlign() {
      if (this.adaptiveAlignObserver) {
        this.scheduleAdaptiveVerticalAlign();
        return;
      }
      if (typeof ResizeObserver !== "undefined") {
        this.adaptiveAlignObserver = new ResizeObserver(() => {
          this.scheduleAdaptiveVerticalAlign();
        });
        this.adaptiveAlignObserver.observe(this.contentWrapper);
      }
      globalThis.addEventListener("resize", this.handleViewportChange, {
        passive: true
      });
      if (globalThis.visualViewport) {
        globalThis.visualViewport.addEventListener(
          "resize",
          this.handleViewportChange,
          { passive: true }
        );
        globalThis.visualViewport.addEventListener(
          "scroll",
          this.handleViewportChange,
          { passive: true }
        );
      }
      this.scheduleAdaptiveVerticalAlign();
    }
    detachAdaptiveVerticalAlign() {
      if (this.adaptiveAlignObserver) {
        this.adaptiveAlignObserver.disconnect();
        this.adaptiveAlignObserver = void 0;
      }
      globalThis.removeEventListener("resize", this.handleViewportChange);
      globalThis.visualViewport?.removeEventListener(
        "resize",
        this.handleViewportChange
      );
      globalThis.visualViewport?.removeEventListener(
        "scroll",
        this.handleViewportChange
      );
      if (this.adaptiveAlignRaf !== null) {
        cancelAnimationFrame(this.adaptiveAlignRaf);
        this.adaptiveAlignRaf = null;
      }
    }
    scheduleAdaptiveVerticalAlign() {
      if (this.adaptiveAlignRaf !== null) {
        cancelAnimationFrame(this.adaptiveAlignRaf);
      }
      this.adaptiveAlignRaf = requestAnimationFrame(() => {
        this.adaptiveAlignRaf = null;
        this.updateAdaptiveVerticalAlign();
      });
    }
    updateAdaptiveVerticalAlign() {
      const viewportHeight = globalThis.visualViewport?.height ?? globalThis.innerHeight;
      if (!viewportHeight || viewportHeight <= 0) {
        return;
      }
      const marginPx = 16;
      const centerMaxPx = Math.max(160, Math.round(viewportHeight * 0.75));
      const topMaxPx = Math.max(160, Math.round(viewportHeight - marginPx * 2));
      const contentHeightPx = this.contentWrapper.scrollHeight;
      const currentlyTop = this.box.dataset.verticalAlign === "top";
      const enterTopThresholdPx = centerMaxPx - 8;
      const exitTopThresholdPx = Math.round(viewportHeight * 0.6);
      const shouldTop = currentlyTop ? contentHeightPx > exitTopThresholdPx : contentHeightPx >= enterTopThresholdPx;
      if (shouldTop) {
        this.box.dataset.verticalAlign = "top";
        this.box.style.setProperty("--vot-dialog-max-height", `${topMaxPx}px`);
      } else {
        this.box.dataset.verticalAlign = "center";
        this.box.style.setProperty("--vot-dialog-max-height", `${centerMaxPx}px`);
      }
    }
    restoreFocus() {
      const el = this.previouslyFocused;
      this.previouslyFocused = null;
      if (el && el instanceof HTMLElement && document.contains(el)) {
        el.focus();
      }
    }
    getFocusableElements() {
      const selectors = [
        "button:not([disabled])",
        "[href]",
        "input:not([disabled])",
        "select:not([disabled])",
        "textarea:not([disabled])",
        "[tabindex]:not([tabindex='-1'])",
        "[role='button']:not([aria-disabled='true'])"
      ];
      return Array.from(
        this.container.querySelectorAll(selectors.join(","))
      ).filter((el) => !el.hidden && el.getClientRects().length > 0);
    }
    focusFirst() {
      const focusables = this.getFocusableElements();
      (focusables[0] ?? this.closeButton ?? this.box).focus?.();
    }
    attachKeydownTrap() {
      if (this.keydownListener) return;
      this.keydownListener = (e2) => {
        if (e2.key === "Escape") {
          e2.preventDefault();
          this.close();
          return;
        }
        if (e2.key !== "Tab") {
          return;
        }
        const focusables = this.getFocusableElements();
        if (!focusables.length) {
          e2.preventDefault();
          this.box.focus();
          return;
        }
        const first = focusables[0];
        const last = focusables.at(-1) ?? first;
        const active = document.activeElement;
        if (e2.shiftKey) {
          if (active === first || active === this.box) {
            e2.preventDefault();
            last.focus();
          }
        } else if (active === last) {
          e2.preventDefault();
          first.focus();
        }
      };
      this.container.addEventListener("keydown", this.keydownListener);
    }
    detachKeydownTrap() {
      if (!this.keydownListener) return;
      this.container.removeEventListener("keydown", this.keydownListener);
      this.keydownListener = void 0;
    }
    set hidden(isHidden) {
      setHiddenState(this.container, isHidden);
      this.container.setAttribute("aria-hidden", isHidden ? "true" : "false");
      this.container.toggleAttribute("inert", isHidden);
    }
    get hidden() {
      return getHiddenState(this.container);
    }
    get isDialogOpen() {
      return !this.container.hidden;
    }
  }
  class Textfield {
    container;
    input;
    label;
    onInput = new EventImpl();
    onChange = new EventImpl();
    events = {
      input: this.onInput,
      change: this.onChange
    };
    _labelHtml;
    _multiline;
    _placeholder;
    _value;
    constructor({
      labelHtml = "",
      placeholder = "",
      value = "",
      multiline = false
    }) {
      this._labelHtml = labelHtml;
      this._multiline = multiline;
      this._placeholder = placeholder;
      this._value = value;
      const elements = this.createElements();
      this.container = elements.container;
      this.input = elements.input;
      this.label = elements.label;
    }
    createElements() {
      const container = UI.createEl("vot-block", ["vot-textfield"]);
      const input = document.createElement(
        this._multiline ? "textarea" : "input"
      );
      if (!this._labelHtml) {
        input.classList.add("vot-show-placeholer", "vot-show-placeholder");
      }
      input.placeholder = this._placeholder;
      input.value = this._value;
      const label = UI.createEl("span");
      label.append(this._labelHtml);
      container.append(input, label);
      input.addEventListener("input", () => {
        this._value = this.input.value;
        this.onInput.dispatch(this._value);
      });
      input.addEventListener("change", () => {
        this._value = this.input.value;
        this.onChange.dispatch(this._value);
      });
      return {
        container,
        label,
        input
      };
    }
    addEventListener(type, listener) {
      addComponentEventListener(this.events, type, listener);
      return this;
    }
    removeEventListener(type, listener) {
      removeComponentEventListener(this.events, type, listener);
      return this;
    }
    get value() {
      return this._value;
    }
set value(val) {
      if (this._value === val) {
        return;
      }
      this.input.value = this._value = val;
      this.onChange.dispatch(this._value);
    }
    get placeholder() {
      return this._placeholder;
    }
    set placeholder(text) {
      this.input.placeholder = this._placeholder = text;
    }
    get disabled() {
      return this.input.disabled;
    }
    set disabled(isDisabled) {
      this.input.disabled = isDisabled;
    }
    set hidden(isHidden) {
      setHiddenState(this.container, isHidden);
    }
    get hidden() {
      return getHiddenState(this.container);
    }
  }
  class Select {
    container;
    outer;
    arrowIcon;
    title;
    dialogParent;
    labelElement;
    _selectTitle;
    _dialogTitle;
    multiSelect;
    baseItems;
    _items;
    searchItemsProvider;
    isLoading = false;
    isDialogOpen = false;
    searchRequestId = 0;
    onSelectItem = new EventImpl();
    onBeforeOpen = new EventImpl();
    events = {
      selectItem: this.onSelectItem,
      beforeOpen: this.onBeforeOpen
    };
    contentList;
    contentItemSearchDatasetKey = "votSearchLabel";
    contentItemIndexDatasetKey = "votIndex";
    ignoreNextContentClick = false;
    selectedItems = [];
    selectedValues;
    constructor({
      selectTitle,
      dialogTitle,
      items,
      searchItemsProvider,
      labelElement,
      dialogParent = document.documentElement,
      multiSelect
    }) {
      this._selectTitle = selectTitle;
      this._dialogTitle = dialogTitle;
      this.baseItems = this.cloneItems(items);
      this._items = this.cloneItems(items);
      this.searchItemsProvider = searchItemsProvider;
      this.multiSelect = multiSelect ?? false;
      this.labelElement = labelElement;
      this.dialogParent = dialogParent;
      this.selectedValues = this.calcSelectedValues();
      const elements = this.createElements();
      this.container = elements.container;
      this.outer = elements.outer;
      this.arrowIcon = elements.arrowIcon;
      this.title = elements.title;
    }
    cloneItems(items) {
      return items.map((item) => ({ ...item }));
    }
    static genLanguageItems(langs2, conditionString) {
      return langs2.map((lang2) => {
        const phrase = `langs.${lang2}`;
        const label = localizationProvider.get(phrase);
        return {
          label: label === phrase ? lang2.toUpperCase() : label,
          value: lang2,
          selected: conditionString === lang2
        };
      });
    }
    multiSelectItemHandle = (item) => {
      const value = item.value;
      if (this.selectedValues.has(value) && this.selectedValues.size > 1) {
        this.selectedValues.delete(value);
      } else {
        this.selectedValues.add(value);
      }
      this.syncItemsSelectionState();
      this.syncItemsSelectionState(this.baseItems);
      this.updateSelectedState();
      this.onSelectItem.dispatch(Array.from(this.selectedValues));
    };
    singleSelectItemHandle = (item) => {
      const value = item.value;
      this.selectedValues = new Set([value]);
      this.syncItemsSelectionState();
      this.syncItemsSelectionState(this.baseItems);
      this.updateSelectedState();
      this.onSelectItem.dispatch(value);
    };
    activateContentItem(contentItem) {
      const rawIndex = contentItem.dataset[this.contentItemIndexDatasetKey];
      if (!rawIndex) {
        return;
      }
      const item = this._items[Number(rawIndex)];
      if (!item) {
        return;
      }
      if (this.multiSelect) {
        this.multiSelectItemHandle(item);
        return;
      }
      this.singleSelectItemHandle(item);
    }
    onContentItemClick = (event) => {
      if (this.ignoreNextContentClick) {
        this.ignoreNextContentClick = false;
        return;
      }
      if (!(event.target instanceof HTMLElement)) {
        return;
      }
      const contentItem = event.target.closest(
        ".vot-select-content-item"
      );
      if (!contentItem || contentItem.inert || !this.contentList?.contains(contentItem)) {
        return;
      }
      this.activateContentItem(contentItem);
    };
    onContentItemPointerDown = (event) => {
      if (!(event.target instanceof HTMLElement)) {
        return;
      }
      const contentItem = event.target.closest(
        ".vot-select-content-item"
      );
      if (!contentItem || contentItem.inert || !this.contentList?.contains(contentItem)) {
        return;
      }
      event.preventDefault();
      event.stopPropagation();
      this.ignoreNextContentClick = true;
      this.activateContentItem(contentItem);
    };
    syncItemsSelectionState(items = this._items) {
      for (const item of items) {
        item.selected = this.selectedValues.has(item.value);
      }
    }
    restoreBaseItems() {
      this._items = this.cloneItems(this.baseItems);
      this.syncItemsSelectionState();
      this.updateSelectedState();
    }
    createDialogContentList() {
      const contentList = UI.createEl("vot-block", ["vot-select-content-list"]);
      for (const [index, item] of this._items.entries()) {
        const contentItem = UI.createEl("vot-block", ["vot-select-content-item"]);
        contentItem.textContent = item.label;
        contentItem.dataset.votSelected = item.selected === true ? "true" : "false";
        contentItem.dataset.votValue = item.value;
        contentItem.dataset[this.contentItemSearchDatasetKey] = item.label.toLowerCase();
        contentItem.dataset[this.contentItemIndexDatasetKey] = String(index);
        if (item.disabled) {
          contentItem.inert = true;
        }
        contentList.appendChild(contentItem);
      }
      contentList.addEventListener("pointerdown", this.onContentItemPointerDown);
      contentList.addEventListener("click", this.onContentItemClick);
      this.selectedItems = Array.from(contentList.children);
      return contentList;
    }
    createElements() {
      const container = UI.createEl("vot-block", ["vot-select"]);
      if (this.labelElement) {
        container.classList.add("vot-select--labeled");
        container.append(this.labelElement);
      } else {
        container.classList.add("vot-select--control-only");
      }
      const outer = UI.createEl("vot-block", ["vot-select-outer"]);
      UI.makeButtonLike(outer);
      outer.setAttribute("aria-haspopup", "dialog");
      outer.setAttribute("aria-expanded", "false");
      const title = UI.createEl("vot-block", ["vot-select-title"]);
      title.textContent = this.visibleText;
      const arrowIcon = UI.createEl("vot-block", ["vot-select-arrow-icon"]);
      D(CHEVRON_ICON, arrowIcon);
      outer.append(title, arrowIcon);
      outer.addEventListener("click", () => {
        if (this.disabled) {
          return;
        }
        if (this.isLoading || this.isDialogOpen) {
          return;
        }
        try {
          this.isLoading = true;
          const tempDialog = new Dialog({
            titleHtml: this._dialogTitle,
            isTemp: true
          });
          this.onBeforeOpen.dispatch(tempDialog);
          this.dialogParent.appendChild(tempDialog.container);
          this.isDialogOpen = true;
          outer.setAttribute("aria-expanded", "true");
          const votSearchLangTextfield = new Textfield({
            labelHtml: localizationProvider.get("searchField")
          });
          votSearchLangTextfield.addEventListener("input", async (searchText) => {
            const requestId = ++this.searchRequestId;
            if (this.searchItemsProvider) {
              const providedItems = await this.searchItemsProvider(searchText);
              if (requestId !== this.searchRequestId) {
                return;
              }
              this.updateItems(providedItems, { persist: false });
            }
            const normalizedSearchText = searchText.toLowerCase();
            for (const contentItem of this.selectedItems) {
              const searchableText = contentItem.dataset[this.contentItemSearchDatasetKey] ?? "";
              contentItem.hidden = !searchableText.includes(normalizedSearchText);
            }
          });
          this.contentList = this.createDialogContentList();
          tempDialog.bodyContainer.append(
            votSearchLangTextfield.container,
            this.contentList
          );
          tempDialog.addEventListener("close", () => {
            this.isDialogOpen = false;
            this.restoreBaseItems();
            this.selectedItems = [];
            this.contentList = void 0;
            outer.setAttribute("aria-expanded", "false");
          });
          tempDialog.open();
        } finally {
          this.isLoading = false;
        }
      });
      container.appendChild(outer);
      return {
        container,
        outer,
        arrowIcon,
        title
      };
    }
    calcSelectedValues() {
      return new Set(
        this._items.filter((item) => item.selected).map((item) => item.value)
      );
    }
    addEventListener(type, listener) {
      addComponentEventListener(this.events, type, listener);
      return this;
    }
    removeEventListener(type, listener) {
      removeComponentEventListener(this.events, type, listener);
      return this;
    }
    updateTitle() {
      this.title.textContent = this.visibleText;
      return this;
    }
    updateSelectedState() {
      if (this.selectedItems.length > 0) {
        for (const item of this.selectedItems) {
          const val = item.dataset.votValue;
          if (val === void 0) {
            continue;
          }
          item.dataset.votSelected = this.selectedValues.has(val).toString();
        }
      }
      this.updateTitle();
      return this;
    }
    setSelectedValue(value) {
      const values = Array.isArray(value) ? value : [value];
      let selectedValues;
      if (this.multiSelect) {
        selectedValues = values;
      } else {
        selectedValues = values.length > 0 ? [values[0]] : [];
      }
      this.selectedValues = new Set(selectedValues);
      this.syncItemsSelectionState();
      this.syncItemsSelectionState(this.baseItems);
      this.updateSelectedState();
      return this;
    }
updateItems(newItems, options = {}) {
      const { persist = true } = options;
      const nextItems = this.cloneItems(newItems);
      if (persist) {
        this.baseItems = this.cloneItems(nextItems);
      }
      this._items = nextItems;
      this.selectedValues = this.calcSelectedValues();
      this.updateSelectedState();
      const dialogContainer = this.contentList?.parentElement;
      if (!this.contentList || !dialogContainer) {
        return this;
      }
      const oldContentList = this.contentList;
      this.contentList = this.createDialogContentList();
      dialogContainer.replaceChild(this.contentList, oldContentList);
      return this;
    }
    get items() {
      return this._items;
    }
    get visibleText() {
      if (!this.multiSelect) {
        return this._items.find((item) => item.selected)?.label ?? this._selectTitle;
      }
      return this._items.filter((item) => this.selectedValues.has(item.value)).map((item) => item.label).join(", ") || this._selectTitle;
    }
    set selectTitle(title) {
      this._selectTitle = title;
      this.updateTitle();
    }
    set hidden(isHidden) {
      setHiddenState(this.container, isHidden);
    }
    get hidden() {
      return getHiddenState(this.container);
    }
    get disabled() {
      return this.outer.getAttribute("disabled") === "true" || this.outer.getAttribute("aria-disabled") === "true";
    }
    set disabled(isDisabled) {
      if (isDisabled) {
        this.outer.setAttribute("disabled", "true");
        return;
      }
      this.outer.removeAttribute("disabled");
    }
  }
  class LanguagePairSelect {
    container;
    fromSelect;
    directionIcon;
    toSelect;
    dialogParent;
_fromSelectTitle;
    _fromDialogTitle;
    _fromItems;
_toSelectTitle;
    _toDialogTitle;
    _toItems;
    constructor({
      from: {
        selectTitle: fromSelectTitle = localizationProvider.get("videoLanguage"),
        dialogTitle: fromDialogTitle = localizationProvider.get("videoLanguage"),
        items: fromItems
      },
      to: {
        selectTitle: toSelectTitle = localizationProvider.get(
          "translationLanguage"
        ),
        dialogTitle: toDialogTitle = localizationProvider.get(
          "translationLanguage"
        ),
        items: toItems
      },
      dialogParent = document.documentElement
    }) {
      this._fromSelectTitle = fromSelectTitle;
      this._fromDialogTitle = fromDialogTitle;
      this._fromItems = fromItems;
      this._toSelectTitle = toSelectTitle;
      this._toDialogTitle = toDialogTitle;
      this._toItems = toItems;
      this.dialogParent = dialogParent;
      const elements = this.createElements();
      this.container = elements.container;
      this.fromSelect = elements.fromSelect;
      this.directionIcon = elements.directionIcon;
      this.toSelect = elements.toSelect;
    }
    createElements() {
      const container = UI.createEl("vot-block", ["vot-lang-select"]);
      const fromSelect = new Select({
        selectTitle: this._fromSelectTitle,
        dialogTitle: this._fromDialogTitle,
        items: this._fromItems,
        dialogParent: this.dialogParent
      });
      const directionIcon = UI.createEl("vot-block", ["vot-lang-select-icon"]);
      D(ARROW_RIGHT_ICON, directionIcon);
      const toSelect = new Select({
        selectTitle: this._toSelectTitle,
        dialogTitle: this._toDialogTitle,
        items: this._toItems,
        dialogParent: this.dialogParent
      });
      container.append(fromSelect.container, directionIcon, toSelect.container);
      return {
        container,
        fromSelect,
        directionIcon,
        toSelect
      };
    }
    setSelectedValues(from, to) {
      this.fromSelect.setSelectedValue(from);
      this.toSelect.setSelectedValue(to);
      return this;
    }
    updateItems(fromItems, toItems) {
      this._fromItems = fromItems;
      this._toItems = toItems;
      this.fromSelect = this.fromSelect.updateItems(fromItems);
      this.toSelect = this.toSelect.updateItems(toItems);
      return this;
    }
  }
  class Slider {
    container;
    input;
    label;
    onInput = new EventImpl();
    _labelHtml;
    _value;
    _min;
    _max;
    _step;
    constructor({
      labelHtml,
      value = 50,
      min = 0,
      max = 100,
      step = 1
    }) {
      this._labelHtml = labelHtml;
      this._value = value;
      this._min = min;
      this._max = max;
      this._step = step;
      const elements = this.createElements();
      this.container = elements.container;
      this.input = elements.input;
      this.label = elements.label;
      this.update();
    }
    updateProgress() {
      const range = this._max - this._min;
      const raw = range <= 0 ? 0 : (this._value - this._min) / range;
      const progress = Math.max(0, Math.min(1, raw));
      this.container.style.setProperty("--vot-progress", progress.toString());
      return this;
    }
    update() {
      this._value = this.input.valueAsNumber;
      this._min = +this.input.min;
      this._max = +this.input.max;
      this.updateProgress();
      return this;
    }
    createElements() {
      const container = UI.createEl("vot-block", ["vot-slider"]);
      const input = document.createElement("input");
      input.type = "range";
      input.min = this._min.toString();
      input.max = this._max.toString();
      input.step = this._step.toString();
      input.value = this._value.toString();
      const label = UI.createEl("span");
      D(this._labelHtml, label);
      container.append(input, label);
      input.addEventListener("input", () => {
        this.update();
        this.onInput.dispatch(this._value, false);
      });
      return {
        container,
        label,
        input
      };
    }
    addEventListener(_type, listener) {
      this.onInput.addListener(listener);
      return this;
    }
    removeEventListener(_type, listener) {
      this.onInput.removeListener(listener);
      return this;
    }
    get value() {
      return this._value;
    }
set value(val) {
      this._value = clampNumber$1(val, this._min, this._max);
      this.input.value = this._value.toString();
      this.updateProgress();
      this.onInput.dispatch(this._value, true);
    }
    get min() {
      return this._min;
    }
    set min(val) {
      this._min = val;
      this.input.min = this._min.toString();
      this._value = clampNumber$1(this._value, this._min, this._max);
      this.input.value = this._value.toString();
      this.updateProgress();
    }
    get max() {
      return this._max;
    }
    set max(val) {
      this._max = val;
      this.input.max = this._max.toString();
      this._value = clampNumber$1(this._value, this._min, this._max);
      this.input.value = this._value.toString();
      this.updateProgress();
    }
    get step() {
      return this._step;
    }
    set step(val) {
      this._step = val;
      this.input.step = this._step.toString();
    }
    get disabled() {
      return this.input.disabled;
    }
    set disabled(isDisabled) {
      this.input.disabled = isDisabled;
    }
    set hidden(isHidden) {
      setHiddenState(this.container, isHidden);
    }
    get hidden() {
      return getHiddenState(this.container);
    }
  }
  function clampNumber$1(value, min, max) {
    if (!Number.isFinite(value)) return min;
    if (max < min) return min;
    return Math.max(min, Math.min(max, value));
  }
  class SliderLabel {
    container;
    strong;
    text;
    _labelText;
    _labelEOL;
    _value;
    _symbol;
    constructor({
      labelText,
      labelEOL = "",
      value = 50,
      symbol: symbol2 = "%"
    }) {
      this._labelText = labelText;
      this._labelEOL = labelEOL;
      this._value = value;
      this._symbol = symbol2;
      const elements = this.createElements();
      this.container = elements.container;
      this.strong = elements.strong;
      this.text = elements.text;
    }
    createElements() {
      const container = UI.createEl("vot-block", ["vot-slider-label"]);
      const text = UI.createEl("span", ["vot-slider-label-text"]);
      text.textContent = this.labelText;
      const strong = UI.createEl("span", ["vot-slider-label-value"]);
      strong.textContent = this.valueText;
      container.append(text, strong);
      return {
        container,
        strong,
        text
      };
    }
    get labelText() {
      return `${this._labelText}${this._labelEOL}`;
    }
    get valueText() {
      return `${this._value}${this._symbol}`;
    }
    get value() {
      return this._value;
    }
    set value(val) {
      this._value = val;
      this.strong.textContent = this.valueText;
    }
    set hidden(isHidden) {
      setHiddenState(this.container, isHidden);
    }
    get hidden() {
      return getHiddenState(this.container);
    }
  }
  class VOTMenu {
    container;
    contentWrapper;
    headerContainer;
    bodyContainer;
    footerContainer;
    titleContainer;
    title;
    _position;
    _titleHtml;
menuId = typeof crypto !== "undefined" && "randomUUID" in crypto ? `vot-menu-${crypto.randomUUID()}` : `vot-menu-${Math.random().toString(36).slice(2)}`;
    titleId = typeof crypto !== "undefined" && "randomUUID" in crypto ? `vot-menu-title-${crypto.randomUUID()}` : `vot-menu-title-${Math.random().toString(36).slice(2)}`;
    constructor({ position: position2 = "default", titleHtml = "" }) {
      this._position = position2;
      this._titleHtml = titleHtml;
      const elements = this.createElements();
      this.container = elements.container;
      this.contentWrapper = elements.contentWrapper;
      this.headerContainer = elements.headerContainer;
      this.bodyContainer = elements.bodyContainer;
      this.footerContainer = elements.footerContainer;
      this.titleContainer = elements.titleContainer;
      this.title = elements.title;
    }
    createElements() {
      const container = UI.createEl("vot-block", ["vot-menu"]);
      container.hidden = true;
      container.id = this.menuId;
      container.dataset.position = this._position;
      container.setAttribute("role", "dialog");
      container.setAttribute("aria-modal", "false");
      container.setAttribute("aria-hidden", "true");
      container.toggleAttribute("inert", true);
      const contentWrapper = UI.createEl("vot-block", [
        "vot-menu-content-wrapper"
      ]);
      container.appendChild(contentWrapper);
      const headerContainer = UI.createEl("vot-block", [
        "vot-menu-header-container"
      ]);
      const titleContainer = UI.createEl("vot-block", [
        "vot-menu-title-container"
      ]);
      headerContainer.appendChild(titleContainer);
      const title = UI.createEl("vot-block", ["vot-menu-title"]);
      title.id = this.titleId;
      title.append(this._titleHtml);
      titleContainer.appendChild(title);
      container.setAttribute("aria-labelledby", this.titleId);
      const bodyContainer = UI.createEl("vot-block", ["vot-menu-body-container"]);
      const footerContainer = UI.createEl("vot-block", [
        "vot-menu-footer-container"
      ]);
      contentWrapper.append(headerContainer, bodyContainer, footerContainer);
      return {
        container,
        contentWrapper,
        headerContainer,
        bodyContainer,
        footerContainer,
        titleContainer,
        title
      };
    }
    setText(titleText) {
      this._titleHtml = this.title.textContent = titleText;
      return this;
    }
    remove() {
      this.container.remove();
      return this;
    }
    set hidden(isHidden) {
      setHiddenState(this.container, isHidden);
      this.container.setAttribute("aria-hidden", isHidden ? "true" : "false");
      this.container.toggleAttribute("inert", isHidden);
    }
    get hidden() {
      return getHiddenState(this.container);
    }
    get position() {
      return this._position;
    }
    set position(position2) {
      this._position = this.container.dataset.position = position2;
    }
  }
  class VOTRail {
    container;
    translateGroup;
    translateButton;
    translateLabel;
    translateChevron;
    subtitlesButton;
    pipButton;
    menuButton;
    separator2;
    _opacity = 1;
    _position;
    _direction;
    _status;
    _labelText;
    constructor({
      position: position2 = "left",
      direction = "column",
      status = "none",
      labelHtml = ""
    } = {}) {
      this._position = position2;
      this._direction = direction;
      this._status = status;
      this._labelText = labelHtml;
      const elements = this.createElements();
      this.container = elements.container;
      this.translateGroup = elements.translateGroup;
      this.translateButton = elements.translateButton;
      this.translateLabel = elements.translateLabel;
      this.translateChevron = elements.translateChevron;
      this.subtitlesButton = elements.subtitlesButton;
      this.pipButton = elements.pipButton;
      this.menuButton = elements.menuButton;
      this.separator2 = this.pipButton;
    }
    createElements() {
      const container = UI.createEl("vot-block", ["vot-rail"]);
      container.dataset.position = this._position;
      container.dataset.direction = this._direction;
      container.dataset.status = this._status;
      const translateGroup = UI.createEl("vot-block", [
        "vot-rail-translate-group"
      ]);
      const translateButton = UI.createEl("vot-block", [
        "vot-rail-button",
        "vot-translate-button",
        "vot-rail-button-primary",
        "vot-rail-button-primary-action"
      ]);
      UI.makeButtonLike(translateButton, {
        ariaLabel: this._labelText || "Translate"
      });
      const translateIcon = UI.createEl("vot-block", [
        "vot-rail-button-icon",
        "vot-rail-button-icon--translate"
      ]);
      D(TRANSLATE_ICON_SVG, translateIcon);
      const voiceIcon = UI.createEl("vot-block", [
        "vot-rail-button-icon",
        "vot-rail-button-icon--voice"
      ]);
      D(VOICE_WAVE_ICON, voiceIcon);
      translateButton.setAttribute("title", this._labelText || "Translate");
      const translateLabel = UI.createEl("span", ["vot-rail-button-label"]);
      translateLabel.textContent = this._labelText || "Translate";
      const translateChevron = UI.createEl("vot-block", [
        "vot-rail-button",
        "vot-rail-button-chevron"
      ]);
      UI.makeButtonLike(translateChevron, {
        ariaLabel: "Select voice mode"
      });
      translateChevron.setAttribute("aria-haspopup", "dialog");
      translateChevron.setAttribute("aria-expanded", "false");
      D(CHEVRON_ICON, translateChevron);
      translateButton.append(translateIcon, voiceIcon, translateLabel);
      translateGroup.append(translateButton, translateChevron);
      const subtitlesButton = UI.createEl("vot-block", [
        "vot-rail-button",
        "vot-rail-button-secondary",
        "vot-subtitles-button"
      ]);
      UI.makeButtonLike(subtitlesButton, {
        ariaLabel: "Subtitles"
      });
      D(SUBTITLES_ICON, subtitlesButton);
      subtitlesButton.setAttribute("title", "Subtitles");
      const pipButton = UI.createEl("vot-block", [
        "vot-rail-button",
        "vot-rail-button-secondary",
        "vot-pip-button"
      ]);
      UI.makeButtonLike(pipButton, {
        ariaLabel: "Picture in picture"
      });
      D(PIP_ICON_SVG, pipButton);
      pipButton.setAttribute("title", "Picture in picture");
      const menuButton = UI.createEl("vot-block", [
        "vot-rail-button",
        "vot-rail-button-secondary",
        "vot-menu-button"
      ]);
      UI.makeButtonLike(menuButton, {
        ariaLabel: "Menu"
      });
      menuButton.setAttribute("aria-haspopup", "dialog");
      menuButton.setAttribute("aria-expanded", "false");
      D(MENU_ICON, menuButton);
      menuButton.setAttribute("title", "Menu");
      container.append(translateGroup, subtitlesButton, pipButton, menuButton);
      return {
        container,
        translateGroup,
        translateButton,
        translateLabel,
        translateChevron,
        subtitlesButton,
        pipButton,
        menuButton
      };
    }
    setText(labelText) {
      this._labelText = labelText;
      const nextLabel = labelText || "Translate";
      this.translateLabel.textContent = nextLabel;
      this.translateButton.setAttribute("aria-label", nextLabel);
      this.translateButton.setAttribute("title", nextLabel);
      return this;
    }
    setButtonLabels({
      subtitles,
      pip,
      menu
    }) {
      if (subtitles) {
        this.subtitlesButton.setAttribute("aria-label", subtitles);
        this.subtitlesButton.setAttribute("title", subtitles);
      }
      if (pip) {
        this.pipButton.setAttribute("aria-label", pip);
        this.pipButton.setAttribute("title", pip);
      }
      if (menu) {
        this.menuButton.setAttribute("aria-label", menu);
        this.menuButton.setAttribute("title", menu);
      }
      return this;
    }
    showPiPButton(visible) {
      this.pipButton.hidden = !visible;
      return this;
    }
    showSubtitlesButton(visible) {
      this.subtitlesButton.hidden = !visible;
      return this;
    }
    remove() {
      this.container.remove();
      return this;
    }
    get tooltipPos() {
      return this.position === "right" ? "left" : "right";
    }
    set status(status) {
      this._status = status;
      this.container.dataset.status = status;
    }
    get status() {
      return this._status;
    }
    set loading(isLoading) {
      this.container.dataset.loading = isLoading.toString();
    }
    get loading() {
      return this.container.dataset.loading === "true";
    }
    set hidden(isHidden) {
      setHiddenState(this.container, isHidden);
    }
    get hidden() {
      return getHiddenState(this.container);
    }
    get position() {
      return this._position;
    }
    set position(position2) {
      this._position = position2;
      this.container.dataset.position = position2;
    }
    get direction() {
      return this._direction;
    }
    set direction(direction) {
      this._direction = direction;
      this.container.dataset.direction = direction;
    }
    set opacity(opacity) {
      const nextOpacity = Number.isFinite(opacity) ? opacity : 1;
      this._opacity = nextOpacity;
      this.container.classList.toggle("vot-rail--hidden", nextOpacity <= 0.01);
    }
    get opacity() {
      return this._opacity;
    }
  }
  const subtitleFormats = ["srt", "vtt", "ass", "json"];
  const subtitleFontFamilies = [
    "default-sans",
    "arial",
    "helvetica",
    "roboto",
    "verdana",
    "open-sans",
    "poppins",
    "lato",
    "montserrat",
    "barlow"
  ];
  const subtitleFontFamilyCss = {
    "default-sans": `"Roboto", "Segoe UI", system-ui, sans-serif`,
    arial: `Arial, "Helvetica Neue", Helvetica, sans-serif`,
    helvetica: `"Helvetica Neue", Helvetica, Arial, sans-serif`,
    roboto: `"Roboto", "Segoe UI", system-ui, sans-serif`,
    verdana: `Verdana, Geneva, sans-serif`,
    "open-sans": `"Open Sans", "Segoe UI", system-ui, sans-serif`,
    poppins: `"Poppins", "Segoe UI", system-ui, sans-serif`,
    lato: `"Lato", "Segoe UI", system-ui, sans-serif`,
    montserrat: `"Montserrat", "Segoe UI", system-ui, sans-serif`,
    barlow: `"Barlow", "Segoe UI", system-ui, sans-serif`
  };
  function isBuiltInSubtitleFontFamily(fontFamily) {
    return subtitleFontFamilies.includes(fontFamily);
  }
  const GOOGLE_SUBTITLE_FONT_PREFIX = "google:";
  const GOOGLE_FONTS_CSS_API_URL = "https://fonts.googleapis.com/css2";
  const GOOGLE_FONTS_METADATA_URL = "https://fonts.google.com/metadata/fonts";
  const subtitleGoogleFontFamilyNames = {
    roboto: "Roboto",
    "open-sans": "Open Sans",
    poppins: "Poppins",
    lato: "Lato",
    montserrat: "Montserrat",
    barlow: "Barlow"
  };
  const loadedSubtitleGoogleFonts = new Set();
  const pendingSubtitleGoogleFonts = new Map();
  let googleFontsCatalogPromise = null;
  function toGoogleSubtitleFontFamily(familyName) {
    return `${GOOGLE_SUBTITLE_FONT_PREFIX}${familyName}`;
  }
  function getGoogleSubtitleFontFamilyName(fontFamily) {
    if (fontFamily.startsWith(GOOGLE_SUBTITLE_FONT_PREFIX)) {
      const familyName = fontFamily.slice(GOOGLE_SUBTITLE_FONT_PREFIX.length).trim();
      return familyName.length > 0 ? familyName : null;
    }
    return subtitleGoogleFontFamilyNames[fontFamily] ?? null;
  }
  function getSubtitleFontFamilyCssValue(fontFamily) {
    if (isBuiltInSubtitleFontFamily(fontFamily)) {
      return subtitleFontFamilyCss[fontFamily];
    }
    const googleFontFamilyName = getGoogleSubtitleFontFamilyName(fontFamily);
    if (googleFontFamilyName) {
      return `"${googleFontFamilyName}", "Segoe UI", system-ui, sans-serif`;
    }
    return subtitleFontFamilyCss["default-sans"];
  }
  function buildGoogleFontsCssUrl(fontFamily) {
    const googleFontFamily = getGoogleSubtitleFontFamilyName(fontFamily);
    if (!googleFontFamily) {
      return null;
    }
    const familyQuery = googleFontFamily.trim().replaceAll(/\s+/g, "+");
    return `${GOOGLE_FONTS_CSS_API_URL}?family=${familyQuery}&display=swap`;
  }
  function injectFontStylesheet(fontFamily, cssText) {
    const styleId = `vot-google-font-${fontFamily}`;
    if (document.getElementById(styleId)) {
      return;
    }
    const gmAddStyle = globalThis.GM_addStyle;
    const styleElement = typeof gmAddStyle === "function" ? gmAddStyle(cssText) : document.createElement("style");
    if (!(styleElement instanceof HTMLElement)) {
      return;
    }
    styleElement.id = styleId;
    if (!styleElement.textContent) {
      styleElement.textContent = cssText;
    }
    if (!styleElement.parentElement) {
      (document.head || document.documentElement).appendChild(styleElement);
    }
  }
  async function ensureGoogleSubtitleFontLoaded(fontFamily, options = {}) {
    if (loadedSubtitleGoogleFonts.has(fontFamily)) {
      return;
    }
    const existingLoad = pendingSubtitleGoogleFonts.get(fontFamily);
    if (existingLoad !== void 0) {
      await existingLoad;
      return;
    }
    const cssUrl = buildGoogleFontsCssUrl(fontFamily);
    if (!cssUrl) {
      loadedSubtitleGoogleFonts.add(fontFamily);
      return;
    }
    const googleFontFamily = getGoogleSubtitleFontFamilyName(fontFamily);
    const loadPromise = (async () => {
      try {
        const response = await GM_fetch(cssUrl, {
          timeout: 1e4,
          forceGmXhr: options.forceGmXhr ?? true,
          headers: {
            Accept: "text/css,*/*;q=0.1"
          }
        });
        if (!response.ok) {
          throw new Error(
            `Google Fonts CSS request failed with ${response.status}`
          );
        }
        const cssText = await response.text();
        if (!cssText.trim()) {
          throw new Error("Google Fonts CSS response is empty");
        }
        injectFontStylesheet(fontFamily, cssText);
        loadedSubtitleGoogleFonts.add(fontFamily);
        if (document.fonts && googleFontFamily) {
          await document.fonts.load(`500 20px "${googleFontFamily}"`);
        }
        options.onLoaded?.();
      } catch (error2) {
        debug.log("Failed to load Google Font for subtitles", {
          fontFamily,
          error: error2
        });
      } finally {
        pendingSubtitleGoogleFonts.delete(fontFamily);
      }
    })();
    pendingSubtitleGoogleFonts.set(fontFamily, loadPromise);
    await loadPromise;
  }
  async function loadGoogleFontsCatalog() {
    if (googleFontsCatalogPromise !== null) {
      return await googleFontsCatalogPromise;
    }
    googleFontsCatalogPromise = (async () => {
      const response = await GM_fetch(GOOGLE_FONTS_METADATA_URL, {
        timeout: 15e3,
        forceGmXhr: isSupportGMXhr
      });
      if (!response.ok) {
        throw new Error(
          `Google Fonts metadata request failed with ${response.status}`
        );
      }
      const rawText = await response.text();
      const sanitizedText = rawText.replace(/^\)\]\}'\n?/, "");
      const payload = JSON.parse(sanitizedText);
      const fontFamilies = payload.familyMetadataList?.map((entry) => entry.family?.trim() ?? "").filter((familyName) => familyName.length > 0);
      return Array.from(new Set(fontFamilies)).sort(
        (left, right) => left.localeCompare(right)
      );
    })().catch((error2) => {
      googleFontsCatalogPromise = null;
      debug.log("Failed to load Google Fonts catalog", error2);
      return [];
    });
    return await googleFontsCatalogPromise;
  }
  const positions$1 = ["default", "top", "left", "right"];
  const BROWSER_ALIASES_MAP = {
    AmazonBot: "amazonbot",
    "Amazon Silk": "amazon_silk",
    "Android Browser": "android",
    BaiduSpider: "baiduspider",
    Bada: "bada",
    BingCrawler: "bingcrawler",
    Brave: "brave",
    BlackBerry: "blackberry",
    "ChatGPT-User": "chatgpt_user",
    Chrome: "chrome",
    ClaudeBot: "claudebot",
    Chromium: "chromium",
    Diffbot: "diffbot",
    DuckDuckBot: "duckduckbot",
    DuckDuckGo: "duckduckgo",
    Electron: "electron",
    Epiphany: "epiphany",
    FacebookExternalHit: "facebookexternalhit",
    Firefox: "firefox",
    Focus: "focus",
    Generic: "generic",
    "Google Search": "google_search",
    Googlebot: "googlebot",
    GPTBot: "gptbot",
    "Internet Explorer": "ie",
    InternetArchiveCrawler: "internetarchivecrawler",
    "K-Meleon": "k_meleon",
    LibreWolf: "librewolf",
    Linespider: "linespider",
    Maxthon: "maxthon",
    "Meta-ExternalAds": "meta_externalads",
    "Meta-ExternalAgent": "meta_externalagent",
    "Meta-ExternalFetcher": "meta_externalfetcher",
    "Meta-WebIndexer": "meta_webindexer",
    "Microsoft Edge": "edge",
    "MZ Browser": "mz",
    "NAVER Whale Browser": "naver",
    "OAI-SearchBot": "oai_searchbot",
    Omgilibot: "omgilibot",
    Opera: "opera",
    "Opera Coast": "opera_coast",
    "Pale Moon": "pale_moon",
    PerplexityBot: "perplexitybot",
    "Perplexity-User": "perplexity_user",
    PhantomJS: "phantomjs",
    PingdomBot: "pingdombot",
    Puffin: "puffin",
    QQ: "qq",
    QQLite: "qqlite",
    QupZilla: "qupzilla",
    Roku: "roku",
    Safari: "safari",
    Sailfish: "sailfish",
    "Samsung Internet for Android": "samsung_internet",
    SlackBot: "slackbot",
    SeaMonkey: "seamonkey",
    Sleipnir: "sleipnir",
    "Sogou Browser": "sogou",
    Swing: "swing",
    Tizen: "tizen",
    "UC Browser": "uc",
    Vivaldi: "vivaldi",
    "WebOS Browser": "webos",
    WeChat: "wechat",
    YahooSlurp: "yahooslurp",
    "Yandex Browser": "yandex",
    YandexBot: "yandexbot",
    YouBot: "youbot"
  };
  const BROWSER_MAP = {
    amazonbot: "AmazonBot",
    amazon_silk: "Amazon Silk",
    android: "Android Browser",
    baiduspider: "BaiduSpider",
    bada: "Bada",
    bingcrawler: "BingCrawler",
    blackberry: "BlackBerry",
    brave: "Brave",
    chatgpt_user: "ChatGPT-User",
    chrome: "Chrome",
    claudebot: "ClaudeBot",
    chromium: "Chromium",
    diffbot: "Diffbot",
    duckduckbot: "DuckDuckBot",
    duckduckgo: "DuckDuckGo",
    edge: "Microsoft Edge",
    electron: "Electron",
    epiphany: "Epiphany",
    facebookexternalhit: "FacebookExternalHit",
    firefox: "Firefox",
    focus: "Focus",
    generic: "Generic",
    google_search: "Google Search",
    googlebot: "Googlebot",
    gptbot: "GPTBot",
    ie: "Internet Explorer",
    internetarchivecrawler: "InternetArchiveCrawler",
    k_meleon: "K-Meleon",
    librewolf: "LibreWolf",
    linespider: "Linespider",
    maxthon: "Maxthon",
    meta_externalads: "Meta-ExternalAds",
    meta_externalagent: "Meta-ExternalAgent",
    meta_externalfetcher: "Meta-ExternalFetcher",
    meta_webindexer: "Meta-WebIndexer",
    mz: "MZ Browser",
    naver: "NAVER Whale Browser",
    oai_searchbot: "OAI-SearchBot",
    omgilibot: "Omgilibot",
    opera: "Opera",
    opera_coast: "Opera Coast",
    pale_moon: "Pale Moon",
    perplexitybot: "PerplexityBot",
    perplexity_user: "Perplexity-User",
    phantomjs: "PhantomJS",
    pingdombot: "PingdomBot",
    puffin: "Puffin",
    qq: "QQ Browser",
    qqlite: "QQ Browser Lite",
    qupzilla: "QupZilla",
    roku: "Roku",
    safari: "Safari",
    sailfish: "Sailfish",
    samsung_internet: "Samsung Internet for Android",
    seamonkey: "SeaMonkey",
    slackbot: "SlackBot",
    sleipnir: "Sleipnir",
    sogou: "Sogou Browser",
    swing: "Swing",
    tizen: "Tizen",
    uc: "UC Browser",
    vivaldi: "Vivaldi",
    webos: "WebOS Browser",
    wechat: "WeChat",
    yahooslurp: "YahooSlurp",
    yandex: "Yandex Browser",
    yandexbot: "YandexBot",
    youbot: "YouBot"
  };
  const PLATFORMS_MAP = {
    bot: "bot",
    desktop: "desktop",
    mobile: "mobile",
    tablet: "tablet",
    tv: "tv"
  };
  const OS_MAP = {
    Android: "Android",
    Bada: "Bada",
    BlackBerry: "BlackBerry",
    ChromeOS: "Chrome OS",
    HarmonyOS: "HarmonyOS",
    iOS: "iOS",
    Linux: "Linux",
    MacOS: "macOS",
    PlayStation4: "PlayStation 4",
    Roku: "Roku",
    Tizen: "Tizen",
    WebOS: "WebOS",
    Windows: "Windows",
    WindowsPhone: "Windows Phone"
  };
  const ENGINE_MAP = {
    Blink: "Blink",
    EdgeHTML: "EdgeHTML",
    Gecko: "Gecko",
    Presto: "Presto",
    Trident: "Trident",
    WebKit: "WebKit"
  };
  class Utils {
static getFirstMatch(regexp, ua) {
      const match = ua.match(regexp);
      return match && match.length > 0 && match[1] || "";
    }
static getSecondMatch(regexp, ua) {
      const match = ua.match(regexp);
      return match && match.length > 1 && match[2] || "";
    }
static matchAndReturnConst(regexp, ua, _const) {
      if (regexp.test(ua)) {
        return _const;
      }
      return void 0;
    }
    static getWindowsVersionName(version) {
      switch (version) {
        case "NT":
          return "NT";
        case "XP":
          return "XP";
        case "NT 5.0":
          return "2000";
        case "NT 5.1":
          return "XP";
        case "NT 5.2":
          return "2003";
        case "NT 6.0":
          return "Vista";
        case "NT 6.1":
          return "7";
        case "NT 6.2":
          return "8";
        case "NT 6.3":
          return "8.1";
        case "NT 10.0":
          return "10";
        default:
          return void 0;
      }
    }
static getMacOSVersionName(version) {
      const v2 = version.split(".").splice(0, 2).map((s2) => parseInt(s2, 10) || 0);
      v2.push(0);
      const major = v2[0];
      const minor = v2[1];
      if (major === 10) {
        switch (minor) {
          case 5:
            return "Leopard";
          case 6:
            return "Snow Leopard";
          case 7:
            return "Lion";
          case 8:
            return "Mountain Lion";
          case 9:
            return "Mavericks";
          case 10:
            return "Yosemite";
          case 11:
            return "El Capitan";
          case 12:
            return "Sierra";
          case 13:
            return "High Sierra";
          case 14:
            return "Mojave";
          case 15:
            return "Catalina";
          default:
            return void 0;
        }
      }
      switch (major) {
        case 11:
          return "Big Sur";
        case 12:
          return "Monterey";
        case 13:
          return "Ventura";
        case 14:
          return "Sonoma";
        case 15:
          return "Sequoia";
        default:
          return void 0;
      }
    }
static getAndroidVersionName(version) {
      const v2 = version.split(".").splice(0, 2).map((s2) => parseInt(s2, 10) || 0);
      v2.push(0);
      if (v2[0] === 1 && v2[1] < 5) return void 0;
      if (v2[0] === 1 && v2[1] < 6) return "Cupcake";
      if (v2[0] === 1 && v2[1] >= 6) return "Donut";
      if (v2[0] === 2 && v2[1] < 2) return "Eclair";
      if (v2[0] === 2 && v2[1] === 2) return "Froyo";
      if (v2[0] === 2 && v2[1] > 2) return "Gingerbread";
      if (v2[0] === 3) return "Honeycomb";
      if (v2[0] === 4 && v2[1] < 1) return "Ice Cream Sandwich";
      if (v2[0] === 4 && v2[1] < 4) return "Jelly Bean";
      if (v2[0] === 4 && v2[1] >= 4) return "KitKat";
      if (v2[0] === 5) return "Lollipop";
      if (v2[0] === 6) return "Marshmallow";
      if (v2[0] === 7) return "Nougat";
      if (v2[0] === 8) return "Oreo";
      if (v2[0] === 9) return "Pie";
      return void 0;
    }
static getVersionPrecision(version) {
      return version.split(".").length;
    }

static compareVersions(versionA, versionB, isLoose = false) {
      const versionAPrecision = Utils.getVersionPrecision(versionA);
      const versionBPrecision = Utils.getVersionPrecision(versionB);
      let precision = Math.max(versionAPrecision, versionBPrecision);
      let lastPrecision = 0;
      const chunks = Utils.map([versionA, versionB], (version) => {
        const delta = precision - Utils.getVersionPrecision(version);
        const _version = version + new Array(delta + 1).join(".0");
        return Utils.map(_version.split("."), (chunk) => new Array(20 - chunk.length).join("0") + chunk).reverse();
      });
      if (isLoose) {
        lastPrecision = precision - Math.min(versionAPrecision, versionBPrecision);
      }
      precision -= 1;
      while (precision >= lastPrecision) {
        if (chunks[0][precision] > chunks[1][precision]) {
          return 1;
        }
        if (chunks[0][precision] === chunks[1][precision]) {
          if (precision === lastPrecision) {
            return 0;
          }
          precision -= 1;
        } else if (chunks[0][precision] < chunks[1][precision]) {
          return -1;
        }
      }
      return void 0;
    }
static map(arr, iterator) {
      const result = [];
      let i2;
      if (Array.prototype.map) {
        return Array.prototype.map.call(arr, iterator);
      }
      for (i2 = 0; i2 < arr.length; i2 += 1) {
        result.push(iterator(arr[i2]));
      }
      return result;
    }
static find(arr, predicate) {
      let i2;
      let l2;
      if (Array.prototype.find) {
        return Array.prototype.find.call(arr, predicate);
      }
      for (i2 = 0, l2 = arr.length; i2 < l2; i2 += 1) {
        const value = arr[i2];
        if (predicate(value, i2)) {
          return value;
        }
      }
      return void 0;
    }
static assign(obj, ...assigners) {
      const result = obj;
      let i2;
      let l2;
      if (Object.assign) {
        return Object.assign(obj, ...assigners);
      }
      for (i2 = 0, l2 = assigners.length; i2 < l2; i2 += 1) {
        const assigner = assigners[i2];
        if (typeof assigner === "object" && assigner !== null) {
          const keys = Object.keys(assigner);
          keys.forEach((key) => {
            result[key] = assigner[key];
          });
        }
      }
      return obj;
    }
static getBrowserAlias(browserName) {
      return BROWSER_ALIASES_MAP[browserName];
    }
static getBrowserTypeByAlias(browserAlias) {
      return BROWSER_MAP[browserAlias] || "";
    }
  }
  const commonVersionIdentifier = /version\/(\d+(\.?_?\d+)+)/i;
  const browsersList = [
{
      test: [/gptbot/i],
      describe(ua) {
        const browser = {
          name: "GPTBot"
        };
        const version = Utils.getFirstMatch(/gptbot\/(\d+(\.\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
{
      test: [/chatgpt-user/i],
      describe(ua) {
        const browser = {
          name: "ChatGPT-User"
        };
        const version = Utils.getFirstMatch(/chatgpt-user\/(\d+(\.\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
{
      test: [/oai-searchbot/i],
      describe(ua) {
        const browser = {
          name: "OAI-SearchBot"
        };
        const version = Utils.getFirstMatch(/oai-searchbot\/(\d+(\.\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
{
      test: [/claudebot/i, /claude-web/i, /claude-user/i, /claude-searchbot/i],
      describe(ua) {
        const browser = {
          name: "ClaudeBot"
        };
        const version = Utils.getFirstMatch(/(?:claudebot|claude-web|claude-user|claude-searchbot)\/(\d+(\.\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
{
      test: [/omgilibot/i, /webzio-extended/i],
      describe(ua) {
        const browser = {
          name: "Omgilibot"
        };
        const version = Utils.getFirstMatch(/(?:omgilibot|webzio-extended)\/(\d+(\.\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
{
      test: [/diffbot/i],
      describe(ua) {
        const browser = {
          name: "Diffbot"
        };
        const version = Utils.getFirstMatch(/diffbot\/(\d+(\.\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
{
      test: [/perplexitybot/i],
      describe(ua) {
        const browser = {
          name: "PerplexityBot"
        };
        const version = Utils.getFirstMatch(/perplexitybot\/(\d+(\.\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
{
      test: [/perplexity-user/i],
      describe(ua) {
        const browser = {
          name: "Perplexity-User"
        };
        const version = Utils.getFirstMatch(/perplexity-user\/(\d+(\.\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
{
      test: [/youbot/i],
      describe(ua) {
        const browser = {
          name: "YouBot"
        };
        const version = Utils.getFirstMatch(/youbot\/(\d+(\.\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
{
      test: [/meta-webindexer/i],
      describe(ua) {
        const browser = {
          name: "Meta-WebIndexer"
        };
        const version = Utils.getFirstMatch(/meta-webindexer\/(\d+(\.\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
{
      test: [/meta-externalads/i],
      describe(ua) {
        const browser = {
          name: "Meta-ExternalAds"
        };
        const version = Utils.getFirstMatch(/meta-externalads\/(\d+(\.\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
{
      test: [/meta-externalagent/i],
      describe(ua) {
        const browser = {
          name: "Meta-ExternalAgent"
        };
        const version = Utils.getFirstMatch(/meta-externalagent\/(\d+(\.\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
{
      test: [/meta-externalfetcher/i],
      describe(ua) {
        const browser = {
          name: "Meta-ExternalFetcher"
        };
        const version = Utils.getFirstMatch(/meta-externalfetcher\/(\d+(\.\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
{
      test: [/googlebot/i],
      describe(ua) {
        const browser = {
          name: "Googlebot"
        };
        const version = Utils.getFirstMatch(/googlebot\/(\d+(\.\d+))/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
{
      test: [/linespider/i],
      describe(ua) {
        const browser = {
          name: "Linespider"
        };
        const version = Utils.getFirstMatch(/(?:linespider)(?:-[-\w]+)?[\s/](\d+(\.\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
{
      test: [/amazonbot/i],
      describe(ua) {
        const browser = {
          name: "AmazonBot"
        };
        const version = Utils.getFirstMatch(/amazonbot\/(\d+(\.\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
{
      test: [/bingbot/i],
      describe(ua) {
        const browser = {
          name: "BingCrawler"
        };
        const version = Utils.getFirstMatch(/bingbot\/(\d+(\.\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
{
      test: [/baiduspider/i],
      describe(ua) {
        const browser = {
          name: "BaiduSpider"
        };
        const version = Utils.getFirstMatch(/baiduspider\/(\d+(\.\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
{
      test: [/duckduckbot/i],
      describe(ua) {
        const browser = {
          name: "DuckDuckBot"
        };
        const version = Utils.getFirstMatch(/duckduckbot\/(\d+(\.\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
{
      test: [/ia_archiver/i],
      describe(ua) {
        const browser = {
          name: "InternetArchiveCrawler"
        };
        const version = Utils.getFirstMatch(/ia_archiver\/(\d+(\.\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
{
      test: [/facebookexternalhit/i, /facebookcatalog/i],
      describe() {
        return {
          name: "FacebookExternalHit"
        };
      }
    },
{
      test: [/slackbot/i, /slack-imgProxy/i],
      describe(ua) {
        const browser = {
          name: "SlackBot"
        };
        const version = Utils.getFirstMatch(/(?:slackbot|slack-imgproxy)(?:-[-\w]+)?[\s/](\d+(\.\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
{
      test: [/yahoo!?[\s/]*slurp/i],
      describe() {
        return {
          name: "YahooSlurp"
        };
      }
    },
{
      test: [/yandexbot/i, /yandexmobilebot/i],
      describe() {
        return {
          name: "YandexBot"
        };
      }
    },
{
      test: [/pingdom/i],
      describe() {
        return {
          name: "PingdomBot"
        };
      }
    },
{
      test: [/opera/i],
      describe(ua) {
        const browser = {
          name: "Opera"
        };
        const version = Utils.getFirstMatch(commonVersionIdentifier, ua) || Utils.getFirstMatch(/(?:opera)[\s/](\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
{
      test: [/opr\/|opios/i],
      describe(ua) {
        const browser = {
          name: "Opera"
        };
        const version = Utils.getFirstMatch(/(?:opr|opios)[\s/](\S+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/SamsungBrowser/i],
      describe(ua) {
        const browser = {
          name: "Samsung Internet for Android"
        };
        const version = Utils.getFirstMatch(commonVersionIdentifier, ua) || Utils.getFirstMatch(/(?:SamsungBrowser)[\s/](\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/Whale/i],
      describe(ua) {
        const browser = {
          name: "NAVER Whale Browser"
        };
        const version = Utils.getFirstMatch(commonVersionIdentifier, ua) || Utils.getFirstMatch(/(?:whale)[\s/](\d+(?:\.\d+)+)/i, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/PaleMoon/i],
      describe(ua) {
        const browser = {
          name: "Pale Moon"
        };
        const version = Utils.getFirstMatch(commonVersionIdentifier, ua) || Utils.getFirstMatch(/(?:PaleMoon)[\s/](\d+(?:\.\d+)+)/i, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/MZBrowser/i],
      describe(ua) {
        const browser = {
          name: "MZ Browser"
        };
        const version = Utils.getFirstMatch(/(?:MZBrowser)[\s/](\d+(?:\.\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/focus/i],
      describe(ua) {
        const browser = {
          name: "Focus"
        };
        const version = Utils.getFirstMatch(/(?:focus)[\s/](\d+(?:\.\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/swing/i],
      describe(ua) {
        const browser = {
          name: "Swing"
        };
        const version = Utils.getFirstMatch(/(?:swing)[\s/](\d+(?:\.\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/coast/i],
      describe(ua) {
        const browser = {
          name: "Opera Coast"
        };
        const version = Utils.getFirstMatch(commonVersionIdentifier, ua) || Utils.getFirstMatch(/(?:coast)[\s/](\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/opt\/\d+(?:.?_?\d+)+/i],
      describe(ua) {
        const browser = {
          name: "Opera Touch"
        };
        const version = Utils.getFirstMatch(/(?:opt)[\s/](\d+(\.?_?\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/yabrowser/i],
      describe(ua) {
        const browser = {
          name: "Yandex Browser"
        };
        const version = Utils.getFirstMatch(/(?:yabrowser)[\s/](\d+(\.?_?\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/ucbrowser/i],
      describe(ua) {
        const browser = {
          name: "UC Browser"
        };
        const version = Utils.getFirstMatch(commonVersionIdentifier, ua) || Utils.getFirstMatch(/(?:ucbrowser)[\s/](\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/Maxthon|mxios/i],
      describe(ua) {
        const browser = {
          name: "Maxthon"
        };
        const version = Utils.getFirstMatch(commonVersionIdentifier, ua) || Utils.getFirstMatch(/(?:Maxthon|mxios)[\s/](\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/epiphany/i],
      describe(ua) {
        const browser = {
          name: "Epiphany"
        };
        const version = Utils.getFirstMatch(commonVersionIdentifier, ua) || Utils.getFirstMatch(/(?:epiphany)[\s/](\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/puffin/i],
      describe(ua) {
        const browser = {
          name: "Puffin"
        };
        const version = Utils.getFirstMatch(commonVersionIdentifier, ua) || Utils.getFirstMatch(/(?:puffin)[\s/](\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/sleipnir/i],
      describe(ua) {
        const browser = {
          name: "Sleipnir"
        };
        const version = Utils.getFirstMatch(commonVersionIdentifier, ua) || Utils.getFirstMatch(/(?:sleipnir)[\s/](\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/k-meleon/i],
      describe(ua) {
        const browser = {
          name: "K-Meleon"
        };
        const version = Utils.getFirstMatch(commonVersionIdentifier, ua) || Utils.getFirstMatch(/(?:k-meleon)[\s/](\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/micromessenger/i],
      describe(ua) {
        const browser = {
          name: "WeChat"
        };
        const version = Utils.getFirstMatch(/(?:micromessenger)[\s/](\d+(\.?_?\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/qqbrowser/i],
      describe(ua) {
        const browser = {
          name: /qqbrowserlite/i.test(ua) ? "QQ Browser Lite" : "QQ Browser"
        };
        const version = Utils.getFirstMatch(/(?:qqbrowserlite|qqbrowser)[/](\d+(\.?_?\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/msie|trident/i],
      describe(ua) {
        const browser = {
          name: "Internet Explorer"
        };
        const version = Utils.getFirstMatch(/(?:msie |rv:)(\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/\sedg\//i],
      describe(ua) {
        const browser = {
          name: "Microsoft Edge"
        };
        const version = Utils.getFirstMatch(/\sedg\/(\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/edg([ea]|ios)/i],
      describe(ua) {
        const browser = {
          name: "Microsoft Edge"
        };
        const version = Utils.getSecondMatch(/edg([ea]|ios)\/(\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/vivaldi/i],
      describe(ua) {
        const browser = {
          name: "Vivaldi"
        };
        const version = Utils.getFirstMatch(/vivaldi\/(\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/seamonkey/i],
      describe(ua) {
        const browser = {
          name: "SeaMonkey"
        };
        const version = Utils.getFirstMatch(/seamonkey\/(\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/sailfish/i],
      describe(ua) {
        const browser = {
          name: "Sailfish"
        };
        const version = Utils.getFirstMatch(/sailfish\s?browser\/(\d+(\.\d+)?)/i, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/silk/i],
      describe(ua) {
        const browser = {
          name: "Amazon Silk"
        };
        const version = Utils.getFirstMatch(/silk\/(\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/phantom/i],
      describe(ua) {
        const browser = {
          name: "PhantomJS"
        };
        const version = Utils.getFirstMatch(/phantomjs\/(\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/slimerjs/i],
      describe(ua) {
        const browser = {
          name: "SlimerJS"
        };
        const version = Utils.getFirstMatch(/slimerjs\/(\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/blackberry|\bbb\d+/i, /rim\stablet/i],
      describe(ua) {
        const browser = {
          name: "BlackBerry"
        };
        const version = Utils.getFirstMatch(commonVersionIdentifier, ua) || Utils.getFirstMatch(/blackberry[\d]+\/(\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/(web|hpw)[o0]s/i],
      describe(ua) {
        const browser = {
          name: "WebOS Browser"
        };
        const version = Utils.getFirstMatch(commonVersionIdentifier, ua) || Utils.getFirstMatch(/w(?:eb)?[o0]sbrowser\/(\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/bada/i],
      describe(ua) {
        const browser = {
          name: "Bada"
        };
        const version = Utils.getFirstMatch(/dolfin\/(\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/tizen/i],
      describe(ua) {
        const browser = {
          name: "Tizen"
        };
        const version = Utils.getFirstMatch(/(?:tizen\s?)?browser\/(\d+(\.?_?\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/qupzilla/i],
      describe(ua) {
        const browser = {
          name: "QupZilla"
        };
        const version = Utils.getFirstMatch(/(?:qupzilla)[\s/](\d+(\.?_?\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/librewolf/i],
      describe(ua) {
        const browser = {
          name: "LibreWolf"
        };
        const version = Utils.getFirstMatch(/(?:librewolf)[\s/](\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/firefox|iceweasel|fxios/i],
      describe(ua) {
        const browser = {
          name: "Firefox"
        };
        const version = Utils.getFirstMatch(/(?:firefox|iceweasel|fxios)[\s/](\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/electron/i],
      describe(ua) {
        const browser = {
          name: "Electron"
        };
        const version = Utils.getFirstMatch(/(?:electron)\/(\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/sogoumobilebrowser/i, /metasr/i, /se 2\.[x]/i],
      describe(ua) {
        const browser = {
          name: "Sogou Browser"
        };
        const sogouMobileVersion = Utils.getFirstMatch(/(?:sogoumobilebrowser)[\s/](\d+(\.?_?\d+)+)/i, ua);
        const chromiumVersion = Utils.getFirstMatch(/(?:chrome|crios|crmo)\/(\d+(\.?_?\d+)+)/i, ua);
        const seVersion = Utils.getFirstMatch(/se ([\d.]+)x/i, ua);
        const version = sogouMobileVersion || chromiumVersion || seVersion;
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/MiuiBrowser/i],
      describe(ua) {
        const browser = {
          name: "Miui"
        };
        const version = Utils.getFirstMatch(/(?:MiuiBrowser)[\s/](\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
{
      test(parser) {
        if (parser.hasBrand("DuckDuckGo")) {
          return true;
        }
        return parser.test(/\sDdg\/[\d.]+$/i);
      },
      describe(ua, parser) {
        const browser = {
          name: "DuckDuckGo"
        };
        if (parser) {
          const hintsVersion = parser.getBrandVersion("DuckDuckGo");
          if (hintsVersion) {
            browser.version = hintsVersion;
            return browser;
          }
        }
        const uaVersion = Utils.getFirstMatch(/\sDdg\/([\d.]+)$/i, ua);
        if (uaVersion) {
          browser.version = uaVersion;
        }
        return browser;
      }
    },
{
      test(parser) {
        return parser.hasBrand("Brave");
      },
      describe(ua, parser) {
        const browser = {
          name: "Brave"
        };
        if (parser) {
          const hintsVersion = parser.getBrandVersion("Brave");
          if (hintsVersion) {
            browser.version = hintsVersion;
            return browser;
          }
        }
        return browser;
      }
    },
    {
      test: [/chromium/i],
      describe(ua) {
        const browser = {
          name: "Chromium"
        };
        const version = Utils.getFirstMatch(/(?:chromium)[\s/](\d+(\.?_?\d+)+)/i, ua) || Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/chrome|crios|crmo/i],
      describe(ua) {
        const browser = {
          name: "Chrome"
        };
        const version = Utils.getFirstMatch(/(?:chrome|crios|crmo)\/(\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
    {
      test: [/GSA/i],
      describe(ua) {
        const browser = {
          name: "Google Search"
        };
        const version = Utils.getFirstMatch(/(?:GSA)\/(\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
{
      test(parser) {
        const notLikeAndroid = !parser.test(/like android/i);
        const butAndroid = parser.test(/android/i);
        return notLikeAndroid && butAndroid;
      },
      describe(ua) {
        const browser = {
          name: "Android Browser"
        };
        const version = Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
{
      test: [/playstation 4/i],
      describe(ua) {
        const browser = {
          name: "PlayStation 4"
        };
        const version = Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
{
      test: [/safari|applewebkit/i],
      describe(ua) {
        const browser = {
          name: "Safari"
        };
        const version = Utils.getFirstMatch(commonVersionIdentifier, ua);
        if (version) {
          browser.version = version;
        }
        return browser;
      }
    },
{
      test: [/.*/i],
      describe(ua) {
        const regexpWithoutDeviceSpec = /^(.*)\/(.*) /;
        const regexpWithDeviceSpec = /^(.*)\/(.*)[ \t]\((.*)/;
        const hasDeviceSpec = ua.search("\\(") !== -1;
        const regexp = hasDeviceSpec ? regexpWithDeviceSpec : regexpWithoutDeviceSpec;
        return {
          name: Utils.getFirstMatch(regexp, ua),
          version: Utils.getSecondMatch(regexp, ua)
        };
      }
    }
  ];
  const osParsersList = [
{
      test: [/Roku\/DVP/],
      describe(ua) {
        const version = Utils.getFirstMatch(/Roku\/DVP-(\d+\.\d+)/i, ua);
        return {
          name: OS_MAP.Roku,
          version
        };
      }
    },
{
      test: [/windows phone/i],
      describe(ua) {
        const version = Utils.getFirstMatch(/windows phone (?:os)?\s?(\d+(\.\d+)*)/i, ua);
        return {
          name: OS_MAP.WindowsPhone,
          version
        };
      }
    },
{
      test: [/windows /i],
      describe(ua) {
        const version = Utils.getFirstMatch(/Windows ((NT|XP)( \d\d?.\d)?)/i, ua);
        const versionName = Utils.getWindowsVersionName(version);
        return {
          name: OS_MAP.Windows,
          version,
          versionName
        };
      }
    },
{
      test: [/Macintosh(.*?) FxiOS(.*?)\//],
      describe(ua) {
        const result = {
          name: OS_MAP.iOS
        };
        const version = Utils.getSecondMatch(/(Version\/)(\d[\d.]+)/, ua);
        if (version) {
          result.version = version;
        }
        return result;
      }
    },
{
      test: [/macintosh/i],
      describe(ua) {
        const version = Utils.getFirstMatch(/mac os x (\d+(\.?_?\d+)+)/i, ua).replace(/[_\s]/g, ".");
        const versionName = Utils.getMacOSVersionName(version);
        const os = {
          name: OS_MAP.MacOS,
          version
        };
        if (versionName) {
          os.versionName = versionName;
        }
        return os;
      }
    },
{
      test: [/(ipod|iphone|ipad)/i],
      describe(ua) {
        const version = Utils.getFirstMatch(/os (\d+([_\s]\d+)*) like mac os x/i, ua).replace(/[_\s]/g, ".");
        return {
          name: OS_MAP.iOS,
          version
        };
      }
    },
{
      test: [/OpenHarmony/i],
      describe(ua) {
        const version = Utils.getFirstMatch(/OpenHarmony\s+(\d+(\.\d+)*)/i, ua);
        return {
          name: OS_MAP.HarmonyOS,
          version
        };
      }
    },
{
      test(parser) {
        const notLikeAndroid = !parser.test(/like android/i);
        const butAndroid = parser.test(/android/i);
        return notLikeAndroid && butAndroid;
      },
      describe(ua) {
        const version = Utils.getFirstMatch(/android[\s/-](\d+(\.\d+)*)/i, ua);
        const versionName = Utils.getAndroidVersionName(version);
        const os = {
          name: OS_MAP.Android,
          version
        };
        if (versionName) {
          os.versionName = versionName;
        }
        return os;
      }
    },
{
      test: [/(web|hpw)[o0]s/i],
      describe(ua) {
        const version = Utils.getFirstMatch(/(?:web|hpw)[o0]s\/(\d+(\.\d+)*)/i, ua);
        const os = {
          name: OS_MAP.WebOS
        };
        if (version && version.length) {
          os.version = version;
        }
        return os;
      }
    },
{
      test: [/blackberry|\bbb\d+/i, /rim\stablet/i],
      describe(ua) {
        const version = Utils.getFirstMatch(/rim\stablet\sos\s(\d+(\.\d+)*)/i, ua) || Utils.getFirstMatch(/blackberry\d+\/(\d+([_\s]\d+)*)/i, ua) || Utils.getFirstMatch(/\bbb(\d+)/i, ua);
        return {
          name: OS_MAP.BlackBerry,
          version
        };
      }
    },
{
      test: [/bada/i],
      describe(ua) {
        const version = Utils.getFirstMatch(/bada\/(\d+(\.\d+)*)/i, ua);
        return {
          name: OS_MAP.Bada,
          version
        };
      }
    },
{
      test: [/tizen/i],
      describe(ua) {
        const version = Utils.getFirstMatch(/tizen[/\s](\d+(\.\d+)*)/i, ua);
        return {
          name: OS_MAP.Tizen,
          version
        };
      }
    },
{
      test: [/linux/i],
      describe() {
        return {
          name: OS_MAP.Linux
        };
      }
    },
{
      test: [/CrOS/],
      describe() {
        return {
          name: OS_MAP.ChromeOS
        };
      }
    },
{
      test: [/PlayStation 4/],
      describe(ua) {
        const version = Utils.getFirstMatch(/PlayStation 4[/\s](\d+(\.\d+)*)/i, ua);
        return {
          name: OS_MAP.PlayStation4,
          version
        };
      }
    }
  ];
  const platformParsersList = [
{
      test: [/googlebot/i],
      describe() {
        return {
          type: PLATFORMS_MAP.bot,
          vendor: "Google"
        };
      }
    },
{
      test: [/linespider/i],
      describe() {
        return {
          type: PLATFORMS_MAP.bot,
          vendor: "Line"
        };
      }
    },
{
      test: [/amazonbot/i],
      describe() {
        return {
          type: PLATFORMS_MAP.bot,
          vendor: "Amazon"
        };
      }
    },
{
      test: [/gptbot/i],
      describe() {
        return {
          type: PLATFORMS_MAP.bot,
          vendor: "OpenAI"
        };
      }
    },
{
      test: [/chatgpt-user/i],
      describe() {
        return {
          type: PLATFORMS_MAP.bot,
          vendor: "OpenAI"
        };
      }
    },
{
      test: [/oai-searchbot/i],
      describe() {
        return {
          type: PLATFORMS_MAP.bot,
          vendor: "OpenAI"
        };
      }
    },
{
      test: [/baiduspider/i],
      describe() {
        return {
          type: PLATFORMS_MAP.bot,
          vendor: "Baidu"
        };
      }
    },
{
      test: [/bingbot/i],
      describe() {
        return {
          type: PLATFORMS_MAP.bot,
          vendor: "Bing"
        };
      }
    },
{
      test: [/duckduckbot/i],
      describe() {
        return {
          type: PLATFORMS_MAP.bot,
          vendor: "DuckDuckGo"
        };
      }
    },
{
      test: [/claudebot/i, /claude-web/i, /claude-user/i, /claude-searchbot/i],
      describe() {
        return {
          type: PLATFORMS_MAP.bot,
          vendor: "Anthropic"
        };
      }
    },
{
      test: [/omgilibot/i, /webzio-extended/i],
      describe() {
        return {
          type: PLATFORMS_MAP.bot,
          vendor: "Webz.io"
        };
      }
    },
{
      test: [/diffbot/i],
      describe() {
        return {
          type: PLATFORMS_MAP.bot,
          vendor: "Diffbot"
        };
      }
    },
{
      test: [/perplexitybot/i],
      describe() {
        return {
          type: PLATFORMS_MAP.bot,
          vendor: "Perplexity AI"
        };
      }
    },
{
      test: [/perplexity-user/i],
      describe() {
        return {
          type: PLATFORMS_MAP.bot,
          vendor: "Perplexity AI"
        };
      }
    },
{
      test: [/youbot/i],
      describe() {
        return {
          type: PLATFORMS_MAP.bot,
          vendor: "You.com"
        };
      }
    },
{
      test: [/ia_archiver/i],
      describe() {
        return {
          type: PLATFORMS_MAP.bot,
          vendor: "Internet Archive"
        };
      }
    },
{
      test: [/meta-webindexer/i],
      describe() {
        return {
          type: PLATFORMS_MAP.bot,
          vendor: "Meta"
        };
      }
    },
{
      test: [/meta-externalads/i],
      describe() {
        return {
          type: PLATFORMS_MAP.bot,
          vendor: "Meta"
        };
      }
    },
{
      test: [/meta-externalagent/i],
      describe() {
        return {
          type: PLATFORMS_MAP.bot,
          vendor: "Meta"
        };
      }
    },
{
      test: [/meta-externalfetcher/i],
      describe() {
        return {
          type: PLATFORMS_MAP.bot,
          vendor: "Meta"
        };
      }
    },
{
      test: [/facebookexternalhit/i, /facebookcatalog/i],
      describe() {
        return {
          type: PLATFORMS_MAP.bot,
          vendor: "Meta"
        };
      }
    },
{
      test: [/slackbot/i, /slack-imgProxy/i],
      describe() {
        return {
          type: PLATFORMS_MAP.bot,
          vendor: "Slack"
        };
      }
    },
{
      test: [/yahoo/i],
      describe() {
        return {
          type: PLATFORMS_MAP.bot,
          vendor: "Yahoo"
        };
      }
    },
{
      test: [/yandexbot/i, /yandexmobilebot/i],
      describe() {
        return {
          type: PLATFORMS_MAP.bot,
          vendor: "Yandex"
        };
      }
    },
{
      test: [/pingdom/i],
      describe() {
        return {
          type: PLATFORMS_MAP.bot,
          vendor: "Pingdom"
        };
      }
    },
{
      test: [/huawei/i],
      describe(ua) {
        const model = Utils.getFirstMatch(/(can-l01)/i, ua) && "Nova";
        const platform = {
          type: PLATFORMS_MAP.mobile,
          vendor: "Huawei"
        };
        if (model) {
          platform.model = model;
        }
        return platform;
      }
    },
{
      test: [/nexus\s*(?:7|8|9|10).*/i],
      describe() {
        return {
          type: PLATFORMS_MAP.tablet,
          vendor: "Nexus"
        };
      }
    },
{
      test: [/ipad/i],
      describe() {
        return {
          type: PLATFORMS_MAP.tablet,
          vendor: "Apple",
          model: "iPad"
        };
      }
    },
{
      test: [/Macintosh(.*?) FxiOS(.*?)\//],
      describe() {
        return {
          type: PLATFORMS_MAP.tablet,
          vendor: "Apple",
          model: "iPad"
        };
      }
    },
{
      test: [/kftt build/i],
      describe() {
        return {
          type: PLATFORMS_MAP.tablet,
          vendor: "Amazon",
          model: "Kindle Fire HD 7"
        };
      }
    },
{
      test: [/silk/i],
      describe() {
        return {
          type: PLATFORMS_MAP.tablet,
          vendor: "Amazon"
        };
      }
    },
{
      test: [/tablet(?! pc)/i],
      describe() {
        return {
          type: PLATFORMS_MAP.tablet
        };
      }
    },
{
      test(parser) {
        const iDevice = parser.test(/ipod|iphone/i);
        const likeIDevice = parser.test(/like (ipod|iphone)/i);
        return iDevice && !likeIDevice;
      },
      describe(ua) {
        const model = Utils.getFirstMatch(/(ipod|iphone)/i, ua);
        return {
          type: PLATFORMS_MAP.mobile,
          vendor: "Apple",
          model
        };
      }
    },
{
      test: [/nexus\s*[0-6].*/i, /galaxy nexus/i],
      describe() {
        return {
          type: PLATFORMS_MAP.mobile,
          vendor: "Nexus"
        };
      }
    },
{
      test: [/Nokia/i],
      describe(ua) {
        const model = Utils.getFirstMatch(/Nokia\s+([0-9]+(\.[0-9]+)?)/i, ua);
        const platform = {
          type: PLATFORMS_MAP.mobile,
          vendor: "Nokia"
        };
        if (model) {
          platform.model = model;
        }
        return platform;
      }
    },
{
      test: [/[^-]mobi/i],
      describe() {
        return {
          type: PLATFORMS_MAP.mobile
        };
      }
    },
{
      test(parser) {
        return parser.getBrowserName(true) === "blackberry";
      },
      describe() {
        return {
          type: PLATFORMS_MAP.mobile,
          vendor: "BlackBerry"
        };
      }
    },
{
      test(parser) {
        return parser.getBrowserName(true) === "bada";
      },
      describe() {
        return {
          type: PLATFORMS_MAP.mobile
        };
      }
    },
{
      test(parser) {
        return parser.getBrowserName() === "windows phone";
      },
      describe() {
        return {
          type: PLATFORMS_MAP.mobile,
          vendor: "Microsoft"
        };
      }
    },
{
      test(parser) {
        const osMajorVersion = Number(String(parser.getOSVersion()).split(".")[0]);
        return parser.getOSName(true) === "android" && osMajorVersion >= 3;
      },
      describe() {
        return {
          type: PLATFORMS_MAP.tablet
        };
      }
    },
{
      test(parser) {
        return parser.getOSName(true) === "android";
      },
      describe() {
        return {
          type: PLATFORMS_MAP.mobile
        };
      }
    },
{
      test: [/smart-?tv|smarttv/i],
      describe() {
        return {
          type: PLATFORMS_MAP.tv
        };
      }
    },
{
      test: [/netcast/i],
      describe() {
        return {
          type: PLATFORMS_MAP.tv
        };
      }
    },
{
      test(parser) {
        return parser.getOSName(true) === "macos";
      },
      describe() {
        return {
          type: PLATFORMS_MAP.desktop,
          vendor: "Apple"
        };
      }
    },
{
      test(parser) {
        return parser.getOSName(true) === "windows";
      },
      describe() {
        return {
          type: PLATFORMS_MAP.desktop
        };
      }
    },
{
      test(parser) {
        return parser.getOSName(true) === "linux";
      },
      describe() {
        return {
          type: PLATFORMS_MAP.desktop
        };
      }
    },
{
      test(parser) {
        return parser.getOSName(true) === "playstation 4";
      },
      describe() {
        return {
          type: PLATFORMS_MAP.tv
        };
      }
    },
{
      test(parser) {
        return parser.getOSName(true) === "roku";
      },
      describe() {
        return {
          type: PLATFORMS_MAP.tv
        };
      }
    }
  ];
  const enginesParsersList = [
{
      test(parser) {
        return parser.getBrowserName(true) === "microsoft edge";
      },
      describe(ua) {
        const isBlinkBased = /\sedg\//i.test(ua);
        if (isBlinkBased) {
          return {
            name: ENGINE_MAP.Blink
          };
        }
        const version = Utils.getFirstMatch(/edge\/(\d+(\.?_?\d+)+)/i, ua);
        return {
          name: ENGINE_MAP.EdgeHTML,
          version
        };
      }
    },
{
      test: [/trident/i],
      describe(ua) {
        const engine = {
          name: ENGINE_MAP.Trident
        };
        const version = Utils.getFirstMatch(/trident\/(\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          engine.version = version;
        }
        return engine;
      }
    },
{
      test(parser) {
        return parser.test(/presto/i);
      },
      describe(ua) {
        const engine = {
          name: ENGINE_MAP.Presto
        };
        const version = Utils.getFirstMatch(/presto\/(\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          engine.version = version;
        }
        return engine;
      }
    },
{
      test(parser) {
        const isGecko = parser.test(/gecko/i);
        const likeGecko = parser.test(/like gecko/i);
        return isGecko && !likeGecko;
      },
      describe(ua) {
        const engine = {
          name: ENGINE_MAP.Gecko
        };
        const version = Utils.getFirstMatch(/gecko\/(\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          engine.version = version;
        }
        return engine;
      }
    },
{
      test: [/(apple)?webkit\/537\.36/i],
      describe() {
        return {
          name: ENGINE_MAP.Blink
        };
      }
    },
{
      test: [/(apple)?webkit/i],
      describe(ua) {
        const engine = {
          name: ENGINE_MAP.WebKit
        };
        const version = Utils.getFirstMatch(/webkit\/(\d+(\.?_?\d+)+)/i, ua);
        if (version) {
          engine.version = version;
        }
        return engine;
      }
    }
  ];
  class Parser {
constructor(UA, skipParsingOrHints = false, clientHints = null) {
      if (UA === void 0 || UA === null || UA === "") {
        throw new Error("UserAgent parameter can't be empty");
      }
      this._ua = UA;
      let skipParsing = false;
      if (typeof skipParsingOrHints === "boolean") {
        skipParsing = skipParsingOrHints;
        this._hints = clientHints;
      } else if (skipParsingOrHints != null && typeof skipParsingOrHints === "object") {
        this._hints = skipParsingOrHints;
      } else {
        this._hints = null;
      }
      this.parsedResult = {};
      if (skipParsing !== true) {
        this.parse();
      }
    }
getHints() {
      return this._hints;
    }
hasBrand(brandName) {
      if (!this._hints || !Array.isArray(this._hints.brands)) {
        return false;
      }
      const brandLower = brandName.toLowerCase();
      return this._hints.brands.some(
        (b2) => b2.brand && b2.brand.toLowerCase() === brandLower
      );
    }
getBrandVersion(brandName) {
      if (!this._hints || !Array.isArray(this._hints.brands)) {
        return void 0;
      }
      const brandLower = brandName.toLowerCase();
      const brand = this._hints.brands.find(
        (b2) => b2.brand && b2.brand.toLowerCase() === brandLower
      );
      return brand ? brand.version : void 0;
    }
getUA() {
      return this._ua;
    }
test(regex) {
      return regex.test(this._ua);
    }
parseBrowser() {
      this.parsedResult.browser = {};
      const browserDescriptor = Utils.find(browsersList, (_browser) => {
        if (typeof _browser.test === "function") {
          return _browser.test(this);
        }
        if (Array.isArray(_browser.test)) {
          return _browser.test.some((condition) => this.test(condition));
        }
        throw new Error("Browser's test function is not valid");
      });
      if (browserDescriptor) {
        this.parsedResult.browser = browserDescriptor.describe(this.getUA(), this);
      }
      return this.parsedResult.browser;
    }
getBrowser() {
      if (this.parsedResult.browser) {
        return this.parsedResult.browser;
      }
      return this.parseBrowser();
    }
getBrowserName(toLowerCase) {
      if (toLowerCase) {
        return String(this.getBrowser().name).toLowerCase() || "";
      }
      return this.getBrowser().name || "";
    }
getBrowserVersion() {
      return this.getBrowser().version;
    }
getOS() {
      if (this.parsedResult.os) {
        return this.parsedResult.os;
      }
      return this.parseOS();
    }
parseOS() {
      this.parsedResult.os = {};
      const os = Utils.find(osParsersList, (_os) => {
        if (typeof _os.test === "function") {
          return _os.test(this);
        }
        if (Array.isArray(_os.test)) {
          return _os.test.some((condition) => this.test(condition));
        }
        throw new Error("Browser's test function is not valid");
      });
      if (os) {
        this.parsedResult.os = os.describe(this.getUA());
      }
      return this.parsedResult.os;
    }
getOSName(toLowerCase) {
      const { name } = this.getOS();
      if (toLowerCase) {
        return String(name).toLowerCase() || "";
      }
      return name || "";
    }
getOSVersion() {
      return this.getOS().version;
    }
getPlatform() {
      if (this.parsedResult.platform) {
        return this.parsedResult.platform;
      }
      return this.parsePlatform();
    }
getPlatformType(toLowerCase = false) {
      const { type } = this.getPlatform();
      if (toLowerCase) {
        return String(type).toLowerCase() || "";
      }
      return type || "";
    }
parsePlatform() {
      this.parsedResult.platform = {};
      const platform = Utils.find(platformParsersList, (_platform) => {
        if (typeof _platform.test === "function") {
          return _platform.test(this);
        }
        if (Array.isArray(_platform.test)) {
          return _platform.test.some((condition) => this.test(condition));
        }
        throw new Error("Browser's test function is not valid");
      });
      if (platform) {
        this.parsedResult.platform = platform.describe(this.getUA());
      }
      return this.parsedResult.platform;
    }
getEngine() {
      if (this.parsedResult.engine) {
        return this.parsedResult.engine;
      }
      return this.parseEngine();
    }
getEngineName(toLowerCase) {
      if (toLowerCase) {
        return String(this.getEngine().name).toLowerCase() || "";
      }
      return this.getEngine().name || "";
    }
parseEngine() {
      this.parsedResult.engine = {};
      const engine = Utils.find(enginesParsersList, (_engine) => {
        if (typeof _engine.test === "function") {
          return _engine.test(this);
        }
        if (Array.isArray(_engine.test)) {
          return _engine.test.some((condition) => this.test(condition));
        }
        throw new Error("Browser's test function is not valid");
      });
      if (engine) {
        this.parsedResult.engine = engine.describe(this.getUA());
      }
      return this.parsedResult.engine;
    }
parse() {
      this.parseBrowser();
      this.parseOS();
      this.parsePlatform();
      this.parseEngine();
      return this;
    }
getResult() {
      return Utils.assign({}, this.parsedResult);
    }
satisfies(checkTree) {
      const platformsAndOSes = {};
      let platformsAndOSCounter = 0;
      const browsers = {};
      let browsersCounter = 0;
      const allDefinitions = Object.keys(checkTree);
      allDefinitions.forEach((key) => {
        const currentDefinition = checkTree[key];
        if (typeof currentDefinition === "string") {
          browsers[key] = currentDefinition;
          browsersCounter += 1;
        } else if (typeof currentDefinition === "object") {
          platformsAndOSes[key] = currentDefinition;
          platformsAndOSCounter += 1;
        }
      });
      if (platformsAndOSCounter > 0) {
        const platformsAndOSNames = Object.keys(platformsAndOSes);
        const OSMatchingDefinition = Utils.find(platformsAndOSNames, (name) => this.isOS(name));
        if (OSMatchingDefinition) {
          const osResult = this.satisfies(platformsAndOSes[OSMatchingDefinition]);
          if (osResult !== void 0) {
            return osResult;
          }
        }
        const platformMatchingDefinition = Utils.find(
          platformsAndOSNames,
          (name) => this.isPlatform(name)
        );
        if (platformMatchingDefinition) {
          const platformResult = this.satisfies(platformsAndOSes[platformMatchingDefinition]);
          if (platformResult !== void 0) {
            return platformResult;
          }
        }
      }
      if (browsersCounter > 0) {
        const browserNames = Object.keys(browsers);
        const matchingDefinition = Utils.find(browserNames, (name) => this.isBrowser(name, true));
        if (matchingDefinition !== void 0) {
          return this.compareVersion(browsers[matchingDefinition]);
        }
      }
      return void 0;
    }
isBrowser(browserName, includingAlias = false) {
      const defaultBrowserName = this.getBrowserName().toLowerCase();
      let browserNameLower = browserName.toLowerCase();
      const alias = Utils.getBrowserTypeByAlias(browserNameLower);
      if (includingAlias && alias) {
        browserNameLower = alias.toLowerCase();
      }
      return browserNameLower === defaultBrowserName;
    }
    compareVersion(version) {
      let expectedResults = [0];
      let comparableVersion = version;
      let isLoose = false;
      const currentBrowserVersion = this.getBrowserVersion();
      if (typeof currentBrowserVersion !== "string") {
        return void 0;
      }
      if (version[0] === ">" || version[0] === "<") {
        comparableVersion = version.substr(1);
        if (version[1] === "=") {
          isLoose = true;
          comparableVersion = version.substr(2);
        } else {
          expectedResults = [];
        }
        if (version[0] === ">") {
          expectedResults.push(1);
        } else {
          expectedResults.push(-1);
        }
      } else if (version[0] === "=") {
        comparableVersion = version.substr(1);
      } else if (version[0] === "~") {
        isLoose = true;
        comparableVersion = version.substr(1);
      }
      return expectedResults.indexOf(
        Utils.compareVersions(currentBrowserVersion, comparableVersion, isLoose)
      ) > -1;
    }
isOS(osName) {
      return this.getOSName(true) === String(osName).toLowerCase();
    }
isPlatform(platformType) {
      return this.getPlatformType(true) === String(platformType).toLowerCase();
    }
isEngine(engineName) {
      return this.getEngineName(true) === String(engineName).toLowerCase();
    }
is(anything, includingAlias = false) {
      return this.isBrowser(anything, includingAlias) || this.isOS(anything) || this.isPlatform(anything);
    }
some(anythings = []) {
      return anythings.some((anything) => this.is(anything));
    }
  }
  class Bowser {
static getParser(UA, skipParsingOrHints = false, clientHints = null) {
      if (typeof UA !== "string") {
        throw new Error("UserAgent should be a string");
      }
      return new Parser(UA, skipParsingOrHints, clientHints);
    }
static parse(UA, clientHints = null) {
      return new Parser(UA, clientHints).getResult();
    }
    static get BROWSER_MAP() {
      return BROWSER_MAP;
    }
    static get ENGINE_MAP() {
      return ENGINE_MAP;
    }
    static get OS_MAP() {
      return OS_MAP;
    }
    static get PLATFORMS_MAP() {
      return PLATFORMS_MAP;
    }
  }
  const browserInfo = Bowser.getParser(
    globalThis.navigator.userAgent
  ).getResult();
  const UNKNOWN_VALUE = "unknown";
  const joinParts = (...parts) => {
    const value = parts.filter(Boolean).join(" ").trim();
    return value || UNKNOWN_VALUE;
  };
  function getEnvironmentInfo() {
    const os = joinParts(browserInfo.os?.name, browserInfo.os?.version);
    const browser = joinParts(
      browserInfo.browser?.name,
      browserInfo.browser?.version
    );
    const loader = (() => {
      const handler = GM_info?.scriptHandler;
      const version = GM_info?.version;
      if (handler && version) return `${handler} v${version}`;
      return handler || version || UNKNOWN_VALUE;
    })();
    const scriptVersion = GM_info?.script?.version ?? UNKNOWN_VALUE;
    const scriptName = GM_info?.script?.name ?? UNKNOWN_VALUE;
    const url = globalThis?.location?.href ?? UNKNOWN_VALUE;
    return {
      os,
      browser,
      loader,
      scriptVersion,
      scriptName,
      url
    };
  }
  const SETTINGS_CACHE_TTL_MS = 5e3;
  const IMMUTABLE_API_CACHE_TTL_MS = Number.MAX_SAFE_INTEGER;
  let cachedTranslationService = null;
  let cachedTranslationServiceAt = 0;
  let cachedDetectService = null;
  let cachedDetectServiceAt = 0;
  async function getTranslationServiceCached() {
    const now2 = Date.now();
    if (cachedTranslationService && now2 - cachedTranslationServiceAt < SETTINGS_CACHE_TTL_MS) {
      return cachedTranslationService;
    }
    const service = await votStorage.get(
      "translationService",
      defaultTranslationService
    );
    cachedTranslationService = String(service);
    cachedTranslationServiceAt = now2;
    return cachedTranslationService;
  }
  async function getDetectServiceCached() {
    const now2 = Date.now();
    if (cachedDetectService && now2 - cachedDetectServiceAt < SETTINGS_CACHE_TTL_MS) {
      return cachedDetectService;
    }
    const service = await votStorage.get("detectService", defaultDetectService);
    cachedDetectService = String(service);
    cachedDetectServiceAt = now2;
    return cachedDetectService;
  }
  const foswlyServices = ["yandexbrowser", "msedge"];
  const FOSWLYTranslateAPI = new class {
    isFOSWLYError(data) {
      return Object.hasOwn(data, "error");
    }
    async request(path, opts = {}) {
      try {
        const res = await GM_fetch(`${foswlyTranslateUrl}${path}`, {
          ...opts,
          timeout: 3e3,
          forceGmXhr: true,
          responseCache: {
            ttlMs: IMMUTABLE_API_CACHE_TTL_MS,
            cacheName: "vot-foswly-api-v1",
            allowStaleOnError: true
          }
        });
        const data = await res.json();
        if (this.isFOSWLYError(data)) {
          throw new Error(data.error);
        }
        return data;
      } catch (err) {
        console.error(
          `[VOT] Failed to get data from FOSWLY Translate API, because ${err instanceof Error ? err.message : String(err)}`
        );
        return void 0;
      }
    }
    async translateMultiple(text, lang2, service) {
      const result = await this.request(
        "/translate",
        {
          method: "POST",
          headers: {
            "Content-Type": "application/json"
          },
          body: JSON.stringify({
            text,
            lang: lang2,
            service
          })
        }
      );
      return result ? result.translations : text;
    }
    async translate(text, lang2, service) {
      const result = await this.request(
        `/translate?${new URLSearchParams({
        text,
        lang: lang2,
        service
      })}`
      );
      return result ? result.translations[0] : text;
    }
    async detect(text, service) {
      const result = await this.request(
        `/detect?${new URLSearchParams({
        text,
        service
      })}`
      );
      return result ? result.lang : "en";
    }
  }();
  const RustServerAPI = {
    async detect(text) {
      try {
        const response = await GM_fetch(detectRustServerUrl, {
          method: "POST",
          body: text,
          timeout: 3e3,
          forceGmXhr: true,
          responseCache: {
            ttlMs: IMMUTABLE_API_CACHE_TTL_MS,
            cacheName: "vot-rust-detect-v1",
            allowStaleOnError: true
          }
        });
        return await response.text();
      } catch (error2) {
        console.error(
          `[VOT] Error getting lang from text, because ${error2.message}`
        );
        return "en";
      }
    }
  };
  async function translate(text, fromLang = "", toLang = "ru") {
    if (fromLang && toLang && fromLang === toLang) {
      return text;
    }
    const service = await getTranslationServiceCached();
    switch (service) {
      case "yandexbrowser":
      case "msedge": {
        const langPair = fromLang && toLang ? `${fromLang}-${toLang}` : toLang;
        return Array.isArray(text) ? await FOSWLYTranslateAPI.translateMultiple(text, langPair, service) : await FOSWLYTranslateAPI.translate(text, langPair, service);
      }
      default:
        return text;
    }
  }
  async function detect(text) {
    const service = await getDetectServiceCached();
    switch (service) {
      case "yandexbrowser":
      case "msedge":
        return await FOSWLYTranslateAPI.detect(text, service);
      case "rust-server":
        return await RustServerAPI.detect(text);
      default:
        return "en";
    }
  }
  const detectServices = [...foswlyServices, "rust-server"];
  class AccountButton {
    container;
    accountWrapper;
    buttons;
    usernameEl;
    avatarEl;
    avatarImg;
    actionButton;
    refreshButton;
    tokenButton;
    onClick = new EventImpl();
    onRefresh = new EventImpl();
    onClickSecret = new EventImpl();
    events = {
      click: this.onClick,
      "click:secret": this.onClickSecret,
      refresh: this.onRefresh
    };
    _loggedIn;
    _username;
    _avatarId;
    constructor({
      loggedIn = false,
      username = "unnamed",
      avatarId = "0/0-0"
    } = {}) {
      this._loggedIn = loggedIn;
      this._username = username;
      this._avatarId = avatarId;
      const elements = this.createElements();
      this.container = elements.container;
      this.accountWrapper = elements.accountWrapper;
      this.buttons = elements.buttons;
      this.usernameEl = elements.usernameEl;
      this.avatarEl = elements.avatarEl;
      this.avatarImg = elements.avatarImg;
      this.actionButton = elements.actionButton;
      this.refreshButton = elements.refreshButton;
      this.tokenButton = elements.tokenButton;
    }
    createElements() {
      const container = UI.createEl("vot-block", ["vot-account"]);
      const accountWrapper = UI.createEl("vot-block", ["vot-account-wrapper"]);
      accountWrapper.hidden = !this._loggedIn;
      const avatarImg = UI.createEl("img", [
        "vot-account-avatar-img"
      ]);
      avatarImg.src = `${avatarServerUrl}/${this._avatarId}/islands-retina-middle`;
      avatarImg.loading = "lazy";
      avatarImg.alt = "user avatar";
      const avatarEl = UI.createEl(
        "vot-block",
        ["vot-account-avatar"],
        avatarImg
      );
      const usernameEl = UI.createEl("vot-block", ["vot-account-username"]);
      usernameEl.textContent = this._username;
      accountWrapper.append(avatarEl, usernameEl);
      const buttons = UI.createEl("vot-block", ["vot-account-buttons"]);
      const actionButton = UI.createOutlinedButton(this.buttonText);
      actionButton.addEventListener("click", () => {
        this.onClick.dispatch();
      });
      const tokenButton = UI.createIconButton(KEY_ICON, {
        ariaLabel: localizationProvider.get("VOTLoginViaToken")
      });
      tokenButton.hidden = this._loggedIn;
      tokenButton.addEventListener("click", () => {
        this.onClickSecret.dispatch();
      });
      const refreshButton = UI.createIconButton(REFRESH_ICON, {
        ariaLabel: localizationProvider.get("VOTRefresh")
      });
      refreshButton.addEventListener("click", () => {
        this.onRefresh.dispatch();
      });
      buttons.append(actionButton, tokenButton, refreshButton);
      container.append(accountWrapper, buttons);
      return {
        container,
        accountWrapper,
        buttons,
        usernameEl,
        avatarImg,
        avatarEl,
        actionButton,
        refreshButton,
        tokenButton
      };
    }
    addEventListener(type, listener) {
      addComponentEventListener(this.events, type, listener);
      return this;
    }
    removeEventListener(type, listener) {
      removeComponentEventListener(this.events, type, listener);
      return this;
    }
    get buttonText() {
      return this._loggedIn ? localizationProvider.get("VOTLogout") : localizationProvider.get("VOTLogin");
    }
    get loggedIn() {
      return this._loggedIn;
    }
    set loggedIn(isLoggedIn) {
      this._loggedIn = isLoggedIn;
      this.accountWrapper.hidden = !this._loggedIn;
      this.actionButton.textContent = this.buttonText;
      this.tokenButton.hidden = this._loggedIn;
    }
    get avatarId() {
      return this._avatarId;
    }
    set avatarId(avatarId) {
      this._avatarId = avatarId ?? "0/0-0";
      this.avatarImg.src = `${avatarServerUrl}/${this._avatarId}/islands-retina-middle`;
    }
    get username() {
      return this._username;
    }
    set username(username) {
      this._username = username ?? "unnamed";
      this.usernameEl.textContent = this._username;
    }
    set hidden(isHidden) {
      setHiddenState(this.container, isHidden);
    }
    get hidden() {
      return getHiddenState(this.container);
    }
  }
  class Checkbox {
    container;
    input;
    label;
    onChange = new EventImpl();
    events = {
      change: this.onChange
    };
    _labelHtml;
    _checked;
    _isSubCheckbox;
    constructor({
      labelHtml,
      checked = false,
      isSubCheckbox = false
    }) {
      this._labelHtml = labelHtml;
      this._checked = checked;
      this._isSubCheckbox = isSubCheckbox;
      const elements = this.createElements();
      this.container = elements.container;
      this.input = elements.input;
      this.label = elements.label;
    }
    createElements() {
      const container = UI.createEl("label", ["vot-checkbox"]);
      if (this._isSubCheckbox) {
        container.classList.add("vot-checkbox-sub");
      }
      const input = document.createElement("input");
      input.type = "checkbox";
      input.checked = this._checked;
      input.addEventListener("change", () => {
        this._checked = input.checked;
        this.onChange.dispatch(this._checked);
      });
      const label = UI.createEl("span");
      D(this._labelHtml, label);
      container.append(input, label);
      return { container, input, label };
    }
    addEventListener(_type, listener) {
      addComponentEventListener(this.events, "change", listener);
      return this;
    }
    removeEventListener(_type, listener) {
      removeComponentEventListener(this.events, "change", listener);
      return this;
    }
    set hidden(isHidden) {
      setHiddenState(this.container, isHidden);
    }
    get hidden() {
      return getHiddenState(this.container);
    }
    get disabled() {
      return this.input.disabled;
    }
    set disabled(isDisabled) {
      this.input.disabled = isDisabled;
    }
    get checked() {
      return this._checked;
    }
set checked(isChecked) {
      if (this._checked === isChecked) {
        return;
      }
      this._checked = this.input.checked = isChecked;
      this.onChange.dispatch(this._checked);
    }
  }
  class Details {
    container;
    header;
    arrowIcon;
    onClick = new EventImpl();
    events = {
      click: this.onClick
    };
    _titleHtml;
    constructor({ titleHtml }) {
      this._titleHtml = titleHtml;
      const elements = this.createElements();
      this.container = elements.container;
      this.header = elements.header;
      this.arrowIcon = elements.arrowIcon;
    }
    createElements() {
      const container = UI.createEl("vot-block", ["vot-details"]);
      UI.makeButtonLike(container);
      const header = UI.createEl("vot-block");
      header.append(this._titleHtml);
      const arrowIcon = UI.createEl("vot-block", ["vot-details-arrow-icon"]);
      D(CHEVRON_ICON, arrowIcon);
      container.append(header, arrowIcon);
      container.addEventListener("click", () => {
        this.onClick.dispatch();
      });
      return {
        container,
        header,
        arrowIcon
      };
    }
    addEventListener(_type, listener) {
      addComponentEventListener(this.events, "click", listener);
      return this;
    }
    removeEventListener(_type, listener) {
      removeComponentEventListener(this.events, "click", listener);
      return this;
    }
    set hidden(isHidden) {
      setHiddenState(this.container, isHidden);
    }
    get hidden() {
      return getHiddenState(this.container);
    }
  }
  class HotkeyButton {
    container;
    button;
    onChange = new EventImpl();
    events = {
      change: this.onChange
    };
    _labelHtml;
    _key;
pressedKeys;
comboKeys;
    recording = false;
    constructor({ labelHtml, key = null }) {
      this._labelHtml = labelHtml;
      this._key = key;
      this.pressedKeys = new Set();
      this.comboKeys = new Set();
      const elements = this.createElements();
      this.container = elements.container;
      this.button = elements.button;
    }
    stopRecordingKeys() {
      this.recording = false;
      document.removeEventListener("keydown", this.keydownHandle);
      document.removeEventListener("keyup", this.keyupOrBlurHandle);
      globalThis.removeEventListener("blur", this.blurHandle);
      delete this.button.dataset.status;
      this.pressedKeys.clear();
      this.comboKeys.clear();
    }
    keydownHandle = (event) => {
      if (!this.recording || event.repeat) {
        return;
      }
      event.preventDefault();
      if (event.code === "Escape") {
        this.key = null;
        this.button.textContent = this.keyText;
        this.stopRecordingKeys();
        return;
      }
      this.pressedKeys.add(event.code);
      this.comboKeys.add(event.code);
      this.button.textContent = formatKeysComboDisplay(this.pressedKeys);
    };
    keyupOrBlurHandle = (event) => {
      if (!this.recording) return;
      if (event) {
        this.pressedKeys.delete(event.code);
        this.button.textContent = this.pressedKeys.size ? formatKeysComboDisplay(this.pressedKeys) : formatKeysComboDisplay(this.comboKeys);
        if (this.pressedKeys.size) {
          return;
        }
      }
      this.key = this.comboKeys.size ? formatKeysCombo(this.comboKeys) : null;
      this.stopRecordingKeys();
    };
    blurHandle = () => {
      this.keyupOrBlurHandle();
    };
    createElements() {
      const container = UI.createEl("vot-block", ["vot-hotkey"]);
      const label = UI.createEl("vot-block", ["vot-hotkey-label"]);
      label.textContent = this._labelHtml;
      const button2 = UI.createEl("vot-block", ["vot-hotkey-button"]);
      UI.makeButtonLike(button2);
      button2.textContent = this.keyText;
      button2.addEventListener("click", () => {
        if (this.recording) {
          this.stopRecordingKeys();
          this.button.textContent = this.keyText;
          return;
        }
        button2.dataset.status = "active";
        this.recording = true;
        this.pressedKeys.clear();
        this.comboKeys.clear();
        this.button.textContent = localizationProvider.get(
          "PressTheKeyCombination"
        );
        document.addEventListener("keydown", this.keydownHandle);
        document.addEventListener("keyup", this.keyupOrBlurHandle);
        globalThis.addEventListener("blur", this.blurHandle);
      });
      container.append(label, button2);
      return { container, button: button2, label };
    }
    addEventListener(_type, listener) {
      addComponentEventListener(this.events, "change", listener);
      return this;
    }
    removeEventListener(_type, listener) {
      removeComponentEventListener(this.events, "change", listener);
      return this;
    }
    set hidden(isHidden) {
      setHiddenState(this.container, isHidden);
    }
    get hidden() {
      return getHiddenState(this.container);
    }
    get key() {
      return this._key;
    }
    get keyText() {
      if (!this._key) {
        return localizationProvider.get("None");
      }
      return formatKeysComboDisplay(this._key);
    }
set key(newKey) {
      if (this._key === newKey) {
        return;
      }
      this._key = newKey;
      this.button.textContent = this.keyText;
      this.onChange.dispatch(this._key);
    }
  }
  function formatKeysCombo(keys) {
    const keysArray = Array.isArray(keys) ? keys : Array.from(keys);
    return keysArray.map((code) => code.replace("Key", "").replace("Digit", "")).join("+");
  }
  function formatKeysComboDisplay(keys) {
    let parts;
    if (typeof keys === "string") {
      parts = keys.split("+").filter(Boolean);
    } else if (Array.isArray(keys)) {
      parts = keys;
    } else {
      parts = Array.from(keys);
    }
    const map = (k2) => {
      switch (k2) {
        case "ControlLeft":
        case "ControlRight":
        case "Control":
          return "Ctrl";
        case "ShiftLeft":
        case "ShiftRight":
        case "Shift":
          return "Shift";
        case "AltLeft":
        case "AltRight":
        case "Alt":
          return "Alt";
        case "MetaLeft":
        case "MetaRight":
        case "Meta":
          return "Meta";
        case "Space":
          return "Space";
        case "ArrowUp":
          return "↑";
        case "ArrowDown":
          return "↓";
        case "ArrowLeft":
          return "←";
        case "ArrowRight":
          return "→";
        default:
          return k2.replace("Key", "").replace("Digit", "");
      }
    };
    const priority = (k2) => {
      const m2 = map(k2);
      if (m2 === "Ctrl") return 0;
      if (m2 === "Alt") return 1;
      if (m2 === "Shift") return 2;
      if (m2 === "Meta") return 3;
      return 10;
    };
    return parts.slice().sort((a2, b2) => priority(a2) - priority(b2)).map(map).join("+");
  }
  const positions = ["left", "top", "right", "bottom"];
  const triggers = ["hover", "click"];
  class Tooltip {
showed = false;
    target;
    anchor;
    content;
    position;
    preferredPosition;
    trigger;
    parentElement;
    layoutRoot;
    offsetX;
    offsetY;
    _hidden;
    autoLayout;
    pageWidth;
    pageHeight;
    globalOffsetX;
    globalOffsetY;
    maxWidth;
    backgroundColor;
    borderRadius;
    _bordered;
    container;
    onResizeObserver;
    intersectionObserver;
    scrollListening = false;
    positionRafId = null;
    destroyFallbackTimerId;
    static DESTROY_FALLBACK_MS = 700;
tooltipId = typeof crypto !== "undefined" && "randomUUID" in crypto ? crypto.randomUUID() : `vot-tooltip-${Math.random().toString(36).slice(2)}`;
    prevAriaDescribedBy = null;
    constructor({
      target,
      anchor = void 0,
      content = "",
      position: position2 = "top",
      trigger = "hover",
      offset = 4,
      maxWidth = void 0,
      hidden = false,
      autoLayout = true,
      backgroundColor = void 0,
      borderRadius = void 0,
      bordered = true,
      parentElement = document.body,
      layoutRoot = document.documentElement
    }) {
      if (!(target instanceof HTMLElement)) {
        throw new TypeError("target must be a valid HTMLElement");
      }
      this.target = target;
      this.anchor = anchor instanceof HTMLElement ? anchor : target;
      this.content = content;
      if (typeof offset === "number") {
        this.offsetY = this.offsetX = offset;
      } else {
        this.offsetX = offset.x;
        this.offsetY = offset.y;
      }
      this._hidden = hidden;
      this.autoLayout = autoLayout;
      this.trigger = Tooltip.validateTrigger(trigger) ? trigger : "hover";
      this.position = Tooltip.validatePos(position2) ? position2 : "top";
      this.preferredPosition = this.position;
      this.parentElement = parentElement;
      this.layoutRoot = layoutRoot;
      this.borderRadius = borderRadius;
      this._bordered = bordered;
      this.maxWidth = maxWidth;
      this.backgroundColor = backgroundColor;
      this.updatePageSize();
      this.init();
    }
    static validatePos(position2) {
      return positions.includes(position2);
    }
    static validateTrigger(trigger) {
      return triggers.includes(trigger);
    }
    setPosition(position2) {
      this.preferredPosition = Tooltip.validatePos(position2) ? position2 : "top";
      this.position = this.preferredPosition;
      this.schedulePositionUpdate();
      return this;
    }
    setContent(content) {
      this.content = content;
      if (this.container) {
        this.container.replaceChildren();
        if (typeof content === "string") {
          this.container.textContent = content;
        } else {
          this.container.append(content);
        }
        this.schedulePositionUpdate();
        return this;
      }
      return this;
    }
updateMount({
      parentElement,
      layoutRoot
    }) {
      if (parentElement && this.parentElement !== parentElement) {
        this.parentElement = parentElement;
        if (this.container?.isConnected) {
          parentElement.appendChild(this.container);
        }
      }
      if (layoutRoot && this.layoutRoot !== layoutRoot) {
        this.layoutRoot = layoutRoot;
      }
      this.schedulePositionUpdate();
      return this;
    }
    onResize = () => {
      this.schedulePositionUpdate();
    };
    onClick = () => {
      this.showed ? this.destroy() : this.create();
    };
    onTargetKeyDown = (event) => {
      if (event.key !== "Escape" || !this.showed) {
        return;
      }
      this.destroy();
    };
    onScroll = () => {
      this.schedulePositionUpdate();
    };
    onHoverPointerDown = (e2) => {
      if (e2.pointerType === "mouse") {
        return;
      }
      this.create();
    };
    onHoverPointerUp = (e2) => {
      if (e2.pointerType === "mouse") {
        return;
      }
      this.destroy();
    };
    onMouseEnter = () => {
      this.create();
    };
    onMouseLeave = (event) => {
      if (this.isInTooltipContext(event.relatedTarget)) {
        return;
      }
      this.destroy();
    };
    onTooltipMouseLeave = (event) => {
      if (this.isInTooltipContext(event.relatedTarget)) {
        return;
      }
      this.destroy();
    };
    isInTooltipContext(nextTarget) {
      if (!(nextTarget instanceof Node)) {
        return false;
      }
      return this.target.contains(nextTarget) || this.container?.contains(nextTarget);
    }
    updatePageSize() {
      if (this.layoutRoot === document.documentElement) {
        this.globalOffsetX = 0;
        this.globalOffsetY = 0;
      } else {
        const { left, top } = this.layoutRoot.getBoundingClientRect();
        this.globalOffsetX = left;
        this.globalOffsetY = top;
      }
      this.pageWidth = this.layoutRoot.clientWidth || document.documentElement.clientWidth;
      this.pageHeight = this.layoutRoot.clientHeight || document.documentElement.clientHeight;
      return this;
    }
    onIntersect = ([entry]) => {
      if (!entry.isIntersecting) {
        return this.destroy(true);
      }
    };
    init() {
      this.onResizeObserver = new ResizeObserver(this.onResize);
      this.intersectionObserver = new IntersectionObserver(this.onIntersect);
      this.target.addEventListener("keydown", this.onTargetKeyDown);
      if (this.trigger === "click") {
        this.target.addEventListener("pointerdown", this.onClick);
        return this;
      }
      this.target.addEventListener("mouseenter", this.onMouseEnter);
      this.target.addEventListener("mouseleave", this.onMouseLeave);
      this.target.addEventListener("focusin", this.onMouseEnter);
      this.target.addEventListener("focusout", this.onMouseLeave);
      this.target.addEventListener("pointerdown", this.onHoverPointerDown);
      this.target.addEventListener("pointerup", this.onHoverPointerUp);
      return this;
    }
    release() {
      this.destroy(true);
      this.detachScrollListener();
      this.target.removeEventListener("keydown", this.onTargetKeyDown);
      if (this.trigger === "click") {
        this.target.removeEventListener("pointerdown", this.onClick);
        return this;
      }
      this.target.removeEventListener("mouseenter", this.onMouseEnter);
      this.target.removeEventListener("mouseleave", this.onMouseLeave);
      this.target.removeEventListener("focusin", this.onMouseEnter);
      this.target.removeEventListener("focusout", this.onMouseLeave);
      this.target.removeEventListener("pointerdown", this.onHoverPointerDown);
      this.target.removeEventListener("pointerup", this.onHoverPointerUp);
      return this;
    }
    schedulePositionUpdate() {
      if (!this.container) {
        return;
      }
      if (this.positionRafId !== null) {
        return;
      }
      this.positionRafId = requestAnimationFrame(() => {
        this.positionRafId = null;
        this.updatePageSize();
        this.updatePos();
      });
    }
    cancelPositionUpdate() {
      if (this.positionRafId === null) {
        return;
      }
      cancelAnimationFrame(this.positionRafId);
      this.positionRafId = null;
    }
    clearDestroyFallbackTimer() {
      if (this.destroyFallbackTimerId === void 0) {
        return;
      }
      globalThis.clearTimeout(this.destroyFallbackTimerId);
      this.destroyFallbackTimerId = void 0;
    }
    create() {
      this.destroy(true);
      this.showed = true;
      this.container = UI.createEl("vot-block", ["vot-tooltip"], this.content);
      if (this.bordered) {
        this.container.classList.add("vot-tooltip-bordered");
      }
      this.container.setAttribute("role", "tooltip");
      this.container.id = this.tooltipId;
      this.container.dataset.trigger = this.trigger;
      this.container.dataset.position = this.position;
      this.parentElement.appendChild(this.container);
      this.schedulePositionUpdate();
      if (this.backgroundColor !== void 0) {
        this.container.style.backgroundColor = this.backgroundColor;
      }
      if (this.borderRadius !== void 0) {
        this.container.style.borderRadius = `${this.borderRadius}px`;
      }
      if (this.hidden) {
        this.container.hidden = true;
      } else {
        this.syncAriaDescribedBy(true);
      }
      this.container.style.opacity = "1";
      if (this.trigger === "hover") {
        this.container.addEventListener("mouseleave", this.onTooltipMouseLeave);
      }
      this.attachScrollListener();
      this.onResizeObserver?.observe(this.layoutRoot);
      if (this.anchor !== this.layoutRoot) {
        this.onResizeObserver?.observe(this.anchor);
      }
      this.intersectionObserver?.observe(this.target);
      return this;
    }
    updatePos() {
      if (!this.container) {
        return this;
      }
      const { top, left } = this.calcPos(this.autoLayout, this.preferredPosition);
      const availableWidth = Math.max(0, this.pageWidth - this.offsetX * 2);
      const maxWidth = clamp$2(this.maxWidth ?? availableWidth, 0, availableWidth);
      this.container.style.transform = `translate(${left}px, ${top}px)`;
      this.container.dataset.position = this.position;
      this.container.style.maxWidth = `${maxWidth}px`;
      return this;
    }
    calcPos(autoLayout = true, position2 = this.preferredPosition) {
      if (!this.container) {
        return { top: 0, left: 0 };
      }
      const {
        left: anchorLeft,
        right: anchorRight,
        top: anchorTop,
        bottom: anchorBottom,
        width: anchorWidth,
        height: anchorHeight
      } = this.anchor.getBoundingClientRect();
      const { width: containerWidth, height: containerHeight } = this.container.getBoundingClientRect();
      const width = clamp$2(containerWidth, 0, this.pageWidth);
      const height = clamp$2(containerHeight, 0, this.pageHeight);
      const left = anchorLeft - this.globalOffsetX;
      const right = anchorRight - this.globalOffsetX;
      const top = anchorTop - this.globalOffsetY;
      const bottom = anchorBottom - this.globalOffsetY;
      let resolvedPosition = position2;
      if (autoLayout) {
        switch (position2) {
          case "top": {
            const pTop = clamp$2(top - height - this.offsetY, 0, this.pageHeight);
            if (pTop + this.offsetY < height) {
              resolvedPosition = "bottom";
            }
            break;
          }
          case "right": {
            const pLeft = clamp$2(right + this.offsetX, 0, this.pageWidth - width);
            if (pLeft + width > this.pageWidth - this.offsetX) {
              resolvedPosition = "left";
            }
            break;
          }
          case "bottom": {
            const pTop = clamp$2(
              bottom + this.offsetY,
              0,
              this.pageHeight - height
            );
            if (pTop + height > this.pageHeight - this.offsetY) {
              resolvedPosition = "top";
            }
            break;
          }
          case "left": {
            const pLeft = Math.max(0, left - width - this.offsetX);
            if (pLeft + width > left - this.offsetX) {
              resolvedPosition = "right";
            }
            break;
          }
        }
      }
      let coords;
      switch (resolvedPosition) {
        case "top":
          coords = {
            top: clamp$2(top - height - this.offsetY, 0, this.pageHeight),
            left: clamp$2(
              left - width / 2 + anchorWidth / 2,
              this.offsetX,
              this.pageWidth - width - this.offsetX
            )
          };
          break;
        case "right":
          coords = {
            top: clamp$2(
              top + (anchorHeight - height) / 2,
              this.offsetY,
              this.pageHeight - height - this.offsetY
            ),
            left: clamp$2(right + this.offsetX, 0, this.pageWidth - width)
          };
          break;
        case "bottom":
          coords = {
            top: clamp$2(bottom + this.offsetY, 0, this.pageHeight - height),
            left: clamp$2(
              left - width / 2 + anchorWidth / 2,
              this.offsetX,
              this.pageWidth - width - this.offsetX
            )
          };
          break;
        case "left":
          coords = {
            top: clamp$2(
              top + (anchorHeight - height) / 2,
              this.offsetY,
              this.pageHeight - height - this.offsetY
            ),
            left: Math.max(0, left - width - this.offsetX)
          };
          break;
        default:
          coords = { top: 0, left: 0 };
      }
      this.position = resolvedPosition;
      return {
        top: coords.top + this.globalOffsetY,
        left: coords.left + this.globalOffsetX
      };
    }
    destroy(instant = false) {
      if (!this.container) {
        return this;
      }
      const container = this.container;
      this.cancelPositionUpdate();
      this.clearDestroyFallbackTimer();
      this.showed = false;
      this.syncAriaDescribedBy(false);
      this.onResizeObserver?.disconnect();
      this.intersectionObserver?.disconnect();
      this.detachScrollListener();
      if (instant) {
        container.remove();
        this.container = void 0;
        return this;
      }
      container.removeEventListener("mouseleave", this.onTooltipMouseLeave);
      container.style.pointerEvents = "none";
      container.style.opacity = "0";
      const handleTransitionDone = () => {
        this.clearDestroyFallbackTimer();
        container?.remove();
        if (this.container === container) {
          this.container = void 0;
        }
      };
      container.addEventListener("transitionend", handleTransitionDone, {
        once: true
      });
      container.addEventListener("transitioncancel", handleTransitionDone, {
        once: true
      });
      this.destroyFallbackTimerId = globalThis.setTimeout(
        handleTransitionDone,
        Tooltip.DESTROY_FALLBACK_MS
      );
      return this;
    }
    syncAriaDescribedBy(isShowing) {
      const existing = this.target.getAttribute("aria-describedby");
      this.prevAriaDescribedBy ??= existing;
      if (!isShowing) {
        if (this.prevAriaDescribedBy === null) {
          this.target.removeAttribute("aria-describedby");
        } else {
          this.target.setAttribute("aria-describedby", this.prevAriaDescribedBy);
        }
        this.prevAriaDescribedBy = null;
        return;
      }
      const tokens = new Set((existing ?? "").split(/\s+/).filter(Boolean));
      tokens.add(this.tooltipId);
      this.target.setAttribute("aria-describedby", Array.from(tokens).join(" "));
    }
    set bordered(isBordered) {
      this._bordered = isBordered;
      this.container?.classList.toggle("vot-tooltip-bordered", isBordered);
    }
    get bordered() {
      return this._bordered;
    }
    set hidden(isHidden) {
      this._hidden = isHidden;
      if (this.container) {
        this.container.hidden = isHidden;
      }
      if (this.showed) {
        this.syncAriaDescribedBy(!isHidden);
      }
    }
    get hidden() {
      return this._hidden;
    }
    attachScrollListener() {
      if (this.scrollListening) return;
      this.scrollListening = true;
      document.addEventListener("scroll", this.onScroll, {
        passive: true,
        capture: true
      });
    }
    detachScrollListener() {
      if (!this.scrollListening) return;
      this.scrollListening = false;
      document.removeEventListener("scroll", this.onScroll, {
        capture: true
      });
    }
  }
  const SETTINGS_EVENT_KEYS = [
    "click:bugReport",
    "click:resetSettings",
    "update:account",
    "change:autoTranslate",
    "change:autoSubtitles",
    "change:showVideoVolume",
    "change:audioBooster",
    "change:syncVolume",
    "change:useLivelyVoice",
    "change:subtitlesHighlightWords",
    "change:subtitlesSmartLayout",
    "select:subtitlesFontFamily",
    "change:proxyWorkerHost",
    "change:useNewAudioPlayer",
    "change:onlyBypassMediaCSP",
    "change:showPiPButton",
    "input:subtitlesMaxLength",
    "input:subtitlesFontSize",
    "input:subtitlesBackgroundOpacity",
    "input:autoHideButtonDelay",
    "select:proxyTranslationStatus",
    "select:translationTextService",
    "select:buttonPosition",
    "select:menuLanguage"
  ];
  function createSettingsEvents() {
    const events = {};
    for (const key of SETTINGS_EVENT_KEYS) {
      events[key] = new EventImpl();
    }
    return events;
  }
  const GOOGLE_FONTS_SEARCH_LIMIT = 30;
  const subtitleFontFamilyLabels = {
    "default-sans": "Default Sans",
    arial: "Arial",
    helvetica: "Helvetica",
    roboto: "Roboto",
    verdana: "Verdana",
    "open-sans": "Open Sans",
    poppins: "Poppins",
    lato: "Lato",
    montserrat: "Montserrat",
    barlow: "Barlow"
  };
  function getSubtitleFontFamilyLabel(fontFamily) {
    if (isBuiltInSubtitleFontFamily(fontFamily)) {
      return subtitleFontFamilyLabels[fontFamily];
    }
    return getGoogleSubtitleFontFamilyName(fontFamily) ?? "Default Sans";
  }
  class SettingsView {
    static PERSIST_DELAY_MS = 250;
    globalPortal;
    initialized = false;
    data;
    videoHandler;
    suppressSubtitlesSmartLayoutCheckboxChange = false;
    events = createSettingsEvents();
    oauthMessageListenerBound = false;
    persistTimerIds = {};
    dialog;
    accountButton;
    accountButtonRefreshTooltip;
    accountButtonTokenTooltip;
    autoTranslateCheckbox;
    autoSubtitlesCheckbox;
    dontTranslateLanguagesCheckbox;
    dontTranslateLanguagesSelect;
    autoSetVolumeSliderLabel;
    autoSetVolumeCheckbox;
    smartDuckingCheckbox;
    autoSetVolumeSlider;
    showVideoVolumeSliderCheckbox;
    audioBoosterCheckbox;
    audioBoosterTooltip;
    syncVolumeCheckbox;
    downloadWithNameCheckbox;
    sendNotifyOnCompleteCheckbox;
    useLivelyVoiceCheckbox;
    useLivelyVoiceTooltip;
    useAudioDownloadCheckbox;
    useAudioDownloadCheckboxLabel;
    useAudioDownloadCheckboxTooltip;
    subtitlesDownloadFormatSelectLabel;
    subtitlesDownloadFormatSelect;
    subtitlesHighlightWordsCheckbox;
    subtitlesSmartLayoutCheckbox;
    subtitlesMaxLengthSliderLabel;
    subtitlesMaxLengthSlider;
    subtitlesFontSizeSliderLabel;
    subtitlesFontSizeSlider;
    subtitlesFontFamilySelectLabel;
    subtitlesFontFamilySelect;
    subtitlesBackgroundOpacitySliderLabel;
    subtitlesBackgroundOpacitySlider;
    translateHotkeyButton;
    subtitlesHotkeyButton;
    proxyWorkerHostTextfield;
    proxyTranslationStatusSelectLabel;
    proxyTranslationStatusSelectTooltip;
    proxyTranslationStatusSelect;
    translateAPIErrorsCheckbox;
    useNewAudioPlayerCheckbox;
    useNewAudioPlayerTooltip;
    onlyBypassMediaCSPCheckbox;
    onlyBypassMediaCSPTooltip;
    translationTextServiceLabel;
    translationTextServiceSelect;
    translationTextServiceTooltip;
    detectServiceLabel;
    detectServiceSelect;
    showPiPButtonCheckbox;
    autoHideButtonDelaySliderLabel;
    autoHideButtonDelaySlider;
    buttonPositionSelectLabel;
    buttonPositionSelect;
    buttonPositionTooltip;
    menuLanguageSelectLabel;
    menuLanguageSelect;
    bugReportButton;
    resetSettingsButton;
    constructor({ globalPortal, data = {}, videoHandler }) {
      this.globalPortal = globalPortal;
      this.data = data;
      this.videoHandler = videoHandler;
    }
    isInitialized() {
      return this.initialized;
    }
    bindOAuthMessageListener() {
      if (this.oauthMessageListenerBound) return;
      this.oauthMessageListenerBound = true;
      globalThis.addEventListener("message", async (event) => {
        const data = event.data;
        if (!data || typeof data !== "object") return;
        if (data.source !== "vot-auth") return;
        if (event.origin !== authCallbackOrigin) {
          debug.log(
            "[VOT] Ignoring OAuth message from unexpected origin:",
            event.origin
          );
          return;
        }
        if (data.type === "error") {
          debug.log("[VOT] OAuth error:", data.error, data.error_description);
          return;
        }
        if (data.type !== "code") return;
        const expectedState = sessionStorage.getItem("vot-yandex-oauth-state") ?? void 0;
        if (!data.state || !expectedState || data.state !== expectedState) {
          debug.log("[VOT] OAuth state mismatch");
          return;
        }
        try {
          const tokenData = await exchangeCodeForToken(data.code);
          await saveOAuthAccount(tokenData);
          this.data.account = {
            ...this.data.account ?? {},
            token: tokenData.access_token,
            expires: Date.now() + tokenData.expires_in * 1e3
          };
          debug.log("[VOT] OAuth login success");
        } catch (err) {
          console.error("[VOT] OAuth token exchange failed:", err);
          return;
        }
        if (this.isInitialized()) {
          try {
            this.updateAccountInfo();
          } catch (err) {
            console.warn("[VOT] Failed to update account UI:", err);
          }
        } else {
          debug.log(
            "[VOT] SettingsView is not initialized, skipping account UI update"
          );
        }
      });
    }
    createAccordionSection(title, options = {}) {
      const section = UI.createEl("vot-block", ["vot-settings-section"]);
      const header = new Details({ titleHtml: title });
      header.container.classList.add("vot-settings-section-header");
      const sectionId = typeof crypto !== "undefined" && "randomUUID" in crypto ? crypto.randomUUID() : `${Date.now()}-${Math.random().toString(16).slice(2)}`;
      const headerId = `vot-settings-section-header-${sectionId}`;
      const contentId = `vot-settings-section-content-${sectionId}`;
      header.container.id = headerId;
      const content = UI.createEl("vot-block", ["vot-settings-section-content"]);
      content.id = contentId;
      content.setAttribute("role", "region");
      content.setAttribute("aria-labelledby", headerId);
      header.container.setAttribute("aria-controls", contentId);
      const setOpen = (open) => {
        header.container.dataset.open = open ? "true" : "false";
        header.container.setAttribute("aria-expanded", open ? "true" : "false");
        content.hidden = !open;
      };
      const getOpen = () => header.container.dataset.open === "true";
      setOpen(!!options.open);
      header.addEventListener("click", () => {
        const isOpen = header.container.dataset.open === "true";
        setOpen(!isOpen);
      });
      section.append(header.container, content);
      return {
        title,
        container: section,
        header: header.container,
        content,
        setOpen,
        getOpen
      };
    }
    setSubtitlesSmartLayout(checked) {
      this.data.subtitlesSmartLayout = checked;
      void votStorage.set("subtitlesSmartLayout", checked);
      debug.log("subtitlesSmartLayout value changed. New value:", checked);
      if (this.subtitlesSmartLayoutCheckbox?.checked !== checked) {
        this.suppressSubtitlesSmartLayoutCheckboxChange = true;
        this.subtitlesSmartLayoutCheckbox.checked = checked;
        this.suppressSubtitlesSmartLayoutCheckboxChange = false;
      }
      this.events["change:subtitlesSmartLayout"].dispatch(checked);
    }
    scheduleStoragePersist(key, value) {
      const prevTimerId = this.persistTimerIds[key];
      if (prevTimerId !== void 0) {
        globalThis.clearTimeout(prevTimerId);
      }
      this.persistTimerIds[key] = globalThis.setTimeout(() => {
        this.persistTimerIds[key] = void 0;
        void votStorage.set(key, value);
      }, SettingsView.PERSIST_DELAY_MS);
    }
    flushStoragePersists() {
      for (const key of Object.keys(this.persistTimerIds)) {
        const timerId = this.persistTimerIds[key];
        if (timerId === void 0) continue;
        globalThis.clearTimeout(timerId);
        this.persistTimerIds[key] = void 0;
        const value = this.data[key];
        if (typeof value === "number") {
          void votStorage.set(key, value);
        }
      }
    }
    bindPersistedSetting({
      control,
      event,
      apply,
      storageKey,
      readPersistedValue,
      logLabel,
      dispatch,
      afterPersist
    }) {
      control.addEventListener(event, async (value) => {
        apply(value);
        await votStorage.set(storageKey, readPersistedValue());
        debug.log(`${logLabel} value changed. New value:`, value);
        if (afterPersist) {
          await afterPersist(value);
        }
        dispatch?.(value);
      });
    }
    initUI() {
      if (this.isInitialized()) {
        throw new Error("[VOT] SettingsView is already initialized");
      }
      this.dialog = new Dialog({
        titleHtml: localizationProvider.get("VOTSettings")
      });
      this.globalPortal.appendChild(this.dialog.container);
      const accountSection = this.createAccordionSection(
        localizationProvider.get("VOTMyAccount"),
        { open: true }
      );
      const translationSection = this.createAccordionSection(
        localizationProvider.get("translationSettings"),
        { open: true }
      );
      const subtitlesSection = this.createAccordionSection(
        localizationProvider.get("subtitlesSettings")
      );
      const hotkeysSection = this.createAccordionSection(
        localizationProvider.get("hotkeysSettings")
      );
      const proxySection = this.createAccordionSection(
        localizationProvider.get("proxySettings")
      );
      const miscSection = this.createAccordionSection(
        localizationProvider.get("miscSettings")
      );
      const appearanceSection = this.createAccordionSection(
        localizationProvider.get("appearance")
      );
      const aboutSection = this.createAccordionSection(
        localizationProvider.get("aboutExtension")
      );
      const sections = [
        accountSection,
        translationSection,
        subtitlesSection,
        hotkeysSection,
        proxySection,
        miscSection,
        appearanceSection,
        aboutSection
      ];
      this.dialog.bodyContainer.append(
        ...sections.map((section) => section.container)
      );
      this.accountButton = new AccountButton({
        avatarId: this.data.account?.avatarId,
        username: this.data.account?.username,
        loggedIn: !!this.data.account?.token
      });
      if (votStorage.isSupportOnlyLS) {
        this.accountButton.refreshButton.setAttribute("disabled", "true");
        this.accountButton.actionButton.setAttribute("disabled", "true");
      } else {
        this.accountButtonRefreshTooltip = new Tooltip({
          target: this.accountButton.refreshButton,
          content: localizationProvider.get("VOTRefresh"),
          position: "bottom",
          backgroundColor: "var(--vot-helper-ondialog)",
          parentElement: this.globalPortal
        });
      }
      this.accountButtonTokenTooltip = new Tooltip({
        target: this.accountButton.tokenButton,
        content: localizationProvider.get("VOTLoginViaToken"),
        position: "bottom",
        backgroundColor: "var(--vot-helper-ondialog)",
        parentElement: this.globalPortal
      });
      this.autoTranslateCheckbox = new Checkbox({
        labelHtml: localizationProvider.get("VOTAutoTranslate"),
        checked: this.data.autoTranslate
      });
      this.autoSubtitlesCheckbox = new Checkbox({
        labelHtml: localizationProvider.get("VOTAutoSubtitles"),
        checked: this.data.autoSubtitles
      });
      const dontTranslateLanguages = this.data.dontTranslateLanguages ?? [];
      this.dontTranslateLanguagesCheckbox = new Checkbox({
        labelHtml: localizationProvider.get("DontTranslateSelectedLanguages"),
        checked: this.data.enabledDontTranslateLanguages
      });
      this.dontTranslateLanguagesSelect = new Select({
        dialogParent: this.globalPortal,
        dialogTitle: localizationProvider.get("DontTranslateSelectedLanguages"),
        selectTitle: dontTranslateLanguages.map((lang2) => localizationProvider.get(`langs.${lang2}`)).join(", ") || localizationProvider.get("DontTranslateSelectedLanguages"),
        items: Select.genLanguageItems(availableLangs).map((item) => ({
          ...item,
          selected: dontTranslateLanguages.includes(item.value)
        })),
        multiSelect: true,
        labelElement: this.dontTranslateLanguagesCheckbox.container
      });
      this.dontTranslateLanguagesSelect.disabled = !this.dontTranslateLanguagesCheckbox.checked;
      const autoVolume = this.data.autoVolume ?? defaultAutoVolume;
      this.autoSetVolumeSliderLabel = new SliderLabel({
        labelText: localizationProvider.get("VOTAutoSetVolume"),
        value: autoVolume
      });
      this.autoSetVolumeCheckbox = new Checkbox({
        labelHtml: this.autoSetVolumeSliderLabel.container,
        checked: this.data.enabledAutoVolume ?? true
      });
      this.autoSetVolumeSlider = new Slider({
        labelHtml: this.autoSetVolumeCheckbox.container,
        value: autoVolume,
        min: 0
      });
      const syncVolumeEnabled = Boolean(this.data.syncVolume);
      this.autoSetVolumeSlider.disabled = !this.autoSetVolumeCheckbox.checked;
      this.smartDuckingCheckbox = new Checkbox({
        labelHtml: localizationProvider.get("smartDucking"),
        checked: this.data.enabledSmartDucking ?? true
      });
      this.smartDuckingCheckbox.disabled = syncVolumeEnabled || !this.autoSetVolumeCheckbox.checked;
      this.showVideoVolumeSliderCheckbox = new Checkbox({
        labelHtml: localizationProvider.get("showVideoVolumeSlider"),
        checked: this.data.showVideoSlider
      });
      this.audioBoosterCheckbox = new Checkbox({
        labelHtml: localizationProvider.get("VOTAudioBooster"),
        checked: this.data.audioBooster
      });
      if (!this.videoHandler?.isAudioContextSupported) {
        this.audioBoosterCheckbox.disabled = true;
        this.audioBoosterTooltip = new Tooltip({
          target: this.audioBoosterCheckbox.container,
          content: localizationProvider.get("VOTNeedWebAudioAPI"),
          position: "bottom",
          backgroundColor: "var(--vot-helper-ondialog)",
          parentElement: this.globalPortal
        });
      }
      this.syncVolumeCheckbox = new Checkbox({
        labelHtml: localizationProvider.get("VOTSyncVolume"),
        checked: this.data.syncVolume
      });
      this.downloadWithNameCheckbox = new Checkbox({
        labelHtml: localizationProvider.get("VOTDownloadWithName"),
        checked: this.data.downloadWithName
      });
      this.downloadWithNameCheckbox.disabled = !isSupportGMXhr;
      this.sendNotifyOnCompleteCheckbox = new Checkbox({
        labelHtml: localizationProvider.get("VOTSendNotifyOnComplete"),
        checked: this.data.sendNotifyOnComplete
      });
      this.useLivelyVoiceCheckbox = new Checkbox({
        labelHtml: localizationProvider.get("VOTUseLivelyVoice"),
        checked: this.data.useLivelyVoice
      });
      this.useLivelyVoiceTooltip = new Tooltip({
        target: this.useLivelyVoiceCheckbox.container,
        content: localizationProvider.get("VOTAccountRequired"),
        position: "bottom",
        backgroundColor: "var(--vot-helper-ondialog)",
        parentElement: this.globalPortal,
        hidden: !!this.data.account?.token
      });
      if (!this.data.account?.token) {
        this.useLivelyVoiceCheckbox.disabled = true;
      }
      this.useAudioDownloadCheckboxLabel = new Label({
        labelText: localizationProvider.get("VOTUseAudioDownload"),
        icon: WARNING_ICON
      });
      this.useAudioDownloadCheckbox = new Checkbox({
        labelHtml: this.useAudioDownloadCheckboxLabel.container,
        checked: this.data.useAudioDownload
      });
      if (!isSupportGMXhr) {
        this.useAudioDownloadCheckbox.disabled = true;
      }
      this.useAudioDownloadCheckboxTooltip = new Tooltip({
        target: this.useAudioDownloadCheckboxLabel.container,
        content: localizationProvider.get("VOTUseAudioDownloadWarning"),
        position: "bottom",
        backgroundColor: "var(--vot-helper-ondialog)",
        parentElement: this.globalPortal
      });
      accountSection.content.append(this.accountButton.container);
      translationSection.content.append(
        this.autoTranslateCheckbox.container,
        this.autoSubtitlesCheckbox.container,
        this.dontTranslateLanguagesSelect.container,
        this.autoSetVolumeSlider.container,
        this.smartDuckingCheckbox.container,
        this.showVideoVolumeSliderCheckbox.container,
        this.audioBoosterCheckbox.container,
        this.syncVolumeCheckbox.container,
        this.downloadWithNameCheckbox.container,
        this.sendNotifyOnCompleteCheckbox.container,
        this.useLivelyVoiceCheckbox.container,
        this.useAudioDownloadCheckbox.container
      );
      this.subtitlesDownloadFormatSelectLabel = new Label({
        labelText: localizationProvider.get("VOTSubtitlesDownloadFormat")
      });
      this.subtitlesDownloadFormatSelect = new Select({
        selectTitle: this.data.subtitlesDownloadFormat ?? localizationProvider.get("VOTSubtitlesDownloadFormat"),
        dialogTitle: localizationProvider.get("VOTSubtitlesDownloadFormat"),
        dialogParent: this.globalPortal,
        labelElement: this.subtitlesDownloadFormatSelectLabel.container,
        items: subtitleFormats.map((format) => ({
          label: format.toUpperCase(),
          value: format,
          selected: format === this.data.subtitlesDownloadFormat
        }))
      });
      this.subtitlesHighlightWordsCheckbox = new Checkbox({
        labelHtml: localizationProvider.get("VOTHighlightWords"),
        checked: this.data.highlightWords
      });
      const subtitlesSmartLayout2 = this.data.subtitlesSmartLayout ?? true;
      this.subtitlesSmartLayoutCheckbox = new Checkbox({
        labelHtml: localizationProvider.get("subtitlesSmartLayout"),
        checked: subtitlesSmartLayout2
      });
      const subtitlesMaxLength = this.data.subtitlesMaxLength ?? 300;
      this.subtitlesMaxLengthSliderLabel = new SliderLabel({
        labelText: localizationProvider.get("VOTSubtitlesMaxLength"),
        labelEOL: ":",
        value: subtitlesMaxLength,
        symbol: ""
      });
      this.subtitlesMaxLengthSlider = new Slider({
        labelHtml: this.subtitlesMaxLengthSliderLabel.container,
        value: subtitlesMaxLength,
        min: 50,
        max: 300
      });
      const subtitlesFontSize = this.data.subtitlesFontSize ?? 20;
      this.subtitlesFontSizeSliderLabel = new SliderLabel({
        labelText: localizationProvider.get("VOTSubtitlesFontSize"),
        labelEOL: ":",
        value: subtitlesFontSize,
        symbol: "px"
      });
      this.subtitlesFontSizeSlider = new Slider({
        labelHtml: this.subtitlesFontSizeSliderLabel.container,
        value: subtitlesFontSize,
        min: 8,
        max: 50
      });
      const storedSubtitlesFontFamily = typeof this.data.subtitlesFontFamily === "string" ? this.data.subtitlesFontFamily : void 0;
      const subtitlesFontFamily = storedSubtitlesFontFamily && (isBuiltInSubtitleFontFamily(storedSubtitlesFontFamily) || getGoogleSubtitleFontFamilyName(storedSubtitlesFontFamily)) ? storedSubtitlesFontFamily : "default-sans";
      const buildSubtitleFontItems = (selectedFontFamily, dynamicFontFamilies = []) => {
        const items = subtitleFontFamilies.map(
          (fontFamily) => ({
            label: subtitleFontFamilyLabels[fontFamily],
            value: fontFamily,
            selected: fontFamily === selectedFontFamily
          })
        );
        const dynamicItems = dynamicFontFamilies.filter((familyName) => {
          const lowerFamilyName = familyName.toLowerCase();
          return !items.some(
            (item) => item.label.toLowerCase() === lowerFamilyName
          );
        }).map((familyName) => {
          const fontValue = toGoogleSubtitleFontFamily(familyName);
          return {
            label: familyName,
            value: fontValue,
            selected: fontValue === selectedFontFamily
          };
        });
        if (!isBuiltInSubtitleFontFamily(selectedFontFamily) && !dynamicItems.some((item) => item.value === selectedFontFamily)) {
          const currentGoogleFontFamily = getGoogleSubtitleFontFamilyName(selectedFontFamily);
          if (currentGoogleFontFamily) {
            dynamicItems.unshift({
              label: currentGoogleFontFamily,
              value: selectedFontFamily,
              selected: true
            });
          }
        }
        return [...items, ...dynamicItems];
      };
      this.subtitlesFontFamilySelectLabel = new Label({
        labelText: localizationProvider.get("VOTSubtitlesFont")
      });
      this.subtitlesFontFamilySelect = new Select({
        selectTitle: getSubtitleFontFamilyLabel(subtitlesFontFamily),
        dialogTitle: localizationProvider.get("VOTSubtitlesFont"),
        dialogParent: this.globalPortal,
        labelElement: this.subtitlesFontFamilySelectLabel.container,
        items: buildSubtitleFontItems(subtitlesFontFamily),
        searchItemsProvider: async (query) => {
          const activeFontFamily = Array.from(this.subtitlesFontFamilySelect?.selectedValues ?? [])[0] ?? subtitlesFontFamily;
          const normalizedQuery = query.trim().toLowerCase();
          if (!normalizedQuery) {
            return buildSubtitleFontItems(activeFontFamily);
          }
          const googleFontsCatalog = await loadGoogleFontsCatalog();
          const matchingGoogleFonts = googleFontsCatalog.filter(
            (familyName) => familyName.toLowerCase().includes(normalizedQuery)
          ).slice(0, GOOGLE_FONTS_SEARCH_LIMIT);
          return buildSubtitleFontItems(activeFontFamily, matchingGoogleFonts);
        }
      });
      this.subtitlesFontFamilySelect.addEventListener("selectItem", (item) => {
        if (!this.subtitlesFontFamilySelect) return;
        this.subtitlesFontFamilySelect.updateItems(buildSubtitleFontItems(item));
        this.subtitlesFontFamilySelect.selectTitle = getSubtitleFontFamilyLabel(item);
      });
      const subtitlesOpacity = this.data.subtitlesOpacity ?? 20;
      this.subtitlesBackgroundOpacitySliderLabel = new SliderLabel({
        labelText: localizationProvider.get("VOTSubtitlesOpacity"),
        labelEOL: ":",
        value: subtitlesOpacity,
        symbol: "%"
      });
      this.subtitlesBackgroundOpacitySlider = new Slider({
        labelHtml: this.subtitlesBackgroundOpacitySliderLabel.container,
        value: subtitlesOpacity,
        min: 0,
        max: 100
      });
      subtitlesSection.content.append(
        this.subtitlesDownloadFormatSelect.container,
        this.subtitlesFontFamilySelect.container,
        this.subtitlesHighlightWordsCheckbox.container,
        this.subtitlesSmartLayoutCheckbox.container,
        this.subtitlesMaxLengthSlider.container,
        this.subtitlesFontSizeSlider.container,
        this.subtitlesBackgroundOpacitySlider.container
      );
      this.translateHotkeyButton = new HotkeyButton({
        labelHtml: localizationProvider.get("translateVideo"),
        key: this.data.translationHotkey
      });
      this.subtitlesHotkeyButton = new HotkeyButton({
        labelHtml: localizationProvider.get("VOTSubtitles"),
        key: this.data.subtitlesHotkey
      });
      hotkeysSection.content.append(
        this.translateHotkeyButton.container,
        this.subtitlesHotkeyButton.container
      );
      this.proxyWorkerHostTextfield = new Textfield({
        labelHtml: localizationProvider.get("VOTProxyWorkerHost"),
        value: this.data.proxyWorkerHost,
        placeholder: proxyWorkerHost
      });
      const proxyEnabledLabels = [
        localizationProvider.get("VOTTranslateProxyDisabled"),
        localizationProvider.get("VOTTranslateProxyEnabled"),
        localizationProvider.get("VOTTranslateProxyEverything")
      ];
      const translateProxyEnabled = this.data.translateProxyEnabled ?? 0;
      const isTranslateProxyRequired = countryCode && proxyOnlyCountries.includes(countryCode);
      this.proxyTranslationStatusSelectLabel = new Label({
        icon: isTranslateProxyRequired ? WARNING_ICON : void 0,
        labelText: localizationProvider.get("VOTTranslateProxyStatus")
      });
      if (isTranslateProxyRequired) {
        this.proxyTranslationStatusSelectTooltip = new Tooltip({
          target: this.proxyTranslationStatusSelectLabel.icon,
          content: localizationProvider.get("VOTTranslateProxyStatusDefault"),
          position: "bottom",
          backgroundColor: "var(--vot-helper-ondialog)",
          parentElement: this.globalPortal
        });
      }
      this.proxyTranslationStatusSelect = new Select({
        selectTitle: proxyEnabledLabels[translateProxyEnabled],
        dialogTitle: localizationProvider.get("VOTTranslateProxyStatus"),
        dialogParent: this.globalPortal,
        labelElement: this.proxyTranslationStatusSelectLabel.container,
        items: proxyEnabledLabels.map((label, idx) => ({
          label,
          value: idx.toString(),
          selected: idx === translateProxyEnabled,
          disabled: idx === 0 && isProxyOnlyExtension
        }))
      });
      proxySection.content.append(
        this.proxyWorkerHostTextfield.container,
        this.proxyTranslationStatusSelect.container
      );
      this.translateAPIErrorsCheckbox = new Checkbox({
        labelHtml: localizationProvider.get("VOTTranslateAPIErrors"),
        checked: this.data.translateAPIErrors ?? true
      });
      this.translateAPIErrorsCheckbox.hidden = localizationProvider.lang === "ru";
      this.useNewAudioPlayerCheckbox = new Checkbox({
        labelHtml: localizationProvider.get("VOTNewAudioPlayer"),
        checked: this.data.newAudioPlayer
      });
      if (!this.videoHandler?.isAudioContextSupported) {
        this.useNewAudioPlayerCheckbox.disabled = true;
        this.useNewAudioPlayerTooltip = new Tooltip({
          target: this.useNewAudioPlayerCheckbox.container,
          content: localizationProvider.get("VOTNeedWebAudioAPI"),
          position: "bottom",
          backgroundColor: "var(--vot-helper-ondialog)",
          parentElement: this.globalPortal
        });
      }
      const onlyBypassMediaCSPLabel = this.videoHandler?.site.needBypassCSP ? `${localizationProvider.get("VOTOnlyBypassMediaCSP")} (${localizationProvider.get("VOTMediaCSPEnabledOnSite")})` : localizationProvider.get("VOTOnlyBypassMediaCSP");
      this.onlyBypassMediaCSPCheckbox = new Checkbox({
        labelHtml: onlyBypassMediaCSPLabel,
        checked: this.data.onlyBypassMediaCSP,
        isSubCheckbox: true
      });
      if (!this.videoHandler?.isAudioContextSupported) {
        this.onlyBypassMediaCSPTooltip = new Tooltip({
          target: this.onlyBypassMediaCSPCheckbox.container,
          content: localizationProvider.get("VOTNeedWebAudioAPI"),
          position: "bottom",
          backgroundColor: "var(--vot-helper-ondialog)",
          parentElement: this.globalPortal
        });
      }
      this.onlyBypassMediaCSPCheckbox.disabled = !this.data.newAudioPlayer && !!this.videoHandler?.isAudioContextSupported;
      if (!this.data.newAudioPlayer) {
        this.onlyBypassMediaCSPCheckbox.hidden = true;
      }
      this.translationTextServiceLabel = new Label({
        labelText: localizationProvider.get("VOTTranslationTextService"),
        icon: HELP_ICON
      });
      const translationService = this.data.translationService ?? defaultTranslationService;
      this.translationTextServiceSelect = new Select({
        selectTitle: localizationProvider.get(`services.${translationService}`),
        dialogTitle: localizationProvider.get("VOTTranslationTextService"),
        dialogParent: this.globalPortal,
        labelElement: this.translationTextServiceLabel.container,
        items: foswlyServices.map((service) => ({
          label: localizationProvider.get(`services.${service}`),
          value: service,
          selected: service === translationService
        }))
      });
      this.translationTextServiceTooltip = new Tooltip({
        target: this.translationTextServiceLabel.icon,
        content: localizationProvider.get("VOTNotAffectToVoice"),
        position: "bottom",
        backgroundColor: "var(--vot-helper-ondialog)",
        parentElement: this.globalPortal
      });
      this.detectServiceLabel = new Label({
        labelText: localizationProvider.get("VOTDetectService")
      });
      const detectService = this.data.detectService ?? defaultDetectService;
      this.detectServiceSelect = new Select({
        selectTitle: localizationProvider.get(`services.${detectService}`),
        dialogTitle: localizationProvider.get("VOTDetectService"),
        dialogParent: this.globalPortal,
        labelElement: this.detectServiceLabel.container,
        items: detectServices.map((service) => ({
          label: localizationProvider.get(`services.${service}`),
          value: service,
          selected: service === detectService
        }))
      });
      this.showPiPButtonCheckbox = new Checkbox({
        labelHtml: localizationProvider.get("VOTShowPiPButton"),
        checked: this.data.showPiPButton
      });
      this.showPiPButtonCheckbox.hidden = !isPiPAvailable();
      const autoHideButtonDelaySec = Math.round(
        (this.data.autoHideButtonDelay ?? defaultAutoHideDelay) / 1e3 * 10
      ) / 10;
      this.autoHideButtonDelaySliderLabel = new SliderLabel({
        labelText: localizationProvider.get("autoHideButtonDelay"),
        labelEOL: ":",
        value: autoHideButtonDelaySec,
        symbol: ` ${localizationProvider.get("secs")}`
      });
      this.autoHideButtonDelaySlider = new Slider({
        labelHtml: this.autoHideButtonDelaySliderLabel.container,
        value: autoHideButtonDelaySec,
        min: 0.1,
        max: 3,
        step: 0.1
      });
      this.buttonPositionSelectLabel = new Label({
        labelText: localizationProvider.get("buttonPosition"),
        icon: HELP_ICON
      });
      const buttonPos = this.data.buttonPos ?? "default";
      this.buttonPositionSelect = new Select({
        selectTitle: localizationProvider.get(`position.${buttonPos}`),
        dialogTitle: localizationProvider.get("buttonPosition"),
        labelElement: this.buttonPositionSelectLabel.container,
        dialogParent: this.globalPortal,
        items: positions$1.map((position2) => ({
          label: localizationProvider.get(`position.${position2}`),
          value: position2,
          selected: position2 === buttonPos
        }))
      });
      this.buttonPositionTooltip = new Tooltip({
        target: this.buttonPositionSelectLabel.icon,
        content: localizationProvider.get("minButtonPositionContainer"),
        position: "bottom",
        backgroundColor: "var(--vot-helper-ondialog)",
        parentElement: this.globalPortal
      });
      this.menuLanguageSelectLabel = new Label({
        labelText: localizationProvider.get("VOTMenuLanguage")
      });
      this.menuLanguageSelect = new Select({
        selectTitle: localizationProvider.get(
          `langs.${localizationProvider.langOverride}`
        ),
        dialogTitle: localizationProvider.get("VOTMenuLanguage"),
        labelElement: this.menuLanguageSelectLabel.container,
        dialogParent: this.globalPortal,
        items: Select.genLanguageItems(
          localizationProvider.getAvailableLangs(),
          localizationProvider.langOverride
        )
      });
      this.bugReportButton = UI.createOutlinedButton(
        localizationProvider.get("VOTBugReport")
      );
      this.resetSettingsButton = UI.createButton(
        localizationProvider.get("resetSettings")
      );
      miscSection.content.append(
        this.translateAPIErrorsCheckbox.container,
        this.useNewAudioPlayerCheckbox.container,
        this.onlyBypassMediaCSPCheckbox.container
      );
      translationSection.content.append(
        this.translationTextServiceSelect.container,
        this.detectServiceSelect.container
      );
      appearanceSection.content.append(
        this.showPiPButtonCheckbox.container,
        this.autoHideButtonDelaySlider.container,
        this.buttonPositionSelect.container,
        this.menuLanguageSelect.container
      );
      const envInfo = getEnvironmentInfo();
      const versionInfo = UI.createInformation(
        `${localizationProvider.get("VOTVersion")}:`,
        envInfo.scriptVersion || GM_info.script.version || localizationProvider.get("notFound")
      );
      const buildAuthors = String("Toil, SashaXser, MrSoczekXD, mynovelhost, sodapng, Acobat12");
      const authorsInfo = UI.createInformation(
        `${localizationProvider.get("VOTAuthors")}:`,
        GM_info.script.author || buildAuthors || localizationProvider.get("notFound")
      );
      const loaderInfo = UI.createInformation(
        `${localizationProvider.get("VOTLoader")}:`,
        envInfo.loader
      );
      const userBrowserInfo = UI.createInformation(
        `${localizationProvider.get("VOTBrowser")}:`,
        `${envInfo.browser} (${envInfo.os})`
      );
      const localeUpdatedAt = new Date(
        (this.data.localeUpdatedAt ?? 0) * 1e3
      ).toLocaleString();
      const localeHashValue = this.data.localeHash ?? localizationProvider.get("notFound");
      const localeInfoValue = b`${localeHashValue}<br />(${localizationProvider.get(
      "VOTUpdatedAt"
    )} ${localeUpdatedAt})`;
      const localeInfo = UI.createInformation(
        `${localizationProvider.get("VOTLocaleHash")}:`,
        localeInfoValue
      );
      const updateLocaleFilesButton = UI.createOutlinedButton(
        localizationProvider.get("VOTUpdateLocaleFiles")
      );
      updateLocaleFilesButton.addEventListener("click", async () => {
        await votStorage.set("localeHash", "");
        await localizationProvider.update(true);
        globalThis.location.reload();
      });
      aboutSection.content.append(
        versionInfo.container,
        authorsInfo.container,
        loaderInfo.container,
        userBrowserInfo.container,
        localeInfo.container,
        updateLocaleFilesButton
      );
      this.dialog.footerContainer.append(
        this.bugReportButton,
        this.resetSettingsButton
      );
      this.initialized = true;
      return this;
    }
    initUIEvents() {
      if (!this.isInitialized()) {
        throw new Error("[VOT] SettingsView isn't initialized");
      }
      this.bindOAuthMessageListener();
      this.accountButton.addEventListener("click", async () => {
        if (votStorage.isSupportOnlyLS) return;
        if (this.accountButton.loggedIn) {
          await votStorage.delete("account");
          this.data.account = {};
          return this.updateAccountInfo();
        }
        const authUrl = await createPkceAuthUrl();
        globalThis.open(
          authUrl,
          "yandex-oauth",
          "popup=yes,width=520,height=720,resizable=yes,scrollbars=yes"
        )?.focus();
      });
      this.accountButton.addEventListener("click:secret", async () => {
        const dialog = new Dialog({
          titleHtml: localizationProvider.get("VOTLoginViaToken"),
          isTemp: true
        });
        this.globalPortal.appendChild(dialog.container);
        const tokenInfoEl = UI.createEl(
          "vot-block",
          void 0,
          localizationProvider.get("VOTYandexTokenInfo")
        );
        const tokenTextfield = new Textfield({
          labelHtml: localizationProvider.get("VOTYandexToken"),
          value: this.data.account?.token
        });
        tokenTextfield.addEventListener("change", async (token) => {
          this.data.account = token ? { expires: Date.now() + 3153418e4, token } : {};
          await votStorage.set("account", this.data.account);
          this.updateAccountInfo();
        });
        dialog.bodyContainer.append(tokenInfoEl, tokenTextfield.container);
        dialog.open();
      });
      this.accountButton.addEventListener("refresh", async () => {
        if (votStorage.isSupportOnlyLS) return;
        this.data.account = await votStorage.get("account", {});
        this.updateAccountInfo();
      });
      this.bindPersistedSetting({
        control: this.autoTranslateCheckbox,
        event: "change",
        apply: (checked) => {
          this.data.autoTranslate = checked;
        },
        storageKey: "autoTranslate",
        readPersistedValue: () => this.data.autoTranslate,
        logLabel: "autoTranslate",
        dispatch: (checked) => this.events["change:autoTranslate"].dispatch(checked)
      });
      this.bindPersistedSetting({
        control: this.autoSubtitlesCheckbox,
        event: "change",
        apply: (checked) => {
          this.data.autoSubtitles = checked;
        },
        storageKey: "autoSubtitles",
        readPersistedValue: () => this.data.autoSubtitles,
        logLabel: "autoSubtitles",
        dispatch: (checked) => this.events["change:autoSubtitles"].dispatch(checked)
      });
      this.dontTranslateLanguagesCheckbox.addEventListener(
        "change",
        async (checked) => {
          this.data.enabledDontTranslateLanguages = checked;
          this.dontTranslateLanguagesSelect.disabled = !checked;
          await votStorage.set(
            "enabledDontTranslateLanguages",
            this.data.enabledDontTranslateLanguages
          );
          debug.log(
            "enabledDontTranslateLanguages value changed. New value:",
            checked
          );
        }
      );
      this.dontTranslateLanguagesSelect.addEventListener(
        "selectItem",
        async (values) => {
          this.data.dontTranslateLanguages = values;
          await votStorage.set(
            "dontTranslateLanguages",
            this.data.dontTranslateLanguages
          );
          debug.log("dontTranslateLanguages value changed. New value:", values);
        }
      );
      this.bindPersistedSetting({
        control: this.autoSetVolumeCheckbox,
        event: "change",
        apply: (checked) => {
          this.data.enabledAutoVolume = checked;
          this.autoSetVolumeSlider.disabled = !checked;
          this.smartDuckingCheckbox.disabled = !checked || Boolean(this.syncVolumeCheckbox?.checked);
        },
        storageKey: "enabledAutoVolume",
        readPersistedValue: () => this.data.enabledAutoVolume,
        logLabel: "enabledAutoVolume",
        afterPersist: async () => this.videoHandler?.setupAudioSettings?.()
      });
      this.bindPersistedSetting({
        control: this.smartDuckingCheckbox,
        event: "change",
        apply: (checked) => {
          this.data.enabledSmartDucking = checked;
        },
        storageKey: "enabledSmartDucking",
        readPersistedValue: () => this.data.enabledSmartDucking,
        logLabel: "enabledSmartDucking",
        afterPersist: async () => this.videoHandler?.setupAudioSettings?.()
      });
      this.bindPersistedSetting({
        control: this.autoSetVolumeSlider,
        event: "input",
        apply: (value) => {
          this.data.autoVolume = this.autoSetVolumeSliderLabel.value = value;
        },
        storageKey: "autoVolume",
        readPersistedValue: () => this.data.autoVolume,
        logLabel: "autoVolume"
      });
      this.bindPersistedSetting({
        control: this.showVideoVolumeSliderCheckbox,
        event: "change",
        apply: (checked) => {
          this.data.showVideoSlider = checked;
        },
        storageKey: "showVideoSlider",
        readPersistedValue: () => this.data.showVideoSlider,
        logLabel: "showVideoVolumeSlider",
        dispatch: (checked) => this.events["change:showVideoVolume"].dispatch(checked)
      });
      this.bindPersistedSetting({
        control: this.audioBoosterCheckbox,
        event: "change",
        apply: (checked) => {
          this.data.audioBooster = checked;
        },
        storageKey: "audioBooster",
        readPersistedValue: () => this.data.audioBooster,
        logLabel: "audioBooster",
        dispatch: (checked) => this.events["change:audioBooster"].dispatch(checked)
      });
      this.bindPersistedSetting({
        control: this.syncVolumeCheckbox,
        event: "change",
        apply: (checked) => {
          this.data.syncVolume = checked;
          this.autoSetVolumeSlider.disabled = !this.autoSetVolumeCheckbox?.checked;
          this.smartDuckingCheckbox.disabled = checked || !this.autoSetVolumeCheckbox?.checked;
          if (checked && this.smartDuckingCheckbox?.checked) {
            this.smartDuckingCheckbox.checked = false;
          }
        },
        storageKey: "syncVolume",
        readPersistedValue: () => this.data.syncVolume,
        logLabel: "syncVolume",
        dispatch: (checked) => this.events["change:syncVolume"].dispatch(checked)
      });
      this.bindPersistedSetting({
        control: this.downloadWithNameCheckbox,
        event: "change",
        apply: (checked) => {
          this.data.downloadWithName = checked;
        },
        storageKey: "downloadWithName",
        readPersistedValue: () => this.data.downloadWithName,
        logLabel: "downloadWithName"
      });
      this.bindPersistedSetting({
        control: this.sendNotifyOnCompleteCheckbox,
        event: "change",
        apply: (checked) => {
          this.data.sendNotifyOnComplete = checked;
        },
        storageKey: "sendNotifyOnComplete",
        readPersistedValue: () => this.data.sendNotifyOnComplete,
        logLabel: "sendNotifyOnComplete"
      });
      this.bindPersistedSetting({
        control: this.useLivelyVoiceCheckbox,
        event: "change",
        apply: (checked) => {
          this.data.useLivelyVoice = checked;
        },
        storageKey: "useLivelyVoice",
        readPersistedValue: () => this.data.useLivelyVoice,
        logLabel: "useLivelyVoice",
        dispatch: (checked) => this.events["change:useLivelyVoice"].dispatch(checked)
      });
      this.bindPersistedSetting({
        control: this.useAudioDownloadCheckbox,
        event: "change",
        apply: (checked) => {
          this.data.useAudioDownload = checked;
        },
        storageKey: "useAudioDownload",
        readPersistedValue: () => this.data.useAudioDownload,
        logLabel: "useAudioDownload"
      });
      this.bindPersistedSetting({
        control: this.subtitlesDownloadFormatSelect,
        event: "selectItem",
        apply: (item) => {
          this.data.subtitlesDownloadFormat = item;
        },
        storageKey: "subtitlesDownloadFormat",
        readPersistedValue: () => this.data.subtitlesDownloadFormat,
        logLabel: "subtitlesDownloadFormat"
      });
      this.bindPersistedSetting({
        control: this.subtitlesHighlightWordsCheckbox,
        event: "change",
        apply: (checked) => {
          this.data.highlightWords = checked;
        },
        storageKey: "highlightWords",
        readPersistedValue: () => this.data.highlightWords,
        logLabel: "highlightWords",
        dispatch: (checked) => this.events["change:subtitlesHighlightWords"].dispatch(checked)
      });
      this.subtitlesSmartLayoutCheckbox?.addEventListener("change", (checked) => {
        if (this.suppressSubtitlesSmartLayoutCheckboxChange) return;
        this.setSubtitlesSmartLayout(checked);
      });
      this.subtitlesMaxLengthSlider.addEventListener("input", (value) => {
        this.subtitlesMaxLengthSliderLabel.value = value;
        if ((this.data.subtitlesSmartLayout ?? true) === true) {
          this.setSubtitlesSmartLayout(false);
        }
        this.data.subtitlesMaxLength = value;
        this.scheduleStoragePersist(
          "subtitlesMaxLength",
          this.data.subtitlesMaxLength
        );
        debug.log("subtitlesMaxLength value changed. New value:", value);
        this.events["input:subtitlesMaxLength"].dispatch(value);
      });
      this.subtitlesFontSizeSlider.addEventListener("input", (value) => {
        this.subtitlesFontSizeSliderLabel.value = value;
        if ((this.data.subtitlesSmartLayout ?? true) === true) {
          this.setSubtitlesSmartLayout(false);
        }
        this.data.subtitlesFontSize = value;
        this.scheduleStoragePersist(
          "subtitlesFontSize",
          this.data.subtitlesFontSize
        );
        debug.log("subtitlesFontSize value changed. New value:", value);
        this.events["input:subtitlesFontSize"].dispatch(value);
      });
      this.subtitlesBackgroundOpacitySlider.addEventListener("input", (value) => {
        this.subtitlesBackgroundOpacitySliderLabel.value = value;
        this.data.subtitlesOpacity = value;
        this.scheduleStoragePersist(
          "subtitlesOpacity",
          this.data.subtitlesOpacity
        );
        debug.log("subtitlesOpacity value changed. New value:", value);
        this.events["input:subtitlesBackgroundOpacity"].dispatch(value);
      });
      this.bindPersistedSetting({
        control: this.subtitlesFontFamilySelect,
        event: "selectItem",
        apply: (item) => {
          this.data.subtitlesFontFamily = item;
        },
        storageKey: "subtitlesFontFamily",
        readPersistedValue: () => this.data.subtitlesFontFamily,
        logLabel: "subtitlesFontFamily",
        dispatch: (item) => this.events["select:subtitlesFontFamily"].dispatch(item)
      });
      this.bindPersistedSetting({
        control: this.translateHotkeyButton,
        event: "change",
        apply: (key) => {
          this.data.translationHotkey = key;
        },
        storageKey: "translationHotkey",
        readPersistedValue: () => this.data.translationHotkey,
        logLabel: "translationHotkey"
      });
      this.bindPersistedSetting({
        control: this.subtitlesHotkeyButton,
        event: "change",
        apply: (key) => {
          this.data.subtitlesHotkey = key;
        },
        storageKey: "subtitlesHotkey",
        readPersistedValue: () => this.data.subtitlesHotkey,
        logLabel: "subtitlesHotkey"
      });
      this.proxyWorkerHostTextfield.addEventListener("change", async (value) => {
        this.data.proxyWorkerHost = value || proxyWorkerHost;
        await votStorage.set("proxyWorkerHost", this.data.proxyWorkerHost);
        debug.log(
          "proxyWorkerHost value changed. New value:",
          this.data.proxyWorkerHost
        );
        this.events["change:proxyWorkerHost"].dispatch(value);
      });
      this.proxyTranslationStatusSelect.addEventListener(
        "selectItem",
        async (item) => {
          this.data.translateProxyEnabled = Number.parseInt(
            item,
            10
          );
          await votStorage.set(
            "translateProxyEnabled",
            this.data.translateProxyEnabled
          );
          await votStorage.set("translateProxyEnabledDefault", false);
          debug.log(
            "translateProxyEnabled value changed. New value:",
            this.data.translateProxyEnabled
          );
          this.events["select:proxyTranslationStatus"].dispatch(item);
        }
      );
      this.bindPersistedSetting({
        control: this.translateAPIErrorsCheckbox,
        event: "change",
        apply: (checked) => {
          this.data.translateAPIErrors = checked;
        },
        storageKey: "translateAPIErrors",
        readPersistedValue: () => this.data.translateAPIErrors,
        logLabel: "translateAPIErrors"
      });
      this.bindPersistedSetting({
        control: this.useNewAudioPlayerCheckbox,
        event: "change",
        apply: (checked) => {
          this.data.newAudioPlayer = checked;
          this.onlyBypassMediaCSPCheckbox.disabled = this.onlyBypassMediaCSPCheckbox.hidden = !checked;
        },
        storageKey: "newAudioPlayer",
        readPersistedValue: () => this.data.newAudioPlayer,
        logLabel: "newAudioPlayer",
        dispatch: (checked) => this.events["change:useNewAudioPlayer"].dispatch(checked)
      });
      this.bindPersistedSetting({
        control: this.onlyBypassMediaCSPCheckbox,
        event: "change",
        apply: (checked) => {
          this.data.onlyBypassMediaCSP = checked;
        },
        storageKey: "onlyBypassMediaCSP",
        readPersistedValue: () => this.data.onlyBypassMediaCSP,
        logLabel: "onlyBypassMediaCSP",
        dispatch: (checked) => this.events["change:onlyBypassMediaCSP"].dispatch(checked)
      });
      this.bindPersistedSetting({
        control: this.translationTextServiceSelect,
        event: "selectItem",
        apply: (item) => {
          this.data.translationService = item;
        },
        storageKey: "translationService",
        readPersistedValue: () => this.data.translationService,
        logLabel: "translationService",
        dispatch: (item) => this.events["select:translationTextService"].dispatch(item)
      });
      this.bindPersistedSetting({
        control: this.detectServiceSelect,
        event: "selectItem",
        apply: (item) => {
          this.data.detectService = item;
        },
        storageKey: "detectService",
        readPersistedValue: () => this.data.detectService,
        logLabel: "detectService"
      });
      this.bindPersistedSetting({
        control: this.showPiPButtonCheckbox,
        event: "change",
        apply: (checked) => {
          this.data.showPiPButton = checked;
        },
        storageKey: "showPiPButton",
        readPersistedValue: () => this.data.showPiPButton,
        logLabel: "showPiPButton",
        dispatch: (checked) => this.events["change:showPiPButton"].dispatch(checked)
      });
      this.autoHideButtonDelaySlider.addEventListener("input", (value) => {
        this.autoHideButtonDelaySliderLabel.value = value;
        const newDelay = Math.round(value * 1e3);
        debug.log("autoHideButtonDelay value changed. New value:", newDelay);
        this.data.autoHideButtonDelay = newDelay;
        this.scheduleStoragePersist(
          "autoHideButtonDelay",
          this.data.autoHideButtonDelay
        );
        this.events["input:autoHideButtonDelay"].dispatch(value);
      });
      this.bindPersistedSetting({
        control: this.buttonPositionSelect,
        event: "selectItem",
        apply: (item) => {
          this.data.buttonPos = item;
        },
        storageKey: "buttonPos",
        readPersistedValue: () => this.data.buttonPos,
        logLabel: "buttonPos",
        dispatch: (item) => this.events["select:buttonPosition"].dispatch(item)
      });
      this.menuLanguageSelect.addEventListener("selectItem", async (item) => {
        const result = await localizationProvider.changeLang(item);
        if (!result) return;
        this.data.localeUpdatedAt = await votStorage.get("localeUpdatedAt", 0);
        this.events["select:menuLanguage"].dispatch(item);
      });
      this.bugReportButton.addEventListener(
        "click",
        () => this.events["click:bugReport"].dispatch()
      );
      this.resetSettingsButton.addEventListener(
        "click",
        () => this.events["click:resetSettings"].dispatch()
      );
      return this;
    }
    addEventListener(type, listener) {
      this.events[type].addListener(listener);
      return this;
    }
    removeEventListener(type, listener) {
      this.events[type].removeListener(listener);
      return this;
    }
    doReleaseUI() {
      this.dialog?.remove();
      for (const tooltip of [
        this.accountButtonRefreshTooltip,
        this.accountButtonTokenTooltip,
        this.audioBoosterTooltip,
        this.useLivelyVoiceTooltip,
        this.useAudioDownloadCheckboxTooltip,
        this.useNewAudioPlayerTooltip,
        this.onlyBypassMediaCSPTooltip,
        this.translationTextServiceTooltip,
        this.proxyTranslationStatusSelectTooltip,
        this.buttonPositionTooltip
      ]) {
        tooltip?.release();
      }
    }
    doReleaseUIEvents() {
      this.flushStoragePersists();
      for (const event of Object.values(this.events)) event.clear();
    }
    release() {
      if (!this.isInitialized()) return this;
      this.doReleaseUIEvents();
      this.doReleaseUI();
      this.initialized = false;
      return this;
    }
    updateAccountInfo() {
      if (!this.isInitialized()) {
        throw new Error("[VOT] SettingsView isn't initialized");
      }
      const loggedIn = !!this.data.account?.token;
      this.accountButton.avatarId = this.data.account?.avatarId;
      this.useLivelyVoiceTooltip.hidden = this.accountButton.loggedIn = loggedIn;
      this.accountButton.username = this.data.account?.username;
      this.useLivelyVoiceCheckbox.disabled = !loggedIn;
      this.events["update:account"].dispatch(this.data.account);
      return this;
    }
    open() {
      if (!this.isInitialized()) {
        throw new Error("[VOT] SettingsView isn't initialized");
      }
      return this.dialog.open();
    }
    close() {
      if (!this.isInitialized()) {
        throw new Error("[VOT] SettingsView isn't initialized");
      }
      return this.dialog.close();
    }
  }
  const GOOGLE_DRIVE_TOP_FRAME_ROOT_ATTR = "data-vot-google-drive-top-frame-root";
  const GOOGLE_DRIVE_TOP_FRAME_POPUP_ROOT_ATTR = "data-vot-google-drive-top-frame-popup-root";
  const GOOGLE_DRIVE_TOP_FRAME_DIALOG_ROOT_ATTR = "data-vot-google-drive-top-frame-dialog-root";
  const GOOGLE_DRIVE_TOP_FRAME_IFRAME_SELECTOR = 'iframe[src*="youtube.googleapis.com/embed/"]';
  const GOOGLE_DRIVE_BRIDGE_FLAG = "__votGoogleDriveBridge";
  let installed$2 = false;
  let layoutSyncScheduled = false;
  let overlayRoot = null;
  let popupPortalRoot = null;
  let dialogPortalRoot = null;
  let button = null;
  let quickMenu = null;
  let voiceModeMenu = null;
  let downloadTranslationButton = null;
  let downloadSubtitlesButton = null;
  let openSettingsButton = null;
  let languagePairSelect = null;
  let subtitlesSelectLabel = null;
  let subtitlesSelect = null;
  let videoVolumeSliderLabel = null;
  let videoVolumeSlider = null;
  let translationVolumeSliderLabel = null;
  let translationVolumeSlider = null;
  let settingsView = null;
  let settingsData = null;
  let settingsInitPromise = null;
  let currentState = null;
  let observedIframe = null;
  let iframeResizeObserver = null;
  let domObserver = null;
  let bridgePollTimer = null;
  let dialogLayerCounter = 10;
  const DRIVE_TOP_FRAME_BUTTON_LAYER_Z = "10";
  const DRIVE_TOP_FRAME_POPUP_LAYER_Z = "20";
  const DRIVE_TOP_FRAME_DIALOG_LAYER_Z = "30";
  function debugDriveTopFrame(message, details) {
    const debugMessage = details ? `${message} ${JSON.stringify(details)}` : message;
    console.info(`[VOT][DriveTopFrame] ${debugMessage}`);
    document.documentElement.setAttribute(
      "data-vot-drive-top-frame-debug-last",
      debugMessage
    );
  }
  function isGoogleDriveTopFrameContext() {
    const host = String(globalThis.location.hostname || "").toLowerCase();
    return globalThis.top === globalThis && (host === "drive.google.com" || host === "docs.google.com");
  }
  function isVisibleIframe(iframe) {
    if (!(iframe instanceof HTMLIFrameElement) || !iframe.isConnected) {
      return false;
    }
    const rect = iframe.getBoundingClientRect();
    return rect.width > 120 && rect.height > 80;
  }
  function getDrivePlayerIframe() {
    const selectors = [
      `#drive-viewer-video-player-object-0 ${GOOGLE_DRIVE_TOP_FRAME_IFRAME_SELECTOR}`,
      `.rSvt3b ${GOOGLE_DRIVE_TOP_FRAME_IFRAME_SELECTOR}`,
      `.xpiQs ${GOOGLE_DRIVE_TOP_FRAME_IFRAME_SELECTOR}`,
      GOOGLE_DRIVE_TOP_FRAME_IFRAME_SELECTOR
    ];
    for (const selector of selectors) {
      const matched = Array.from(
        document.querySelectorAll(selector)
      ).find((iframe) => isVisibleIframe(iframe));
      if (matched) {
        return matched;
      }
    }
    return null;
  }
  function getPlayerRect() {
    return getDrivePlayerIframe()?.getBoundingClientRect() ?? null;
  }
  function getBridgeTargetOrigin(iframe) {
    try {
      return new URL(iframe.src, globalThis.location.href).origin;
    } catch {
      return "*";
    }
  }
  function postBridgeMessage(message) {
    const iframe = getDrivePlayerIframe();
    if (!iframe?.contentWindow) {
      return;
    }
    iframe.contentWindow.postMessage(message, getBridgeTargetOrigin(iframe));
  }
  function postBridgeCommand(command, extra = {}) {
    postBridgeMessage({
      [GOOGLE_DRIVE_BRIDGE_FLAG]: true,
      kind: "command",
      command,
      ...extra
    });
  }
  function getDefaultButtonText() {
    return localizationProvider.get("VOTRailTranslateAndDub");
  }
  function getDisableButtonText() {
    return localizationProvider.get("VOTRailDisableTranslation");
  }
  function formatLocalizedTemplate(template, value) {
    return template.replace("{0}", String(value));
  }
  function extractLoadingEtaValue(rawText) {
    const text = rawText.trim();
    if (!text) {
      return null;
    }
    if (text === localizationProvider.get("translationTakeAboutMinute") || text === "The translation will take about a minute") {
      return 1;
    }
    if (text === localizationProvider.get("translationTakeFewMinutes") || text === "The translation will take a few minutes") {
      return 5;
    }
    if (text === localizationProvider.get("translationTakeMoreThanHour") || text === "The translation will take more than an hour") {
      return "more-than-hour";
    }
    const translationTakePrefix = localizationProvider.get("translationTake");
    const englishTranslationTakePrefix = "The translation will take";
    if (!text.includes(translationTakePrefix) && !text.includes(englishTranslationTakePrefix)) {
      return null;
    }
    const digits = text.match(/(\d+)/);
    if (!digits) {
      return null;
    }
    const value = Number.parseInt(digits[1], 10);
    return Number.isFinite(value) && value > 0 ? value : null;
  }
  function formatCompactEtaValue(eta) {
    if (!eta) {
      return "";
    }
    if (eta === "more-than-hour") {
      return localizationProvider.get("VOTRailCompactMoreThanHour");
    }
    if (eta <= 1) {
      return localizationProvider.get("VOTRailCompactMinute");
    }
    return formatLocalizedTemplate(
      localizationProvider.get("VOTRailCompactMinutes"),
      eta
    );
  }
  function getEstimatedEtaLabel(rawText) {
    return formatCompactEtaValue(extractLoadingEtaValue(rawText));
  }
  function getVoiceModeEtaLabel(mode) {
    if (normalizeStatus(currentState) !== "loading") {
      return "";
    }
    const baseEta = extractLoadingEtaValue(currentState?.labelText ?? "");
    if (!baseEta) {
      return "";
    }
    const activeMode = getConfiguredVoiceMode();
    if (baseEta === "more-than-hour") {
      return formatCompactEtaValue(baseEta);
    }
    const adjustedEta = activeMode === mode ? baseEta : mode === "lively" ? Math.max(2, Math.round(baseEta * 8)) : Math.max(1, Math.round(baseEta / 8));
    return formatCompactEtaValue(adjustedEta);
  }
  function getLoadingButtonText(state) {
    const rawText = state?.labelText?.trim() ?? "";
    const eta = getEstimatedEtaLabel(rawText);
    if (!eta) {
      const localizedKnownText = localizeKnownBridgeText(rawText);
      if (localizedKnownText) {
        return localizedKnownText;
      }
      if (isBridgeLoadingLabel(rawText)) {
        return rawText;
      }
      if (rawText && rawText !== getDefaultButtonText() && rawText !== getDisableButtonText() && rawText !== getVoiceModeLabel("standard") && rawText !== getVoiceModeLabel("lively")) {
        return rawText;
      }
      return localizationProvider.get("videoBeingTranslated");
    }
    return formatLocalizedTemplate(
      localizationProvider.get("VOTRailTranslatingEta"),
      eta
    );
  }
  function localizeBridgeButtonText(rawText, state) {
    const text = rawText.trim();
    if (!text) {
      return normalizeStatus(state) === "success" ? getDisableButtonText() : getDefaultButtonText();
    }
    if (text === "Translate video") {
      return getDefaultButtonText();
    }
    if (text === "Turn off" || text === "Disable translation") {
      return getDisableButtonText();
    }
    const localizedKnownText = localizeKnownBridgeText(text);
    if (localizedKnownText) {
      return localizedKnownText;
    }
    return text;
  }
  function normalizeStatus(state) {
    if (!state) {
      return "none";
    }
    if (state.loading || isBridgeLoadingLabel(state.labelText ?? "")) {
      return "loading";
    }
    switch (state.status) {
      case "none":
      case "error":
      case "success":
      case "loading":
      case "disabled":
        return state.status;
      default:
        return "none";
    }
  }
  function isBridgeLoadingLabel(rawText) {
    const text = rawText.trim();
    if (!text) {
      return false;
    }
    if (text.includes(localizationProvider.get("translationTake")) || text.includes("The translation will take")) {
      return true;
    }
    const delayed = localizationProvider.get("TranslationDelayed");
    if (delayed && text.includes(delayed)) {
      return true;
    }
    return [
      localizationProvider.get("videoBeingTranslated"),
      "The video is being translated",
      "Подготавливаем перевод",
      "Preparing translation",
      "Видео передано в обработку",
      "Video sent for processing",
      "Ожидаем перевод видео",
      "Waiting for video translation",
      "Загружаем переведенное аудио",
      "Uploading translated audio"
    ].includes(text);
  }
  function localizeKnownBridgeText(rawText) {
    const text = rawText.trim();
    if (!text) {
      return null;
    }
    switch (text) {
      case "The video is being translated":
        return localizationProvider.get("videoBeingTranslated");
      case "Failed to request video translation":
      case "Yandex couldn't translate video":
        return localizationProvider.get("requestTranslationFailed");
      case "Audio link not received":
        return localizationProvider.get("audioNotReceived");
      case "Failed to download audio":
        return localizationProvider.get("VOTFailedDownloadAudio");
      case "Preparing translation":
        return localizationProvider.get("videoBeingTranslated");
      case "Video sent for processing":
      case "Waiting for video translation":
      case "Uploading translated audio":
        return localizationProvider.get("videoBeingTranslated");
      default:
        return null;
    }
  }
  function getButtonText(state) {
    const status = normalizeStatus(state);
    if (status === "loading") {
      return getLoadingButtonText(state);
    }
    if (status === "success") {
      return getVoiceModeLabel(getSelectedVoiceMode(state));
    }
    if (status === "error") {
      return localizeBridgeButtonText(state?.labelText ?? "", state);
    }
    return getDefaultButtonText();
  }
  function getDriveRailPosition() {
    return settingsData?.buttonPos ?? "default";
  }
  function getConfiguredVoiceMode() {
    return settingsData?.useLivelyVoice ? "lively" : "standard";
  }
  function getSelectedVoiceMode(state) {
    const configuredMode = getConfiguredVoiceMode();
    const status = normalizeStatus(state);
    if (status === "success" && (state?.voiceMode === "lively" || state?.voiceMode === "standard")) {
      return state.voiceMode;
    }
    return configuredMode;
  }
  function getDisplayedVoiceMode(state) {
    const status = normalizeStatus(state);
    const playbackState = state?.voicePlaybackState || "idle";
    const isActive = status === "success" || status === "loading" || playbackState === "loading" || playbackState === "playing" || playbackState === "paused";
    if (isActive && (state?.voiceMode === "lively" || state?.voiceMode === "standard")) {
      return state.voiceMode;
    }
    return "standard";
  }
  function getVoiceModeLabel(mode) {
    return mode === "lively" ? localizationProvider.get("VOTLivelyVoices") : localizationProvider.get("VOTStandardVoices");
  }
  function getVoiceModeDescription(mode) {
    return mode === "lively" ? localizationProvider.get("VOTLivelyVoicesDescription") : localizationProvider.get("VOTStandardVoicesDescription");
  }
  function getVoiceModeMenuTitle() {
    return localizationProvider.get("VOTVoiceModeMenuTitle");
  }
  function createVoiceModeMenuItem(mode) {
    const item = UI.createEl("vot-block", ["vot-voice-mode-menu-item"]);
    UI.makeButtonLike(item, {
      ariaLabel: getVoiceModeLabel(mode)
    });
    item.dataset.mode = mode;
    const icon = UI.createEl("vot-block", ["vot-voice-mode-menu-item-icon"]);
    D(VOICE_WAVE_ICON, icon);
    const content = UI.createEl("vot-block", [
      "vot-voice-mode-menu-item-content"
    ]);
    const title = UI.createEl("vot-block", ["vot-voice-mode-menu-item-title"]);
    title.textContent = getVoiceModeLabel(mode);
    const description = UI.createEl("vot-block", [
      "vot-voice-mode-menu-item-description"
    ]);
    description.textContent = getVoiceModeDescription(mode);
    content.append(title, description);
    const check = UI.createEl("vot-block", ["vot-voice-mode-menu-item-check"]);
    D(CHECK_ICON, check);
    item.append(icon, content, check);
    return item;
  }
  function cleanupLegacyTopFrameUi() {
    const selectors = [
      `[${GOOGLE_DRIVE_TOP_FRAME_ROOT_ATTR}="1"]`,
      ".vot-google-drive-top-frame-menu",
      ".vot-voice-mode-menu",
      ".vot-voice-mode-menu",
      ".vot-dialog-container"
    ];
    for (const selector of selectors) {
      for (const node of document.querySelectorAll(selector)) {
        node.remove();
      }
    }
    settingsView = null;
    settingsData = null;
    settingsInitPromise = null;
    voiceModeMenu = null;
  }
  function createPortalRoot(attrName, zIndex) {
    const root = document.createElement("vot-block");
    root.setAttribute(attrName, "1");
    root.style.position = "fixed";
    root.style.inset = "0";
    root.style.pointerEvents = "none";
    root.style.zIndex = zIndex;
    root.style.margin = "0";
    root.style.padding = "0";
    root.style.border = "0";
    root.style.overflow = "visible";
    root.style.visibility = "visible";
    root.style.transform = "none";
    root.style.filter = "none";
    root.style.perspective = "none";
    return root;
  }
  function ensureOverlayRoot() {
    if (overlayRoot?.isConnected) {
      return overlayRoot;
    }
    overlayRoot = document.createElement("vot-block");
    overlayRoot.setAttribute(GOOGLE_DRIVE_TOP_FRAME_ROOT_ATTR, "1");
    overlayRoot.style.position = "fixed";
    overlayRoot.style.inset = "0";
    overlayRoot.style.margin = "0";
    overlayRoot.style.padding = "0";
    overlayRoot.style.border = "0";
    overlayRoot.style.background = "transparent";
    overlayRoot.style.overflow = "visible";
    overlayRoot.style.pointerEvents = "none";
    overlayRoot.style.zIndex = "2147483647";
    overlayRoot.style.isolation = "isolate";
    overlayRoot.style.visibility = "visible";
    overlayRoot.style.transform = "none";
    overlayRoot.style.filter = "none";
    overlayRoot.style.perspective = "none";
    popupPortalRoot = createPortalRoot(
      GOOGLE_DRIVE_TOP_FRAME_POPUP_ROOT_ATTR,
      DRIVE_TOP_FRAME_POPUP_LAYER_Z
    );
    dialogPortalRoot = createPortalRoot(
      GOOGLE_DRIVE_TOP_FRAME_DIALOG_ROOT_ATTR,
      DRIVE_TOP_FRAME_DIALOG_LAYER_Z
    );
    overlayRoot.append(popupPortalRoot, dialogPortalRoot);
    (document.body ?? document.documentElement).appendChild(overlayRoot);
    return overlayRoot;
  }
  function ensurePopupPortalRoot() {
    ensureOverlayRoot();
    if (!popupPortalRoot) {
      popupPortalRoot = createPortalRoot(
        GOOGLE_DRIVE_TOP_FRAME_POPUP_ROOT_ATTR,
        DRIVE_TOP_FRAME_POPUP_LAYER_Z
      );
      overlayRoot?.appendChild(popupPortalRoot);
    }
    return popupPortalRoot;
  }
  function ensureDialogPortalRoot() {
    ensureOverlayRoot();
    if (!dialogPortalRoot) {
      dialogPortalRoot = createPortalRoot(
        GOOGLE_DRIVE_TOP_FRAME_DIALOG_ROOT_ATTR,
        DRIVE_TOP_FRAME_DIALOG_LAYER_Z
      );
      overlayRoot?.appendChild(dialogPortalRoot);
    }
    return dialogPortalRoot;
  }
  function claimNextDialogLayer(minimum = 10) {
    dialogLayerCounter = Math.max(dialogLayerCounter + 1, minimum);
    return String(dialogLayerCounter);
  }
  function createPlaceholderQuickMenuState() {
    return {
      fromItems: [
        {
          label: localizationProvider.get("videoLanguage"),
          value: "auto",
          selected: true,
          disabled: false
        }
      ],
      toItems: [
        {
          label: localizationProvider.get("translationLanguage"),
          value: "ru",
          selected: true,
          disabled: false
        }
      ],
      subtitlesItems: [
        {
          label: localizationProvider.get("VOTSubtitlesDisabled"),
          value: "disabled",
          selected: true,
          disabled: false
        }
      ],
      videoVolume: 100,
      translationVolume: 100,
      translationVolumeMax: 100,
      showVideoSlider: true
    };
  }
  function getQuickMenuState() {
    return currentState?.quickMenu ?? createPlaceholderQuickMenuState();
  }
  async function ensureSettingsData() {
    if (settingsData) {
      return settingsData;
    }
    if (settingsInitPromise) {
      return settingsInitPromise;
    }
    settingsInitPromise = (async () => {
      let data = await votStorage.getValues({
        autoTranslate: false,
        autoSubtitles: false,
        dontTranslateLanguages: [calculatedResLang],
        enabledDontTranslateLanguages: true,
        enabledAutoVolume: true,
        enabledSmartDucking: true,
        autoVolume: defaultAutoVolume,
        buttonPos: "default",
        showVideoSlider: true,
        syncVolume: false,
        downloadWithName: true,
        sendNotifyOnComplete: false,
        subtitlesMaxLength: 300,
        subtitlesSmartLayout: true,
        highlightWords: true,
        subtitlesFontSize: 20,
        subtitlesFontFamily: "default-sans",
        subtitlesOpacity: 20,
        subtitlesDownloadFormat: "srt",
        responseLanguage: calculatedResLang,
        defaultVolume: 100,
        onlyBypassMediaCSP: true,
        newAudioPlayer: true,
        showPiPButton: false,
        translateAPIErrors: true,
        translationService: defaultTranslationService,
        detectService: defaultDetectService,
        translationHotkey: null,
        subtitlesHotkey: null,
        m3u8ProxyHost,
        proxyWorkerHost,
        translateProxyEnabled: 0,
        translateProxyEnabledDefault: true,
        audioBooster: false,
        useLivelyVoice: false,
        autoHideButtonDelay: defaultAutoHideDelay,
        useAudioDownload: true,
        compatVersion: "",
        account: {},
        localeHash: "",
        localeUpdatedAt: 0
      });
      if (data.compatVersion !== actualCompatVersion) {
        data = await updateConfig(data);
        await votStorage.set("compatVersion", actualCompatVersion);
      }
      settingsData = data;
      settingsInitPromise = null;
      return data;
    })().catch((error2) => {
      settingsInitPromise = null;
      throw error2;
    });
    return settingsInitPromise;
  }
  function mapBridgeItemsToSelectItems(items) {
    return items.map((item) => ({
      label: item.label,
      value: item.value,
      selected: item.selected,
      disabled: item.disabled
    }));
  }
  function getSelectedValue(items, fallback) {
    return items.find((item) => item.selected)?.value ?? fallback;
  }
  function prepareDialogForTopFrame(dialog) {
    const dialogRoot = ensureDialogPortalRoot();
    if (dialog.container.parentElement !== dialogRoot) {
      dialogRoot.appendChild(dialog.container);
    }
    dialog.container.style.position = "fixed";
    dialog.container.style.inset = "0";
    dialog.container.style.pointerEvents = "auto";
    dialog.container.style.zIndex = claimNextDialogLayer();
    dialog.container.style.overflow = "visible";
    dialog.container.style.transform = "none";
    dialog.container.style.filter = "none";
    dialog.container.style.perspective = "none";
    dialogRoot.style.pointerEvents = "auto";
    dialog.backdrop.style.pointerEvents = "auto";
    dialog.backdrop.style.background = "transparent";
    dialog.box.style.pointerEvents = "auto";
    dialog.box.style.maxHeight = "calc(100vh - 48px)";
    dialog.box.style.overflow = "visible";
    dialog.contentWrapper.style.maxHeight = "calc(100vh - 80px)";
    dialog.contentWrapper.style.overflowY = "auto";
    dialog.contentWrapper.style.overflowX = "visible";
    dialog.addEventListener("close", () => {
      if (!dialogRoot.querySelector(
        '.vot-dialog-container[aria-hidden="false"]:not([hidden])'
      )) {
        dialogRoot.style.pointerEvents = "none";
      }
    });
  }
  function bindSettingsViewEvents(view) {
    view.addEventListener("select:menuLanguage", async () => {
      globalThis.location.reload();
    }).addEventListener("click:resetSettings", async () => {
      const valuesForClear = await votStorage.list();
      await Promise.all(valuesForClear.map((key) => votStorage.delete(key)));
      await votStorage.set("compatVersion", actualCompatVersion);
      globalThis.location.reload();
    }).addEventListener("click:bugReport", () => {
      globalThis.open(`${repositoryUrl}/issues/new`, "_blank")?.focus();
    }).addEventListener("update:account", () => {
      syncBridge();
    }).addEventListener("change:useLivelyVoice", (checked) => {
      postBridgeCommand("set-use-lively-voice", { value: checked });
      syncBridge();
    }).addEventListener("change:autoTranslate", () => {
      syncBridge();
    }).addEventListener("change:autoSubtitles", () => {
      syncBridge();
    }).addEventListener("select:buttonPosition", (position2) => {
      if (settingsData) {
        settingsData.buttonPos = position2;
      }
      scheduleLayoutSync();
    }).addEventListener("change:syncVolume", (checked) => {
      postBridgeCommand("set-sync-volume", { value: checked });
      syncBridge();
    }).addEventListener("change:showVideoVolume", (checked) => {
      postBridgeCommand("set-show-video-slider", { value: checked });
      syncBridge();
    }).addEventListener("change:audioBooster", (checked) => {
      postBridgeCommand("set-audio-booster", { value: checked });
      syncBridge();
    });
  }
  async function ensureSettingsView() {
    if (settingsView?.isInitialized()) {
      return settingsView;
    }
    const data = await ensureSettingsData();
    const dialogRoot = ensureDialogPortalRoot();
    settingsView = new SettingsView({
      globalPortal: dialogRoot,
      data
    });
    settingsView.initUI();
    settingsView.initUIEvents();
    bindSettingsViewEvents(settingsView);
    const settingsSelects = [
      settingsView.dontTranslateLanguagesSelect,
      settingsView.subtitlesDownloadFormatSelect,
      settingsView.subtitlesFontFamilySelect,
      settingsView.proxyTranslationStatusSelect,
      settingsView.translationTextServiceSelect,
      settingsView.detectServiceSelect,
      settingsView.buttonPositionSelect,
      settingsView.menuLanguageSelect
    ].filter((select) => Boolean(select));
    for (const select of settingsSelects) {
      select.addEventListener("beforeOpen", (dialog) => {
        prepareDialogForTopFrame(dialog);
      });
    }
    if (settingsView.dialog) {
      prepareDialogForTopFrame(settingsView.dialog);
      settingsView.dialog.container.style.zIndex = claimNextDialogLayer(10);
    }
    return settingsView;
  }
  function ensureButton() {
    const root = ensureOverlayRoot();
    if (button) {
      if (button.container.parentElement !== root) {
        root.appendChild(button.container);
      }
      return button;
    }
    button = new VOTRail({
      position: getDriveRailPosition(),
      status: "none",
      labelHtml: getDefaultButtonText()
    });
    button.container.style.position = "fixed";
    button.container.style.pointerEvents = "auto";
    button.container.style.zIndex = DRIVE_TOP_FRAME_BUTTON_LAYER_Z;
    button.container.style.userSelect = "none";
    button.hidden = false;
    button.opacity = 1;
    button.showPiPButton(true);
    button.showSubtitlesButton(true);
    button.setButtonLabels({
      subtitles: localizationProvider.get("VOTSubtitles"),
      pip: "Picture in picture",
      menu: localizationProvider.get("VOTSettings")
    });
    button.menuButton.hidden = false;
    button.menuButton.setAttribute("aria-haspopup", "dialog");
    button.menuButton.setAttribute("aria-expanded", "false");
    button.translateButton.removeAttribute("aria-haspopup");
    button.translateButton.removeAttribute("aria-expanded");
    root.appendChild(button.container);
    debugDriveTopFrame("Drive top-frame controller mounted");
    const handleTranslate = (event) => {
      event?.preventDefault?.();
      event?.stopPropagation?.();
      const status = normalizeStatus(currentState);
      if (status === "success") {
        postBridgeMessage({
          [GOOGLE_DRIVE_BRIDGE_FLAG]: true,
          kind: "hello"
        });
        postBridgeCommand("translate");
        debugDriveTopFrame("Drive translate button activated");
        return;
      }
      if (status === "loading") {
        return;
      }
      setQuickMenuOpen(false);
      setVoiceModeMenuOpen(voiceModeMenu?.hidden ?? true);
    };
    button.translateButton.addEventListener("click", handleTranslate);
    button.translateButton.addEventListener("keydown", (event) => {
      if (event instanceof KeyboardEvent && (event.key === "Enter" || event.key === " ")) {
        handleTranslate(event);
      }
    });
    const handleVoiceModeToggle = (event) => {
      event?.preventDefault?.();
      event?.stopPropagation?.();
      setQuickMenuOpen(false);
      setVoiceModeMenuOpen(voiceModeMenu?.hidden ?? true);
    };
    button.translateChevron.addEventListener("click", handleVoiceModeToggle);
    button.translateChevron.addEventListener("keydown", (event) => {
      if (event instanceof KeyboardEvent && (event.key === "Enter" || event.key === " ")) {
        handleVoiceModeToggle(event);
      }
    });
    button.menuButton.addEventListener("click", (event) => {
      event.preventDefault();
      event.stopPropagation();
      setQuickMenuOpen(quickMenu?.hidden ?? true);
    });
    const handleOpenSubtitles = (event) => {
      event?.preventDefault?.();
      event?.stopPropagation?.();
      ensureQuickMenu();
      postBridgeCommand("request-state");
      queueMicrotask(() => subtitlesSelect?.outer.click());
    };
    button.subtitlesButton.addEventListener("click", handleOpenSubtitles);
    button.subtitlesButton.addEventListener("keydown", (event) => {
      if (event instanceof KeyboardEvent && (event.key === "Enter" || event.key === " ")) {
        handleOpenSubtitles(event);
      }
    });
    const handleOpenPiP = (event) => {
      event?.preventDefault?.();
      event?.stopPropagation?.();
      postBridgeCommand("pip");
    };
    button.pipButton.addEventListener("click", handleOpenPiP);
    button.pipButton.addEventListener("keydown", (event) => {
      if (event instanceof KeyboardEvent && (event.key === "Enter" || event.key === " ")) {
        handleOpenPiP(event);
      }
    });
    button.menuButton.addEventListener("keydown", (event) => {
      if (event instanceof KeyboardEvent && (event.key === "Enter" || event.key === " ")) {
        event.preventDefault();
        setQuickMenuOpen(quickMenu?.hidden ?? true);
      }
    });
    return button;
  }
  function ensureQuickMenu() {
    if (quickMenu) {
      if (quickMenu.container.parentElement !== ensurePopupPortalRoot()) {
        ensurePopupPortalRoot().appendChild(quickMenu.container);
      }
      return quickMenu;
    }
    const popupRoot = ensurePopupPortalRoot();
    const dialogRoot = ensureDialogPortalRoot();
    quickMenu = new VOTMenu({
      titleHtml: localizationProvider.get("VOTSettings"),
      position: "default"
    });
    quickMenu.container.classList.add("vot-google-drive-top-frame-menu");
    quickMenu.container.style.position = "fixed";
    quickMenu.container.style.left = "0";
    quickMenu.container.style.top = "0";
    quickMenu.container.style.zIndex = DRIVE_TOP_FRAME_POPUP_LAYER_Z;
    quickMenu.container.style.pointerEvents = "auto";
    quickMenu.container.style.width = "min(420px, calc(100vw - 24px))";
    quickMenu.container.style.maxHeight = "calc(100vh - 24px)";
    quickMenu.container.style.overflow = "visible";
    quickMenu.contentWrapper.style.maxHeight = "calc(100vh - 80px)";
    quickMenu.contentWrapper.style.overflowY = "auto";
    quickMenu.contentWrapper.style.overflowX = "visible";
    quickMenu.hidden = true;
    for (const eventName of ["click", "pointerdown", "mousedown"]) {
      quickMenu.container.addEventListener(eventName, (event) => {
        event.stopPropagation();
      });
    }
    quickMenu.container.addEventListener("keydown", (event) => {
      if (event.key !== "Escape") {
        return;
      }
      event.preventDefault();
      event.stopPropagation();
      setQuickMenuOpen(false);
      queueMicrotask(() => button?.menuButton.focus?.());
    });
    downloadTranslationButton = new DownloadButton();
    downloadTranslationButton.hidden = true;
    downloadTranslationButton.addEventListener("click", () => {
      postBridgeCommand("download-translation");
    });
    downloadSubtitlesButton = UI.createIconButton(SUBTITLES_ICON, {
      ariaLabel: "Download subtitles"
    });
    downloadSubtitlesButton.hidden = true;
    downloadSubtitlesButton.addEventListener("click", () => {
      postBridgeCommand("download-subtitles");
    });
    openSettingsButton = UI.createIconButton(SETTINGS_ICON, {
      ariaLabel: localizationProvider.get("VOTSettings")
    });
    openSettingsButton.addEventListener("click", async () => {
      setQuickMenuOpen(false);
      const view = await ensureSettingsView();
      view.open();
    });
    quickMenu.headerContainer.append(
      downloadTranslationButton.button,
      downloadSubtitlesButton,
      openSettingsButton
    );
    const state = getQuickMenuState();
    languagePairSelect = new LanguagePairSelect({
      from: {
        selectTitle: localizationProvider.get("videoLanguage"),
        dialogTitle: localizationProvider.get("videoLanguage"),
        items: mapBridgeItemsToSelectItems(state.fromItems)
      },
      to: {
        selectTitle: localizationProvider.get("translationLanguage"),
        dialogTitle: localizationProvider.get("translationLanguage"),
        items: mapBridgeItemsToSelectItems(state.toItems)
      },
      dialogParent: dialogRoot
    });
    languagePairSelect.fromSelect.addEventListener("beforeOpen", (dialog) => {
      prepareDialogForTopFrame(dialog);
    });
    languagePairSelect.toSelect.addEventListener("beforeOpen", (dialog) => {
      prepareDialogForTopFrame(dialog);
    });
    languagePairSelect.fromSelect.addEventListener("selectItem", (value) => {
      debugDriveTopFrame("Drive picker option pointerdown", {
        type: "fromLanguage",
        value
      });
      postBridgeCommand("set-from-language", { value });
    });
    languagePairSelect.toSelect.addEventListener("selectItem", (value) => {
      debugDriveTopFrame("Drive picker option pointerdown", {
        type: "toLanguage",
        value
      });
      postBridgeCommand("set-to-language", { value });
    });
    subtitlesSelectLabel = new Label({
      labelText: localizationProvider.get("VOTSubtitles")
    });
    subtitlesSelect = new Select({
      selectTitle: localizationProvider.get("VOTSubtitlesDisabled"),
      dialogTitle: localizationProvider.get("VOTSubtitles"),
      labelElement: subtitlesSelectLabel.container,
      dialogParent: dialogRoot,
      items: mapBridgeItemsToSelectItems(state.subtitlesItems)
    });
    subtitlesSelect.addEventListener("beforeOpen", (dialog) => {
      prepareDialogForTopFrame(dialog);
    });
    subtitlesSelect.addEventListener("selectItem", (value) => {
      debugDriveTopFrame("Drive picker option pointerdown", {
        type: "subtitles",
        value
      });
      postBridgeCommand("set-subtitles", { value });
    });
    videoVolumeSliderLabel = new SliderLabel({
      labelText: localizationProvider.get("VOTVolume"),
      value: state.videoVolume
    });
    videoVolumeSlider = new Slider({
      labelHtml: videoVolumeSliderLabel.container,
      value: state.videoVolume
    });
    videoVolumeSlider.hidden = !state.showVideoSlider;
    videoVolumeSlider.addEventListener("input", (value, fromSetter) => {
      videoVolumeSliderLabel.value = value;
      if (!fromSetter) {
        postBridgeCommand("set-video-volume", { value });
      }
    });
    translationVolumeSliderLabel = new SliderLabel({
      labelText: localizationProvider.get("VOTVolumeTranslation"),
      value: state.translationVolume
    });
    translationVolumeSlider = new Slider({
      labelHtml: translationVolumeSliderLabel.container,
      value: state.translationVolume,
      max: state.translationVolumeMax
    });
    translationVolumeSlider.addEventListener("input", (value, fromSetter) => {
      translationVolumeSliderLabel.value = value;
      if (!fromSetter) {
        postBridgeCommand("set-translation-volume", { value });
      }
    });
    quickMenu.bodyContainer.style.display = "flex";
    quickMenu.bodyContainer.style.flexDirection = "column";
    quickMenu.bodyContainer.style.gap = "16px";
    quickMenu.bodyContainer.append(
      languagePairSelect.container,
      subtitlesSelect.container,
      videoVolumeSlider.container,
      translationVolumeSlider.container
    );
    popupRoot.appendChild(quickMenu.container);
    ensureButton().menuButton.setAttribute(
      "aria-controls",
      quickMenu.container.id
    );
    return quickMenu;
  }
  function ensureVoiceModeMenu() {
    if (voiceModeMenu) {
      if (voiceModeMenu.container.parentElement !== ensurePopupPortalRoot()) {
        ensurePopupPortalRoot().appendChild(voiceModeMenu.container);
      }
      return voiceModeMenu;
    }
    const popupRoot = ensurePopupPortalRoot();
    voiceModeMenu = new VOTMenu({
      titleHtml: getVoiceModeMenuTitle(),
      position: "default"
    });
    voiceModeMenu.container.classList.add("vot-voice-mode-menu");
    voiceModeMenu.container.style.position = "fixed";
    voiceModeMenu.container.style.left = "0";
    voiceModeMenu.container.style.top = "0";
    voiceModeMenu.container.style.zIndex = DRIVE_TOP_FRAME_POPUP_LAYER_Z;
    voiceModeMenu.container.style.pointerEvents = "auto";
    voiceModeMenu.container.style.width = "min(360px, calc(100vw - 24px))";
    voiceModeMenu.container.style.maxHeight = "calc(100vh - 24px)";
    voiceModeMenu.container.style.overflow = "visible";
    voiceModeMenu.contentWrapper.style.maxHeight = "calc(100vh - 80px)";
    voiceModeMenu.contentWrapper.style.overflowY = "auto";
    voiceModeMenu.contentWrapper.style.overflowX = "visible";
    voiceModeMenu.hidden = true;
    for (const eventName of ["click", "pointerdown", "mousedown"]) {
      voiceModeMenu.container.addEventListener(eventName, (event) => {
        event.stopPropagation();
      });
    }
    voiceModeMenu.container.addEventListener("keydown", (event) => {
      if (event.key !== "Escape") {
        return;
      }
      event.preventDefault();
      event.stopPropagation();
      setVoiceModeMenuOpen(false);
      queueMicrotask(() => button?.translateButton.focus?.());
    });
    const standardItem = createVoiceModeMenuItem("standard");
    const livelyItem = createVoiceModeMenuItem("lively");
    const handleSelectMode = async (mode) => {
      const livelyEnabled = mode === "lively";
      const nextSettingsData = await ensureSettingsData();
      const previousConfiguredMode = getConfiguredVoiceMode();
      const previousActiveMode = currentState?.voiceMode === "lively" || currentState?.voiceMode === "standard" ? currentState.voiceMode : previousConfiguredMode;
      if (livelyEnabled && !nextSettingsData.account?.token) {
        const view = await ensureSettingsView();
        const authUrl = await createPkceAuthUrl();
        globalThis.open(
          authUrl,
          "yandex-oauth",
          "popup=yes,width=520,height=720,resizable=yes,scrollbars=yes"
        )?.focus();
        view.open();
        setVoiceModeMenuOpen(false);
        return;
      }
      const previousStatus = normalizeStatus(currentState);
      nextSettingsData.useLivelyVoice = livelyEnabled;
      await votStorage.set("useLivelyVoice", livelyEnabled);
      const checkbox = settingsView?.useLivelyVoiceCheckbox;
      if (checkbox) {
        if (checkbox.checked !== livelyEnabled) {
          checkbox.checked = livelyEnabled;
        }
      }
      if (previousConfiguredMode !== mode || previousActiveMode !== mode) {
        postBridgeCommand("set-use-lively-voice", {
          value: livelyEnabled
        });
      }
      currentState = {
        ...currentState ?? {
          status: "none",
          loading: false,
          voicePlaybackState: "idle",
          labelText: "",
          canDownloadTranslation: false,
          canDownloadSubtitles: false,
          quickMenu: null
        },
        voiceMode: mode
      };
      applyBridgeState(currentState);
      setVoiceModeMenuOpen(false);
      if (previousStatus !== "success" && previousStatus !== "loading") {
        postBridgeCommand("translate");
      }
    };
    const bindVoiceModeItem = (item, mode) => {
      item.addEventListener("pointerdown", (event) => {
        event.preventDefault();
        event.stopPropagation();
        void handleSelectMode(mode);
      });
      item.addEventListener("click", (event) => {
        if (!(event instanceof MouseEvent) || event.detail !== 0) {
          return;
        }
        event.preventDefault();
        event.stopPropagation();
        void handleSelectMode(mode);
      });
    };
    bindVoiceModeItem(standardItem, "standard");
    bindVoiceModeItem(livelyItem, "lively");
    voiceModeMenu.bodyContainer.style.display = "flex";
    voiceModeMenu.bodyContainer.style.flexDirection = "column";
    voiceModeMenu.bodyContainer.style.gap = "0";
    voiceModeMenu.bodyContainer.append(standardItem, livelyItem);
    popupRoot.appendChild(voiceModeMenu.container);
    ensureButton().translateChevron.setAttribute(
      "aria-controls",
      voiceModeMenu.container.id
    );
    return voiceModeMenu;
  }
  function positionQuickMenu() {
    if (!quickMenu || !button) {
      return;
    }
    const anchorRect = button.menuButton.getBoundingClientRect();
    const groupRect = button.container.getBoundingClientRect();
    const playerRect = getPlayerRect();
    const menuWidth = quickMenu.container.offsetWidth || 420;
    const menuHeight = quickMenu.container.offsetHeight || 520;
    const isVertical = button.position === "left" || button.position === "right";
    const boundsLeft = Math.max(12, (playerRect?.left ?? 0) + 12);
    const boundsTop = Math.max(12, (playerRect?.top ?? 0) + 12);
    const boundsRight = Math.min(
      globalThis.innerWidth - 12,
      (playerRect?.right ?? globalThis.innerWidth) - 12
    );
    const boundsBottom = Math.min(
      globalThis.innerHeight - 12,
      (playerRect?.bottom ?? globalThis.innerHeight) - 12
    );
    const desiredLeft = isVertical ? button.position === "right" ? anchorRect.left - menuWidth - 12 : anchorRect.right + 12 : anchorRect.right - menuWidth;
    const left = Math.max(
      boundsLeft,
      Math.min(Math.max(boundsLeft, boundsRight - menuWidth), desiredLeft)
    );
    const desiredTop = isVertical ? anchorRect.top : groupRect.bottom + 12;
    const fallbackTop = isVertical ? Math.max(boundsTop, boundsBottom - menuHeight) : groupRect.top - menuHeight - 12;
    const top = Math.max(
      boundsTop,
      Math.min(
        Math.max(boundsTop, boundsBottom - menuHeight),
        desiredTop + menuHeight <= boundsBottom ? desiredTop : Math.max(boundsTop, fallbackTop)
      )
    );
    quickMenu.container.style.left = `${left}px`;
    quickMenu.container.style.top = `${top}px`;
  }
  function positionVoiceModeMenu() {
    if (!voiceModeMenu || !button) {
      return;
    }
    const anchorRect = button.translateGroup.getBoundingClientRect();
    const playerRect = getPlayerRect();
    const menuWidth = voiceModeMenu.container.offsetWidth || 360;
    const menuHeight = voiceModeMenu.container.offsetHeight || 240;
    const isVertical = button.position === "left" || button.position === "right";
    const boundsLeft = Math.max(12, (playerRect?.left ?? 0) + 12);
    const boundsTop = Math.max(12, (playerRect?.top ?? 0) + 12);
    const boundsRight = Math.min(
      globalThis.innerWidth - 12,
      (playerRect?.right ?? globalThis.innerWidth) - 12
    );
    const boundsBottom = Math.min(
      globalThis.innerHeight - 12,
      (playerRect?.bottom ?? globalThis.innerHeight) - 12
    );
    const desiredLeft = isVertical ? button.position === "right" ? anchorRect.left - menuWidth - 12 : anchorRect.right + 12 : anchorRect.left;
    const left = Math.max(
      boundsLeft,
      Math.min(Math.max(boundsLeft, boundsRight - menuWidth), desiredLeft)
    );
    const desiredTop = isVertical ? anchorRect.top : anchorRect.bottom + 12;
    const fallbackTop = anchorRect.top - menuHeight - 12;
    const top = Math.max(
      boundsTop,
      Math.min(
        Math.max(boundsTop, boundsBottom - menuHeight),
        desiredTop + menuHeight <= boundsBottom ? desiredTop : Math.max(boundsTop, fallbackTop)
      )
    );
    voiceModeMenu.container.style.left = `${left}px`;
    voiceModeMenu.container.style.top = `${top}px`;
  }
  function setQuickMenuOpen(open) {
    const menu = ensureQuickMenu();
    const currentButton = ensureButton();
    if (open) {
      if (voiceModeMenu && !voiceModeMenu.hidden) {
        voiceModeMenu.hidden = true;
        currentButton.translateChevron.setAttribute("aria-expanded", "false");
      }
      menu.hidden = false;
      currentButton.menuButton.setAttribute("aria-expanded", "true");
      debugDriveTopFrame("Drive picker opened", { type: "quickMenu" });
      positionQuickMenu();
      postBridgeCommand("request-state");
      return;
    }
    menu.hidden = true;
    currentButton.menuButton.setAttribute("aria-expanded", "false");
  }
  function syncVoiceModeMenuState() {
    if (!voiceModeMenu) {
      return;
    }
    const status = normalizeStatus(currentState);
    const selectedMode = getSelectedVoiceMode(currentState);
    const selectedPlaybackState = status === "success" ? currentState?.voicePlaybackState || "idle" : "idle";
    for (const item of voiceModeMenu.bodyContainer.querySelectorAll(
      ".vot-voice-mode-menu-item"
    )) {
      const mode = item.dataset.mode === "lively" ? "lively" : "standard";
      const isSelected = item.dataset.mode === selectedMode;
      const title = item.querySelector(
        ".vot-voice-mode-menu-item-title"
      );
      const etaLabel = getVoiceModeEtaLabel(mode);
      item.dataset.selected = String(isSelected);
      item.setAttribute("aria-pressed", String(isSelected));
      item.dataset.playbackState = isSelected ? selectedPlaybackState : "idle";
      item.dataset.loading = String(isSelected && status === "loading");
      if (title) {
        title.textContent = etaLabel ? `${getVoiceModeLabel(mode)} ${etaLabel}` : getVoiceModeLabel(mode);
      }
    }
  }
  function setVoiceModeMenuOpen(open) {
    const menu = ensureVoiceModeMenu();
    const currentButton = ensureButton();
    if (open) {
      setQuickMenuOpen(false);
      syncVoiceModeMenuState();
      menu.hidden = false;
      currentButton.translateChevron.setAttribute("aria-expanded", "true");
      debugDriveTopFrame("Drive picker opened", { type: "voiceMode" });
      positionVoiceModeMenu();
      postBridgeCommand("request-state");
      return;
    }
    menu.hidden = true;
    currentButton.translateChevron.setAttribute("aria-expanded", "false");
  }
  function applyQuickMenuState(state) {
    if (!languagePairSelect || !subtitlesSelect || !videoVolumeSlider || !translationVolumeSlider || !videoVolumeSliderLabel || !translationVolumeSliderLabel) {
      return;
    }
    const menuState = state ?? createPlaceholderQuickMenuState();
    const fromItems = mapBridgeItemsToSelectItems(menuState.fromItems);
    const toItems = mapBridgeItemsToSelectItems(menuState.toItems);
    const subtitlesItems = mapBridgeItemsToSelectItems(menuState.subtitlesItems);
    languagePairSelect.fromSelect.updateItems(fromItems);
    languagePairSelect.toSelect.updateItems(toItems);
    languagePairSelect.setSelectedValues(
      getSelectedValue(menuState.fromItems, "auto"),
      getSelectedValue(menuState.toItems, "ru")
    );
    subtitlesSelect.updateItems(subtitlesItems);
    subtitlesSelect.setSelectedValue(
      getSelectedValue(menuState.subtitlesItems, "disabled")
    );
    videoVolumeSlider.hidden = !menuState.showVideoSlider;
    videoVolumeSlider.value = menuState.videoVolume;
    videoVolumeSliderLabel.value = menuState.videoVolume;
    translationVolumeSlider.max = menuState.translationVolumeMax;
    translationVolumeSlider.value = menuState.translationVolume;
    translationVolumeSliderLabel.value = menuState.translationVolume;
    if (quickMenu && !quickMenu.hidden) {
      queueMicrotask(() => positionQuickMenu());
    }
  }
  function applyBridgeState(state) {
    currentState = state;
    const root = ensureOverlayRoot();
    const currentButton = ensureButton();
    const nextStatus = normalizeStatus(state);
    const displayedVoiceMode = getDisplayedVoiceMode(state);
    root.hidden = false;
    currentButton.hidden = false;
    currentButton.opacity = 1;
    currentButton.status = nextStatus;
    currentButton.loading = nextStatus === "loading";
    currentButton.setText(getButtonText(state));
    currentButton.container.dataset.voiceMode = displayedVoiceMode;
    currentButton.container.dataset.voicePlaybackState = state.voicePlaybackState || "idle";
    currentButton.showPiPButton(state.pipVisible ?? true);
    if (downloadTranslationButton) {
      downloadTranslationButton.hidden = !state.canDownloadTranslation;
    }
    if (downloadSubtitlesButton) {
      downloadSubtitlesButton.hidden = !state.canDownloadSubtitles;
    }
    applyQuickMenuState(state.quickMenu);
    syncVoiceModeMenuState();
  }
  function positionButton(rect) {
    if (!button) {
      return;
    }
    const buttonWidth = button.container.offsetWidth || 58;
    const buttonHeight = button.container.offsetHeight || 220;
    const isVertical = button.position === "left" || button.position === "right";
    const left = isVertical ? button.position === "right" ? Math.max(
      12,
      Math.min(
        globalThis.innerWidth - buttonWidth - 12,
        rect.right - buttonWidth - 16
      )
    ) : Math.max(
      12,
      Math.min(globalThis.innerWidth - buttonWidth - 12, rect.left + 16)
    ) : Math.max(
      12,
      Math.min(
        globalThis.innerWidth - buttonWidth - 12,
        rect.right - buttonWidth - 16
      )
    );
    const top = isVertical ? Math.max(
      12,
      Math.min(
        globalThis.innerHeight - buttonHeight - 12,
        rect.top + (rect.height - buttonHeight) / 2
      )
    ) : Math.max(
      12,
      Math.min(globalThis.innerHeight - buttonHeight - 12, rect.top + 16)
    );
    button.container.style.left = `${left}px`;
    button.container.style.top = `${top}px`;
  }
  function syncOverlayGeometry() {
    const root = ensureOverlayRoot();
    const rect = getPlayerRect();
    if (!rect || rect.width < 120 || rect.height < 80) {
      root.hidden = false;
      if (button) {
        button.hidden = true;
      }
      if (quickMenu && !quickMenu.hidden) {
        setQuickMenuOpen(false);
      }
      return;
    }
    root.hidden = false;
    const currentButton = ensureButton();
    currentButton.hidden = false;
    currentButton.position = getDriveRailPosition();
    currentButton.opacity = 1;
    positionButton(rect);
    if (currentState) {
      applyBridgeState(currentState);
    } else {
      currentButton.status = "none";
      currentButton.loading = false;
      currentButton.setText(getDefaultButtonText());
    }
    if (quickMenu && !quickMenu.hidden) {
      positionQuickMenu();
    }
    if (voiceModeMenu && !voiceModeMenu.hidden) {
      positionVoiceModeMenu();
    }
  }
  function scheduleLayoutSync() {
    if (layoutSyncScheduled) {
      return;
    }
    layoutSyncScheduled = true;
    globalThis.requestAnimationFrame(() => {
      layoutSyncScheduled = false;
      syncOverlayGeometry();
      observePlayerIframe();
    });
  }
  function syncBridge() {
    postBridgeMessage({
      [GOOGLE_DRIVE_BRIDGE_FLAG]: true,
      kind: "hello"
    });
    postBridgeCommand("request-state");
  }
  function ensureBridgePolling() {
    if (bridgePollTimer !== null) {
      return;
    }
    bridgePollTimer = globalThis.setInterval(() => {
      if (document.visibilityState === "hidden") {
        return;
      }
      if (!getDrivePlayerIframe()) {
        return;
      }
      syncBridge();
    }, 1500);
  }
  function observePlayerIframe() {
    const nextIframe = getDrivePlayerIframe();
    if (observedIframe === nextIframe) {
      return;
    }
    iframeResizeObserver?.disconnect();
    iframeResizeObserver = null;
    observedIframe = nextIframe;
    if (!nextIframe || typeof ResizeObserver === "undefined") {
      return;
    }
    iframeResizeObserver = new ResizeObserver(() => {
      scheduleLayoutSync();
    });
    iframeResizeObserver.observe(nextIframe);
    syncBridge();
  }
  function handleBridgeMessage(event) {
    const data = event.data;
    if (!data || typeof data !== "object" || !(data[GOOGLE_DRIVE_BRIDGE_FLAG] === true)) {
      return;
    }
    const message = data;
    if (message.kind !== "state") {
      return;
    }
    debugDriveTopFrame("Drive top-frame state received", {
      status: message.state.status,
      loading: message.state.loading,
      labelText: message.state.labelText
    });
    applyBridgeState(message.state);
    scheduleLayoutSync();
  }
  function isEventInsideActiveUi(target) {
    const node = target;
    if (!node) {
      return false;
    }
    return Boolean(
      button?.container.contains(node) || voiceModeMenu?.container.contains(node) || quickMenu?.container.contains(node) || ensureDialogPortalRoot().contains(node)
    );
  }
  function installGoogleDriveTopFramePatch() {
    if (installed$2 || !isGoogleDriveTopFrameContext()) {
      return;
    }
    installed$2 = true;
    cleanupLegacyTopFrameUi();
    ensureOverlayRoot();
    ensureButton();
    ensureQuickMenu();
    ensureVoiceModeMenu();
    void ensureSettingsData().then(() => scheduleLayoutSync());
    scheduleLayoutSync();
    syncBridge();
    ensureBridgePolling();
    globalThis.addEventListener("message", handleBridgeMessage);
    globalThis.addEventListener("resize", scheduleLayoutSync, { passive: true });
    globalThis.addEventListener("scroll", scheduleLayoutSync, {
      passive: true,
      capture: true
    });
    document.addEventListener("fullscreenchange", scheduleLayoutSync, true);
    document.addEventListener("webkitfullscreenchange", scheduleLayoutSync, true);
    document.addEventListener("visibilitychange", scheduleLayoutSync, true);
    document.addEventListener(
      "pointermove",
      () => {
        scheduleLayoutSync();
      },
      { passive: true, capture: true }
    );
    document.addEventListener(
      "pointerdown",
      (event) => {
        if (!isEventInsideActiveUi(event.target)) {
          if (quickMenu && !quickMenu.hidden) {
            setQuickMenuOpen(false);
          }
          if (voiceModeMenu && !voiceModeMenu.hidden) {
            setVoiceModeMenuOpen(false);
          }
        }
      },
      { capture: true, passive: true }
    );
    domObserver = new MutationObserver(() => {
      scheduleLayoutSync();
    });
    domObserver.observe(document.documentElement, {
      childList: true,
      subtree: true,
      attributes: true,
      attributeFilter: ["class", "style", "hidden", "src"]
    });
  }
  function isVkLikeHost() {
    return /(?:^|\.)vkvideo\.ru$|(?:^|\.)vk\.(?:com|ru)$/i.test(
      String(globalThis.location?.hostname || "")
    );
  }
  let vkOverlayPatchInstalled = false;
  const VK_OVERLAY_BUTTON_SELECTOR = "vot-block.vot-segmented-button, .vot-segmented-button, vot-block.vot-rail, .vot-rail";
  const VK_OVERLAY_SUBTITLE_SELECTOR = ".vot-subtitles-widget, .vot-subtitles-layer";
  function adaptStylesForShadowRoot(cssText) {
    return cssText.replace(/(^|\n):root\s*\{/g, "$1:host, :root {");
  }
  const VK_OVERLAY_PATCH_TEXT = `
  .vot-segmented-button,
  .vot-rail {
    display: flex !important;
    opacity: 1 !important;
    pointer-events: auto !important;
    z-index: 2147483647 !important;
    visibility: visible !important;
  }

  .vot-segmented-button.vot-segmented-button--hidden,
  .vot-rail.vot-rail--hidden {
    opacity: 1 !important;
    pointer-events: auto !important;
  }

  .vot-subtitles-layer,
  .vot-subtitles-widget {
    display: block !important;
    opacity: 1 !important;
    z-index: 2147483647 !important;
    visibility: visible !important;
  }
`;
  const VK_OVERLAY_SHADOW_STYLE_TEXT = `${adaptStylesForShadowRoot(mainStylesInline)}

${VK_OVERLAY_PATCH_TEXT}`;
  function collectQueryScopes() {
    const scopes = [document];
    const seen = new Set([document]);
    const queue = [document];
    while (queue.length > 0) {
      const current = queue.shift();
      if (!current) {
        continue;
      }
      const elements = current instanceof Document || current instanceof ShadowRoot ? current.querySelectorAll("*") : [];
      for (const element of elements) {
        const shadowRoot = element.shadowRoot;
        if (!shadowRoot || seen.has(shadowRoot)) {
          continue;
        }
        seen.add(shadowRoot);
        scopes.push(shadowRoot);
        queue.push(shadowRoot);
      }
    }
    return scopes;
  }
  function queryAllAcrossShadowRoots(selector) {
    const result = [];
    const seen = new Set();
    for (const scope of collectQueryScopes()) {
      for (const element of Array.from(scope.querySelectorAll(selector))) {
        if (seen.has(element)) {
          continue;
        }
        seen.add(element);
        result.push(element);
      }
    }
    return result;
  }
  function refreshVkOverlayProbe() {
    const buttons = queryAllAcrossShadowRoots(
      VK_OVERLAY_BUTTON_SELECTOR
    );
    for (const button2 of buttons) {
      button2.hidden = false;
      button2.removeAttribute("hidden");
      button2.removeAttribute("inert");
      button2.classList.remove("vot-segmented-button--hidden");
      button2.classList.remove("vot-rail--hidden");
      button2.style.setProperty("display", "flex", "important");
      button2.style.setProperty("opacity", "1", "important");
      button2.style.setProperty("pointer-events", "auto", "important");
      button2.style.setProperty("z-index", "2147483647", "important");
      button2.style.setProperty("visibility", "visible", "important");
    }
    const subtitles = queryAllAcrossShadowRoots(
      VK_OVERLAY_SUBTITLE_SELECTOR
    );
    for (const subtitle of subtitles) {
      subtitle.hidden = false;
      subtitle.removeAttribute("hidden");
      subtitle.style.setProperty("display", "block", "important");
      subtitle.style.setProperty("opacity", "1", "important");
      subtitle.style.setProperty("pointer-events", "none", "important");
      subtitle.style.setProperty("z-index", "2147483647", "important");
      subtitle.style.setProperty("visibility", "visible", "important");
    }
    const videos = queryAllAcrossShadowRoots("video");
    const probeState = {
      buttons: buttons.length,
      subtitlesWidgets: subtitles.length,
      videos: videos.length,
      visibleVideos: videos.filter((video) => {
        const rect = video.getBoundingClientRect();
        return rect.width > 64 && rect.height > 64;
      }).length,
      href: globalThis.location.href
    };
    globalThis.__VOT_VK_PROBE__ = probeState;
    return buttons.length + subtitles.length;
  }
  function ensureVkOverlayStyle() {
    for (const scope of collectQueryScopes()) {
      const styleHost = scope instanceof Document ? scope.head || scope.documentElement : scope instanceof ShadowRoot ? scope : null;
      if (!styleHost) {
        continue;
      }
      const root = scope instanceof Document ? scope : scope instanceof ShadowRoot ? scope : null;
      if (!root) {
        continue;
      }
      if (root.querySelector("#vot-vk-overlay-fix-style")) {
        continue;
      }
      try {
        const style = document.createElement("style");
        style.id = "vot-vk-overlay-fix-style";
        style.textContent = scope instanceof ShadowRoot ? VK_OVERLAY_SHADOW_STYLE_TEXT : VK_OVERLAY_PATCH_TEXT;
        styleHost.appendChild(style);
      } catch {
      }
    }
  }
  function installVkOverlayPatch() {
    if (!isVkLikeHost()) {
      return;
    }
    ensureVkOverlayStyle();
    if (vkOverlayPatchInstalled) {
      return;
    }
    vkOverlayPatchInstalled = true;
    try {
      const logProbeState = (message) => {
        console.log(
          message,
          globalThis.__VOT_VK_PROBE__
        );
      };
      let lastProbeSignature = "";
      const initialCount = refreshVkOverlayProbe();
      console.log(
        initialCount > 0 ? "[VOT][VK probe] overlay nodes detected" : "[VOT][VK probe] no VOT overlay nodes yet",
        globalThis.__VOT_VK_PROBE__
      );
      const refreshAndLog = () => {
        ensureVkOverlayStyle();
        const count = refreshVkOverlayProbe();
        const currentProbe = globalThis.__VOT_VK_PROBE__;
        const signature = JSON.stringify(currentProbe);
        if (count > 0 && signature !== lastProbeSignature) {
          lastProbeSignature = signature;
          logProbeState("[VOT][VK probe] standard overlay refresh");
        }
      };
      let ticks = 0;
      const intervalId = globalThis.setInterval(() => {
        ticks += 1;
        refreshAndLog();
        if (ticks >= 160) {
          globalThis.clearInterval(intervalId);
        }
      }, 500);
      document.addEventListener(
        "visibilitychange",
        () => {
          if (!document.hidden) {
            refreshAndLog();
          }
        },
        { passive: true }
      );
    } catch (error2) {
      console.warn("[VOT][VK probe] failed", error2);
    }
  }
  function installYandexDiskOverlayPatch() {
    let patchedHost = null;
    let hideTimer2 = null;
    const IDLE_TIME = 3e3;
    const clearHideTimer2 = () => {
      if (hideTimer2 !== null) {
        window.clearTimeout(hideTimer2);
        hideTimer2 = null;
      }
    };
    const showButton2 = () => {
      const overlay = document.querySelector(
        ".vot-segmented-button"
      );
      if (!overlay) return;
      overlay.hidden = false;
      overlay.removeAttribute("hidden");
      overlay.style.opacity = "1";
      overlay.classList.remove("vot-segmented-button--hidden");
      clearHideTimer2();
      hideTimer2 = window.setTimeout(() => {
        const currentOverlay = document.querySelector(
          ".vot-segmented-button"
        );
        if (!currentOverlay) return;
        currentOverlay.style.opacity = "0";
        currentOverlay.classList.add("vot-segmented-button--hidden");
      }, IDLE_TIME);
    };
    const patchHost = () => {
      const video = document.querySelector("video");
      if (!video) return;
      const host = video.closest('[class*="player"], [class*="video"], main, article') || video.parentElement || video;
      if (!host || host === patchedHost) return;
      patchedHost = host;
      const activityHandler = () => showButton2();
      host.addEventListener("mouseenter", activityHandler, { passive: true });
      host.addEventListener("mousemove", activityHandler, { passive: true });
      host.addEventListener("pointerenter", activityHandler, { passive: true });
      host.addEventListener("pointermove", activityHandler, { passive: true });
      showButton2();
    };
    patchHost();
    const observer = new MutationObserver(() => {
      patchHost();
    });
    observer.observe(document.documentElement, {
      childList: true,
      subtree: true
    });
  }
  const isIframe = () => globalThis.self !== globalThis.top;
  function initIframeInteractor() {
    const configs = {
      "https://dev.epicgames.com": {
        targetOrigin: "https://dev.epicgames.com",
        dataFilter: (data) => typeof data === "string" && data.startsWith("getVideoId:"),
        extractVideoId: (url) => url.pathname.split("/").at(-2) ?? null,
        responseFormatter: (videoId, data) => `${typeof data === "string" ? data : ""}:${videoId}`
      },
      "https://www.dailymotion.com": {
        targetOrigin: "https://geo.dailymotion.com",
        dataFilter: (data) => typeof data === "string" && data.startsWith("getVideoId:"),
        extractVideoId: (url) => {
          return /(?:^|\/)video\/([^/]+)/.exec(url.pathname)?.[1];
        },
        responseFormatter: (videoId) => `getVideoId:${videoId}`
      }
    };
    const currentConfig = Object.entries(configs).find(
      ([origin]) => globalThis.location.origin === origin
    )?.[1];
    if (!currentConfig) return;
    globalThis.addEventListener("message", (event) => {
      try {
        if (event.origin !== currentConfig.targetOrigin) return;
        if (!currentConfig.dataFilter(event.data)) return;
        const videoId = currentConfig.extractVideoId(
          new URL(globalThis.location.href)
        );
        if (!videoId) return;
        const response = currentConfig.responseFormatter(videoId, event.data);
        if (event.source && "postMessage" in event.source) {
          event.source.postMessage(
            response,
            currentConfig.targetOrigin
          );
        }
      } catch (error2) {
        console.error("Iframe communication error:", error2);
      }
    });
  }
  let runtimeActivated = false;
  let runtimeActivationPromise = null;
  let iframeInteractorBound = false;
  function isAuthPage() {
    return globalThis.location.origin === authCallbackOrigin && globalThis.location.pathname === authCallbackPath;
  }
  async function ensureRuntimeActivated(reason, logBootstrap2) {
    if (runtimeActivated) return;
    if (runtimeActivationPromise !== null) {
      await runtimeActivationPromise;
      return;
    }
    runtimeActivationPromise = (async () => {
      logBootstrap2("Activating runtime", { reason });
      if (isAuthPage()) {
        await initAuth();
        runtimeActivated = true;
        return;
      }
      if (!isIframe()) {
        await ensureLocalizationProviderReady();
        await localizationProvider.update();
        debug.log(`Selected menu language: ${localizationProvider.lang}`);
      } else {
        debug.log("[VOT] iframe mode: skip localization init");
      }
      if (!iframeInteractorBound) {
        iframeInteractorBound = true;
        initIframeInteractor();
      }
      function isYandexDiskHost() {
        const host = globalThis.location.hostname.toLowerCase();
        return host === "disk.yandex.ru" || host === "disk.yandex.com";
      }
      function isGoogleDriveEmbedHost() {
        const host = globalThis.location.hostname.toLowerCase();
        return host === "youtube.googleapis.com" && globalThis.location.pathname.startsWith("/embed");
      }
      function isGoogleDriveTopFrameHost() {
        const host = globalThis.location.hostname.toLowerCase();
        return !isIframe() && (host === "drive.google.com" || host === "docs.google.com");
      }
      if (isYandexDiskHost()) {
        installYandexDiskOverlayPatch();
      }
      if (isGoogleDriveEmbedHost()) {
        installGoogleDriveOverlayPatch();
      }
      if (isGoogleDriveTopFrameHost()) {
        installGoogleDriveTopFramePatch();
      }
      installVkOverlayPatch();
      runtimeActivated = true;
    })();
    try {
      await runtimeActivationPromise;
    } finally {
      runtimeActivationPromise = null;
    }
  }
  const boundObservers = new WeakSet();
  const loggedNativeSubtitleSignatures = new WeakMap();
  const MOBILE_YOUTUBE_RELEASE_GRACE_MS = 2200;
  function isVkProbeHost$2() {
    return /(?:^|\.)vkvideo\.ru$|(?:^|\.)vk\.(?:com|ru)$/i.test(
      String(globalThis.location.hostname || "")
    );
  }
  function isMobileYouTubeDebugHost$2() {
    return /^(m|music)\.youtube\.com$/i.test(
      String(globalThis.location.hostname || "")
    );
  }
  function logMobileOverlay$1(message, details) {
    if (!isMobileYouTubeDebugHost$2()) {
      return;
    }
    console.log(`[VOT][mobile-overlay][observer] ${message}`, details ?? {});
  }
  function isGracefulMobileYouTubeSite(site) {
    return String(site.host || "") === "youtube" && (site.additionalData === "mobile" || site.additionalData === "music");
  }
  function getMobileYouTubePageKey$1() {
    return `${globalThis.location.origin}${globalThis.location.pathname}${globalThis.location.search}`;
  }
  function isRenderableVideo(video) {
    if (!video.isConnected) {
      return false;
    }
    const rect = video.getBoundingClientRect();
    if (rect.width < 64 || rect.height < 64) {
      return false;
    }
    const style = globalThis.getComputedStyle(video);
    if (style.display === "none" || style.visibility === "hidden" || Number(style.opacity || "1") === 0) {
      return false;
    }
    return true;
  }
  function getVideoArea(video) {
    const rect = video.getBoundingClientRect();
    return Math.max(0, rect.width) * Math.max(0, rect.height);
  }
  function hasResolvableMediaSource$1(video) {
    if (video.currentSrc || video.src || video.srcObject) {
      return true;
    }
    const source = video.querySelector("source");
    return Boolean(source?.getAttribute("src") || source?.src);
  }
  function logNativeSubtitleTracks(video) {
    const trackEntries = Array.from(video.querySelectorAll("track")).map((track, index) => {
      const rawUrl = String(
        track.src || track.getAttribute("src") || ""
      ).trim();
      if (!rawUrl) {
        return null;
      }
      let url = rawUrl;
      try {
        url = new URL(rawUrl, document.baseURI).href;
      } catch {
      }
      return {
        index,
        kind: String(track.kind || track.getAttribute("kind") || "").trim(),
        srclang: String(
          track.srclang || track.track?.language || track.getAttribute("srclang") || ""
        ).trim(),
        label: String(track.label || track.getAttribute("label") || "").trim(),
        url
      };
    }).filter((entry) => Boolean(entry));
    const signature = trackEntries.map(
      (entry) => [entry.index, entry.kind, entry.srclang, entry.label, entry.url].join(
        "|"
      )
    ).join("||");
    if (loggedNativeSubtitleSignatures.get(video) === signature) {
      return;
    }
    loggedNativeSubtitleSignatures.set(video, signature);
    globalThis.__VOT_DETECTED_NATIVE_SUBTITLE_TRACKS__ = trackEntries;
    if (!trackEntries.length) {
      console.log(
        "[VOT][subtitles][native] no <track> subtitle URLs detected for video"
      );
      return;
    }
    console.log(
      `[VOT][subtitles][native] detected ${trackEntries.length} <track> subtitle URL(s). Inspect window.__VOT_DETECTED_NATIVE_SUBTITLE_TRACKS__.`
    );
    console.table(trackEntries);
  }
  function shouldReplaceActiveVideo(activeVideo, nextVideo) {
    if (!activeVideo.isConnected) {
      return true;
    }
    const activeArea = getVideoArea(activeVideo);
    const nextArea = getVideoArea(nextVideo);
    const activeRenderable = isRenderableVideo(activeVideo);
    const nextRenderable = isRenderableVideo(nextVideo);
    const activeHasSource = hasResolvableMediaSource$1(activeVideo);
    const nextHasSource = hasResolvableMediaSource$1(nextVideo);
    if (!activeRenderable && nextRenderable) {
      return true;
    }
    if (!activeHasSource && nextHasSource) {
      return true;
    }
    if (nextArea > activeArea * 1.1) {
      return true;
    }
    return false;
  }
  function bindObserverListeners(options) {
    const {
      videoObserver: videoObserver2,
      videosWrappers: videosWrappers2,
      ensureRuntimeActivated: ensureRuntimeActivated2,
      getServicesCached: getServicesCached2,
      findContainer: findContainer2,
      createVideoHandler
    } = options;
    if (boundObservers.has(videoObserver2)) return;
    boundObservers.add(videoObserver2);
    const initializingVideos = new WeakSet();
    const containerOwners = new WeakMap();
    const videoContainers = new WeakMap();
    const videoSites = new WeakMap();
    const pendingVideoByContainer = new WeakMap();
    const pendingGraceReleases = new Map();
    let youtubeObserverStoppedAfterPrimaryAttach = false;
    const clearContainerOwner = (video) => {
      const container = videoContainers.get(video);
      if (container && containerOwners.get(container) === video) {
        containerOwners.delete(container);
      }
      videoContainers.delete(video);
      videoSites.delete(video);
      return container ?? void 0;
    };
    const clearPendingVideo = (container) => {
      if (!container) {
        return;
      }
      pendingVideoByContainer.delete(container);
    };
    const findBestMatchingVideo = (site) => {
      let bestVideo = null;
      let bestScore = -1;
      for (const candidate of document.querySelectorAll("video")) {
        if (!(candidate instanceof HTMLVideoElement) || !candidate.isConnected) {
          continue;
        }
        if (!findContainer2(site, candidate)) {
          continue;
        }
        let score = getVideoArea(candidate);
        if (hasResolvableMediaSource$1(candidate)) {
          score += 1e7;
        }
        if (isRenderableVideo(candidate)) {
          score += 1e6;
        }
        if (score > bestScore) {
          bestVideo = candidate;
          bestScore = score;
        }
      }
      return bestVideo;
    };
    const cancelPendingGraceRelease = (video, reason) => {
      const state = pendingGraceReleases.get(video);
      if (!state) {
        return void 0;
      }
      clearTimeout(state.timer);
      pendingGraceReleases.delete(video);
      logMobileOverlay$1("cancel delayed cleanup", {
        reason,
        scheduledReason: state.reason,
        pageKey: state.pageKey
      });
      return state;
    };
    const releaseVideoHandler = async (video, reason) => {
      cancelPendingGraceRelease(video, `release:${reason}`);
      const videoHandler = videosWrappers2.get(video);
      if (!videoHandler) {
        return;
      }
      logMobileOverlay$1("release video handler", {
        reason,
        videoConnected: video.isConnected,
        src: video.currentSrc || video.src || ""
      });
      try {
        await videoHandler.release();
      } catch (error2) {
        console.error(`[VOT] Failed to release videoHandler (${reason})`, error2);
      } finally {
        videosWrappers2.delete(video);
      }
    };
    const getMatchedSiteAndContainer = (video) => {
      for (const candidate of getServicesCached2()) {
        const container = findContainer2(candidate, video);
        if (container) {
          return { site: candidate, container };
        }
      }
      return null;
    };
    const withRuntimeSiteUrl = (site) => {
      const host = String(site.host);
      return host === "peertube" || host === "directlink" ? { ...site, url: globalThis.location.origin } : site;
    };
    const shouldStopObserverAfterSuccessfulAttach = (site) => site.host === "youtube" && !site.additionalData;
    const stopYouTubeVideoDiscovery = (site) => {
      if (youtubeObserverStoppedAfterPrimaryAttach || !shouldStopObserverAfterSuccessfulAttach(site)) {
        return;
      }
      youtubeObserverStoppedAfterPrimaryAttach = true;
      console.log(
        "[VOT][observer] disabling video discovery after first successful YouTube attach",
        {
          host: globalThis.location.hostname,
          path: globalThis.location.pathname
        }
      );
      videoObserver2.disable();
    };
    const scheduleGracefulRelease = async (video, site, reason, container) => {
      cancelPendingGraceRelease(video, `reschedule:${reason}`);
      const pageKey = getMobileYouTubePageKey$1();
      logMobileOverlay$1("schedule delayed cleanup", {
        reason,
        pageKey,
        delayMs: MOBILE_YOUTUBE_RELEASE_GRACE_MS,
        videoConnected: video.isConnected,
        hasSource: hasResolvableMediaSource$1(video)
      });
      const timer = globalThis.setTimeout(async () => {
        const currentState2 = pendingGraceReleases.get(video);
        if (!currentState2 || currentState2.timer !== timer) {
          return;
        }
        pendingGraceReleases.delete(video);
        if (video.isConnected) {
          logMobileOverlay$1("skip delayed cleanup: video reconnected", {
            reason,
            pageKey
          });
          const match = getMatchedSiteAndContainer(video);
          if (match) {
            videoContainers.set(video, match.container);
            containerOwners.set(match.container, video);
          }
          try {
            await videosWrappers2.get(video)?.setCanPlay();
          } catch (error2) {
            console.error(
              "[VOT] Failed to restore reconnected mobile YouTube video",
              error2
            );
          }
          return;
        }
        const replacementVideo = findBestMatchingVideo(site);
        if (replacementVideo && replacementVideo !== video) {
          logMobileOverlay$1(
            "cleanup stale handler after replacement video found",
            {
              reason,
              pageKey,
              replacementSrc: replacementVideo.currentSrc || replacementVideo.src || ""
            }
          );
          await releaseVideoHandler(video, `${reason}:replacement-video-found`);
          await promotePendingVideo(container);
          return;
        }
        const currentPageKey = getMobileYouTubePageKey$1();
        if (currentPageKey === pageKey) {
          logMobileOverlay$1("keep overlay alive on same page during repaint", {
            reason,
            pageKey,
            currentPageKey,
            mode: site.additionalData
          });
          await scheduleGracefulRelease(
            video,
            site,
            `${reason}:same-page-retry`,
            container
          );
          return;
        }
        logMobileOverlay$1("delayed cleanup expired after page change", {
          reason,
          pageKey,
          currentPageKey
        });
        await releaseVideoHandler(video, `${reason}:page-changed`);
        await promotePendingVideo(container);
      }, MOBILE_YOUTUBE_RELEASE_GRACE_MS);
      pendingGraceReleases.set(video, {
        pageKey,
        reason,
        site,
        timer
      });
    };
    const promotePendingVideo = async (container) => {
      if (!container) {
        return;
      }
      const pendingVideo = pendingVideoByContainer.get(container);
      if (!pendingVideo) {
        return;
      }
      pendingVideoByContainer.delete(container);
      if (!pendingVideo.isConnected || videosWrappers2.has(pendingVideo) || initializingVideos.has(pendingVideo)) {
        return;
      }
      await handleVideoAdded(pendingVideo);
    };
    const handleVideoAdded = async (video) => {
      const canceledRelease = cancelPendingGraceRelease(
        video,
        "video-added-again"
      );
      if (videosWrappers2.has(video)) {
        const match = getMatchedSiteAndContainer(video);
        if (match) {
          videoContainers.set(video, match.container);
          videoSites.set(video, match.site);
          containerOwners.set(match.container, video);
        }
        if (canceledRelease) {
          try {
            await videosWrappers2.get(video)?.setCanPlay();
          } catch (error2) {
            console.error(
              "[VOT] Failed to refresh reattached mobile YouTube handler",
              error2
            );
          }
        }
        return;
      }
      if (initializingVideos.has(video)) return;
      initializingVideos.add(video);
      try {
        try {
          await ensureRuntimeActivated2("video-detected");
        } catch (err) {
          console.error("[VOT] Failed to activate runtime", err);
          return;
        }
        logNativeSubtitleTracks(video);
        const match = getMatchedSiteAndContainer(video);
        if (!match) {
          if (isVkProbeHost$2()) {
            const rect = video.getBoundingClientRect();
            console.warn(
              "[VOT][VK probe] video detected but no site/container match",
              {
                src: video.currentSrc || video.src || "",
                w: rect.width,
                h: rect.height,
                path: globalThis.location.pathname
              }
            );
          }
          return;
        }
        const { site, container } = match;
        console.log("[VOT][source-audio] video detected", {
          host: site.host,
          path: globalThis.location.pathname,
          currentTime: Number(video.currentTime.toFixed(3)),
          paused: video.paused,
          readyState: video.readyState,
          src: video.currentSrc || video.src || ""
        });
        if (isVkProbeHost$2()) {
          const rect = video.getBoundingClientRect();
          console.log("[VOT][VK probe] matched video", {
            site: site.host,
            selector: site.selector,
            container: container?.tagName,
            classes: container?.className,
            w: rect.width,
            h: rect.height,
            src: video.currentSrc || video.src || ""
          });
        }
        if ((site.host === "googledrive" || globalThis.location.hostname === "youtube.googleapis.com") && !isRenderableVideo(video)) {
          return;
        }
        const activeVideoForContainer = containerOwners.get(container);
        if (activeVideoForContainer && activeVideoForContainer !== video) {
          if (activeVideoForContainer.isConnected) {
            if (shouldReplaceActiveVideo(activeVideoForContainer, video)) {
              await releaseVideoHandler(
                activeVideoForContainer,
                "smaller duplicate"
              );
              clearContainerOwner(activeVideoForContainer);
            } else {
              pendingVideoByContainer.set(container, video);
              return;
            }
          } else {
            await releaseVideoHandler(activeVideoForContainer, "stale container");
            clearContainerOwner(activeVideoForContainer);
          }
        }
        const videoHandler = createVideoHandler(
          video,
          container,
          withRuntimeSiteUrl(site)
        );
        videosWrappers2.set(video, videoHandler);
        videoContainers.set(video, container);
        videoSites.set(video, site);
        containerOwners.set(container, video);
        videoHandler.onPrimaryAttachReady = () => {
          if (videosWrappers2.get(video) !== videoHandler) {
            return;
          }
          console.log("[VOT][observer] video attached", {
            host: site.host,
            path: globalThis.location.pathname,
            src: video.currentSrc || video.src || ""
          });
          void (async () => {
            const verified = await videoHandler.ensureOverlayVerified?.("primary-attach") ?? false;
            if (videosWrappers2.get(video) !== videoHandler) {
              return;
            }
            if (verified) {
              console.log(
                "[VOT][observer] discovery disabled only after verified overlay",
                {
                  host: site.host,
                  path: globalThis.location.pathname
                }
              );
              stopYouTubeVideoDiscovery(site);
            }
          })();
        };
        try {
          await videoHandler.init();
          if (videosWrappers2.get(video) !== videoHandler) {
            return;
          }
          try {
            await videoHandler.setCanPlay();
          } catch (err) {
            console.error("[VOT] Failed to get video data", err);
          }
        } catch (err) {
          if (videosWrappers2.get(video) === videoHandler) {
            await releaseVideoHandler(video, "init failed");
            const container2 = clearContainerOwner(video);
            clearPendingVideo(container2);
            await promotePendingVideo(container2);
          }
          console.error("[VOT] Failed to initialize videoHandler", err);
        }
      } finally {
        initializingVideos.delete(video);
      }
    };
    videoObserver2.onVideoAdded.addListener(handleVideoAdded);
    videoObserver2.onVideoRemoved.addListener(async (video) => {
      const site = videoSites.get(video);
      const container = clearContainerOwner(video);
      const pendingReplacement = container ? pendingVideoByContainer.get(container) : void 0;
      const shouldDelayCleanup = site && isGracefulMobileYouTubeSite(site) && (!pendingReplacement || pendingReplacement === video);
      logMobileOverlay$1("video removed", {
        shouldDelayCleanup,
        hasContainer: Boolean(container),
        pendingReplacement: Boolean(
          pendingReplacement && pendingReplacement !== video
        ),
        videoConnected: video.isConnected,
        src: video.currentSrc || video.src || ""
      });
      if (shouldDelayCleanup && site) {
        await scheduleGracefulRelease(video, site, "video-removed", container);
      } else {
        await releaseVideoHandler(video, "video removed");
      }
      initializingVideos.delete(video);
      if (container && pendingVideoByContainer.get(container) === video) {
        clearPendingVideo(container);
      }
      if (!shouldDelayCleanup) {
        await promotePendingVideo(container);
      }
    });
  }
  function normalizeAbsoluteUrl(value, baseUrl) {
    const raw = String(value || "").trim();
    if (!raw) {
      return null;
    }
    try {
      return new URL(raw, baseUrl).toString();
    } catch {
      return raw;
    }
  }
  function getTunnelPlayerContext(href = globalThis.location.href) {
    try {
      const url = new URL(href, globalThis.location.href);
      if (!/\/player\.html$/i.test(url.pathname)) {
        return null;
      }
      const sourceUrl = normalizeAbsoluteUrl(
        url.searchParams.get("src"),
        url.toString()
      );
      const playlistUrl = normalizeAbsoluteUrl(
        url.searchParams.get("list"),
        url.toString()
      );
      if (!sourceUrl && !playlistUrl) {
        return null;
      }
      return {
        kind: "playlist-player",
        playerUrl: url.toString(),
        sourceUrl,
        playlistUrl,
        hasTranslationReadyCallback: Boolean(playlistUrl)
      };
    } catch {
      return null;
    }
  }
  function isTunnelPlayerUrl(url) {
    return Boolean(getTunnelPlayerContext(url.toString()));
  }
  const GENERIC_PLAYER_SELECTORS = [
    ".player",
    ".video-player",
    ".video_player",
    ".video-container",
    ".video-wrapper",
    ".player-container",
    ".player-wrapper",
    ".plyr",
    ".jwplayer",
    ".shaka-video-container",
    ".dplayer",
    ".artplayer",
    ".vjs-player",
    ".video-js",
".bpx-player-video-wrap",
    ".squirtle-video-wrap",
".react-player",
    ".flowplayer",
    ".fp-player",
    "[data-player]",
    "[data-player-container]",
    "[data-video-player]",
    "[data-testid*='player']",
    "[class*='player']",
    "[class*='Player']",
    "[class*='video-player']",
    "[class*='VideoPlayer']",
    "[class*='video-container']",
    "[class*='videoContainer']",
    "[id*='player']",
    "video-player",
    "video"
  ];
  const GENERIC_PLAYER_SELECTOR = GENERIC_PLAYER_SELECTORS.join(", ");
  const VK_PLAYER_SELECTOR$1 = ".videoplayer_media, vk-video-player";
  const VK_CLIP_SELECTOR = 'div[data-testid="clipcontainer-video"], [data-testid="clipcontainer-video"]';
  const extraSites = [
    {
      host: VideoService.vk,
      url: "https://vk.com/video?z=",
      additionalData: "mobile",
      match: [/^m\.vk\.(com|ru)$/i, /^m\.vkvideo\.ru$/i],
      selector: VK_PLAYER_SELECTOR$1,
      shadowRoot: true,
      needExtraData: true
    },
    {
      host: VideoService.vk,
      url: "https://vk.com/video?z=",
      additionalData: "clips",
      match: [/^(www\.|m\.)?vk\.(com|ru)$/i, /^(.*\.)?vkvideo\.ru$/i],
      selector: VK_CLIP_SELECTOR,
      needExtraData: true
    },
    {
      host: VideoService.vk,
      url: "https://vk.com/video?z=",
      match: [/^(www\.|m\.)?vk\.(com|ru)$/i, /^(.*\.)?vkvideo\.ru$/i],
      selector: VK_PLAYER_SELECTOR$1,
      needExtraData: true
    },
    {
      host: "douyin",
      url: "https://www.douyin.com/",
      match: /(^|\.)douyin\.com$/i,
      selector: "video",
      eventSelector: "video",
      needExtraData: true,
      needBypassCSP: true
    },
    {
      host: "bilibili",
      url: "https://www.bilibili.com/video/",
      match: /(^|\.)bilibili\.com$/i,
      selector: "video",
      eventSelector: "video",
      needExtraData: true
    },
    {
      host: VideoService.custom,
      url: "stub",
      match: (url) => isTunnelPlayerUrl(url),
      selector: GENERIC_PLAYER_SELECTOR,
      eventSelector: GENERIC_PLAYER_SELECTOR,
      rawResult: true
    },
    {
      host: VideoService.custom,
      url: "stub",
      match: /(^|\.)kodikplayer\.com$/i,
      selector: GENERIC_PLAYER_SELECTOR
    },
    {
      host: VideoService.custom,
      url: "stub",
      match: /(^|\.)player\.cdnvideohub\.com$/i,
      selector: GENERIC_PLAYER_SELECTOR,
      rawResult: true
    },
    {
      host: VideoService.custom,
      url: "stub",
      match: /(^|\.)cdnvideohub\.com$/i,
      selector: GENERIC_PLAYER_SELECTOR,
      rawResult: true
    },
    {
      host: VideoService.custom,
      url: "stub",
      match: /(^|\.)okcdn\.ru$/i,
      selector: GENERIC_PLAYER_SELECTOR,
      rawResult: true
    },
    {
      host: VideoService.custom,
      url: "stub",
      match: /(^|\.)dailymotion\.com$/i,
      selector: GENERIC_PLAYER_SELECTOR,
      eventSelector: GENERIC_PLAYER_SELECTOR,
      rawResult: true
    },
    {
      host: VideoService.custom,
      url: "stub",
      match: /(^|\.)geo\.dailymotion\.com$/i,
      selector: GENERIC_PLAYER_SELECTOR,
      eventSelector: GENERIC_PLAYER_SELECTOR,
      rawResult: true
    },
{
      host: VideoService.rutube,
      url: "https://rutube.ru/video/",
      match: /(^|\.)rutube\.ru$/i,
      selector: ".video-player, [class*='VideoPlayer'], [class*='video-player'], #app > div > div",
      rawResult: true
    },
{
      host: VideoService.custom,
      url: "stub",
      match: /(^|\.)iframe\.mediadelivery\.net$/i,
      selector: GENERIC_PLAYER_SELECTOR,
      eventSelector: GENERIC_PLAYER_SELECTOR,
      rawResult: true
    },
    {
      host: VideoService.custom,
      url: "stub",
      match: /(^|\.)video\.bunnycdn\.com$/i,
      selector: GENERIC_PLAYER_SELECTOR,
      eventSelector: GENERIC_PLAYER_SELECTOR,
      rawResult: true
    },
{
      host: VideoService.custom,
      url: "stub",
      match: /(^|\.)ivi\.ru$/i,
      selector: ".iv-player-container, .player-container, .vjs-v7, " + GENERIC_PLAYER_SELECTOR,
      rawResult: true
    },
    {
      host: VideoService.custom,
      url: "stub",
      match: /(^|\.)kinopoisk\.ru$/i,
      selector: ".ott-player, [class*='player'], .yp-player, " + GENERIC_PLAYER_SELECTOR,
      rawResult: true
    },
    {
      host: VideoService.custom,
      url: "stub",
      match: [/(^|\.)okko\.tv$/i, /(^|\.)okko\.ru$/i],
      selector: GENERIC_PLAYER_SELECTOR,
      rawResult: true
    },
    {
      host: VideoService.custom,
      url: "stub",
      match: [/(^|\.)kion\.ru$/i],
      selector: GENERIC_PLAYER_SELECTOR,
      rawResult: true
    },

{
      host: VideoService.custom,
      url: "stub",
      match: /(^|\.)kodik\.(fun|pw|io|online|me)$/i,
      selector: ".fp-player, " + GENERIC_PLAYER_SELECTOR,
      rawResult: true
    },
{
      host: VideoService.custom,
      url: "stub",
      match: /(^|\.)wink\.ru$/i,
      selector: GENERIC_PLAYER_SELECTOR,
      rawResult: true
    },
{
      host: VideoService.custom,
      url: "stub",
      match: /(^|\.)seasonvar\.ru$/i,
      selector: GENERIC_PLAYER_SELECTOR,
      rawResult: true
    },
{
      host: VideoService.custom,
      url: "stub",
      match: /(^|\.)solodcdn\.com$/i,
      selector: GENERIC_PLAYER_SELECTOR,
      rawResult: true
    },

{
      host: VideoService.custom,
      url: "stub",
      match: [/^wikianimex\.ru$/i, /(?:^|\.)wikianimex\.ru$/i],
      selector: GENERIC_PLAYER_SELECTOR,
      eventSelector: GENERIC_PLAYER_SELECTOR,
      rawResult: true
    },
{
      host: VideoService.okru,
      url: "https://ok.ru/video/",
      match: /^m\.ok\.ru$/i,
      selector: "vk-video-player",
      shadowRoot: true
    },

{
      host: VideoService.youtube,
      url: "https://youtu.be/",
      match: /^m\.youtube\.com$/i,
      selector: ".player-container, #movie_player, ytm-player, #player, video",
      needExtraData: true,
      additionalData: "mobile"
    },

{
      host: VideoService.youtube,
      url: "https://youtu.be/",
      match: /^music\.youtube\.com$/i,
      selector: "ytmusic-player-page, ytmusic-player, ytm-player, #player, .player-container, video",
      needExtraData: true,
      additionalData: "music"
    }
  ];
  const SCAN_ONLY_BOOTSTRAP_SITE_HOSTS = new Set(["youtube"]);
  const POLL_PROBE_SITE_HOSTS = new Set(["youtube"]);
  const SINGLE_SHOT_SITE_HOSTS = new Set(["youtube"]);
  const EAGER_BOOTSTRAP_SITE_HOSTS = new Set([
    "vk",
    "googledrive",
    "yandexdisk",
    "rutube",
    "okru",
    "bilibili",
    "dzen"
  ]);
  const EAGER_BOOTSTRAP_HOSTNAME_PATTERNS = [
    /(?:^|\.)vkvideo\.ru$/i,
    /(?:^|\.)vk\.(?:com|ru)$/i,
    /^youtube\.googleapis\.com$/i,
    /^disk\.yandex\./i,
    /(?:^|\.)rutube\.ru$/i,
    /(?:^|\.)ok\.ru$/i,
    /(?:^|\.)bilibili\.(com|tv)$/i,
    /(?:^|\.)dzen\.ru$/i,
    /^player\.cdnvideohub\.com$/i,
    /(?:^|\.)wikianimex\.ru$/i,
    /(?:^|\.)kodikplayer\.com$/i,
    /(?:^|\.)kodik\.(info|biz|cc|fun|pw|io|online|me)$/i
  ];
  const PERSISTENT_OBSERVER_SITE_HOSTS = new Set([
    "vk",
    "googledrive",
    "yandexdisk",
    "rutube",
    "okru",
    "bilibili",
    "twitter",
    "tiktok",
    "dzen",
    "kick",
    "twitch"
  ]);
  const PERSISTENT_OBSERVER_HOSTNAME_PATTERNS = [
    /(?:^|\.)vkvideo\.ru$/i,
    /(?:^|\.)vk\.(?:com|ru)$/i,
    /^youtube\.googleapis\.com$/i,
    /^disk\.yandex\./i,
    /(?:^|\.)rutube\.ru$/i,
    /(?:^|\.)ok\.ru$/i,
    /(?:^|\.)bilibili\.(com|tv)$/i,
    /(?:^|\.)twitter\.com$/i,
    /(?:^|\.)x\.com$/i,
    /(?:^|\.)tiktok\.com$/i,
    /(?:^|\.)dzen\.ru$/i,
    /(?:^|\.)kick\.com$/i,
    /(?:^|\.)twitch\.tv$/i,
    /(?:^|\.)wikianimex\.ru$/i
  ];
  function isMobileYouTubeLikeService(site) {
    return String(site.host || "").trim() === "youtube" && (site.additionalData === "mobile" || site.additionalData === "music");
  }
  function isMobileYouTubeLikeHostname(hostname) {
    return hostname === "m.youtube.com" || hostname === "music.youtube.com";
  }
  function normalizeSelectorList(services2) {
    const deduped = new Set();
    for (const site of services2) {
      if (site.shadowRoot !== true) {
        continue;
      }
      const selector = typeof site.selector === "string" ? site.selector.trim() : "";
      if (!selector) {
        continue;
      }
      deduped.add(selector);
    }
    return Array.from(deduped);
  }
  function resolveProbeSelectors(hostname, services2) {
    const hosts = new Set(services2.map((site) => String(site.host || "").trim()));
    if (hosts.has("youtube") || /(?:^|\.)youtube(?:-nocookie|kids)?\.com$/i.test(hostname)) {
      return [
        "video.html5-main-video, .html5-video-container video, ytmusic-player video, ytm-player video, #player video, .player-container video, video"
      ];
    }
    return ["video"];
  }
  function resolveVideoObserverPolicy(input) {
    const hostname = String(input.hostname || "").trim().toLowerCase();
    const services2 = input.services;
    const isMobileYouTubeLike = isMobileYouTubeLikeHostname(hostname) || services2.some((site) => isMobileYouTubeLikeService(site));
    const isScanOnlyBootstrap = services2.some(
      (site) => !isMobileYouTubeLike && SCAN_ONLY_BOOTSTRAP_SITE_HOSTS.has(String(site.host || "").trim())
    );
    const startDomObservationOnEnable = !isScanOnlyBootstrap;
    const probeStrategy = services2.some(
      (site) => POLL_PROBE_SITE_HOSTS.has(String(site.host || "").trim())
    ) ? "poll" : "mutation";
    const stopAfterFirstVideoDetected = services2.some(
      (site) => !isMobileYouTubeLike && SINGLE_SHOT_SITE_HOSTS.has(String(site.host || "").trim())
    );
    const probeSelectors = resolveProbeSelectors(hostname, services2);
    const preferProbeBootstrap = !services2.some(
      (site) => EAGER_BOOTSTRAP_SITE_HOSTS.has(String(site.host || "").trim())
    ) && !EAGER_BOOTSTRAP_HOSTNAME_PATTERNS.some(
      (pattern) => pattern.test(hostname)
    );
    const shadowHostSelectors = startDomObservationOnEnable ? normalizeSelectorList(services2) : [];
    const observeShadowRoots = shadowHostSelectors.length > 0;
    const keepWatchingAfterVideoDetected = isMobileYouTubeLike || services2.some(
      (site) => PERSISTENT_OBSERVER_SITE_HOSTS.has(String(site.host || "").trim())
    ) || PERSISTENT_OBSERVER_HOSTNAME_PATTERNS.some(
      (pattern) => pattern.test(hostname)
    );
    return {
      preferProbeBootstrap,
      startDomObservationOnEnable,
      probeStrategy,
      probeSelectors,
      probePollIntervalMs: probeStrategy === "poll" ? 120 : 0,
      probePollTimeoutMs: probeStrategy === "poll" ? 4e3 : 0,
      stopAfterFirstVideoDetected,
      keepWatchingAfterVideoDetected,
      observeShadowRoots,
      shadowHostSelectors
    };
  }
  function shouldSkipIframeBootstrap(input) {
    if (!input.isIframe) return false;
    return input.origin === "null";
  }
  function resolveBootstrapMode(input) {
    if (shouldSkipIframeBootstrap(input)) {
      return "skip";
    }
    if (!input.isIframe && input.origin === input.authOrigin) {
      return "auth-eager";
    }
    return "lazy";
  }
  class Chaimu {
    _debug = false;
    audioContext;
    player;
    video;
    fetchFn;
    fetchOpts;
    constructor({
      url,
      video,
      debug: debug2 = false,
      fetchFn = config.fetchFn,
      fetchOpts = {},
      preferAudio = false
    }) {
      this._debug = config.debug = debug2;
      this.fetchFn = fetchFn;
      this.fetchOpts = fetchOpts;
      this.audioContext = preferAudio ? void 0 : initAudioContext();
      this.player = preferAudio ? new AudioPlayer(this, url) : new ChaimuPlayer(this, url);
      this.video = video;
    }
    async init() {
      await this.player.init();
      if (this.video && !this.video.paused) {
        this.player.lipSync("play");
      }
      this.player.addVideoEvents();
    }
    set debug(value) {
      this._debug = config.debug = value;
    }
    get debug() {
      return this._debug;
    }
  }
  function getComposableParent(node) {
    if (!node) return null;
    if (typeof ShadowRoot !== "undefined" && node instanceof ShadowRoot) {
      return node.host;
    }
    return node.parentNode ?? null;
  }
  function containsCrossShadow(container, target) {
    let node = target;
    while (node) {
      if (node === container) return true;
      node = getComposableParent(node);
    }
    return false;
  }
  function isDocumentViewportElement(element) {
    return element === document.body || element === document.documentElement;
  }
  function resolveScopedFullscreenElement(fullscreenEl, anchors, options = {}) {
    const { allowDocumentViewport = false } = options;
    if (!(fullscreenEl instanceof HTMLElement)) {
      return null;
    }
    if (isDocumentViewportElement(fullscreenEl) && !allowDocumentViewport) {
      return null;
    }
    for (const anchor of anchors) {
      if (anchor && (containsCrossShadow(fullscreenEl, anchor) || containsCrossShadow(anchor, fullscreenEl))) {
        return fullscreenEl;
      }
    }
    return null;
  }
  function closestCrossShadow(element, selector) {
    if (!element || !selector) return null;
    const origin = element instanceof Document ? null : element;
    const walk = (current) => {
      if (!current) return null;
      if (current instanceof Document) {
        if (origin) {
          const matches = current.querySelectorAll(selector);
          for (const match of matches) {
            if (containsCrossShadow(match, origin)) {
              return match;
            }
          }
          return null;
        }
        return current.querySelector(selector);
      }
      const closest = current.closest(selector);
      if (closest) return closest;
      const root = current.getRootNode();
      if (root instanceof ShadowRoot) {
        return walk(root.host);
      }
      if (root instanceof Document) {
        return walk(root);
      }
      if (root !== current) {
        const parent = getComposableParent(root);
        if (parent && parent !== current && parent instanceof Element) {
          return walk(parent);
        }
      }
      return null;
    };
    return walk(element);
  }
  function promoteVideoElementContainer(matched, video) {
    if (matched !== video) {
      return matched;
    }
    let parent = video.parentElement;
    while (parent) {
      if (parent !== document.body && parent !== document.documentElement) {
        return parent;
      }
      parent = parent.parentElement;
    }
    return matched;
  }
  function findConnectedContainerBySelector(video, selector) {
    if (!selector) {
      return null;
    }
    const matched = closestCrossShadow(video, selector);
    if (matched instanceof HTMLElement && matched.isConnected && containsCrossShadow(matched, video)) {
      return promoteVideoElementContainer(matched, video);
    }
    return null;
  }
  const EXTERNAL_VOLUME_HOSTS = new Set(["youtube", "googledrive"]);
  const YOUTUBE_LIKE_HOSTS = EXTERNAL_VOLUME_HOSTS;
  const MUTE_SYNC_DISABLED_HOSTS = new Set(["rutube", "ok"]);
  const TRANSLATION_DOWNLOAD_HOSTS = new Set([
    "youtube",
    "invidious",
    "piped"
  ]);
  function isExternalVolumeHost(host) {
    return EXTERNAL_VOLUME_HOSTS.has(host);
  }
  function isYouTubeLikeHost(host) {
    return YOUTUBE_LIKE_HOSTS.has(host);
  }
  function isMuteSyncDisabledHost(host) {
    return MUTE_SYNC_DISABLED_HOSTS.has(host);
  }
  function isDesktopYouTubeLikeSite(site) {
    return isYouTubeLikeHost(site.host) && !site.additionalData;
  }
  function isMobileYouTubeLikeSite(site) {
    return isYouTubeLikeHost(site.host) && (site.additionalData === "mobile" || site.additionalData === "music");
  }
  function isTranslationDownloadHost(host) {
    return TRANSLATION_DOWNLOAD_HOSTS.has(host);
  }
  const MOBILE_YOUTUBE_OVERLAY_ROOT_ATTR = "data-vot-mobile-youtube-overlay-root";
  function getPersistentOverlayHost() {
    return document.body ?? document.documentElement;
  }
  function getPersistentMobileYouTubeOverlayRoot(site) {
    const attrValue = site.additionalData === "music" ? "music" : "mobile";
    const host = getPersistentOverlayHost();
    let root = document.querySelector(
      `[${MOBILE_YOUTUBE_OVERLAY_ROOT_ATTR}="${attrValue}"]`
    );
    if (!root) {
      root = document.createElement("vot-block");
      root.setAttribute(MOBILE_YOUTUBE_OVERLAY_ROOT_ATTR, attrValue);
      root.style.position = "fixed";
      root.style.left = "0";
      root.style.top = "0";
      root.style.width = "100vw";
      root.style.height = "100vh";
      root.style.margin = "0";
      root.style.padding = "0";
      root.style.border = "0";
      root.style.background = "transparent";
      root.style.overflow = "visible";
      root.style.pointerEvents = "none";
      root.style.zIndex = "2147483645";
      host.appendChild(root);
    } else if (root.parentElement !== host) {
      host.appendChild(root);
    }
    return root;
  }
  function resolveOverlayBaseContainer(container, site) {
    if (container instanceof HTMLVideoElement) {
      return container.parentElement ?? container;
    }
    return isDesktopYouTubeLikeSite(site) ? container.parentElement ?? container : container;
  }
  function resolveOverlayMountTargets(input) {
    const base = resolveOverlayBaseContainer(input.container, input.site);
    const stableMobileYouTubeRoot = !input.fullscreenRoot && isMobileYouTubeLikeSite(input.site) ? getPersistentMobileYouTubeOverlayRoot(input.site) : null;
    const root = input.fullscreenRoot ?? stableMobileYouTubeRoot ?? base;
    const shouldUseViewportSubtitlesMount = input.site.host === "youtube" && input.site.additionalData === "music";
    const subtitlesMountContainer = input.site.host === "googledrive" ? input.fullscreenRoot ?? document.body : input.site.host === "vk" ? input.fullscreenRoot ?? document.documentElement : shouldUseViewportSubtitlesMount ? root : stableMobileYouTubeRoot ? base : root;
    return {
      base,
      root,
      portalContainer: stableMobileYouTubeRoot ?? base,
      subtitlesMountContainer
    };
  }
  function isCustomPlaybackTarget(siteHost, videoHost) {
    return siteHost === "custom" || videoHost === "custom" || siteHost === "okru" || videoHost === "okru";
  }
  function shouldUsePlainAudioPlayback(input) {
    if (isCustomPlaybackTarget(input.siteHost, input.videoHost)) {
      return true;
    }
    if (input.isStream) {
      return true;
    }
    if (!input.hasAudioContext) {
      return true;
    }
    if (!input.newAudioPlayer) {
      return true;
    }
    if (!input.onlyBypassMediaCSP) {
      return false;
    }
    return !input.needBypassCSP;
  }
  const lastLoggedAudioSignatureByVideo = new WeakMap();
  const lastLoggedStatusSignatureByVideo = new WeakMap();
  function hasResolvableMediaSource(video) {
    if (video.currentSrc || video.src || video.srcObject) {
      return true;
    }
    const source = video.querySelector("source");
    return Boolean(source?.getAttribute("src") || source?.src);
  }
  function hasPlaybackStarted(video) {
    return !video.paused || video.currentTime > 0.01 || video.ended || video.played.length > 0;
  }
  function getCapturedAudioTrackCount(video) {
    const candidate = video;
    const captureStream = candidate.captureStream ?? candidate.mozCaptureStream;
    if (typeof captureStream !== "function") {
      return null;
    }
    try {
      const stream = captureStream.call(video);
      return stream.getAudioTracks().length;
    } catch {
      return null;
    }
  }
  function inspectAudioPresence(video, playbackStarted) {
    const candidate = video;
    const signals = [];
    const readyForNegativeInference = playbackStarted && video.readyState >= HTMLMediaElement.HAVE_CURRENT_DATA;
    if (video.srcObject instanceof MediaStream) {
      const trackCount = video.srcObject.getAudioTracks().length;
      signals.push(`srcObject:${trackCount}`);
      return {
        presence: trackCount > 0,
        source: "srcObject",
        signals
      };
    }
    if (typeof candidate.mozHasAudio === "boolean") {
      signals.push(`mozHasAudio:${candidate.mozHasAudio}`);
      if (candidate.mozHasAudio) {
        return {
          presence: true,
          source: "mozHasAudio",
          signals
        };
      }
    }
    if (typeof candidate.webkitAudioDecodedByteCount === "number" && candidate.webkitAudioDecodedByteCount > 0) {
      signals.push(
        `webkitAudioDecodedByteCount:${candidate.webkitAudioDecodedByteCount}`
      );
      return {
        presence: true,
        source: "webkitAudioDecodedByteCount",
        signals
      };
    }
    let negativeSignals = 0;
    if ("audioTracks" in candidate && typeof candidate.audioTracks?.length === "number") {
      signals.push(`audioTracks:${candidate.audioTracks.length}`);
      if (candidate.audioTracks.length > 0) {
        return {
          presence: true,
          source: "audioTracks",
          signals
        };
      }
      if (readyForNegativeInference) {
        negativeSignals += 1;
      }
    }
    const capturedAudioTrackCount = getCapturedAudioTrackCount(video);
    if (capturedAudioTrackCount !== null) {
      signals.push(`captureStream:${capturedAudioTrackCount}`);
      if (capturedAudioTrackCount > 0) {
        return {
          presence: true,
          source: "captureStream",
          signals
        };
      }
      if (readyForNegativeInference) {
        negativeSignals += 1;
      }
    }
    if (readyForNegativeInference && typeof candidate.mozHasAudio === "boolean" && candidate.mozHasAudio === false) {
      negativeSignals += 1;
    }
    if (readyForNegativeInference && negativeSignals >= 2) {
      return {
        presence: false,
        source: "negative-signals",
        signals
      };
    }
    return {
      presence: null,
      source: "unknown",
      signals
    };
  }
  function maybeLogSourceAudioState(video, state, details) {
    const audioSignature = JSON.stringify({
      audioDetected: state.audioDetected,
      detectionSource: state.detectionSource,
      signals: details.signals
    });
    if (lastLoggedAudioSignatureByVideo.get(video) !== audioSignature) {
      lastLoggedAudioSignatureByVideo.set(video, audioSignature);
      console.log("[VOT][source-audio] audio detected", {
        audioDetected: state.audioDetected,
        detectionSource: state.detectionSource,
        signals: details.signals,
        paused: details.paused,
        currentTime: Number(details.currentTime.toFixed(3)),
        readyState: details.readyState
      });
    }
    const statusSignature = JSON.stringify({
      kind: state.kind,
      ready: state.ready,
      localizationKey: state.localizationKey ?? null,
      audioDetected: state.audioDetected,
      detectionSource: state.detectionSource,
      playbackStarted: details.playbackStarted,
      hasMediaSource: details.hasMediaSource,
      readyState: details.readyState
    });
    if (lastLoggedStatusSignatureByVideo.get(video) !== statusSignature) {
      lastLoggedStatusSignatureByVideo.set(video, statusSignature);
      console.log("[VOT][source-audio] status selected", {
        kind: state.kind,
        ready: state.ready,
        localizationKey: state.localizationKey ?? null,
        audioDetected: state.audioDetected,
        detectionSource: state.detectionSource,
        playbackStarted: details.playbackStarted,
        hasMediaSource: details.hasMediaSource,
        paused: details.paused,
        currentTime: Number(details.currentTime.toFixed(3)),
        playedRanges: details.playedRanges,
        readyState: details.readyState
      });
    }
  }
  function getSourceAudioAvailability(video) {
    const playbackStarted = hasPlaybackStarted(video);
    const hasMediaSource = hasResolvableMediaSource(video);
    const audioInspection = inspectAudioPresence(video, playbackStarted);
    let state;
    if (!playbackStarted) {
      state = {
        kind: "needsPlayback",
        ready: false,
        audioDetected: audioInspection.presence,
        detectionSource: audioInspection.source,
        localizationKey: "VOTStartVideoForTranslation"
      };
    } else if (audioInspection.presence === true) {
      state = {
        kind: "ready",
        ready: true,
        audioDetected: true,
        detectionSource: audioInspection.source
      };
    } else if (audioInspection.presence === false) {
      state = {
        kind: "missing",
        ready: false,
        audioDetected: false,
        detectionSource: audioInspection.source,
        localizationKey: "VOTAudioNotDetected"
      };
    } else if (!hasMediaSource) {
      state = {
        kind: "pending",
        ready: false,
        audioDetected: null,
        detectionSource: audioInspection.source,
        localizationKey: "VOTAudioNotYetAvailable"
      };
    } else if (video.readyState < HTMLMediaElement.HAVE_METADATA) {
      state = {
        kind: "pending",
        ready: false,
        audioDetected: null,
        detectionSource: audioInspection.source,
        localizationKey: "VOTAudioNotYetAvailable"
      };
    } else if (video.readyState < HTMLMediaElement.HAVE_CURRENT_DATA) {
      state = {
        kind: "pending",
        ready: false,
        audioDetected: null,
        detectionSource: audioInspection.source,
        localizationKey: "VOTAudioNotYetAvailable"
      };
    } else {
      state = {
        kind: "ready",
        ready: true,
        audioDetected: audioInspection.presence,
        detectionSource: audioInspection.source
      };
    }
    maybeLogSourceAudioState(video, state, {
      playbackStarted,
      hasMediaSource,
      readyState: video.readyState,
      paused: video.paused,
      currentTime: video.currentTime,
      playedRanges: video.played.length,
      signals: audioInspection.signals
    });
    return state;
  }
  function makeFileId(downloadType, itag, fileSize, minChunkSize) {
    return JSON.stringify({
      downloadType,
      itag,
      minChunkSize,
      fileSize
    });
  }
  function normalizeMimeType(mimeType) {
    return mimeType?.toLowerCase() ?? "";
  }
  function isAudioOnlyMimeType(mimeType) {
    const normalizedMimeType = normalizeMimeType(mimeType);
    return normalizedMimeType.includes("audio/") && !normalizedMimeType.includes("video/");
  }
  function isMp4aAdaptiveAudioMimeType(mimeType) {
    const normalizedMimeType = normalizeMimeType(mimeType);
    return normalizedMimeType.includes("audio/mp4") && normalizedMimeType.includes("mp4a.");
  }
  function isOpusAdaptiveAudioMimeType(mimeType) {
    const normalizedMimeType = normalizeMimeType(mimeType);
    return normalizedMimeType.includes("audio/webm") && normalizedMimeType.includes("opus");
  }
  function extractAudioCodecFromMimeType(mimeType) {
    if (!mimeType) {
      return "mp4a.40.2";
    }
    const codecsMatch = /codecs="([^"]+)"/i.exec(mimeType);
    if (!codecsMatch?.[1]) {
      return "mp4a.40.2";
    }
    const codecs = codecsMatch[1].split(",").map((value) => value.trim());
    return codecs.find((value) => value.toLowerCase().startsWith("mp4a.")) ?? codecs[0] ?? "mp4a.40.2";
  }
  function pickByBitrate(formats, direction) {
    let selected = null;
    let selectedBitrate = direction === "max" ? -Infinity : Infinity;
    for (const format of formats) {
      const bitrate = format.bitrate ?? 0;
      if (direction === "max" && bitrate > selectedBitrate || direction === "min" && bitrate < selectedBitrate) {
        selected = format;
        selectedBitrate = bitrate;
      }
    }
    return selected;
  }
  function pickAdaptiveAudioFormat(formats, quality) {
    const audioFormats = formats.filter(
      (format) => isAudioOnlyMimeType(format.mimeType)
    );
    if (!audioFormats.length) {
      throw new Error("No adaptive audio formats were found in player response");
    }
    const pickDirection = quality === "bestefficiency" ? "min" : "max";
    const candidateGroups = quality === "bestefficiency" ? [
      audioFormats.filter(
        (format) => isOpusAdaptiveAudioMimeType(format.mimeType)
      ),
      audioFormats
    ] : [
      audioFormats.filter(
        (format) => isMp4aAdaptiveAudioMimeType(format.mimeType)
      ),
      audioFormats.filter(
        (format) => isOpusAdaptiveAudioMimeType(format.mimeType)
      ),
      audioFormats
    ];
    for (const candidates of candidateGroups) {
      if (!candidates.length) {
        continue;
      }
      const selected = pickByBitrate(candidates, pickDirection);
      if (selected) {
        return selected;
      }
    }
    throw new Error("No adaptive audio formats were found in player response");
  }
  class YtWatchContextForbiddenError extends Error {
    status;
    constructor(status = 403) {
      super(`Failed to load watch page: ${status}`);
      this.name = "YtWatchContextForbiddenError";
      this.status = status;
    }
  }
  const VIDEO_ID_PATTERN = /^[a-zA-Z0-9_-]{11}$/;
  const YT_BASE = "https://www.youtube.com";
  const ANDROID_CLIENT_VERSION = "19.44.38";
  const ANDROID_VR_CLIENT_VERSION = "1.60.19";
  const IOS_CLIENT_VERSION = "19.45.4";
  const CLIENT_FALLBACK_ORDER = [
    "ANDROID_VR",
    "ANDROID",
    "IOS",
    "WEB",
    "MWEB"
  ];
  const DEFAULT_HEADERS = {
    accept: "*/*",
    origin: YT_BASE,
    referer: `${YT_BASE}/`
  };
  const RANGE_FALLBACK_CHUNK_SIZE = 256 * 1024;
  function withSignal(signal) {
    return signal ? { signal } : {};
  }
  function resolveInnertubeClient(requestedClient, watchContext, videoId) {
    switch (requestedClient) {
      case "ANDROID":
      case "YTMUSIC_ANDROID":
      case "YTSTUDIO_ANDROID":
        return {
          clientName: "ANDROID",
          clientVersion: ANDROID_CLIENT_VERSION,
          hl: "en",
          gl: "US",
          androidSdkVersion: 34,
          osName: "Android",
          osVersion: "14",
          platform: "MOBILE"
        };
      case "ANDROID_VR":
        return {
          clientName: "ANDROID_VR",
          clientVersion: ANDROID_VR_CLIENT_VERSION,
          hl: "en",
          gl: "US",
          androidSdkVersion: 31,
          osName: "Android",
          osVersion: "12",
          platform: "MOBILE"
        };
      case "IOS":
        return {
          clientName: "IOS",
          clientVersion: IOS_CLIENT_VERSION,
          hl: "en",
          gl: "US",
          platform: "MOBILE",
          osName: "iPhone",
          osVersion: "18.0.0.22A3354",
          deviceMake: "Apple",
          deviceModel: "iPhone16,2"
        };
      case "MWEB":
        return {
          clientName: "MWEB",
          clientVersion: watchContext.clientVersion,
          hl: "en",
          gl: "US",
          originalUrl: `${YT_BASE}/watch?v=${videoId}`
        };
      default:
        return {
          clientName: "WEB",
          clientVersion: watchContext.clientVersion,
          hl: "en",
          gl: "US",
          utcOffsetMinutes: 0,
          originalUrl: `${YT_BASE}/watch?v=${videoId}`
        };
    }
  }
  function extractVideoId(input) {
    const value = input.trim();
    if (VIDEO_ID_PATTERN.test(value)) {
      return value;
    }
    let url;
    try {
      url = new URL(value);
    } catch {
      throw new Error(`Cannot extract YouTube video id from: ${input}`);
    }
    const hostname = url.hostname.toLowerCase();
    if (hostname === "youtu.be" || hostname.endsWith(".youtu.be")) {
      const id = url.pathname.split("/").find(Boolean);
      if (id && VIDEO_ID_PATTERN.test(id)) {
        return id;
      }
    }
    const searchId = url.searchParams.get("v");
    if (searchId && VIDEO_ID_PATTERN.test(searchId)) {
      return searchId;
    }
    const pathSegments = url.pathname.split("/").filter(Boolean);
    const shortsIndex = pathSegments.indexOf("shorts");
    if (shortsIndex !== -1) {
      const shortsId = pathSegments[shortsIndex + 1];
      if (shortsId && VIDEO_ID_PATTERN.test(shortsId)) {
        return shortsId;
      }
    }
    const embedIndex = pathSegments.indexOf("embed");
    if (embedIndex !== -1) {
      const embedId = pathSegments[embedIndex + 1];
      if (embedId && VIDEO_ID_PATTERN.test(embedId)) {
        return embedId;
      }
    }
    throw new Error(`Cannot extract YouTube video id from: ${input}`);
  }
  function decodeEscapedJsonString(input) {
    return input.replaceAll("\\u0026", "&").replaceAll("\\/", "/");
  }
  function getRequiredVideoId(request) {
    const source = request.videoId ?? request.videoUrl;
    if (!source) {
      throw new Error("Either videoId or videoUrl is required");
    }
    return extractVideoId(source);
  }
  function matchFirst(source, patterns) {
    for (const pattern of patterns) {
      const matched = pattern.exec(source)?.[1];
      if (matched) {
        return matched;
      }
    }
    return void 0;
  }
  async function readResponseBytes(response) {
    return new Uint8Array(await response.arrayBuffer());
  }
  function makeCPN(length = 16) {
    const alphabet = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789-_";
    let output = "";
    if (typeof crypto !== "undefined" && typeof crypto.getRandomValues === "function") {
      const bytes = new Uint8Array(length);
      crypto.getRandomValues(bytes);
      for (const byte of bytes) {
        output += alphabet[byte % alphabet.length] ?? "a";
      }
      return output;
    }
    for (let i2 = 0; i2 < length; i2++) {
      output += alphabet[Math.floor(Math.random() * alphabet.length)] ?? "a";
    }
    return output;
  }
  function parsePositiveInteger(value) {
    if (!value) {
      return null;
    }
    const parsed = Number.parseInt(value, 10);
    if (!Number.isFinite(parsed) || parsed <= 0) {
      return null;
    }
    return parsed;
  }
  function parseContentLengthFromContentRange(contentRange) {
    if (!contentRange) {
      return null;
    }
    const matched = /\/(\d+)\s*$/i.exec(contentRange);
    return parsePositiveInteger(matched?.[1]);
  }
  function parseContentRangeHeader(contentRange) {
    if (!contentRange) {
      return null;
    }
    const matched = /^bytes\s+(\d+)-(\d+)\/(?:\d+|\*)$/i.exec(
      contentRange.trim()
    );
    if (!matched) {
      return null;
    }
    const start = Number.parseInt(matched[1] ?? "", 10);
    const end = Number.parseInt(matched[2] ?? "", 10);
    if (!Number.isFinite(start) || !Number.isFinite(end) || start < 0 || end < start) {
      return null;
    }
    return { start, end };
  }
  function getExpectedRangeLength(start, end) {
    return end - start + 1;
  }
  function isValidRangeChunkResponse(response, bytes, start, end) {
    const expectedLength = getExpectedRangeLength(start, end);
    if (expectedLength <= 0) {
      return false;
    }
    if (bytes.byteLength <= 0 || bytes.byteLength > expectedLength) {
      return false;
    }
    const contentRange = parseContentRangeHeader(
      response.headers.get("content-range")
    );
    if (contentRange) {
      return contentRange.start === start && contentRange.end === start + bytes.byteLength - 1;
    }
    if (response.status === 206) {
      return bytes.byteLength === expectedLength;
    }
    if (response.status === 200) {
      return start === 0 && bytes.byteLength === expectedLength;
    }
    return false;
  }
  function describeRangeChunkResponse(response, bytes) {
    const contentRange = response.headers.get("content-range") ?? "none";
    const contentLength = response.headers.get("content-length") ?? "none";
    return `status=${response.status}; bytes=${bytes.byteLength}; content-range=${contentRange}; content-length=${contentLength}`;
  }
  function getAudioMimeType(mimeType) {
    const normalizedMimeType = mimeType?.toLowerCase() ?? "";
    if (normalizedMimeType.includes("audio/webm")) {
      return "audio/webm";
    }
    if (normalizedMimeType.includes("audio/mp4")) {
      return "audio/mp4";
    }
    return "audio/aac";
  }
  function buildClientAttemptOrder(requestedClient) {
    const ordered = requestedClient ? [requestedClient, ...CLIENT_FALLBACK_ORDER] : [...CLIENT_FALLBACK_ORDER];
    const seen = new Set();
    return ordered.filter((client) => {
      if (seen.has(client)) {
        return false;
      }
      seen.add(client);
      return true;
    });
  }
  let AudioDownloader$1 = class AudioDownloader {
    fetchFn;
    constructor(options = {}) {
      this.fetchFn = options.fetchImplementation ?? fetch;
    }
    async fetchRangeChunk(streamUrl, start, end, signal) {
      const rangeHeader = `bytes=${start}-${end}`;
      const response = await this.fetchFn(streamUrl, {
        headers: {
          ...DEFAULT_HEADERS,
          range: rangeHeader
        },
        ...withSignal(signal)
      });
      if (!response.ok) {
        throw new Error(`Failed to download stream chunk: ${response.status}`);
      }
      const bytes = await readResponseBytes(response);
      if (!isValidRangeChunkResponse(response, bytes, start, end)) {
        throw new Error(
          `Received unexpected stream chunk payload: ${describeRangeChunkResponse(response, bytes)}`
        );
      }
      return bytes;
    }
    async downloadStreamByRanges(streamUrl, contentLengthHint, signal) {
      const fileSize = await this.resolveStreamContentLength(
        streamUrl,
        contentLengthHint,
        signal,
        true
      );
      const merged = new Uint8Array(fileSize);
      let offset = 0;
      for (let start = 0; start < fileSize; start += RANGE_FALLBACK_CHUNK_SIZE) {
        const end = Math.min(fileSize - 1, start + RANGE_FALLBACK_CHUNK_SIZE - 1);
        const chunk = await this.fetchRangeChunk(streamUrl, start, end, signal);
        if (offset + chunk.byteLength > merged.byteLength) {
          throw new Error(
            "Downloaded stream chunk exceeds probed stream content length"
          );
        }
        merged.set(chunk, offset);
        offset += chunk.byteLength;
      }
      if (offset === merged.byteLength) {
        return merged;
      }
      return merged.slice(0, offset);
    }
    async downloadAudioToChunkStream(request, options) {
      if (options.chunkSize <= 0) {
        throw new RangeError("Audio downloader. ytAudio. chunkSize must be > 0");
      }
      return this.withResolvedPlayableAudioFormat(
        request,
        request.videoQuality ?? "best",
        "Chunk mode requires an adaptive audio stream format",
        "Unable to resolve streamable format for chunk mode",
        async ({ resolved, signal }) => {
          const fileSize = await this.resolveStreamContentLength(
            resolved.streamUrl,
            resolved.chosenFormat.contentLength,
            signal,
            true
          );
          const mediaPartsLength = Math.max(
            1,
            Math.ceil(fileSize / options.chunkSize)
          );
          return {
            videoId: resolved.videoId,
            fileSize,
            itag: resolved.chosenFormat.itag ?? 0,
            mediaPartsLength,
            getMediaBuffers: (async function* () {
              for (let index = 0; index < mediaPartsLength; index++) {
                const start = index * options.chunkSize;
                const end = Math.min(fileSize - 1, start + options.chunkSize - 1);
                const bytes = await this.fetchRangeChunk(
                  resolved.streamUrl,
                  start,
                  end,
                  signal
                );
                yield bytes;
              }
            }).bind(this)
          };
        }
      );
    }
    async downloadAudioToUint8Array(request) {
      const chunks = [];
      let total = 0;
      const streamResult = await this.extractAndWriteAudio(request, {
        async write(chunk) {
          chunks.push(chunk);
          total += chunk.byteLength;
        }
      });
      const bytes = new Uint8Array(total);
      let offset = 0;
      for (const chunk of chunks) {
        bytes.set(chunk, offset);
        offset += chunk.byteLength;
      }
      return {
        ...streamResult,
        bytes
      };
    }
    async extractAndWriteAudio(request, sink) {
      return this.withResolvedPlayableAudioFormat(
        request,
        request.videoQuality ?? "bestefficiency",
        "Selected stream is not audio-only",
        "Unable to download playable stream format",
        async ({ resolved, signal }) => {
          const streamBytes = await this.downloadStreamByRanges(
            resolved.streamUrl,
            resolved.chosenFormat.contentLength,
            signal
          );
          const hints = this.getExtractionHints(resolved.chosenFormat);
          await sink.write(streamBytes);
          return {
            videoId: resolved.videoId,
            bytesWritten: streamBytes.byteLength,
            mimeType: getAudioMimeType(resolved.chosenFormat.mimeType),
            codec: hints.codec,
            sampleRate: hints.sampleRate,
            channels: hints.channels
          };
        }
      );
    }
    async withResolvedPlayableAudioFormat(request, quality, audioOnlyErrorMessage, failurePrefix, onResolved) {
      const videoId = getRequiredVideoId(request);
      const { signal } = request;
      const watchContext = await this.fetchWatchContext(videoId, signal);
      const clientAttempts = buildClientAttemptOrder(request.client);
      const attemptErrors = [];
      for (const client of clientAttempts) {
        try {
          const resolved = await this.resolvePlayableFormatForClient({
            videoId,
            watchContext,
            client,
            quality,
            signal
          });
          if (!isAudioOnlyMimeType(resolved.chosenFormat.mimeType)) {
            throw new Error(audioOnlyErrorMessage);
          }
          return await onResolved({ resolved, signal });
        } catch (error2) {
          const message = error2 instanceof Error ? error2.message : String(error2);
          attemptErrors.push(`${client}: ${message}`);
        }
      }
      throw new Error(`${failurePrefix}. Attempts: ${attemptErrors.join(" | ")}`);
    }
    async resolvePlayableFormatForClient({
      videoId,
      watchContext,
      client,
      quality,
      signal
    }) {
      const response = await this.fetchPlayerResponse(
        videoId,
        watchContext,
        client,
        signal
      );
      const adaptiveFormats = response.streamingData?.adaptiveFormats ?? [];
      const directAdaptiveFormats = adaptiveFormats.filter(
        (format) => Boolean(format.url)
      );
      if (!directAdaptiveFormats.length) {
        throw new Error(
          "Player response did not contain direct adaptive audio stream URLs"
        );
      }
      const chosenFormat = pickAdaptiveAudioFormat(
        directAdaptiveFormats,
        quality
      );
      const streamUrl = this.resolveFormatUrl(
        chosenFormat,
        watchContext.clientVersion
      );
      return {
        videoId,
        chosenFormat,
        streamUrl
      };
    }
    async resolveStreamContentLength(streamUrl, contentLengthHint, signal, forceProbe = false) {
      const hintedLength = parsePositiveInteger(contentLengthHint);
      if (hintedLength !== null && !forceProbe) {
        return hintedLength;
      }
      let probeResponse;
      try {
        probeResponse = await this.fetchFn(streamUrl, {
          headers: {
            ...DEFAULT_HEADERS,
            range: "bytes=0-0"
          },
          ...withSignal(signal)
        });
      } catch (error2) {
        if (hintedLength !== null) {
          return hintedLength;
        }
        const message = error2 instanceof Error ? error2.message : String(error2);
        throw new Error(`Failed to probe stream content length: ${message}`);
      }
      if (!probeResponse.ok) {
        if (hintedLength !== null) {
          return hintedLength;
        }
        throw new Error(
          `Failed to probe stream content length: ${probeResponse.status}`
        );
      }
      const contentRangeLength = parseContentLengthFromContentRange(
        probeResponse.headers.get("content-range")
      );
      const storedLength = parsePositiveInteger(
        probeResponse.headers.get("x-goog-stored-content-length")
      );
      const contentLength = parsePositiveInteger(
        probeResponse.headers.get("content-length")
      );
      if (typeof probeResponse.body?.cancel === "function") {
        try {
          await probeResponse.body.cancel();
        } catch {
        }
      }
      return contentRangeLength ?? storedLength ?? hintedLength ?? contentLength ?? (() => {
        throw new Error("Failed to resolve stream content length");
      })();
    }
    getExtractionHints(format) {
      const codec = extractAudioCodecFromMimeType(format.mimeType);
      const sampleRate = Number.parseInt(format.audioSampleRate ?? "", 10);
      return {
        codec,
        sampleRate: Number.isFinite(sampleRate) && sampleRate > 0 ? sampleRate : 44100,
        channels: format.audioChannels && format.audioChannels > 0 ? format.audioChannels : 2
      };
    }
    resolveFormatUrl(format, clientVersion) {
      if (!format.url) {
        throw new Error("Selected format does not contain a direct stream URL");
      }
      const streamUrl = new URL(format.url);
      const client = streamUrl.searchParams.get("c");
      if (client === "WEB") {
        streamUrl.searchParams.set("cver", clientVersion);
      }
      streamUrl.searchParams.set("cpn", makeCPN());
      return streamUrl.toString();
    }
    async fetchWatchContext(videoId, signal) {
      const watchUrl = `${YT_BASE}/watch?v=${encodeURIComponent(videoId)}&hl=en`;
      const response = await this.fetchFn(watchUrl, {
        headers: DEFAULT_HEADERS,
        ...withSignal(signal)
      });
      if (!response.ok) {
        if (response.status === 403) {
          throw new YtWatchContextForbiddenError(response.status);
        }
        throw new Error(`Failed to load watch page: ${response.status}`);
      }
      const html = await response.text();
      const apiKey = matchFirst(html, [
        /"INNERTUBE_API_KEY":"([^"]+)"/,
        /["']INNERTUBE_API_KEY["']\s*:\s*"([^"]+)"/
      ]);
      const clientVersion = matchFirst(html, [
        /"INNERTUBE_CLIENT_VERSION":"([^"]+)"/,
        /["']INNERTUBE_CLIENT_VERSION["']\s*:\s*"([^"]+)"/
      ]);
      const stsRaw = matchFirst(html, [/"STS":(\d+)/, /["']STS["']\s*:\s*(\d+)/]);
      const visitorData = matchFirst(html, [
        /"VISITOR_DATA":"([^"]+)"/,
        /"visitorData":"([^"]+)"/,
        /["'](?:VISITOR_DATA|visitorData)["']\s*:\s*"([^"]+)"/
      ]);
      if (!apiKey || !clientVersion) {
        throw new Error(
          "Failed to extract required player context from watch page"
        );
      }
      const signatureTimestamp = stsRaw ? Number.parseInt(stsRaw, 10) : void 0;
      const context = {
        apiKey,
        clientVersion
      };
      if (typeof signatureTimestamp === "number" && Number.isFinite(signatureTimestamp)) {
        context.signatureTimestamp = signatureTimestamp;
      }
      if (visitorData) {
        context.visitorData = decodeEscapedJsonString(visitorData);
      }
      return context;
    }
    async fetchPlayerResponse(videoId, watchContext, requestedClient, signal) {
      const client = resolveInnertubeClient(
        requestedClient,
        watchContext,
        videoId
      );
      if (watchContext.visitorData) {
        client.visitorData = watchContext.visitorData;
      }
      const body = {
        context: {
          client
        },
        videoId,
        contentCheckOk: true,
        racyCheckOk: true
      };
      if (watchContext.signatureTimestamp) {
        body.playbackContext = {
          contentPlaybackContext: {
            signatureTimestamp: watchContext.signatureTimestamp
          }
        };
      }
      const endpoint = `${YT_BASE}/youtubei/v1/player?key=${encodeURIComponent(watchContext.apiKey)}`;
      const response = await this.fetchFn(endpoint, {
        method: "POST",
        headers: {
          ...DEFAULT_HEADERS,
          "content-type": "application/json",
          ...watchContext.visitorData ? { "x-goog-visitor-id": watchContext.visitorData } : {}
        },
        body: JSON.stringify(body),
        ...withSignal(signal)
      });
      if (!response.ok) {
        throw new Error(
          `Player API request failed with status ${response.status}`
        );
      }
      const json = await response.json();
      const hasFormats = Boolean(json.streamingData?.formats?.length);
      const hasAdaptiveFormats = Boolean(
        json.streamingData?.adaptiveFormats?.length
      );
      if (!hasFormats && !hasAdaptiveFormats) {
        throw new Error("Player response did not contain streaming formats");
      }
      return json;
    }
  };
  const DEFAULT_YT_AUDIO_QUALITY = "bestefficiency";
  const DEFAULT_FETCH_TIMEOUT_MS = 3e4;
  function assertValidChunkSize(chunkSize) {
    if (chunkSize <= 0) {
      throw new RangeError("Audio downloader. ytAudio. chunkSize must be > 0");
    }
  }
  function createYtAudioFetch({
    signal,
    timeoutMs
  }) {
    return async (input, init2 = {}) => await GM_fetch(input, {
      ...init2,
      signal: init2.signal ?? signal,
      forceGmXhr: true,
      timeout: timeoutMs
    });
  }
  function isWatchContextForbiddenError(error2) {
    if (error2 instanceof YtWatchContextForbiddenError) {
      return true;
    }
    return error2 instanceof Error && /failed to load watch page:\s*403/i.test(error2.message);
  }
  async function getAudioFromYtAudio({ videoId, signal }, deps = {}) {
    const chunkSize = deps.chunkSize ?? votConfig.minChunkSize;
    assertValidChunkSize(chunkSize);
    const fetchImplementation = createYtAudioFetch({
      signal,
      timeoutMs: deps.fetchTimeoutMs ?? DEFAULT_FETCH_TIMEOUT_MS
    });
    const downloader = deps.createDownloader?.(fetchImplementation) ?? new AudioDownloader$1({ fetchImplementation });
    try {
      const streamResult = await downloader.downloadAudioToChunkStream(
        {
          videoId,
          videoQuality: DEFAULT_YT_AUDIO_QUALITY,
          signal
        },
        { chunkSize }
      );
      return {
        fileId: makeFileId(
          AudioDownloadType.WEB_API_STEAL_SIG_AND_N,
          streamResult.itag,
          String(streamResult.fileSize),
          chunkSize
        ),
        mediaPartsLength: streamResult.mediaPartsLength,
        getMediaBuffers: streamResult.getMediaBuffers
      };
    } catch (error2) {
      if (isWatchContextForbiddenError(error2)) {
        throw error2;
      }
      console.warn(
        "[VOT] ytAudio streaming mode failed, falling back to buffered mode",
        error2
      );
    }
    const result = await downloader.downloadAudioToUint8Array({
      videoId,
      videoQuality: DEFAULT_YT_AUDIO_QUALITY,
      signal
    });
    const bytes = result.bytes;
    if (!bytes || bytes.byteLength === 0) {
      throw new Error("Audio downloader. ytAudio. Empty audio");
    }
    const mediaPartsLength = Math.max(1, Math.ceil(bytes.byteLength / chunkSize));
    const fileId = makeFileId(
      AudioDownloadType.WEB_API_STEAL_SIG_AND_N,
      0,
      String(bytes.byteLength),
      chunkSize
    );
    return {
      fileId,
      mediaPartsLength,
      async *getMediaBuffers() {
        for (let start = 0; start < bytes.byteLength; start += chunkSize) {
          const end = Math.min(start + chunkSize, bytes.byteLength);
          yield bytes.subarray(start, end);
        }
      }
    };
  }
  function makeSimpleFileId$2(size, chunkSize) {
    return `local_${size}_${chunkSize}_${Date.now()}`;
  }
  async function fetchLocalMedia(src, signal) {
    if (!src) {
      throw new Error("[VOT] Local file: empty media src");
    }
    if (src.startsWith("blob:")) {
      const res = await fetch(src, { signal });
      if (!res.ok) {
        throw new Error(
          `[VOT] Local file: failed to fetch blob media: ${res.status}`
        );
      }
      return res;
    }
    try {
      const res = await fetch(src, { signal });
      if (res.ok) {
        return res;
      }
    } catch {
    }
    const gmRes = await GM_fetch(src, { signal, timeout: 0 });
    if (!gmRes.ok) {
      throw new Error(
        `[VOT] Local file: failed to fetch media source: ${gmRes.status}`
      );
    }
    return gmRes;
  }
  function getContentLength(response) {
    const raw = response.headers.get("content-length") || response.headers.get("Content-Length");
    if (!raw) return null;
    const value = Number.parseInt(raw, 10);
    return Number.isFinite(value) && value > 0 ? value : null;
  }
  async function getAudioFromLocalFile({
    signal
  }) {
    const video = document.querySelector("video");
    if (!(video instanceof HTMLVideoElement)) {
      throw new Error("[VOT] Local file: video element not found");
    }
    const sourceEl = video.querySelector("source");
    const src = video.currentSrc || video.src || sourceEl?.src || sourceEl?.getAttribute("src") || "";
    if (!src) {
      throw new Error("[VOT] Local file: empty video src");
    }
    const response = await fetchLocalMedia(src, signal);
    const chunkSize = 256 * 1024;
    const contentLength = getContentLength(response);
    if (response.body && contentLength) {
      const mediaPartsLength2 = Math.max(1, Math.ceil(contentLength / chunkSize));
      const fileId2 = makeSimpleFileId$2(contentLength, chunkSize);
      return {
        fileId: fileId2,
        mediaPartsLength: mediaPartsLength2,
        async *getMediaBuffers() {
          const reader = response.body?.getReader();
          let pending = new Uint8Array(0);
          try {
            while (true) {
              const { done, value } = await reader.read();
              if (done) break;
              if (!value?.byteLength) continue;
              let merged;
              if (pending.byteLength === 0) {
                merged = value;
              } else {
                merged = new Uint8Array(pending.byteLength + value.byteLength);
                merged.set(pending, 0);
                merged.set(value, pending.byteLength);
              }
              let offset = 0;
              while (merged.byteLength - offset >= chunkSize) {
                yield merged.subarray(offset, offset + chunkSize);
                offset += chunkSize;
              }
              pending = offset < merged.byteLength ? merged.slice(offset) : new Uint8Array(0);
            }
            if (pending.byteLength) {
              yield pending;
            }
          } finally {
            reader.releaseLock();
          }
        }
      };
    }
    const buffer = await response.arrayBuffer();
    const bytes = new Uint8Array(buffer);
    if (!bytes.byteLength) {
      throw new Error("[VOT] Local file: empty media bytes");
    }
    const mediaPartsLength = Math.max(1, Math.ceil(bytes.byteLength / chunkSize));
    const fileId = makeSimpleFileId$2(bytes.byteLength, chunkSize);
    return {
      fileId,
      mediaPartsLength,
      async *getMediaBuffers() {
        for (let start = 0; start < bytes.byteLength; start += chunkSize) {
          const end = Math.min(start + chunkSize, bytes.byteLength);
          yield bytes.subarray(start, end);
        }
      }
    };
  }
  const manifestPatterns = [
    /\.m3u8(?:$|[?#])/i,
    /master\.m3u8/i,
    /manifest/i,
    /dashplaylist/i,
    /\.mp4(?:$|[?#])/i
  ];
  function isManifestUrl(url) {
    return manifestPatterns.some((re) => re.test(url));
  }
  let bestManifest = null;
  let installed$1 = false;
  function normalizeUrl$1(input) {
    try {
      return new URL(input, globalThis.location.href).href;
    } catch {
      return input;
    }
  }
  function isDirectMediaCandidate(url) {
    return /\.m3u8(?:$|[?#])/i.test(url) || /master\.m3u8/i.test(url) || /dashplaylist/i.test(url) || /\.mp4(?:$|[?#])/i.test(url);
  }
  function isBadSegmentUrl(url) {
    const lower = url.toLowerCase();
    if (isDirectMediaCandidate(lower)) {
      return false;
    }
    return lower.includes("okcdn.ru/?") || /[?&]bytes=\d+-\d+/i.test(lower) || /[?&]type=\d+/i.test(lower);
  }
  function rememberManifest(url) {
    const normalized = normalizeUrl$1(url);
    if (isBadSegmentUrl(normalized)) {
      return;
    }
    if (!isManifestUrl(normalized)) {
      return;
    }
    console.log("[VOT][manifestSniffer] candidate", normalized);
    if (!bestManifest) {
      bestManifest = { url: normalized, seenAt: Date.now() };
      console.log("[VOT][manifestSniffer] selected", bestManifest.url);
      return;
    }
    const currentScore = scoreManifestUrl(bestManifest.url);
    const nextScore = scoreManifestUrl(normalized);
    if (nextScore >= currentScore) {
      bestManifest = { url: normalized, seenAt: Date.now() };
      console.log("[VOT][manifestSniffer] selected", bestManifest.url);
    }
  }
  function scoreManifestUrl(url) {
    let score = 0;
    if (/\.mp4(?:$|[?#])/i.test(url)) score += 5;
    if (/\.m3u8(?:$|[?#])/i.test(url)) score += 4;
    if (/master\.m3u8/i.test(url)) score += 3;
    if (/\.mpd(?:$|[?#])/i.test(url)) {
      return Number.NEGATIVE_INFINITY;
    }
    if (/manifest/i.test(url)) score += 1;
    if (/dashplaylist/i.test(url)) score += 1;
    if (/vkvd\d+\.okcdn\.ru|\.okcdn\.ru|vkvideo\.ru/i.test(url)) {
      score += 2;
    }
    return score;
  }
  function getLastManifestUrl() {
    return bestManifest?.url ?? "";
  }
  const DIRECT_SOURCES_KEY = "__VOT_DIRECT_SOURCES__";
  function tryInjectDirectSources(text) {
    try {
      const data = JSON.parse(text);
      if (!data || typeof data !== "object") return;
      const hasId = "unitedVideoId" in data || "video" in data && typeof data.video === "object";
      if (!hasId) return;
      const existing = globalThis[DIRECT_SOURCES_KEY];
      if (existing && typeof existing === "object") return;
      globalThis[DIRECT_SOURCES_KEY] = data;
      console.log("[VOT][manifestSniffer] injected __VOT_DIRECT_SOURCES__", data);
    } catch {
    }
  }
  function installManifestSniffer() {
    if (installed$1) {
      return;
    }
    installed$1 = true;
    const originalFetch = globalThis.fetch.bind(globalThis);
    globalThis.fetch = async (...args) => {
      const input = args[0];
      const url = typeof input === "string" ? input : input instanceof Request ? input.url : String(input ?? "");
      rememberManifest(url);
      const response = await originalFetch(...args);
      if (response.headers.get("content-type")?.includes("application/json")) {
        response.clone().text().then(tryInjectDirectSources).catch(() => {
        });
      }
      return response;
    };
    const originalOpen = XMLHttpRequest.prototype.open;
    XMLHttpRequest.prototype.open = function(method, url, ...rest) {
      rememberManifest(String(url));
      this.addEventListener("load", function() {
        const ct = this.getResponseHeader("content-type") ?? "";
        if (!ct.includes("application/json") && !ct.includes("text/javascript")) {
          return;
        }
        if (this.responseType !== "" && this.responseType !== "text") {
          return;
        }
        try {
          if (typeof this.responseText === "string") {
            tryInjectDirectSources(this.responseText);
          }
        } catch {
        }
      });
      return originalOpen.call(this, method, url, ...rest);
    };
  }
  const VK_PLAYER_SELECTOR = ".videoplayer_media, vk-video-player";
  function makeSimpleFileId$1(size, chunkSize) {
    return `vk_${size}_${chunkSize}_${Date.now()}`;
  }
  function isM3u8(url) {
    return /\.m3u8(?:$|[?#])/i.test(url);
  }
  function resolveUrl(url, baseUrl) {
    return new URL(url, baseUrl).toString();
  }
  function getVideoSrc(video) {
    const sourceEl = video.querySelector("source");
    return String(
      video.currentSrc || video.src || sourceEl?.src || sourceEl?.getAttribute("src") || ""
    ).trim();
  }
  function isVisibleVideo(video) {
    const rect = video.getBoundingClientRect();
    return rect.width > 64 && rect.height > 64;
  }
  function collectVideoCandidates(preferredVideo) {
    const seen = new Set();
    const result = [];
    const push = (video) => {
      if (!(video instanceof HTMLVideoElement) || seen.has(video)) {
        return;
      }
      seen.add(video);
      result.push(video);
    };
    push(preferredVideo);
    for (const video of Array.from(document.querySelectorAll("video"))) {
      push(video);
    }
    return result;
  }
  function scoreVideoCandidate(video, preferredVideo) {
    let score = 0;
    const src = getVideoSrc(video);
    if (video === preferredVideo) score += 100;
    if (video.isConnected) score += 10;
    if (isVisibleVideo(video)) score += 25;
    if (video.closest(VK_PLAYER_SELECTOR)) score += 20;
    if (!video.paused) score += 8;
    if (video.readyState > 0) score += 8;
    if (src) score += 4;
    if (src && !src.startsWith("blob:")) score += 6;
    return score;
  }
  function normalizeCandidateUrl(url) {
    try {
      return new URL(url, globalThis.location.href).toString();
    } catch {
      return String(url || "").trim();
    }
  }
  function scoreVkMediaUrl(url) {
    const normalized = normalizeCandidateUrl(url);
    if (!normalized) {
      return Number.NEGATIVE_INFINITY;
    }
    const lower = normalized.toLowerCase();
    let score = 0;
    if (lower.startsWith("blob:")) score -= 100;
    if (/\.mp4(?:$|[?#])/i.test(normalized)) score += 50;
    if (/\.m3u8(?:$|[?#])/i.test(normalized)) score += 45;
    if (/master\.m3u8/i.test(normalized)) score += 35;
    if (/\.mpd(?:$|[?#])/i.test(normalized)) {
      return Number.NEGATIVE_INFINITY;
    }
    if (/manifest/i.test(normalized)) score += 15;
    if (/dashplaylist/i.test(normalized)) score += 15;
    if (/vkvd\d+\.okcdn\.ru|\.okcdn\.ru|vkvideo\.ru/i.test(normalized))
      score += 10;
    if (/[?&]bytes=\d+-\d+/i.test(normalized)) score -= 60;
    if (/[?&]subid=/i.test(lower)) score -= 80;
    if (/[?&]type=2(?:[&#]|$)/i.test(normalized)) score -= 80;
    return score;
  }
  function pickBestVkMediaUrl(candidates) {
    let best = "";
    let bestScore = Number.NEGATIVE_INFINITY;
    for (const candidate of candidates) {
      const normalized = normalizeCandidateUrl(String(candidate || "").trim());
      const score = scoreVkMediaUrl(normalized);
      if (score > bestScore) {
        best = normalized;
        bestScore = score;
      }
    }
    return best;
  }
  function stripBytesParam(url) {
    try {
      const u2 = new URL(url);
      u2.searchParams.delete("bytes");
      return u2.toString();
    } catch {
      return url;
    }
  }
  function getPerformanceMediaUrl() {
    try {
      const entries = performance.getEntriesByType("resource");
      const candidates = entries.map((entry) => {
        const raw = String(
          entry?.name || ""
        ).trim();
        return /[?&]bytes=\d+-\d+/i.test(raw) ? stripBytesParam(raw) : raw;
      }).filter(
        (candidate) => /vkvd\d+\.okcdn\.ru|\.okcdn\.ru|vkvideo\.ru/i.test(candidate)
      ).filter(
        (candidate) => /\.mp4(?:$|[?#])|\.m3u8(?:$|[?#])|[?&]type=1(?:[&#]|$)/i.test(
          candidate
        )
      ).filter(Boolean);
      return pickBestVkMediaUrl(candidates);
    } catch {
      return "";
    }
  }
  async function fetchVkMedia(src, signal) {
    const isVkCdn = /(?:^|\.)okcdn\.ru/i.test(src) || /vkvd\d+\.okcdn\.ru/i.test(src) || /vkvideo\.ru/i.test(src);
    if (isVkCdn) {
      const gmRes2 = await GM_fetch(src, {
        signal,
        timeout: 0
      });
      if (!gmRes2.ok) {
        throw new Error(
          `[VOT] VK: failed to fetch media source via GM_fetch: ${gmRes2.status}`
        );
      }
      return gmRes2;
    }
    try {
      const res = await fetch(src, {
        signal,
        credentials: "include"
      });
      if (res.ok) return res;
    } catch {
    }
    const gmRes = await GM_fetch(src, {
      signal,
      timeout: 0
    });
    if (!gmRes.ok) {
      throw new Error(`[VOT] VK: failed to fetch media source: ${gmRes.status}`);
    }
    return gmRes;
  }
  async function fetchText(src, signal) {
    const res = await fetchVkMedia(src, signal);
    return await res.text();
  }
  async function fetchBytes(src, signal) {
    const res = await fetchVkMedia(src, signal);
    const buffer = await res.arrayBuffer();
    return new Uint8Array(buffer);
  }
  function parseM3u8Urls(text, baseUrl) {
    return text.split(/\r?\n/).map((line) => line.trim()).filter((line) => line && !line.startsWith("#")).map((line) => resolveUrl(line, baseUrl));
  }
  async function resolveM3u8Segments(manifestUrl, signal) {
    const manifestText = await fetchText(manifestUrl, signal);
    const urls = parseM3u8Urls(manifestText, manifestUrl);
    const nestedManifest = urls.find((url) => isM3u8(url));
    if (nestedManifest) {
      const nestedText = await fetchText(nestedManifest, signal);
      return parseM3u8Urls(nestedText, nestedManifest).filter(
        (url) => !isM3u8(url)
      );
    }
    return urls.filter((url) => !isM3u8(url));
  }
  async function getAudioFromVkVideo({
    videoId,
    signal,
    preferredVideo
  }) {
    const videos = collectVideoCandidates(preferredVideo).sort(
      (left, right) => scoreVideoCandidate(right, preferredVideo) - scoreVideoCandidate(left, preferredVideo)
    );
    const video = videos[0];
    if (!(video instanceof HTMLVideoElement)) {
      throw new Error("[VOT] VK: video element not found");
    }
    const sniffedManifestUrl = getLastManifestUrl();
    const performanceMediaUrl = getPerformanceMediaUrl();
    const selectedVideoSrc = getVideoSrc(video);
    const directVideoUrls = videos.map((candidate) => getVideoSrc(candidate)).filter((candidate) => candidate && !candidate.startsWith("blob:"));
    const src = pickBestVkMediaUrl([
      sniffedManifestUrl,
      performanceMediaUrl,
      ...directVideoUrls,
      selectedVideoSrc
    ]);
    debug.log("[VOT] VK strategy videoId:", videoId);
    debug.log("[VOT] VK strategy manifest:", sniffedManifestUrl);
    debug.log("[VOT] VK strategy performance media:", performanceMediaUrl);
    debug.log("[VOT] VK strategy currentSrc:", video.currentSrc);
    debug.log("[VOT] VK strategy src:", video.src);
    debug.log("[VOT] VK strategy selected video src:", selectedVideoSrc);
    debug.log(
      "[VOT] VK strategy candidate videos:",
      videos.map((candidate) => ({
        src: getVideoSrc(candidate),
        visible: isVisibleVideo(candidate),
        paused: candidate.paused,
        readyState: candidate.readyState,
        score: scoreVideoCandidate(candidate, preferredVideo)
      }))
    );
    debug.log("[VOT] VK strategy selected src:", src);
    if (!src) {
      throw new Error("[VOT] VK: empty video src");
    }
    if (src.startsWith("blob:")) {
      throw new Error(
        "[VOT] VK: blob source detected; need direct mp4/m3u8 URL from VK player/network"
      );
    }
    const chunkSize = 256 * 1024;
    if (isM3u8(src)) {
      const segmentUrls = await resolveM3u8Segments(src, signal);
      if (!segmentUrls.length) {
        throw new Error("[VOT] VK: empty m3u8 segment list");
      }
      const fileId2 = `vk_hls_${Date.now()}`;
      debug.log("[VOT] VK strategy m3u8 segments:", segmentUrls.length);
      return {
        fileId: fileId2,
        mediaPartsLength: segmentUrls.length,
        async *getMediaBuffers() {
          for (const segmentUrl of segmentUrls) {
            const bytes2 = await fetchBytes(segmentUrl, signal);
            if (!bytes2.byteLength) {
              throw new Error("[VOT] VK: empty m3u8 segment");
            }
            yield bytes2;
          }
        }
      };
    }
    const bytes = await fetchBytes(src, signal);
    if (!bytes.byteLength) {
      throw new Error("[VOT] VK: empty media bytes");
    }
    const mediaPartsLength = Math.max(1, Math.ceil(bytes.byteLength / chunkSize));
    const fileId = makeSimpleFileId$1(bytes.byteLength, chunkSize);
    debug.log("[VOT] VK strategy bytes:", bytes.byteLength);
    debug.log("[VOT] VK strategy mediaPartsLength:", mediaPartsLength);
    return {
      fileId,
      mediaPartsLength,
      async *getMediaBuffers() {
        for (let start = 0; start < bytes.byteLength; start += chunkSize) {
          const end = Math.min(start + chunkSize, bytes.byteLength);
          yield bytes.subarray(start, end);
        }
      }
    };
  }
  function makeSimpleFileId(size, chunkSize) {
    return `yadisk_${size}_${chunkSize}_${Date.now()}`;
  }
  async function getAudioFromYandexDisk({
    videoId,
    signal
  }) {
    const video = document.querySelector("video");
    if (!(video instanceof HTMLVideoElement)) {
      throw new Error("[VOT] Yandex Disk: video element not found");
    }
    const src = video.currentSrc || video.src;
    debug.log("[VOT] Yandex Disk strategy video src:", src);
    if (!src) {
      throw new Error("[VOT] Yandex Disk: empty video src");
    }
    const response = await fetch(src, { signal });
    if (!response.ok) {
      throw new Error(
        `[VOT] Yandex Disk: failed to fetch media source: ${response.status}`
      );
    }
    debug.log("[VOT] Yandex Disk strategy videoId:", videoId);
    debug.log("[VOT] Yandex Disk strategy currentSrc:", video.currentSrc);
    debug.log("[VOT] Yandex Disk strategy src:", video.src);
    const buffer = await response.arrayBuffer();
    const bytes = new Uint8Array(buffer);
    if (!bytes.byteLength) {
      throw new Error("[VOT] Yandex Disk: empty audio/video bytes");
    }
    const chunkSize = 256 * 1024;
    const mediaPartsLength = Math.max(1, Math.ceil(bytes.byteLength / chunkSize));
    const fileId = makeSimpleFileId(bytes.byteLength, chunkSize);
    debug.log("[VOT] Yandex Disk strategy bytes:", bytes.byteLength);
    debug.log("[VOT] Yandex Disk strategy mediaPartsLength:", mediaPartsLength);
    return {
      fileId,
      mediaPartsLength,
      async *getMediaBuffers() {
        for (let start = 0; start < bytes.byteLength; start += chunkSize) {
          const end = Math.min(start + chunkSize, bytes.byteLength);
          yield bytes.subarray(start, end);
        }
      }
    };
  }
  const YT_AUDIO_STRATEGY = "ytAudio";
  const VK_AUDIO_STRATEGY = "vkAudio";
  const strategies = {
    [YT_AUDIO_STRATEGY]: getAudioFromYtAudio,
    [VK_AUDIO_STRATEGY]: getAudioFromVkVideo,
    yandexDisk: getAudioFromYandexDisk,
    localFile: getAudioFromLocalFile
  };
  function assertValidMediaPartsLength(mediaPartsLength) {
    if (!Number.isInteger(mediaPartsLength) || mediaPartsLength < 1) {
      throw new Error("Audio downloader. Invalid media parts length");
    }
  }
  function assertHasAudioChunk(chunk) {
    if (!chunk || chunk.byteLength === 0) {
      throw new Error("Audio downloader. Empty audio");
    }
    return chunk;
  }
  async function handleCommonAudioDownloadRequest({
    audioDownloader,
    translationId,
    videoId,
    signal,
    preferredVideo
  }) {
    const audioData = await strategies[audioDownloader.strategy]({
      videoId,
      signal,
      preferredVideo
    });
    if (!audioData) {
      throw new Error("Audio downloader. Can not get audio data");
    }
    debug.log("Audio downloader. Url found", {
      audioDownloadType: audioDownloader.strategy
    });
    const { getMediaBuffers, mediaPartsLength, fileId } = audioData;
    assertValidMediaPartsLength(mediaPartsLength);
    if (mediaPartsLength < 2) {
      const iterator = getMediaBuffers();
      const { value } = await iterator.next();
      const singleChunk = assertHasAudioChunk(value);
      await audioDownloader.onDownloadedAudio.dispatchAsync(translationId, {
        videoId,
        fileId,
        audioData: singleChunk
      });
      return;
    }
    let index = 0;
    for await (const audioChunk of getMediaBuffers()) {
      const chunk = assertHasAudioChunk(audioChunk);
      await audioDownloader.onDownloadedPartialAudio.dispatchAsync(
        translationId,
        {
          videoId,
          fileId,
          audioData: chunk,
          version: 1,
          index,
          amount: mediaPartsLength
        }
      );
      index++;
    }
    if (index !== mediaPartsLength) {
      throw new Error(
        `Audio downloader. Expected ${mediaPartsLength} chunks, got ${index}`
      );
    }
  }
  class AudioDownloader2 {
    onDownloadedAudio = new EventImpl();
    onDownloadedPartialAudio = new EventImpl();
    onDownloadAudioError = new EventImpl();
    strategy;
    constructor(strategy = YT_AUDIO_STRATEGY) {
      this.strategy = strategy;
      debug.log("Audio downloader created", {
        strategy
      });
    }
    async runAudioDownload(videoId, translationId, signal, preferredVideo) {
      try {
        await handleCommonAudioDownloadRequest({
          audioDownloader: this,
          translationId,
          videoId,
          signal,
          preferredVideo
        });
        debug.log("Audio downloader. Audio download finished", {
          videoId
        });
      } catch (err) {
        debug.error("Audio downloader. Failed to download audio", {
          videoId,
          error: err instanceof Error ? err.message : String(err)
        });
        this.onDownloadAudioError.dispatch(videoId);
      }
    }
    addEventListener(type, listener) {
      switch (type) {
        case "downloadedAudio":
          this.onDownloadedAudio.addListener(listener);
          break;
        case "downloadedPartialAudio":
          this.onDownloadedPartialAudio.addListener(listener);
          break;
        case "downloadAudioError":
          this.onDownloadAudioError.addListener(listener);
          break;
      }
      return this;
    }
    removeEventListener(type, listener) {
      switch (type) {
        case "downloadedAudio":
          this.onDownloadedAudio.removeListener(listener);
          break;
        case "downloadedPartialAudio":
          this.onDownloadedPartialAudio.removeListener(listener);
          break;
        case "downloadAudioError":
          this.onDownloadAudioError.removeListener(listener);
          break;
      }
      return this;
    }
  }
  const MAX_SECS_FRACTION = 0.66;
  const LOCAL_UPLOAD_DISPLAY_ETA_MULTIPLIER = 2.3;
  function adjustTranslationEtaForDisplay(secs2, options = {}) {
    const safeSecs = Number.isFinite(secs2) ? Math.max(0, secs2) : 0;
    if (!options.optimisticLocalUpload) {
      return safeSecs;
    }
    return Math.ceil(safeSecs * LOCAL_UPLOAD_DISPLAY_ETA_MULTIPLIER);
  }
  function formatTranslationEta(secs2, getMessage) {
    let minutes = Math.floor(secs2 / 60);
    const seconds = Math.floor(secs2 % 60);
    const fraction = seconds / 60;
    if (fraction >= MAX_SECS_FRACTION) {
      minutes += 1;
    }
    if (minutes >= 60) {
      return getMessage("translationTakeMoreThanHour");
    }
    if (minutes <= 1) {
      return getMessage("translationTakeAboutMinute");
    }
    const minutesStr = String(minutes);
    if (minutes !== 11 && minutes % 10 === 1) {
      return getMessage("translationTakeApproximatelyMinute2").replace(
        "{0}",
        minutesStr
      );
    }
    if (![12, 13, 14].includes(minutes) && [2, 3, 4].includes(minutes % 10)) {
      return getMessage("translationTakeApproximatelyMinute").replace(
        "{0}",
        minutesStr
      );
    }
    return getMessage("translationTakeApproximatelyMinutes").replace(
      "{0}",
      minutesStr
    );
  }
  class VOTLocalizedError extends Error {
    name = "VOTLocalizedError";
unlocalizedMessage;
localizedMessage;
    constructor(message) {
      super(localizationProvider.getDefault(message));
      this.unlocalizedMessage = message;
      this.localizedMessage = localizationProvider.get(message);
    }
  }
  function normalizeTranslationHelp(translationHelp) {
    return translationHelp ?? null;
  }
  async function requestTranslationAudio(requester, options) {
    const response = await requester.translateVideoImpl(
      options.videoData,
      options.requestLang,
      options.responseLang,
      normalizeTranslationHelp(options.translationHelp),
      !options.useAudioDownload,
      options.signal
    );
    if (!response?.url) {
      return null;
    }
    return {
      url: response.url,
      usedLivelyVoice: Boolean(response.usedLivelyVoice)
    };
  }
  function buildTranslationCacheValue(options) {
    return {
      videoId: options.videoId,
      from: options.requestLang,
      to: options.responseLang,
      url: options.downloadTranslationUrl ?? options.fallbackUrl,
      useLivelyVoice: options.usedLivelyVoice
    };
  }
  async function updateTranslationAndSchedule(options) {
    if (options.isActionStale(options.actionContext)) {
      return false;
    }
    await options.updateTranslation(options.url, options.actionContext);
    if (options.isActionStale(options.actionContext)) {
      return false;
    }
    options.scheduleTranslationRefresh();
    return true;
  }
  async function requestAndApplyTranslation(options) {
    const translateRes = await requestTranslationAudio(options.requester, {
      videoData: options.request.videoData,
      requestLang: options.request.requestLang,
      responseLang: options.request.responseLang,
      translationHelp: options.request.translationHelp,
      useAudioDownload: options.request.useAudioDownload,
      signal: options.request.signal
    });
    if (!translateRes) {
      return null;
    }
    const updated = await updateTranslationAndSchedule({
      url: translateRes.url,
      actionContext: options.actionContext,
      isActionStale: options.isActionStale,
      updateTranslation: options.updateTranslation,
      scheduleTranslationRefresh: options.scheduleTranslationRefresh
    });
    if (!updated || options.isActionStale(options.actionContext)) {
      return null;
    }
    return translateRes;
  }
  function setTranslationCacheValue(options) {
    options.setTranslation(
      options.cacheKey,
      buildTranslationCacheValue({
        videoId: options.videoId,
        requestLang: options.requestLang,
        responseLang: options.responseLang,
        fallbackUrl: options.fallbackUrl,
        downloadTranslationUrl: options.downloadTranslationUrl,
        usedLivelyVoice: options.usedLivelyVoice
      })
    );
  }
  function notifyTranslationFailureIfNeeded(options) {
    if (options.aborted || !options.translateApiErrorsEnabled || !options.hadAsyncWait) {
      return options.hadAsyncWait;
    }
    options.notify({
      videoId: options.videoId,
      message: options.error
    });
    return false;
  }
  function asVotClientErrorShape(value) {
    if (!value || typeof value !== "object") {
      return null;
    }
    const candidate = value;
    const data = candidate.data && typeof candidate.data === "object" ? candidate.data : void 0;
    return {
      name: candidate.name,
      message: candidate.message,
      data
    };
  }
  function getServerErrorMessage(value) {
    const err = asVotClientErrorShape(value);
    const message = err?.data?.message;
    return typeof message === "string" && message.length > 0 ? message : void 0;
  }
  function isCompletedTranslationResponse(response) {
    return Boolean(
      response.translated && (response.status === VideoTranslationStatus.FINISHED || response.status === VideoTranslationStatus.PART_CONTENT) && typeof response.url === "string" && response.url.length > 0
    );
  }
  function mapVotClientErrorForUi(error2, siteHost) {
    const err = asVotClientErrorShape(error2);
    if (!err) {
      return error2;
    }
    if (err.name !== "VOTJSError") {
      return error2;
    }
    const message = typeof err.message === "string" ? err.message : "";
    const serverMessage = typeof err.data?.message === "string" ? err.data.message : "";
    console.log("[VOT][mapVotClientErrorForUi]", {
      siteHost,
      originalMessage: message,
      serverMessage,
      rawError: error2
    });
    if (message === "Audio link wasn't received" || message === "Audio link wasn't received from VOT response") {
      return new VOTLocalizedError("audioNotReceived");
    }
    if (siteHost === "yandexdisk") {
      if (serverMessage) {
        return new Error(serverMessage);
      }
      return error2;
    }
    if (message === "Failed to request video translation") {
      return new VOTLocalizedError("requestTranslationFailed");
    }
    if (message === "Yandex couldn't translate video") {
      return new VOTLocalizedError("requestTranslationFailed");
    }
    return error2;
  }
  class VOTTranslationHandler {
    videoHandler;
    audioDownloader;
    downloading;
    downloadWaiters = new Set();
    requestedFailAudio = new Set();
    activeTranslationUrl;
    activeYandexDiskResolvedVideoData;
    constructor(videoHandler) {
      this.videoHandler = videoHandler;
      const strategy = this.videoHandler.site.host === "vk" ? VK_AUDIO_STRATEGY : this.videoHandler.site.host === "youtube" ? YT_AUDIO_STRATEGY : this.videoHandler.site.host === "yandexdisk" ? "yandexDisk" : this.videoHandler.site.host === "custom" ? "localFile" : YT_AUDIO_STRATEGY;
      this.audioDownloader = new AudioDownloader2(strategy);
      this.downloading = false;
      this.audioDownloader.addEventListener("downloadedAudio", this.onDownloadedAudio).addEventListener("downloadedPartialAudio", this.onDownloadedPartialAudio).addEventListener("downloadAudioError", this.onDownloadAudioError);
    }
    normalizeUrlForRequest(raw) {
      try {
        return new URL(raw, globalThis.location.href).toString();
      } catch {
        return String(raw || "");
      }
    }
    normalizeOdyseeDirectUrl(url) {
      try {
        if (!url) {
          return url;
        }
        const m2 = url.match(
          /^https:\/\/player\.odycdn\.com\/api\/v3\/streams\/free\/[^/]+\/([a-f0-9]+)\/([^/?#]+\.mp4)(?:[?#].*)?$/i
        );
        if (m2) {
          const claimId = m2[1];
          const fileName = m2[2];
          return `https://player.odycdn.com/v6/streams/${claimId}/${fileName}`;
        }
      } catch {
      }
      return url;
    }
    getCurrentMediaRequestUrl(videoData2) {
      return this.normalizeUrlForRequest(
        String(
          videoData2?.url || this.videoHandler.video?.currentSrc || this.videoHandler.video?.src || globalThis.location.href
        )
      );
    }
    isHlsManifestUrl(url) {
      return /\.m3u8(?:[?#]|$)/i.test(String(url || ""));
    }
    isCustomLinkUrl(url) {
      if (!url) {
        return false;
      }
      try {
        return this.videoHandler.votClient.isCustomLink(url);
      } catch {
        return /\.(m3u8|m4(?:a|v)|mpd)(?:[?#]|$)/i.test(url) || /^https:\/\/cdn\.qstv\.on\.epicgames\.com/i.test(url);
      }
    }
    shouldUseCustomLinkWorkflow(videoData2) {
      if (!videoData2) {
        return false;
      }
      if (this.videoHandler.site.host !== "custom" && videoData2.host !== "custom") {
        return false;
      }
      return this.isCustomLinkUrl(this.getCurrentMediaRequestUrl(videoData2));
    }
    getM3u8ProxyEndpointUrl() {
      const rawHost = String(this.videoHandler.data?.m3u8ProxyHost || "").trim();
      if (!rawHost) {
        return null;
      }
      const withScheme = /^[a-z][a-z\d+.-]*:/i.test(rawHost) ? rawHost : `https://${rawHost}`;
      try {
        const parsed = new URL(withScheme);
        const normalizedPath = parsed.pathname && parsed.pathname !== "/" ? parsed.pathname.replace(/\/+$/g, "") : "/v1/proxy/m3u8";
        return new URL(`${parsed.origin}${normalizedPath}`);
      } catch (error2) {
        console.log("[VOT][upload] invalid m3u8 proxy host", {
          rawHost,
          error: error2
        });
        return null;
      }
    }
    buildProxiedHlsUrl(rawUrl) {
      const normalizedTargetUrl = this.normalizeUrlForRequest(rawUrl);
      const proxyEndpoint = this.getM3u8ProxyEndpointUrl();
      if (!proxyEndpoint) {
        return normalizedTargetUrl;
      }
      try {
        const target = new URL(normalizedTargetUrl, globalThis.location.href);
        const proxyUrl = new URL(proxyEndpoint.toString());
        proxyUrl.searchParams.set("format", "base64");
        proxyUrl.searchParams.set("force", "true");
        proxyUrl.searchParams.set("all", "1");
        proxyUrl.searchParams.set("url", btoa(target.toString()));
        if (target.origin && target.origin !== "null") {
          proxyUrl.searchParams.set("origin", target.origin);
          proxyUrl.searchParams.set("referer", target.origin);
        }
        proxyUrl.hash = "playlist.m3u8";
        return proxyUrl.toString();
      } catch (error2) {
        console.log("[VOT][upload] failed to build proxied hls url", {
          rawUrl,
          error: error2
        });
        return normalizedTargetUrl;
      }
    }
    buildCustomLinkWorkflowVideoData(videoData2) {
      const currentUrl = this.getCurrentMediaRequestUrl(videoData2);
      const requestUrl = this.isHlsManifestUrl(currentUrl) ? this.buildProxiedHlsUrl(currentUrl) : currentUrl;
      const videoId = typeof videoData2.videoId === "string" && videoData2.videoId.trim().length > 0 ? videoData2.videoId : currentUrl;
      console.log("[VOT][upload] custom-link workflow input", {
        originalUrl: currentUrl,
        requestUrl,
        host: videoData2.host,
        proxied: requestUrl !== currentUrl,
        proxyHost: this.videoHandler.data?.m3u8ProxyHost
      });
      return {
        ...videoData2,
        url: requestUrl,
        videoId
      };
    }
    isDirectMediaUrlCandidate(url) {
      if (!url) {
        return false;
      }
      if (this.isHlsManifestUrl(url)) {
        return false;
      }
      try {
        return this.videoHandler.votClient.isDirectMediaUrl(url);
      } catch {
        return false;
      }
    }
    isCrossOriginMediaUrl(url) {
      try {
        const parsed = new URL(url, globalThis.location.href);
        return parsed.hostname !== globalThis.location.hostname;
      } catch {
        return false;
      }
    }
    shouldUseLocalFileWorkflow(videoData2) {
      if (!videoData2) {
        return false;
      }
      const url = this.getCurrentMediaRequestUrl(videoData2);
      if (!this.isDirectMediaUrlCandidate(url)) {
        return false;
      }
      if (this.videoHandler.site.host === "custom" || videoData2.host === "custom") {
        return true;
      }
      return this.isCrossOriginMediaUrl(url);
    }
    buildLocalFileWorkflowVideoData(videoData2) {
      let url = this.getCurrentMediaRequestUrl(videoData2);
      if (this.videoHandler.site.host === "odysee" || videoData2.host === "odysee") {
        url = this.normalizeUrlForRequest(this.normalizeOdyseeDirectUrl(url));
      }
      const videoId = typeof videoData2.videoId === "string" && videoData2.videoId.trim().length > 0 ? videoData2.videoId : url;
      return {
        ...videoData2,
        host: "custom",
        url,
        videoId
      };
    }
    updateAudioDownloaderStrategy(videoData2) {
      const url = videoData2 ? this.getCurrentMediaRequestUrl(videoData2) : "";
      const isLocalFileCompatibleCustom = (this.videoHandler.site.host === "custom" || videoData2?.host === "custom") && !this.isHlsManifestUrl(url) && this.isDirectMediaUrlCandidate(url);
      const useLocalFileWorkflow = isLocalFileCompatibleCustom || this.shouldUseLocalFileWorkflow(videoData2);
      const isVkCdnContext = this.videoHandler.site.host === "vk" || this.videoHandler.site.host === "okru" || /^player\.cdnvideohub\.com$/i.test(globalThis.location.hostname) || /(?:^|\.)okcdn\.ru$/i.test(globalThis.location.hostname) || /(?:^|\.)okcdn\.ru/i.test(url);
      const nextStrategy = useLocalFileWorkflow ? "localFile" : isVkCdnContext ? VK_AUDIO_STRATEGY : this.videoHandler.site.host === "yandexdisk" ? "yandexDisk" : YT_AUDIO_STRATEGY;
      if (this.audioDownloader.strategy === nextStrategy) {
        return;
      }
      this.audioDownloader.strategy = nextStrategy;
      console.log("[VOT][audio] switched downloader strategy", {
        siteHost: this.videoHandler.site.host,
        videoHost: videoData2?.host,
        strategy: nextStrategy,
        url: videoData2?.url
      });
    }
    isDirectResolvedUploadVideoData(data) {
      if (!data) {
        return false;
      }
      const rawUrl = String(data.url || "");
      if (!rawUrl.length || this.isYandexDiskDownloadUrl(rawUrl)) {
        return false;
      }
      if (data.host === "yandexdisk") {
        return true;
      }
      if (data.host === "custom") {
        try {
          return this.videoHandler.votClient.isDirectMediaUrl(rawUrl);
        } catch {
          return false;
        }
      }
      return false;
    }
    parseYandexDiskUrl(rawUrl) {
      const fallback = String(rawUrl || globalThis.location.href || "");
      try {
        const parsed = new URL(fallback, globalThis.location.href);
        const pathname = parsed.pathname || "/";
        const fileMatch = pathname.match(/^\/i\/([^/]+)$/i);
        if (fileMatch) {
          return {
            mode: "file",
            origin: parsed.origin,
            pathname,
            fileId: fileMatch[1]
          };
        }
        const folderRootMatch = pathname.match(/^\/d\/([^/]+)\/?$/i);
        if (folderRootMatch) {
          return {
            mode: "folderRoot",
            origin: parsed.origin,
            pathname,
            folderId: folderRootMatch[1]
          };
        }
        const folderFileMatch = pathname.match(/^\/d\/([^/]+)\/.+$/i);
        if (folderFileMatch) {
          return {
            mode: "folderFile",
            origin: parsed.origin,
            pathname,
            folderId: folderFileMatch[1]
          };
        }
        return {
          mode: "unknown",
          origin: parsed.origin,
          pathname
        };
      } catch {
        return {
          mode: "unknown",
          origin: globalThis.location.origin,
          pathname: fallback.split("?")[0].split("#")[0]
        };
      }
    }
    normalizeYandexDiskPublicUrl(rawUrl) {
      const parsed = this.parseYandexDiskUrl(rawUrl);
      if (parsed.mode === "file" && parsed.fileId) {
        return `${parsed.origin}/i/${parsed.fileId}`;
      }
      if (parsed.mode === "folderRoot" || parsed.mode === "folderFile") {
        return `${parsed.origin}${parsed.pathname}`;
      }
      return String(rawUrl || globalThis.location.href || "").split("?")[0].split("#")[0];
    }
    extractYandexDiskPublicTarget(value) {
      try {
        const parsed = new URL(value, globalThis.location.href);
        const pathname = parsed.pathname || "/";
        const origin = "https://disk.yandex.com";
        const inlineMatch = pathname.match(/^\/i\/([^/?#]+)$/i);
        if (inlineMatch) {
          return {
            url: `${origin}/i/${inlineMatch[1]}`,
            videoId: `/i/${inlineMatch[1]}`
          };
        }
        const publicFileMatch = pathname.match(/^\/d\/([^/?#]+)\/?$/i);
        if (publicFileMatch) {
          return {
            url: `${origin}/d/${publicFileMatch[1]}`,
            videoId: `/d/${publicFileMatch[1]}`
          };
        }
        return null;
      } catch {
        return null;
      }
    }
    extractYandexDiskPublicTargetFromMedia() {
      const tryValue = (value) => this.extractYandexDiskPublicTarget(String(value || ""));
      const href = String(globalThis.location.href || "");
      const locationParsed = this.parseYandexDiskUrl(href);
      if (locationParsed.mode === "file" && locationParsed.fileId) {
        return {
          url: `${locationParsed.origin}/i/${locationParsed.fileId}`,
          videoId: locationParsed.fileId
        };
      }
      const videos = Array.from(document.querySelectorAll("video"));
      for (const video of videos) {
        const htmlVideo = video;
        const candidates = [
          htmlVideo.currentSrc || "",
          htmlVideo.src || "",
          htmlVideo.getAttribute("src") || ""
        ];
        for (const candidate of candidates) {
          const target = tryValue(candidate);
          if (target) {
            return target;
          }
        }
        const sources = Array.from(video.querySelectorAll("source"));
        for (const source of sources) {
          const target = tryValue(source.getAttribute("src") || "");
          if (target) {
            return target;
          }
        }
      }
      return null;
    }
    isYandexDiskFolderRootTarget(target, parsed) {
      if (!parsed.folderId) {
        return false;
      }
      const folderRootUrl = `${parsed.origin}/d/${parsed.folderId}`;
      return target.videoId === parsed.folderId || target.url === folderRootUrl;
    }
    async gmGetJson(url) {
      return new Promise((resolve, reject) => {
        GM_xmlhttpRequest({
          method: "GET",
          url,
          headers: {
            Accept: "application/json"
          },
          onload: (res) => {
            try {
              console.log("[VOT][yandexdisk] GM response", {
                url,
                status: res.status,
                responseText: String(res.responseText || "").slice(0, 1e3)
              });
              resolve(JSON.parse(res.responseText || "null"));
            } catch (error2) {
              reject(error2);
            }
          },
          onerror: (error2) => reject(error2),
          ontimeout: () => reject(new Error("Yandex Disk public API timeout"))
        });
      });
    }
    getYandexDiskServiceVideoId(url, fallbackPath) {
      try {
        const parsed = new URL(url, globalThis.location.href);
        const pathname = parsed.pathname || "";
        if (/^\/i\/[^/]+$/i.test(pathname)) {
          return pathname;
        }
        if (/^\/d\/.+$/i.test(pathname)) {
          return pathname;
        }
      } catch {
      }
      return fallbackPath || String(url || "");
    }
    extractBridgeDirectMediaTarget() {
      try {
        const raw = globalThis.__VOT_DIRECT_SOURCES__ || JSON.parse(
          document?.documentElement?.dataset?.votDirectSources || "null"
        );
        if (!raw || typeof raw !== "object") {
          return null;
        }
        const bridgeData = raw;
        const candidates = [
          bridgeData.hlsUrl,
          bridgeData.dashUrl,
          bridgeData.mpegLowUrl,
          bridgeData.url
        ].filter((value) => Boolean(value));
        for (const candidate of candidates) {
          const normalized = this.normalizeUrlForRequest(candidate);
          if (!normalized) {
            continue;
          }
          try {
            if (this.videoHandler.votClient.isDirectMediaUrl(normalized)) {
              return {
                url: normalized,
                videoId: String(bridgeData.unitedVideoId || normalized),
                host: "custom",
                title: bridgeData.title || ""
              };
            }
          } catch {
          }
        }
      } catch {
      }
      return null;
    }
    extractDirectMediaTargetFromVideo() {
      const video = document.querySelector("video");
      if (!(video instanceof HTMLVideoElement)) {
        return null;
      }
      const candidates = [
        video.currentSrc || "",
        video.src || "",
        video.getAttribute("src") || ""
      ];
      for (const candidate of candidates) {
        const normalized = this.normalizeUrlForRequest(candidate);
        if (!normalized) {
          continue;
        }
        try {
          if (this.videoHandler.votClient.isDirectMediaUrl(normalized)) {
            return {
              url: normalized,
              videoId: normalized,
              host: "custom"
            };
          }
        } catch {
        }
      }
      return null;
    }
    extractOdyseeMetaMediaTarget() {
      try {
        if (!/odysee\.com$/i.test(location.hostname)) {
          return null;
        }
        const ldJsonNodes = Array.from(
          document.querySelectorAll('script[type="application/ld+json"]')
        );
        for (const node of ldJsonNodes) {
          const text = node.textContent || "";
          if (!text.includes('"contentUrl"')) {
            continue;
          }
          const data = JSON.parse(text);
          const contentUrl2 = data?.contentUrl;
          if (typeof contentUrl2 === "string" && contentUrl2) {
            const normalized = this.normalizeUrlForRequest(contentUrl2);
            return {
              url: normalized,
              videoId: normalized,
              host: "custom",
              title: data?.name || document.title || ""
            };
          }
        }
      } catch {
      }
      return null;
    }
    async resolveYandexDiskFolderFileTargetViaApi(parsed) {
      if (parsed.mode !== "folderFile" || !parsed.folderId) {
        return null;
      }
      const relativePathRaw = parsed.pathname.replace(/^\/d\/[^/]+/, "") || "/";
      let relativePath = relativePathRaw;
      try {
        relativePath = decodeURIComponent(relativePathRaw);
      } catch {
        relativePath = relativePathRaw;
      }
      const publicKey = `${parsed.origin}/d/${parsed.folderId}`;
      const apiUrl = new URL(
        "https://cloud-api.yandex.com/v1/disk/public/resources"
      );
      apiUrl.searchParams.set("public_key", publicKey);
      apiUrl.searchParams.set("path", relativePath);
      try {
        const payload = await this.gmGetJson(apiUrl.toString());
        console.log("[VOT][yandexdisk] public API payload", {
          relativePathRaw,
          relativePath,
          publicKey,
          type: payload?.type,
          path: payload?.path,
          name: payload?.name,
          public_url: payload?.public_url,
          short_url: payload?.short_url,
          file: payload?.file
        });
        if (!payload || typeof payload !== "object") {
          return null;
        }
        if ("error" in payload && payload.error) {
          console.log("[VOT][yandexdisk] public API returned error", {
            error: payload.error,
            message: payload.message,
            description: payload.description
          });
          return null;
        }
        const candidates = [payload.public_url, payload.short_url].filter(
          (value) => typeof value === "string" && value.length > 0
        );
        for (const candidate of candidates) {
          const target = this.extractYandexDiskPublicTarget(candidate);
          if (!target) {
            continue;
          }
          if (this.isYandexDiskFolderRootTarget(target, parsed)) {
            console.log("[VOT][yandexdisk] skip folder-root target from API", {
              target,
              relativePath
            });
            continue;
          }
          const serviceVideoId = this.getYandexDiskServiceVideoId(
            target.url,
            parsed.pathname
          );
          console.log("[VOT][yandexdisk] public target from API", {
            target,
            candidate,
            relativePath,
            serviceVideoId
          });
          return {
            url: target.url,
            videoId: serviceVideoId
          };
        }
        if (payload.type === "file" && typeof payload.file === "string" && payload.file) {
          const directUrl = this.normalizeUrlForRequest(payload.file);
          console.log("[VOT][yandexdisk] use direct file url from API", {
            directUrl,
            relativePath
          });
          return {
            url: directUrl,
            videoId: directUrl,
            host: "custom"
          };
        }
        if (payload.type === "file") {
          console.log(
            "[VOT][yandexdisk] API did not return usable public target, continue fallback chain",
            {
              public_url: payload.public_url,
              short_url: payload.short_url,
              file: payload.file,
              relativePath
            }
          );
          return null;
        }
      } catch (error2) {
        console.log("[VOT][yandexdisk] failed to resolve public target via API", {
          relativePathRaw,
          relativePath,
          publicKey,
          error: error2
        });
      }
      return null;
    }
    isYandexDiskDownloadUrl(url) {
      try {
        return new URL(url, globalThis.location.href).hostname === "downloader.disk.yandex.ru";
      } catch {
        return false;
      }
    }
    isYandexDiskStreamUrl(url) {
      return /^https:\/\/streaming\.disk\.yandex\.net\/.+\.m3u8(?:[?#].*)?$/i.test(
        String(url || "")
      );
    }
    extractYandexDiskStreamTargetFromPlayerState() {
      const pick = (value) => {
        if (typeof value !== "string") {
          return null;
        }
        const normalized = this.normalizeUrlForRequest(value);
        return this.isYandexDiskStreamUrl(normalized) ? normalized : null;
      };
      const visited = new WeakSet();
      const scan = (value, depth) => {
        if (depth < 0) {
          return null;
        }
        const direct = pick(value);
        if (direct) {
          return direct;
        }
        if (!value || typeof value !== "object") {
          return null;
        }
        const obj = value;
        if (visited.has(obj)) {
          return null;
        }
        visited.add(obj);
        const preferredKeys = [
          "streamUrl",
          "url",
          "stream",
          "streams",
          "source",
          "sources",
          "controller",
          "state",
          "playerState",
          "playerApiState",
          "internalInitialConfig",
          "config",
          "store",
          "redux"
        ];
        for (const key of preferredKeys) {
          if (!(key in obj)) {
            continue;
          }
          const found = scan(obj[key], depth - 1);
          if (found) {
            return found;
          }
        }
        const values = Array.isArray(obj) ? obj : Object.keys(obj).slice(0, 50).map((key) => obj[key]);
        for (const item of values) {
          const found = scan(item, depth - 1);
          if (found) {
            return found;
          }
        }
        return null;
      };
      const w2 = globalThis;
      for (const key of Object.keys(w2)) {
        if (!/player|state|store|redux|disk|video|ya|vh/i.test(key)) {
          continue;
        }
        const found = scan(w2[key], 6);
        if (found) {
          console.log("[VOT][yandexdisk] use stream url from deep player state", {
            key,
            url: found
          });
          return {
            url: found,
            videoId: found,
            host: "yandexdisk"
          };
        }
      }
      return null;
    }
    extractYandexDiskStreamTargetFromPerformance() {
      try {
        const entries = performance.getEntriesByType("resource");
        console.log("[VOT][yandexdisk] inspect performance resources", {
          count: entries.length
        });
        for (let i2 = entries.length - 1; i2 >= 0; i2 -= 1) {
          const entry = entries[i2];
          const raw = String(entry?.name || "");
          const normalized = this.normalizeUrlForRequest(raw);
          if (!this.isYandexDiskStreamUrl(normalized)) {
            continue;
          }
          console.log("[VOT][yandexdisk] use stream url from performance", {
            url: normalized
          });
          return {
            url: normalized,
            videoId: normalized,
            host: "yandexdisk"
          };
        }
      } catch (error2) {
        console.log("[VOT][yandexdisk] failed to inspect performance resources", {
          error: error2
        });
      }
      return null;
    }
    async buildYandexDiskVideoData(videoData2) {
      const bridgeTarget = this.extractBridgeDirectMediaTarget();
      if (bridgeTarget) {
        return {
          ...videoData2,
          ...bridgeTarget,
          host: "custom"
        };
      }
      const odyseeTarget = this.extractOdyseeMetaMediaTarget();
      if (odyseeTarget) {
        return {
          ...videoData2,
          ...odyseeTarget,
          host: "custom"
        };
      }
      const sourceUrl = this.getYandexDiskSourceUrl(videoData2);
      const parsed = this.parseYandexDiskUrl(sourceUrl);
      console.log("[VOT][yandexdisk] build source", {
        pageUrl: globalThis.location.href,
        videoDataUrl: videoData2.url,
        sourceUrl,
        parsedMode: parsed.mode,
        videoId: videoData2.videoId
      });
      if (parsed.mode === "file" && parsed.fileId) {
        const url = `${parsed.origin}/i/${parsed.fileId}`;
        return {
          ...videoData2,
          url,
          videoId: this.getYandexDiskServiceVideoId(url, parsed.pathname),
          host: "yandexdisk"
        };
      }
      if (parsed.mode === "folderFile") {
        const target = await this.resolveYandexDiskFolderFileTargetViaApi(parsed);
        if (target) {
          const normalizedUrl = this.normalizeUrlForRequest(target.url);
          const serviceVideoId = target.host === "custom" ? normalizedUrl : this.getYandexDiskServiceVideoId(normalizedUrl, parsed.pathname);
          console.log("[VOT][yandexdisk] resolved folder file target", {
            sourceUrl,
            resolvedUrl: normalizedUrl,
            videoId: serviceVideoId,
            host: target.host ?? "yandexdisk"
          });
          return {
            ...videoData2,
            url: normalizedUrl,
            videoId: serviceVideoId,
            host: target.host ?? "yandexdisk"
          };
        }
        const streamTarget = this.extractYandexDiskStreamTargetFromPlayerState();
        if (streamTarget) {
          return {
            ...videoData2,
            url: streamTarget.url,
            videoId: streamTarget.videoId,
            host: "yandexdisk"
          };
        }
        const performanceStreamTarget = this.extractYandexDiskStreamTargetFromPerformance();
        if (performanceStreamTarget) {
          return {
            ...videoData2,
            url: performanceStreamTarget.url,
            videoId: performanceStreamTarget.videoId,
            host: "yandexdisk"
          };
        }
      }
      const directTarget = this.extractYandexDiskPublicTarget(sourceUrl);
      if (directTarget) {
        return {
          ...videoData2,
          url: directTarget.url,
          videoId: this.getYandexDiskServiceVideoId(
            directTarget.url,
            parsed.pathname
          ),
          host: "yandexdisk"
        };
      }
      const mediaTarget = this.extractYandexDiskPublicTargetFromMedia();
      if (mediaTarget) {
        return {
          ...videoData2,
          url: mediaTarget.url,
          videoId: this.getYandexDiskServiceVideoId(
            mediaTarget.url,
            parsed.pathname
          ),
          host: "yandexdisk"
        };
      }
      const directMediaTarget = this.extractDirectMediaTargetFromVideo();
      if (directMediaTarget) {
        return {
          ...videoData2,
          url: directMediaTarget.url,
          videoId: directMediaTarget.videoId,
          host: "custom"
        };
      }
      throw new Error("Failed to build Yandex Disk translation target");
    }
    onDownloadedAudio = async (translationId, data) => {
      debug.log("downloadedAudio", data);
      if (!this.downloading) {
        debug.log("skip downloadedAudio");
        return;
      }
      const { videoId, fileId, audioData } = data;
      const videoUrl = this.getCanonicalUrl(videoId);
      try {
        console.log("[VOT] Uploading full audio", {
          translationId,
          videoId,
          fileId,
          size: audioData.byteLength,
          videoUrl
        });
        await this.videoHandler.votClient.requestVtransAudio(
          videoUrl,
          translationId,
          {
            audioFile: audioData,
            fileId
          }
        );
      } catch (error2) {
        debug.error("Failed to upload downloaded audio", error2);
        console.log("[VOT] Upload full audio failed", {
          message: getErrorMessage(error2),
          serverMessage: getServerErrorMessage(error2),
          error: asVotClientErrorShape(error2)
        });
        this.finishDownloadFailure(
          new Error("Audio downloader failed while uploading full audio")
        );
        return;
      }
      this.finishDownloadSuccess();
    };
    onDownloadedPartialAudio = async (translationId, data) => {
      debug.log("downloadedPartialAudio", data);
      if (!this.downloading) {
        debug.log("skip downloadedPartialAudio");
        return;
      }
      const { audioData, fileId, videoId, amount, version, index } = data;
      const videoUrl = this.getCanonicalUrl(videoId);
      try {
        console.log("[VOT] Uploading audio chunk", {
          translationId,
          videoId,
          fileId,
          index,
          amount,
          size: audioData.byteLength,
          videoUrl
        });
        await this.videoHandler.votClient.requestVtransAudio(
          videoUrl,
          translationId,
          {
            audioFile: audioData,
            chunkId: index
          },
          {
            audioPartsLength: amount,
            fileId,
            version
          }
        );
      } catch (error2) {
        debug.error("Failed to upload downloaded audio chunk", error2);
        console.log("[VOT] Upload audio chunk failed", {
          message: getErrorMessage(error2),
          serverMessage: getServerErrorMessage(error2),
          error: asVotClientErrorShape(error2),
          index,
          amount,
          size: audioData.byteLength
        });
        this.finishDownloadFailure(
          new Error("Audio downloader failed while uploading chunk")
        );
        return;
      }
      if (index === amount - 1) {
        this.finishDownloadSuccess();
      }
    };
    onDownloadAudioError = async (videoId) => {
      if (!this.downloading) {
        debug.log("skip downloadAudioError");
        return;
      }
      debug.log(`Failed to download audio ${videoId}`);
      const videoUrl = this.getCanonicalUrl(videoId);
      const shouldUseFallback = this.videoHandler.site.host === "youtube" && Boolean(this.videoHandler.data?.useAudioDownload);
      console.log("[VOT] downloadAudioError host:", this.videoHandler.site.host);
      console.log(
        "[VOT] downloadAudioError strategy:",
        this.audioDownloader.strategy
      );
      if (!shouldUseFallback) {
        this.finishDownloadFailure(
          new VOTLocalizedError("VOTFailedDownloadAudio")
        );
        return;
      }
      try {
        if (this.requestedFailAudio.has(videoUrl)) {
          debug.log("fail-audio-js request already sent for this video");
        } else {
          debug.log("Sending fail-audio-js request");
          await this.videoHandler.votClient.requestVtransFailAudio(videoUrl);
          this.requestedFailAudio.add(videoUrl);
        }
        this.finishDownloadSuccess();
      } catch (error2) {
        debug.error("fail-audio-js request failed", error2);
        this.finishDownloadFailure(
          new VOTLocalizedError("VOTFailedDownloadAudio")
        );
      }
    };
    finishDownloadSuccess() {
      this.downloading = false;
      this.resolveDownloadWaiters();
    }
    finishDownloadFailure(error2) {
      this.downloading = false;
      this.rejectDownloadWaiters(error2);
    }
    getCanonicalUrl(videoId) {
      if (this.shouldUseLocalFileWorkflow(this.videoHandler.videoData)) {
        return this.activeTranslationUrl || this.getCurrentMediaRequestUrl(this.videoHandler.videoData);
      }
      if (this.videoHandler.site.host === "youtube") {
        return `https://youtu.be/${videoId}`;
      }
      if (this.videoHandler.site.host === "yandexdisk") {
        return this.activeTranslationUrl || this.normalizeYandexDiskPublicUrl(
          this.videoHandler.videoData?.url || globalThis.location.href
        );
      }
      if (this.videoHandler.site.host === "custom") {
        return this.activeTranslationUrl || this.normalizeUrlForRequest(
          String(this.videoHandler.videoData?.url || globalThis.location.href)
        );
      }
      return this.videoHandler.videoData?.url || globalThis.location.href;
    }
    isLivelyVoiceUnavailableError(value) {
      const msg = getErrorMessage(value);
      return !!msg && msg.toLowerCase().includes("обычная озвучка");
    }
    scheduleRetry(fn, delayMs, signal) {
      return new Promise((resolve, reject) => {
        let timeoutId = null;
        const cleanup = () => {
          if (timeoutId !== null) {
            clearTimeout(timeoutId);
          }
          signal.removeEventListener("abort", onAbort);
        };
        const onAbort = () => {
          cleanup();
          reject(makeAbortError());
        };
        signal.addEventListener("abort", onAbort, { once: true });
        if (signal.aborted) {
          onAbort();
          return;
        }
        timeoutId = setTimeout(async () => {
          if (signal.aborted) {
            onAbort();
            return;
          }
          cleanup();
          try {
            const result = await fn();
            resolve(result);
          } catch (error2) {
            reject(error2);
          }
        }, delayMs);
        if (timeoutId !== null) {
          this.videoHandler.autoRetry = timeoutId;
        }
      });
    }
    async translateVideoYDImpl(videoData2, requestLang, responseLang2, translationHelp = null, signal = NEVER_ABORTED_SIGNAL, disableLivelyVoice = false) {
      let normalizedVideoData;
      this.updateAudioDownloaderStrategy(videoData2);
      if ((this.videoHandler.site.host === "okru" || videoData2.host === "okru") && videoData2.host !== "custom") {
        videoData2 = {
          ...videoData2,
          host: "custom"
        };
      }
      const currentUrl = this.getCurrentMediaRequestUrl(videoData2);
      const canUseLocalFileWorkflow = !this.isHlsManifestUrl(currentUrl) && (this.videoHandler.site.host === "custom" || videoData2.host === "custom" || this.shouldUseLocalFileWorkflow(videoData2));
      const canUseCustomLinkWorkflow = !canUseLocalFileWorkflow && this.shouldUseCustomLinkWorkflow(videoData2);
      if (canUseLocalFileWorkflow) {
        normalizedVideoData = this.buildLocalFileWorkflowVideoData(videoData2);
      } else if (canUseCustomLinkWorkflow) {
        normalizedVideoData = this.buildCustomLinkWorkflowVideoData(videoData2);
      } else {
        const cachedVideoData = this.activeYandexDiskResolvedVideoData && this.isDirectResolvedUploadVideoData(
          this.activeYandexDiskResolvedVideoData
        ) ? this.activeYandexDiskResolvedVideoData : void 0;
        const currentVideoData = this.isDirectResolvedUploadVideoData(videoData2) ? videoData2 : void 0;
        const resolvedVideoData = cachedVideoData || currentVideoData || await this.buildYandexDiskVideoData(videoData2);
        normalizedVideoData = {
          ...resolvedVideoData,
          url: this.normalizeUrlForRequest(String(resolvedVideoData.url || ""))
        };
      }
      if (normalizedVideoData.host === "odysee" && typeof normalizedVideoData.url === "string") {
        const rewrittenUrl = this.normalizeOdyseeDirectUrl(
          normalizedVideoData.url
        );
        normalizedVideoData = rewrittenUrl !== normalizedVideoData.url ? {
          ...normalizedVideoData,
          url: rewrittenUrl,
          host: "custom"
        } : {
          ...normalizedVideoData,
          url: this.normalizeUrlForRequest(normalizedVideoData.url)
        };
      }
      if (normalizedVideoData && typeof normalizedVideoData.url === "string" && normalizedVideoData.url && !/\/frame\/?$/i.test(normalizedVideoData.url) && !/blob:/i.test(normalizedVideoData.url)) {
        this.activeYandexDiskResolvedVideoData = normalizedVideoData;
      }
      this.activeTranslationUrl = normalizedVideoData.url;
      console.log("[VOT][upload] translateVideoYDImpl input", {
        host: normalizedVideoData.host,
        url: normalizedVideoData.url,
        videoId: normalizedVideoData.videoId
      });
      try {
        throwIfAborted(signal);
        const useLivelyVoice = !disableLivelyVoice && this.videoHandler.isLivelyVoiceAllowed(requestLang, responseLang2) && Boolean(this.videoHandler.data?.useLivelyVoice);
        const res = await this.videoHandler.votClient.translateVideo({
          videoData: normalizedVideoData,
          requestLang,
          responseLang: responseLang2,
          translationHelp,
          extraOpts: {
            useLivelyVoice,
            videoTitle: this.videoHandler.videoData?.title
          },
          shouldSendFailedAudio: true
        });
        if (!res) {
          throw new Error("Failed to get translation response");
        }
        console.log("[VOT][upload] translate response", {
          translated: res.translated,
          status: res.status,
          remainingTime: res.remainingTime,
          message: res.message
        });
        if (isCompletedTranslationResponse(res)) {
          return { ...res, usedLivelyVoice: useLivelyVoice };
        }
        const message = res.message ?? localizationProvider.get("translationTakeFewMinutes");
        const displayRemainingTime = adjustTranslationEtaForDisplay(
          res.remainingTime,
          {
            optimisticLocalUpload: canUseLocalFileWorkflow
          }
        );
        await this.videoHandler.updateTranslationErrorMsg(
          displayRemainingTime > 0 ? formatTranslationEta(
            displayRemainingTime,
            (key) => localizationProvider.get(key)
          ) : message,
          signal
        );
        if (res.status === VideoTranslationStatus.AUDIO_REQUESTED && this.videoHandler.canUploadAudioForCurrentSite()) {
          this.videoHandler.hadAsyncWait = true;
          this.downloading = true;
          await this.audioDownloader.runAudioDownload(
            normalizedVideoData.videoId,
            res.translationId,
            signal,
            this.videoHandler.video
          );
          await this.waitForAudioDownloadCompletion(signal, 12e4);
          return await this.translateVideoYDImpl(
            normalizedVideoData,
            requestLang,
            responseLang2,
            translationHelp,
            signal,
            disableLivelyVoice || !useLivelyVoice
          );
        }
        if (res.status === VideoTranslationStatus.WAITING || res.status === VideoTranslationStatus.LONG_WAITING) {
          this.videoHandler.hadAsyncWait = true;
          const retryDelay = normalizedVideoData.host === "custom" ? 15e3 : 5e3;
          return this.scheduleRetry(
            () => this.translateVideoYDImpl(
              normalizedVideoData,
              requestLang,
              responseLang2,
              translationHelp,
              signal,
              disableLivelyVoice || !useLivelyVoice
            ),
            retryDelay,
            signal
          );
        }
        throw new Error(
          typeof res.message === "string" && res.message ? res.message : "Yandex couldn't translate video"
        );
      } catch (err) {
        if (isAbortError(err)) {
          return null;
        }
        const uiError = mapVotClientErrorForUi(err, this.videoHandler.site.host);
        await this.videoHandler.updateTranslationErrorMsg(
          getServerErrorMessage(uiError) ?? uiError,
          signal
        );
        this.videoHandler.hadAsyncWait = notifyTranslationFailureIfNeeded({
          aborted: Boolean(
            this.videoHandler.actionsAbortController?.signal?.aborted
          ),
          translateApiErrorsEnabled: Boolean(
            this.videoHandler.data?.translateAPIErrors
          ),
          hadAsyncWait: this.videoHandler.hadAsyncWait,
          videoId: normalizedVideoData.videoId,
          error: err,
          notify: (params) => this.videoHandler.notifier.translationFailed(params)
        });
        console.error("[VOT][upload]", err);
        return null;
      }
    }
    async translateVideoImpl(videoData2, requestLang, responseLang2, translationHelp = null, shouldSendFailedAudio = false, signal = NEVER_ABORTED_SIGNAL, disableLivelyVoice = false) {
      clearTimeout(this.videoHandler.autoRetry);
      this.finishDownloadSuccess();
      const requestLangForApi = this.videoHandler.getRequestLangForTranslation(
        requestLang,
        responseLang2
      );
      debug.log(
        videoData2,
        `Translate video (requestLang: ${requestLang}, requestLangForApi: ${requestLangForApi}, responseLang: ${responseLang2})`
      );
      let livelyDisabled = disableLivelyVoice;
      const useLocalFileWorkflow = this.shouldUseLocalFileWorkflow(videoData2);
      this.updateAudioDownloaderStrategy(videoData2);
      if (this.videoHandler.site.host === "yandexdisk" || this.videoHandler.site.host === "custom" || videoData2.host === "custom" || useLocalFileWorkflow) {
        return await this.translateVideoYDImpl(
          videoData2,
          requestLangForApi,
          responseLang2,
          translationHelp,
          signal
        );
      }
      let requestVideoData = videoData2;
      if (this.videoHandler.site.host === "odysee" && typeof videoData2.url === "string") {
        const normalizedUrl = this.normalizeUrlForRequest(videoData2.url);
        const rewrittenUrl = this.normalizeOdyseeDirectUrl(normalizedUrl);
        requestVideoData = rewrittenUrl !== normalizedUrl ? {
          ...videoData2,
          url: rewrittenUrl,
          host: "custom"
        } : {
          ...videoData2,
          url: normalizedUrl
        };
      }
      if (this.shouldUseCustomLinkWorkflow(requestVideoData)) {
        requestVideoData = this.buildCustomLinkWorkflowVideoData(requestVideoData);
      }
      this.activeTranslationUrl = this.videoHandler.site.host === "odysee" ? requestVideoData.url : this.getCanonicalUrl(videoData2.videoId);
      try {
        throwIfAborted(signal);
        const livelyVoiceAllowed = this.videoHandler.isLivelyVoiceAllowed(
          requestLangForApi,
          responseLang2
        );
        let useLivelyVoice = !livelyDisabled && livelyVoiceAllowed && Boolean(this.videoHandler.data?.useLivelyVoice);
        let res;
        for (let attempt = 0; attempt < 2; attempt++) {
          try {
            res = await this.videoHandler.votClient.translateVideo({
              videoData: requestVideoData,
              requestLang: requestLangForApi,
              responseLang: responseLang2,
              translationHelp,
              extraOpts: {
                useLivelyVoice,
                videoTitle: this.videoHandler.videoData?.title
              },
              shouldSendFailedAudio
            });
            console.log("[VOT][translate] translate response", {
              translated: res.translated,
              status: res.status,
              remainingTime: res.remainingTime,
              message: res.message,
              requestHost: requestVideoData.host,
              requestUrl: requestVideoData.url
            });
          } catch (err) {
            if (useLivelyVoice && this.isLivelyVoiceUnavailableError(err)) {
              debug.log(
                "[translateVideoImpl] Lively voices are unavailable. Falling back to standard translation.",
                err
              );
              livelyDisabled = true;
              useLivelyVoice = false;
              continue;
            }
            throw err;
          }
          if (useLivelyVoice && this.isLivelyVoiceUnavailableError(res)) {
            debug.log(
              "[translateVideoImpl] Server responded that lively voices are unavailable. Falling back to standard translation.",
              res
            );
            livelyDisabled = true;
            useLivelyVoice = false;
            res = void 0;
            continue;
          }
          break;
        }
        if (!res) {
          throw new Error("Failed to get translation response");
        }
        debug.log("Translate video result", res);
        console.log("[VOT] host:", this.videoHandler.site.host);
        console.log("[VOT] status:", res.status);
        console.log("[VOT] translated:", res.translated);
        console.log("[VOT] remainingTime:", res.remainingTime);
        console.log("[VOT] translationId:", res.translationId);
        console.log(
          "[VOT] canUploadAudio:",
          this.videoHandler.canUploadAudioForCurrentSite()
        );
        console.log("[VOT] downloader strategy:", this.audioDownloader.strategy);
        if (this.videoHandler.site.host === "vk" && !res.translated && res.status === VideoTranslationStatus.AUDIO_REQUESTED && this.videoHandler.canUploadAudioForCurrentSite() && res.translationId && videoData2.videoId) {
          debug.log("[VOT][VK subtitles] force audio upload", {
            videoId: videoData2.videoId,
            translationId: res.translationId,
            strategy: this.audioDownloader.strategy
          });
          try {
            this.downloading = true;
            void this.audioDownloader.runAudioDownload(
              videoData2.videoId,
              String(res.translationId),
              signal,
              this.videoHandler.video
            );
            await this.waitForAudioDownloadCompletion(signal, 2e4);
          } catch (error2) {
            debug.log(
              "[VOT][VK subtitles] force audio upload failed after successful translation",
              {
                videoId: videoData2.videoId,
                translationId: res.translationId,
                message: getErrorMessage(error2),
                serverMessage: getServerErrorMessage(error2),
                error: asVotClientErrorShape(error2)
              }
            );
          }
        }
        throwIfAborted(signal);
        if (isCompletedTranslationResponse(res)) {
          debug.log("Video translation finished with this data: ", res);
          return { ...res, usedLivelyVoice: useLivelyVoice };
        }
        const message = res.message ?? localizationProvider.get("translationTakeFewMinutes");
        await this.videoHandler.updateTranslationErrorMsg(
          res.remainingTime > 0 ? formatTranslationEta(
            res.remainingTime,
            (key) => localizationProvider.get(key)
          ) : message,
          signal
        );
        if (res.status === VideoTranslationStatus.AUDIO_REQUESTED && this.videoHandler.canUploadAudioForCurrentSite()) {
          this.videoHandler.hadAsyncWait = true;
          debug.log("Start audio download");
          this.downloading = true;
          await this.audioDownloader.runAudioDownload(
            videoData2.videoId,
            res.translationId,
            signal,
            this.videoHandler.video
          );
          debug.log("waiting downloading finish");
          await this.waitForAudioDownloadCompletion(
            signal,
            this.audioDownloader.strategy === "yandexDisk" ? 12e4 : 15e3
          );
          return await this.translateVideoImpl(
            videoData2,
            requestLang,
            responseLang2,
            translationHelp,
            true,
            signal,
            livelyDisabled
          );
        }
      } catch (err) {
        if (isAbortError(err)) {
          debug.log("aborted video translation");
          return null;
        }
        const uiError = mapVotClientErrorForUi(err, this.videoHandler.site.host);
        await this.videoHandler.updateTranslationErrorMsg(
          getServerErrorMessage(uiError) ?? uiError,
          signal
        );
        this.videoHandler.hadAsyncWait = notifyTranslationFailureIfNeeded({
          aborted: Boolean(
            this.videoHandler.actionsAbortController?.signal?.aborted
          ),
          translateApiErrorsEnabled: Boolean(
            this.videoHandler.data?.translateAPIErrors
          ),
          hadAsyncWait: this.videoHandler.hadAsyncWait,
          videoId: videoData2.videoId,
          error: err,
          notify: (params) => this.videoHandler.notifier.translationFailed(params)
        });
        console.error("[VOT]", err);
        return null;
      }
      this.videoHandler.hadAsyncWait = true;
      return this.scheduleRetry(
        () => this.translateVideoImpl(
          videoData2,
          requestLang,
          responseLang2,
          translationHelp,
          shouldSendFailedAudio,
          signal,
          livelyDisabled
        ),
        2e4,
        signal
      );
    }
    getYandexDiskSourceUrl(videoData2) {
      const currentUrl = String(videoData2.url || "");
      if (currentUrl) {
        if (this.isYandexDiskStreamUrl(currentUrl)) {
          return currentUrl;
        }
        if (!this.isYandexDiskDownloadUrl(currentUrl)) {
          const parsedVideo = this.parseYandexDiskUrl(currentUrl);
          if (parsedVideo.mode !== "unknown") {
            return currentUrl;
          }
        }
      }
      const pageUrl = String(globalThis.location.href || "");
      const parsedPage = this.parseYandexDiskUrl(pageUrl);
      if (parsedPage.mode !== "unknown") {
        return pageUrl;
      }
      return currentUrl || pageUrl;
    }
    waitForAudioDownloadCompletion(signal, timeoutMs) {
      if (!this.downloading) {
        return Promise.resolve();
      }
      return new Promise((resolve, reject) => {
        let entry;
        const onAbort = () => {
          cleanup();
          reject(makeAbortError());
        };
        const timeoutId = setTimeout(() => {
          cleanup();
          reject(new Error("Audio download wait timeout"));
        }, timeoutMs);
        const cleanup = () => {
          clearTimeout(timeoutId);
          signal.removeEventListener("abort", onAbort);
          this.downloadWaiters.delete(entry);
        };
        entry = {
          resolve: () => {
            cleanup();
            resolve();
          },
          reject: (error2) => {
            cleanup();
            reject(error2);
          }
        };
        this.downloadWaiters.add(entry);
        signal.addEventListener("abort", onAbort, { once: true });
        if (signal.aborted) {
          onAbort();
        }
      });
    }
    resolveDownloadWaiters() {
      this.forEachDownloadWaiter((waiter) => waiter.resolve());
    }
    rejectDownloadWaiters(error2) {
      this.forEachDownloadWaiter((waiter) => waiter.reject(error2));
    }
    forEachDownloadWaiter(handler) {
      if (!this.downloadWaiters.size) {
        return;
      }
      const waiters = Array.from(this.downloadWaiters);
      this.downloadWaiters.clear();
      for (const waiter of waiters) {
        handler(waiter);
      }
    }
  }
  class TranslationOrchestrator {
    state = { status: "idle" };
    deps;
    constructor(deps) {
      this.deps = deps;
    }
    get currentState() {
      return this.state;
    }
    setState(next) {
      this.state = next;
      debug.log("[TranslationOrchestrator] state", next);
    }
    reset() {
      this.setState({ status: "idle" });
    }
    async runAutoTranslationIfEligible() {
      if (this.state.status !== "idle") {
        return;
      }
      if (!(this.deps.isFirstPlay() && this.deps.isAutoTranslateEnabled() && this.deps.getVideoId())) {
        return;
      }
      if (this.deps.isMobileYouTubeMuted?.()) {
        debug.log(
          "[TranslationOrchestrator] Mobile YouTube video is muted, deferring auto-translate"
        );
        this.setState({ status: "deferred", reason: "muted" });
        this.deps.setMuteWatcher?.(() => {
          debug.log(
            "[TranslationOrchestrator] Video unmuted, running deferred auto-translate"
          );
          this.setState({ status: "idle" });
          void this.runAutoTranslationIfEligible();
        });
        return;
      }
      this.setState({ status: "pending", reason: "auto" });
      try {
        const started = await this.deps.scheduleAutoTranslate();
        if (!started) {
          debug.log(
            "[TranslationOrchestrator] Auto-translate prerequisites not ready yet; keeping firstPlay for retry"
          );
          this.reset();
          return;
        }
        this.deps.setFirstPlay(false);
        this.reset();
      } catch (err) {
        this.setState({ status: "error", message: err });
        throw err;
      }
    }
  }
  function isMobileYouTubeDebugHost$1() {
    return /^(m|music)\.youtube\.com$/i.test(
      String(globalThis.location.hostname || "")
    );
  }
  function logMobileOverlayLifecycle(message, details) {
    if (!isMobileYouTubeDebugHost$1()) {
      return;
    }
    console.log(`[VOT][mobile-overlay][lifecycle] ${message}`, details ?? {});
  }
  function resetLifecycleTranslation(host, options = {}) {
    const { requireVideoData = false, clearVideoData = false } = options;
    if (requireVideoData && !host.videoData) {
      logMobileOverlayLifecycle("skip translation reset: no videoData", {
        requireVideoData,
        clearVideoData
      });
      return;
    }
    logMobileOverlayLifecycle("reset translation state", {
      requireVideoData,
      clearVideoData,
      hadVideoData: Boolean(host.videoData)
    });
    if (clearVideoData) {
      host.videoData = void 0;
    }
    host.stopTranslation();
    host.resetSubtitlesWidget();
  }
  function hideLifecycleOverlay(overlayView, options = {}) {
    const { hideMenu = false } = options;
    logMobileOverlayLifecycle("hide overlay", {
      hideMenu
    });
    if (overlayView?.votButton?.container) {
      overlayView.votButton.container.hidden = true;
    }
    if (hideMenu && overlayView?.votMenu) {
      overlayView.votMenu.hidden = true;
    }
  }
  function resetAndHideLifecycle(host, overlayView, options = {}) {
    const { requireVideoData, clearVideoData, hideMenu } = options;
    logMobileOverlayLifecycle("reset and hide lifecycle", {
      requireVideoData,
      clearVideoData,
      hideMenu
    });
    resetLifecycleTranslation(host, {
      requireVideoData,
      clearVideoData
    });
    hideLifecycleOverlay(overlayView, { hideMenu });
  }
  function isVkProbeHost$1() {
    return /(?:^|\.)vkvideo\.ru$|(?:^|\.)vk\.(?:com|ru)$/i.test(
      String(globalThis.location.hostname || "")
    );
  }
  class VideoLifecycleController {
    host;
    lifecycleGeneration = 0;
    lastSetCanPlaySourceKey = "";
    activeSetCanPlaySourceKey = "";
    setCanPlayRequested = false;
    setCanPlayLoopPromise;
    deferredAutoStartupTimer;
    constructor(host) {
      this.host = host;
    }
    isMobileYouTubeDebugHost() {
      return /^(m|music)\.youtube\.com$/i.test(
        String(globalThis.location.hostname || "")
      );
    }
    logMobileOverlay(message, details) {
      if (!this.isMobileYouTubeDebugHost()) {
        return;
      }
      console.log(
        `[VOT][mobile-overlay][lifecycle-controller] ${message}`,
        details ?? {}
      );
    }
    isGracefulMobileYouTubeSite() {
      return this.host.site.host === "youtube" && (this.host.site.additionalData === "mobile" || this.host.site.additionalData === "music");
    }
    getCurrentPageKey() {
      return `${globalThis.location.origin}${globalThis.location.pathname}${globalThis.location.search}`;
    }
    shouldKeepOverlayAlive(expectedPageKey) {
      if (!this.isGracefulMobileYouTubeSite()) {
        return false;
      }
      return this.getCurrentPageKey() === (expectedPageKey ?? this.getCurrentPageKey());
    }
    isStale(generation) {
      return generation !== this.lifecycleGeneration;
    }
    resetActions(reason) {
      if (typeof this.host.resetActionsAbortController === "function") {
        this.host.resetActionsAbortController(reason);
        return;
      }
      this.host.actionsAbortController?.abort(reason);
    }
    invalidateActiveSession(reason) {
      this.clearDeferredAutoStartup();
      if (this.lifecycleGeneration === 0) return;
      this.lifecycleGeneration += 1;
      this.resetActions(`[VideoLifecycle] ${reason}`);
      debug.log(
        `[VideoLifecycle] cancelled active session (active: ${this.lifecycleGeneration})`,
        { reason }
      );
    }
    startSession(reason) {
      this.lifecycleGeneration += 1;
      const sessionId = this.lifecycleGeneration;
      this.resetActions(`[VideoLifecycle][session:${sessionId}] ${reason}`);
      debug.log(`[VideoLifecycle][session:${sessionId}] started`, { reason });
      return sessionId;
    }
    shouldAbortHandleSrcChanged(callId, stage) {
      if (!this.isStale(callId)) {
        return false;
      }
      debug.log(
        `[VideoLifecycle][session:${callId}] handleSrcChanged aborted at ${stage} (active: ${this.lifecycleGeneration})`
      );
      return true;
    }
    showOverlayButton(overlayView) {
      overlayView.votButton.container.hidden = false;
      overlayView.votButton.opacity = 1;
      this.host.queueOverlayAutoHide?.();
    }
    hasResolvableMediaSource() {
      const { video } = this.host;
      if (video.currentSrc || video.src || video.srcObject) {
        return true;
      }
      const source = video.querySelector("source");
      return Boolean(source?.getAttribute("src") || source?.src);
    }
    teardown() {
      this.logMobileOverlay("teardown controller", {
        pageKey: this.getCurrentPageKey(),
        videoId: this.host.videoData?.videoId
      });
      this.clearDeferredAutoStartup();
      this.setCanPlayRequested = false;
      this.invalidateActiveSession("teardown");
    }
    clearDeferredAutoStartup() {
      if (this.deferredAutoStartupTimer === void 0) {
        return;
      }
      clearTimeout(this.deferredAutoStartupTimer);
      this.deferredAutoStartupTimer = void 0;
    }
    shouldDeferAutoStartup() {
      return this.host.site.host === "youtube" && !this.host.site.additionalData;
    }
    hasAutoStartupWork() {
      return Boolean(
        this.host.data.autoTranslate || this.host.data.autoSubtitles
      );
    }
    async runAutoStartupSequence(sessionId, mode) {
      if (mode === "immediate") {
        const autoSubtitlesPromise = this.runAutoSubtitlesIfEnabled(sessionId);
        await this.host.translationOrchestrator.runAutoTranslationIfEligible();
        if (this.isStale(sessionId)) {
          debug.log(
            `[VideoLifecycle][session:${sessionId}] auto-translation result ignored (stale session)`
          );
          return;
        }
        await autoSubtitlesPromise;
        if (this.isStale(sessionId)) {
          debug.log(
            `[VideoLifecycle][session:${sessionId}] auto-subtitles result ignored (stale session)`
          );
        }
        return;
      }
      await this.host.translationOrchestrator.runAutoTranslationIfEligible();
      if (this.isStale(sessionId)) {
        debug.log(
          `[VideoLifecycle][session:${sessionId}] deferred auto-translation ignored (stale session)`
        );
        return;
      }
      await this.runAutoSubtitlesIfEnabled(sessionId);
      if (this.isStale(sessionId)) {
        debug.log(
          `[VideoLifecycle][session:${sessionId}] deferred auto-subtitles ignored (stale session)`
        );
      }
    }
    scheduleDeferredAutoStartup(sessionId, sourceKey) {
      if (!this.hasAutoStartupWork()) {
        return;
      }
      this.clearDeferredAutoStartup();
      this.deferredAutoStartupTimer = globalThis.setTimeout(() => {
        this.deferredAutoStartupTimer = void 0;
        if (this.isStale(sessionId)) {
          return;
        }
        if (this.getCurrentSourceKey() !== sourceKey) {
          debug.log(
            `[VideoLifecycle][session:${sessionId}] deferred auto startup skipped after source change`,
            { sourceKey }
          );
          return;
        }
        void this.runAutoStartupSequence(sessionId, "deferred").catch((err) => {
          debug.log(
            `[VideoLifecycle][session:${sessionId}] deferred auto startup failed`,
            err
          );
        });
      }, 800);
    }
    getCurrentSourceKey() {
      const hasSrcObject = this.host.video.srcObject ? "1" : "0";
      if (this.host.site.host === "youtube") {
        const path = globalThis.location.pathname;
        const stableUrlKey = `${globalThis.location.origin}${path}${globalThis.location.search}`;
        return `${stableUrlKey}||${hasSrcObject}`;
      }
      const src = this.host.video.currentSrc || this.host.video.src || "";
      return `${globalThis.location.href}||${src}||${hasSrcObject}`;
    }
    resolveContainer() {
      const { site, video, container } = this.host;
      if (!site.selector) {
        return video.parentElement ?? container;
      }
      const matched = findConnectedContainerBySelector(video, site.selector);
      if (matched) {
        return matched;
      }
      if (container.isConnected && containsCrossShadow(container, video)) {
        return container;
      }
      return video.parentElement ?? container;
    }
    async setCanPlay() {
      this.setCanPlayRequested = true;
      if (this.setCanPlayLoopPromise !== void 0) {
        const incomingSourceKey = this.getCurrentSourceKey();
        if (this.activeSetCanPlaySourceKey && incomingSourceKey !== this.activeSetCanPlaySourceKey) {
          this.invalidateActiveSession(
            "setCanPlay source changed while previous trigger is running"
          );
        } else {
          debug.log("[VideoLifecycle] setCanPlay deduplicated for same source", {
            sourceKey: incomingSourceKey
          });
        }
        return await this.setCanPlayLoopPromise;
      }
      const loopPromise = (async () => {
        while (this.setCanPlayRequested) {
          this.setCanPlayRequested = false;
          await this.runSetCanPlayOnce();
        }
      })();
      this.setCanPlayLoopPromise = loopPromise;
      try {
        await loopPromise;
      } finally {
        if (this.setCanPlayLoopPromise === loopPromise) {
          this.setCanPlayLoopPromise = void 0;
        }
      }
    }
    async runSetCanPlayOnce() {
      const sourceKey = this.getCurrentSourceKey();
      const pageKey = this.getCurrentPageKey();
      if (this.host.videoData?.videoId && sourceKey === this.lastSetCanPlaySourceKey) {
        debug.log("[VideoLifecycle] setCanPlay deduplicated for same source", {
          sourceKey
        });
        const overlayView = this.host.uiManager.votOverlayView;
        this.showOverlayButton(overlayView);
        const sourceAudioState = this.host.syncSourceAudioAvailabilityUi({
          forceVisible: true
        });
        console.log("[VOT][source-audio] status refresh after setCanPlay", {
          sourceKey,
          kind: sourceAudioState.kind,
          ready: sourceAudioState.ready,
          audioDetected: sourceAudioState.audioDetected,
          detectionSource: sourceAudioState.detectionSource
        });
        if (this.shouldDeferAutoStartup()) {
          return;
        }
        await this.runAutoSubtitlesIfEnabled(this.lifecycleGeneration);
        return;
      }
      let nextVideoData;
      try {
        nextVideoData = await this.host.getVideoData();
      } catch (err) {
        debug.log(
          `[VideoLifecycle] getVideoData failed for source ${sourceKey}`,
          err
        );
        this.host.videoData = void 0;
        debug.log(
          `[VideoLifecycle] keeping overlay visible despite getVideoData failure`,
          { sourceKey }
        );
        this.logMobileOverlay("keep overlay after getVideoData failure", {
          sourceKey,
          pageKey,
          hasResolvableMediaSource: this.hasResolvableMediaSource()
        });
        this.showOverlayButton(this.host.uiManager.votOverlayView);
        this.host.syncSourceAudioAvailabilityUi({ forceVisible: true });
        return;
      }
      if (this.getCurrentSourceKey() !== sourceKey) {
        debug.log(
          "[VideoLifecycle] discarded stale getVideoData result after source change",
          { sourceKey }
        );
        return;
      }
      this.host.videoData = nextVideoData;
      if (isVkProbeHost$1()) {
        console.log("[VOT][VK probe] videoData", {
          videoId: nextVideoData?.videoId,
          host: nextVideoData?.host,
          url: nextVideoData?.url,
          duration: nextVideoData?.duration,
          subtitles: Array.isArray(nextVideoData?.subtitles) ? nextVideoData.subtitles.length : null
        });
      }
      this.activeSetCanPlaySourceKey = sourceKey;
      const currentId = this.startSession(`setCanPlay (source: ${sourceKey})`);
      debug.log(`[VideoLifecycle][session:${currentId}] setCanPlay started`, {
        sourceKey
      });
      try {
        await this.handleSrcChanged(currentId, sourceKey);
        if (this.isStale(currentId)) {
          debug.log(
            `[VideoLifecycle][session:${currentId}] setCanPlay aborted after src change (active: ${this.lifecycleGeneration})`
          );
          return;
        }
        if (this.shouldDeferAutoStartup()) {
          this.scheduleDeferredAutoStartup(currentId, sourceKey);
          debug.log(
            `[VideoLifecycle][session:${currentId}] deferred auto startup scheduled`,
            { sourceKey }
          );
          return;
        }
        await this.runAutoStartupSequence(currentId, "immediate");
        debug.log(`[VideoLifecycle][session:${currentId}] setCanPlay finished`);
      } finally {
        if (this.activeSetCanPlaySourceKey === sourceKey) {
          this.activeSetCanPlaySourceKey = "";
        }
      }
    }
    async runAutoSubtitlesIfEnabled(sessionId) {
      if (!this.host.data.autoSubtitles || !this.host.videoData?.videoId) {
        return;
      }
      try {
        await this.host.enableSubtitlesForCurrentLangPair();
      } catch (err) {
        debug.log(
          `[VideoLifecycle][session:${sessionId}] auto-subtitles failed`,
          err
        );
      }
    }
    async handleSrcChanged(callId, expectedSourceKey) {
      const sessionId = typeof callId === "number" ? callId : this.startSession("manual handleSrcChanged");
      const sourceKey = typeof expectedSourceKey === "string" && expectedSourceKey.length > 0 ? expectedSourceKey : this.getCurrentSourceKey();
      const pageKey = this.getCurrentPageKey();
      if (this.shouldAbortHandleSrcChanged(sessionId, "before start")) {
        return;
      }
      debug.log(`[VideoLifecycle][session:${sessionId}] src changed`, {
        sourceKey
      });
      this.host.translationOrchestrator.reset();
      this.host.firstPlay = true;
      const overlayView = this.host.uiManager.votOverlayView;
      if (this.shouldKeepOverlayAlive(pageKey)) {
        this.logMobileOverlay("reset lifecycle without hiding overlay", {
          sourceKey,
          pageKey,
          videoId: this.host.videoData?.videoId
        });
        resetLifecycleTranslation(this.host, { requireVideoData: true });
        overlayView.votMenu.hidden = true;
      } else {
        resetAndHideLifecycle(this.host, overlayView, { requireVideoData: true });
      }
      const noSrc = !this.host.video.src && !this.host.video.currentSrc && !this.host.video.srcObject;
      if (noSrc && !this.shouldKeepOverlayAlive(pageKey)) {
        hideLifecycleOverlay(overlayView, { hideMenu: true });
      } else if (noSrc) {
        this.logMobileOverlay(
          "keep overlay with empty currentSrc during src change",
          {
            sourceKey,
            pageKey
          }
        );
      }
      const nextContainer = this.resolveContainer();
      if (nextContainer !== this.host.container) {
        this.host.container = nextContainer;
      }
      if (this.shouldAbortHandleSrcChanged(sessionId, "before getVideoData")) {
        return;
      }
      this.showOverlayButton(overlayView);
      this.host.syncSourceAudioAvailabilityUi({ forceVisible: true });
      if (this.shouldAbortHandleSrcChanged(sessionId, "after getVideoData")) {
        return;
      }
      if (!this.host.videoData?.videoId) {
        debug.log(`[VideoLifecycle][session:${sessionId}] No videoId resolved`);
        debug.log(
          `[VideoLifecycle][session:${sessionId}] keeping overlay visible for manual retry`,
          { sourceKey }
        );
        this.logMobileOverlay("keep overlay without resolved videoId", {
          sourceKey,
          pageKey,
          hasResolvableMediaSource: this.hasResolvableMediaSource()
        });
        this.showOverlayButton(overlayView);
        this.host.syncSourceAudioAvailabilityUi({ forceVisible: true });
        return;
      }
      const cacheKey2 = this.host.getSubtitlesCacheKey(
        this.host.videoData.videoId,
        this.host.videoData.detectedLanguage,
        this.host.videoData.responseLanguage
      );
      const cachedSubtitles = this.host.cacheManager.getSubtitles(cacheKey2);
      this.host.subtitles = cachedSubtitles ?? [];
      this.host.subtitlesCacheKey = cachedSubtitles !== void 0 ? cacheKey2 : null;
      await this.host.updateSubtitlesLangSelect();
      if (this.shouldAbortHandleSrcChanged(sessionId, "after subtitles update")) {
        return;
      }
      this.host.translateToLang = this.host.data.responseLanguage ?? "ru";
      this.host.setSelectMenuValues(
        this.host.videoData.detectedLanguage,
        this.host.videoData.responseLanguage
      );
      this.showOverlayButton(overlayView);
      this.host.syncSourceAudioAvailabilityUi({ forceVisible: true });
      this.lastSetCanPlaySourceKey = sourceKey;
      this.host.onPrimaryAttachReady?.();
      debug.log(`[VideoLifecycle][session:${sessionId}] src handling finished`);
    }
  }
  const VK_HOST_PATTERN = /(?:^|\.)vkvideo\.ru$|(?:^|\.)vk\.(?:com|ru)$/i;
  const VK_REQUEST_PATTERN = /api\.vkvideo\.ru|\/al_video\.php(?:$|[?#])|\/method\/video\.|video_ext\.php|vkvideo\.ru/i;
  const SUBTITLE_URL_PATTERN = /\.(vtt|srt|ass|json)(?:$|[?#])/i;
  const MAX_BODY_LENGTH = 15e5;
  const MAX_TRACKS_PER_HOST = 64;
  const TRACK_MAX_AGE_MS = 10 * 60 * 1e3;
  const sniffedTracksByHost = new Map();
  const xhrUrlKey = Symbol("votSniffedSubtitleUrl");
  let installed = false;
  function isVkHost(hostname) {
    return VK_HOST_PATTERN.test(hostname);
  }
  function isVkPage() {
    return isVkHost(String(globalThis.location.hostname || ""));
  }
  function normalizeUrl(input) {
    try {
      return new URL(input, globalThis.location.href).href;
    } catch {
      return input;
    }
  }
  function inferSubtitleFormat(url) {
    const normalized = url.split(/[?#]/u, 1)[0]?.toLowerCase() ?? "";
    if (normalized.endsWith(".vtt")) return "vtt";
    if (normalized.endsWith(".srt")) return "srt";
    if (normalized.endsWith(".ass")) return "ass";
    if (normalized.endsWith(".json")) return "json";
    return null;
  }
  function normalizeLanguageCandidate(value) {
    if (typeof value !== "string") {
      return void 0;
    }
    const normalized = value.trim();
    if (!normalized) {
      return void 0;
    }
    try {
      return normalizeLang$1(normalized);
    } catch {
      return normalized.toLowerCase().split(/[_;-]/u)[0]?.trim() || void 0;
    }
  }
  function inferLanguageFromUrl(rawUrl) {
    try {
      const url = new URL(rawUrl, globalThis.location.href);
      const queryValue = url.searchParams.get("lang") || url.searchParams.get("language") || url.searchParams.get("locale") || url.searchParams.get("srclang");
      if (queryValue) {
        return normalizeLanguageCandidate(queryValue);
      }
      const pathname = decodeURIComponent(url.pathname);
      const match = /(?:^|[._/-])([a-z]{2,3}(?:-[a-z]{2,4})?)(?=\.(?:vtt|srt|ass|json)\b)/i.exec(
        pathname
      ) || /(?:^|[._/-])([a-z]{2,3})(?:[._-]captions?)(?=\b)/i.exec(pathname);
      return normalizeLanguageCandidate(match?.[1]);
    } catch {
      return void 0;
    }
  }
  function parseVideoId(ownerId, videoId) {
    const normalizedOwner = String(ownerId || "").trim();
    const normalizedVideoId = String(videoId || "").trim();
    if (!normalizedOwner || !normalizedVideoId) {
      return void 0;
    }
    const ownerNumber = Number.parseInt(normalizedOwner, 10);
    if (Number.isNaN(ownerNumber)) {
      return void 0;
    }
    return `video-${Math.abs(ownerNumber)}_${normalizedVideoId}`;
  }
  function extractVideoIdFromUrl(rawUrl) {
    try {
      const url = new URL(rawUrl, globalThis.location.href);
      const pathnameMatch = /\/(video-?\d+_\d+)/i.exec(url.pathname) || /\/playlist\/[^/]+\/(video-?\d+_\d+)/i.exec(url.pathname);
      if (pathnameMatch?.[1]) {
        return pathnameMatch[1];
      }
      const paramZ = url.searchParams.get("z");
      if (paramZ) {
        const zMatch = /(video-?\d+_\d+)/i.exec(paramZ);
        if (zMatch?.[1]) {
          return zMatch[1];
        }
      }
      const oid = url.searchParams.get("oid");
      const id = url.searchParams.get("id");
      if (oid && id) {
        return parseVideoId(oid, id);
      }
    } catch {
      return void 0;
    }
    return void 0;
  }
  function isRecord$2(value) {
    return value !== null && typeof value === "object";
  }
  function pickString(record, keys) {
    for (const key of keys) {
      const value = record[key];
      if (typeof value === "string" && value.trim()) {
        return value.trim();
      }
    }
    return void 0;
  }
  function pickNumberish(record, keys) {
    for (const key of keys) {
      const value = record[key];
      if (typeof value === "number" && Number.isFinite(value)) {
        return String(value);
      }
      if (typeof value === "string" && value.trim()) {
        return value.trim();
      }
    }
    return void 0;
  }
  function toOptionalBoolean(value) {
    if (typeof value === "boolean") {
      return value;
    }
    if (typeof value === "number") {
      return value !== 0;
    }
    if (typeof value === "string") {
      const normalized = value.trim().toLowerCase();
      if (normalized === "true" || normalized === "1") return true;
      if (normalized === "false" || normalized === "0") return false;
    }
    return void 0;
  }
  function extractVideoIdFromRecord(record) {
    const explicitVideoId = pickString(record, [
      "videoId",
      "video_id",
      "video",
      "player_id"
    ]);
    if (explicitVideoId) {
      const matched = /(video-?\d+_\d+)/i.exec(explicitVideoId);
      if (matched?.[1]) {
        return matched[1];
      }
    }
    const ownerId = pickNumberish(record, [
      "owner_id",
      "ownerId",
      "oid",
      "video_oid"
    ]);
    const videoId = pickNumberish(record, ["id", "vid", "video_id", "videoId"]);
    if (ownerId && videoId) {
      return parseVideoId(ownerId, videoId);
    }
    return void 0;
  }
  function isSubtitleCollectionKey(key) {
    return key === "subs" || key === "subtitles" || key === "captions" || key === "text_tracks" || key.includes("subtitle") || key.includes("caption");
  }
  function toSubtitleDescriptor(record, context) {
    const rawUrl = pickString(record, [
      "url",
      "src",
      "file",
      "download_url",
      "downloadUrl",
      "webVttUrl",
      "webvtturl",
      "link"
    ]);
    if (!rawUrl) {
      return null;
    }
    const url = normalizeUrl(rawUrl);
    const format = inferSubtitleFormat(url) ?? (context.collectionHint ? "vtt" : null);
    if (!format) {
      return null;
    }
    const language = normalizeLanguageCandidate(
      pickString(record, [
        "lang",
        "language",
        "lang_code",
        "language_code",
        "locale",
        "locale_id",
        "srclang",
        "code",
        "subtitles_lang"
      ]) ?? context.languageHint ?? inferLanguageFromUrl(url)
    );
    if (!language) {
      return null;
    }
    return {
      source: "vk",
      format,
      language,
      url,
      translatedFromLanguage: normalizeLanguageCandidate(
        pickString(record, [
          "translatedFromLanguage",
          "translated_from_language",
          "from_lang",
          "source_lang"
        ])
      ),
      isAutoGenerated: toOptionalBoolean(
        record.is_auto ?? record.isAutoGenerated ?? record.auto_generated ?? record.autogenerated
      )
    };
  }
  function collectDescriptors(value, context, collected, visited, depth = 0) {
    if (depth > 7 || value == null) {
      return;
    }
    if (Array.isArray(value)) {
      for (const item of value.slice(0, 400)) {
        collectDescriptors(item, context, collected, visited, depth + 1);
      }
      return;
    }
    if (!isRecord$2(value) || visited.has(value)) {
      return;
    }
    visited.add(value);
    const record = value;
    const nextVideoId = context.videoId ?? extractVideoIdFromRecord(record);
    const nextLanguageHint = context.languageHint ?? normalizeLanguageCandidate(
      pickString(record, [
        "subtitles_lang",
        "lang",
        "language",
        "locale",
        "locale_id"
      ])
    );
    const descriptor = toSubtitleDescriptor(record, {
      ...context,
      languageHint: nextLanguageHint
    });
    if (descriptor) {
      collected.push({
        descriptor,
        seenAt: Date.now(),
        videoId: nextVideoId
      });
    }
    for (const [key, nestedValue] of Object.entries(record)) {
      if (nestedValue == null) {
        continue;
      }
      const nextContext = {
        collectionHint: context.collectionHint || isSubtitleCollectionKey(key.toLowerCase()),
        languageHint: nextLanguageHint,
        videoId: nextVideoId
      };
      collectDescriptors(nestedValue, nextContext, collected, visited, depth + 1);
    }
  }
  function unescapeResponseString(value) {
    return value.replace(/\\u0026/giu, "&").replace(/\\u002f/giu, "/").replace(/\\\//gu, "/");
  }
  function extractDescriptorsFromText(text, requestUrl) {
    const result = [];
    const seen = new Set();
    const defaultVideoId = extractVideoIdFromUrl(requestUrl);
    const normalizedText = unescapeResponseString(text);
    const push = (language, url, isAutoGenerated) => {
      const normalizedLanguage = normalizeLanguageCandidate(
        language ?? inferLanguageFromUrl(String(url || ""))
      );
      const normalizedUrl = typeof url === "string" ? normalizeUrl(url) : "";
      const format = inferSubtitleFormat(normalizedUrl) ?? "vtt";
      if (!normalizedLanguage || !normalizedUrl) {
        return;
      }
      const key = `${defaultVideoId ?? ""}|${normalizedLanguage}|${normalizedUrl}`;
      if (seen.has(key)) {
        return;
      }
      seen.add(key);
      result.push({
        videoId: defaultVideoId,
        seenAt: Date.now(),
        descriptor: {
          source: "vk",
          format,
          language: normalizedLanguage,
          url: normalizedUrl,
          isAutoGenerated
        }
      });
    };
    const langBeforeUrlPattern = /["'](?:lang|language|locale|subtitles_lang)["']\s*:\s*["']([a-zA-Z_-]{2,12})["'][\s\S]{0,400}?["']url["']\s*:\s*["']([^"'\\]+(?:\\.[^"'\\]*)*)["'][\s\S]{0,120}?(?:["']is_auto["']\s*:\s*(true|false|0|1))?/g;
    const urlBeforeLangPattern = /["']url["']\s*:\s*["']([^"'\\]+(?:\\.[^"'\\]*)*)["'][\s\S]{0,400}?["'](?:lang|language|locale|subtitles_lang)["']\s*:\s*["']([a-zA-Z_-]{2,12})["'][\s\S]{0,120}?(?:["']is_auto["']\s*:\s*(true|false|0|1))?/g;
    for (const match of normalizedText.matchAll(langBeforeUrlPattern)) {
      push(match[1], match[2], toOptionalBoolean(match[3]));
    }
    for (const match of normalizedText.matchAll(urlBeforeLangPattern)) {
      push(match[2], match[1], toOptionalBoolean(match[3]));
    }
    return result;
  }
  function rememberTracks(siteHost, tracks) {
    if (!tracks.length) {
      return;
    }
    const now2 = Date.now();
    const existing = sniffedTracksByHost.get(siteHost) ?? [];
    const deduped = new Map();
    for (const track of existing) {
      if (now2 - track.seenAt > TRACK_MAX_AGE_MS) {
        continue;
      }
      deduped.set(
        [
          track.videoId ?? "",
          track.descriptor.language,
          track.descriptor.translatedFromLanguage ?? "",
          track.descriptor.url
        ].join("|"),
        track
      );
    }
    for (const track of tracks) {
      if (now2 - track.seenAt > TRACK_MAX_AGE_MS) {
        continue;
      }
      deduped.set(
        [
          track.videoId ?? "",
          track.descriptor.language,
          track.descriptor.translatedFromLanguage ?? "",
          track.descriptor.url
        ].join("|"),
        track
      );
    }
    const nextTracks = Array.from(deduped.values()).sort((left, right) => right.seenAt - left.seenAt).slice(0, MAX_TRACKS_PER_HOST);
    sniffedTracksByHost.set(siteHost, nextTracks);
  }
  function rememberSubtitleUrl(rawUrl) {
    const url = normalizeUrl(rawUrl);
    const format = inferSubtitleFormat(url);
    const language = inferLanguageFromUrl(url);
    if (!format || !language) {
      return;
    }
    rememberTracks("vk", [
      {
        videoId: extractVideoIdFromUrl(url),
        seenAt: Date.now(),
        descriptor: {
          source: "vk",
          format,
          language,
          url
        }
      }
    ]);
  }
  function shouldInspectVkRequest(url) {
    return VK_REQUEST_PATTERN.test(url);
  }
  function processPotentialVkPayload(requestUrl, payload) {
    const collected = [];
    collectDescriptors(
      payload,
      {
        collectionHint: false,
        videoId: extractVideoIdFromUrl(requestUrl)
      },
      collected,
new WeakSet()
    );
    rememberTracks("vk", collected);
  }
  function processPotentialVkTextResponse(requestUrl, text) {
    if (!text || text.length > MAX_BODY_LENGTH) {
      return;
    }
    try {
      processPotentialVkPayload(requestUrl, JSON.parse(text));
      return;
    } catch {
    }
    rememberTracks("vk", extractDescriptorsFromText(text, requestUrl));
  }
  function inspectFetchResponse(url, response) {
    const contentType = String(response.headers.get("content-type") || "").trim().toLowerCase();
    if (!shouldInspectVkRequest(url) && !(contentType.includes("json") && /vkvideo|vk\.com|okcdn\.ru/i.test(url))) {
      return;
    }
    if (contentType.includes("json")) {
      void response.clone().json().then((payload) => {
        processPotentialVkPayload(url, payload);
      }).catch(() => {
        void response.clone().text().then((text) => {
          processPotentialVkTextResponse(url, text);
        }).catch(() => void 0);
      });
      return;
    }
    void response.clone().text().then((text) => {
      processPotentialVkTextResponse(url, text);
    }).catch(() => void 0);
  }
  function getSniffedSiteSubtitles(siteHost, videoId) {
    const now2 = Date.now();
    const currentTracks = (sniffedTracksByHost.get(siteHost) ?? []).filter(
      (track) => now2 - track.seenAt <= TRACK_MAX_AGE_MS
    );
    sniffedTracksByHost.set(siteHost, currentTracks);
    const filteredTracks = videoId ? currentTracks.filter(
      (track) => !track.videoId || track.videoId === videoId
    ) : currentTracks;
    const deduped = new Map();
    for (const track of filteredTracks) {
      deduped.set(
        [
          track.descriptor.language,
          track.descriptor.translatedFromLanguage ?? "",
          track.descriptor.url
        ].join("|"),
        track.descriptor
      );
    }
    return Array.from(deduped.values());
  }
  function installSiteSubtitlesSniffer() {
    if (installed) {
      return;
    }
    installed = true;
    const originalFetch = globalThis.fetch.bind(globalThis);
    globalThis.fetch = async (...args) => {
      const response = await originalFetch(...args);
      if (!isVkPage()) {
        return response;
      }
      const input = args[0];
      const url = typeof input === "string" ? input : input instanceof Request ? input.url : String(input ?? "");
      if (SUBTITLE_URL_PATTERN.test(url)) {
        rememberSubtitleUrl(url);
      }
      inspectFetchResponse(url, response);
      return response;
    };
    const originalOpen = XMLHttpRequest.prototype.open;
    XMLHttpRequest.prototype.open = function(method, url, ...rest) {
      const stringUrl = String(url);
      this[xhrUrlKey] = stringUrl;
      if (isVkPage() && SUBTITLE_URL_PATTERN.test(stringUrl)) {
        rememberSubtitleUrl(stringUrl);
      }
      return originalOpen.call(this, method, url, ...rest);
    };
    const originalSend = XMLHttpRequest.prototype.send;
    XMLHttpRequest.prototype.send = function(...args) {
      if (isVkPage()) {
        this.addEventListener(
          "load",
          () => {
            const requestUrl = this[xhrUrlKey];
            if (!requestUrl) {
              return;
            }
            const contentType = String(
              this.getResponseHeader("content-type") || ""
            ).trim().toLowerCase();
            if (!shouldInspectVkRequest(requestUrl) && !(contentType.includes("json") && /vkvideo|vk\.com|okcdn\.ru/i.test(requestUrl))) {
              return;
            }
            if (this.responseType === "json" && this.response) {
              processPotentialVkPayload(requestUrl, this.response);
              return;
            }
            if (this.responseType !== "" && this.responseType !== "text" && this.responseType !== "json") {
              return;
            }
            if (this.responseType === "json") {
              return;
            }
            try {
              const responseText = typeof this.responseText === "string" ? this.responseText : "";
              processPotentialVkTextResponse(requestUrl, responseText);
            } catch {
            }
          },
          { once: true }
        );
      }
      return originalSend.apply(this, args);
    };
  }
  const MAX_TEXT_LENGTH = 450;
  const REMOVABLE_TOKEN_FILTER = new RegExp(
    [
String.raw`(?:https?:\/\/|www\.)\S+`,
      String.raw`#[^\s#]+`,
      String.raw`auto-generated\s+by\s+youtube`,
      String.raw`provided\s+to\s+youtube\s+by`,
      String.raw`released\s+on`,
      String.raw`\bpaypal\b`,
      String.raw`\b0x[a-f0-9]{40}\b`,
String.raw`\b[13][a-km-zA-HJ-NP-Z1-9]{25,34}\b`,
String.raw`\b(?:bc1|tb1|bcrt1)[ac-hj-np-z02-9]{11,71}\b`,
String.raw`\b(?:-1|0):[a-f0-9]{64}\b`
    ].join("|"),
    "giu"
  );
  const NOISE_CHARACTER_FILTER = /[\p{N}\p{P}\p{S}]+/gu;
  const WHITESPACE_FILTER = /\s+/g;
  const LETTER_FILTER = new RegExp("\\p{L}", "u");
  function trimToMaxLength(text, maxLength) {
    if (text.length <= maxLength) return text;
    return text.slice(0, maxLength).trimEnd();
  }
  function cleanText(title, description) {
    const raw = `${title ?? ""} ${description ?? ""}`.trim();
    if (!raw) return "";
    const cleaned = raw.normalize("NFKC").replace(REMOVABLE_TOKEN_FILTER, " ").replace(NOISE_CHARACTER_FILTER, " ").replace(WHITESPACE_FILTER, " ").trim();
    if (!LETTER_FILTER.test(cleaned)) {
      return "";
    }
    return trimToMaxLength(cleaned, MAX_TEXT_LENGTH);
  }
  const VIDEO_VOLUME_MIN_PERCENT = 0;
  const VIDEO_VOLUME_MAX_PERCENT = 100;
  const VIDEO_VOLUME_STEP_01 = 0.01;
  const EPS = 1e-6;
  function clampNumber(value, min, max) {
    if (!Number.isFinite(value)) return min;
    if (max < min) return min;
    return Math.max(min, Math.min(max, value));
  }
  function clampInt(value, min, max) {
    return Math.trunc(clampNumber(value, min, max));
  }
  function clampPercentInt(value, min = VIDEO_VOLUME_MIN_PERCENT, max = VIDEO_VOLUME_MAX_PERCENT) {
    if (!Number.isFinite(value)) return min;
    return clampInt(Math.round(value), min, max);
  }
  function volume01ToPercent(volume01) {
    const v2 = clampNumber(volume01, 0, 1);
    return clampPercentInt(v2 * 100);
  }
  function percentToVolume01(percent) {
    return clampPercentInt(percent) / 100;
  }
  function quantizeToStep(value, step, direction) {
    if (!Number.isFinite(value)) return value;
    if (!Number.isFinite(step) || step <= 0) return value;
    const inv = 1 / step;
    const scaled = value * inv;
    switch (direction) {
      case "down":
        return Math.floor(scaled + EPS) / inv;
      case "up":
        return Math.ceil(scaled - EPS) / inv;
      default:
        return Math.round(scaled) / inv;
    }
  }
  function snapVolume01(volume01, direction = "nearest", step = VIDEO_VOLUME_STEP_01) {
    const clamped = clampNumber(volume01, 0, 1);
    const quantized = quantizeToStep(clamped, step, direction);
    return clampNumber(quantized, 0, 1);
  }
  function snapVolume01Towards(next, current, desired, step = VIDEO_VOLUME_STEP_01) {
    const cur = clampNumber(current, 0, 1);
    const des = clampNumber(desired, 0, 1);
    if (des < cur) {
      const q = snapVolume01(next, "down", step);
      return Math.max(des, q);
    }
    if (des > cur) {
      const q = snapVolume01(next, "up", step);
      return Math.min(des, q);
    }
    return snapVolume01(next, "nearest", step);
  }
  function getStructuredVideoData() {
    const scripts = Array.from(
      document.querySelectorAll('script[type="application/ld+json"]')
    );
    for (const script of scripts) {
      const raw = script.textContent?.trim();
      if (!raw) continue;
      try {
        const parsed = JSON.parse(raw);
        const items = Array.isArray(parsed) ? parsed : [parsed];
        for (const item of items) {
          if (!item || typeof item !== "object") continue;
          if (item["@type"] !== "VideoObject") continue;
          return {
            contentUrl: typeof item.contentUrl === "string" ? item.contentUrl : void 0,
            embedUrl: typeof item.embedUrl === "string" ? item.embedUrl : void 0,
            pageUrl: typeof item.url === "string" ? item.url : void 0,
            title: typeof item.name === "string" ? item.name : void 0
          };
        }
      } catch {
      }
    }
    return null;
  }
  function toStableVideoId(candidate, fallback) {
    const value = String(candidate || "").trim();
    if (!value) {
      return fallback;
    }
    try {
      const parsed = new URL(value, globalThis.location.href);
      parsed.hash = "";
      return parsed.toString();
    } catch {
      return value;
    }
  }
  function pickBestResourceUrl(names) {
    const clean = names.filter(Boolean).map((x2) => String(x2).trim()).filter(Boolean);
    const reject = (url) => {
      const lower = url.toLowerCase();
      const isManifest = /\.m3u8([?#]|$)/i.test(lower) || /\.mpd([?#]|$)/i.test(lower);
      return lower.includes("thumbnails.") || lower.includes("/speech/") || lower.endsWith(".jpg") || lower.endsWith(".jpeg") || lower.endsWith(".png") || lower.endsWith(".webp") || lower.includes("okcdn.ru/?") || /[?&]bytes=\d+-\d+/i.test(lower) || !isManifest && /[?&]type=\d+/i.test(lower);
    };
    const candidates = clean.filter((url) => !reject(url));
    return candidates.find((u2) => /\.m3u8([?#]|$)/i.test(u2)) || candidates.find((u2) => /\.mpd([?#]|$)/i.test(u2)) || candidates.find((u2) => /\.mp4([?#]|$)/i.test(u2)) || candidates.find((u2) => /\/streams\//i.test(u2)) || null;
  }
  function pickBestDirectSource(direct) {
    const candidates = [
      String(direct?.hlsUrl || "").trim(),
      String(direct?.sources?.hlsUrl || "").trim(),
      String(direct?.dashUrl || "").trim(),
      String(direct?.sources?.dashUrl || "").trim(),
      String(direct?.mpegLowUrl || "").trim(),
      String(direct?.url || "").trim()
    ].filter(Boolean);
    return candidates.find((u2) => /\.m3u8([?#]|$)/i.test(u2)) || candidates.find((u2) => /\.mpd([?#]|$)/i.test(u2)) || candidates.find((u2) => /\.mp4([?#]|$)/i.test(u2)) || candidates[0] || "";
  }
  function isBadDirectVideoUrl(url) {
    const lower = String(url || "").toLowerCase();
    const isManifest = /\.m3u8([?#]|$)/i.test(lower) || /\.mpd([?#]|$)/i.test(lower);
    return !lower || lower.startsWith("blob:") || lower.includes("thumbnails.") || lower.includes("/speech/") || lower.endsWith(".jpg") || lower.endsWith(".jpeg") || lower.endsWith(".png") || lower.endsWith(".webp") || lower.includes("okcdn.ru/?") || /[?&]bytes=\d+-\d+/i.test(lower) || !isManifest && /[?&]type=\d+/i.test(lower);
  }
  async function resolveCustomSiteVideo(hostname, href) {
    const tunnelPlayer = getTunnelPlayerContext(href);
    if (tunnelPlayer) {
      const entries = performance.getEntriesByType("resource");
      const names = entries.map((e2) => typeof e2.name === "string" ? e2.name : "").filter(Boolean);
      const resourceUrl = pickBestResourceUrl(names);
      const video = document.querySelector("video");
      const currentSrc = String(video?.currentSrc || video?.src || "").trim();
      const resolvedUrl = (!isBadDirectVideoUrl(tunnelPlayer.sourceUrl || "") ? String(tunnelPlayer.sourceUrl) : "") || (!isBadDirectVideoUrl(currentSrc) ? currentSrc : "") || tunnelPlayer.playerUrl;
      console.log("[VOT][custom][tunnel] resolved tunnel player source", {
        playerUrl: tunnelPlayer.playerUrl,
        sourceUrl: tunnelPlayer.sourceUrl,
        playlistUrl: tunnelPlayer.playlistUrl,
        currentSrc,
        resourceUrl,
        resolvedUrl
      });
      return {
        url: resolvedUrl,
        videoId: toStableVideoId(
          resolvedUrl || tunnelPlayer.sourceUrl || href,
          tunnelPlayer.sourceUrl || resolvedUrl || href
        ),
        title: document.title
      };
    }
    let direct = globalThis.__VOT_DIRECT_SOURCES__;
    if (!direct) {
      try {
        const raw = document.documentElement.dataset.votDirectSources;
        if (raw) {
          direct = JSON.parse(raw);
        }
      } catch {
      }
    }
    if (direct && typeof direct === "object") {
      const bestDirect = pickBestDirectSource(direct);
      const unitedVideoId = String(direct.unitedVideoId || href).trim();
      if (bestDirect && !isBadDirectVideoUrl(bestDirect)) {
        console.log("[VOT][custom][direct-sources] resolved direct source", {
          bestDirect,
          unitedVideoId
        });
        return {
          url: bestDirect,
          videoId: toStableVideoId(
            unitedVideoId || bestDirect,
            bestDirect || href
          ),
          title: String(direct.title || document.title || "")
        };
      }
    }
    if (/^odysee\.com$/i.test(hostname)) {
      const structured = getStructuredVideoData();
      if (structured?.contentUrl && /^https?:\/\//i.test(structured.contentUrl)) {
        console.log(
          "[VOT][custom][odysee] resolved contentUrl",
          structured.contentUrl
        );
        return {
          url: structured.contentUrl,
          videoId: href,
          title: structured.title || document.title
        };
      }
      const entries = performance.getEntriesByType("resource");
      const names = entries.map((e2) => typeof e2.name === "string" ? e2.name : "").filter(Boolean);
      const isBadAsset = (url) => {
        const lower = url.toLowerCase();
        return lower.includes(".jpg") || lower.includes(".jpeg") || lower.includes(".png") || lower.includes(".webp") || lower.includes("/speech/") || lower.includes("thumbnails.odycdn.com");
      };
      const pick = (predicate) => names.find((url) => !isBadAsset(url) && predicate(url)) || null;
      const bestUrl = pick((url) => url.toLowerCase().includes(".m3u8")) || pick((url) => url.toLowerCase().includes(".mpd")) || pick((url) => url.toLowerCase().includes(".mp4")) || pick((url) => url.toLowerCase().includes("/streams/"));
      if (bestUrl) {
        console.log("[VOT][custom][odysee] resolved media url", bestUrl);
        return {
          url: bestUrl,
          videoId: href,
          title: structured?.title || document.title
        };
      }
      const video = document.querySelector("video");
      const currentSrc = String(video?.currentSrc || video?.src || "");
      if (currentSrc && !currentSrc.startsWith("blob:") && !isBadAsset(currentSrc)) {
        console.log("[VOT][custom][odysee] resolved direct src", currentSrc);
        return {
          url: currentSrc,
          videoId: href,
          title: structured?.title || document.title
        };
      }
      if (structured?.embedUrl) {
        console.log(
          "[VOT][custom][odysee] fallback embedUrl",
          structured.embedUrl
        );
        return {
          url: structured.embedUrl,
          videoId: href,
          title: structured.title || document.title
        };
      }
      console.log("[VOT][custom][odysee] fallback page url", href);
      return {
        url: href,
        videoId: href,
        title: structured?.title || document.title
      };
    }
    if (/^player\.cdnvideohub\.com$/i.test(hostname) || /(^|\.)okcdn\.ru$/i.test(hostname)) {
      const referrer = String(document.referrer || "").trim();
      const video = document.querySelector("video");
      const currentSrc = String(video?.currentSrc || video?.src || "").trim();
      if (!isBadDirectVideoUrl(currentSrc)) {
        console.log("[VOT][custom][cdnvideohub] using direct src", currentSrc);
        return {
          url: currentSrc,
          videoId: toStableVideoId(currentSrc, referrer || href),
          title: document.title
        };
      }
      const entries = performance.getEntriesByType("resource");
      const names = entries.map((e2) => typeof e2.name === "string" ? e2.name : "").filter(Boolean);
      const best = pickBestResourceUrl(names);
      if (best) {
        console.log(
          "[VOT][custom][cdnvideohub] using performance resource",
          best
        );
        return {
          url: best,
          videoId: toStableVideoId(best, referrer || href),
          title: document.title
        };
      }
      if (/^https?:\/\/([^/]+\.)?kodik(player\.com|\.info|\.biz|\.cc|\.fun|\.pw|\.io|\.online|\.me)\//i.test(
        referrer
      )) {
        console.log("[VOT][custom][cdnvideohub] using kodik referrer", referrer);
        return {
          url: referrer,
          videoId: referrer,
          title: document.title
        };
      }
      console.log(
        "[VOT][custom][cdnvideohub] no usable url found, returning null for sniffed fallback"
      );
      return null;
    }
    if (/(^|\.)wikianimex\.ru$/i.test(hostname)) {
      const video = document.querySelector("video");
      const currentSrc = String(video?.currentSrc || video?.src || "").trim();
      if (!isBadDirectVideoUrl(currentSrc)) {
        console.log("[VOT][custom][wikianimex] using direct src", currentSrc);
        return {
          url: currentSrc,
          videoId: toStableVideoId(currentSrc, href),
          title: document.title
        };
      }
      const entries = performance.getEntriesByType("resource");
      const names = entries.map((e2) => typeof e2.name === "string" ? e2.name : "").filter(Boolean);
      const best = pickBestResourceUrl(names);
      if (best) {
        console.log("[VOT][custom][wikianimex] using performance resource", best);
        return {
          url: best,
          videoId: toStableVideoId(best, href),
          title: document.title
        };
      }
      const iframes = Array.from(document.querySelectorAll("iframe"));
      for (const iframe of iframes) {
        const src = String(iframe.src || iframe.getAttribute("src") || "").trim();
        if (!src || src === "about:blank" || src.startsWith("javascript:")) {
          continue;
        }
        if (/kodik(player\.com|\.info|\.biz|\.cc|\.fun|\.pw|\.io|\.online|\.me)/i.test(
          src
        )) {
          console.log("[VOT][custom][wikianimex] using kodik iframe src", src);
          return { url: src, videoId: src, title: document.title };
        }
      }
      console.log(
        "[VOT][custom][wikianimex] no usable url, deferring to sniffedManifestUrl"
      );
      return null;
    }
    {
      const entries = performance.getEntriesByType("resource");
      const names = entries.map((e2) => typeof e2.name === "string" ? e2.name : "").filter(Boolean);
      const best = pickBestResourceUrl(names);
      if (best) {
        console.log("[VOT][custom][generic] resolved media url", best);
        return {
          url: best,
          videoId: toStableVideoId(best, href),
          title: document.title
        };
      }
      const video = document.querySelector("video");
      const currentSrc = String(video?.currentSrc || video?.src || "");
      if (!isBadDirectVideoUrl(currentSrc)) {
        console.log("[VOT][custom][generic] resolved direct src", currentSrc);
        return {
          url: currentSrc,
          videoId: toStableVideoId(currentSrc, href),
          title: document.title
        };
      }
    }
    return null;
  }
  function isRutubeSupportedPageHost(hostname) {
    return /(?:^|\.)rutube\.ru$/i.test(String(hostname || "").trim());
  }
  function extractRutubeVideoId(...candidates) {
    for (const candidate of candidates) {
      if (typeof candidate !== "string") {
        continue;
      }
      const normalized = candidate.trim();
      if (!normalized) {
        continue;
      }
      const pageMatch = normalized.match(
        /(?:^|\/)video\/([a-z0-9]{12,})(?:\/|$)/i
      );
      if (pageMatch?.[1]) {
        return pageMatch[1].toLowerCase();
      }
      const manifestMatch = normalized.match(
        /(?:^|\/)route\/([a-z0-9]{12,})\.m3u8(?:[?#]|$)/i
      );
      if (manifestMatch?.[1]) {
        return manifestMatch[1].toLowerCase();
      }
    }
    return void 0;
  }
  function buildCanonicalRutubeFallbackTarget(pageUrl, ...candidates) {
    const videoId = extractRutubeVideoId(pageUrl, ...candidates);
    if (!videoId) {
      return null;
    }
    return {
      videoId,
      url: `https://rutube.ru/video/${videoId}/`
    };
  }
  function isVkSupportedPageHost(hostname) {
    return /(?:^|\.)vkvideo\.ru$|(?:^|\.)vk\.(?:com|ru)$/i.test(
      String(hostname || "").trim()
    );
  }
  function extractVkMediaId(...candidates) {
    for (const candidate of candidates) {
      if (typeof candidate !== "string") {
        continue;
      }
      const normalized = candidate.trim();
      if (!normalized) {
        continue;
      }
      const match = normalized.match(/\b((?:video|clip)-\d+_\d+)\b/i);
      if (match?.[1]) {
        return match[1].toLowerCase();
      }
    }
    return void 0;
  }
  function normalizeVkMediaIdToVideo(mediaId) {
    const normalized = String(mediaId || "").trim().toLowerCase();
    if (!normalized) {
      return normalized;
    }
    if (normalized.startsWith("clip-")) {
      return `video-${normalized.slice("clip-".length)}`;
    }
    return normalized;
  }
  function buildCanonicalVkFallbackTarget(pageUrl, ...candidates) {
    const mediaId = extractVkMediaId(...candidates, pageUrl);
    if (!mediaId) {
      return null;
    }
    const normalizedVideoId = normalizeVkMediaIdToVideo(mediaId);
    if (!normalizedVideoId) {
      return null;
    }
    return {
      videoId: normalizedVideoId,
      url: `https://vk.com/video?z=${normalizedVideoId}`
    };
  }
  const MOBILE_OR_MUSIC_PLAYER_SELECTORS = [
    "#movie_player",
    "ytm-player",
    "ytmusic-player",
    "#player",
    ".player-container",
    "video"
  ];
  function getHostname() {
    return String(globalThis.location.hostname || "").trim().toLowerCase();
  }
  function asRecord(value) {
    return value && typeof value === "object" ? value : null;
  }
  function parseMaybeJson(value) {
    if (!value) {
      return void 0;
    }
    if (typeof value === "string") {
      try {
        return JSON.parse(value);
      } catch {
        return void 0;
      }
    }
    if (typeof value === "object") {
      return value;
    }
    return void 0;
  }
  function isPlayerResponse(value) {
    const candidate = asRecord(value);
    return Boolean(
      candidate && (typeof candidate.videoDetails === "object" || typeof candidate.captions === "object")
    );
  }
  function isPlayerData(value) {
    const candidate = asRecord(value);
    return Boolean(
      candidate && (typeof candidate.video_id === "string" || typeof candidate.videoId === "string" || typeof candidate.title === "string")
    );
  }
  function getPlayerFallback() {
    for (const selector of MOBILE_OR_MUSIC_PLAYER_SELECTORS) {
      const matched = document.querySelector(selector);
      if (matched) {
        return matched;
      }
    }
    return null;
  }
  function getWindowPlayerResponse() {
    const win = globalThis;
    const direct = parseMaybeJson(
      win.ytInitialPlayerResponse
    );
    if (isPlayerResponse(direct)) {
      return direct;
    }
    const ytplayer = asRecord(win.ytplayer);
    const config2 = asRecord(ytplayer?.config);
    const args = asRecord(config2?.args);
    const fromArgs = parseMaybeJson(args?.player_response);
    if (isPlayerResponse(fromArgs)) {
      return fromArgs;
    }
    const fromConfig = parseMaybeJson(
      config2?.player_response
    );
    return isPlayerResponse(fromConfig) ? fromConfig : void 0;
  }
  function getWindowPlayerData() {
    const win = globalThis;
    const direct = parseMaybeJson(win.ytInitialData);
    if (isPlayerData(direct)) {
      return direct;
    }
    return void 0;
  }
  function getLocationVideoId(url) {
    const searchVideoId = url.searchParams.get("v");
    if (searchVideoId) {
      return searchVideoId;
    }
    const pathSegments = url.pathname.split("/").filter(Boolean);
    for (const marker of ["shorts", "embed", "live"]) {
      const markerIndex = pathSegments.indexOf(marker);
      if (markerIndex !== -1) {
        const candidate = pathSegments[markerIndex + 1];
        if (candidate) {
          return candidate;
        }
      }
    }
    if (url.hostname === "youtu.be") {
      return pathSegments[0];
    }
    return void 0;
  }
  function isMobileYouTubeAdditionalData(value) {
    return value === "mobile" || value === "music";
  }
  class YoutubeHelper2 {
    static isMobile() {
      return getHostname() === "m.youtube.com";
    }
    static isMusic() {
      return getHostname() === "music.youtube.com";
    }
    static isMobileVariant() {
      return YoutubeHelper2.isMobile() || YoutubeHelper2.isMusic();
    }
    static getPlayer() {
      const player2 = YoutubeHelper$1.getPlayer();
      if (player2) {
        return player2;
      }
      if (!YoutubeHelper2.isMobileVariant()) {
        return null;
      }
      return getPlayerFallback();
    }
    static getPlayerResponse() {
      const playerResponse = YoutubeHelper2.getPlayer()?.getPlayerResponse?.call(void 0);
      if (isPlayerResponse(playerResponse)) {
        return playerResponse;
      }
      const extResponse = YoutubeHelper$1.getPlayerResponse();
      if (isPlayerResponse(extResponse)) {
        return extResponse;
      }
      return getWindowPlayerResponse();
    }
    static getPlayerData() {
      const playerData = YoutubeHelper2.getPlayer()?.getVideoData?.call(void 0);
      if (isPlayerData(playerData)) {
        return playerData;
      }
      const extPlayerData = YoutubeHelper$1.getPlayerData();
      if (isPlayerData(extPlayerData)) {
        return extPlayerData;
      }
      const response = YoutubeHelper2.getPlayerResponse();
      if (response?.videoDetails?.title || response?.videoDetails?.videoId) {
        return {
          title: response.videoDetails.title,
          videoId: response.videoDetails.videoId
        };
      }
      return getWindowPlayerData();
    }
    static getVolume() {
      const player2 = YoutubeHelper2.getPlayer();
      if (player2?.getVolume) {
        return player2.getVolume() / 100;
      }
      const media = document.querySelector("video");
      return media instanceof HTMLMediaElement ? media.volume : 1;
    }
    static setVolume(volume) {
      const player2 = YoutubeHelper2.getPlayer();
      if (player2?.setVolume) {
        return player2.setVolume(Math.round(volume * 100));
      }
      const media = document.querySelector("video");
      if (media instanceof HTMLMediaElement) {
        media.volume = volume;
        return true;
      }
      return false;
    }
    static isMuted() {
      const player2 = YoutubeHelper2.getPlayer();
      if (player2?.isMuted) {
        return player2.isMuted();
      }
      const media = document.querySelector("video");
      return media instanceof HTMLMediaElement ? media.muted : false;
    }
    static getVideoDuration(video) {
      const playerDuration = YoutubeHelper2.getPlayer()?.getDuration?.call(void 0);
      if (typeof playerDuration === "number" && Number.isFinite(playerDuration)) {
        return playerDuration;
      }
      const media = video ?? document.querySelector("video");
      return media instanceof HTMLMediaElement && Number.isFinite(media.duration) && media.duration > 0 ? media.duration : void 0;
    }
    static getCurrentVideoId(url = new URL(globalThis.location.href)) {
      const playerData = YoutubeHelper2.getPlayerData();
      const response = YoutubeHelper2.getPlayerResponse();
      const candidates = [
        playerData?.videoId,
        playerData?.video_id,
        response?.videoDetails?.videoId,
        (() => {
          try {
            const playerUrl = YoutubeHelper2.getPlayer()?.getVideoUrl?.call(void 0);
            return playerUrl ? getLocationVideoId(new URL(playerUrl)) : void 0;
          } catch {
            return void 0;
          }
        })(),
        getLocationVideoId(url)
      ];
      return candidates.find(
        (candidate) => typeof candidate === "string" && candidate.trim().length > 0
      );
    }
    static getCanonicalVideoUrl(videoId) {
      const resolvedVideoId = videoId ?? YoutubeHelper2.getCurrentVideoId();
      return resolvedVideoId ? `https://youtu.be/${encodeURIComponent(resolvedVideoId)}` : void 0;
    }
    static getSubtitles(userLang) {
      const response = YoutubeHelper2.getPlayerResponse();
      const playerCaptions = response?.captions?.playerCaptionsTracklistRenderer;
      if (!playerCaptions) {
        return [];
      }
      const captionTracks = playerCaptions.captionTracks ?? [];
      const translationLanguages = playerCaptions.translationLanguages ?? [];
      const userLangSupported = translationLanguages.find(
        (language) => language.languageCode === userLang
      );
      const asrSubtitleItem = captionTracks.find(
        (captionTrack) => captionTrack?.kind === "asr"
      );
      const asrLang = asrSubtitleItem?.languageCode ?? "en";
      return captionTracks.reduce((result, captionTrack) => {
        if (!captionTrack.languageCode || !captionTrack.baseUrl) {
          return result;
        }
        const language = normalizeLang$1(captionTrack.languageCode);
        if (!language) {
          return result;
        }
        const baseUrl = captionTrack.baseUrl;
        const captionUrl = `${baseUrl.startsWith("http") ? baseUrl : `${window.location.origin}/${baseUrl}`}&fmt=json3`;
        result.push({
          source: "youtube",
          format: "json",
          language,
          isAutoGenerated: captionTrack.kind === "asr",
          url: captionUrl
        });
        if (userLangSupported && captionTrack.isTranslatable && captionTrack.languageCode === asrLang && userLang !== language) {
          result.push({
            source: "youtube",
            format: "json",
            language: userLang,
            isAutoGenerated: captionTrack.kind === "asr",
            translatedFromLanguage: language,
            url: `${captionUrl}&tlang=${userLang}`
          });
        }
        return result;
      }, []);
    }
    static getLanguage() {
      const trackInfo = YoutubeHelper2.getPlayer()?.getAudioTrack?.()?.getLanguageInfo?.();
      if (trackInfo?.id && trackInfo.id !== "und") {
        return normalizeLang$1(trackInfo.id.split(".")[0] ?? trackInfo.id);
      }
      const response = YoutubeHelper2.getPlayerResponse();
      const autoCaption = response?.captions?.playerCaptionsTracklistRenderer?.captionTracks?.find(
        (caption) => caption.kind === "asr" && caption.languageCode
      );
      return autoCaption?.languageCode ? normalizeLang$1(autoCaption.languageCode) : void 0;
    }
  }
  const FORCED_DETECTED_LANGUAGE_BY_HOST = {
    rutube: "ru",
    "ok.ru": "ru",
    mail_ru: "ru",
    weverse: "ko",
    niconico: "ja",
    youku: "zh",
    bilibili: "zh",
    weibo: "zh",
    zdf: "de"
  };
  const YT_VOLUME_NOW_SELECTOR = ".ytp-volume-panel [aria-valuenow]";
  const MIN_DETECT_TEXT_LENGTH = 35;
  const MAX_SHARED_LANGUAGE_STATES = 500;
  const REQUEST_LANG_SET = new Set(
    availableLangs
  );
  const sharedLanguageStateByVideoId = new Map();
  function normalizeLocalTitleFromUrl(value) {
    if (typeof value !== "string") return void 0;
    try {
      const parsed = new URL(value, globalThis.location.href);
      const lastSegment = decodeURIComponent(
        parsed.pathname.split("/").pop() || ""
      );
      return lastSegment.replace(/\.[^.]+$/u, "").trim() || void 0;
    } catch {
      return void 0;
    }
  }
  function getGoogleDrivePageTitle() {
    const exactDriveTitle = Array.from(
      document.querySelectorAll('[data-is-tooltip-wrapper="true"] > span')
    ).map((el) => el.textContent?.trim() ?? "").find((text) => /\.(mp4|mkv|mov|webm|avi|m4v)$/i.test(text)) || "";
    const metaTitle = document.querySelector('meta[property="og:title"], meta[name="title"]')?.getAttribute("content")?.trim() || "";
    const headerTitle = document.querySelector(
      'h1, [role="heading"], div[role="heading"], [data-tooltip]'
    )?.textContent?.trim() || "";
    const docTitle = String(document.title || "").trim();
    const raw = exactDriveTitle || metaTitle || headerTitle || docTitle;
    if (!raw) return void 0;
    return raw.replace(/\s*-\s*Google Drive\s*$/i, "").replace(/\s*-\s*Google Диск\s*$/i, "").trim();
  }
  function isBadGoogleDriveTitle(value) {
    if (typeof value !== "string") return true;
    const normalized = value.trim();
    if (!normalized) return true;
    return /^youtube$/i.test(normalized) || /^google drive$/i.test(normalized) || /^google диск$/i.test(normalized) || /^[A-Za-z0-9_-]{20,}$/.test(normalized);
  }
  function normalizeGoogleDriveTitle(value) {
    if (typeof value !== "string") return void 0;
    const normalized = value.replace(/\s*-\s*Google Drive\s*$/i, "").replace(/\s*-\s*Google Диск\s*$/i, "").trim();
    return normalized || void 0;
  }
  async function getStoredGoogleDriveTitle(videoId) {
    try {
      const storedTitle = await votStorage.get(`googledrive:title:${videoId}`);
      return normalizeGoogleDriveTitle(storedTitle);
    } catch {
      return void 0;
    }
  }
  function inferSubtitleFormatFromUrl(url) {
    const normalized = url.split(/[?#]/u, 1)[0]?.toLowerCase() ?? "";
    if (normalized.endsWith(".srt")) return "srt";
    if (normalized.endsWith(".json")) return "json";
    return "vtt";
  }
  function getTrackSubtitleSource(siteHost) {
    if (siteHost === "custom") {
      return String(globalThis.location.hostname || "native").trim() || "native";
    }
    return siteHost || String(globalThis.location.hostname || "native").trim();
  }
  function getElementTrackSubtitles(video, source) {
    const subtitles = [];
    const tracks = Array.from(video.querySelectorAll("track"));
    for (const track of tracks) {
      const kind = String(track.kind || track.getAttribute("kind") || "").trim().toLowerCase();
      if (kind === "metadata") {
        continue;
      }
      const language = String(
        track.srclang || track.track?.language || track.getAttribute("srclang") || ""
      ).trim().toLowerCase();
      const rawUrl = String(track.src || track.getAttribute("src") || "").trim();
      if (!language || !rawUrl) {
        continue;
      }
      let url = rawUrl;
      try {
        url = new URL(rawUrl, document.baseURI).href;
      } catch {
      }
      subtitles.push({
        language,
        url,
        format: inferSubtitleFormatFromUrl(url),
        source,
        isAutoGenerated: kind === "captions"
      });
    }
    return subtitles;
  }
  function mergeSubtitleDescriptors(primary, extra) {
    const merged = [];
    const seen = new Set();
    const append = (list) => {
      if (!Array.isArray(list)) {
        return;
      }
      for (const item of list) {
        if (!item || typeof item !== "object") {
          continue;
        }
        const subtitle = item;
        if (typeof subtitle.language !== "string" || typeof subtitle.url !== "string" || typeof subtitle.source !== "string" || typeof subtitle.format !== "string") {
          continue;
        }
        const key = [
          subtitle.source,
          subtitle.language,
          subtitle.translatedFromLanguage ?? "",
          subtitle.url
        ].join("|");
        if (seen.has(key)) {
          continue;
        }
        seen.add(key);
        merged.push({
          language: subtitle.language,
          url: subtitle.url,
          source: subtitle.source,
          format: subtitle.format,
          translatedFromLanguage: typeof subtitle.translatedFromLanguage === "string" ? subtitle.translatedFromLanguage : void 0,
          isAutoGenerated: typeof subtitle.isAutoGenerated === "boolean" ? subtitle.isAutoGenerated : void 0
        });
      }
    };
    append(primary);
    append(extra);
    return merged;
  }
  function isBadGenericVideoId(value) {
    if (typeof value !== "string") return true;
    const normalized = value.trim().toLowerCase();
    if (!normalized) return true;
    return normalized === "undefined" || normalized === "null" || normalized.startsWith("blob:") || normalized.includes("/frame/") || /\/s\d+\/v[\d.]+\/frame\/?$/i.test(normalized) || normalized.includes("okcdn.ru/?") || /[?&]bytes=\d+-\d+/i.test(normalized) || /[?&]type=\d+/i.test(normalized);
  }
  function isPageScopedVideoId(value, pageUrl) {
    if (typeof value !== "string") {
      return false;
    }
    const normalized = value.trim();
    if (!normalized) {
      return false;
    }
    if (normalized === pageUrl) {
      return true;
    }
    try {
      const candidateUrl = new URL(normalized, globalThis.location.href);
      const currentUrl = new URL(pageUrl, globalThis.location.href);
      candidateUrl.hash = "";
      currentUrl.hash = "";
      if (candidateUrl.toString() === currentUrl.toString()) {
        return true;
      }
      if (candidateUrl.origin !== currentUrl.origin || candidateUrl.pathname !== currentUrl.pathname) {
        return false;
      }
      const normalizeSearch = (url) => Array.from(url.searchParams.entries()).sort(([leftKey, leftValue], [rightKey, rightValue]) => {
        if (leftKey === rightKey) {
          return leftValue.localeCompare(rightValue);
        }
        return leftKey.localeCompare(rightKey);
      }).map(([key, paramValue]) => `${key}=${paramValue}`).join("&");
      return normalizeSearch(candidateUrl) === normalizeSearch(currentUrl);
    } catch {
      return false;
    }
  }
  function isBadGenericMediaUrl(value) {
    if (typeof value !== "string") return true;
    const normalized = value.trim().toLowerCase();
    if (!normalized) return true;
    const isManifest = /\.m3u8([?#]|$)/i.test(normalized) || /\.mpd([?#]|$)/i.test(normalized);
    return normalized.startsWith("blob:") || normalized.startsWith("data:") || normalized.includes("/frame/") || /\/s\d+\/v[\d.]+\/frame\/?$/i.test(normalized) || normalized.includes("okcdn.ru/?") || /[?&]bytes=\d+-\d+/i.test(normalized) || !isManifest && /[?&]type=\d+/i.test(normalized);
  }
  function pickPreferredVideoUrl(...candidates) {
    for (const candidate of candidates) {
      if (typeof candidate !== "string") {
        continue;
      }
      const normalized = candidate.trim();
      if (!normalized || isBadGenericMediaUrl(normalized)) {
        continue;
      }
      return normalized;
    }
    return "";
  }
  function isUsefulResolvedFallback(currentUrl, currentVideoId, resolved) {
    if (!resolved || isBadGenericMediaUrl(resolved.url)) {
      return false;
    }
    return isBadGenericMediaUrl(currentUrl) || isBadGenericVideoId(currentVideoId) || /^https?:\/\//i.test(resolved.url) && !/^https?:\/\//i.test(String(currentUrl || "").trim());
  }
  function isBilibiliSupportedPageHost(hostname) {
    return /^(www|m)\.bilibili\.com$/i.test(String(hostname || "").trim());
  }
  function getSharedLanguageState(videoId) {
    const cachedState = sharedLanguageStateByVideoId.get(videoId);
    if (cachedState) {
      return cachedState;
    }
    const createdState = {};
    sharedLanguageStateByVideoId.set(videoId, createdState);
    while (sharedLanguageStateByVideoId.size > MAX_SHARED_LANGUAGE_STATES) {
      const oldestVideoId = sharedLanguageStateByVideoId.keys().next().value;
      if (typeof oldestVideoId !== "string") {
        break;
      }
      sharedLanguageStateByVideoId.delete(oldestVideoId);
    }
    return createdState;
  }
  function normalizeToRequestLang(value) {
    if (typeof value !== "string") return void 0;
    const normalized = value.toLowerCase().split(/[-_]/)[0];
    return REQUEST_LANG_SET.has(normalized) ? normalized : void 0;
  }
  function isResolvedLanguage(value) {
    return Boolean(value && value !== "auto");
  }
  function buildDetectText(title, description) {
    const textTitle = typeof title === "string" ? title : "";
    const textDescription = typeof description === "string" ? description : void 0;
    return cleanText(textTitle, textDescription);
  }
  function resolveHostDetectedLanguage(host) {
    const forcedDetectedLanguage = FORCED_DETECTED_LANGUAGE_BY_HOST[host];
    if (forcedDetectedLanguage) {
      return forcedDetectedLanguage;
    }
    if (host === "vk") {
      const trackLang = document.getElementsByTagName("track")?.[0]?.srclang;
      return normalizeToRequestLang(trackLang);
    }
    return void 0;
  }
  function resolveYoutubeDetectedLanguageFromSubtitles(subtitles) {
    if (!Array.isArray(subtitles) || subtitles.length === 0) {
      return void 0;
    }
    const pickLanguage = (preferManual) => {
      for (const subtitle of subtitles) {
        if (!subtitle || typeof subtitle !== "object") {
          continue;
        }
        const candidate = subtitle;
        if (candidate.source !== "youtube") {
          continue;
        }
        if (typeof candidate.translatedFromLanguage === "string") {
          continue;
        }
        if (preferManual && candidate.isAutoGenerated === true) {
          continue;
        }
        const language = normalizeToRequestLang(candidate.language);
        if (isResolvedLanguage(language)) {
          return language;
        }
      }
      return void 0;
    };
    return pickLanguage(true) ?? pickLanguage(false);
  }
  async function resolveDetectedLanguageForVideo(options) {
    if (options.isStream) {
      return { detectedLanguage: "auto" };
    }
    if (options.userOverrideLanguage) {
      return { detectedLanguage: options.userOverrideLanguage };
    }
    const hostDetectedLanguage = resolveHostDetectedLanguage(options.host);
    if (isResolvedLanguage(hostDetectedLanguage)) {
      return {
        detectedLanguage: hostDetectedLanguage,
        cacheLanguage: hostDetectedLanguage
      };
    }
    const normalizedPossibleLanguage = normalizeToRequestLang(
      options.possibleLanguage
    );
    if (isResolvedLanguage(normalizedPossibleLanguage)) {
      return {
        detectedLanguage: normalizedPossibleLanguage,
        cacheLanguage: normalizedPossibleLanguage
      };
    }
    const youtubeSubtitleDetectedLanguage = options.host === "youtube" ? resolveYoutubeDetectedLanguageFromSubtitles(options.subtitles) : void 0;
    if (isResolvedLanguage(youtubeSubtitleDetectedLanguage)) {
      return {
        detectedLanguage: youtubeSubtitleDetectedLanguage,
        cacheLanguage: youtubeSubtitleDetectedLanguage
      };
    }
    if (options.cachedDetectedLanguage) {
      return { detectedLanguage: options.cachedDetectedLanguage };
    }
    if (!options.allowTextLanguageDetection) {
      return { detectedLanguage: "auto" };
    }
    const text = buildDetectText(options.title, options.description);
    if (!text || text.length < MIN_DETECT_TEXT_LENGTH) {
      return { detectedLanguage: "auto" };
    }
    const detectedLanguage = await options.detectLanguage(text);
    if (!detectedLanguage) {
      return { detectedLanguage: "auto" };
    }
    return {
      detectedLanguage,
      cacheLanguage: detectedLanguage
    };
  }
  function getAriaValueNowPercent(selector) {
    const el = document.querySelector(selector);
    const rawNow = el?.getAttribute("aria-valuenow");
    const rawMax = el?.getAttribute("aria-valuemax");
    const now2 = rawNow == null ? Number.NaN : Number.parseFloat(rawNow);
    const max = rawMax == null ? Number.NaN : Number.parseFloat(rawMax);
    if (!Number.isFinite(now2)) return null;
    if (Number.isFinite(max) && max > 0) {
      return clampPercentInt(now2 / max * 100);
    }
    return clampPercentInt(now2);
  }
  class VOTVideoManager {
    videoHandler;
    constructor(videoHandler) {
      this.videoHandler = videoHandler;
    }
    setDetectedLanguageCache(videoId, language) {
      getSharedLanguageState(videoId).detectedLanguage = language;
    }
    rememberUserLanguageSelection(videoId, language) {
      const normalizedLanguage = normalizeToRequestLang(language);
      if (!isResolvedLanguage(normalizedLanguage)) {
        const sharedLanguageState2 = sharedLanguageStateByVideoId.get(videoId);
        if (sharedLanguageState2) {
          delete sharedLanguageState2.userLanguageOverride;
        }
        return;
      }
      const sharedLanguageState = getSharedLanguageState(videoId);
      sharedLanguageState.userLanguageOverride = normalizedLanguage;
    }
    rememberDetectedLanguage(videoId, language) {
      const normalizedLanguage = normalizeToRequestLang(language);
      if (!isResolvedLanguage(normalizedLanguage)) {
        return;
      }
      this.setDetectedLanguageCache(videoId, normalizedLanguage);
      if (this.videoHandler.videoData?.videoId === videoId) {
        this.videoHandler.videoData.detectedLanguage = normalizedLanguage;
      }
    }
    async detectLanguageSingleFlight(videoId, text) {
      const sharedLanguageState = getSharedLanguageState(videoId);
      const inFlightDetect = sharedLanguageState.detectInFlight;
      if (inFlightDetect !== void 0) {
        return inFlightDetect;
      }
      const task = (async () => {
        debug.log(`Detecting language text: ${text}`);
        const language = normalizeToRequestLang(await detect(text));
        return isResolvedLanguage(language) ? language : void 0;
      })();
      sharedLanguageState.detectInFlight = task;
      try {
        return await task;
      } finally {
        if (sharedLanguageState.detectInFlight === task) {
          delete sharedLanguageState.detectInFlight;
        }
      }
    }
    async ensureDetectedLanguageForTranslation(videoData2) {
      if (!videoData2?.videoId || videoData2.detectedLanguage !== "auto") {
        return;
      }
      const sharedLanguageState = getSharedLanguageState(videoData2.videoId);
      const { detectedLanguage, cacheLanguage } = await resolveDetectedLanguageForVideo({
        isStream: videoData2.isStream,
        host: this.videoHandler.site.host,
        possibleLanguage: videoData2.detectedLanguage,
        subtitles: videoData2.subtitles,
        userOverrideLanguage: sharedLanguageState.userLanguageOverride,
        cachedDetectedLanguage: sharedLanguageState.detectedLanguage,
        title: videoData2.title,
        description: videoData2.description,
        allowTextLanguageDetection: true,
        detectLanguage: async (text) => await this.detectLanguageSingleFlight(videoData2.videoId, text)
      });
      if (cacheLanguage) {
        this.setDetectedLanguageCache(videoData2.videoId, cacheLanguage);
      }
      if (detectedLanguage === "auto") {
        return;
      }
      videoData2.detectedLanguage = detectedLanguage;
    }
    shouldUseRuntimeYouTubeHelper() {
      return this.videoHandler.site.host === "youtube" && isMobileYouTubeAdditionalData(this.videoHandler.site.additionalData);
    }
    buildRuntimeYouTubeVideoData() {
      const videoId = YoutubeHelper2.getCurrentVideoId();
      if (!videoId) {
        throw new Error("Failed to resolve mobile YouTube video id");
      }
      const response = YoutubeHelper2.getPlayerResponse();
      const playerData = YoutubeHelper2.getPlayerData();
      const subtitles = YoutubeHelper2.getSubtitles(localizationProvider.lang);
      let detectedLanguage = YoutubeHelper2.getLanguage();
      if (detectedLanguage && !availableLangs.includes(detectedLanguage)) {
        detectedLanguage = void 0;
      }
      const title = response?.videoDetails?.title || playerData?.title || document.title;
      const canonicalUrl = YoutubeHelper2.getCanonicalVideoUrl(videoId) || `https://www.youtube.com/watch?v=${encodeURIComponent(videoId)}`;
      return {
        url: canonicalUrl,
        videoId,
        host: "youtube",
        title,
        localizedTitle: playerData?.title || title,
        description: response?.videoDetails?.shortDescription,
        detectedLanguage: detectedLanguage ?? "auto",
        subtitles,
        duration: YoutubeHelper2.getVideoDuration(this.videoHandler.video) ?? this.videoHandler.video?.duration ?? votConfig.defaultDuration,
        translationHelp: null,
        isStream: Boolean(
          response?.videoDetails?.isLive || response?.videoDetails?.isLiveContent
        )
      };
    }
    async getVideoData() {
      const pageUrl = String(globalThis.location.href || "").trim();
      const hostname = String(globalThis.location.hostname || "").trim();
      const sniffedManifestUrl = getLastManifestUrl();
      const mediaUrl = String(
        this.videoHandler.video.currentSrc || this.videoHandler.video.src || ""
      ).trim();
      const nativeTrackSubtitles = getElementTrackSubtitles(
        this.videoHandler.video,
        getTrackSubtitleSource(this.videoHandler.site.host)
      );
      let rawVideoDataError;
      let rawVideoData;
      try {
        rawVideoData = this.shouldUseRuntimeYouTubeHelper() ? this.buildRuntimeYouTubeVideoData() : await getVideoData(this.videoHandler.site, {
          fetchFn: GM_fetch,
          video: this.videoHandler.video,
          language: localizationProvider.lang
        });
      } catch (error2) {
        rawVideoDataError = error2;
        console.warn(
          "[VOT][fallback:getVideoData] site getVideoData() failed, using DOM fallback",
          {
            siteHost: this.videoHandler.site.host,
            hostname,
            error: error2
          }
        );
        rawVideoData = {
          duration: this.videoHandler.video?.duration || votConfig.defaultDuration,
          url: "",
          videoId: "",
          host: this.videoHandler.site.host,
          title: document.title,
          translationHelp: null,
          localizedTitle: document.title,
          description: void 0,
          detectedLanguage: "auto",
          subtitles: nativeTrackSubtitles,
          isStream: false
        };
      }
      let {
        duration,
        url,
        videoId,
        host,
        title,
        translationHelp = null,
        localizedTitle,
        description,
        detectedLanguage: possibleLanguage,
        subtitles,
        isStream = false
      } = rawVideoData;
      subtitles = mergeSubtitleDescriptors(subtitles, nativeTrackSubtitles);
      if (this.videoHandler.site.host === "vk") {
        subtitles = mergeSubtitleDescriptors(
          subtitles,
          getSniffedSiteSubtitles("vk", videoId)
        );
      }
      const canonicalVkTarget = this.videoHandler.site.host === "vk" ? buildCanonicalVkFallbackTarget(pageUrl, videoId, url) : null;
      const canonicalRutubeTarget = this.videoHandler.site.host === "rutube" ? buildCanonicalRutubeFallbackTarget(pageUrl, videoId, url) : null;
      if (canonicalVkTarget) {
        url = canonicalVkTarget.url;
        videoId = canonicalVkTarget.videoId;
        host = "vk";
      }
      if (canonicalRutubeTarget && (host === "custom" || /(?:^|\.)bl\.rutube\.ru$/i.test(
        String(new URL(url || pageUrl, pageUrl).hostname || "")
      ) || /\.m3u8([?#]|$)/i.test(String(url || "")))) {
        url = canonicalRutubeTarget.url;
        videoId = canonicalRutubeTarget.videoId;
        host = "rutube";
      }
      const resolvedFallback = await resolveCustomSiteVideo(hostname, pageUrl);
      const youtubeFallbackVideoId = this.videoHandler.site.host === "youtube" ? YoutubeHelper2.getCurrentVideoId() : void 0;
      const youtubeFallbackUrl = this.videoHandler.site.host === "youtube" && youtubeFallbackVideoId ? YoutubeHelper2.getCanonicalVideoUrl(youtubeFallbackVideoId) : void 0;
      const shouldUseDomFallback = this.videoHandler.site.host === "custom" || Boolean(rawVideoDataError) || isUsefulResolvedFallback(url, videoId, resolvedFallback);
      if (shouldUseDomFallback) {
        const shouldPreserveVkSiteRoute = this.videoHandler.site.host === "vk" && isVkSupportedPageHost(hostname);
        const shouldPreserveRutubeSiteRoute = this.videoHandler.site.host === "rutube" && isRutubeSupportedPageHost(hostname);
        const shouldPreserveBilibiliSiteRoute = this.videoHandler.site.host === "bilibili" && isBilibiliSupportedPageHost(hostname);
        this.videoHandler.site.host === "douyin" && /(^|\.)douyin\.com$/i.test(hostname);
        const fallbackUrl = pickPreferredVideoUrl(
          resolvedFallback?.url,
          sniffedManifestUrl,
          mediaUrl,
          url,
          youtubeFallbackUrl,
          pageUrl
        );
        const fallbackCanonicalVkTarget = shouldPreserveVkSiteRoute ? buildCanonicalVkFallbackTarget(
          pageUrl,
          videoId,
          url,
          fallbackUrl,
          resolvedFallback?.videoId,
          resolvedFallback?.url
        ) : null;
        const fallbackCanonicalRutubeTarget = shouldPreserveRutubeSiteRoute ? buildCanonicalRutubeFallbackTarget(
          pageUrl,
          videoId,
          url,
          fallbackUrl,
          resolvedFallback?.videoId,
          resolvedFallback?.url
        ) : null;
        if (fallbackCanonicalVkTarget && !isBadGenericMediaUrl(fallbackCanonicalVkTarget.url)) {
          url = fallbackCanonicalVkTarget.url;
        } else if (fallbackCanonicalRutubeTarget && !isBadGenericMediaUrl(fallbackCanonicalRutubeTarget.url)) {
          url = fallbackCanonicalRutubeTarget.url;
        } else if (fallbackUrl && !shouldPreserveBilibiliSiteRoute) {
          url = fallbackUrl;
        }
        if (isBadGenericVideoId(videoId) || isPageScopedVideoId(videoId, pageUrl)) {
          const fallbackVideoIdCandidate = !isBadGenericVideoId(fallbackUrl) && !isPageScopedVideoId(fallbackUrl, pageUrl) ? fallbackUrl : "";
          const resolvedVideoIdCandidate = !isBadGenericVideoId(youtubeFallbackVideoId) && !isPageScopedVideoId(String(youtubeFallbackVideoId || ""), pageUrl) ? String(youtubeFallbackVideoId).trim() : "";
          const resolvedFallbackVideoIdCandidate = !isBadGenericVideoId(resolvedFallback?.videoId) && !isPageScopedVideoId(String(resolvedFallback?.videoId || ""), pageUrl) ? String(resolvedFallback?.videoId).trim() : "";
          videoId = fallbackVideoIdCandidate || resolvedVideoIdCandidate || resolvedFallbackVideoIdCandidate || pageUrl;
        }
        if (shouldPreserveVkSiteRoute && fallbackCanonicalVkTarget) {
          url = fallbackCanonicalVkTarget.url;
          host = "vk";
          videoId = fallbackCanonicalVkTarget.videoId;
        } else if (shouldPreserveRutubeSiteRoute && fallbackCanonicalRutubeTarget) {
          url = fallbackCanonicalRutubeTarget.url;
          host = "rutube";
          videoId = fallbackCanonicalRutubeTarget.videoId;
        } else if (shouldPreserveBilibiliSiteRoute) {
          host = "bilibili";
          videoId = !isBadGenericVideoId(videoId) ? videoId : pageUrl;
        } else {
          host = this.videoHandler.site.host === "youtube" && youtubeFallbackVideoId ? "youtube" : "custom";
        }
        const fallbackTitle = resolvedFallback?.title || normalizeLocalTitleFromUrl(url) || (typeof document.title === "string" ? document.title.trim() : void 0);
        if (fallbackTitle) {
          title = fallbackTitle;
          if (!localizedTitle) {
            localizedTitle = fallbackTitle;
          }
        }
        console.log("[VOT][fallback:getVideoData] using DOM/media fallback", {
          siteHost: this.videoHandler.site.host,
          sniffedManifestUrl,
          mediaUrl,
          pageUrl,
          resolvedFallback,
          finalUrl: url,
          finalVideoId: videoId,
          finalHost: host,
          preservedSiteRoute: shouldPreserveVkSiteRoute || shouldPreserveRutubeSiteRoute || shouldPreserveBilibiliSiteRoute
        });
      }
      if (this.videoHandler.site.host === "googledrive") {
        subtitles = mergeSubtitleDescriptors(
          subtitles,
          getElementTrackSubtitles(this.videoHandler.video, "googledrive")
        );
        const pageTitle = normalizeGoogleDriveTitle(getGoogleDrivePageTitle());
        const storedTitle = await getStoredGoogleDriveTitle(videoId);
        const safeTitle = normalizeGoogleDriveTitle(title);
        const safeLocalizedTitle = normalizeGoogleDriveTitle(localizedTitle);
        const resolvedGoogleDriveTitle = !isBadGoogleDriveTitle(safeTitle) ? safeTitle : !isBadGoogleDriveTitle(storedTitle) ? storedTitle : !isBadGoogleDriveTitle(pageTitle) ? pageTitle : !isBadGoogleDriveTitle(safeLocalizedTitle) ? safeLocalizedTitle : void 0;
        if (resolvedGoogleDriveTitle) {
          title = resolvedGoogleDriveTitle;
          if (isBadGoogleDriveTitle(localizedTitle)) {
            localizedTitle = resolvedGoogleDriveTitle;
          }
        }
      }
      const sharedLanguageState = getSharedLanguageState(videoId);
      const { detectedLanguage, cacheLanguage } = await resolveDetectedLanguageForVideo({
        isStream,
        host: this.videoHandler.site.host,
        possibleLanguage,
        subtitles,
        userOverrideLanguage: sharedLanguageState.userLanguageOverride,
        cachedDetectedLanguage: sharedLanguageState.detectedLanguage,
        title,
        description,
        allowTextLanguageDetection: false,
        detectLanguage: async (text) => await this.detectLanguageSingleFlight(videoId, text)
      });
      if (cacheLanguage) {
        this.setDetectedLanguageCache(videoId, cacheLanguage);
      }
      if (host === "custom") {
        const localTitle = normalizeLocalTitleFromUrl(url) || (typeof document.title === "string" ? document.title.trim() : void 0);
        if (localTitle) {
          title = localTitle;
          if (!localizedTitle) {
            localizedTitle = localTitle;
          }
        }
      }
      const videoData2 = {
        translationHelp,
        isStream,
        duration: duration || this.videoHandler.video?.duration || votConfig.defaultDuration,
        videoId,
        url,
        host,
        detectedLanguage,
        responseLanguage: this.videoHandler.translateToLang,
        subtitles,
        title,
        localizedTitle,
        description,
        downloadTitle: this.videoHandler.site.host === "googledrive" ? normalizeGoogleDriveTitle(title) ?? normalizeGoogleDriveTitle(localizedTitle) ?? normalizeGoogleDriveTitle(getGoogleDrivePageTitle()) ?? videoId : host === "custom" ? normalizeLocalTitleFromUrl(url) ?? title ?? localizedTitle ?? videoId : localizedTitle ?? title ?? videoId
      };
      if (sharedLanguageState.lastLoggedDetectedLanguage !== detectedLanguage) {
        console.log("[VOT] Detected language:", detectedLanguage);
        sharedLanguageState.lastLoggedDetectedLanguage = detectedLanguage;
      }
      if (Array.isArray(videoData2.subtitles) && videoData2.subtitles.length > 0) {
        console.log(
          `[VOT][subtitles] video data contains ${videoData2.subtitles.length} subtitle track(s).`
        );
        console.table(
          videoData2.subtitles.map((subtitle, index) => ({
            index,
            language: subtitle.language,
            translatedFromLanguage: subtitle.translatedFromLanguage ?? "",
            source: subtitle.source,
            isAutoGenerated: Boolean(subtitle.isAutoGenerated),
            url: subtitle.url
          }))
        );
      }
      return videoData2;
    }
    async videoValidator() {
      const videoData2 = this.videoHandler.videoData;
      const data = this.videoHandler.data;
      if (!videoData2 || !data) {
        throw new VOTLocalizedError("VOTNoVideoIDFound");
      }
      debug.log("VideoValidator videoData: ", this.videoHandler.videoData);
      const sourceLanguage = this.videoHandler.translateFromLang === "auto" ? this.videoHandler.videoData.detectedLanguage : this.videoHandler.translateFromLang;
      if (this.videoHandler.data.enabledDontTranslateLanguages && this.videoHandler.data.dontTranslateLanguages?.includes(sourceLanguage)) {
        throw new VOTLocalizedError("VOTDisableFromYourLang");
      }
      if (this.videoHandler.videoData.isStream) {
        throw new VOTLocalizedError("VOTStreamNotAvailable");
      }
      if (this.videoHandler.videoData.duration > 14400) {
        throw new VOTLocalizedError("VOTVideoIsTooLong");
      }
      const sourceAudioState = getSourceAudioAvailability(
        this.videoHandler.video
      );
      if (!sourceAudioState.ready) {
        throw new VOTLocalizedError(sourceAudioState.localizationKey);
      }
      return true;
    }
getVideoVolume() {
      const video = this.videoHandler.video;
      if (!video) return void 0;
      if (isExternalVolumeHost(this.videoHandler.site.host)) {
        const ariaPercent = getAriaValueNowPercent(YT_VOLUME_NOW_SELECTOR);
        if (ariaPercent != null) {
          return percentToVolume01(ariaPercent);
        }
        const extVolume = YoutubeHelper2.getVolume();
        if (typeof extVolume === "number" && Number.isFinite(extVolume)) {
          return snapVolume01(extVolume);
        }
      }
      return snapVolume01(video.volume);
    }
setVideoVolume(volume) {
      const snapped = snapVolume01(volume);
      if (!isExternalVolumeHost(this.videoHandler.site.host)) {
        this.videoHandler.video.volume = snapped;
        return this;
      }
      try {
        const result = YoutubeHelper2.setVolume(snapped);
        const ok = typeof result === "boolean" && result || typeof result === "number" && Number.isFinite(result);
        if (ok) return this;
      } catch {
      }
      this.videoHandler.video.volume = snapped;
      return this;
    }
isMuted() {
      return isExternalVolumeHost(this.videoHandler.site.host) ? YoutubeHelper2.isMuted() : this.videoHandler.video?.muted;
    }
syncVideoVolumeSlider() {
      const overlayView = this.videoHandler.uiManager.votOverlayView;
      if (!overlayView?.isInitialized()) return this;
      const ariaPercent = isExternalVolumeHost(this.videoHandler.site.host) ? getAriaValueNowPercent(YT_VOLUME_NOW_SELECTOR) : null;
      const volumePercent = this.isMuted() ? 0 : ariaPercent ?? volume01ToPercent(this.getVideoVolume() ?? 0);
      overlayView.videoVolumeSlider.value = volumePercent;
      this.videoHandler.onVideoVolumeSliderSynced?.(volumePercent);
      return this;
    }
    setSelectMenuValues(from, to) {
      const videoData2 = this.videoHandler.videoData;
      if (!videoData2) {
        return this;
      }
      const normalizedFrom = normalizeToRequestLang(from) ?? "auto";
      const langPairLogKey = `${normalizedFrom}->${to}`;
      const sharedLanguageState = getSharedLanguageState(videoData2.videoId);
      if (sharedLanguageState.lastLoggedLangPair !== langPairLogKey) {
        console.log(`[VOT] Set translation from ${normalizedFrom} to ${to}`);
        sharedLanguageState.lastLoggedLangPair = langPairLogKey;
      }
      videoData2.responseLanguage = to;
      if (this.videoHandler.translateFromLang === "auto") {
        this.videoHandler.translateFromLang = "auto";
      }
      this.videoHandler.translateToLang = to;
      const overlayView = this.videoHandler.uiManager.votOverlayView;
      if (!overlayView?.isInitialized()) {
        return this;
      }
      overlayView.languagePairSelect.fromSelect.selectTitle = localizationProvider.getLangLabel(normalizedFrom);
      overlayView.languagePairSelect.toSelect.selectTitle = localizationProvider.getLangLabel(to);
      overlayView.languagePairSelect.fromSelect.setSelectedValue(normalizedFrom);
      overlayView.languagePairSelect.toSelect.setSelectedValue(to);
      return this;
    }
  }
  class FullscreenLayerController {
    video;
    container;
    fullscreenLayer = null;
    constructor({ video, container }) {
      this.video = video;
      this.container = container;
    }
    updateContainer(container) {
      this.container = container;
    }
    getWidgetParentElement() {
      return this.shouldUseFullscreenViewportLayer() ? this.ensureFullscreenLayer() : this.container;
    }
    getLayoutRootElement() {
      return this.fullscreenLayer?.isConnected ? this.fullscreenLayer : this.container;
    }
    syncWidgetContainer(widgetContainer) {
      const widgetParent = this.getWidgetParentElement();
      if (widgetParent === this.container && getComputedStyle(this.container).position === "static") {
        this.container.style.position = "relative";
      }
      if (widgetContainer && widgetContainer.parentElement !== widgetParent) {
        widgetParent.appendChild(widgetContainer);
      }
      if (widgetParent === this.container && this.fullscreenLayer?.parentElement) {
        this.fullscreenLayer.remove();
        this.fullscreenLayer = null;
      }
    }
    release() {
      if (!this.fullscreenLayer) return;
      this.fullscreenLayer.remove();
      this.fullscreenLayer = null;
    }
    getActiveFullscreenElement() {
      const doc = document;
      const fullscreenEl = doc.fullscreenElement ?? doc.webkitFullscreenElement;
      return resolveScopedFullscreenElement(
        fullscreenEl,
        [this.container, this.video],
        {
          allowDocumentViewport: true
        }
      );
    }
    isCurrentVideoInFullscreenSession() {
      const fullscreenEl = this.getActiveFullscreenElement();
      if (!fullscreenEl) return false;
      if (fullscreenEl === this.container || fullscreenEl.contains(this.container) || this.container.contains(fullscreenEl)) {
        return true;
      }
      return Boolean(
        this.video && (fullscreenEl === this.video || fullscreenEl.contains(this.video) || this.video.contains(fullscreenEl))
      );
    }
    shouldUseFullscreenViewportLayer() {
      return this.isCurrentVideoInFullscreenSession();
    }
    ensureFullscreenLayer() {
      if (!this.fullscreenLayer) {
        const layer = document.createElement("vot-block");
        layer.classList.add("vot-subtitles-layer");
        Object.assign(layer.style, {
          position: "absolute",
          inset: "0",
          zIndex: "2147483647",
          pointerEvents: "none"
        });
        this.fullscreenLayer = layer;
      }
      const fullscreenEl = this.getActiveFullscreenElement();
      const parent = fullscreenEl instanceof HTMLElement ? fullscreenEl : this.container;
      if (getComputedStyle(parent).position === "static") {
        parent.style.position = "relative";
      }
      if (this.fullscreenLayer.parentElement !== parent) {
        parent.appendChild(this.fullscreenLayer);
      }
      return this.fullscreenLayer;
    }
  }
  function isTimeInLine(time, line) {
    return time >= line.startMs && time < line.startMs + line.durationMs;
  }
  function findActiveSubtitleLineIndex(time, subtitlesList) {
    let low = 0;
    let high = subtitlesList.length - 1;
    while (low <= high) {
      const mid = low + high >> 1;
      const line = subtitlesList[mid];
      if (time < line.startMs) {
        high = mid - 1;
      } else if (time >= line.startMs + line.durationMs) {
        low = mid + 1;
      } else {
        return mid;
      }
    }
    return -1;
  }
  function clampToRange(value, min, max) {
    return Math.max(min, Math.min(value, max));
  }
  function hasDragThresholdBeenExceeded(startClientX, startClientY, nextClientX, nextClientY, thresholdPx) {
    const dx = nextClientX - startClientX;
    const dy = nextClientY - startClientY;
    return dx * dx + dy * dy >= thresholdPx * thresholdPx;
  }
  function clampAnchorWithinBox({
    anchorX,
    anchorY,
    elementWidth,
    elementHeight,
    boxWidth,
    boxHeight,
    bottomInset
  }) {
    let nextAnchorX = anchorX;
    let nextAnchorY = anchorY;
    const maxAnchorY = Math.max(0, boxHeight - bottomInset);
    const minAnchorY = elementHeight || 0;
    if (elementWidth) {
      let leftPx = nextAnchorX - elementWidth / 2;
      const maxLeftPx = boxWidth - elementWidth;
      if (maxLeftPx >= 0) {
        leftPx = clampToRange(leftPx, 0, maxLeftPx);
      } else {
        leftPx = maxLeftPx / 2;
      }
      nextAnchorX = leftPx + elementWidth / 2;
    }
    nextAnchorY = clampToRange(nextAnchorY, minAnchorY, maxAnchorY);
    return { anchorX: nextAnchorX, anchorY: nextAnchorY };
  }
  function snapValueToNearestCandidate({
    current,
    candidates,
    thresholdPx
  }) {
    let closestValue = current;
    let closestDistance = Number.POSITIVE_INFINITY;
    for (const candidate of candidates) {
      const distance = Math.abs(candidate - current);
      if (distance < closestDistance) {
        closestDistance = distance;
        closestValue = candidate;
      }
    }
    if (!Number.isFinite(closestDistance) || closestDistance > thresholdPx) {
      return { snapped: false, value: current };
    }
    return { snapped: true, value: closestValue };
  }
  const stylesEqual$1 = (left, right) => Boolean(left?.italic) === Boolean(right?.italic) && Boolean(left?.bold) === Boolean(right?.bold) && Boolean(left?.underline) === Boolean(right?.underline);
  function buildSubtitleRenderPlan(tokens, renderEndTokenIndex, breakAfterTokenIndexSet) {
    const plan = [];
    let pendingPrefix = "";
    let pendingPrefixStyle;
    for (let i2 = 0; i2 <= renderEndTokenIndex; ) {
      const token = tokens[i2];
      const tokenText = token?.text ?? "";
      if (!tokenText) {
        i2 += 1;
        continue;
      }
      if (tokenText === "\n") {
        if (pendingPrefix) {
          plan.push({
            kind: "text",
            text: pendingPrefix,
            style: pendingPrefixStyle
          });
          pendingPrefix = "";
          pendingPrefixStyle = void 0;
        }
        plan.push({ kind: "break" });
        i2 += 1;
        continue;
      }
      if (token.isWordLike) {
        let text = pendingPrefix + tokenText;
        const style = pendingPrefix ? pendingPrefixStyle : token.style;
        pendingPrefix = "";
        pendingPrefixStyle = void 0;
        let endIndex = i2;
        const hasBreakAfterWord = Boolean(breakAfterTokenIndexSet?.has(i2));
        let breakTokenIndex = hasBreakAfterWord ? i2 : null;
        while (breakTokenIndex === null && endIndex + 1 <= renderEndTokenIndex) {
          const next = tokens[endIndex + 1];
          if (!next || next.isWordLike || next.text === "\n" || !stylesEqual$1(next.style, style))
            break;
          text += next.text;
          endIndex += 1;
          if (breakAfterTokenIndexSet?.has(endIndex)) {
            breakTokenIndex = endIndex;
            break;
          }
        }
        if (breakTokenIndex !== null) {
          plan.push(
            {
              kind: "word",
              text,
              style
            },
            { kind: "break" }
          );
          i2 = breakTokenIndex + 1;
          while (i2 <= renderEndTokenIndex && !tokens[i2]?.isWordLike && !tokens[i2]?.text.trim()) {
            i2 += 1;
          }
          continue;
        }
        plan.push({
          kind: "word",
          text,
          style
        });
        i2 = endIndex + 1;
        continue;
      }
      const hasBreakAfter = Boolean(breakAfterTokenIndexSet?.has(i2));
      const isWhitespaceOnly = tokenText.trim().length === 0;
      if (!isWhitespaceOnly) {
        if (hasBreakAfter) {
          plan.push(
            {
              kind: "text",
              text: pendingPrefix + tokenText,
              style: pendingPrefix ? pendingPrefixStyle : token.style
            },
            { kind: "break" }
          );
          pendingPrefix = "";
          pendingPrefixStyle = void 0;
        } else {
          if (pendingPrefix && !stylesEqual$1(pendingPrefixStyle, token.style)) {
            plan.push({
              kind: "text",
              text: pendingPrefix,
              style: pendingPrefixStyle
            });
            pendingPrefix = "";
          }
          pendingPrefix += tokenText;
          pendingPrefixStyle = token.style;
        }
        i2 += 1;
        continue;
      }
      if (pendingPrefix) {
        plan.push({
          kind: "text",
          text: pendingPrefix,
          style: pendingPrefixStyle
        });
        pendingPrefix = "";
        pendingPrefixStyle = void 0;
      }
      if (hasBreakAfter) {
        plan.push(
          {
            kind: "text",
            text: tokenText,
            style: token.style
          },
          { kind: "break" }
        );
      } else {
        plan.push({
          kind: "text",
          text: tokenText,
          style: token.style
        });
      }
      i2 += 1;
    }
    if (pendingPrefix) {
      plan.push({
        kind: "text",
        text: pendingPrefix,
        style: pendingPrefixStyle
      });
    }
    return plan;
  }
  const EST_CHAR_WIDTH_RATIO = 0.55;
  function clamp$1(value, min, max) {
    if (Number.isNaN(value)) return min;
    return Math.min(max, Math.max(min, value));
  }
  function targetCharsPerLine(aspect) {
    if (aspect < 1) return 28;
    if (aspect < 1.4) return 32;
    return 42;
  }
  function computeSmartLayoutForBox(anchorBox, cssMetrics) {
    const w2 = Math.max(1, anchorBox.w);
    const h2 = Math.max(1, anchorBox.h);
    const aspect = w2 / h2;
    let computedCPL = targetCharsPerLine(aspect);
    if (cssMetrics) {
      const { fontSizePx, maxWidthPx } = cssMetrics;
      if (Number.isFinite(fontSizePx) && Number.isFinite(maxWidthPx) && fontSizePx > 0 && maxWidthPx > 0) {
        const estCharW = fontSizePx * EST_CHAR_WIDTH_RATIO;
        if (estCharW > 0) {
          computedCPL = maxWidthPx / estCharW;
        }
      }
    }
    const maxLength = clamp$1(Math.round(computedCPL * 2), 50, 180);
    return { maxLength };
  }
  const isWordToken = (token) => Boolean(token?.isWordLike && token.text?.trim());
  const clamp = (value, min, max) => Math.min(max, Math.max(min, value));
  const rangeSum = (prefix, start, end) => {
    if (end < start) return 0;
    return prefix[end + 1] - prefix[start];
  };
  const sentenceEndingWordRegexp = /[.!?\u2026]+(?:["'`)\]}\u00BB\u201D\u2019]+)?$/u;
  function buildWordSlices(tokens) {
    const slices = [];
    let key = "";
    for (let tokenIndex = 0; tokenIndex < tokens.length; tokenIndex += 1) {
      const token = tokens[tokenIndex];
      if (!isWordToken(token)) continue;
      let textToNextWord = token.text;
      let breakAfterTokenIndex = tokenIndex;
      let breakTextLength = token.text.length;
      let cursor = tokenIndex + 1;
      let seenTrailingPunctuation = false;
      let punctuationAttachmentClosed = false;
      while (cursor < tokens.length) {
        const nextToken = tokens[cursor];
        if (!nextToken) {
          cursor += 1;
          continue;
        }
        if (isWordToken(nextToken)) break;
        if (nextToken.text === "\n") {
          breakAfterTokenIndex = cursor;
          break;
        }
        const tokenText = nextToken.text ?? "";
        textToNextWord += tokenText;
        if (!tokenText.trim()) {
          if (seenTrailingPunctuation) {
            punctuationAttachmentClosed = true;
          }
          cursor += 1;
          continue;
        }
        if (!punctuationAttachmentClosed) {
          breakAfterTokenIndex = cursor;
          seenTrailingPunctuation = true;
          breakTextLength = textToNextWord.length;
        }
        cursor += 1;
      }
      const trailingGapAfterBreakText = textToNextWord.slice(breakTextLength);
      slices.push({
        tokenIndex,
        breakAfterTokenIndex,
        textToNextWord,
        trailingGapAfterBreakText
      });
      if (key) key += "";
      key += `${textToNextWord}${trailingGapAfterBreakText}${breakAfterTokenIndex}`;
    }
    return {
      slices,
      key
    };
  }
  function measureWordSlices(slices, measure) {
    const wordsCount = slices.length;
    const widths = new Array(wordsCount);
    const chars = new Array(wordsCount);
    const trailingGapWidths = new Array(wordsCount);
    const trailingGapChars = new Array(wordsCount);
    const prefixWidths = new Array(wordsCount + 1);
    const prefixChars = new Array(wordsCount + 1);
    prefixWidths[0] = 0;
    prefixChars[0] = 0;
    for (let i2 = 0; i2 < wordsCount; i2 += 1) {
      const textToNextWord = slices[i2].textToNextWord;
      const trailingGapAfterBreakText = slices[i2].trailingGapAfterBreakText;
      const width = measure(textToNextWord);
      const charCount = textToNextWord.length;
      const trailingCharCount = trailingGapAfterBreakText.length;
      const trailingWidth = trailingCharCount ? measure(trailingGapAfterBreakText) : 0;
      widths[i2] = width;
      chars[i2] = charCount;
      trailingGapWidths[i2] = trailingWidth;
      trailingGapChars[i2] = trailingCharCount;
      prefixWidths[i2 + 1] = prefixWidths[i2] + width;
      prefixChars[i2 + 1] = prefixChars[i2] + charCount;
    }
    return {
      widths,
      chars,
      trailingGapWidths,
      trailingGapChars,
      prefixWidths,
      prefixChars
    };
  }
  const getWordRangeWidthUnsafe = (metrics, startWord, endWord) => {
    const total = rangeSum(metrics.prefixWidths, startWord, endWord);
    return total - (metrics.trailingGapWidths[endWord] ?? 0);
  };
  function getWordRangeWidth(metrics, startWord, endWord) {
    if (endWord < startWord) return 0;
    if (!metrics.widths.length) return 0;
    const start = clamp(startWord, 0, metrics.widths.length - 1);
    const end = clamp(endWord, 0, metrics.widths.length - 1);
    if (end < start) return 0;
    return getWordRangeWidthUnsafe(metrics, start, end);
  }
  const getWordRangeCharsUnsafe = (metrics, startWord, endWord) => {
    const total = rangeSum(metrics.prefixChars, startWord, endWord);
    return total - (metrics.trailingGapChars[endWord] ?? 0);
  };
  function fitsInTwoLines(metrics, startWord, endWord, maxWidth) {
    if (endWord < startWord) return true;
    if (maxWidth <= 0) return false;
    if (!metrics.widths.length) return true;
    const start = clamp(startWord, 0, metrics.widths.length - 1);
    const end = clamp(endWord, 0, metrics.widths.length - 1);
    if (end < start) return true;
    const prefixWidths = metrics.prefixWidths;
    const trailingGapWidths = metrics.trailingGapWidths;
    const startPrefix = prefixWidths[start];
    const endPrefix = prefixWidths[end + 1];
    const endTrailingGapWidth = trailingGapWidths[end] ?? 0;
    if (endPrefix - startPrefix - endTrailingGapWidth <= maxWidth) {
      return true;
    }
    for (let k2 = start; k2 < end; k2 += 1) {
      const splitPrefix = prefixWidths[k2 + 1];
      const top = splitPrefix - startPrefix - (trailingGapWidths[k2] ?? 0);
      const bottom = endPrefix - splitPrefix - endTrailingGapWidth;
      if (top <= maxWidth && bottom <= maxWidth) {
        return true;
      }
    }
    return false;
  }
  const scoreTwoLineCandidate = (w1, w2, maxWidth, count1, count2, wordsInRange) => {
    const mean = (w1 + w2) / 2;
    const slack1 = maxWidth - w1;
    const slack2 = maxWidth - w2;
    const slackCost = slack1 * slack1 + slack2 * slack2;
    const delta1 = w1 - mean;
    const delta2 = w2 - mean;
    const variance = delta1 * delta1 + delta2 * delta2;
    const canAvoidSingleton = wordsInRange >= 4;
    const singletonPenalty = canAvoidSingleton && (count1 <= 1 || count2 <= 1) ? 1e9 : 0;
    const canAvoidTwoWordOrphans = wordsInRange >= 6;
    const twoWordOrphanPenalty = canAvoidTwoWordOrphans && (count1 <= 2 || count2 <= 2) ? 2e7 : 0;
    let cost = slackCost + variance + singletonPenalty + twoWordOrphanPenalty;
    if (w1 > w2) {
      cost += (w1 - w2) / Math.max(1, maxWidth) * 0.15;
    }
    return cost;
  };
  function computeBestTwoLineBreak(metrics, startWord, endWord, maxWidth) {
    if (maxWidth <= 0) return null;
    if (!metrics.widths.length) return null;
    const start = clamp(startWord, 0, metrics.widths.length - 1);
    const end = clamp(endWord, 0, metrics.widths.length - 1);
    if (end <= start) return null;
    const prefixWidths = metrics.prefixWidths;
    const trailingGapWidths = metrics.trailingGapWidths;
    const startPrefix = prefixWidths[start];
    const endPrefix = prefixWidths[end + 1];
    const endTrailingGapWidth = trailingGapWidths[end] ?? 0;
    let bestBreak = null;
    let bestCost = Number.POSITIVE_INFINITY;
    const wordsInRange = end - start + 1;
    for (let k2 = start; k2 < end; k2 += 1) {
      const splitPrefix = prefixWidths[k2 + 1];
      const w1 = splitPrefix - startPrefix - (trailingGapWidths[k2] ?? 0);
      const w2 = endPrefix - splitPrefix - endTrailingGapWidth;
      if (w1 > maxWidth || w2 > maxWidth) continue;
      const count1 = k2 - start + 1;
      const count2 = end - k2;
      const cost = scoreTwoLineCandidate(
        w1,
        w2,
        maxWidth,
        count1,
        count2,
        wordsInRange
      );
      if (cost < bestCost) {
        bestCost = cost;
        bestBreak = k2;
      }
    }
    return bestBreak;
  }
  function computeBalancedBreaks(metrics, maxWidth) {
    const n2 = metrics.widths.length;
    if (n2 <= 1 || maxWidth <= 0) return [];
    if (getWordRangeWidthUnsafe(metrics, 0, n2 - 1) <= maxWidth) {
      return [];
    }
    const breakIndex = computeBestTwoLineBreak(metrics, 0, n2 - 1, maxWidth);
    return breakIndex === null ? [] : [breakIndex];
  }
  function findLongestPrefixFittingTwoLines(metrics, maxWidth) {
    const n2 = metrics.widths.length;
    if (n2 <= 0 || maxWidth <= 0) return null;
    let low = 0;
    let high = n2 - 1;
    let best = null;
    while (low <= high) {
      const middle = low + high >> 1;
      if (fitsInTwoLines(metrics, 0, middle, maxWidth)) {
        best = middle;
        low = middle + 1;
      } else {
        high = middle - 1;
      }
    }
    return best;
  }
  function resolveStrictTwoLineLayout(metrics, maxWidth) {
    if (maxWidth <= 0) {
      return {
        breakAfterWordIndices: [],
        truncateAfterWordIndex: null
      };
    }
    const n2 = metrics.widths.length;
    if (n2 <= 1) {
      return {
        breakAfterWordIndices: [],
        truncateAfterWordIndex: null
      };
    }
    const fullLineFits = getWordRangeWidthUnsafe(metrics, 0, n2 - 1) <= maxWidth;
    if (fullLineFits) {
      return {
        breakAfterWordIndices: [],
        truncateAfterWordIndex: null
      };
    }
    const balancedBreaks = computeBalancedBreaks(metrics, maxWidth);
    if (balancedBreaks.length) {
      return {
        breakAfterWordIndices: balancedBreaks,
        truncateAfterWordIndex: null
      };
    }
    const prefixEndWordRaw = findLongestPrefixFittingTwoLines(metrics, maxWidth);
    const prefixEndWord = prefixEndWordRaw ?? 0;
    if (prefixEndWord >= n2 - 1) {
      return {
        breakAfterWordIndices: [],
        truncateAfterWordIndex: null
      };
    }
    const prefixBreak = computeBestTwoLineBreak(
      metrics,
      0,
      prefixEndWord,
      maxWidth
    );
    return {
      breakAfterWordIndices: prefixBreak === null ? [] : [prefixBreak],
      truncateAfterWordIndex: prefixEndWord
    };
  }
  const getRangeWordCount = (range) => range.endWord - range.startWord + 1;
  const buildInitialWordRanges = (wordsCount, isRangeAllowed) => {
    const wordRanges = [];
    let startWord = 0;
    while (startWord < wordsCount) {
      let endWord = startWord;
      while (endWord + 1 < wordsCount && isRangeAllowed(startWord, endWord + 1)) {
        endWord += 1;
      }
      wordRanges.push({ startWord, endWord });
      startWord = endWord + 1;
    }
    return wordRanges;
  };
  const tryBorrowFromPrevious = (wordRanges, index, isRangeAllowed) => {
    if (index <= 0) return false;
    const current = wordRanges[index];
    const previous = wordRanges[index - 1];
    if (!current || !previous) return false;
    if (getRangeWordCount(previous) < 3) return false;
    const movedWord = previous.endWord;
    const nextPrevious = {
      startWord: previous.startWord,
      endWord: movedWord - 1
    };
    const nextCurrent = {
      startWord: movedWord,
      endWord: current.endWord
    };
    if (!isRangeAllowed(nextPrevious.startWord, nextPrevious.endWord) || !isRangeAllowed(nextCurrent.startWord, nextCurrent.endWord)) {
      return false;
    }
    previous.endWord = nextPrevious.endWord;
    current.startWord = nextCurrent.startWord;
    return true;
  };
  const tryBorrowFromNext = (wordRanges, index, isRangeAllowed) => {
    if (index >= wordRanges.length - 1) return false;
    const current = wordRanges[index];
    const next = wordRanges[index + 1];
    if (!current || !next) return false;
    if (getRangeWordCount(next) < 3) return false;
    const movedWord = next.startWord;
    const nextCurrent = {
      startWord: current.startWord,
      endWord: movedWord
    };
    const nextNext = {
      startWord: movedWord + 1,
      endWord: next.endWord
    };
    if (!isRangeAllowed(nextCurrent.startWord, nextCurrent.endWord) || !isRangeAllowed(nextNext.startWord, nextNext.endWord)) {
      return false;
    }
    current.endWord = nextCurrent.endWord;
    next.startWord = nextNext.startWord;
    return true;
  };
  const rebalanceSingletonRanges = (wordRanges, isRangeAllowed) => {
    for (let i2 = wordRanges.length - 1; i2 >= 0; i2 -= 1) {
      if (getRangeWordCount(wordRanges[i2]) !== 1) continue;
      if (!tryBorrowFromPrevious(wordRanges, i2, isRangeAllowed)) {
        tryBorrowFromNext(wordRanges, i2, isRangeAllowed);
      }
    }
  };
  const getWordPayload = (tokens, words, wordIndex) => {
    const word = words[wordIndex];
    if (!word) return "";
    let text = "";
    for (let tokenIndex = word.tokenIndex; tokenIndex <= word.breakAfterTokenIndex; tokenIndex += 1) {
      text += tokens[tokenIndex]?.text ?? "";
    }
    return text.trimEnd();
  };
  const applySentenceBoundarySplits = (wordRanges, tokens, words, isRangeAllowed) => {
    for (let i2 = 0; i2 < wordRanges.length - 1; i2 += 1) {
      const previous = wordRanges[i2];
      const next = wordRanges[i2 + 1];
      if (!previous || !next) continue;
      let sentenceBoundaryWord = -1;
      const minCandidate = Math.max(previous.startWord, previous.endWord - 2);
      for (let candidate = previous.endWord - 1; candidate >= minCandidate; candidate -= 1) {
        if (sentenceEndingWordRegexp.test(getWordPayload(tokens, words, candidate))) {
          sentenceBoundaryWord = candidate;
          break;
        }
      }
      if (sentenceBoundaryWord < previous.startWord) continue;
      const nextPreviousStartWord = previous.startWord;
      const nextPreviousEndWord = sentenceBoundaryWord;
      const nextPreviousWordCount = nextPreviousEndWord - nextPreviousStartWord + 1;
      if (nextPreviousWordCount < 3) continue;
      const nextStartWord = sentenceBoundaryWord + 1;
      const nextEndWord = next.endWord;
      if (!isRangeAllowed(nextPreviousStartWord, nextPreviousEndWord) || !isRangeAllowed(nextStartWord, nextEndWord)) {
        continue;
      }
      previous.endWord = nextPreviousEndWord;
      next.startWord = nextStartWord;
    }
  };
  const resolveSegmentStartToken = (tokens, words, startWordIndex) => {
    const startWord = words[startWordIndex];
    if (!startWord) return 0;
    const previousBreakAfterTokenIndex = startWordIndex > 0 ? words[startWordIndex - 1]?.breakAfterTokenIndex ?? startWord.tokenIndex - 1 : -1;
    let startToken = clamp(
      previousBreakAfterTokenIndex + 1,
      0,
      startWord.tokenIndex
    );
    while (startToken < startWord.tokenIndex && !tokens[startToken]?.text.trim()) {
      startToken += 1;
    }
    return startToken;
  };
  const mapWordRangesToTimedSegments = (wordRanges, words, tokens) => {
    const segments = [];
    for (const range of wordRanges) {
      const startWord = words[range.startWord];
      const endWord = words[range.endWord];
      if (!startWord || !endWord) continue;
      const startToken = resolveSegmentStartToken(tokens, words, range.startWord);
      const endToken = endWord.breakAfterTokenIndex + 1;
      const startMs = tokens[startWord.tokenIndex]?.startMs ?? tokens[startToken]?.startMs ?? 0;
      const endTokenStartMs = tokens[endWord.tokenIndex]?.startMs ?? startMs;
      const endTokenDurationMs = tokens[endWord.tokenIndex]?.durationMs ?? 0;
      const nextWord = words[range.endWord + 1];
      const nextWordStartMs = nextWord ? tokens[nextWord.tokenIndex]?.startMs : void 0;
      const endMs = nextWordStartMs ?? endTokenStartMs + endTokenDurationMs;
      segments.push({
        startToken,
        endToken,
        startMs,
        endMs
      });
    }
    return segments;
  };
  function computeTwoLineSegments(tokens, words, metrics, maxWidthPx, maxChars) {
    const wordsCount = words.length;
    if (wordsCount === 0) return [];
    const charLimit = Number.isFinite(maxChars) && maxChars > 0 ? maxChars : null;
    const isRangeAllowed = (startWord, endWord) => {
      if (endWord < startWord) return false;
      if (charLimit !== null && getWordRangeCharsUnsafe(metrics, startWord, endWord) > charLimit) {
        return false;
      }
      return fitsInTwoLines(metrics, startWord, endWord, maxWidthPx);
    };
    const wordRanges = buildInitialWordRanges(wordsCount, isRangeAllowed);
    rebalanceSingletonRanges(wordRanges, isRangeAllowed);
    applySentenceBoundarySplits(wordRanges, tokens, words, isRangeAllowed);
    return mapWordRangesToTimedSegments(wordRanges, words, tokens);
  }
  const WRAP_WIDTH_GUARD_PX = 8;
  const WRAP_WIDTH_GUARD_RATIO = 0.97;
  const MIN_EFFECTIVE_WRAP_WIDTH_PX = 24;
  function applyWrapWidthGuard(maxWidthPx) {
    if (!Number.isFinite(maxWidthPx) || maxWidthPx <= 0) return 0;
    const byPixelGuard = maxWidthPx - WRAP_WIDTH_GUARD_PX;
    const byRatioGuard = maxWidthPx * WRAP_WIDTH_GUARD_RATIO;
    const guarded = Math.min(byPixelGuard, byRatioGuard);
    return Math.max(MIN_EFFECTIVE_WRAP_WIDTH_PX, guarded);
  }
  class SubtitlesWidget {
    video;
    container;
    fullscreenLayerController;
    tooltipLayoutRoot;
    subtitlesContainer = null;
    subtitlesBlock = null;
    renderedTokenEls = [];
    passedFlagsBuffer = [];
    subtitles = null;
    subtitleLang;
    lastRenderKey = null;
    lastActiveLineIndex = null;
    highlightWords = false;
    fontSize = 20;
    fontSizeOverridden = false;
    fontFamily = "default-sans";
    manualMaxLength = 300;
    smartLayoutEnabled = true;
    smartFontSizePx = 0;
    smartMaxWidthPx = 0;
    smartMaxLength = 0;
    smartAnchorWidthPx = 0;
    smartAnchorHeightPx = 0;
    lastSmartLayoutKey = null;
    lastSmartLayoutCheckTs = 0;
    opacity = "0.2";
    maxLength = 300;
    repositionPending = false;
    positionRefreshPending = false;
    updatePending = false;
    lastUpdateRequestTs = 0;
    updateMinIntervalMs = 100;
    updateMinIntervalHighlightMs = 33;
    useVideoFrameCallbacks;
    videoFrameRequestId = null;
    dragDocListenersAttached = false;
    lastPositionRefreshTs = 0;
    positionRefreshIntervalMs = 250;
    subtitleMaxWidthPx = 0;
    breakAfterTokenIndices = [];
    breakAfterTokenIndexSet = null;
    smartTruncateAfterTokenIndex = null;
    wrapPending = false;
    lastWrapKey = null;
    lastWrapTokens = null;
    measureCanvas = null;
    measureCtx = null;
    tokenProcessingMemo = null;
    tokenPrecomputeMemo = null;
    lineMeasureMemo = null;
    lastSegmentIndex = 0;
    lastAppliedLeftPct = null;
    lastAppliedTopPct = null;
    position = {
      left: 50,
      top: 100
    };
    positionPreset = "bottom-center";
    dragging = {
      pointerId: null,
      candidate: false,
      active: false,
      moved: false,
      startClientX: 0,
      startClientY: 0,
      offset: {
        x: 0,
        y: 0
      }
    };
    dragStartThresholdPx = 4;
    snapThresholdPx = 18;
    suppressTokenClicksUntil = 0;
    abortController = new AbortController();
    resizeObserver;
    tokenTooltip;
    tooltipTranslationRequestId = 0;
    intervalIdleChecker;
    checkerUnsubscribe = null;
    edgePunctuationTrimRe = /(?:^[\p{P}\p{S}]+|[\p{P}\p{S}]+$)/gu;
    strTokens = "";
    strTranslatedTokens = "";
    passedStateKey = null;
    passedThresholds = [];
    normalizeTokenTextForTranslation(raw) {
      return raw.trim().replace(this.edgePunctuationTrimRe, "");
    }
    bottomInsetCachedPx = 0;
safeAreaBottomInsetCachedPx = 0;
    containerPaddingBottomCachedPx = 0;
    insetCacheReady = false;
    bottomInsetByMode = {
      normal: {
        ratio: 0.1,
        minPx: 56,
        maxPx: 220,
        gapPx: 10
      },
      fullscreen: {
        ratio: 0.07,
        minPx: 44,
        maxPx: 140,
        gapPx: 9
      }
    };
    safeAreaProbeEl = null;
    guidesLayer = null;
    verticalGuide = null;
    horizontalGuide = null;
    onPointerDownBound;
    onPointerUpBound;
    onPointerMoveBound;
    onTimeUpdateBound;
    onPlaybackStateChangeBound;
    onVisualViewportChangeBound;
    static shouldUseVideoFrameCallbacksForCurrentHost() {
      const host = String(globalThis.location.hostname || "").toLowerCase();
      return host !== "music.youtube.com";
    }
    constructor(video, container, intervalIdleChecker, tooltipLayoutRoot = void 0) {
      this.video = video;
      this.container = container;
      this.fullscreenLayerController = new FullscreenLayerController({
        video,
        container
      });
      this.intervalIdleChecker = intervalIdleChecker;
      this.tooltipLayoutRoot = tooltipLayoutRoot;
      this.useVideoFrameCallbacks = !!this.video && typeof this.video.requestVideoFrameCallback === "function" && SubtitlesWidget.shouldUseVideoFrameCallbacksForCurrentHost();
      this.onPointerDownBound = (event) => this.onPointerDown(event);
      this.onPointerUpBound = (event) => this.onPointerUp(event);
      this.onPointerMoveBound = (event) => this.onPointerMove(event);
      this.onTimeUpdateBound = () => this.requestUpdate();
      this.onPlaybackStateChangeBound = () => this.handlePlaybackStateChange();
      this.onVisualViewportChangeBound = () => this.scheduleReposition();
      this.checkerUnsubscribe = this.intervalIdleChecker.subscribe(() => {
        this.onCheckerTick();
      });
      this.bindEvents();
    }
    updateMount({
      container,
      tooltipLayoutRoot
    }) {
      const containerChanged = this.container !== container;
      const tooltipRootChanged = this.tooltipLayoutRoot !== tooltipLayoutRoot;
      this.container = container;
      this.fullscreenLayerController.updateContainer(container);
      this.tooltipLayoutRoot = tooltipLayoutRoot;
      this.syncWidgetMount();
      if (containerChanged || tooltipRootChanged) {
        this.tokenTooltip?.updateMount({
          parentElement: this.getTokenTooltipParentElement(),
          layoutRoot: this.tooltipLayoutRoot ?? document.documentElement
        });
      }
      if (this.subtitles) {
        this.insetCacheReady = false;
        this.lastAppliedLeftPct = null;
        this.lastAppliedTopPct = null;
        this.updateContainerRect();
        this.requestUpdate();
      }
    }
    resetTranslationContext(releaseTooltip = false) {
      this.strTranslatedTokens = "";
      if (releaseTooltip) {
        this.releaseTooltip();
      }
    }
    resetSegmentationMemo() {
      this.tokenProcessingMemo = null;
      this.tokenPrecomputeMemo = null;
      this.lineMeasureMemo = null;
      this.lastSegmentIndex = 0;
    }
    resetWrapMemo() {
      this.setBreakAfterTokenIndices([]);
      this.smartTruncateAfterTokenIndex = null;
      this.lastWrapKey = null;
    }
    resetRenderMemo() {
      this.lastRenderKey = null;
    }
    computeAnchorBoxLayout(layout) {
      const fallback = {
        left: 0,
        top: 0,
        w: layout.w,
        h: layout.h
      };
      const video = this.video;
      if (!video) return fallback;
      const videoRect = video.getBoundingClientRect();
      if (!(videoRect.width > 0 && videoRect.height > 0)) return fallback;
      const containerRect = layout.rect;
      const intersects = videoRect.right > containerRect.left && videoRect.left < containerRect.right && videoRect.bottom > containerRect.top && videoRect.top < containerRect.bottom;
      if (!intersects) return fallback;
      const w2 = videoRect.width / layout.scaleX;
      const h2 = videoRect.height / layout.scaleY;
      if (!(w2 > 0 && h2 > 0)) return fallback;
      const rawLeft = (videoRect.left - containerRect.left) / layout.scaleX;
      const rawTop = (videoRect.top - containerRect.top) / layout.scaleY;
      const maxLeft = layout.w - w2;
      const maxTop = layout.h - h2;
      const left = maxLeft >= 0 ? clampToRange(rawLeft, 0, maxLeft) : (layout.w - w2) / 2;
      const top = maxTop >= 0 ? clampToRange(rawTop, 0, maxTop) : (layout.h - h2) / 2;
      return { left, top, w: w2, h: h2 };
    }
    readSmartCssMetrics() {
      const block = this.subtitlesBlock;
      if (!block) return null;
      const cs = getComputedStyle(block);
      const fontSizePx = Number.parseFloat(cs.fontSize);
      const maxWidthRawPx = Number.parseFloat(cs.maxWidth);
      if (!Number.isFinite(fontSizePx) || !Number.isFinite(maxWidthRawPx) || fontSizePx <= 0 || maxWidthRawPx <= 0) {
        return null;
      }
      this.subtitleMaxWidthPx = maxWidthRawPx;
      const paddingLeft = Number.parseFloat(cs.paddingLeft) || 0;
      const paddingRight = Number.parseFloat(cs.paddingRight) || 0;
      const maxWidthPx = Math.max(0, maxWidthRawPx - paddingLeft - paddingRight);
      if (maxWidthPx <= 0) return null;
      return { fontSizePx, maxWidthPx };
    }
    ensureSmartLayout(anchorBox) {
      if (!this.smartLayoutEnabled) {
        this.maxLength = this.manualMaxLength;
        return null;
      }
      const cssMetrics = this.readSmartCssMetrics();
      const nextFontSizePx = cssMetrics?.fontSizePx ?? this.smartFontSizePx;
      const nextMaxWidthPx = cssMetrics?.maxWidthPx ?? this.smartMaxWidthPx;
      const next = computeSmartLayoutForBox(anchorBox, cssMetrics);
      const nextKey = `${Math.round(nextFontSizePx)}|${Math.round(
      nextMaxWidthPx
    )}|${next.maxLength}`;
      const fontChanged = Math.abs(nextFontSizePx - this.smartFontSizePx) > 0.5;
      const widthChanged = Math.abs(nextMaxWidthPx - this.smartMaxWidthPx) > 0.5;
      const lengthChanged = next.maxLength !== this.smartMaxLength;
      if (nextKey !== this.lastSmartLayoutKey) {
        this.lastSmartLayoutKey = nextKey;
        this.smartFontSizePx = nextFontSizePx;
        this.smartMaxWidthPx = nextMaxWidthPx;
        this.smartMaxLength = next.maxLength;
      }
      if (lengthChanged) {
        this.maxLength = next.maxLength;
        this.resetRenderMemo();
        this.resetSegmentationMemo();
      }
      if ((fontChanged || widthChanged) && this.lastWrapTokens) {
        this.lastWrapKey = null;
        this.scheduleWrapRecompute();
        this.resetSegmentationMemo();
      }
      return next;
    }
    scheduleReposition() {
      if (this.abortController.signal.aborted) return;
      if (!this.subtitles) return;
      this.repositionPending = true;
      this.intervalIdleChecker.markActivity("subtitles-reposition");
      this.intervalIdleChecker.requestImmediateTick();
    }
    setSubtitlesContainerVar(name, value) {
      const container = this.subtitlesContainer;
      if (!container) return;
      if (value === null) {
        container.style.removeProperty(name);
        return;
      }
      container.style.setProperty(name, value);
    }
    applyOpacityStyle() {
      this.setSubtitlesContainerVar("--vot-subtitles-opacity", this.opacity);
    }
    applyManualFontSizeStyle() {
      if (!this.smartLayoutEnabled && this.fontSizeOverridden) {
        this.setSubtitlesContainerVar(
          "--vot-subtitles-font-size",
          `${this.fontSize}px`
        );
        return;
      }
      this.setSubtitlesContainerVar("--vot-subtitles-font-size", null);
    }
    applyFontFamilyStyle() {
      const fontFamily = this.fontFamily;
      this.setSubtitlesContainerVar(
        "--vot-subtitles-font-family-custom",
        getSubtitleFontFamilyCssValue(fontFamily)
      );
      void ensureGoogleSubtitleFontLoaded(fontFamily, {
        forceGmXhr: true,
        onLoaded: () => {
          if (this.fontFamily !== fontFamily) {
            return;
          }
          this.lastWrapKey = null;
          this.resetSegmentationMemo();
          this.scheduleWrapRecompute();
          this.scheduleReposition();
        }
      });
    }
    syncVisualStyleVars() {
      this.applyOpacityStyle();
      this.applyManualFontSizeStyle();
      this.applyFontFamilyStyle();
    }
    ensureGuidesLayer() {
      if (this.guidesLayer) {
        return this.guidesLayer;
      }
      const layer = document.createElement("vot-block");
      layer.classList.add("vot-subtitles-guides");
      const verticalGuide = document.createElement("vot-block");
      verticalGuide.classList.add(
        "vot-subtitles-guide",
        "vot-subtitles-guide--vertical"
      );
      const horizontalGuide = document.createElement("vot-block");
      horizontalGuide.classList.add(
        "vot-subtitles-guide",
        "vot-subtitles-guide--horizontal"
      );
      layer.append(verticalGuide, horizontalGuide);
      this.guidesLayer = layer;
      this.verticalGuide = verticalGuide;
      this.horizontalGuide = horizontalGuide;
      this.hideSnapGuides();
      return layer;
    }
    hideSnapGuides() {
      this.verticalGuide?.removeAttribute("data-visible");
      this.horizontalGuide?.removeAttribute("data-visible");
    }
    updateSnapGuides(anchorBox, options) {
      const { showVerticalCenter = false, showHorizontalCenter = false } = options;
      const layer = this.ensureGuidesLayer();
      if (!layer.isConnected) {
        this.syncGuideLayerMount();
      }
      if (this.verticalGuide) {
        this.verticalGuide.style.left = `${anchorBox.left + anchorBox.w / 2}px`;
        this.verticalGuide.style.top = `${anchorBox.top}px`;
        this.verticalGuide.style.height = `${anchorBox.h}px`;
        if (showVerticalCenter) {
          this.verticalGuide.dataset.visible = "true";
        } else {
          delete this.verticalGuide.dataset.visible;
        }
      }
      if (this.horizontalGuide) {
        this.horizontalGuide.style.left = `${anchorBox.left}px`;
        this.horizontalGuide.style.top = `${anchorBox.top + anchorBox.h / 2}px`;
        this.horizontalGuide.style.width = `${anchorBox.w}px`;
        if (showHorizontalCenter) {
          this.horizontalGuide.dataset.visible = "true";
        } else {
          delete this.horizontalGuide.dataset.visible;
        }
      }
    }
    syncGuideLayerMount() {
      const widgetParent = this.fullscreenLayerController.getWidgetParentElement();
      const guidesLayer = this.ensureGuidesLayer();
      if (guidesLayer.parentElement !== widgetParent) {
        widgetParent.appendChild(guidesLayer);
      }
    }
    syncWidgetMount() {
      this.fullscreenLayerController.syncWidgetContainer(this.subtitlesContainer);
      this.syncGuideLayerMount();
    }
    getTokenTooltipParentElement() {
      const widgetParent = this.fullscreenLayerController.getWidgetParentElement();
      return widgetParent === this.container ? document.documentElement : widgetParent;
    }
    createSubtitlesContainer() {
      if (this.subtitlesContainer) {
        return this.subtitlesContainer;
      }
      const container = document.createElement("vot-block");
      container.classList.add("vot-subtitles-widget");
      this.subtitlesContainer = container;
      this.syncWidgetMount();
      container.addEventListener("pointerdown", this.onPointerDownBound, {
        signal: this.abortController.signal,
        passive: true
      });
      this.syncVisualStyleVars();
      this.insetCacheReady = false;
      this.updateContainerRect();
      return container;
    }
    bindEvents() {
      const { signal } = this.abortController;
      const opts = { signal };
      this.video?.addEventListener("play", this.onPlaybackStateChangeBound, opts);
      this.video?.addEventListener(
        "pause",
        this.onPlaybackStateChangeBound,
        opts
      );
      this.video?.addEventListener(
        "seeking",
        this.onPlaybackStateChangeBound,
        opts
      );
      this.video?.addEventListener(
        "seeked",
        this.onPlaybackStateChangeBound,
        opts
      );
      this.video?.addEventListener(
        "ended",
        this.onPlaybackStateChangeBound,
        opts
      );
      this.resizeObserver = new ResizeObserver(() => this.onResize());
      this.resizeObserver.observe(this.container);
      if (this.video) this.resizeObserver.observe(this.video);
      globalThis.visualViewport?.addEventListener(
        "resize",
        this.onVisualViewportChangeBound,
        opts
      );
      globalThis.visualViewport?.addEventListener(
        "scroll",
        this.onVisualViewportChangeBound,
        opts
      );
    }
    getUpdateMinIntervalMs() {
      return this.highlightWords ? this.updateMinIntervalHighlightMs : this.updateMinIntervalMs;
    }
    requestUpdate(now2 = performance.now()) {
      if (this.abortController.signal.aborted) return;
      if (!this.subtitles) return;
      const minInterval = this.getUpdateMinIntervalMs();
      if (now2 - this.lastUpdateRequestTs < minInterval) return;
      this.lastUpdateRequestTs = now2;
      this.updatePending = true;
      this.intervalIdleChecker.requestImmediateTick();
    }
    handlePlaybackStateChange() {
      if (!this.subtitles) {
        this.stopVideoFrameLoop();
        return;
      }
      this.scheduleReposition();
      this.requestUpdate();
      this.syncVideoFrameLoop();
    }
    syncVideoFrameLoop() {
      if (!this.useVideoFrameCallbacks) return;
      const video = this.video;
      if (!video) return;
      if (!this.subtitles || video.paused || video.ended) {
        this.stopVideoFrameLoop();
        return;
      }
      this.startVideoFrameLoop();
    }
    startVideoFrameLoop() {
      if (!this.useVideoFrameCallbacks) return;
      const video = this.video;
      if (!video) return;
      if (this.videoFrameRequestId !== null) return;
      this.videoFrameRequestId = video.requestVideoFrameCallback(
        this.onVideoFrame
      );
    }
    stopVideoFrameLoop() {
      if (!this.useVideoFrameCallbacks) return;
      const video = this.video;
      if (!video) return;
      if (this.videoFrameRequestId === null) return;
      try {
        video.cancelVideoFrameCallback(this.videoFrameRequestId);
      } catch {
      }
      this.videoFrameRequestId = null;
    }
    onVideoFrame = (now2, _metadata) => {
      this.videoFrameRequestId = null;
      if (this.abortController.signal.aborted) return;
      const video = this.video;
      if (!video || video.paused || video.ended) return;
      if (!this.subtitles) return;
      this.requestUpdate(now2);
      this.startVideoFrameLoop();
    };
    onCheckerTick() {
      if (this.abortController.signal.aborted) return;
      if (this.repositionPending) {
        this.repositionPending = false;
        this.updateContainerRect();
        this.updatePending = true;
      }
      if (this.wrapPending) {
        this.wrapPending = false;
        this.recomputeWrapNow();
      }
      if (this.positionRefreshPending) {
        this.positionRefreshPending = false;
        this.applySubtitlePosition();
      }
      if (this.updatePending) {
        this.updatePending = false;
        this.update();
      }
    }
    attachDragDocumentListeners() {
      if (this.dragDocListenersAttached) return;
      this.dragDocListenersAttached = true;
      document.addEventListener("pointermove", this.onPointerMoveBound, {
        passive: false
      });
      document.addEventListener("pointerup", this.onPointerUpBound);
      document.addEventListener("pointercancel", this.onPointerUpBound);
    }
    detachDragDocumentListeners() {
      if (!this.dragDocListenersAttached) return;
      this.dragDocListenersAttached = false;
      document.removeEventListener("pointermove", this.onPointerMoveBound);
      document.removeEventListener("pointerup", this.onPointerUpBound);
      document.removeEventListener("pointercancel", this.onPointerUpBound);
    }
    onResize() {
      this.syncWidgetMount();
      this.scheduleReposition();
    }
    updateContainerRect() {
      const layout = this.getLayoutSize();
      if (!layout.w || !layout.h) return;
      const anchorBox = this.computeAnchorBoxLayout(layout);
      if (!anchorBox.w || !anchorBox.h) return;
      this.refreshBottomInsetNow(layout, anchorBox);
      this.applySubtitlePositionWithLayout(layout, anchorBox);
    }
    getLayoutSize() {
      const layoutRoot = this.fullscreenLayerController.getLayoutRootElement();
      const rect = layoutRoot.getBoundingClientRect();
      const w2 = layoutRoot.clientWidth || rect.width;
      const h2 = layoutRoot.clientHeight || rect.height;
      const scaleX = rect.width && w2 ? rect.width / w2 : 1;
      const scaleY = rect.height && h2 ? rect.height / h2 : 1;
      return { w: w2, h: h2, rect, scaleX, scaleY };
    }
    ensureSafeAreaProbe() {
      if (this.safeAreaProbeEl) return;
      const el = document.createElement("div");
      el.style.position = "fixed";
      el.style.left = "0";
      el.style.right = "0";
      el.style.bottom = "0";
      el.style.height = "env(safe-area-inset-bottom, 0px)";
      el.style.pointerEvents = "none";
      el.style.opacity = "0";
      el.style.zIndex = "-1";
      document.documentElement.appendChild(el);
      this.safeAreaProbeEl = el;
    }
    getSafeAreaBottomInsetPx() {
      this.ensureSafeAreaProbe();
      if (!this.safeAreaProbeEl) return 0;
      const h2 = this.safeAreaProbeEl.offsetHeight || 0;
      return h2;
    }
    refreshInsetCache() {
      const layoutRoot = this.fullscreenLayerController.getLayoutRootElement();
      this.safeAreaBottomInsetCachedPx = this.getSafeAreaBottomInsetPx();
      this.containerPaddingBottomCachedPx = Number.parseFloat(getComputedStyle(layoutRoot).paddingBottom || "0") || 0;
      this.insetCacheReady = true;
    }
    isMobileViewport() {
      if (typeof globalThis.matchMedia !== "function") return false;
      return globalThis.matchMedia("(max-width: 900px) and (pointer: coarse)").matches;
    }
    getBottomInsetPreset() {
      const doc = document;
      const fullscreenEl = doc.fullscreenElement ?? doc.webkitFullscreenElement;
      if (!(fullscreenEl instanceof Element)) {
        return this.bottomInsetByMode.normal;
      }
      const { container, video } = this;
      const fullscreenContainsContainer = fullscreenEl === container || fullscreenEl.contains(container) || container.contains(fullscreenEl);
      if (fullscreenContainsContainer) {
        return this.bottomInsetByMode.fullscreen;
      }
      if (video && (fullscreenEl === video || fullscreenEl.contains(video) || video.contains(fullscreenEl))) {
        return this.bottomInsetByMode.fullscreen;
      }
      return this.bottomInsetByMode.normal;
    }
    computeReservedBottomInsetPx(anchorBoxH, preset = this.getBottomInsetPreset()) {
      const raw = anchorBoxH * preset.ratio;
      return clampToRange(raw, preset.minPx, preset.maxPx);
    }
    refreshBottomInsetNow(layout, anchorBox) {
      this.refreshInsetCache();
      const anchorH = anchorBox?.h ?? this.computeAnchorBoxLayout(layout ?? this.getLayoutSize()).h;
      if (!anchorH) {
        this.bottomInsetCachedPx = 0;
        return;
      }
      const preset = this.getBottomInsetPreset();
      this.bottomInsetCachedPx = this.computeReservedBottomInsetPx(
        anchorH,
        preset
      );
    }
    getBottomInsetPx(layout, anchorBox) {
      if (!this.insetCacheReady) {
        this.refreshInsetCache();
      }
      const preset = this.getBottomInsetPreset();
      const safeAreaBottom = this.safeAreaBottomInsetCachedPx;
      const paddingBottom = this.containerPaddingBottomCachedPx;
      if (this.isMobileViewport()) {
        return Math.max(paddingBottom, safeAreaBottom);
      }
      const anchorH = anchorBox?.h ?? this.computeAnchorBoxLayout(layout ?? this.getLayoutSize()).h;
      const reserved = anchorH ? this.computeReservedBottomInsetPx(anchorH, preset) : preset.minPx;
      const stableInset = Math.max(this.bottomInsetCachedPx, reserved);
      return Math.max(paddingBottom, safeAreaBottom, stableInset) + preset.gapPx;
    }
    onPointerDown(event) {
      const subtitlesContainer = this.subtitlesContainer;
      if (!subtitlesContainer) return;
      const target = event.target;
      if (!(target instanceof Node) || !subtitlesContainer.contains(target))
        return;
      if (!event.isPrimary) return;
      if (event.pointerType === "mouse" && event.button !== 0) return;
      const layout = this.getLayoutSize();
      const { rect: containerRect, w: w2, h: h2, scaleX, scaleY } = layout;
      if (!w2 || !h2) return;
      const anchorBox = this.computeAnchorBoxLayout(layout);
      if (!anchorBox.w || !anchorBox.h) return;
      this.lastPositionRefreshTs = performance.now();
      const subRect = subtitlesContainer.getBoundingClientRect();
      const pointerX = (event.clientX - containerRect.left) / scaleX - anchorBox.left;
      const pointerY = (event.clientY - containerRect.top) / scaleY - anchorBox.top;
      const anchorX = (subRect.left - containerRect.left + subRect.width / 2) / scaleX - anchorBox.left;
      const anchorY = (subRect.top - containerRect.top + subRect.height) / scaleY - anchorBox.top;
      this.dragging.pointerId = event.pointerId;
      this.dragging.candidate = true;
      this.dragging.active = false;
      this.dragging.moved = false;
      this.dragging.startClientX = event.clientX;
      this.dragging.startClientY = event.clientY;
      this.dragging.offset.x = anchorX - pointerX;
      this.dragging.offset.y = anchorY - pointerY;
      this.hideSnapGuides();
      this.attachDragDocumentListeners();
      const captureEl = this.subtitlesBlock ?? (target instanceof Element ? target : null);
      try {
        captureEl?.setPointerCapture(event.pointerId);
      } catch {
      }
    }
    onPointerUp(event) {
      if (this.dragging.pointerId === null) return;
      if (event.pointerId !== this.dragging.pointerId) return;
      if (this.dragging.moved) {
        this.suppressTokenClicksUntil = performance.now() + 450;
      }
      this.dragging.pointerId = null;
      this.dragging.candidate = false;
      this.dragging.active = false;
      this.dragging.moved = false;
      this.hideSnapGuides();
      this.detachDragDocumentListeners();
    }
    onPointerMove(event) {
      if (!this.dragging.candidate || this.dragging.pointerId === null) return;
      if (event.pointerId !== this.dragging.pointerId) return;
      if (!this.dragging.active) {
        const thresholdExceeded = hasDragThresholdBeenExceeded(
          this.dragging.startClientX,
          this.dragging.startClientY,
          event.clientX,
          event.clientY,
          this.dragStartThresholdPx
        );
        if (!thresholdExceeded) {
          return;
        }
        this.dragging.active = true;
        this.dragging.moved = true;
        this.suppressTokenClicksUntil = performance.now() + 450;
        this.releaseTooltip();
      } else {
        this.dragging.moved = true;
      }
      event.preventDefault();
      event.stopPropagation();
      const layout = this.getLayoutSize();
      const { rect: containerRect, w: w2, h: h2, scaleX, scaleY } = layout;
      if (!w2 || !h2) return;
      const anchorBox = this.computeAnchorBoxLayout(layout);
      if (!anchorBox.w || !anchorBox.h) return;
      const pointerX = (event.clientX - containerRect.left) / scaleX - anchorBox.left;
      const pointerY = (event.clientY - containerRect.top) / scaleY - anchorBox.top;
      let anchorX = pointerX + this.dragging.offset.x;
      let anchorY = pointerY + this.dragging.offset.y;
      const elW = this.subtitlesContainer?.offsetWidth ?? 0;
      const elH = this.subtitlesContainer?.offsetHeight ?? 0;
      const bottomInset = this.getBottomInsetPx(layout, anchorBox);
      const snappedX = snapValueToNearestCandidate({
        current: anchorX,
        candidates: [anchorBox.w / 2],
        thresholdPx: this.snapThresholdPx
      });
      if (snappedX.snapped) {
        anchorX = snappedX.value;
      }
      const verticalCenterAnchor = anchorBox.h / 2 + elH / 2;
      const snappedY = snapValueToNearestCandidate({
        current: anchorY,
        candidates: [verticalCenterAnchor],
        thresholdPx: this.snapThresholdPx
      });
      if (snappedY.snapped) {
        anchorY = snappedY.value;
      }
      ({ anchorX, anchorY } = clampAnchorWithinBox({
        anchorX,
        anchorY,
        elementWidth: elW,
        elementHeight: elH,
        boxWidth: anchorBox.w,
        boxHeight: anchorBox.h,
        bottomInset
      }));
      this.positionPreset = "custom";
      this.position.left = anchorX / anchorBox.w * 100;
      this.position.top = anchorY / anchorBox.h * 100;
      this.updateSnapGuides(anchorBox, {
        showVerticalCenter: snappedX.snapped,
        showHorizontalCenter: snappedY.snapped
      });
      this.applySubtitlePositionWithLayout(layout, anchorBox);
    }
    applySubtitlePosition() {
      const subtitlesContainer = this.subtitlesContainer;
      if (!subtitlesContainer) return;
      const layout = this.getLayoutSize();
      if (!layout.w || !layout.h) return;
      const anchorBox = this.computeAnchorBoxLayout(layout);
      if (!anchorBox.w || !anchorBox.h) return;
      this.applySubtitlePositionWithLayout(layout, anchorBox);
    }
    applySubtitlePositionWithLayout(layout, anchorBox) {
      const subtitlesContainer = this.subtitlesContainer;
      if (!subtitlesContainer) return;
      const visualScale = Math.min(layout.scaleX || 1, layout.scaleY || 1);
      const compensate = visualScale > 0 && visualScale < 0.999 ? Math.min(1 / visualScale, 3) : 1;
      if (Math.abs(compensate - 1) < 1e-3) {
        subtitlesContainer.style.removeProperty(
          "--vot-subtitles-scale-compensation"
        );
      } else {
        subtitlesContainer.style.setProperty(
          "--vot-subtitles-scale-compensation",
          compensate.toFixed(3)
        );
      }
      const anchorWidthPx = Math.max(1, Math.round(anchorBox.w));
      const anchorHeightPx = Math.max(1, Math.round(anchorBox.h));
      const anchorDimsChanged = anchorWidthPx !== this.smartAnchorWidthPx || anchorHeightPx !== this.smartAnchorHeightPx;
      if (anchorDimsChanged) {
        this.smartAnchorWidthPx = anchorWidthPx;
        this.smartAnchorHeightPx = anchorHeightPx;
        subtitlesContainer.style.setProperty(
          "--vot-subtitles-anchor-width",
          `${anchorWidthPx}px`
        );
        subtitlesContainer.style.setProperty(
          "--vot-subtitles-anchor-height",
          `${anchorHeightPx}px`
        );
        if (this.lastWrapTokens) {
          this.lastWrapKey = null;
          this.resetSegmentationMemo();
          this.scheduleWrapRecompute();
        }
      }
      if (this.smartLayoutEnabled) this.ensureSmartLayout(anchorBox);
      const elW = subtitlesContainer.offsetWidth;
      const elH = subtitlesContainer.offsetHeight;
      const bottomInset = this.getBottomInsetPx(layout, anchorBox);
      let anchorX = this.position.left / 100 * anchorBox.w;
      let anchorY = this.position.top / 100 * anchorBox.h;
      if (this.positionPreset !== "custom") {
        const presetPosition = this.resolvePresetAnchorPosition({
          preset: this.positionPreset,
          anchorBox,
          elementWidth: elW,
          elementHeight: elH,
          bottomInset
        });
        anchorX = presetPosition.anchorX;
        anchorY = presetPosition.anchorY;
        if (anchorBox.w > 0) {
          this.position.left = anchorX / anchorBox.w * 100;
        }
        if (anchorBox.h > 0) {
          this.position.top = anchorY / anchorBox.h * 100;
        }
      }
      let leftPx = anchorX - elW / 2;
      let topPx = anchorY - elH;
      const maxLeftPx = anchorBox.w - elW;
      const maxTopPx = anchorBox.h - bottomInset - elH;
      if (maxLeftPx >= 0) {
        leftPx = clampToRange(leftPx, 0, maxLeftPx);
      } else {
        leftPx = maxLeftPx / 2;
      }
      if (maxTopPx >= 0) {
        topPx = clampToRange(topPx, 0, maxTopPx);
      } else {
        topPx = 0;
      }
      anchorX = leftPx + elW / 2;
      anchorY = topPx + elH;
      const containerAnchorX = anchorBox.left + anchorX;
      const containerAnchorY = anchorBox.top + anchorY;
      const leftPct = containerAnchorX / layout.w * 100;
      const topPct = containerAnchorY / layout.h * 100;
      if (this.lastAppliedLeftPct === null || Math.abs(leftPct - this.lastAppliedLeftPct) >= 0.01) {
        subtitlesContainer.style.left = `${leftPct}%`;
        this.lastAppliedLeftPct = leftPct;
      }
      if (this.lastAppliedTopPct === null || Math.abs(topPct - this.lastAppliedTopPct) >= 0.01) {
        subtitlesContainer.style.top = `${topPct}%`;
        this.lastAppliedTopPct = topPct;
      }
      this.tokenTooltip?.updatePos();
    }
    resolvePresetAnchorPosition({
      preset,
      anchorBox,
      elementWidth,
      elementHeight,
      bottomInset
    }) {
      let anchorX = anchorBox.w / 2;
      let anchorY = anchorBox.h - bottomInset;
      switch (preset) {
        case "top-center":
          anchorY = elementHeight;
          break;
        case "center":
          anchorY = anchorBox.h / 2 + elementHeight / 2;
          break;
        case "bottom-left":
          anchorX = elementWidth / 2;
          break;
        case "bottom-right":
          anchorX = anchorBox.w - elementWidth / 2;
          break;
      }
      return clampAnchorWithinBox({
        anchorX,
        anchorY,
        elementWidth,
        elementHeight,
        boxWidth: anchorBox.w,
        boxHeight: anchorBox.h,
        bottomInset
      });
    }
    applyPositionAfterContentRender() {
      const layout = this.getLayoutSize();
      if (layout.w && layout.h) {
        const anchorBox = this.computeAnchorBoxLayout(layout);
        if (anchorBox.w && anchorBox.h) {
          this.refreshBottomInsetNow(layout, anchorBox);
          this.applySubtitlePositionWithLayout(layout, anchorBox);
          return;
        }
        this.refreshBottomInsetNow(layout);
        this.applySubtitlePosition();
        return;
      }
      this.refreshBottomInsetNow();
      this.applySubtitlePosition();
    }
    trimEdgeWhitespaceTokens(tokens) {
      if (!tokens.length) return tokens;
      let s2 = 0;
      let e2 = tokens.length;
      while (s2 < e2 && !tokens[s2]?.text.trim()) s2 += 1;
      while (e2 > s2 && !tokens[e2 - 1]?.text.trim()) e2 -= 1;
      if (s2 === 0 && e2 === tokens.length) return tokens;
      return s2 >= e2 ? [] : tokens.slice(s2, e2);
    }
    selectTokensByMaxLength(tokens, time) {
      if (!tokens.length) return tokens;
      let start = 0;
      let length = 0;
      let overflowed = false;
      let chosenStart = 0;
      let chosenEnd = tokens.length;
      let hasChosenRange = false;
      let matchedByTime = false;
      const considerRange = (rangeStart, rangeEnd) => {
        if (rangeEnd <= rangeStart) return;
        if (!hasChosenRange) {
          chosenStart = rangeStart;
          chosenEnd = rangeEnd;
          hasChosenRange = true;
        }
        if (matchedByTime) return;
        const first = tokens[rangeStart];
        const last = tokens[rangeEnd - 1];
        if (!first || !last) return;
        const nextStartMs = rangeEnd < tokens.length ? tokens[rangeEnd]?.startMs : void 0;
        const endMs = nextStartMs ?? last.startMs + (last.durationMs ?? 0);
        if (first.startMs <= time && time < endMs) {
          chosenStart = rangeStart;
          chosenEnd = rangeEnd;
          matchedByTime = true;
        }
      };
      for (const [index, token] of tokens.entries()) {
        const nextLength = length + token.text.length;
        if (nextLength > this.maxLength && index > start) {
          overflowed = true;
          considerRange(start, index);
          start = index;
          length = token.text.length;
          continue;
        }
        length = nextLength;
      }
      if (!overflowed) {
        return this.trimEdgeWhitespaceTokens(tokens);
      }
      considerRange(start, tokens.length);
      return this.trimEdgeWhitespaceTokens(tokens.slice(chosenStart, chosenEnd));
    }
    buildTokenPrecomputeInput(tokens) {
      const cached = this.tokenPrecomputeMemo;
      if (cached?.tokens === tokens) {
        return cached.value;
      }
      const { slices, key } = buildWordSlices(tokens);
      const words = slices.map((slice) => ({
        tokenIndex: slice.tokenIndex,
        breakAfterTokenIndex: slice.breakAfterTokenIndex
      }));
      const value = {
        words,
        wordSlices: slices,
        normalizedWordsKey: key
      };
      this.tokenPrecomputeMemo = {
        tokens,
        value
      };
      return value;
    }
    getTokenLayoutInputs(ctx) {
      const block = this.subtitlesBlock;
      if (block) {
        const cs = getComputedStyle(block);
        const fontKey2 = `${cs.fontStyle} ${cs.fontVariant} ${cs.fontWeight} ${cs.fontSize} ${cs.fontFamily}`;
        ctx.font = fontKey2;
        const cssMaxWidth = Number.parseFloat(cs.maxWidth);
        const paddingLeft = Number.parseFloat(cs.paddingLeft) || 0;
        const paddingRight = Number.parseFloat(cs.paddingRight) || 0;
        const baseMaxWidth2 = Number.isFinite(cssMaxWidth) ? cssMaxWidth : this.subtitleMaxWidthPx || globalThis.innerWidth * 0.8;
        if (Number.isFinite(baseMaxWidth2) && baseMaxWidth2 > 0) {
          this.subtitleMaxWidthPx = baseMaxWidth2;
        }
        return {
          fontKey: fontKey2,
          maxWidthPx: Math.max(0, baseMaxWidth2 - paddingLeft - paddingRight)
        };
      }
      const remPx = Number.parseFloat(getComputedStyle(document.documentElement).fontSize) || 16;
      const maxRem = 52;
      const cssFallbackVw = 0.8;
      const baseMaxWidth = Math.min(
        remPx * maxRem,
        this.subtitleMaxWidthPx || globalThis.innerWidth * cssFallbackVw
      );
      const fontSizePx = this.fontSizeOverridden ? this.fontSize : Math.min(24, Math.max(14, globalThis.innerWidth * 0.016));
      const fontKey = `normal normal 500 ${fontSizePx}px ${getSubtitleFontFamilyCssValue(this.fontFamily)}`;
      ctx.font = fontKey;
      return {
        fontKey,
        maxWidthPx: Math.max(0, baseMaxWidth - fontSizePx)
      };
    }
    getActiveLineKey(tokens) {
      if (this.lastActiveLineIndex !== null) {
        return `${this.lastActiveLineIndex}`;
      }
      return `${tokens[0]?.startMs ?? 0}:${tokens[0]?.durationMs ?? 0}:${tokens.length}`;
    }
    getLineMeasureMemo(tokens, activeLineKey) {
      const { words, wordSlices, normalizedWordsKey } = this.buildTokenPrecomputeInput(tokens);
      if (!words.length) return null;
      const ctx = this.getMeasureContext();
      if (!ctx) return null;
      const { fontKey, maxWidthPx } = this.getTokenLayoutInputs(ctx);
      if (!Number.isFinite(maxWidthPx) || maxWidthPx < 24) {
        return null;
      }
      const key = `${activeLineKey}|${fontKey}|${Math.round(
      maxWidthPx
    )}|${normalizedWordsKey}`;
      if (this.lineMeasureMemo?.key === key) {
        return this.lineMeasureMemo;
      }
      const metrics = measureWordSlices(
        wordSlices,
        (text) => ctx.measureText(text).width
      );
      const memo = {
        key,
        words,
        metrics,
        maxWidthPx
      };
      this.lineMeasureMemo = memo;
      return memo;
    }
    buildTokenProcessingMemo(tokens, activeLineKey) {
      const lineMeasure = this.getLineMeasureMemo(tokens, activeLineKey);
      if (!lineMeasure) return null;
      const memoKey = `${lineMeasure.key}|${this.maxLength}`;
      if (this.tokenProcessingMemo?.key === memoKey) {
        return this.tokenProcessingMemo;
      }
      const safeMaxWidthPx = applyWrapWidthGuard(lineMeasure.maxWidthPx);
      const segmentRanges = computeTwoLineSegments(
        tokens,
        lineMeasure.words,
        lineMeasure.metrics,
        safeMaxWidthPx,
        this.maxLength
      );
      const memo = {
        key: memoKey,
        segmentRanges
      };
      this.tokenProcessingMemo = memo;
      this.lastSegmentIndex = 0;
      return memo;
    }
    selectSegmentIndexFromRanges(segmentRanges, time) {
      if (!segmentRanges.length) return -1;
      let idx = this.lastSegmentIndex;
      if (idx >= segmentRanges.length) idx = 0;
      while (idx < segmentRanges.length - 1 && time >= segmentRanges[idx].endMs) {
        idx += 1;
      }
      while (idx > 0 && time < segmentRanges[idx].startMs) {
        idx -= 1;
      }
      if (!(time >= segmentRanges[idx].startMs && time < segmentRanges[idx].endMs)) {
        const found = segmentRanges.findIndex(
          (s2) => time >= s2.startMs && time < s2.endMs
        );
        if (found >= 0) {
          idx = found;
        } else {
          idx = time < segmentRanges[0].startMs ? 0 : segmentRanges.length - 1;
        }
      }
      this.lastSegmentIndex = idx;
      return idx;
    }
    processTokens(tokens, time) {
      if (!tokens.length) return tokens;
      const activeLineKey = this.getActiveLineKey(tokens);
      const memo = this.buildTokenProcessingMemo(tokens, activeLineKey);
      if (!memo) {
        return this.selectTokensByMaxLength(tokens, time);
      }
      const { segmentRanges } = memo;
      if (!segmentRanges.length) {
        return this.trimEdgeWhitespaceTokens(tokens);
      }
      const segmentIndex = this.selectSegmentIndexFromRanges(segmentRanges, time);
      if (segmentIndex < 0) {
        return this.trimEdgeWhitespaceTokens(tokens);
      }
      const seg = segmentRanges[segmentIndex];
      return this.trimEdgeWhitespaceTokens(
        tokens.slice(seg.startToken, seg.endToken)
      );
    }
    async translateStrTokens(text) {
      const fromLang = this.subtitleLang ?? "";
      const toLang = localizationProvider.lang;
      if (this.strTranslatedTokens) {
        const translated2 = await translate(text, fromLang, toLang);
        return [
          this.strTranslatedTokens,
          typeof translated2 === "string" ? translated2 : ""
        ];
      }
      const translated = await translate(
        [this.strTokens, text],
        fromLang,
        toLang
      );
      const pair = Array.isArray(translated) ? translated : [translated, translated];
      const context = typeof pair[0] === "string" ? pair[0] : "";
      const current = typeof pair[1] === "string" ? pair[1] : "";
      this.strTranslatedTokens = context;
      return [context, current];
    }
    isTokenSpanElement(el) {
      return el instanceof HTMLSpanElement && el.dataset.votToken === "1";
    }
    findTokenSpanInPath(path, root) {
      for (const node of path) {
        if (this.isTokenSpanElement(node) && root.contains(node)) {
          return node;
        }
      }
      return null;
    }
    findTokenSpanByPoint(x2, y2, root) {
      const hit = document.elementFromPoint(x2, y2);
      if (this.isTokenSpanElement(hit) && root.contains(hit)) {
        return hit;
      }
      if (!(hit instanceof Element)) return null;
      const closest = hit.closest('span[data-vot-token="1"]');
      if (closest instanceof HTMLSpanElement && root.contains(closest)) {
        return closest;
      }
      return null;
    }
    resolveTokenSpanFromClick(event) {
      const root = this.subtitlesBlock ?? this.subtitlesContainer;
      if (!root) return null;
      if (this.isTokenSpanElement(event.target) && root.contains(event.target)) {
        return event.target;
      }
      const path = typeof event.composedPath === "function" ? event.composedPath() : [];
      const fromPath = this.findTokenSpanInPath(path, root);
      if (fromPath) {
        return fromPath;
      }
      const x2 = event.clientX;
      const y2 = event.clientY;
      if (Number.isFinite(x2) && Number.isFinite(y2)) {
        return this.findTokenSpanByPoint(x2, y2, root);
      }
      return null;
    }
    releaseTooltip() {
      this.tooltipTranslationRequestId += 1;
      if (this.tokenTooltip?.target) {
        this.tokenTooltip.target.classList.remove("selected");
      }
      this.tokenTooltip?.release();
      this.tokenTooltip = void 0;
      return this;
    }
    clearPendingSchedulerState() {
      this.repositionPending = false;
      this.updatePending = false;
      this.wrapPending = false;
      this.positionRefreshPending = false;
    }
    clearRenderedContent({
      releaseTooltip = false
    } = {}) {
      if (releaseTooltip) this.releaseTooltip();
      this.resetRenderMemo();
      this.lastActiveLineIndex = null;
      this.strTokens = "";
      this.resetTranslationContext();
      this.subtitlesBlock = null;
      this.renderedTokenEls = [];
      this.resetWrapMemo();
      this.lastWrapTokens = null;
      this.subtitleMaxWidthPx = 0;
      this.smartAnchorWidthPx = 0;
      this.smartAnchorHeightPx = 0;
      this.smartFontSizePx = 0;
      this.smartMaxWidthPx = 0;
      this.lastAppliedLeftPct = null;
      this.lastAppliedTopPct = null;
      this.passedStateKey = null;
      this.passedThresholds.length = 0;
      this.insetCacheReady = false;
      this.hideSnapGuides();
      this.resetSegmentationMemo();
      this.clearPendingSchedulerState();
      if (this.subtitlesContainer) {
        D(null, this.subtitlesContainer);
      }
    }
    onClick = async (event) => {
      if (performance.now() < this.suppressTokenClicksUntil) {
        event.preventDefault();
        event.stopPropagation();
        return;
      }
      const target = this.resolveTokenSpanFromClick(event);
      if (!target) return;
      if (this.tokenTooltip?.target === target && this.tokenTooltip?.container) {
        if (this.tokenTooltip.showed) target.classList.add("selected");
        else target.classList.remove("selected");
        return;
      }
      this.releaseTooltip();
      const requestId = this.tooltipTranslationRequestId;
      const text = this.normalizeTokenTextForTranslation(
        target.textContent ?? ""
      );
      if (!text) return;
      const service = await votStorage.get(
        "translationService",
        defaultTranslationService
      );
      if (requestId !== this.tooltipTranslationRequestId) return;
      target.classList.add("selected");
      const subtitlesInfo = UI.createSubtitleInfo(
        text,
        this.strTranslatedTokens || this.strTokens,
        service
      );
      const tooltipMaxWidth = Math.max(
        this.subtitleMaxWidthPx,
        this.subtitlesContainer?.offsetWidth ?? 0,
        this.subtitlesBlock?.offsetWidth ?? 0,
        Math.min(globalThis.innerWidth * 0.6, 320)
      );
      const tooltip = new Tooltip({
        target,
        anchor: this.subtitlesBlock ?? target,
        layoutRoot: this.tooltipLayoutRoot,
        content: subtitlesInfo.container,
        parentElement: this.getTokenTooltipParentElement(),
        offset: { x: 4, y: 12 },
        maxWidth: tooltipMaxWidth,
        borderRadius: 12,
        bordered: false,
        position: "top",
        trigger: "click"
      });
      this.tokenTooltip = tooltip;
      tooltip.onClick();
      const strTokens = this.strTokens;
      const translated = await this.translateStrTokens(text);
      if (requestId !== this.tooltipTranslationRequestId) return;
      if (strTokens !== this.strTokens || this.tokenTooltip !== tooltip || tooltip.target !== target || !tooltip.showed)
        return;
      subtitlesInfo.header.textContent = translated[1];
      subtitlesInfo.context.textContent = translated[0];
      tooltip.setContent(subtitlesInfo.container);
    };
    buildPassedState(tokens, time, stateKey) {
      if (this.passedStateKey !== stateKey) {
        this.passedStateKey = stateKey;
        this.passedThresholds.length = 0;
        for (const token of tokens) {
          if (!token.isWordLike) continue;
          const halfway = token.startMs + token.durationMs / 2;
          const earlyPassThreshold = Math.max(token.startMs - 100, halfway - 275);
          this.passedThresholds.push(Math.min(halfway, earlyPassThreshold));
        }
      }
      const flags = this.passedFlagsBuffer;
      const thresholds = this.passedThresholds;
      for (let i2 = 0; i2 < thresholds.length; i2 += 1) {
        flags[i2] = time > thresholds[i2];
      }
      flags.length = thresholds.length;
      return flags;
    }
    renderTokens(tokens) {
      const breakAfter = this.breakAfterTokenIndexSet;
      const out = [];
      const plan = buildSubtitleRenderPlan(tokens, tokens.length - 1, breakAfter);
      for (const part of plan) {
        if (part.kind === "word") {
          out.push(
            b`<span
            data-vot-token="1"
            data-vot-style-italic=${part.style?.italic ? "1" : "0"}
            data-vot-style-bold=${part.style?.bold ? "1" : "0"}
            data-vot-style-underline=${part.style?.underline ? "1" : "0"}
            >${part.text}</span
          >`
          );
          continue;
        }
        if (part.kind === "break") {
          out.push(b`<br class="vot-subtitles-br" />`);
          continue;
        }
        if (part.style) {
          out.push(
            b`<span
            data-vot-style-italic=${part.style.italic ? "1" : "0"}
            data-vot-style-bold=${part.style.bold ? "1" : "0"}
            data-vot-style-underline=${part.style.underline ? "1" : "0"}
            >${part.text}</span
          >`
          );
          continue;
        }
        out.push(part.text);
      }
      return out;
    }
    updatePassedClasses(passedFlags) {
      const tokenEls = this.renderedTokenEls;
      const len = Math.min(tokenEls.length, passedFlags.length);
      for (let i2 = 0; i2 < len; i2 += 1) {
        tokenEls[i2].classList.toggle("passed", passedFlags[i2]);
      }
      for (let i2 = len; i2 < tokenEls.length; i2 += 1) {
        tokenEls[i2].classList.remove("passed");
      }
    }
    clearPassedClasses() {
      for (const tokenEl of this.renderedTokenEls) {
        tokenEl.classList.remove("passed");
      }
    }
    setBreakAfterTokenIndices(indices) {
      this.breakAfterTokenIndices = indices;
      this.breakAfterTokenIndexSet = indices.length ? new Set(indices) : null;
    }
    scheduleWrapRecompute(tokens = null) {
      if (tokens) {
        this.lastWrapTokens = tokens;
      }
      const shouldRequestTick = !this.wrapPending;
      this.wrapPending = true;
      if (shouldRequestTick) {
        this.intervalIdleChecker.requestImmediateTick();
      }
    }
    maybeRefreshPosition(force = false) {
      if (this.abortController.signal.aborted) return;
      if (!this.subtitlesContainer) return;
      const now2 = performance.now();
      if (!force && now2 - this.lastPositionRefreshTs < this.positionRefreshIntervalMs)
        return;
      this.lastPositionRefreshTs = now2;
      this.positionRefreshPending = true;
      this.intervalIdleChecker.requestImmediateTick();
    }
    getMeasureContext(font) {
      if (!this.measureCanvas) {
        this.measureCanvas = document.createElement("canvas");
        this.measureCanvas.width = 1;
        this.measureCanvas.height = 1;
      }
      if (!this.measureCtx) {
        this.measureCtx = this.measureCanvas.getContext("2d", { alpha: false }) ?? this.measureCanvas.getContext("2d");
      }
      if (!this.measureCtx) return null;
      if (typeof font === "string" && font) {
        this.measureCtx.font = font;
      }
      return this.measureCtx;
    }
    arraysEqual(a2, b2) {
      if (a2 === b2) return true;
      if (a2.length !== b2.length) return false;
      for (let i2 = 0; i2 < a2.length; i2 += 1) {
        if (a2[i2] !== b2[i2]) return false;
      }
      return true;
    }
    recomputeWrapNow() {
      const tokens = this.lastWrapTokens;
      const block = this.subtitlesBlock;
      if (!tokens || !block) return;
      const lineMeasure = this.getLineMeasureMemo(
        tokens,
        this.getActiveLineKey(tokens)
      );
      if (!lineMeasure || lineMeasure.maxWidthPx < 50) return;
      const { words, metrics, maxWidthPx } = lineMeasure;
      const safeMaxWidthPx = applyWrapWidthGuard(maxWidthPx);
      if (words.length <= 1) {
        if (this.breakAfterTokenIndices.length || this.smartTruncateAfterTokenIndex !== null) {
          this.resetWrapMemo();
          this.resetRenderMemo();
          this.update();
        }
        return;
      }
      const wrapKey = lineMeasure.key;
      if (wrapKey === this.lastWrapKey) return;
      this.lastWrapKey = wrapKey;
      let nextBreakAfterTokens = [];
      let nextSmartTruncateAfterTokenIndex = null;
      const lineFitsOneLine = getWordRangeWidth(metrics, 0, words.length - 1) <= safeMaxWidthPx;
      if (!lineFitsOneLine) {
        const breakWordIndices = computeBalancedBreaks(
          metrics,
          safeMaxWidthPx
        );
        if (breakWordIndices.length) {
          nextBreakAfterTokens = breakWordIndices.map(
            (wordIdx) => words[wordIdx].breakAfterTokenIndex
          );
        } else if (this.smartLayoutEnabled) {
          const strict = resolveStrictTwoLineLayout(metrics, safeMaxWidthPx);
          nextBreakAfterTokens = strict.breakAfterWordIndices.map(
            (wordIdx) => words[wordIdx].breakAfterTokenIndex
          );
          if (strict.truncateAfterWordIndex !== null) {
            nextSmartTruncateAfterTokenIndex = words[strict.truncateAfterWordIndex]?.breakAfterTokenIndex ?? null;
          }
        }
      }
      const breaksChanged = !this.arraysEqual(
        nextBreakAfterTokens,
        this.breakAfterTokenIndices
      );
      const truncateChanged = nextSmartTruncateAfterTokenIndex !== this.smartTruncateAfterTokenIndex;
      if (breaksChanged || truncateChanged) {
        this.setBreakAfterTokenIndices(nextBreakAfterTokens);
        this.smartTruncateAfterTokenIndex = nextSmartTruncateAfterTokenIndex;
        this.resetRenderMemo();
        this.update();
      }
    }
    setContent(subtitles, lang2 = void 0) {
      this.releaseTooltip();
      this.subtitleLang = lang2;
      if (!subtitles || !this.video) {
        this.clearRenderedContent();
        this.subtitles = null;
        this.clearPendingSchedulerState();
        this.video?.removeEventListener("timeupdate", this.onTimeUpdateBound);
        this.stopVideoFrameLoop();
        this.detachDragDocumentListeners();
        return;
      }
      this.createSubtitlesContainer();
      this.subtitles = subtitles;
      this.lastActiveLineIndex = null;
      if (!this.useVideoFrameCallbacks) {
        this.video.addEventListener("timeupdate", this.onTimeUpdateBound, {
          signal: this.abortController.signal
        });
      }
      this.syncVideoFrameLoop();
      this.updateContainerRect();
      this.update();
      this.intervalIdleChecker.requestImmediateTick();
    }
    setMaxLength(len) {
      if (typeof len === "number" && len > 0) {
        this.manualMaxLength = len;
        if (!this.smartLayoutEnabled) {
          this.maxLength = len;
          this.resetSegmentationMemo();
          this.update();
          this.scheduleReposition();
        }
      }
    }
    setHighlightWords(value) {
      const wasEnabled = this.highlightWords;
      this.highlightWords = Boolean(value);
      if (wasEnabled && !this.highlightWords) {
        this.clearPassedClasses();
      }
      this.update();
    }
    setSmartLayout(enabled) {
      const next = enabled !== false;
      if (next === this.smartLayoutEnabled) return;
      this.smartLayoutEnabled = next;
      this.subtitlesContainer?.style.removeProperty("--vot-subtitles-max-width");
      this.lastSmartLayoutKey = null;
      this.resetWrapMemo();
      this.resetRenderMemo();
      this.resetSegmentationMemo();
      if (!this.smartLayoutEnabled) {
        this.maxLength = this.manualMaxLength;
      }
      this.applyManualFontSizeStyle();
      this.update();
      this.scheduleWrapRecompute();
      this.scheduleReposition();
    }
    setFontSize(size) {
      this.fontSize = size;
      this.fontSizeOverridden = true;
      if (!this.smartLayoutEnabled) {
        this.applyManualFontSizeStyle();
        this.lastWrapKey = null;
        this.resetSegmentationMemo();
        this.scheduleWrapRecompute();
        this.scheduleReposition();
      }
    }
    setFontFamily(fontFamily) {
      this.fontFamily = fontFamily;
      this.applyFontFamilyStyle();
      this.lastWrapKey = null;
      this.resetSegmentationMemo();
      this.scheduleWrapRecompute();
      this.scheduleReposition();
    }
    setOpacity(rate) {
      const numericRate = Number(rate);
      const clampedRate = Number.isFinite(numericRate) ? clampToRange(numericRate, 0, 100) : 0;
      this.opacity = ((100 - clampedRate) / 100).toFixed(2);
      this.applyOpacityStyle();
    }
    stringifyTokens(tokens) {
      let out = "";
      for (const token of tokens) {
        out += token.text;
      }
      return out;
    }
    update() {
      if (!this.video || !this.subtitles) return;
      const time = this.video.currentTime * 1e3;
      const subtitlesList = this.subtitles.subtitles;
      let line;
      let lineIndex = -1;
      const lastIndex = this.lastActiveLineIndex;
      if (typeof lastIndex === "number" && lastIndex >= 0 && lastIndex < subtitlesList.length) {
        const candidate = subtitlesList[lastIndex];
        if (isTimeInLine(time, candidate)) {
          line = candidate;
          lineIndex = lastIndex;
        }
      }
      if (!line) {
        const index = findActiveSubtitleLineIndex(time, subtitlesList);
        if (index !== -1) {
          line = subtitlesList[index];
          lineIndex = index;
        }
      }
      if (!line) {
        this.lastActiveLineIndex = null;
        if (this.subtitlesBlock || this.lastRenderKey !== null || this.strTokens) {
          this.clearRenderedContent({ releaseTooltip: true });
        } else {
          this.releaseTooltip();
        }
        return;
      }
      this.lastActiveLineIndex = lineIndex;
      if (this.smartLayoutEnabled) {
        const now2 = performance.now();
        if (this.lastSmartLayoutKey === null || now2 - this.lastSmartLayoutCheckTs > 500) {
          this.lastSmartLayoutCheckTs = now2;
          const layout = this.getLayoutSize();
          if (layout.w && layout.h) {
            const anchorBox = this.computeAnchorBoxLayout(layout);
            if (anchorBox.w && anchorBox.h) {
              this.ensureSmartLayout(anchorBox);
            }
          }
        }
      } else {
        this.maxLength = this.manualMaxLength;
      }
      const tokens = this.processTokens(line.tokens, time);
      this.lastWrapTokens = tokens;
      const hasSmartTruncation = this.smartLayoutEnabled && typeof this.smartTruncateAfterTokenIndex === "number" && this.smartTruncateAfterTokenIndex >= 0 && this.smartTruncateAfterTokenIndex < tokens.length - 1;
      const strTokens = this.stringifyTokens(tokens);
      const tokensChanged = strTokens !== this.strTokens;
      if (tokensChanged) {
        this.releaseTooltip();
        this.strTokens = strTokens;
        this.resetTranslationContext();
        this.resetWrapMemo();
      }
      const passedStateKey = `${lineIndex}:${strTokens}`;
      const passedFlags = this.highlightWords ? this.buildPassedState(tokens, time, passedStateKey) : null;
      const wrapKey = `${this.breakAfterTokenIndices.join(",")}|${this.smartTruncateAfterTokenIndex ?? ""}`;
      const renderKey = `${lineIndex}:${strTokens}:${wrapKey}`;
      if (renderKey === this.lastRenderKey) {
        if (this.highlightWords && !tokensChanged && passedFlags) {
          this.updatePassedClasses(passedFlags);
        }
        this.maybeRefreshPosition();
        return;
      }
      this.lastRenderKey = renderKey;
      this.subtitlesContainer = this.subtitlesContainer ?? this.createSubtitlesContainer();
      const subtitlesClass = hasSmartTruncation ? "vot-subtitles vot-subtitles--clamped" : "vot-subtitles";
      D(
        b`<vot-block
        class="${subtitlesClass}"
        @click=${this.onClick}
      >
        ${this.renderTokens(tokens)}
      </vot-block>`,
        this.subtitlesContainer
      );
      const firstChild = this.subtitlesContainer.firstElementChild;
      this.subtitlesBlock = firstChild instanceof HTMLElement && firstChild.classList.contains("vot-subtitles") ? firstChild : null;
      this.renderedTokenEls = this.subtitlesBlock ? Array.from(
        this.subtitlesBlock.querySelectorAll(
          'span[data-vot-token="1"]'
        )
      ) : [];
      if (this.highlightWords && passedFlags) {
        this.updatePassedClasses(passedFlags);
      }
      if (tokensChanged) {
        this.applyPositionAfterContentRender();
        this.scheduleWrapRecompute(tokens);
        this.scheduleReposition();
      } else {
        this.maybeRefreshPosition();
      }
    }
    release() {
      this.detachDragDocumentListeners();
      this.stopVideoFrameLoop();
      this.abortController.abort();
      this.resizeObserver?.disconnect();
      this.clearPendingSchedulerState();
      this.checkerUnsubscribe?.();
      this.checkerUnsubscribe = null;
      this.releaseTooltip();
      if (this.subtitlesContainer) {
        this.subtitlesContainer.remove();
        this.subtitlesContainer = null;
      }
      this.fullscreenLayerController.release();
      if (this.safeAreaProbeEl) {
        this.safeAreaProbeEl.remove();
        this.safeAreaProbeEl = null;
      }
      if (this.guidesLayer) {
        this.guidesLayer.remove();
        this.guidesLayer = null;
        this.verticalGuide = null;
        this.horizontalGuide = null;
      }
      this.measureCtx = null;
      this.measureCanvas = null;
      this.lastAppliedLeftPct = null;
      this.lastAppliedTopPct = null;
      this.passedStateKey = null;
      this.passedThresholds.length = 0;
      this.insetCacheReady = false;
    }
  }
  const HTML_TAG_RE = /^<\s*(\/?)\s*([a-z0-9]+)(?:[.\s][^>]*)?>/iu;
  const ASS_OVERRIDE_RE = /^\{([^}]*)\}/u;
  const LEADING_SPEAKER_MARKER_RE = /^(\s*)>>\s*/u;
  const toTokenStyle = (style) => {
    const tokenStyle = {};
    if (style.italic) tokenStyle.italic = true;
    if (style.bold) tokenStyle.bold = true;
    if (style.underline) tokenStyle.underline = true;
    return Object.keys(tokenStyle).length > 0 ? tokenStyle : void 0;
  };
  const stylesEqual = (left, right) => Boolean(left?.italic) === Boolean(right?.italic) && Boolean(left?.bold) === Boolean(right?.bold) && Boolean(left?.underline) === Boolean(right?.underline);
  const pushSegment = (segments, text, style) => {
    if (!text) return;
    const tokenStyle = toTokenStyle(style);
    const previous = segments.at(-1);
    if (previous && stylesEqual(previous.style, tokenStyle)) {
      previous.text += text;
      return;
    }
    segments.push({
      text,
      style: tokenStyle
    });
  };
  const applyAssStyleDirective = (directive, style) => {
    const match = /^\\([ibu])([01])$/u.exec(directive.trim());
    if (!match) return;
    const enabled = match[2] === "1";
    if (match[1] === "i") {
      style.italic = enabled;
      return;
    }
    if (match[1] === "b") {
      style.bold = enabled;
      return;
    }
    if (match[1] === "u") {
      style.underline = enabled;
    }
  };
  const normalizeLeadingSpeakerMarker = (segments) => {
    for (const segment of segments) {
      if (!segment.text) continue;
      const normalized = segment.text.replace(LEADING_SPEAKER_MARKER_RE, "$1");
      segment.text = normalized;
      if (normalized.length > 0) {
        break;
      }
    }
    while (segments[0]?.text === "") {
      segments.shift();
    }
  };
  const buildStyledDisplayModel = (rawText) => {
    const segments = [];
    const activeStyle = {
      italic: false,
      bold: false,
      underline: false
    };
    let cursor = 0;
    while (cursor < rawText.length) {
      const remainder = rawText.slice(cursor);
      if (remainder.startsWith("\\N") || remainder.startsWith("\\n")) {
        pushSegment(segments, "\n", activeStyle);
        cursor += 2;
        continue;
      }
      if (remainder.startsWith("\\h")) {
        pushSegment(segments, " ", activeStyle);
        cursor += 2;
        continue;
      }
      if (remainder[0] === "\n") {
        pushSegment(segments, "\n", activeStyle);
        cursor += 1;
        continue;
      }
      const assMatch = ASS_OVERRIDE_RE.exec(remainder);
      if (assMatch) {
        const directives = assMatch[1].match(/\\[ibu][01]/gu) ?? [];
        for (const directive of directives) {
          applyAssStyleDirective(directive, activeStyle);
        }
        cursor += assMatch[0].length;
        continue;
      }
      const htmlMatch = HTML_TAG_RE.exec(remainder);
      if (htmlMatch) {
        const isClosing = htmlMatch[1] === "/";
        const tagName = htmlMatch[2].toLowerCase();
        if (tagName === "br") {
          pushSegment(segments, "\n", activeStyle);
        } else if (tagName === "i") {
          activeStyle.italic = !isClosing;
        } else if (tagName === "b") {
          activeStyle.bold = !isClosing;
        } else if (tagName === "u") {
          activeStyle.underline = !isClosing;
        }
        cursor += htmlMatch[0].length;
        continue;
      }
      pushSegment(segments, remainder[0], activeStyle);
      cursor += 1;
    }
    normalizeLeadingSpeakerMarker(segments);
    const styledSpans = [];
    let text = "";
    for (const segment of segments) {
      if (!segment.text) continue;
      const start = text.length;
      text += segment.text;
      const end = text.length;
      styledSpans.push({
        start,
        end,
        style: segment.style
      });
    }
    const normalizedText = text.replaceAll(" ", " ");
    const leadingTrim = /^\s*/u.exec(normalizedText)?.[0].length ?? 0;
    const trailingTrim = /\s*$/u.exec(normalizedText)?.[0].length ?? 0;
    const trimmedEnd = Math.max(
      leadingTrim,
      normalizedText.length - trailingTrim
    );
    const finalText = normalizedText.slice(leadingTrim, trimmedEnd);
    const finalSpans = styledSpans.map((span) => ({
      start: Math.max(0, span.start - leadingTrim),
      end: Math.max(0, span.end - leadingTrim),
      style: span.style
    })).filter((span) => span.end > span.start && span.start < finalText.length).map((span) => ({
      ...span,
      end: Math.min(span.end, finalText.length)
    }));
    return {
      text: finalText,
      styledSpans: finalSpans
    };
  };
  const getStyleForRange = (styledSpans, start, end) => {
    if (!styledSpans?.length || end <= start) {
      return void 0;
    }
    const overlap = styledSpans.find(
      (span) => start < span.end && end > span.start && span.style
    );
    return overlap?.style;
  };
  const BOM = "\uFEFF";
  const ASS_OVERRIDE_TAG_RE = /\{[^}]*\}/gu;
  const SRT_TIMING_RE = /^\s*(?<start>\d{1,2}:\d{2}:\d{2}[,.]\d{1,3})\s*-->\s*(?<end>\d{1,2}:\d{2}:\d{2}[,.]\d{1,3})\s*$/u;
  const VTT_TIMING_RE = /^(?<start>(?:\d{2}:)?\d{2}:\d{2}\.\d{3})\s+-->\s+(?<end>(?:\d{2}:)?\d{2}:\d{2}\.\d{3})(?<settings>(?:[ \t]+.+)?)$/u;
  const normalizeNewlines = (value) => value.replace(BOM, "").replaceAll(/\r\n?/gu, "\n");
  const padMilliseconds = (value) => value.length >= 3 ? value.slice(0, 3) : value.padEnd(3, "0");
  const parseClockTime = (value, millisecondsLength) => {
    const parts = value.trim().split(":");
    if (parts.length < 2 || parts.length > 3) {
      return null;
    }
    const normalizedParts = parts.length === 2 ? ["00", ...parts] : parts;
    const [hoursRaw, minutesRaw, secondsRaw] = normalizedParts;
    const [secondsPart, millisecondsPart = "0"] = secondsRaw.split(/[.,]/u);
    if (!/^\d+$/u.test(hoursRaw) || !/^\d{2}$/u.test(minutesRaw) || !/^\d{2}$/u.test(secondsPart) || !/^\d+$/u.test(millisecondsPart)) {
      return null;
    }
    const hours = Number(hoursRaw);
    const minutes = Number(minutesRaw);
    const seconds = Number(secondsPart);
    const milliseconds = Number(
      padMilliseconds(millisecondsPart).slice(0, millisecondsLength)
    );
    if (!Number.isFinite(hours) || !Number.isFinite(minutes) || !Number.isFinite(seconds) || minutes > 59 || seconds > 59) {
      return null;
    }
    return ((hours * 60 + minutes) * 60 + seconds) * 1e3 + milliseconds;
  };
  const formatClockTime = (totalMs, {
    delimiter,
    allowOptionalHours,
    fractionDigits
  }) => {
    const safeMs = Math.max(0, Math.round(totalMs));
    const hours = Math.floor(safeMs / 36e5);
    const minutes = Math.floor(safeMs % 36e5 / 6e4);
    const seconds = Math.floor(safeMs % 6e4 / 1e3);
    const milliseconds = safeMs % 1e3;
    const fraction = milliseconds.toString().padStart(3, "0").slice(0, fractionDigits);
    const hourPart = allowOptionalHours && hours === 0 ? "" : `${hours.toString().padStart(2, "0")}:`;
    return `${hourPart}${minutes.toString().padStart(2, "0")}:${seconds.toString().padStart(2, "0")}${delimiter}${fraction}`;
  };
  const formatAssTime = (totalMs) => {
    const safeMs = Math.max(0, Math.round(totalMs));
    const hours = Math.floor(safeMs / 36e5);
    const minutes = Math.floor(safeMs % 36e5 / 6e4);
    const seconds = Math.floor(safeMs % 6e4 / 1e3);
    const centiseconds = Math.floor(safeMs % 1e3 / 10);
    return `${hours}:${minutes.toString().padStart(2, "0")}:${seconds.toString().padStart(2, "0")}.${centiseconds.toString().padStart(2, "0")}`;
  };
  const parseAssTime = (value) => {
    const match = /^(?<hours>\d+):(?<minutes>\d{2}):(?<seconds>\d{2})\.(?<centiseconds>\d{2})$/u.exec(
      value.trim()
    );
    if (!match?.groups) return null;
    const hours = Number(match.groups.hours);
    const minutes = Number(match.groups.minutes);
    const seconds = Number(match.groups.seconds);
    const centiseconds = Number(match.groups.centiseconds);
    if (minutes > 59 || seconds > 59 || !Number.isFinite(hours) || !Number.isFinite(centiseconds)) {
      return null;
    }
    return ((hours * 60 + minutes) * 60 + seconds) * 1e3 + centiseconds * 10;
  };
  const normalizeSubtitleTextForDisplay = (value) => buildStyledDisplayModel(value).text;
  const toComparableSubtitleOrder = (subtitles) => subtitles.sort((left, right) => {
    const startDiff = left.line.startMs - right.line.startMs;
    if (startDiff !== 0) return startDiff;
    const endDiff = left.line.startMs + Math.max(0, left.line.durationMs) - (right.line.startMs + Math.max(0, right.line.durationMs));
    if (endDiff !== 0) return endDiff;
    return left.index - right.index;
  }).map(({ line }) => line);
  const trimEmptyBoundaryLines = (lines) => {
    let start = 0;
    let end = lines.length;
    while (start < end && lines[start] === "") {
      start += 1;
    }
    while (end > start && lines[end - 1] === "") {
      end -= 1;
    }
    return lines.slice(start, end);
  };
  const parseCueSettings = (rawSettings) => {
    const raw = rawSettings.trim();
    if (!raw) return void 0;
    const values = {};
    for (const token of raw.split(/\s+/u)) {
      const separatorIndex = token.indexOf(":");
      if (separatorIndex <= 0) continue;
      values[token.slice(0, separatorIndex)] = token.slice(separatorIndex + 1);
    }
    return {
      raw,
      values
    };
  };
  const extractVttVoice = (payload) => {
    const match = /^\s*<v(?:\.[^ >]+)?(?:\s+([^>]*?))?>/iu.exec(payload) ?? /^\s*<v\s+([^>]*?)>/iu.exec(payload);
    const voice = match?.[1]?.trim();
    return voice ? voice : void 0;
  };
  const toSrtDisplayText = (payload) => normalizeSubtitleTextForDisplay(payload);
  const toVttDisplayText = (payload) => normalizeSubtitleTextForDisplay(payload);
  const toAssDisplayText = (rawText) => normalizeSubtitleTextForDisplay(rawText);
  const parseSrt = (text) => {
    const normalized = normalizeNewlines(text);
    const lines = normalized.split("\n");
    const cues = [];
    const isSrtCueStart = (index) => {
      const current = lines[index]?.trim() ?? "";
      const next = lines[index + 1]?.trim() ?? "";
      return SRT_TIMING_RE.test(current) || /^\d+$/u.test(current) && SRT_TIMING_RE.test(next);
    };
    let cursor = 0;
    let cueIndex = 0;
    while (cursor < lines.length) {
      while (cursor < lines.length && lines[cursor].trim() === "") {
        cursor += 1;
      }
      if (cursor >= lines.length) break;
      let timingLineIndex = cursor;
      if (/^\d+$/u.test(lines[cursor].trim()) && SRT_TIMING_RE.test(lines[cursor + 1]?.trim() ?? "")) {
        timingLineIndex = cursor + 1;
      }
      const timingMatch = SRT_TIMING_RE.exec(
        lines[timingLineIndex]?.trim() ?? ""
      );
      if (!timingMatch?.groups) {
        cursor += 1;
        continue;
      }
      const startMs = parseClockTime(timingMatch.groups.start, 3);
      const endMs = parseClockTime(timingMatch.groups.end, 3);
      if (startMs == null || endMs == null || endMs < startMs) {
        cursor = timingLineIndex + 1;
        continue;
      }
      cursor = timingLineIndex + 1;
      const payloadLines = [];
      while (cursor < lines.length) {
        if (lines[cursor].trim() === "") {
          const nextCursor = cursor + 1;
          if (isSrtCueStart(nextCursor)) {
            break;
          }
          cursor += 1;
          continue;
        }
        if (payloadLines.length > 0 && isSrtCueStart(cursor)) {
          break;
        }
        payloadLines.push(lines[cursor]);
        cursor += 1;
      }
      const rawText = trimEmptyBoundaryLines(payloadLines).join("\n");
      const displayModel = buildStyledDisplayModel(rawText);
      cues.push({
        index: cueIndex,
        line: {
          text: toSrtDisplayText(rawText),
          startMs,
          durationMs: Math.max(0, endMs - startMs),
          speakerId: "0",
          tokens: [],
          metadata: {
            rawText,
            styledSpans: displayModel.styledSpans
          }
        }
      });
      cueIndex += 1;
    }
    return {
      format: "srt",
      subtitles: toComparableSubtitleOrder(cues)
    };
  };
  const resolveSerializedText = (processed, line, format) => {
    if (processed.format === format) {
      return line.metadata?.rawText ?? line.text;
    }
    if (format === "ass") {
      return line.text.replaceAll("\n", "\\N");
    }
    return line.text;
  };
  const serializeSrt = (processed) => processed.subtitles.map((line, index) => {
    const rawText = resolveSerializedText(processed, line, "srt");
    const endMs = line.startMs + Math.max(0, line.durationMs);
    return [
      String(index + 1),
      `${formatClockTime(line.startMs, {
      delimiter: ",",
      allowOptionalHours: false,
      fractionDigits: 3
    })} --> ${formatClockTime(endMs, {
      delimiter: ",",
      allowOptionalHours: false,
      fractionDigits: 3
    })}`,
      rawText
    ].join("\n");
  }).join("\n\n");
  const pushWebVttBlock = (blocks, cueIndex, lines) => {
    blocks.push({
      cueIndex,
      lines
    });
  };
  const parseVtt = (text) => {
    const normalized = normalizeNewlines(text);
    const lines = normalized.split("\n");
    const headerLine = lines[0] ?? "";
    if (!headerLine.startsWith("WEBVTT")) {
      return {
        format: "vtt",
        subtitles: [],
        metadata: {
          vtt: {
            headerText: "",
            blocks: []
          }
        }
      };
    }
    const metadata = {
      headerText: headerLine.slice("WEBVTT".length).trim(),
      blocks: []
    };
    const cues = [];
    let cursor = 1;
    while (cursor < lines.length) {
      while (cursor < lines.length && lines[cursor].trim() === "") {
        cursor += 1;
      }
      if (cursor >= lines.length) break;
      if (lines[cursor].startsWith("NOTE") || lines[cursor] === "STYLE" || lines[cursor] === "REGION") {
        const blockLines = [];
        while (cursor < lines.length && lines[cursor].trim() !== "") {
          blockLines.push(lines[cursor]);
          cursor += 1;
        }
        pushWebVttBlock(metadata.blocks, cues.length, blockLines);
        continue;
      }
      let cueId;
      if (!VTT_TIMING_RE.test(lines[cursor] ?? "") && VTT_TIMING_RE.test(lines[cursor + 1] ?? "")) {
        cueId = lines[cursor];
        cursor += 1;
      }
      const timingMatch = VTT_TIMING_RE.exec(lines[cursor] ?? "");
      if (!timingMatch?.groups) {
        cursor += 1;
        continue;
      }
      const startMs = parseClockTime(timingMatch.groups.start, 3);
      const endMs = parseClockTime(timingMatch.groups.end, 3);
      if (startMs == null || endMs == null || endMs < startMs) {
        cursor += 1;
        continue;
      }
      cursor += 1;
      const payloadLines = [];
      while (cursor < lines.length && lines[cursor].trim() !== "") {
        payloadLines.push(lines[cursor]);
        cursor += 1;
      }
      const rawText = payloadLines.join("\n");
      const displayModel = buildStyledDisplayModel(rawText);
      const voice = extractVttVoice(rawText);
      cues.push({
        index: cues.length,
        line: {
          text: toVttDisplayText(rawText),
          startMs,
          durationMs: Math.max(0, endMs - startMs),
          speakerId: voice ?? "0",
          tokens: [],
          metadata: {
            rawText,
            styledSpans: displayModel.styledSpans,
            vtt: {
              cueId,
              settings: parseCueSettings(timingMatch.groups.settings ?? ""),
              voice,
              rawPayload: payloadLines
            }
          }
        }
      });
    }
    return {
      format: "vtt",
      subtitles: toComparableSubtitleOrder(cues),
      metadata: {
        vtt: metadata
      }
    };
  };
  const serializeVttTiming = (line) => {
    const endMs = line.startMs + Math.max(0, line.durationMs);
    const settings = line.metadata?.vtt?.settings?.raw;
    return `${formatClockTime(line.startMs, {
    delimiter: ".",
    allowOptionalHours: true,
    fractionDigits: 3
  })} --> ${formatClockTime(endMs, {
    delimiter: ".",
    allowOptionalHours: true,
    fractionDigits: 3
  })}${settings ? ` ${settings}` : ""}`;
  };
  const serializeVtt = (processed) => {
    const metadata = processed.metadata?.vtt;
    const sections = [
      `WEBVTT${metadata?.headerText ? ` ${metadata.headerText}` : ""}`
    ];
    const blocksByIndex = new Map();
    for (const block of metadata?.blocks ?? []) {
      const existing = blocksByIndex.get(block.cueIndex) ?? [];
      existing.push(block.lines);
      blocksByIndex.set(block.cueIndex, existing);
    }
    const emitBlocks = (cueIndex) => {
      for (const lines of blocksByIndex.get(cueIndex) ?? []) {
        sections.push(lines.join("\n"));
      }
    };
    emitBlocks(0);
    processed.subtitles.forEach((line, index) => {
      const cueId = line.metadata?.vtt?.cueId;
      const cueSections = [
        ...cueId ? [cueId] : [],
        serializeVttTiming(line),
        (processed.format === "vtt" ? line.metadata?.vtt?.rawPayload : line.text.split("\n"))?.join("\n") ?? line.text
      ];
      sections.push(cueSections.join("\n"));
      emitBlocks(index + 1);
    });
    return sections.filter(Boolean).join("\n\n");
  };
  const splitAssFields = (value, fieldCount) => {
    if (fieldCount <= 1) {
      return [value];
    }
    const fields = [];
    let cursor = 0;
    for (let i2 = 0; i2 < fieldCount - 1; i2 += 1) {
      const separatorIndex = value.indexOf(",", cursor);
      if (separatorIndex < 0) {
        fields.push(value.slice(cursor).trim());
        cursor = value.length;
        break;
      }
      fields.push(value.slice(cursor, separatorIndex).trim());
      cursor = separatorIndex + 1;
    }
    fields.push(value.slice(cursor).trim());
    return fields;
  };
  const createEmptyAssMetadata = () => ({
    scriptInfoLines: [],
    styleFormat: "",
    styleLines: [],
    eventFormat: "",
    preEventLines: [],
    commentLines: []
  });
  const createDefaultAssMetadata = (title = "Exported subtitles") => ({
    scriptInfoLines: [
      `Title: ${title}`,
      "ScriptType: v4.00+",
      "WrapStyle: 0",
      "ScaledBorderAndShadow: yes"
    ],
    styleFormat: "Format: Name, Fontname, Fontsize, PrimaryColour, SecondaryColour, OutlineColour, BackColour, Bold, Italic, Underline, StrikeOut, ScaleX, ScaleY, Spacing, Angle, BorderStyle, Outline, Shadow, Alignment, MarginL, MarginR, MarginV, Encoding",
    styleLines: [
      "Style: Default,Arial,42,&H00FFFFFF,&H000000FF,&H00000000,&H64000000,0,0,0,0,100,100,0,0,1,2,0,2,20,20,20,1"
    ],
    eventFormat: "Format: Layer, Start, End, Style, Name, MarginL, MarginR, MarginV, Effect, Text",
    preEventLines: [],
    commentLines: []
  });
  const parseAss = (text) => {
    const normalized = normalizeNewlines(text);
    const lines = normalized.split("\n");
    const metadata = createEmptyAssMetadata();
    const cues = [];
    let currentSection = "";
    let eventFields = [];
    lines.forEach((line, index) => {
      const trimmed = line.trim();
      if (!trimmed) return;
      const sectionMatch = /^\[(.+)\]$/u.exec(trimmed);
      if (sectionMatch) {
        currentSection = sectionMatch[1];
        return;
      }
      if (currentSection === "Script Info") {
        metadata.scriptInfoLines.push(line);
        return;
      }
      if (currentSection === "V4+ Styles" || currentSection === "V4 Styles") {
        if (line.startsWith("Format:")) {
          metadata.styleFormat = line;
          return;
        }
        if (line.startsWith("Style:")) {
          metadata.styleLines.push(line);
          return;
        }
        metadata.preEventLines.push(line);
        return;
      }
      if (currentSection === "Events") {
        if (line.startsWith("Format:")) {
          metadata.eventFormat = line;
          eventFields = line.slice("Format:".length).split(",").map((field) => field.trim());
          return;
        }
        if (!line.includes(":")) {
          metadata.preEventLines.push(line);
          return;
        }
        const separatorIndex = line.indexOf(":");
        const kind = line.slice(0, separatorIndex).trim();
        const value = line.slice(separatorIndex + 1).trim();
        if (!eventFields.length && metadata.eventFormat) {
          eventFields = metadata.eventFormat.slice("Format:".length).split(",").map((field) => field.trim());
        }
        const values = splitAssFields(value, eventFields.length);
        const event = Object.fromEntries(
          eventFields.map((field, fieldIndex) => [
            field,
            values[fieldIndex] ?? ""
          ])
        );
        if (kind === "Comment") {
          metadata.commentLines.push(line);
          return;
        }
        if (kind !== "Dialogue") {
          metadata.preEventLines.push(line);
          return;
        }
        const startMs = parseAssTime(event.Start ?? "");
        const endMs = parseAssTime(event.End ?? "");
        if (startMs == null || endMs == null || endMs < startMs) {
          return;
        }
        const rawText = event.Text ?? "";
        const displayModel = buildStyledDisplayModel(rawText);
        const assMetadata = {
          kind: "dialogue",
          layer: event.Layer ?? "0",
          style: event.Style ?? "Default",
          name: event.Name ?? "",
          marginL: event.MarginL ?? "0",
          marginR: event.MarginR ?? "0",
          marginV: event.MarginV ?? "0",
          effect: event.Effect ?? "",
          rawText,
          overrideTags: rawText.match(ASS_OVERRIDE_TAG_RE) ?? []
        };
        cues.push({
          index,
          line: {
            text: toAssDisplayText(rawText),
            startMs,
            durationMs: Math.max(0, endMs - startMs),
            speakerId: assMetadata.name || "0",
            tokens: [],
            metadata: {
              rawText,
              styledSpans: displayModel.styledSpans,
              ass: assMetadata
            }
          }
        });
      }
    });
    return {
      format: "ass",
      subtitles: toComparableSubtitleOrder(cues),
      metadata: {
        ass: metadata
      }
    };
  };
  const serializeAssDialogue = (line) => {
    const ass = line.metadata?.ass;
    const endMs = line.startMs + Math.max(0, line.durationMs);
    const rawText = ass?.rawText ?? line.metadata?.rawText ?? line.text.replaceAll("\n", "\\N");
    return [
      ass?.kind === "comment" ? "Comment" : "Dialogue",
      ": ",
      [
        ass?.layer ?? "0",
        formatAssTime(line.startMs),
        formatAssTime(endMs),
        ass?.style ?? "Default",
        ass?.name ?? "",
        ass?.marginL ?? "0",
        ass?.marginR ?? "0",
        ass?.marginV ?? "0",
        ass?.effect ?? "",
        rawText
      ].join(",")
    ].join("");
  };
  const withAssTitle = (metadata, assTitle) => {
    if (!assTitle) {
      return metadata;
    }
    const titleLine = `Title: ${assTitle}`;
    const scriptInfoLines = [...metadata.scriptInfoLines];
    const existingIndex = scriptInfoLines.findIndex(
      (line) => line.startsWith("Title:")
    );
    if (existingIndex >= 0) {
      if (scriptInfoLines[existingIndex] === "Title: Exported subtitles") {
        scriptInfoLines[existingIndex] = titleLine;
      }
    } else {
      scriptInfoLines.unshift(titleLine);
    }
    return {
      ...metadata,
      scriptInfoLines
    };
  };
  const serializeAss = (processed, options) => {
    const sourceMetadata = processed.metadata?.ass;
    const metadataBase = sourceMetadata && sourceMetadata.scriptInfoLines.length > 0 && sourceMetadata.styleFormat && sourceMetadata.styleLines.length > 0 && sourceMetadata.eventFormat ? sourceMetadata : createDefaultAssMetadata(options?.assTitle);
    const metadata = withAssTitle(metadataBase, options?.assTitle);
    const sections = [
      "[Script Info]",
      ...metadata.scriptInfoLines,
      "",
      "[V4+ Styles]",
      metadata.styleFormat,
      ...metadata.styleLines,
      ...metadata.preEventLines,
      "",
      "[Events]",
      metadata.eventFormat,
      ...metadata.commentLines,
      ...processed.subtitles.map(
        (line) => serializeAssDialogue({
          ...line,
          metadata: processed.format === "ass" ? line.metadata : {
            ...line.metadata,
            rawText: resolveSerializedText(processed, line, "ass")
          }
        })
      )
    ];
    return sections.join("\n").trim();
  };
  const parseSubtitleText = (text, format) => {
    if (format === "srt") return parseSrt(text);
    if (format === "vtt") return parseVtt(text);
    return parseAss(text);
  };
  const sortProcessedSubtitles = (processed) => ({
    ...processed,
    subtitles: toComparableSubtitleOrder(
      processed.subtitles.map((line, index) => ({
        index,
        line
      }))
    )
  });
  const toSubtitlesData = (processed) => {
    const subtitles = processed.subtitles.map((line) => ({
      text: line.text,
      startMs: line.startMs,
      durationMs: line.durationMs,
      speakerId: line.speakerId,
      tokens: line.tokens.map((token) => ({
        text: token.text,
        startMs: token.startMs,
        durationMs: token.durationMs
      }))
    }));
    return {
      containsTokens: subtitles.some((line) => line.tokens.length > 0),
      subtitles
    };
  };
  const serializeProcessedSubtitles = (processed, format, options) => {
    if (format === "json") {
      return toSubtitlesData(processed);
    }
    if (format === "srt") return serializeSrt(processed);
    if (format === "vtt") return serializeVtt(processed);
    return serializeAss(processed, options);
  };
  function toUint32BE(value) {
    return new Uint8Array([
      value >>> 24 & 255,
      value >>> 16 & 255,
      value >>> 8 & 255,
      value & 255
    ]);
  }
  function toSynchsafeInt(value) {
    return new Uint8Array([
      value >>> 21 & 127,
      value >>> 14 & 127,
      value >>> 7 & 127,
      value & 127
    ]);
  }
  function addTitleId3Tag(mp3Buffer, title) {
    const titleBytes = new TextEncoder().encode(title);
    const frameData = new Uint8Array(titleBytes.length + 1);
    frameData[0] = 3;
    frameData.set(titleBytes, 1);
    const frame = new Uint8Array(10 + frameData.length);
    frame.set([84, 73, 84, 50], 0);
    frame.set(toUint32BE(frameData.length), 4);
    frame.set(frameData, 10);
    const header = new Uint8Array(10);
    header.set([73, 68, 51, 3, 0, 0], 0);
    header.set(toSynchsafeInt(frame.length), 6);
    const audioBytes = new Uint8Array(mp3Buffer);
    const out = new Uint8Array(header.length + frame.length + audioBytes.length);
    out.set(header, 0);
    out.set(frame, header.length);
    out.set(audioBytes, header.length + frame.length);
    return new Blob([out], { type: "audio/mpeg" });
  }
  async function readResponseArrayBuffer(res, onProgress) {
    const total = Number(res.headers.get("Content-Length") ?? 0);
    if (!res.body) return res.arrayBuffer();
    const reader = res.body.getReader();
    let loaded = 0;
    let out = total > 0 ? new Uint8Array(total) : null;
    const chunks = [];
    while (true) {
      const { done, value } = await reader.read();
      if (done) break;
      if (!value || value.byteLength === 0) continue;
      if (out) {
        const needed = loaded + value.byteLength;
        if (needed > out.length) {
          const grown = new Uint8Array(Math.max(needed, out.length * 2));
          grown.set(out.subarray(0, loaded));
          out = grown;
        }
        out.set(value, loaded);
        loaded = needed;
      } else {
        chunks.push(value);
        loaded += value.byteLength;
      }
      if (total > 0) {
        onProgress(clamp$2(Math.round(loaded / total * 100)));
      }
    }
    if (out) {
      return out.buffer.slice(0, loaded);
    }
    const merged = new Uint8Array(loaded);
    let offset = 0;
    for (const c2 of chunks) {
      merged.set(c2, offset);
      offset += c2.byteLength;
    }
    return merged.buffer;
  }
  async function downloadTranslation(res, filename, onProgress = () => {
  }, saveOptions = {}) {
    const blob = await buildTranslationBlob(res, filename, onProgress);
    return await downloadBlob(blob, `${filename}.mp3`, saveOptions);
  }
  async function buildTranslationBlob(res, filename, onProgress = () => {
  }) {
    const arrayBuffer = await readResponseArrayBuffer(res, onProgress);
    onProgress(100);
    return addTitleId3Tag(arrayBuffer, filename);
  }
  function isSameOverlayMount(previous, next) {
    return previous.root === next.root && previous.portalContainer === next.portalContainer && previous.subtitlesMountContainer === next.subtitlesMountContainer && previous.tooltipLayoutRoot === next.tooltipLayoutRoot;
  }
  function applyOverlayMountUpdate(previous, next, onChanged) {
    if (isSameOverlayMount(previous, next)) {
      return previous;
    }
    onChanged(next);
    return next;
  }
  function didTooltipMountContextChange(previous, next) {
    return previous.root !== next.root || previous.tooltipLayoutRoot !== next.tooltipLayoutRoot;
  }
  class VOTButton {
    container;
    translateButton;
    separator;
    pipButton;
    separator2;
    menuButton;
    label;

_opacity = 1;
    _position;
    _direction;
    _status;
_labelText;
    constructor({
      position: position2 = "default",
      direction = "default",
      status = "none",
      labelHtml = ""
    }) {
      this._position = position2;
      this._direction = direction;
      this._status = status;
      this._labelText = labelHtml;
      const elements = this.createElements();
      this.container = elements.container;
      this.translateButton = elements.translateButton;
      this.separator = elements.separator;
      this.pipButton = elements.pipButton;
      this.separator2 = elements.separator2;
      this.menuButton = elements.menuButton;
      this.label = elements.label;
    }
    static calcPosition(percentX, isBigContainer) {
      if (!isBigContainer) {
        return "default";
      }
      if (percentX <= 44) {
        return "left";
      }
      if (percentX >= 66) {
        return "right";
      }
      return "default";
    }
    static calcDirection(position2) {
      return ["default", "top"].includes(position2) ? "row" : "column";
    }
    createElements() {
      const container = UI.createEl("vot-block", ["vot-segmented-button"]);
      container.dataset.position = this._position;
      container.dataset.direction = this._direction;
      container.dataset.status = this._status;
      const translateButton = UI.createEl("vot-block", [
        "vot-segment",
        "vot-translate-button"
      ]);
      translateButton.setAttribute("role", "button");
      translateButton.tabIndex = 0;
      translateButton.setAttribute("aria-label", this._labelText || "Translate");
      D(TRANSLATE_ICON_SVG, translateButton);
      const label = UI.createEl("span", ["vot-segment-label"]);
      label.textContent = this._labelText;
      translateButton.appendChild(label);
      const separator = UI.createEl("vot-block", ["vot-separator"]);
      const pipButton = UI.createEl("vot-block", ["vot-segment-only-icon"]);
      pipButton.setAttribute("role", "button");
      pipButton.tabIndex = 0;
      pipButton.setAttribute("aria-label", "Picture in picture");
      D(PIP_ICON_SVG, pipButton);
      const separator2 = UI.createEl("vot-block", ["vot-separator"]);
      const menuButton = UI.createEl("vot-block", ["vot-segment-only-icon"]);
      menuButton.setAttribute("role", "button");
      menuButton.tabIndex = 0;
      menuButton.setAttribute("aria-label", "Menu");
      menuButton.setAttribute("aria-haspopup", "dialog");
      menuButton.setAttribute("aria-expanded", "false");
      D(MENU_ICON, menuButton);
      container.append(
        translateButton,
        separator,
        pipButton,
        separator2,
        menuButton
      );
      return {
        container,
        translateButton,
        separator,
        pipButton,
        separator2,
        menuButton,
        label
      };
    }
    showPiPButton(visible) {
      this.separator2.hidden = this.pipButton.hidden = !visible;
      return this;
    }
    setText(labelText) {
      this._labelText = labelText;
      this.label.textContent = labelText;
      this.translateButton.setAttribute("aria-label", labelText || "Translate");
      return this;
    }
    remove() {
      this.container.remove();
      return this;
    }
    get tooltipPos() {
      switch (this.position) {
        case "left":
          return "right";
        case "right":
          return "left";
        default:
          return "bottom";
      }
    }
    set status(status) {
      this._status = this.container.dataset.status = status;
    }
    get status() {
      return this._status;
    }
    set loading(isLoading) {
      this.container.dataset.loading = isLoading.toString();
    }
    get loading() {
      return this.container.dataset.loading === "true";
    }
    set hidden(isHidden) {
      setHiddenState(this.container, isHidden);
    }
    get hidden() {
      return getHiddenState(this.container);
    }
    get position() {
      return this._position;
    }
    set position(position2) {
      this._position = this.container.dataset.position = position2;
    }
    get direction() {
      return this._direction;
    }
    set direction(direction) {
      this._direction = this.container.dataset.direction = direction;
    }
    set opacity(opacity) {
      const o2 = Number.isFinite(opacity) ? opacity : 1;
      this._opacity = o2;
      const isHidden = o2 <= 0.01;
      this.container.classList.toggle("vot-segmented-button--hidden", isHidden);
    }
    get opacity() {
      return this._opacity;
    }
  }
  class OverlayView {
    static BIG_CONTAINER_WIDTH_PX = 550;
    static MENU_CLAMP_GAP_PX = 12;
    static MENU_CLICK_GUARD_MS = 180;
    mount;
    globalPortal;
    abortController = null;
    defaultVolumePersistTimer;
    defaultVolumePersistDelayMs = 250;
    quickMenuOpenedAt = 0;
    voiceModeMenuOpenedAt = 0;
    dragging = false;
    dragCandidate = false;
    dragDirty = false;
    dragStartX = 0;
    dragStartY = 0;
    currentClientX = 0;
    dragThresholdPx = 6;
    containerRect = null;
    dragIsBigContainer = null;
    checkerUnsubscribe = null;
    initialized = false;
    data;
    videoHandler;
    intervalIdleChecker;
    events = {
      "click:settings": new EventImpl(),
      "click:pip": new EventImpl(),
      "click:downloadTranslation": new EventImpl(),
      "click:downloadSubtitles": new EventImpl(),
      "click:translate": new EventImpl(),
      "click:subtitles": new EventImpl(),
      "select:voiceMode": new EventImpl(),
      "input:videoVolume": new EventImpl(),
      "input:translationVolume": new EventImpl(),
      "select:fromLanguage": new EventImpl(),
      "select:toLanguage": new EventImpl(),
      "select:subtitles": new EventImpl()
    };
votButton;
    votButtonTooltip;
votMenu;
    voiceModeMenu;
    downloadTranslationButton;
    downloadSubtitlesButton;
    openSettingsButton;
    languagePairSelect;
    subtitlesSelectLabel;
    subtitlesSelect;
    videoVolumeSliderLabel;
    videoVolumeSlider;
    translationVolumeSliderLabel;
    translationVolumeSlider;
    constructor({
      mount,
      globalPortal,
      data = {},
      videoHandler,
      intervalIdleChecker
    }) {
      this.mount = mount;
      this.globalPortal = globalPortal;
      this.data = data;
      this.videoHandler = videoHandler;
      this.intervalIdleChecker = intervalIdleChecker;
    }
    get root() {
      return this.mount.root;
    }
    get portalContainer() {
      return this.mount.portalContainer;
    }
    get tooltipLayoutRoot() {
      return this.mount.tooltipLayoutRoot;
    }
    get useRailLayout() {
      const site = this.videoHandler?.site;
      if (!site) {
        return false;
      }
      if (site.host === "googledrive") {
        return false;
      }
      return true;
    }
    get menuHost() {
      return this.useRailLayout ? this.globalPortal : this.root;
    }
    get selectedVoiceMode() {
      return this.data.useLivelyVoice ? "lively" : "standard";
    }
    getVoiceModeLabel(mode = this.selectedVoiceMode) {
      return mode === "lively" ? localizationProvider.get("VOTLivelyVoices") : localizationProvider.get("VOTStandardVoices");
    }
    getVoiceModeDescription(mode = this.selectedVoiceMode) {
      return mode === "lively" ? localizationProvider.get("VOTLivelyVoicesDescription") : localizationProvider.get("VOTStandardVoicesDescription");
    }
    createVoiceModeMenuItem(mode) {
      const item = UI.createEl("vot-block", ["vot-voice-mode-menu-item"]);
      UI.makeButtonLike(item, {
        ariaLabel: this.getVoiceModeLabel(mode)
      });
      item.dataset.mode = mode;
      const icon = UI.createEl("vot-block", ["vot-voice-mode-menu-item-icon"]);
      D(VOICE_WAVE_ICON, icon);
      const content = UI.createEl("vot-block", [
        "vot-voice-mode-menu-item-content"
      ]);
      const title = UI.createEl("vot-block", ["vot-voice-mode-menu-item-title"]);
      title.textContent = this.getVoiceModeLabel(mode);
      const description = UI.createEl("vot-block", [
        "vot-voice-mode-menu-item-description"
      ]);
      description.textContent = this.getVoiceModeDescription(mode);
      content.append(title, description);
      const check = UI.createEl("vot-block", ["vot-voice-mode-menu-item-check"]);
      D(CHECK_ICON, check);
      item.append(icon, content, check);
      return item;
    }
    shouldLogMobileOverlay() {
      return /^(m|music)\.youtube\.com$/i.test(
        String(globalThis.location.hostname || "")
      );
    }
updateMount(nextMount) {
      const prevRoot = this.mount.root;
      const nextRoot = nextMount.root;
      const prevTooltipRoot = this.mount.tooltipLayoutRoot;
      const nextTooltipRoot = nextMount.tooltipLayoutRoot;
      this.mount = nextMount;
      if (!this.isInitialized()) {
        return this;
      }
      if (prevRoot !== nextRoot) {
        if (this.votButton) {
          nextRoot.appendChild(this.votButton.container);
        }
        if (this.votMenu) {
          this.menuHost.appendChild(this.votMenu.container);
        }
        if (this.voiceModeMenu) {
          this.menuHost.appendChild(this.voiceModeMenu.container);
        }
      }
      if (this.votButtonTooltip && didTooltipMountContextChange(
        {
          root: prevRoot,
          portalContainer: this.mount.portalContainer,
          subtitlesMountContainer: this.mount.subtitlesMountContainer,
          tooltipLayoutRoot: prevTooltipRoot
        },
        nextMount
      )) {
        this.votButtonTooltip.updateMount({
          layoutRoot: nextTooltipRoot ?? document.documentElement
        });
      }
      return this;
    }
    ensureMountedNodes() {
      if (!this.isInitialized()) {
        return this;
      }
      if (this.votButton.container.parentElement !== this.root) {
        this.root.appendChild(this.votButton.container);
      }
      const menuHost = this.menuHost;
      if (this.votMenu.container.parentElement !== menuHost) {
        menuHost.appendChild(this.votMenu.container);
      }
      if (this.voiceModeMenu.container.parentElement !== menuHost) {
        menuHost.appendChild(this.voiceModeMenu.container);
      }
      return this;
    }
    isInitialized() {
      return this.initialized;
    }
    calcButtonLayout(position2) {
      if (this.useRailLayout) {
        if (position2 === "left" || position2 === "right") {
          return {
            direction: "column",
            position: position2
          };
        }
        return {
          direction: "row",
          position: position2 === "top" ? "top" : "default"
        };
      }
      if (this.isBigContainer && isSidePosition(position2)) {
        return {
          direction: "column",
          position: position2
        };
      }
      return {
        direction: "row",
        position: "default"
      };
    }
    addEventListener(type, listener) {
      this.events[type].addListener(listener);
      return this;
    }
    removeEventListener(type, listener) {
      this.events[type].removeListener(listener);
      return this;
    }
    scheduleDefaultVolumePersist() {
      if (this.defaultVolumePersistTimer !== void 0) {
        globalThis.clearTimeout(this.defaultVolumePersistTimer);
      }
      this.defaultVolumePersistTimer = globalThis.setTimeout(() => {
        this.defaultVolumePersistTimer = void 0;
        this.flushDefaultVolumePersist();
      }, this.defaultVolumePersistDelayMs);
    }
    flushDefaultVolumePersist() {
      if (this.defaultVolumePersistTimer !== void 0) {
        globalThis.clearTimeout(this.defaultVolumePersistTimer);
        this.defaultVolumePersistTimer = void 0;
      }
      if (typeof this.data.defaultVolume !== "number") {
        return;
      }
      void votStorage.set("defaultVolume", this.data.defaultVolume);
    }
    initUI(buttonPosition2 = "default") {
      if (this.isInitialized()) {
        throw new Error("[VOT] OverlayView is already initialized");
      }
      this.initialized = true;
      const { position: position2, direction } = this.calcButtonLayout(buttonPosition2);
      this.votButton = this.useRailLayout ? new VOTRail({
        position: position2,
        direction,
        status: "none",
        labelHtml: localizationProvider.get("VOTRailTranslateAndDub")
      }) : new VOTButton({
        position: position2,
        direction,
        status: "none",
        labelHtml: localizationProvider.get("translateVideo")
      });
      this.votButton.opacity = 0;
      if (!this.pipButtonVisible) {
        this.votButton.showPiPButton(false);
      }
      if (this.useRailLayout && this.votButton instanceof VOTRail) {
        this.votButton.showSubtitlesButton(true);
        this.votButton.setButtonLabels({
          subtitles: localizationProvider.get("VOTSubtitles"),
          pip: "Picture in picture",
          menu: localizationProvider.get("VOTSettings")
        });
      }
      this.root.appendChild(this.votButton.container);
      this.syncSharedRailPlacement();
      this.votButtonTooltip = new Tooltip({
        target: this.votButton.translateButton,
        content: this.useRailLayout ? localizationProvider.get("VOTRailTranslateAndDub") : localizationProvider.get("translateVideo"),
        position: this.votButton.tooltipPos,

autoLayout: false,
        hidden: direction === "row",
        bordered: false,
        parentElement: this.globalPortal,
        layoutRoot: this.tooltipLayoutRoot
      });
      this.votMenu = new VOTMenu({
        titleHtml: localizationProvider.get("VOTSettings"),
        position: position2
      });
      this.menuHost.appendChild(this.votMenu.container);
      if (this.useRailLayout) {
        this.votMenu.container.classList.add("vot-menu--fixed-anchor");
        this.votMenu.container.style.position = "fixed";
        this.votMenu.container.style.left = "0";
        this.votMenu.container.style.top = "0";
        this.votMenu.container.style.zIndex = "2147483647";
        this.votMenu.container.style.pointerEvents = "auto";
        this.votMenu.container.style.width = "min(420px, calc(100vw - 24px))";
        this.votMenu.container.style.maxHeight = "calc(100vh - 24px)";
        this.votMenu.container.style.overflow = "visible";
        this.votMenu.contentWrapper.style.maxHeight = "calc(100vh - 80px)";
        this.votMenu.contentWrapper.style.overflowY = "auto";
        this.votMenu.contentWrapper.style.overflowX = "visible";
      }
      this.votButton.menuButton.setAttribute(
        "aria-controls",
        this.votMenu.container.id
      );
      this.downloadTranslationButton = new DownloadButton();
      this.downloadTranslationButton.hidden = true;
      this.downloadSubtitlesButton = UI.createIconButton(SUBTITLES_ICON, {
        ariaLabel: "Download subtitles"
      });
      this.downloadSubtitlesButton.hidden = true;
      this.openSettingsButton = UI.createIconButton(SETTINGS_ICON, {
        ariaLabel: localizationProvider.get("VOTSettings")
      });
      this.votMenu.headerContainer.append(
        this.downloadTranslationButton.button,
        this.downloadSubtitlesButton,
        this.openSettingsButton
      );
      const detectedLanguage = this.videoHandler?.videoData?.detectedLanguage ?? "en";
      const responseLanguage = this.data.responseLanguage ?? "ru";
      this.languagePairSelect = new LanguagePairSelect({
        from: {

selectTitle: localizationProvider.get(
            `langs.${detectedLanguage}`
          ),
          items: Select.genLanguageItems(availableLangs, detectedLanguage)
        },
        to: {
          selectTitle: localizationProvider.get(
            `langs.${responseLanguage}`
          ),
          items: Select.genLanguageItems(availableTTS, responseLanguage)
        },
        dialogParent: this.globalPortal
      });
      this.subtitlesSelectLabel = new Label({
        labelText: localizationProvider.get("VOTSubtitles")
      });
      this.subtitlesSelect = new Select({
        selectTitle: localizationProvider.get("VOTSubtitlesDisabled"),
        dialogTitle: localizationProvider.get("VOTSubtitles"),
        labelElement: this.subtitlesSelectLabel.container,
        dialogParent: this.globalPortal,
        items: [
          {
            label: localizationProvider.get("VOTSubtitlesDisabled"),
            value: "disabled",
            selected: true
          }
        ]
      });
      const videoVolume = this.videoHandler ? this.videoHandler.getVideoVolume() * 100 : 100;
      this.videoVolumeSliderLabel = new SliderLabel({
        labelText: localizationProvider.get("VOTVolume"),
        value: videoVolume
      });
      this.videoVolumeSlider = new Slider({
        labelHtml: this.videoVolumeSliderLabel.container,
        value: videoVolume
      });
      this.videoVolumeSlider.hidden = !this.data.showVideoSlider || this.votButton.status !== "success";
      const defaultVolume = this.data.defaultVolume ?? 100;
      this.translationVolumeSliderLabel = new SliderLabel({
        labelText: localizationProvider.get("VOTVolumeTranslation"),
        value: defaultVolume
      });
      this.translationVolumeSlider = new Slider({
        labelHtml: this.translationVolumeSliderLabel.container,
        value: defaultVolume,
        max: this.data.audioBooster ? maxAudioVolume : 100
      });
      this.translationVolumeSlider.hidden = this.votButton.status !== "success";
      this.votMenu.bodyContainer.append(
        this.languagePairSelect.container,
        this.subtitlesSelect.container,
        this.videoVolumeSlider.container,
        this.translationVolumeSlider.container
      );
      if (this.useRailLayout) {
        this.voiceModeMenu = new VOTMenu({
          titleHtml: localizationProvider.get("VOTVoiceModeMenuTitle"),
          position: "default"
        });
        this.voiceModeMenu.container.classList.add("vot-menu--fixed-anchor");
        this.voiceModeMenu.container.classList.add("vot-voice-mode-menu");
        this.voiceModeMenu.container.style.position = "fixed";
        this.voiceModeMenu.container.style.left = "0";
        this.voiceModeMenu.container.style.top = "0";
        this.voiceModeMenu.container.style.zIndex = "2147483647";
        this.voiceModeMenu.container.style.pointerEvents = "auto";
        this.voiceModeMenu.container.style.width = "min(360px, calc(100vw - 24px))";
        this.voiceModeMenu.container.style.maxHeight = "calc(100vh - 24px)";
        this.voiceModeMenu.container.style.overflow = "visible";
        this.voiceModeMenu.contentWrapper.style.maxHeight = "calc(100vh - 80px)";
        this.voiceModeMenu.contentWrapper.style.overflowY = "auto";
        this.voiceModeMenu.contentWrapper.style.overflowX = "visible";
        this.voiceModeMenu.hidden = true;
        const standardItem = this.createVoiceModeMenuItem("standard");
        const livelyItem = this.createVoiceModeMenuItem("lively");
        this.voiceModeMenu.bodyContainer.append(standardItem, livelyItem);
        this.menuHost.appendChild(this.voiceModeMenu.container);
        if (this.votButton instanceof VOTRail) {
          this.votButton.translateChevron.setAttribute(
            "aria-controls",
            this.voiceModeMenu.container.id
          );
        }
        this.syncVoiceModeUi();
        this.syncSubtitlesButtonUi();
      }
      return this;
    }
    getMenuBoundsRect() {
      return this.videoHandler?.container?.getBoundingClientRect?.() ?? this.root.getBoundingClientRect();
    }
    positionFixedMenu(menu, anchorRect, opts = {}) {
      if (!this.useRailLayout || !this.votButton) {
        return;
      }
      const boundsRect = this.getMenuBoundsRect();
      const menuWidth = menu.container.offsetWidth || 360;
      const menuHeight = menu.container.offsetHeight || 240;
      const isVertical = this.votButton.position === "left" || this.votButton.position === "right";
      const gap = OverlayView.MENU_CLAMP_GAP_PX;
      const boundsLeft = Math.max(gap, boundsRect.left + gap);
      const boundsTop = Math.max(gap, boundsRect.top + gap);
      const boundsRight = Math.min(
        globalThis.innerWidth - gap,
        boundsRect.right - gap
      );
      const boundsBottom = Math.min(
        globalThis.innerHeight - gap,
        boundsRect.bottom - gap
      );
      const horizontalAnchorRect = opts.alignToGroup ?? anchorRect;
      const desiredLeft = isVertical ? this.votButton.position === "right" ? anchorRect.left - menuWidth - gap : anchorRect.right + gap : horizontalAnchorRect.left + (horizontalAnchorRect.width - menuWidth) / 2;
      const left = Math.max(
        boundsLeft,
        Math.min(Math.max(boundsLeft, boundsRight - menuWidth), desiredLeft)
      );
      const desiredTop = isVertical ? anchorRect.top : (opts.alignToGroup?.bottom ?? anchorRect.bottom) + gap;
      const fallbackTop = isVertical ? Math.max(boundsTop, boundsBottom - menuHeight) : (opts.alignToGroup?.top ?? anchorRect.top) - menuHeight - gap;
      const top = Math.max(
        boundsTop,
        Math.min(
          Math.max(boundsTop, boundsBottom - menuHeight),
          desiredTop + menuHeight <= boundsBottom ? desiredTop : Math.max(boundsTop, fallbackTop)
        )
      );
      menu.container.style.left = `${left}px`;
      menu.container.style.top = `${top}px`;
    }
    positionQuickMenu() {
      if (!this.useRailLayout || !this.votMenu || !this.votButton) {
        return;
      }
      this.positionFixedMenu(
        this.votMenu,
        this.votButton.menuButton.getBoundingClientRect(),
        {
          alignToGroup: this.votButton.container.getBoundingClientRect()
        }
      );
    }
    positionVoiceModeMenu() {
      if (!this.useRailLayout || !this.voiceModeMenu || !(this.votButton instanceof VOTRail)) {
        return;
      }
      this.positionFixedMenu(
        this.voiceModeMenu,
        this.votButton.translateGroup.getBoundingClientRect()
      );
    }
    shouldSuppressFreshMenuInteraction(kind, event) {
      const openedAt = kind === "quick" ? this.quickMenuOpenedAt : this.voiceModeMenuOpenedAt;
      if (!openedAt) {
        return false;
      }
      const elapsed = Date.now() - openedAt;
      if (elapsed > OverlayView.MENU_CLICK_GUARD_MS) {
        return false;
      }
      event.preventDefault();
      event.stopPropagation();
      if ("stopImmediatePropagation" in event) {
        event.stopImmediatePropagation();
      }
      debug.log("[menu-guard] suppress fresh menu interaction", {
        kind,
        elapsed,
        type: event.type
      });
      return true;
    }
    syncSharedRailPlacement() {
      if (!this.useRailLayout || !(this.votButton instanceof VOTRail)) {
        return;
      }
      const style = this.votButton.container.style;
      style.position = "absolute";
      style.bottom = "";
      switch (this.votButton.position) {
        case "left":
          style.left = "50px";
          style.right = "";
          style.top = "12.5vh";
          style.transform = "";
          break;
        case "right":
          style.left = "";
          style.right = "0";
          style.top = "12.5vh";
          style.transform = "";
          break;
        case "top":
        case "default":
        default:
          style.left = "50%";
          style.right = "";
          style.top = "5rem";
          style.transform = "translate(-50%)";
          break;
      }
    }
    syncSubtitlesButtonUi() {
      if (!this.useRailLayout || !(this.votButton instanceof VOTRail) || !this.subtitlesSelect) {
        return this;
      }
      this.votButton.showSubtitlesButton(true);
      return this;
    }
    setVoiceModeMenuOpen(open) {
      if (!this.voiceModeMenu || !(this.votButton instanceof VOTRail)) {
        return;
      }
      if (open) {
        this.votMenu.hidden = true;
        this.votButton.menuButton.setAttribute("aria-expanded", "false");
        this.syncVoiceModeUi();
        this.voiceModeMenu.hidden = false;
        this.voiceModeMenuOpenedAt = Date.now();
        this.votButton.translateChevron.setAttribute("aria-expanded", "true");
        queueMicrotask(() => this.positionVoiceModeMenu());
        return;
      }
      this.voiceModeMenu.hidden = true;
      this.votButton.translateChevron.setAttribute("aria-expanded", "false");
    }
    syncVoiceModeUi() {
      if (!this.useRailLayout || !this.votButton) {
        return this;
      }
      const configuredMode = this.selectedVoiceMode;
      const actualMode = this.videoHandler?.activeVoiceMode;
      const isLoading = this.votButton.loading || Boolean(this.videoHandler?.hadAsyncWait);
      const hasActiveSource = Boolean(this.videoHandler?.hasActiveSource());
      const displayedMode = hasActiveSource && actualMode ? actualMode : configuredMode;
      const status = this.votButton.status;
      const playbackState = isLoading ? "loading" : !hasActiveSource || status === "error" || status === "disabled" ? "idle" : this.videoHandler?.video?.paused ? "paused" : "playing";
      this.votButton.container.dataset.voiceMode = displayedMode;
      this.votButton.container.dataset.voicePlaybackState = playbackState;
      if (status === "success") {
        this.votButton.setText(this.getVoiceModeLabel(displayedMode));
        this.votButtonTooltip?.setContent(this.getVoiceModeLabel(displayedMode));
      }
      if (!this.voiceModeMenu) {
        return this;
      }
      this.voiceModeMenu.setText(
        localizationProvider.get("VOTVoiceModeMenuTitle")
      );
      for (const item of this.voiceModeMenu.bodyContainer.querySelectorAll(
        ".vot-voice-mode-menu-item"
      )) {
        const mode = item.dataset.mode === "lively" ? "lively" : "standard";
        const isSelected = mode === displayedMode;
        const title = item.querySelector(
          ".vot-voice-mode-menu-item-title"
        );
        const description = item.querySelector(
          ".vot-voice-mode-menu-item-description"
        );
        const label = this.getVoiceModeLabel(mode);
        item.dataset.selected = String(isSelected);
        item.dataset.playbackState = isSelected ? playbackState : "idle";
        item.dataset.loading = String(isSelected && isLoading);
        item.setAttribute("aria-pressed", String(isSelected));
        item.setAttribute("aria-label", label);
        if (title) {
          title.textContent = label;
        }
        if (description) {
          description.textContent = this.getVoiceModeDescription(mode);
        }
      }
      return this;
    }
    initUIEvents() {
      if (!this.isInitialized()) {
        throw new Error("[VOT] OverlayView isn't initialized");
      }
      this.abortController = new AbortController();
      const signal = this.abortController.signal;
      this.checkerUnsubscribe?.();
      this.checkerUnsubscribe = this.intervalIdleChecker.subscribe(() => {
        this.onCheckerTick();
      });
      this.votButton.container.addEventListener(
        "click",
        (e2) => {
          e2.preventDefault();
          e2.stopPropagation();
          e2.stopImmediatePropagation();
        },
        { signal }
      );
      const activateOnKey = (handler) => (e2) => {
        if (e2.key === "Enter" || e2.key === " ") {
          e2.preventDefault();
          handler();
        }
      };
      const isPrimaryActionPointer = (event) => event.isPrimary && event.button === 0;
      const setMenuOpen = (open, { returnFocusToToggle = false } = {}) => {
        if (!this.isInitialized()) return;
        if (open && this.voiceModeMenu && !this.voiceModeMenu.hidden) {
          this.setVoiceModeMenuOpen(false);
        }
        this.votMenu.hidden = !open;
        this.votButton.menuButton.setAttribute("aria-expanded", open.toString());
        if (this.votButtonTooltip) {
          this.votButtonTooltip.hidden = open || this.votButton.direction === "row";
        }
        if (open) {
          this.quickMenuOpenedAt = Date.now();
          if (this.useRailLayout) {
            queueMicrotask(() => this.positionQuickMenu());
          }
          queueMicrotask(() => this.openSettingsButton?.focus?.());
        } else if (returnFocusToToggle) {
          queueMicrotask(() => this.votButton.menuButton.focus?.());
        } else {
          this.votButton.menuButton.blur();
        }
      };
      const toggleMenu = () => setMenuOpen(this.votMenu.hidden);
      const closeMenu = (returnFocusToToggle = false) => setMenuOpen(false, { returnFocusToToggle });
      const handleTranslate = () => {
        if (this.useRailLayout) {
          if (this.votButton?.status === "success") {
            debug.log("[voice-menu] translate button clicked while active");
            this.setVoiceModeMenuOpen(false);
            closeMenu();
            this.events["click:translate"].dispatch();
            return;
          }
          if (this.votButton?.loading) {
            debug.log("[voice-menu] translate button ignored because loading");
            return;
          }
          debug.log("[voice-menu] translate button opens voice mode menu");
          closeMenu();
          this.setVoiceModeMenuOpen(this.voiceModeMenu?.hidden ?? true);
          return;
        }
        closeMenu();
        this.events["click:translate"].dispatch();
      };
      if (this.useRailLayout) {
        this.votButton.translateButton.addEventListener(
          "click",
          (event) => {
            event.preventDefault();
            event.stopPropagation();
            handleTranslate();
          },
          { signal }
        );
      } else {
        this.votButton.translateButton.addEventListener(
          "pointerdown",
          (event) => {
            if (!isPrimaryActionPointer(event)) return;
            event.preventDefault();
            handleTranslate();
          },
          { signal }
        );
      }
      this.votButton.translateButton.addEventListener(
        "keydown",
        activateOnKey(handleTranslate),
        { signal }
      );
      if (this.useRailLayout && this.votButton instanceof VOTRail) {
        const toggleVoiceModeMenu = () => {
          closeMenu();
          this.setVoiceModeMenuOpen(this.voiceModeMenu?.hidden ?? true);
        };
        this.votButton.translateChevron.addEventListener(
          "click",
          (event) => {
            event.preventDefault();
            event.stopPropagation();
            toggleVoiceModeMenu();
          },
          { signal }
        );
        this.votButton.translateChevron.addEventListener(
          "keydown",
          activateOnKey(toggleVoiceModeMenu),
          { signal }
        );
      }
      this.votButton.pipButton.addEventListener(
        "pointerdown",
        (event) => {
          if (!isPrimaryActionPointer(event)) return;
          this.setVoiceModeMenuOpen(false);
          closeMenu();
          this.events["click:pip"].dispatch();
        },
        { signal }
      );
      this.votButton.pipButton.addEventListener(
        "keydown",
        activateOnKey(() => {
          this.setVoiceModeMenuOpen(false);
          closeMenu();
          this.events["click:pip"].dispatch();
        }),
        { signal }
      );
      this.votButton.menuButton.addEventListener(
        "click",
        (e2) => {
          e2.preventDefault();
          e2.stopPropagation();
          this.setVoiceModeMenuOpen(false);
          toggleMenu();
        },
        { signal }
      );
      this.votButton.menuButton.addEventListener(
        "keydown",
        activateOnKey(toggleMenu),
        { signal }
      );
      if (this.useRailLayout && this.votButton instanceof VOTRail) {
        const handleOpenSubtitles = () => {
          this.setVoiceModeMenuOpen(false);
          closeMenu();
          queueMicrotask(() => this.subtitlesSelect?.outer.click());
        };
        this.votButton.subtitlesButton.addEventListener(
          "click",
          (event) => {
            event.preventDefault();
            event.stopPropagation();
            handleOpenSubtitles();
          },
          { signal }
        );
        this.votButton.subtitlesButton.addEventListener(
          "keydown",
          activateOnKey(handleOpenSubtitles),
          { signal }
        );
      }
      const touchAction = "none";
      this.votButton.container.style.touchAction = touchAction;
      this.votButton.translateButton.style.touchAction = touchAction;
      this.votButton.pipButton.style.touchAction = touchAction;
      this.votButton.menuButton.style.touchAction = touchAction;
      if (this.votButton instanceof VOTRail) {
        this.votButton.translateChevron.style.touchAction = touchAction;
        this.votButton.subtitlesButton.style.touchAction = touchAction;
      }
      this.votButton.container.addEventListener("pointerdown", this.onDragStart, {
        signal
      });
      this.votButton.container.addEventListener(
        "touchstart",
        this.onTouchDragStart,
        { signal, passive: false }
      );
      this.votMenu.container.addEventListener(
        "pointerdown",
        (e2) => {
          this.shouldSuppressFreshMenuInteraction("quick", e2);
        },
        { signal, capture: true }
      );
      this.votMenu.container.addEventListener(
        "click",
        (e2) => {
          this.shouldSuppressFreshMenuInteraction("quick", e2);
        },
        { signal, capture: true }
      );
      this.votMenu.container.addEventListener(
        "click",
        (e2) => {
          e2.preventDefault();
          e2.stopPropagation();
          e2.stopImmediatePropagation();
        },
        { signal }
      );
      for (const event of ["pointerdown", "mousedown"]) {
        this.votMenu.container.addEventListener(
          event,
          (e2) => {
            e2.stopImmediatePropagation();
          },
          { signal }
        );
      }
      if (this.voiceModeMenu) {
        this.voiceModeMenu.container.addEventListener(
          "pointerdown",
          (e2) => {
            this.shouldSuppressFreshMenuInteraction("voice", e2);
          },
          { signal, capture: true }
        );
        this.voiceModeMenu.container.addEventListener(
          "click",
          (e2) => {
            this.shouldSuppressFreshMenuInteraction("voice", e2);
          },
          { signal, capture: true }
        );
        this.voiceModeMenu.container.addEventListener(
          "click",
          (e2) => {
            e2.preventDefault();
            e2.stopPropagation();
            e2.stopImmediatePropagation();
          },
          { signal }
        );
        for (const event of ["pointerdown", "mousedown"]) {
          this.voiceModeMenu.container.addEventListener(
            event,
            (e2) => {
              e2.stopImmediatePropagation();
            },
            { signal }
          );
        }
        this.voiceModeMenu.container.addEventListener(
          "keydown",
          (e2) => {
            if (e2.key !== "Escape") return;
            e2.preventDefault();
            e2.stopPropagation();
            this.setVoiceModeMenuOpen(false);
          },
          { signal }
        );
        for (const item of this.voiceModeMenu.bodyContainer.querySelectorAll(
          ".vot-voice-mode-menu-item"
        )) {
          const mode = item.dataset.mode === "lively" ? "lively" : "standard";
          const dispatchMode = () => {
            debug.log("[voice-menu] voice menu item clicked", { mode });
            void this.events["select:voiceMode"].dispatchAsync(mode);
            this.setVoiceModeMenuOpen(false);
          };
          item.addEventListener(
            "pointerdown",
            (event) => {
              if (!isPrimaryActionPointer(event)) return;
              event.preventDefault();
              dispatchMode();
            },
            { signal }
          );
          item.addEventListener("keydown", activateOnKey(dispatchMode), {
            signal
          });
        }
      }
      document.addEventListener(
        "pointerdown",
        (e2) => {
          if (this.votMenu.hidden && this.voiceModeMenu?.hidden !== false) return;
          const target = e2.target;
          const path = typeof e2.composedPath === "function" ? e2.composedPath() : [];
          const isInsideMenu = target && this.votMenu.container.contains(target) || path.includes(this.votMenu.container);
          const isInsideVoiceModeMenu = !!this.voiceModeMenu && (target && this.voiceModeMenu.container.contains(target) || path.includes(this.voiceModeMenu.container));
          const isInsideToggle = target && this.votButton.menuButton.contains(target) || path.includes(this.votButton.menuButton);
          const isInsideVoiceToggle = this.votButton instanceof VOTRail && (target && this.votButton.translateGroup.contains(target) || path.includes(this.votButton.translateGroup));
          const isInsideButton = target && this.votButton.container.contains(target) || path.includes(this.votButton.container);
          const isInsideDialog = target instanceof HTMLElement && !!target.closest(".vot-dialog-container");
          if (isInsideMenu || isInsideVoiceModeMenu || isInsideToggle || isInsideVoiceToggle || isInsideButton || isInsideDialog) {
            return;
          }
          closeMenu(false);
          this.setVoiceModeMenuOpen(false);
        },
        { signal, capture: true, passive: true }
      );
      this.votMenu.container.addEventListener(
        "keydown",
        (e2) => {
          if (e2.key !== "Escape") return;
          const keyboardNav = document.documentElement.classList.contains("vot-keyboard-nav");
          e2.preventDefault();
          e2.stopPropagation();
          closeMenu(keyboardNav);
          const hovered = this.votButton.container.matches(":hover") || this.votMenu.container.matches(":hover") || this.voiceModeMenu?.container.matches(":hover");
          if (!hovered) {
            this.videoHandler?.overlayVisibility?.queueAutoHide?.();
          }
        },
        { signal }
      );
      this.downloadTranslationButton.addEventListener("click", () => {
        this.events["click:downloadTranslation"].dispatch();
      });
      this.downloadSubtitlesButton.addEventListener(
        "click",
        () => {
          this.events["click:downloadSubtitles"].dispatch();
        },
        { signal }
      );
      this.openSettingsButton.addEventListener(
        "click",
        () => {
          closeMenu();
          this.events["click:settings"].dispatch();
        },
        { signal }
      );
      this.languagePairSelect.fromSelect.addEventListener(
        "selectItem",
        (language) => {
          if (this.videoHandler) {
            this.videoHandler.translateFromLang = language;
          }
          if (this.videoHandler?.videoData) {
            this.videoHandler.videoManager.rememberUserLanguageSelection(
              this.videoHandler.videoData.videoId,
              language
            );
          }
          this.events["select:fromLanguage"].dispatch(language);
        }
      );
      this.languagePairSelect.toSelect.addEventListener(
        "selectItem",
        async (language) => {
          if (this.videoHandler?.videoData) {
            this.videoHandler.translateToLang = this.videoHandler.videoData.responseLanguage = language;
          }
          const prevResponseLanguage = this.data.responseLanguage;
          if (prevResponseLanguage !== language) {
            this.data.responseLanguage = language;
            await votStorage.set("responseLanguage", this.data.responseLanguage);
          }
          if (this.data.enabledDontTranslateLanguages && Array.isArray(this.data.dontTranslateLanguages) && this.data.dontTranslateLanguages.length === 1 && prevResponseLanguage !== language && typeof prevResponseLanguage === "string" && this.data.dontTranslateLanguages[0] === prevResponseLanguage) {
            this.data.dontTranslateLanguages = [language];
            await votStorage.set(
              "dontTranslateLanguages",
              this.data.dontTranslateLanguages
            );
          }
          this.events["select:toLanguage"].dispatch(language);
        }
      );
      this.subtitlesSelect.addEventListener("beforeOpen", async (dialog) => {
        if (!this.videoHandler?.videoData) {
          return;
        }
        const cacheKey2 = this.videoHandler.getSubtitlesCacheKey(
          this.videoHandler.videoData.videoId,
          this.videoHandler.videoData.detectedLanguage,
          this.videoHandler.videoData.responseLanguage
        );
        if (this.videoHandler.subtitlesCacheKey === cacheKey2) {
          return;
        }
        if (this.videoHandler.cacheManager.getSubtitles(cacheKey2) !== void 0) {
          await this.videoHandler.ensureSubtitlesForCurrentLangPair();
          return;
        }
        const prevLoading = this.votButton?.loading ?? false;
        if (this.votButton) {
          this.votButton.loading = true;
        }
        const loadingEl = UI.createInlineLoader();
        loadingEl.style.margin = "0 auto";
        dialog.footerContainer.appendChild(loadingEl);
        try {
          await this.videoHandler.ensureSubtitlesForCurrentLangPair();
        } finally {
          loadingEl.remove();
          if (this.votButton) {
            this.votButton.loading = prevLoading;
          }
          this.syncVoiceModeUi();
        }
      });
      this.subtitlesSelect.addEventListener("selectItem", (data) => {
        this.events["select:subtitles"].dispatch(data);
      });
      this.videoVolumeSlider.addEventListener("input", (value, fromSetter) => {
        if (this.videoVolumeSliderLabel) {
          this.videoVolumeSliderLabel.value = value;
        }
        if (fromSetter) {
          return;
        }
        this.events["input:videoVolume"].dispatch(value);
      });
      this.translationVolumeSlider.addEventListener(
        "input",
        (value, fromSetter) => {
          if (this.translationVolumeSliderLabel) {
            this.translationVolumeSliderLabel.value = value;
          }
          if (this.data.defaultVolume !== value) {
            this.data.defaultVolume = value;
            this.scheduleDefaultVolumePersist();
          }
          if (fromSetter) {
            return;
          }
          this.events["input:translationVolume"].dispatch(value);
        }
      );
      if (this.useRailLayout && this.videoHandler?.video) {
        const syncRailUi = () => this.syncVoiceModeUi();
        for (const eventName of [
          "play",
          "pause",
          "waiting",
          "playing",
          "ended"
        ]) {
          this.videoHandler.video.addEventListener(eventName, syncRailUi, {
            signal
          });
        }
      }
      return this;
    }
    updateButtonLayout(position2, direction) {
      if (!this.isInitialized()) {
        return this;
      }
      this.votMenu.position = position2;
      this.votButton.position = position2;
      this.votButton.direction = direction;
      this.syncSharedRailPlacement();
      this.votButtonTooltip.hidden = direction === "row";
      this.votButtonTooltip.setPosition(this.votButton.tooltipPos);
      this.syncVoiceModeUi();
      if (!this.votMenu.hidden) {
        queueMicrotask(() => this.positionQuickMenu());
      }
      if (this.voiceModeMenu && !this.voiceModeMenu.hidden) {
        queueMicrotask(() => this.positionVoiceModeMenu());
      }
      return this;
    }
    moveButton(percentX) {
      if (!this.isInitialized()) {
        return this;
      }
      const isBigContainer = this.dragIsBigContainer ?? this.isBigContainer;
      const position2 = VOTButton.calcPosition(percentX, isBigContainer);
      if (position2 === this.votButton.position) {
        return this;
      }
      const direction = VOTButton.calcDirection(position2);
      this.data.buttonPos = position2;
      this.updateButtonLayout(position2, direction);
      return this;
    }
    startDragSession(clientX, clientY, activitySource) {
      this.dragCandidate = true;
      this.dragging = false;
      this.dragStartX = clientX;
      this.dragStartY = clientY;
      this.currentClientX = clientX;
      this.containerRect = this.root.getBoundingClientRect();
      this.dragIsBigContainer = this.isBigContainer;
      this.dragDirty = false;
      this.intervalIdleChecker.markActivity(activitySource);
      this.intervalIdleChecker.requestImmediateTick();
    }
    queueDragTick(activitySource) {
      if (this.dragDirty) {
        return;
      }
      this.dragDirty = true;
      this.intervalIdleChecker.markActivity(activitySource);
      this.intervalIdleChecker.requestImmediateTick();
    }
    updateDragFromMove(clientX, clientY, activitySource) {
      this.currentClientX = clientX;
      if (!this.dragCandidate) return;
      if (!this.dragging) {
        const dx = Math.abs(this.currentClientX - this.dragStartX);
        const dy = Math.abs(clientY - this.dragStartY);
        if (dx + dy >= this.dragThresholdPx) {
          this.dragging = true;
        }
      }
      if (!this.dragging) {
        return;
      }
      this.queueDragTick(activitySource);
    }
    onDragStart = (event) => {
      if (!event.isPrimary || event.button !== 0) return;
      if (event.pointerType === "touch") return;
      event.preventDefault();
      this.startDragSession(event.clientX, event.clientY, "overlay-pointer-down");
      document.addEventListener("pointermove", this.onGlobalPointerMove, {
        passive: true
      });
      document.addEventListener("pointerup", this.onDragEnd);
      document.addEventListener("pointercancel", this.onDragEnd);
    };

onTouchDragStart = (event) => {
      if (!event.touches || event.touches.length === 0) return;
      const touch = event.touches[0];
      this.startDragSession(touch.clientX, touch.clientY, "overlay-touch-start");
      document.addEventListener("touchmove", this.onGlobalTouchMove, {
        passive: false
      });
      document.addEventListener("touchend", this.onDragEnd);
      document.addEventListener("touchcancel", this.onDragEnd);
    };
    onGlobalTouchMove = (event) => {
      if (!event.touches || event.touches.length === 0) return;
      const t2 = event.touches[0];
      this.updateDragFromMove(t2.clientX, t2.clientY, "overlay-touch-move");
      if (this.dragging) {
        event.preventDefault();
      }
    };
    onGlobalPointerMove = (event) => {
      this.updateDragFromMove(
        event.clientX,
        event.clientY,
        "overlay-pointer-move"
      );
    };
    applyDragFromState = () => {
      if (!this.dragging || !this.dragDirty || !this.containerRect) return;
      const width = this.containerRect.width;
      if (!(width > 0 && Number.isFinite(width))) {
        return;
      }
      this.dragDirty = false;
      const x2 = this.currentClientX - this.containerRect.left;
      const clampedX = Math.max(0, Math.min(x2, width));
      const percentX = clampedX / width * 100;
      this.moveButton(percentX);
    };
    onCheckerTick = () => {
      this.applyDragFromState();
    };
    onDragEnd = () => {
      document.removeEventListener("pointermove", this.onGlobalPointerMove);
      document.removeEventListener("pointerup", this.onDragEnd);
      document.removeEventListener("pointercancel", this.onDragEnd);
      document.removeEventListener("touchmove", this.onGlobalTouchMove);
      document.removeEventListener("touchend", this.onDragEnd);
      document.removeEventListener("touchcancel", this.onDragEnd);
      this.applyDragFromState();
      const isBigContainer = this.dragIsBigContainer ?? this.isBigContainer;
      if (this.dragging && isBigContainer && this.data.buttonPos) {
        void votStorage.set("buttonPos", this.data.buttonPos);
      }
      this.dragging = false;
      this.dragCandidate = false;
      this.dragDirty = false;
      this.containerRect = null;
      this.dragIsBigContainer = null;
    };
    updateButtonOpacity(opacity) {
      if (!this.isInitialized() || !this.votMenu.hidden) {
        return this;
      }
      if (Math.abs(this.votButton.opacity - opacity) > 0.01) {
        this.votButton.opacity = opacity;
      }
      return this;
    }
    doReleaseUI() {
      if (this.shouldLogMobileOverlay()) {
        console.log("[VOT][mobile-overlay][ui] remove overlay UI nodes", {
          hasButton: Boolean(this.votButton?.container?.isConnected),
          hasMenu: Boolean(this.votMenu?.container?.isConnected)
        });
      }
      this.votButton?.remove();
      this.votMenu?.remove();
      this.voiceModeMenu?.remove();
      this.votButtonTooltip?.release();
    }
    doReleaseUIEvents() {
      this.abortController?.abort();
      this.abortController = null;
      this.checkerUnsubscribe?.();
      this.checkerUnsubscribe = null;
      this.onDragEnd();
      this.flushDefaultVolumePersist();
      for (const event of Object.values(this.events)) {
        event.clear();
      }
    }
    release() {
      if (!this.isInitialized()) {
        return this;
      }
      if (this.shouldLogMobileOverlay()) {
        console.log("[VOT][mobile-overlay][ui] overlay view release");
      }
      this.doReleaseUIEvents();
      this.doReleaseUI();
      this.initialized = false;
      return this;
    }
    get isBigContainer() {
      const widthFromVideo = this.videoHandler?.video?.getBoundingClientRect?.().width;
      if (typeof widthFromVideo === "number" && Number.isFinite(widthFromVideo)) {
        return widthFromVideo > OverlayView.BIG_CONTAINER_WIDTH_PX;
      }
      const widthFromContainer = this.videoHandler?.container?.getBoundingClientRect?.().width;
      if (typeof widthFromContainer === "number" && Number.isFinite(widthFromContainer)) {
        return widthFromContainer > OverlayView.BIG_CONTAINER_WIDTH_PX;
      }
      return this.root.clientWidth > OverlayView.BIG_CONTAINER_WIDTH_PX;
    }
    get pipButtonVisible() {
      return isPiPAvailable() && !!this.data.showPiPButton;
    }
  }
  function isSidePosition(position2) {
    return position2 === "left" || position2 === "right";
  }
  class UIManager {
    mount;
    translationActionInFlight = false;
    overlayEventsBound = false;
    settingsEventsBound = false;
    initialized = false;
    videoHandler;
    intervalIdleChecker;
    data;
    votGlobalPortal;
votOverlayView;
votSettingsView;
    constructor({
      mount,
      data = {},
      videoHandler,
      intervalIdleChecker
    }) {
      this.mount = mount;
      this.videoHandler = videoHandler;
      this.data = data;
      this.intervalIdleChecker = intervalIdleChecker;
    }
    get root() {
      return this.mount.root;
    }
    get portalContainer() {
      return this.mount.portalContainer;
    }
    get tooltipLayoutRoot() {
      return this.mount.tooltipLayoutRoot;
    }
    isOverlayButtonVisible(button2) {
      if (!button2.isConnected || button2.hidden) {
        return false;
      }
      const style = globalThis.getComputedStyle(button2);
      if (style.display === "none" || style.visibility === "hidden") {
        return false;
      }
      return button2.getClientRects().length > 0;
    }
    getOverlayVerificationState(options = {}) {
      const requireVisible = options.requireVisible !== false;
      const overlayView = this.votOverlayView;
      const button2 = overlayView?.votButton;
      const buttonContainer = button2?.container;
      const root = this.mount.root;
      const initialized = this.isInitialized();
      const rootConnected = Boolean(root?.isConnected);
      const portalConnected = Boolean(this.votGlobalPortal?.isConnected);
      const overlayViewInitialized = Boolean(overlayView?.isInitialized());
      const buttonConnected = Boolean(buttonContainer?.isConnected);
      const buttonVisible = buttonContainer ? this.isOverlayButtonVisible(buttonContainer) : false;
      const verified = initialized && rootConnected && portalConnected && overlayViewInitialized && buttonConnected && (!requireVisible || buttonVisible);
      return {
        initialized,
        rootConnected,
        portalConnected,
        overlayViewInitialized,
        buttonConnected,
        buttonVisible,
        verified
      };
    }
    ensureOverlayMounted(options = {}) {
      if (!this.isInitialized()) {
        this.initUI();
        this.initUIEvents();
      }
      const overlayView = this.votOverlayView;
      if (!overlayView?.isInitialized()) {
        return this.getOverlayVerificationState({
          requireVisible: options.forceVisible
        });
      }
      const globalPortalHost = this.getGlobalPortalHost(this.mount);
      if (this.votGlobalPortal?.parentElement !== globalPortalHost) {
        globalPortalHost.appendChild(this.votGlobalPortal);
      }
      overlayView.updateMount(this.mount);
      overlayView.ensureMountedNodes();
      if (options.forceVisible) {
        overlayView.votButton.hidden = false;
        overlayView.updateButtonOpacity(1);
      }
      return this.getOverlayVerificationState({
        requireVisible: options.forceVisible
      });
    }
    getSubtitlesMountContainer() {
      return this.mount.subtitlesMountContainer;
    }
    isInitialized() {
      return this.initialized;
    }
    initUI() {
      if (this.isInitialized()) {
        throw new Error("[VOT] UIManager is already initialized");
      }
      this.initialized = true;
      this.votGlobalPortal = UI.createPortal();
      this.getGlobalPortalHost(this.mount).appendChild(this.votGlobalPortal);
      this.votOverlayView = new OverlayView({
        mount: this.mount,
        globalPortal: this.votGlobalPortal,
        data: this.data,
        videoHandler: this.videoHandler,
        intervalIdleChecker: this.intervalIdleChecker
      });
      this.votOverlayView.initUI(this.data.buttonPos ?? "default");
      this.votSettingsView = new SettingsView({
        globalPortal: this.votGlobalPortal,
        data: this.data,
        videoHandler: this.videoHandler
      });
      this.votSettingsView.initUI();
      this.videoHandler?.subtitlesWidget?.updateMount({
        container: this.getSubtitlesMountContainer(),
        tooltipLayoutRoot: this.mount.tooltipLayoutRoot
      });
      return this;
    }
    updateMount(mount) {
      const globalPortalHost = this.getGlobalPortalHost(mount);
      if (this.votGlobalPortal?.parentElement !== globalPortalHost) {
        globalPortalHost.appendChild(this.votGlobalPortal);
      }
      this.mount = applyOverlayMountUpdate(this.mount, mount, (nextMount) => {
        this.votOverlayView?.updateMount(nextMount);
      });
      this.videoHandler?.subtitlesWidget?.updateMount({
        container: this.getSubtitlesMountContainer(),
        tooltipLayoutRoot: mount.tooltipLayoutRoot
      });
      return this;
    }
    getGlobalPortalHost(mount) {
      const doc = document;
      const fullscreenEl = doc.fullscreenElement ?? doc.webkitFullscreenElement;
      const isCurrentVideoFullscreen = Boolean(
        resolveScopedFullscreenElement(fullscreenEl, [mount.root], {
          allowDocumentViewport: true
        })
      );
      return isCurrentVideoFullscreen ? mount.root : document.documentElement;
    }
    initUIEvents() {
      if (!this.isInitialized()) {
        throw new Error("[VOT] UIManager isn't initialized");
      }
      this.votOverlayView.initUIEvents();
      if (!this.overlayEventsBound) {
        this.bindOverlayViewEvents();
        this.overlayEventsBound = true;
      }
      this.votSettingsView.initUIEvents();
      if (!this.settingsEventsBound) {
        this.bindSettingsViewEvents();
        this.settingsEventsBound = true;
      }
    }
    bindOverlayViewEvents() {
      const overlayView = this.votOverlayView;
      if (!overlayView) {
        return;
      }
      overlayView.addEventListener("click:translate", async () => {
        await this.handleTranslationBtnClick();
      }).addEventListener("click:pip", async () => {
        if (!this.videoHandler) {
          return;
        }
        try {
          const isPiPActive = this.videoHandler.video === document.pictureInPictureElement;
          await (isPiPActive ? document.exitPictureInPicture() : this.videoHandler.video.requestPictureInPicture());
        } catch (err) {
          debug.warn("[VOT] Failed to toggle Picture-in-Picture", err);
        }
      }).addEventListener("click:subtitles", async () => {
        if (!this.videoHandler) {
          return;
        }
        try {
          await this.videoHandler.toggleSubtitlesForCurrentLangPair();
        } catch (err) {
          debug.warn("[VOT] Failed to toggle subtitles", err);
        }
      }).addEventListener("click:settings", async () => {
        this.videoHandler?.subtitlesWidget?.releaseTooltip();
        this.videoHandler?.overlayVisibility?.cancel();
        this.videoHandler?.overlayVisibility?.show();
        this.votSettingsView.open();
      }).addEventListener("click:downloadTranslation", async () => {
        await this.handleDownloadTranslationClick();
      }).addEventListener("click:downloadSubtitles", async () => {
        await this.handleDownloadSubtitlesClick();
      }).addEventListener("select:voiceMode", async (mode) => {
        const livelyEnabled = mode === "lively";
        if (livelyEnabled && !this.data.account?.token) {
          this.videoHandler?.subtitlesWidget?.releaseTooltip();
          this.videoHandler?.overlayVisibility?.cancel();
          this.videoHandler?.overlayVisibility?.show();
          this.votSettingsView.open();
          return;
        }
        const previousMode = this.data.useLivelyVoice ? "lively" : "standard";
        debug.log("[voice-menu] selected mode", {
          previousMode,
          mode,
          translationActive: this.videoHandler?.hasActiveSource() ?? false,
          translationBusy: this.isTranslationBusy()
        });
        this.data.useLivelyVoice = livelyEnabled;
        if (this.votSettingsView.useLivelyVoiceCheckbox) {
          this.votSettingsView.useLivelyVoiceCheckbox.checked = livelyEnabled;
        }
        this.votOverlayView?.syncVoiceModeUi();
        if (!this.videoHandler) {
          this.runDetached(
            votStorage.set("useLivelyVoice", livelyEnabled),
            "Failed to persist voice mode selection"
          );
          return;
        }
        try {
          debug.log("[voice-menu] applyVoiceModeSelection called", {
            previousMode,
            nextMode: mode,
            startWhenIdle: true
          });
          await this.applyVoiceModeSelection(previousMode, mode, {
            startWhenIdle: true
          });
        } catch (err) {
          debug.warn("[voice-menu] translation failed", err);
          debug.warn("[VOT] Failed to apply voice mode selection", err);
        } finally {
          this.runDetached(
            votStorage.set("useLivelyVoice", livelyEnabled),
            "Failed to persist voice mode selection"
          );
        }
      }).addEventListener("input:videoVolume", (volume) => {
        if (!this.videoHandler) {
          return;
        }
        this.videoHandler.setVideoVolume(volume / 100);
        if (!this.data.syncVolume) {
          this.videoHandler.onVideoVolumeSliderSynced(volume);
          return;
        }
        this.videoHandler.syncVolumeWrapper("video", volume);
      }).addEventListener("input:translationVolume", (volume) => {
        if (!this.videoHandler) {
          return;
        }
        const nextVolume = volume ?? this.data.defaultVolume ?? 100;
        this.videoHandler.syncTranslationPlaybackVolume();
        if (!this.data.syncVolume) {
          this.videoHandler.onTranslationVolumeSliderSynced(nextVolume);
          return;
        }
        const syncResult = this.videoHandler.syncVolumeWrapper(
          "translation",
          nextVolume
        );
        if (typeof syncResult?.nextVideo === "number") {
          this.videoHandler.applyManualVideoVolumeOverride(
            syncResult.nextVideo / 100
          );
        }
      }).addEventListener("select:subtitles", (data) => {
        if (!this.videoHandler) {
          return;
        }
        this.runDetached(
          this.videoHandler.changeSubtitlesLang(data),
          "Failed to change subtitles language"
        );
      });
    }
    snapshotDriveSelectItems(select) {
      const rawItems = Array.isArray(select?._items) ? select._items : [];
      return rawItems.map((item) => ({
        label: String(item?.label ?? ""),
        value: String(item?.value ?? ""),
        selected: item?.selected === true,
        disabled: item?.disabled === true
      }));
    }
    getDriveQuickMenuState() {
      const overlayView = this.votOverlayView;
      if (!overlayView?.isInitialized()) {
        return null;
      }
      const fromSelect = overlayView.languagePairSelect?.fromSelect;
      const toSelect = overlayView.languagePairSelect?.toSelect;
      const subtitlesSelect2 = overlayView.subtitlesSelect;
      const videoVolumeSlider2 = overlayView.videoVolumeSlider;
      const translationVolumeSlider2 = overlayView.translationVolumeSlider;
      return {
        fromItems: this.snapshotDriveSelectItems(fromSelect),
        toItems: this.snapshotDriveSelectItems(toSelect),
        subtitlesItems: this.snapshotDriveSelectItems(subtitlesSelect2),
        videoVolume: Math.round(
          videoVolumeSlider2?.value ?? (this.videoHandler?.getVideoVolume?.() ?? 1) * 100
        ),
        translationVolume: Math.round(
          translationVolumeSlider2?.value ?? this.data.defaultVolume ?? 100
        ),
        translationVolumeMax: translationVolumeSlider2?.max ?? (this.data.audioBooster ? maxAudioVolume : 100),
        showVideoSlider: !videoVolumeSlider2?.hidden && Boolean(this.data.showVideoSlider)
      };
    }
    getDriveVoiceState() {
      const configuredMode = this.data.useLivelyVoice ? "lively" : "standard";
      const actualMode = this.videoHandler?.activeVoiceMode;
      const displayedMode = this.videoHandler?.hasActiveSource() && actualMode ? actualMode : configuredMode;
      const buttonStatus = this.votOverlayView?.votButton?.status ?? "none";
      const hasSuccessfulStatus = buttonStatus === "success";
      const isLoading = !hasSuccessfulStatus && (this.votOverlayView?.votButton?.loading === true || Boolean(this.videoHandler?.hadAsyncWait));
      if (isLoading) {
        return {
          voiceMode: displayedMode,
          voicePlaybackState: "loading",
          loading: true
        };
      }
      if (!this.videoHandler?.hasActiveSource()) {
        return {
          voiceMode: displayedMode,
          voicePlaybackState: "idle",
          loading: false
        };
      }
      if (buttonStatus === "error" || buttonStatus === "disabled") {
        return {
          voiceMode: displayedMode,
          voicePlaybackState: "idle",
          loading: false
        };
      }
      return {
        voiceMode: displayedMode,
        voicePlaybackState: this.videoHandler.video?.paused ? "paused" : "playing",
        loading: false
      };
    }
    isTranslationBusy() {
      return Boolean(
        this.translationActionInFlight || this.votOverlayView?.votButton?.loading === true || this.videoHandler?.hadAsyncWait
      );
    }
    async waitForTranslationActionSettled(timeoutMs = 2e3) {
      const startedAt = Date.now();
      while (this.isTranslationBusy()) {
        if (Date.now() - startedAt >= timeoutMs) {
          break;
        }
        await new Promise((resolve) => setTimeout(resolve, 25));
      }
    }
    async startTranslationFlow(videoHandler) {
      debug.log("[voice-menu] startTranslationFlow called", {
        status: this.votOverlayView?.votButton.status,
        loading: this.votOverlayView?.votButton.loading ?? false,
        hasActiveSource: videoHandler.hasActiveSource(),
        activeTranslation: Boolean(videoHandler.activeTranslation)
      });
      const sourceAudioState = videoHandler.syncSourceAudioAvailabilityUi({
        forceVisible: true
      });
      if (!sourceAudioState.ready) {
        debug.warn("[voice-menu] startTranslationFlow early return reason", {
          reason: "source-audio-not-ready",
          state: sourceAudioState
        });
        return;
      }
      if (this.votOverlayView.votButton.status === "disabled") {
        this.transformBtn("none", localizationProvider.get("translateVideo"));
      }
      if (this.votOverlayView.votButton.status === "error") {
        this.transformBtn("none", localizationProvider.get("translateVideo"));
      } else if (this.votOverlayView.votButton.status !== "disabled" && this.votOverlayView.votButton.status !== "none" && !videoHandler.hasActiveSource()) {
        debug.log("[startTranslationFlow] reset stale button state");
        this.transformBtn("none", localizationProvider.get("translateVideo"));
      }
      debug.log("[startTranslationFlow] trying execute translation");
      await videoHandler.primePlaybackByGesture("translate-button");
      const videoData2 = await this.getVideoDataForTranslation(videoHandler);
      await videoHandler.videoManager.ensureDetectedLanguageForTranslation(
        videoData2
      );
      debug.log("[startTranslationFlow] Run translateFunc", videoData2.videoId);
      try {
        const requestLang = videoHandler.translateFromLang === "auto" ? videoData2.detectedLanguage : videoHandler.translateFromLang;
        await videoHandler.translateFunc(
          videoData2.videoId,
          videoData2.isStream,
          requestLang,
          videoData2.responseLanguage,
          videoData2.translationHelp
        );
        debug.log("[voice-menu] translation started", {
          videoId: videoData2.videoId,
          hasActiveSource: videoHandler.hasActiveSource(),
          loading: this.votOverlayView?.votButton.loading ?? false,
          activeTranslation: Boolean(videoHandler.activeTranslation)
        });
      } catch (err) {
        debug.warn("[voice-menu] translation failed", err);
        throw err;
      }
    }
    async applyVoiceModeSelection(previousMode, nextMode, options = {}) {
      const videoHandler = this.videoHandler;
      if (!videoHandler) {
        return;
      }
      const { startWhenIdle = false } = options;
      const hasActiveSource = videoHandler.hasActiveSource();
      const isBusy = this.isTranslationBusy();
      debug.log("[voice-menu] applyVoiceModeSelection called", {
        previousMode,
        nextMode,
        startWhenIdle,
        hasActiveSource,
        isBusy,
        loading: this.votOverlayView?.votButton.loading ?? false,
        translationActionInFlight: this.translationActionInFlight
      });
      if (!hasActiveSource && !isBusy && !startWhenIdle) {
        debug.warn("[voice-menu] startTranslationFlow early return reason", {
          reason: "idle-and-startWhenIdle-disabled"
        });
        return;
      }
      if (previousMode === nextMode && hasActiveSource && !isBusy) {
        debug.warn("[voice-menu] startTranslationFlow early return reason", {
          reason: "same-mode-while-translation-active"
        });
        return;
      }
      try {
        await videoHandler.primePlaybackByGesture("voice-mode-selection");
      } catch (err) {
        debug.warn(
          "[VOT] Failed to prime playback before voice mode switch",
          err
        );
      }
      if (hasActiveSource || isBusy) {
        try {
          await videoHandler.stopTranslation();
          await videoHandler.waitForPendingStopTranslate();
          await this.waitForTranslationActionSettled();
        } catch (err) {
          debug.warn(
            "[VOT] Failed to stop translation before voice mode restart",
            err
          );
        }
      }
      if (videoHandler.hasActiveSource()) {
        debug.warn("[voice-menu] startTranslationFlow early return reason", {
          reason: "active-source-still-present-after-stop"
        });
        return;
      }
      if (this.translationActionInFlight) {
        await this.waitForTranslationActionSettled();
      }
      if (this.translationActionInFlight) {
        debug.warn("[voice-menu] startTranslationFlow early return reason", {
          reason: "translation-action-still-in-flight"
        });
        return;
      }
      this.translationActionInFlight = true;
      try {
        await this.startTranslationFlow(videoHandler);
      } finally {
        this.translationActionInFlight = false;
      }
    }
    async restartDriveTranslationIfActive() {
      const videoHandler = this.videoHandler;
      if (!videoHandler || !this.votOverlayView?.isInitialized()) {
        return;
      }
      if (!videoHandler.hasActiveSource()) {
        return;
      }
      try {
        await videoHandler.stopTranslation();
        await videoHandler.waitForPendingStopTranslate();
        await this.waitForTranslationActionSettled();
        await this.handleTranslationBtnClick();
      } catch (err) {
        debug.warn("[VOT] Failed to restart translation after Drive change", err);
      }
    }
    async applyDriveFromLanguage(value) {
      if (!this.videoHandler) {
        return;
      }
      this.videoHandler.translateFromLang = value;
      if (this.videoHandler.videoData) {
        this.videoHandler.videoManager.rememberUserLanguageSelection(
          this.videoHandler.videoData.videoId,
          value
        );
      }
      this.videoHandler.setSelectMenuValues(
        value,
        this.videoHandler.videoData?.responseLanguage ?? this.videoHandler.translateToLang
      );
      await this.videoHandler.ensureSubtitlesForCurrentLangPair?.();
      await this.videoHandler.updateSubtitlesLangSelect?.();
      await this.restartDriveTranslationIfActive();
    }
    async applyDriveToLanguage(value) {
      if (!this.videoHandler) {
        return;
      }
      this.data.responseLanguage = value;
      void votStorage.set("responseLanguage", value);
      this.videoHandler.setSelectMenuValues(
        this.videoHandler.videoData?.detectedLanguage ?? this.videoHandler.translateFromLang,
        value
      );
      await this.videoHandler.ensureSubtitlesForCurrentLangPair?.();
      await this.videoHandler.updateSubtitlesLangSelect?.();
      await this.restartDriveTranslationIfActive();
    }
    async applyDriveSubtitles(value) {
      if (!this.videoHandler) {
        return;
      }
      await this.videoHandler.changeSubtitlesLang(value);
    }
    applyDriveVideoVolume(volume) {
      if (!this.videoHandler) {
        return;
      }
      const overlayView = this.votOverlayView;
      const nextVolume = clamp$2(Math.round(volume), 0, 100);
      if (overlayView?.isInitialized() && overlayView.videoVolumeSlider) {
        overlayView.videoVolumeSlider.value = nextVolume;
      }
      this.videoHandler.setVideoVolume(nextVolume / 100);
      if (!this.data.syncVolume) {
        this.videoHandler.onVideoVolumeSliderSynced(nextVolume);
        return;
      }
      this.videoHandler.syncVolumeWrapper("video", nextVolume);
    }
    applyDriveTranslationVolume(volume) {
      if (!this.videoHandler) {
        return;
      }
      const overlayView = this.votOverlayView;
      const maxVolume = overlayView?.isInitialized() && overlayView.translationVolumeSlider ? overlayView.translationVolumeSlider.max : this.data.audioBooster ? maxAudioVolume : 100;
      const nextVolume = clamp$2(Math.round(volume), 0, maxVolume);
      if (overlayView?.isInitialized() && overlayView.translationVolumeSlider) {
        overlayView.translationVolumeSlider.value = nextVolume;
      } else if (this.data.defaultVolume !== nextVolume) {
        this.data.defaultVolume = nextVolume;
        void votStorage.set("defaultVolume", nextVolume);
      }
      this.videoHandler.syncTranslationPlaybackVolume();
      if (!this.data.syncVolume) {
        this.videoHandler.onTranslationVolumeSliderSynced(nextVolume);
        return;
      }
      const syncResult = this.videoHandler.syncVolumeWrapper(
        "translation",
        nextVolume
      );
      if (typeof syncResult?.nextVideo === "number") {
        this.videoHandler.applyManualVideoVolumeOverride(
          syncResult.nextVideo / 100
        );
      }
    }
    applyDriveSyncVolume(enabled) {
      if (!this.videoHandler) {
        return;
      }
      this.data.syncVolume = enabled;
      this.videoHandler.setupAudioSettings();
      if (!enabled) {
        return;
      }
      this.withInitializedOverlayView((overlayView) => {
        const videoSlider = overlayView.videoVolumeSlider;
        const translationSlider = overlayView.translationVolumeSlider;
        if (!videoSlider || !translationSlider) {
          return;
        }
        this.videoHandler.syncTranslationPlaybackVolume();
        this.videoHandler.resetVolumeLinkState(
          Number(videoSlider.value),
          Number(translationSlider.value)
        );
      });
    }
    applyDriveShowVideoSlider(checked) {
      this.data.showVideoSlider = checked;
      this.withInitializedOverlayView((overlayView) => {
        if (!overlayView.videoVolumeSlider || !overlayView.votButton) {
          return;
        }
        overlayView.videoVolumeSlider.container.hidden = !this.data.showVideoSlider || overlayView.votButton.status !== "success";
      });
    }
    applyDriveAudioBooster(enabled) {
      this.data.audioBooster = enabled;
      this.withInitializedOverlayView((overlayView) => {
        if (!overlayView.translationVolumeSlider) {
          return;
        }
        const currentVolume = overlayView.translationVolumeSlider.value;
        const maxVolume = this.data.audioBooster ? maxAudioVolume : 100;
        overlayView.translationVolumeSlider.max = maxVolume;
        const nextVolume = clamp$2(currentVolume, 0, maxVolume);
        overlayView.translationVolumeSlider.value = nextVolume;
        this.videoHandler?.onTranslationVolumeSliderSynced(nextVolume);
        this.videoHandler?.syncTranslationPlaybackVolume();
      });
    }
    async applyDriveUseLivelyVoice(enabled) {
      const previousMode = this.data.useLivelyVoice ? "lively" : "standard";
      const nextMode = enabled ? "lively" : "standard";
      this.data.useLivelyVoice = enabled;
      if (!this.videoHandler) {
        return;
      }
      if (!this.videoHandler.hasActiveSource() && !this.isTranslationBusy()) {
        return;
      }
      await this.applyVoiceModeSelection(previousMode, nextMode);
    }
    bindSettingsViewEvents() {
      const settingsView2 = this.votSettingsView;
      if (!settingsView2) {
        return;
      }
      settingsView2.addEventListener("update:account", async (account) => {
        if (!this.videoHandler) {
          return;
        }
        this.videoHandler.votClient.apiToken = account?.token;
      }).addEventListener("change:autoTranslate", async (checked) => {
        const videoHandler = this.videoHandler;
        if (checked && videoHandler && !videoHandler.hasActiveSource()) {
          await this.handleTranslationBtnClick();
        }
      }).addEventListener("change:autoSubtitles", async (checked) => {
        if (!checked || !this.videoHandler?.videoData?.videoId) {
          return;
        }
        await this.videoHandler.enableSubtitlesForCurrentLangPair();
      }).addEventListener("change:showVideoVolume", () => {
        this.withInitializedOverlayView((overlayView) => {
          if (!overlayView.videoVolumeSlider || !overlayView.votButton) {
            return;
          }
          overlayView.videoVolumeSlider.container.hidden = !this.data.showVideoSlider || overlayView.votButton.status !== "success";
        });
      }).addEventListener("change:audioBooster", async () => {
        this.withInitializedOverlayView((overlayView) => {
          if (!overlayView.translationVolumeSlider) {
            return;
          }
          const currentVolume = overlayView.translationVolumeSlider.value;
          const maxVolume = this.data.audioBooster ? maxAudioVolume : 100;
          overlayView.translationVolumeSlider.max = maxVolume;
          const nextVolume = clamp$2(currentVolume, 0, maxVolume);
          overlayView.translationVolumeSlider.value = nextVolume;
          this.videoHandler?.onTranslationVolumeSliderSynced(nextVolume);
          this.videoHandler?.syncTranslationPlaybackVolume();
        });
      }).addEventListener("change:syncVolume", (checked) => {
        if (!this.videoHandler) {
          return;
        }
        this.videoHandler.setupAudioSettings();
        if (!checked) {
          return;
        }
        this.withInitializedOverlayView((overlayView) => {
          const videoSlider = overlayView.videoVolumeSlider;
          const translationSlider = overlayView.translationVolumeSlider;
          if (!videoSlider || !translationSlider) {
            return;
          }
          this.videoHandler.syncTranslationPlaybackVolume();
          this.videoHandler.resetVolumeLinkState(
            Number(videoSlider.value),
            Number(translationSlider.value)
          );
        });
      }).addEventListener("change:useLivelyVoice", (checked) => {
        if (!this.videoHandler) {
          return;
        }
        this.votOverlayView?.syncVoiceModeUi();
        const nextMode = checked ? "lively" : "standard";
        const previousMode = checked ? "standard" : "lively";
        this.runDetached(
          this.applyVoiceModeSelection(previousMode, nextMode),
          "Failed to apply voice mode change"
        );
      }).addEventListener("change:subtitlesHighlightWords", (checked) => {
        this.updateSubtitlesWidgetSetting(
          checked,
          this.data.highlightWords,
          (widget, value) => {
            widget.setHighlightWords(value);
          }
        );
      }).addEventListener("change:subtitlesSmartLayout", (checked) => {
        this.updateSubtitlesWidgetSetting(
          checked,
          this.data.subtitlesSmartLayout,
          (widget, value) => {
            widget.setSmartLayout(value);
          }
        );
      }).addEventListener("input:subtitlesMaxLength", (value) => {
        this.updateSubtitlesWidgetSetting(
          value,
          this.data.subtitlesMaxLength,
          (widget, nextValue) => {
            widget.setMaxLength(nextValue);
          }
        );
      }).addEventListener("input:subtitlesFontSize", (value) => {
        this.updateSubtitlesWidgetSetting(
          value,
          this.data.subtitlesFontSize,
          (widget, nextValue) => {
            widget.setFontSize(nextValue);
          }
        );
      }).addEventListener("select:subtitlesFontFamily", (item) => {
        this.updateSubtitlesWidgetSetting(
          item,
          this.data.subtitlesFontFamily,
          (widget, nextValue) => {
            widget.setFontFamily(nextValue);
          }
        );
      }).addEventListener("input:subtitlesBackgroundOpacity", (value) => {
        this.updateSubtitlesWidgetSetting(
          value,
          this.data.subtitlesOpacity,
          (widget, nextValue) => {
            widget.setOpacity(nextValue);
          }
        );
      }).addEventListener("change:proxyWorkerHost", (_value) => {
        if (!this.videoHandler) {
          return;
        }
        this.runDetached(
          this.videoHandler.handleProxySettingsChanged("proxyWorkerHost"),
          "Failed to apply proxyWorkerHost change"
        );
      }).addEventListener("select:proxyTranslationStatus", () => {
        if (!this.videoHandler) {
          return;
        }
        this.runDetached(
          this.videoHandler.handleProxySettingsChanged(
            "proxyTranslationStatus"
          ),
          "Failed to apply proxyTranslationStatus change"
        );
      }).addEventListener("change:useNewAudioPlayer", () => {
        this.restartAudioPlayer();
      }).addEventListener("change:onlyBypassMediaCSP", () => {
        this.restartAudioPlayer();
      }).addEventListener("select:translationTextService", () => {
        this.withSubtitlesWidget((widget) => {
          widget.resetTranslationContext(true);
        });
      }).addEventListener("change:showPiPButton", () => {
        this.withInitializedOverlayView((overlayView) => {
          if (!overlayView.votButton) {
            return;
          }
          overlayView.votButton.showPiPButton(overlayView.pipButtonVisible);
        });
      }).addEventListener("select:buttonPosition", (item) => {
        this.withInitializedOverlayView((overlayView) => {
          const preferredPosition = this.data.buttonPos ?? item;
          const { position: position2, direction } = overlayView.calcButtonLayout(preferredPosition);
          overlayView.updateButtonLayout(position2, direction);
          overlayView.syncVoiceModeUi();
        });
      }).addEventListener("select:menuLanguage", async () => {
        await this.reloadMenu();
      }).addEventListener("click:bugReport", () => {
        if (!this.videoHandler) {
          return;
        }
        const params = new URLSearchParams(
          this.videoHandler.collectReportInfo()
        ).toString();
        globalThis.open(`${repositoryUrl}/issues/new?${params}`, "_blank")?.focus();
      }).addEventListener("click:resetSettings", async () => {
        const valuesForClear = await votStorage.list();
        await Promise.all(valuesForClear.map((key) => votStorage.delete(key)));
        await votStorage.set("compatVersion", actualCompatVersion);
        globalThis.location.reload();
      });
    }
    async downloadCurrentSubtitles() {
      await this.handleDownloadSubtitlesClick();
    }
    async handleDownloadTranslationClick() {
      const overlayView = this.votOverlayView;
      const videoHandler = this.videoHandler;
      if (!overlayView?.isInitialized() || !videoHandler?.downloadTranslationUrl || !videoHandler.videoData) {
        return;
      }
      const downloadButton = overlayView.downloadTranslationButton;
      const downloadUrl = videoHandler.downloadTranslationUrl;
      const filename = this.data.downloadWithName ? clearFileName(videoHandler.getDownloadBaseName()) : `translation_${videoHandler.videoData.videoId}`;
      const isMobile = this.isLikelyMobileDownloadContext();
      const saveOptions = { preferShare: isMobile };
      const setProgress = (progress) => {
        if (downloadButton) {
          downloadButton.progress = progress;
        }
      };
      setProgress(0);
      try {
        await this.downloadTranslationAudio(
          downloadUrl,
          filename,
          setProgress,
          saveOptions
        );
      } catch (err) {
        console.error("[VOT] Download translation failed:", err);
        if (!this.triggerUrlDownload(downloadUrl, `${filename}.mp3`)) {
          globalThis.open(downloadUrl, "_blank")?.focus();
        }
      } finally {
        setProgress(0);
      }
    }
    async downloadTranslationAudio(downloadUrl, filename, onProgress, saveOptions) {
      const response = await GM_fetch(downloadUrl, { timeout: 0 });
      if (!response.ok) {
        throw new Error(`HTTP ${response.status}`);
      }
      await downloadTranslation(response, filename, onProgress, saveOptions);
    }
    async handleDownloadSubtitlesClick() {
      const videoHandler = this.videoHandler;
      if (!videoHandler?.yandexSubtitles || !videoHandler.videoData) {
        return;
      }
      const subsFormat = this.data.subtitlesDownloadFormat ?? "json";
      const subsContent = serializeProcessedSubtitles(
        videoHandler.yandexSubtitles,
        subsFormat,
        {
          assTitle: videoHandler.videoData.localizedTitle ?? videoHandler.videoData.title ?? videoHandler.videoData.downloadTitle
        }
      );
      const blob = new Blob(
        [
          subsFormat === "json" ? JSON.stringify(subsContent) : subsContent
        ],
        {
          type: "text/plain"
        }
      );
      const filename = this.data.downloadWithName ? clearFileName(videoHandler.getDownloadBaseName()) : `subtitles_${videoHandler.videoData.videoId}`;
      const targetFilename = `${filename}.${subsFormat}`;
      const isMobile = this.isLikelyMobileDownloadContext();
      const saveOptions = { preferShare: isMobile };
      await downloadBlob(blob, targetFilename, saveOptions);
    }
    async reloadMenu() {
      if (!this.votOverlayView?.isInitialized()) {
        throw new Error("[VOT] OverlayView isn't initialized");
      }
      const prevButtonOpacity = this.votOverlayView.votButton.opacity;
      const prevButtonHidden = this.votOverlayView.votButton.container.hidden;
      const prevMenuHidden = this.votOverlayView.votMenu.hidden;
      const prevButtonPos = this.data.buttonPos ?? "default";
      const settingsWasOpen = this.votSettingsView?.dialog?.container?.hidden === false;
      await this.videoHandler?.stopTranslation();
      this.release();
      this.initUI();
      this.initUIEvents();
      if (!this.videoHandler) {
        return this;
      }
      try {
        const { position: position2, direction } = this.votOverlayView.calcButtonLayout(prevButtonPos);
        this.votOverlayView.updateButtonLayout(position2, direction);
        this.votOverlayView.votMenu.hidden = prevMenuHidden;
        this.votOverlayView.votButton.container.hidden = prevButtonHidden;
        this.votOverlayView.votButton.opacity = prevButtonOpacity;
        this.votOverlayView.syncVoiceModeUi();
      } catch (err) {
        debug.warn(
          "[VOT] Failed to restore overlay state after menu reload",
          err
        );
      }
      try {
        this.videoHandler.rebindOverlayVisibilityTargets();
      } catch (err) {
        debug.warn("[VOT] Failed to rebind overlay visibility targets", err);
      }
      if (settingsWasOpen) {
        try {
          this.votSettingsView?.open();
        } catch (err) {
          debug.warn("[VOT] Failed to reopen settings after menu reload", err);
        }
      }
      await this.videoHandler.updateSubtitlesLangSelect();
      const widget = this.videoHandler.subtitlesWidget;
      if (widget) {
        widget.resetTranslationContext(true);
      }
      return this;
    }
    async handleTranslationBtnClick() {
      if (!this.votOverlayView?.isInitialized()) {
        throw new Error("[VOT] OverlayView isn't initialized");
      }
      const videoHandler = this.videoHandler;
      if (!videoHandler) {
        return this;
      }
      debug.log("[handleTranslationBtnClick] click translationBtn");
      if (videoHandler.isAwaitingAutoplayRecovery()) {
        debug.log("[handleTranslationBtnClick] resume pending autoplay recovery");
        await videoHandler.resumePendingAutoplayRecovery("button");
        return this;
      }
      if (videoHandler.hasActiveSource()) {
        debug.log("[handleTranslationBtnClick] stop active translation");
        await videoHandler.stopTranslation();
        return this;
      }
      if (this.translationActionInFlight || this.votOverlayView.votButton.loading) {
        debug.log("[handleTranslationBtnClick] ignore re-entry while loading");
        return this;
      }
      this.translationActionInFlight = true;
      try {
        await this.startTranslationFlow(videoHandler);
      } catch (err) {
        if (this.isAbortError(err)) {
          this.transformBtn("none", localizationProvider.get("translateVideo"));
          return this;
        }
        console.error("[VOT]", err);
        if (!(err instanceof Error)) {
          this.transformBtn("error", String(err));
          return this;
        }
        const message = err.name === "VOTLocalizedError" ? err.localizedMessage : err.message;
        this.transformBtn("error", message);
      } finally {
        this.translationActionInFlight = false;
      }
      return this;
    }
    async getVideoDataForTranslation(videoHandler) {
      if (!videoHandler.videoData?.videoId) {
        videoHandler.videoData = await videoHandler.getVideoData();
      }
      if (this.shouldRefreshVideoDataBeforeTranslation(videoHandler)) {
        videoHandler.videoData = await videoHandler.getVideoData();
      }
      if (!videoHandler.videoData?.videoId) {
        throw new VOTLocalizedError("VOTNoVideoIDFound");
      }
      return videoHandler.videoData;
    }
    shouldRefreshVideoDataBeforeTranslation(videoHandler) {
      return videoHandler.site.host === "vk" && videoHandler.site.additionalData === "clips" || videoHandler.site.host === "douyin";
    }
    isAbortError(error2) {
      return error2 instanceof Error && error2.name === "AbortError";
    }
    isLoadingText(text) {
      const delayed = localizationProvider.get("TranslationDelayed");
      return typeof text === "string" && (text.includes(localizationProvider.get("translationTake")) || (delayed ? text.includes(delayed) : false));
    }
    transformBtn(status, text) {
      if (!this.votOverlayView?.isInitialized()) {
        throw new Error("[VOT] OverlayView isn't initialized");
      }
      this.votOverlayView.votButton.status = status;
      this.votOverlayView.votButton.loading = status === "error" && this.isLoadingText(text);
      this.votOverlayView.votButton.setText(text);
      this.votOverlayView.votButtonTooltip.setContent(text);
      this.votOverlayView.syncVoiceModeUi();
      dispatchGoogleDriveBridgeSync();
      return this;
    }
    release() {
      if (!this.isInitialized()) {
        return this;
      }
      if (/^(m|music)\.youtube\.com$/i.test(
        String(globalThis.location.hostname || "")
      )) {
        console.log("[VOT][mobile-overlay][ui] UIManager release");
      }
      this.votOverlayView?.release();
      this.votSettingsView?.release();
      this.votGlobalPortal?.remove();
      this.initialized = false;
      return this;
    }
    withInitializedOverlayView(callback) {
      if (!this.votOverlayView?.isInitialized()) {
        return;
      }
      callback(this.votOverlayView);
    }
    withSubtitlesWidget(callback) {
      const widget = this.videoHandler?.subtitlesWidget;
      if (!widget) {
        return;
      }
      callback(widget);
    }
    updateSubtitlesWidgetSetting(nextValue, storedValue, apply) {
      this.withSubtitlesWidget((widget) => {
        apply(widget, storedValue ?? nextValue);
      });
    }
    runDetached(task, errorMessage) {
      void task.catch((err) => {
        debug.warn(`[VOT] ${errorMessage}`, err);
      });
    }
    triggerUrlDownload(url, filename) {
      try {
        const a2 = document.createElement("a");
        a2.href = url;
        a2.download = filename;
        a2.target = "_blank";
        a2.rel = "noopener noreferrer";
        a2.style.display = "none";
        document.body.appendChild(a2);
        a2.click();
        a2.remove();
        return true;
      } catch {
        return false;
      }
    }
    isLikelyMobileDownloadContext() {
      if (this.videoHandler?.site.additionalData === "mobile" || this.videoHandler?.site.additionalData === "music") {
        return true;
      }
      return typeof matchMedia === "function" && matchMedia("(pointer: coarse)").matches;
    }
    restartAudioPlayer() {
      void this.restartAudioPlayerSafely();
    }
    async restartAudioPlayerSafely() {
      const videoHandler = this.videoHandler;
      if (!videoHandler) {
        return;
      }
      try {
        await videoHandler.stopTranslate();
        videoHandler.createPlayer();
      } catch (err) {
        debug.warn("[VOT] Failed to restart audio player", err);
      }
    }
  }
  class OverlayVisibilityController {
    deps;
    hideDeadlineMs = 0;
    hideArmed = false;
    unsubscribeChecker;
    constructor(deps) {
      this.deps = deps;
      this.unsubscribeChecker = this.deps.checker.subscribe(() => {
        this.onCheckerTick();
      });
    }
show() {
      const view = this.getView();
      if (!view) {
        return null;
      }
      view.updateButtonOpacity(1);
      return view;
    }
cancel() {
      this.hideDeadlineMs = 0;
      this.hideArmed = false;
    }
    release() {
      this.cancel();
      this.unsubscribeChecker();
    }
queueAutoHide() {
      if (!this.show()) {
        return;
      }
      if (!this.shouldAutoHide()) {
        this.cancel();
        return;
      }
      const delay = this.deps.getAutoHideDelay();
      this.hideDeadlineMs = this.nowMs() + Math.max(0, delay);
      this.hideArmed = true;
      this.deps.checker.markActivity("overlay-queue-hide");
      this.deps.checker.requestImmediateTick();
    }
handleOverlayInteraction(event) {
      const type = event?.type;
      if (!type) return;
      if (type === "focusin") {
        this.handleFocusIn();
        return;
      }
      if (type.startsWith("pointer")) {
        this.cancel();
        this.show();
        this.deps.checker.markActivity("overlay-interaction");
        event.stopPropagation?.();
        return;
      }
      this.handleHostInteraction(event);
    }
handleHostInteraction(event) {
      const type = event?.type;
      if (!type) return;
      if (type === "focusin") {
        this.handleFocusIn();
        return;
      }
      if (type.startsWith("pointer")) {
        const target = event.target;
        if (this.deps.isInteractiveNode(target)) {
          event.stopPropagation?.();
        }
        this.deps.checker.markActivity("overlay-host-pointer");
      }
      this.queueAutoHide();
    }
scheduleHide(event) {
      if (!this.getView()) {
        return;
      }
      const currentTarget = event?.currentTarget;
      let relatedTarget = event?.relatedTarget ?? null;
      if (!relatedTarget && typeof event?.composedPath === "function") {
        const path = event.composedPath();
        relatedTarget = path[1] ?? null;
      }
      const relatedNode = relatedTarget instanceof Node ? relatedTarget : null;
      const currentNode = currentTarget instanceof Node ? currentTarget : null;
      if (relatedNode && (currentNode?.contains(relatedNode) || this.deps.isInteractiveNode(relatedNode))) {
        return;
      }
      this.queueAutoHide();
    }
    onCheckerTick() {
      if (!this.shouldAutoHide()) {
        this.cancel();
        this.show();
        return;
      }
      if (!this.hideArmed || this.hideDeadlineMs <= 0) return;
      const now2 = this.nowMs();
      if (now2 + 2 < this.hideDeadlineMs) {
        return;
      }
      this.hideArmed = false;
      let active = null;
      const canCheckFocus = typeof document !== "undefined" && typeof document.hasFocus === "function";
      if (canCheckFocus && document.hasFocus()) {
        active = document.activeElement;
      }
      if (active && this.deps.isInteractiveNode(active)) {
        debug.log("[OverlayVisibility] skip hide (focus inside overlay)");
        return;
      }
      const view = this.getView();
      view?.updateButtonOpacity(0);
    }
    handleFocusIn() {
      this.cancel();
      this.show();
      this.deps.checker.markActivity("overlay-focus-in");
    }
    getView() {
      const view = this.deps.getOverlayView();
      return view?.isInitialized() ? view : null;
    }
    nowMs() {
      if (this.deps.nowMs) {
        return this.deps.nowMs();
      }
      return typeof performance !== "undefined" && typeof performance.now === "function" ? performance.now() : Date.now();
    }
    shouldAutoHide() {
      return this.deps.shouldAutoHide ? this.deps.shouldAutoHide() : true;
    }
  }
  const DEFAULT_PROFILE = {
    checkIntervalMs: 250,
    idleAfterMs: 180
  };
  function normalizePositiveMs(value, fallback) {
    if (typeof value !== "number" || !Number.isFinite(value)) {
      return fallback;
    }
    return Math.max(1, Math.trunc(value));
  }
  function normalizeNonNegativeMs(value, fallback) {
    if (typeof value !== "number" || !Number.isFinite(value)) {
      return fallback;
    }
    return Math.max(0, Math.trunc(value));
  }
  function normalizeProfile(profile = {}) {
    return {
      checkIntervalMs: normalizePositiveMs(
        profile.checkIntervalMs,
        DEFAULT_PROFILE.checkIntervalMs
      ),
      idleAfterMs: normalizeNonNegativeMs(
        profile.idleAfterMs,
        DEFAULT_PROFILE.idleAfterMs
      )
    };
  }
  function getDefaultRuntime() {
    return {
      nowMs: () => typeof performance !== "undefined" && typeof performance.now === "function" ? performance.now() : Date.now(),
      setInterval: globalThis.setInterval.bind(globalThis),
      clearInterval: globalThis.clearInterval.bind(globalThis),
      queueMicrotask: (fn) => {
        if (typeof globalThis.queueMicrotask === "function") {
          globalThis.queueMicrotask(fn);
          return;
        }
        Promise.resolve().then(fn);
      },
      isDocumentHidden: () => typeof document !== "undefined" && typeof document.hidden === "boolean" ? document.hidden : false,
      onVisibilityChange: (listener) => {
        if (typeof document === "undefined" || typeof document.addEventListener !== "function") {
          return () => void 0;
        }
        document.addEventListener("visibilitychange", listener);
        return () => {
          if (typeof document.removeEventListener === "function") {
            document.removeEventListener("visibilitychange", listener);
          }
        };
      }
    };
  }
  class IntervalIdleChecker {
    profile;
    runtime;
    subscribers = new Set();
    intervalId = null;
    unsubscribeVisibilityChange = null;
    running = false;
    destroyed = false;
    immediateQueued = false;
    currentMode = "active";
    lastActivityAt;
    onVisibilityChangeHandler = () => {
      if (this.destroyed || !this.running) return;
      if (this.runtime.isDocumentHidden()) {
        this.clearIntervalTimer();
      } else {
        this.armInterval();
      }
      this.requestImmediateTick();
    };
    constructor(options = {}) {
      this.profile = normalizeProfile(options.profile);
      this.runtime = {
        ...getDefaultRuntime(),
        ...options.runtime
      };
      this.lastActivityAt = this.runtime.nowMs();
    }
    start() {
      if (this.destroyed || this.running) return;
      this.running = true;
      this.lastActivityAt = this.runtime.nowMs();
      this.subscribeVisibilityChange();
      this.armInterval();
      this.runTick("start");
    }
    stop() {
      if (!this.running) return;
      this.running = false;
      this.clearIntervalTimer();
      this.immediateQueued = false;
      this.unsubscribeFromVisibilityChange();
    }
    destroy() {
      if (this.destroyed) return;
      this.stop();
      this.subscribers.clear();
      this.destroyed = true;
    }
    subscribe(fn) {
      if (this.destroyed) {
        return () => void 0;
      }
      this.subscribers.add(fn);
      return () => {
        this.subscribers.delete(fn);
      };
    }
    markActivity(_source) {
      if (this.destroyed) return;
      this.lastActivityAt = this.runtime.nowMs();
      if (!this.running) return;
      const nextMode = this.resolveMode(this.lastActivityAt);
      if (nextMode !== this.currentMode) this.currentMode = nextMode;
    }
    requestImmediateTick() {
      if (this.destroyed || !this.running || this.immediateQueued) return;
      this.immediateQueued = true;
      this.runtime.queueMicrotask(() => {
        this.immediateQueued = false;
        if (this.destroyed || !this.running) return;
        this.runTick("immediate");
      });
    }
    resolveMode(nowMs) {
      if (this.runtime.isDocumentHidden()) {
        return "hidden";
      }
      const inactiveFor = nowMs - this.lastActivityAt;
      return inactiveFor >= this.profile.idleAfterMs ? "idle" : "active";
    }
    clearIntervalTimer() {
      if (this.intervalId === null) return;
      this.runtime.clearInterval(this.intervalId);
      this.intervalId = null;
    }
    armInterval() {
      if (this.intervalId !== null) return;
      this.intervalId = this.runtime.setInterval(() => {
        this.runTick("interval");
      }, this.profile.checkIntervalMs);
    }
    runTick(source) {
      if (this.destroyed || !this.running) return;
      if (this.subscribers.size === 0) return;
      const nowMs = this.runtime.nowMs();
      const nextMode = this.resolveMode(nowMs);
      if (nextMode !== this.currentMode) this.currentMode = nextMode;
      const ctx = {
        nowMs,
        mode: nextMode,
        source
      };
      for (const sub of this.subscribers) {
        try {
          sub(ctx);
        } catch {
        }
      }
    }
    subscribeVisibilityChange() {
      if (this.unsubscribeVisibilityChange !== null) return;
      this.unsubscribeVisibilityChange = this.runtime.onVisibilityChange(
        this.onVisibilityChangeHandler
      );
    }
    unsubscribeFromVisibilityChange() {
      if (this.unsubscribeVisibilityChange === null) return;
      this.unsubscribeVisibilityChange();
      this.unsubscribeVisibilityChange = null;
    }
  }
  function createIntervalIdleChecker(profile) {
    return new IntervalIdleChecker({ profile });
  }
  const now = () => Date.now();
  function getScriptTitle() {
    return GM_info?.script?.name || "VOT";
  }
  function safeL10n(key, fallback) {
    try {
      const value = localizationProvider?.get?.(key);
      return value || fallback;
    } catch {
      return fallback;
    }
  }
  function canSend(lastSentAt, key, cooldownMs) {
    if (!cooldownMs) return true;
    const prev = lastSentAt.get(key) ?? 0;
    return now() - prev >= cooldownMs;
  }
  function markSent(lastSentAt, key) {
    lastSentAt.set(key, now());
  }
  function localizePhraseText(message) {
    const key = message.trim();
    if (!key) return null;
    try {
      const localized = localizationProvider.get(key);
      const defaultText = localizationProvider.getDefault(key);
      const isKnownPhrase = localized !== key || defaultText !== key;
      return isKnownPhrase ? localized || defaultText || key : null;
    } catch {
      return null;
    }
  }
  function resolveLocalizedErrorMessage(message) {
    if (message && typeof message === "object") {
      const localizedError = message;
      if (localizedError.name === "VOTLocalizedError") {
        if (typeof localizedError.localizedMessage === "string" && localizedError.localizedMessage.trim()) {
          return localizedError.localizedMessage;
        }
        if (typeof localizedError.unlocalizedMessage === "string") {
          const byPhraseKey = localizePhraseText(
            localizedError.unlocalizedMessage
          );
          if (byPhraseKey) return byPhraseKey;
        }
      }
    }
    if (typeof message === "string") {
      const byPhraseKey = localizePhraseText(message);
      if (byPhraseKey) return byPhraseKey;
    }
    const extracted = getErrorMessage(message);
    if (extracted) {
      const byPhraseKey = localizePhraseText(extracted);
      return byPhraseKey || extracted;
    }
    return safeL10n("requestTranslationFailed", "Translation failed");
  }
  function trySendViaUserscriptApi(details) {
    try {
      if (typeof GM_notification === "function") {
        GM_notification(details);
        return true;
      }
      const gmApi = globalThis.GM;
      if (gmApi !== void 0 && typeof gmApi.notification === "function") {
        const gmDetails = {
          text: details.text,
          title: details.title,
          image: details.image,
          onclick: details.onclick,
          ondone: details.ondone
        };
        gmApi.notification(gmDetails);
        return true;
      }
    } catch (err) {
      debug.log("[notify] userscript api error", err);
    }
    return false;
  }
  class Notifier {
    lastSentAt = new Map();
    send(details, opts = {}) {
      try {
        const key = opts.key || details.tag || `${details.title ?? ""}|${details.text ?? ""}`;
        const cooldownMs = opts.cooldownMs ?? 0;
        if (!canSend(this.lastSentAt, key, cooldownMs)) return;
        const normalized = {
          ...details,
          title: details.title ?? getScriptTitle()
        };
        const ok = trySendViaUserscriptApi(normalized);
        if (ok) {
          markSent(this.lastSentAt, key);
        } else {
          debug.log("[notify] unavailable", normalized);
        }
      } catch (err) {
        debug.log("[notify] send error", err);
      }
    }
    translationCompleted(host) {
      const text = safeL10n(
        "VOTTranslationCompletedNotify",
        "The translation on the {0} has been completed!"
      ).replace("{0}", host);
      this.send(
        {
          text,
          title: getScriptTitle(),
          timeout: 5e3,
          silent: true,
          tag: "VOTTranslationCompleted",
          onclick: () => {
            try {
              globalThis.focus();
            } catch {
            }
          }
        },
        { key: `translation_completed_${host}`, cooldownMs: 1e4 }
      );
    }
    translationFailed(params) {
      const { videoId, message } = params;
      if (isAbortError(message)) return;
      const msg = resolveLocalizedErrorMessage(message);
      const title = getScriptTitle();
      this.send(
        {
          text: msg,
          title,
          timeout: 8e3,
          silent: true,
tag: `VOTtranslationFailed_${videoId || "unknown"}`,
          onclick: () => {
            try {
              globalThis.focus();
            } catch {
            }
          }
        },
{ key: `translation_failed_${videoId || "unknown"}`, cooldownMs: 3e4 }
      );
    }
  }
  const AD_ATTRS = ["class", "id", "title"];
  const AD_KEYWORDS = [
    "advertise",
    "advertisement",
    "promo",
    "sponsor",
    "banner",
    "commercial",
    "preroll",
    "midroll",
    "postroll",
    "ad-container",
    "sponsored"
  ];
  const AD_KEYWORD_PATTERN = new RegExp(
    AD_KEYWORDS.map(
      (keyword) => keyword.replaceAll(/[.*+?^${}()|[\]\\]/g, String.raw`\$&`)
    ).join("|")
  );
  const ATTACH_SHADOW_HOOK_KEY = Symbol.for("vot.attachShadowHook");
  function shouldLogMobileOverlay() {
    return /^(m|music)\.youtube\.com$/i.test(
      String(globalThis.location?.hostname || "")
    );
  }
  const DEFAULT_VIDEO_OBSERVER_POLICY = {
    preferProbeBootstrap: false,
    startDomObservationOnEnable: true,
    probeStrategy: "mutation",
    probeSelectors: ["video"],
    probePollIntervalMs: 75,
    probePollTimeoutMs: 4e3,
    stopAfterFirstVideoDetected: false,
    keepWatchingAfterVideoDetected: true,
    observeShadowRoots: true,
    shadowHostSelectors: []
  };
  function getAttachShadowDescriptor() {
    const descriptor = Object.getOwnPropertyDescriptor(
      Element.prototype,
      "attachShadow"
    );
    if (!descriptor || typeof descriptor.value !== "function") {
      return null;
    }
    return descriptor;
  }
  function getOrInstallAttachShadowHook() {
    const g2 = globalThis;
    const existing = g2[ATTACH_SHADOW_HOOK_KEY];
    if (existing?.descriptor && existing.subscribers instanceof Set) {
      return existing;
    }
    const descriptor = getAttachShadowDescriptor();
    if (!descriptor) return null;
    const original = descriptor.value;
    const state = {
      descriptor,
      subscribers: new Set()
    };
    const patchedAttachShadow = function(init2) {
      const root = original.call(this, init2);
      for (const sub of state.subscribers) {
        try {
          sub(root);
        } catch (error2) {
          debug.error("attachShadow subscriber failed", error2);
        }
      }
      return root;
    };
    try {
      Object.defineProperty(Element.prototype, "attachShadow", {
        ...descriptor,
        value: patchedAttachShadow
      });
    } catch {
      return null;
    }
    g2[ATTACH_SHADOW_HOOK_KEY] = state;
    return state;
  }
  function removeAttachShadowSubscriber(subscriber) {
    const g2 = globalThis;
    const state = g2[ATTACH_SHADOW_HOOK_KEY];
    if (!state) return;
    state.subscribers.delete(subscriber);
    if (state.subscribers.size > 0) return;
    try {
      Object.defineProperty(Element.prototype, "attachShadow", state.descriptor);
    } catch {
      const original = state.descriptor.value;
      if (typeof original === "function") {
        Element.prototype.attachShadow = original;
      }
    }
    delete g2[ATTACH_SHADOW_HOOK_KEY];
  }
  class VideoObserver {
    seenVideos = new WeakSet();
    activeVideos = new WeakSet();
    observedRoots = new WeakSet();
    videoListenerControllers = new Map();
    pendingAdded = new Set();
    pendingRemoved = new Set();
    flushPending = false;
    static MAX_FLUSH_BUDGET_MS = 6;
    static MAX_NODES_PER_SLICE = 120;
    onVideoAdded = new EventImpl();
    onVideoRemoved = new EventImpl();
    observer = new MutationObserver(
      (muts) => this.onMutations(muts)
    );
    intervalIdleChecker;
    checkerUnsubscribe = null;
    enabled = false;
    attachShadowSubscriber = null;
    onDocumentReady = null;
    policy = DEFAULT_VIDEO_OBSERVER_POLICY;
    constructor(intervalIdleChecker = createIntervalIdleChecker()) {
      this.intervalIdleChecker = intervalIdleChecker;
    }
    setPolicy(policy) {
      this.policy = {
        ...DEFAULT_VIDEO_OBSERVER_POLICY,
        ...policy,
        probeSelectors: Array.isArray(policy.probeSelectors) && policy.probeSelectors.length > 0 ? [...policy.probeSelectors] : [...DEFAULT_VIDEO_OBSERVER_POLICY.probeSelectors],
        shadowHostSelectors: Array.isArray(policy.shadowHostSelectors) ? [...policy.shadowHostSelectors] : [...DEFAULT_VIDEO_OBSERVER_POLICY.shadowHostSelectors]
      };
    }
    static containsAdKeyword(value) {
      return value.length > 0 && AD_KEYWORD_PATTERN.test(value);
    }
    isAdRelated(element) {
      for (const attr of AD_ATTRS) {
        const rawValue = element.getAttribute(attr);
        if (!rawValue) continue;
        if (VideoObserver.containsAdKeyword(rawValue.toLowerCase())) {
          return true;
        }
      }
      return false;
    }
    isInsideAd(video) {
      for (let p2 = video.parentElement; p2; p2 = p2.parentElement) {
        if (this.isAdRelated(p2)) return true;
      }
      return false;
    }
    getCapturedAudioTrackCount(video) {
      const candidate = video;
      const captureStream = candidate.captureStream ?? candidate.mozCaptureStream;
      if (typeof captureStream !== "function") return null;
      try {
        const stream = captureStream.call(video);
        return stream.getAudioTracks().length;
      } catch {
        return null;
      }
    }
    isLikelySilentDecorativeVideo(video) {
      if (!(video.muted || video.defaultMuted)) return false;
      if (!video.autoplay || !video.loop) return false;
      if (video.controls) return false;
      const v2 = video;
      if (typeof v2.mozHasAudio === "boolean") {
        return !v2.mozHasAudio;
      }
      if ("audioTracks" in v2 && typeof v2.audioTracks?.length === "number") {
        if (v2.audioTracks.length > 0) return false;
        const capturedTrackCount2 = this.getCapturedAudioTrackCount(video);
        if (capturedTrackCount2 !== null) {
          return capturedTrackCount2 === 0;
        }
        return true;
      }
      const capturedTrackCount = this.getCapturedAudioTrackCount(video);
      if (capturedTrackCount !== null) {
        return capturedTrackCount === 0;
      }
      return false;
    }
    hasAudio(video) {
      const v2 = video;
      if (video.srcObject instanceof MediaStream) {
        return video.srcObject.getAudioTracks().length > 0;
      }
      if (typeof v2.mozHasAudio === "boolean") return v2.mozHasAudio;
      if (typeof v2.webkitAudioDecodedByteCount === "number" && v2.webkitAudioDecodedByteCount > 0) {
        return true;
      }
      if ("audioTracks" in v2 && typeof v2.audioTracks?.length === "number") {
        if (v2.audioTracks.length > 0) return true;
      }
      if (this.isLikelySilentDecorativeVideo(video)) {
        return false;
      }
      return true;
    }
    isVkLikeVideo(video) {
      const hostname = String(globalThis.location?.hostname || "");
      const sourceUrl = String(video.currentSrc || video.src || "");
      return /(?:^|\.)vkvideo\.ru$|(?:^|\.)vk\.(?:com|ru)$|(?:^|\.)okcdn\.ru$/i.test(
        hostname
      ) || /vkvd\d+\.okcdn\.ru|\.okcdn\.ru|vkvideo\.ru|vk\.(?:com|ru)/i.test(
        sourceUrl
      );
    }
    isValidVideo(video) {
      if (this.isAdRelated(video)) return false;
      if (this.isInsideAd(video)) return false;
      if (!this.hasAudio(video) && !this.isVkLikeVideo(video)) {
        debug.log("Ignoring video without audio:", video);
        return false;
      }
      return true;
    }
    stopObservingDom() {
      if (shouldLogMobileOverlay()) {
        console.log(
          "[VOT][mobile-overlay][video-observer] disconnect DOM observer"
        );
      }
      this.observer.disconnect();
      this.observedRoots = new WeakSet();
      this.pendingAdded.clear();
      this.pendingRemoved.clear();
      this.flushPending = false;
    }
    canObserveRoots() {
      return this.policy.startDomObservationOnEnable;
    }
    observeRoot(root) {
      if (!this.canObserveRoots()) return;
      if (this.observedRoots.has(root)) return;
      this.observedRoots.add(root);
      this.observer.observe(root, { childList: true, subtree: true });
    }
    scan(root) {
      if (!this.policy.startDomObservationOnEnable) {
        this.scanTargeted(root, this.policy.probeSelectors);
        return;
      }
      this.scanDefault(root);
    }
    scanTargeted(root, selectors) {
      if (root instanceof HTMLVideoElement) {
        this.trackVideo(root);
        return;
      }
      if (!(root instanceof Document) && !(root instanceof DocumentFragment) && !(root instanceof Element)) {
        return;
      }
      const seen = new Set();
      const addVideo = (video) => {
        if (!(video instanceof HTMLVideoElement) || seen.has(video)) {
          return;
        }
        seen.add(video);
        this.trackVideo(video);
      };
      const effectiveSelectors = selectors.length > 0 ? selectors : DEFAULT_VIDEO_OBSERVER_POLICY.probeSelectors;
      for (const selector of effectiveSelectors) {
        try {
          if (root instanceof Element && root.matches(selector)) {
            addVideo(root);
          }
        } catch {
        }
        try {
          for (const match of root.querySelectorAll(selector)) {
            addVideo(match);
          }
        } catch {
        }
      }
      if (!this.policy.observeShadowRoots) {
        return;
      }
      for (const selector of this.policy.shadowHostSelectors) {
        let hosts;
        try {
          hosts = root.querySelectorAll(selector);
        } catch {
          continue;
        }
        for (const host of hosts) {
          const shadowRoot = host.shadowRoot;
          if (!shadowRoot) {
            continue;
          }
          this.scanTargeted(shadowRoot, effectiveSelectors);
        }
      }
    }
    scanDefault(root) {
      if (root instanceof HTMLVideoElement) {
        this.trackVideo(root);
        return;
      }
      if (root.nodeType !== Node.ELEMENT_NODE && root.nodeType !== Node.DOCUMENT_FRAGMENT_NODE && root.nodeType !== Node.DOCUMENT_NODE) {
        return;
      }
      const walker = document.createTreeWalker(root, NodeFilter.SHOW_ELEMENT, {
        acceptNode: (node) => {
          const el = node;
          const isVideo = el.tagName === "VIDEO";
          const hasShadowRoot = Boolean(el.shadowRoot);
          return isVideo || hasShadowRoot ? NodeFilter.FILTER_ACCEPT : NodeFilter.FILTER_SKIP;
        }
      });
      while (walker.nextNode()) {
        const el = walker.currentNode;
        if (el instanceof HTMLVideoElement) {
          this.trackVideo(el);
          continue;
        }
        const sr = el.shadowRoot;
        if (sr) {
          this.observeRoot(sr);
          this.scanDefault(sr);
        }
      }
    }
    getVideoListenerSignal(video) {
      const existingController = this.videoListenerControllers.get(video);
      if (existingController) {
        existingController.abort();
      }
      const controller = new AbortController();
      this.videoListenerControllers.set(video, controller);
      return controller.signal;
    }
    cleanupVideoListeners(video) {
      const controller = this.videoListenerControllers.get(video);
      if (!controller) return;
      controller.abort();
      this.videoListenerControllers.delete(video);
    }
    cleanupAllVideoListeners() {
      for (const controller of this.videoListenerControllers.values()) {
        controller.abort();
      }
      this.videoListenerControllers.clear();
    }
    trackVideo(video) {
      if (this.seenVideos.has(video)) return;
      this.seenVideos.add(video);
      const listenerSignal = this.getVideoListenerSignal(video);
      const tryValidate = () => {
        if (!this.isValidVideo(video)) return;
        if (this.activeVideos.has(video)) return;
        this.activeVideos.add(video);
        this.onVideoAdded.dispatch(video);
      };
      const hasInitialMediaState = video.readyState >= HTMLMediaElement.HAVE_METADATA || Boolean(video.currentSrc || video.src || video.srcObject);
      if (hasInitialMediaState) {
        tryValidate();
      } else {
        video.addEventListener("loadedmetadata", tryValidate, {
          once: true,
          signal: listenerSignal
        });
        video.addEventListener("loadeddata", tryValidate, {
          once: true,
          signal: listenerSignal
        });
        video.addEventListener("canplay", tryValidate, {
          once: true,
          signal: listenerSignal
        });
        video.addEventListener("playing", tryValidate, {
          once: true,
          passive: true,
          signal: listenerSignal
        });
        video.addEventListener("timeupdate", tryValidate, {
          once: true,
          passive: true,
          signal: listenerSignal
        });
        const handlePlay = () => {
          if (video.readyState >= HTMLMediaElement.HAVE_METADATA) {
            tryValidate();
          }
        };
        video.addEventListener("play", handlePlay, {
          once: true,
          passive: true,
          signal: listenerSignal
        });
      }
      video.addEventListener(
        "emptied",
        () => {
          if (!video.isConnected) {
            this.untrackVideo(video);
          }
        },
        { passive: true, signal: listenerSignal }
      );
      video.addEventListener(
        "durationchange",
        () => {
          if (!this.activeVideos.has(video)) {
            tryValidate();
          }
        },
        { passive: true, signal: listenerSignal }
      );
    }
    untrackVideo(video) {
      this.cleanupVideoListeners(video);
      if (this.activeVideos.has(video)) {
        this.onVideoRemoved.dispatch(video);
        this.activeVideos.delete(video);
      }
      this.seenVideos.delete(video);
    }
    collectVideos(node) {
      const set = new Set();
      const addAll = (videos) => {
        for (const v2 of videos) set.add(v2);
      };
      if (node instanceof HTMLVideoElement) set.add(node);
      if (node instanceof Document || node instanceof DocumentFragment || node instanceof Element) {
        addAll(node.querySelectorAll("video"));
      }
      if (node instanceof Element) {
        const shadowRoot = node.shadowRoot;
        if (shadowRoot) {
          addAll(shadowRoot.querySelectorAll("video"));
        }
      }
      return Array.from(set);
    }
    getNowMs() {
      if (typeof performance !== "undefined" && typeof performance.now === "function") {
        return performance.now();
      }
      return Date.now();
    }
    isSliceBudgetReached(startMs, processed) {
      if (processed >= VideoObserver.MAX_NODES_PER_SLICE) return true;
      return this.getNowMs() - startMs >= VideoObserver.MAX_FLUSH_BUDGET_MS;
    }
    processPendingAdded(startMs) {
      let processed = 0;
      while (this.pendingAdded.size > 0) {
        const next = this.pendingAdded.values().next();
        if (next.done) break;
        this.pendingAdded.delete(next.value);
        this.scan(next.value);
        processed += 1;
        if (this.isSliceBudgetReached(startMs, processed)) {
          break;
        }
      }
      return processed;
    }
    processPendingRemoved(startMs, processed) {
      let processedCount = processed;
      while (this.pendingRemoved.size > 0) {
        if (this.isSliceBudgetReached(startMs, processedCount)) {
          break;
        }
        const next = this.pendingRemoved.values().next();
        if (next.done) break;
        this.pendingRemoved.delete(next.value);
        for (const video of this.collectVideos(next.value)) {
          if (!video.isConnected) {
            this.untrackVideo(video);
          }
        }
        processedCount += 1;
      }
      return processedCount;
    }
    flushSlice = () => {
      if (!this.enabled) {
        this.pendingAdded.clear();
        this.pendingRemoved.clear();
        this.flushPending = false;
        return;
      }
      const startMs = this.getNowMs();
      const processedAdded = this.processPendingAdded(startMs);
      this.processPendingRemoved(startMs, processedAdded);
      this.flushPending = this.pendingAdded.size > 0 || this.pendingRemoved.size > 0;
      if (this.flushPending) {
        this.intervalIdleChecker.requestImmediateTick();
      }
    };
    onCheckerTick = () => {
      if (!this.flushPending) return;
      this.flushSlice();
    };
    scheduleFlush = () => {
      if (!this.enabled) return;
      this.flushPending = true;
      this.intervalIdleChecker.requestImmediateTick();
    };
    installAttachShadowHook() {
      if (!this.canObserveRoots()) return;
      if (!this.policy.observeShadowRoots) return;
      if (this.attachShadowSubscriber) return;
      const state = getOrInstallAttachShadowHook();
      if (!state) return;
      const subscriber = (root) => {
        if (!this.enabled) return;
        this.observeRoot(root);
        this.pendingAdded.add(root);
        this.scheduleFlush();
      };
      state.subscribers.add(subscriber);
      this.attachShadowSubscriber = subscriber;
    }
    uninstallAttachShadowHook() {
      if (!this.attachShadowSubscriber) return;
      removeAttachShadowSubscriber(this.attachShadowSubscriber);
      this.attachShadowSubscriber = null;
    }
    enqueueAddedNode(node) {
      if (this.canObserveRoots() && this.policy.observeShadowRoots && node.nodeType === Node.ELEMENT_NODE) {
        const shadowRoot = node.shadowRoot;
        if (shadowRoot) {
          this.observeRoot(shadowRoot);
        }
      }
      this.pendingAdded.add(node);
    }
    enqueueMutation(mutation) {
      for (const node of mutation.addedNodes) {
        this.enqueueAddedNode(node);
      }
      for (const node of mutation.removedNodes) {
        this.pendingRemoved.add(node);
      }
    }
    onMutations(mutations) {
      for (const mutation of mutations) {
        if (mutation.type !== "childList") continue;
        this.enqueueMutation(mutation);
      }
      if (this.pendingAdded.size > 0 || this.pendingRemoved.size > 0) {
        this.scheduleFlush();
      }
    }
    onPageShow = () => {
      const root = document.documentElement;
      if (!root) return;
      if (this.canObserveRoots()) {
        this.observeRoot(root);
      }
      this.scan(root);
    };
    enable(initialTarget) {
      if (this.enabled) return;
      this.enabled = true;
      const startWhenReady = () => {
        const root2 = document.documentElement;
        const scanTarget = initialTarget && "isConnected" in initialTarget && initialTarget.isConnected ? initialTarget : root2;
        if (!scanTarget) return;
        if (this.policy.startDomObservationOnEnable) {
          this.observeRoot(root2);
        }
        this.scan(scanTarget);
      };
      if (!this.policy.startDomObservationOnEnable) {
        const root2 = document.documentElement;
        if (root2) {
          startWhenReady();
          return;
        }
        const onReady2 = () => {
          document.removeEventListener("readystatechange", onReady2);
          this.onDocumentReady = null;
          if (!this.enabled) return;
          startWhenReady();
        };
        this.onDocumentReady = onReady2;
        document.addEventListener("readystatechange", onReady2);
        if (typeof queueMicrotask === "function") {
          queueMicrotask(onReady2);
        } else {
          void Promise.resolve().then(onReady2);
        }
        return;
      }
      this.checkerUnsubscribe?.();
      this.checkerUnsubscribe = this.intervalIdleChecker.subscribe(
        this.onCheckerTick
      );
      this.intervalIdleChecker.start();
      this.intervalIdleChecker.markActivity("video-observer-enable");
      this.installAttachShadowHook();
      globalThis.addEventListener("pageshow", this.onPageShow, {
        passive: true
      });
      const root = document.documentElement;
      if (root) {
        startWhenReady();
        return;
      }
      const onReady = () => {
        const r2 = document.documentElement;
        if (!r2) return;
        document.removeEventListener("readystatechange", onReady);
        this.onDocumentReady = null;
        if (!this.enabled) return;
        startWhenReady();
      };
      this.onDocumentReady = onReady;
      document.addEventListener("readystatechange", onReady);
      if (typeof queueMicrotask === "function") {
        queueMicrotask(onReady);
      } else {
        void Promise.resolve().then(onReady);
      }
    }
    disable() {
      if (!this.enabled) return;
      if (shouldLogMobileOverlay()) {
        console.log("[VOT][mobile-overlay][video-observer] disable observer");
      }
      this.enabled = false;
      globalThis.removeEventListener("pageshow", this.onPageShow);
      if (this.onDocumentReady) {
        document.removeEventListener("readystatechange", this.onDocumentReady);
        this.onDocumentReady = null;
      }
      this.uninstallAttachShadowHook();
      this.stopObservingDom();
      this.cleanupAllVideoListeners();
      this.checkerUnsubscribe?.();
      this.checkerUnsubscribe = null;
      this.intervalIdleChecker.stop();
      this.seenVideos = new WeakSet();
      this.activeVideos = new WeakSet();
      this.observedRoots = new WeakSet();
    }
  }
  function syncVideoLinkSnapshot(state, volumePercent) {
    const normalized = clampPercentInt(volumePercent);
    state.lastVideoPercent = normalized;
  }
  function syncTranslationLinkSnapshot(state, volumePercent) {
    const numeric = Number(volumePercent);
    if (!Number.isFinite(numeric)) {
      return;
    }
    const normalized = Math.max(0, Math.round(numeric));
    state.lastTranslationPercent = normalized;
  }
  function applyVolumeLinkDelta({
    state,
    fromType,
    newVolume,
    currentVideo,
    currentTranslation,
    translationMin,
    translationMax
  }) {
    if (!state.initialized) {
      state.lastVideoPercent = Number(currentVideo);
      state.lastTranslationPercent = Number(currentTranslation);
      state.initialized = true;
    }
    if (fromType === "video") {
      const normalizedVideo = clampPercentInt(newVolume);
      const delta2 = normalizedVideo - clampPercentInt(state.lastVideoPercent);
      state.lastVideoPercent = normalizedVideo;
      const nextTranslation = clampInt(
        state.lastTranslationPercent + delta2,
        translationMin,
        translationMax
      );
      state.lastTranslationPercent = nextTranslation;
      return { nextTranslation };
    }
    const normalizedTranslation = clampInt(
      Number.isFinite(newVolume) ? newVolume : currentTranslation,
      translationMin,
      translationMax
    );
    const delta = normalizedTranslation - state.lastTranslationPercent;
    state.lastTranslationPercent = normalizedTranslation;
    const nextVideo = clampPercentInt(state.lastVideoPercent + delta);
    state.lastVideoPercent = nextVideo;
    return { nextVideo };
  }
  const defaultPlatformConfig = {
    allowTouchMoveHandler: true,
    disableContainerDrag: false
  };
  const platformOverrides = {
    xvideos: {
      allowTouchMoveHandler: false
    },
    youtube: {
      disableContainerDrag: true
    }
  };
  function getPlatformEventConfig(host) {
    if (!host) {
      return defaultPlatformConfig;
    }
    const overrides = platformOverrides[host] ?? {};
    return {
      ...defaultPlatformConfig,
      ...overrides
    };
  }
  const MOBILE_YOUTUBE_LIFECYCLE_GRACE_MS = 2200;
  const MOBILE_YOUTUBE_MIN_AUTO_HIDE_DELAY_MS = 2500;
  function mergeListenerSignals(primary, secondary) {
    if (!secondary || secondary === primary) {
      return primary;
    }
    if (primary.aborted) {
      return primary;
    }
    if (secondary.aborted) {
      return secondary;
    }
    const canCombine = typeof AbortSignal !== "undefined" && "any" in AbortSignal;
    if (canCombine) {
      return AbortSignal.any([primary, secondary]);
    }
    const controller = new AbortController();
    const cleanup = () => {
      primary.removeEventListener("abort", onPrimaryAbort);
      secondary.removeEventListener("abort", onSecondaryAbort);
    };
    const onPrimaryAbort = () => {
      cleanup();
      controller.abort(primary.reason);
    };
    const onSecondaryAbort = () => {
      cleanup();
      controller.abort(secondary.reason);
    };
    primary.addEventListener("abort", onPrimaryAbort, { once: true });
    secondary.addEventListener("abort", onSecondaryAbort, { once: true });
    return controller.signal;
  }
  function isVkLikeSiteHost(host) {
    return host === "vk" || /(?:^|\.)vkvideo\.ru$|(?:^|\.)vk\.(?:com|ru)$/i.test(
      String(globalThis.location?.hostname || "")
    );
  }
  function isMobileYouTubeDebugHost() {
    return /^(m|music)\.youtube\.com$/i.test(
      String(globalThis.location?.hostname || "")
    );
  }
  function logMobileOverlay(message, details) {
    if (!isMobileYouTubeDebugHost()) {
      return;
    }
    console.log(`[VOT][mobile-overlay][events] ${message}`, details ?? {});
  }
  function getMobileYouTubePageKey() {
    return `${globalThis.location.origin}${globalThis.location.pathname}${globalThis.location.search}`;
  }
  function clearMobileYouTubeLifecycleDebounce(self) {
    if (self.mobileYouTubeLifecycleDebounceTimer === void 0) {
      return;
    }
    clearTimeout(self.mobileYouTubeLifecycleDebounceTimer);
    self.mobileYouTubeLifecycleDebounceTimer = void 0;
  }
  function closeOverlayMenu(overlayView) {
    if (overlayView.votMenu) {
      overlayView.votMenu.hidden = true;
    }
  }
  function scheduleMobileYouTubeLifecycleRetry(self, overlayView, reason, cleanup) {
    clearMobileYouTubeLifecycleDebounce(self);
    const pageKey = getMobileYouTubePageKey();
    logMobileOverlay("schedule lifecycle retry", {
      reason,
      pageKey,
      delayMs: MOBILE_YOUTUBE_LIFECYCLE_GRACE_MS
    });
    self.mobileYouTubeLifecycleDebounceTimer = globalThis.setTimeout(() => {
      self.mobileYouTubeLifecycleDebounceTimer = void 0;
      if (self.abortController.signal.aborted) {
        logMobileOverlay("skip lifecycle retry: handler aborted", { reason });
        return;
      }
      const currentPageKey = getMobileYouTubePageKey();
      if (currentPageKey !== pageKey) {
        logMobileOverlay("run lifecycle cleanup after page change", {
          reason,
          pageKey,
          currentPageKey
        });
        cleanup();
        return;
      }
      const recoveredVideoId = YoutubeHelper2.getCurrentVideoId();
      const currentVideoId = self.videoData?.videoId;
      const hasLiveVideoId = typeof recoveredVideoId === "string" && recoveredVideoId.length > 0;
      const hasSameVideoId = hasLiveVideoId && Boolean(currentVideoId) && recoveredVideoId === currentVideoId;
      const hasSource = Boolean(
        self.video.currentSrc || self.video.src || self.video.srcObject
      );
      const hasConnectedShell = Boolean(self.container?.isConnected);
      if (hasSameVideoId || hasLiveVideoId || hasSource || hasConnectedShell) {
        logMobileOverlay("keep overlay after lifecycle retry", {
          reason,
          currentVideoId,
          recoveredVideoId,
          hasSource,
          hasConnectedShell
        });
        closeOverlayMenu(overlayView);
        self.refreshOverlayMount();
        return;
      }
      if (self.site.additionalData === "music") {
        logMobileOverlay("keep overlay for music shell without stable video", {
          reason,
          pageKey
        });
        closeOverlayMenu(overlayView);
        self.refreshOverlayMount();
        scheduleMobileYouTubeLifecycleRetry(self, overlayView, reason, cleanup);
        return;
      }
      logMobileOverlay("lifecycle retry expired; cleaning up", {
        reason,
        pageKey
      });
      cleanup();
    }, MOBILE_YOUTUBE_LIFECYCLE_GRACE_MS);
  }
  function createScopedListeners(signal) {
    const add = (element, event, handler, options) => {
      const mergedSignal = mergeListenerSignals(signal, options?.signal);
      if (!options) {
        element.addEventListener(event, handler, { signal: mergedSignal });
        return;
      }
      const { signal: _ignoredSignal, ...restOptions } = options;
      element.addEventListener(event, handler, {
        ...restOptions,
        signal: mergedSignal
      });
    };
    const addMany = (element, events, handler, options) => {
      for (const event of events) {
        add(element, event, handler, options);
      }
    };
    return { add, addMany };
  }
  function bindOverlayHoverFocusEvents(addMany, target, overlayVisibility) {
    addMany(
      target,
      ["pointerenter", "focusin"],
      (event) => overlayVisibility.handleOverlayInteraction(event)
    );
    addMany(
      target,
      ["pointermove"],
      (event) => overlayVisibility.handleOverlayInteraction(event),
      { passive: true }
    );
    addMany(
      target,
      ["pointerleave", "focusout"],
      (event) => overlayVisibility.scheduleHide(event)
    );
  }
  function toPercentInt(value, fallback = 0) {
    const numeric = typeof value === "number" ? value : Number(value);
    return Number.isFinite(numeric) ? clampPercentInt(numeric) : fallback;
  }
  function syncAudioTranslationVolumeFromVideo(self, videoPercent, options = {}) {
    if (options.skipYouTubeLikeHosts && isYouTubeLikeHost(self.site.host)) {
      return;
    }
    if (self.smartVolumeDuckingInterval !== void 0) return;
    if (!self.data?.syncVolume || !self.audioPlayer?.player?.src) return;
    if (self.isLikelyInternalVideoVolumeChange(videoPercent)) return;
    self.syncVolumeWrapper("video", videoPercent);
  }
  function applyOverlayLayout(self, overlayView, heightPx) {
    const menu = overlayView.votMenu?.container;
    if (menu) {
      const height = heightPx ?? self.video.getBoundingClientRect().height;
      menu.style.setProperty("--vot-container-height", `${height}px`);
    }
    const { position: position2, direction } = overlayView.calcButtonLayout(
      self.data?.buttonPos ?? "default"
    );
    overlayView.updateButtonLayout(position2, direction);
  }
  function normalizeHotkeyPart(value) {
    return value.replace("Key", "").replace("Digit", "");
  }
  function buildPressedHotkeyPartsSet(userPressedKeys) {
    const pressedParts = new Set();
    for (const key of userPressedKeys) {
      pressedParts.add(normalizeHotkeyPart(key));
    }
    return pressedParts;
  }
  function getParsedHotkey(hotkey, cache) {
    if (!hotkey) return null;
    const cached = cache.get(hotkey);
    if (cached) return cached;
    const parts = hotkey.split("+").filter(Boolean).map(normalizeHotkeyPart);
    const parsed = {
      parts,
      partsSet: new Set(parts)
    };
    cache.set(hotkey, parsed);
    return parsed;
  }
  function isHotkeyMatch(pressedParts, hotkey) {
    if (!hotkey) return false;
    if (pressedParts.size !== hotkey.parts.length) return false;
    for (const key of hotkey.partsSet) {
      if (!pressedParts.has(key)) return false;
    }
    return true;
  }
  function bindOverlayLayoutEvents(ctx) {
    const { self, overlayView, addMany } = ctx;
    const syncMountAndLayout = () => {
      self.refreshOverlayMount();
      applyOverlayLayout(self, overlayView);
    };
    let recoveryQueued = false;
    const queueOverlayRecovery = (reason) => {
      if (recoveryQueued) {
        return;
      }
      recoveryQueued = true;
      queueMicrotask(() => {
        recoveryQueued = false;
        if (self.abortController.signal.aborted) {
          return;
        }
        const containerStale = !self.container.isConnected || !self.video.isConnected || self.video.isConnected && !containsCrossShadow(self.container, self.video);
        const overlayState = self.uiManager.getOverlayVerificationState({
          requireVisible: false
        });
        if (!containerStale && overlayState.verified) {
          return;
        }
        if (containerStale) {
          debug.log("[VOT][observer] overlay/container stale, resyncing", {
            reason,
            containerConnected: self.container.isConnected,
            videoConnected: self.video.isConnected,
            src: self.video.currentSrc || self.video.src || ""
          });
          syncMountAndLayout();
        }
        if (!overlayState.verified) {
          void self.ensureOverlayVerified(`observer:${reason}`);
        }
      });
    };
    self.resizeObserver = new ResizeObserver((entries) => {
      for (const entry of entries) {
        applyOverlayLayout(self, overlayView, entry.contentRect.height);
      }
    });
    self.resizeObserver.observe(self.video);
    syncMountAndLayout();
    addMany(
      document,
      ["fullscreenchange", "webkitfullscreenchange"],
      () => syncMountAndLayout()
    );
    addMany(
      self.video,
      ["webkitbeginfullscreen", "webkitendfullscreen"],
      () => syncMountAndLayout()
    );
    self.overlayMountObserver = new MutationObserver(() => {
      queueOverlayRecovery("dom-mutation");
    });
    const observedOverlayHosts = new Set();
    const observeOverlayHost = (node) => {
      if (!node || observedOverlayHosts.has(node)) {
        return;
      }
      observedOverlayHosts.add(node);
      self.overlayMountObserver?.observe(node, {
        childList: true,
        subtree: false
      });
    };
    observeOverlayHost(self.uiManager.root.parentElement ?? self.uiManager.root);
    observeOverlayHost(
      self.uiManager.votGlobalPortal?.parentElement ?? self.uiManager.votGlobalPortal
    );
    observeOverlayHost(self.container.parentElement ?? self.container);
    if (isMobileYouTubeLikeSite(self.site)) {
      let syncQueued = false;
      const queueSyncMountAndLayout = () => {
        if (syncQueued) {
          return;
        }
        syncQueued = true;
        queueMicrotask(() => {
          syncQueued = false;
          if (self.abortController.signal.aborted) {
            return;
          }
          const containerStale = !self.container.isConnected || !self.video.isConnected || self.video.isConnected && !containsCrossShadow(self.container, self.video);
          if (!containerStale) {
            return;
          }
          logMobileOverlay("sync mount after DOM repaint", {
            containerConnected: self.container.isConnected,
            videoConnected: self.video.isConnected
          });
          syncMountAndLayout();
        });
      };
      addMany(
        document,
        ["yt-page-data-updated", "yt-navigate-finish"],
        () => queueSyncMountAndLayout()
      );
    }
  }
  function bindYouTubeVolumeSync(ctx) {
    const { self } = ctx;
    if (!isDesktopYouTubeLikeSite(self.site)) return;
    self.syncVolumeObserver = new MutationObserver((mutations) => {
      if (!self.audioPlayer?.player?.src) return;
      let hasVolumeMutation = false;
      let lastObservedAriaValue = null;
      for (const mutation of mutations) {
        if (mutation.type !== "attributes" || mutation.attributeName !== "aria-valuenow") {
          continue;
        }
        hasVolumeMutation = true;
        const ariaValueNow = mutation.target instanceof Element ? mutation.target.getAttribute("aria-valuenow") : null;
        const parsedAriaValue = ariaValueNow != null ? Number.parseFloat(ariaValueNow) : Number.NaN;
        if (Number.isFinite(parsedAriaValue)) {
          lastObservedAriaValue = parsedAriaValue;
        }
      }
      if (!hasVolumeMutation) return;
      let videoPercent;
      if (lastObservedAriaValue != null) {
        videoPercent = toPercentInt(lastObservedAriaValue);
      } else {
        const fallbackVolume = self.isMuted() ? 0 : self.getVideoVolume();
        videoPercent = toPercentInt(fallbackVolume * 100);
      }
      self.syncVideoVolumeSlider();
      syncAudioTranslationVolumeFromVideo(self, videoPercent);
    });
    const ytpVolumePanel = document.querySelector(".ytp-volume-panel");
    if (!ytpVolumePanel) return;
    self.syncVolumeObserver.observe(ytpVolumePanel, {
      attributes: true,
      subtree: true,
      attributeFilter: ["aria-valuenow"]
    });
  }
  function bindAudioTrackLanguageSync(ctx) {
    const { self } = ctx;
    if (!isDesktopYouTubeLikeSite(self.site)) return;
    const syncAudioTrackLanguage = async () => {
      try {
        if (!self.videoData) return;
        const player22 = YoutubeHelper2.getPlayer();
        const availableTracks = player22?.getAvailableAudioTracks?.() ?? null;
        if (!Array.isArray(availableTracks) || availableTracks.length <= 1)
          return;
        const currentTrackInfo = player22?.getAudioTrack?.()?.getLanguageInfo?.();
        const currentTrackId = currentTrackInfo?.id;
        const currentLanguageCode = currentTrackId && currentTrackId !== "und" ? currentTrackId.toLowerCase().split(/[-_.]/)[0] : void 0;
        if (!currentLanguageCode) return;
        if (!availableLangs.includes(currentLanguageCode)) return;
        const currentLanguage = currentLanguageCode;
        if (currentLanguage === self.videoData.detectedLanguage) return;
        self.videoManager.rememberDetectedLanguage(
          self.videoData.videoId,
          currentLanguage
        );
        self.setSelectMenuValues(
          currentLanguage,
          self.videoData.responseLanguage
        );
        if (self.data?.autoTranslate && currentLanguage !== self.videoData.responseLanguage) {
          debug.log(
            `[VOT] Audio track language changed to ${currentLanguage}, triggering auto-translation`
          );
          try {
            await self.uiManager.handleTranslationBtnClick();
          } catch (error2) {
            debug.log(
              "[VOT] Failed to trigger auto-translation on audio track change:",
              error2
            );
          }
        }
      } catch (error2) {
        debug.log("[VOT] Failed to sync audio track language", error2);
      }
    };
    const player2 = YoutubeHelper2.getPlayer();
    const listeners = ["onApiChange", "onStateChange"];
    if (player2?.addEventListener) {
      for (const eventName of listeners) {
        try {
          player2.addEventListener(eventName, syncAudioTrackLanguage);
        } catch (error2) {
          debug.log(`[VOT] Failed to bind ${eventName}`, error2);
        }
      }
    }
    void syncAudioTrackLanguage();
    self.abortController.signal.addEventListener(
      "abort",
      () => {
        if (!player2?.removeEventListener) return;
        for (const eventName of listeners) {
          try {
            player2.removeEventListener(eventName, syncAudioTrackLanguage);
          } catch (error2) {
            debug.log(`[VOT] Failed to unbind ${eventName}`, error2);
          }
        }
      },
      { once: true }
    );
  }
  function bindGlobalDismissAndHotkeys(ctx) {
    const { self, overlayView, add, addMany, platformConfig } = ctx;
    add(document, "click", (event) => {
      const target = event.target;
      const button2 = overlayView.votButton?.container;
      const menu = overlayView.votMenu?.container;
      const settings = self.uiManager.votSettingsView?.dialog?.container;
      const isButton = target && button2 ? button2.contains(target) : false;
      const isMenu = target && menu ? menu.contains(target) : false;
      const isVideo = target ? self.container.contains(target) : false;
      const isSettings = target && settings ? settings.contains(target) : false;
      const isTempDialog = target instanceof Element && target.closest(".vot-dialog-temp") instanceof Element;
      debug.log(
        `[document click] ${isButton} ${isMenu} ${isVideo} ${isSettings} ${isTempDialog}`
      );
      if (isButton || isMenu || isSettings || isTempDialog) return;
      if (!isVideo && !isVkLikeSiteHost(self.site.host) && !isMobileYouTubeLikeSite(self.site)) {
        overlayView.updateButtonOpacity(0);
      }
      if (menu && !menu.hidden) {
        menu.hidden = true;
        self.overlayVisibility?.queueAutoHide();
      }
    });
    const userPressedKeys = new Set();
    const hotkeyCache = new Map();
    const clearUserPressedKeys = () => userPressedKeys.clear();
    const runHotkeyAction = (action, actionName) => {
      void action().catch((error2) => {
        debug.log(`[VOT] ${actionName} hotkey action failed`, error2);
      });
    };
    add(document, "keydown", (event) => {
      const keyboardEvent = event;
      if (keyboardEvent.repeat) return;
      userPressedKeys.add(keyboardEvent.code);
      const activeElement = document.activeElement;
      const activeTag = activeElement?.tagName?.toLowerCase?.() ?? "";
      const isInputElement = ["input", "textarea"].includes(activeTag) || Boolean(activeElement?.isContentEditable);
      if (isInputElement) return;
      const pressedParts = buildPressedHotkeyPartsSet(userPressedKeys);
      if (isHotkeyMatch(
        pressedParts,
        getParsedHotkey(self.data?.translationHotkey, hotkeyCache)
      )) {
        clearUserPressedKeys();
        runHotkeyAction(
          () => self.uiManager.handleTranslationBtnClick(),
          "Translation"
        );
        return;
      }
      if (isHotkeyMatch(
        pressedParts,
        getParsedHotkey(self.data?.subtitlesHotkey, hotkeyCache)
      )) {
        clearUserPressedKeys();
        runHotkeyAction(
          () => self.toggleSubtitlesForCurrentLangPair(),
          "Subtitles"
        );
      }
    });
    add(
      document,
      "keyup",
      (event) => userPressedKeys.delete(event.code)
    );
    add(document, "blur", clearUserPressedKeys);
    add(document, "visibilitychange", () => {
      if (document.hidden) clearUserPressedKeys();
    });
    add(globalThis, "blur", clearUserPressedKeys);
    const hostInteractionTargets = new Set();
    const eventContainer = self.getEventContainer();
    if (eventContainer) {
      hostInteractionTargets.add(eventContainer);
    }
    hostInteractionTargets.add(self.container);
    hostInteractionTargets.add(self.video);
    for (const target of hostInteractionTargets) {
      addMany(
        target,
        ["pointerenter", "pointerdown"],
        (event) => self.overlayVisibility.handleHostInteraction(event)
      );
      if (isMobileYouTubeLikeSite(self.site)) {
        addMany(
          target,
          ["touchstart"],
          (event) => self.overlayVisibility.handleHostInteraction(event),
          { passive: true }
        );
      }
      add(
        target,
        "pointermove",
        (event) => self.overlayVisibility.handleHostInteraction(event),
        { passive: true }
      );
      add(
        target,
        "pointerleave",
        (event) => self.overlayVisibility.scheduleHide(event)
      );
    }
    self.rebindOverlayVisibilityTargets();
    if (platformConfig.allowTouchMoveHandler) {
      add(
        document,
        "touchmove",
        (event) => self.overlayVisibility.handleHostInteraction(event),
        { passive: true }
      );
    }
    if (isMobileYouTubeLikeSite(self.site)) {
      add(
        document,
        "touchstart",
        (event) => self.overlayVisibility.handleHostInteraction(event),
        { passive: true, capture: true }
      );
    }
    if (platformConfig.disableContainerDrag) {
      self.container.draggable = false;
    }
  }
  function bindVideoLifecycleEvents(ctx) {
    const { self, overlayView, add } = ctx;
    const safeSetCanPlay = async () => {
      try {
        await self.setCanPlay();
      } catch (err) {
        debug.log("[VOT] setCanPlay() failed", err);
      }
    };
    let setCanPlayQueued = false;
    const queueSetCanPlay = () => {
      if (setCanPlayQueued) return;
      setCanPlayQueued = true;
      queueMicrotask(async () => {
        setCanPlayQueued = false;
        await safeSetCanPlay();
      });
    };
    add(self.video, "canplay", () => {
      if (self.site.host === "rutube" && self.video.src) return;
      queueSetCanPlay();
    });
    for (const eventName of [
      "loadedmetadata",
      "loadeddata",
      "play",
      "playing"
    ]) {
      add(self.video, eventName, () => {
        if (eventName === "play" || eventName === "playing") {
          console.log("[VOT][source-audio] play event received", {
            eventName,
            host: self.site.host,
            paused: self.video.paused,
            currentTime: Number(self.video.currentTime.toFixed(3)),
            readyState: self.video.readyState,
            src: self.video.currentSrc || self.video.src || ""
          });
          const sourceAudioState = self.syncSourceAudioAvailabilityUi({
            forceVisible: true
          });
          if (sourceAudioState.ready && self.data?.autoTranslate && self.firstPlay && !self.hasActiveSource()) {
            console.log(
              "[VOT][source-audio] retry auto-translate after playback",
              {
                eventName,
                kind: sourceAudioState.kind,
                audioDetected: sourceAudioState.audioDetected,
                detectionSource: sourceAudioState.detectionSource
              }
            );
            void self.translationOrchestrator.runAutoTranslationIfEligible().catch((error2) => {
              debug.log(
                "[VOT] Failed to retry auto-translate after playback start",
                error2
              );
            });
          }
        }
        queueSetCanPlay();
      });
    }
    const handleVideoEmptied = async () => {
      let videoId;
      try {
        videoId = self.site.host === "youtube" && isMobileYouTubeAdditionalData(self.site.additionalData) ? YoutubeHelper2.getCurrentVideoId() : await getVideoID(self.site, {
          fetchFn: GM_fetch,
          video: self.video
        });
      } catch (error2) {
        debug.log("[VOT] Failed to resolve video id on emptied", error2);
      }
      if (self.videoData && videoId && videoId === self.videoData.videoId) {
        return;
      }
      if (self.site.host === "custom" && getTunnelPlayerContext()) {
        debug.log("[VOT][custom][tunnel] ignore video emptied");
        return;
      }
      debug.log("lipsync mode is emptied");
      if (isMobileYouTubeLikeSite(self.site)) {
        logMobileOverlay("video emptied; start grace period", {
          currentVideoId: self.videoData?.videoId,
          resolvedVideoId: videoId,
          pageKey: getMobileYouTubePageKey()
        });
        resetLifecycleTranslation(self, {
          clearVideoData: true
        });
        closeOverlayMenu(overlayView);
        scheduleMobileYouTubeLifecycleRetry(
          self,
          overlayView,
          "video-emptied",
          () => {
            resetAndHideLifecycle(self, overlayView, {
              clearVideoData: true,
              hideMenu: true
            });
          }
        );
        return;
      }
      resetAndHideLifecycle(self, overlayView, {
        clearVideoData: true,
        hideMenu: true
      });
    };
    add(self.video, "emptied", () => {
      void handleVideoEmptied().catch((error2) => {
        debug.log("[VOT] Failed to handle emptied lifecycle event", error2);
      });
    });
    if (!isMuteSyncDisabledHost(self.site.host)) {
      add(self.video, "volumechange", () => {
        self.syncVideoVolumeSlider();
        const activeOverlayView = self.uiManager.votOverlayView;
        if (!activeOverlayView?.isInitialized()) return;
        const videoPercent = toPercentInt(
          activeOverlayView.videoVolumeSlider.value
        );
        syncAudioTranslationVolumeFromVideo(self, videoPercent, {
          skipYouTubeLikeHosts: true
        });
      });
    }
    if (self.site.host === "youtube" && !self.site.additionalData) {
      add(document, "yt-page-data-updated", () => {
        debug.log("yt-page-data-updated");
        if (!globalThis.location.pathname.startsWith("/shorts/")) return;
        queueSetCanPlay();
      });
    }
  }
  function initExtraEvents() {
    const overlayView = this.uiManager.votOverlayView;
    if (!overlayView?.subtitlesSelect) return;
    const { add, addMany } = createScopedListeners(this.abortController.signal);
    const ctx = {
      self: this,
      overlayView,
      platformConfig: getPlatformEventConfig(this.site.host),
      add,
      addMany
    };
    bindOverlayLayoutEvents(ctx);
    bindYouTubeVolumeSync(ctx);
    bindAudioTrackLanguageSync(ctx);
    bindGlobalDismissAndHotkeys(ctx);
    bindVideoLifecycleEvents(ctx);
  }
  function rebindOverlayVisibilityTargets() {
    this.overlayVisibilityTargetsAbortController?.abort();
    this.overlayVisibilityTargetsAbortController = new AbortController();
    const { signal } = this.overlayVisibilityTargetsAbortController;
    const overlayButton = this.uiManager?.votOverlayView?.votButton?.container;
    const overlayMenu = this.uiManager?.votOverlayView?.votMenu?.container;
    if (!overlayButton || !overlayMenu || !this.overlayVisibility) return;
    const overlayVisibility = this.overlayVisibility;
    const { addMany } = createScopedListeners(signal);
    bindOverlayHoverFocusEvents(addMany, overlayButton, overlayVisibility);
    bindOverlayHoverFocusEvents(addMany, overlayMenu, overlayVisibility);
  }
  function isOverlayInteractiveNode(node) {
    if (!(node instanceof Node)) return false;
    const overlayView = this.uiManager?.votOverlayView;
    const buttonContainer = overlayView?.votButton?.container;
    const menuContainer = overlayView?.votMenu?.container;
    return buttonContainer instanceof Node && buttonContainer.contains(node) || menuContainer instanceof Node && menuContainer.contains(node);
  }
  function getAutoHideDelay() {
    if (isVkLikeSiteHost(this.site.host)) {
      return 6e4;
    }
    const delay = this.data?.autoHideButtonDelay;
    const resolvedDelay = typeof delay === "number" && Number.isFinite(delay) ? delay : defaultAutoHideDelay;
    if (isMobileYouTubeLikeSite(this.site)) {
      return Math.max(resolvedDelay, MOBILE_YOUTUBE_MIN_AUTO_HIDE_DELAY_MS);
    }
    return resolvedDelay;
  }
  function releaseExtraEvents() {
    clearMobileYouTubeLifecycleDebounce(this);
    logMobileOverlay("release extra events / disconnect observers", {
      hasResizeObserver: Boolean(this.resizeObserver),
      hasOverlayMountObserver: Boolean(this.overlayMountObserver),
      hasSyncVolumeObserver: Boolean(this.syncVolumeObserver)
    });
    this.resizeObserver?.disconnect();
    this.overlayVisibilityTargetsAbortController?.abort();
    this.overlayVisibilityTargetsAbortController = void 0;
    this.overlayMountObserver?.disconnect();
    this.overlayMountObserver = void 0;
    if (isDesktopYouTubeLikeSite(this.site)) {
      this.syncVolumeObserver?.disconnect();
    }
  }
  let countryCode;
  function setCountryCode(next) {
    countryCode = next;
  }
  let countryCodeRequestInFlight = null;
  async function ensureCountryCode() {
    if (countryCode) {
      return;
    }
    countryCodeRequestInFlight ??= (async () => {
      try {
        const response = await GM_fetch(
          "https://cloudflare-dns.com/cdn-cgi/trace",
          {
            timeout: 7e3
          }
        );
        const trace = await response.text();
        const loc = trace.split("\n").find((line) => line.startsWith("loc="));
        setCountryCode(loc?.slice(4, 6).toUpperCase());
      } catch (err) {
        console.error("[VOT] Error getting country:", err);
      }
    })().finally(() => {
      countryCodeRequestInFlight = null;
    });
    await countryCodeRequestInFlight;
  }
  async function init() {
    if (this.initialized) return;
    const audioContextSupported = this.isAudioContextSupported;
    this.data = await votStorage.getValues({
      autoTranslate: false,
      autoSubtitles: false,
      dontTranslateLanguages: [calculatedResLang],
      enabledDontTranslateLanguages: true,
      enabledAutoVolume: true,
      enabledSmartDucking: true,
      autoVolume: defaultAutoVolume,
      buttonPos: "default",
      showVideoSlider: true,
      syncVolume: false,
      downloadWithName: isSupportGMXhr,
      sendNotifyOnComplete: false,
      subtitlesMaxLength: 300,
      subtitlesSmartLayout: true,
      highlightWords: true,
      subtitlesFontSize: 20,
      subtitlesFontFamily: "default-sans",
      subtitlesOpacity: 20,
      subtitlesDownloadFormat: "srt",
      responseLanguage: calculatedResLang,
      defaultVolume: 100,
      onlyBypassMediaCSP: audioContextSupported,
      newAudioPlayer: audioContextSupported,
      showPiPButton: false,
      translateAPIErrors: true,
      translationService: defaultTranslationService,
      detectService: defaultDetectService,
      translationHotkey: null,
      subtitlesHotkey: null,
      m3u8ProxyHost,
      proxyWorkerHost,
      translateProxyEnabled: 0,
      translateProxyEnabledDefault: true,
      audioBooster: false,
      useLivelyVoice: false,
      autoHideButtonDelay: defaultAutoHideDelay,
useAudioDownload: isSupportGMXhr,
      compatVersion: "",
      account: {},
      localeHash: "",
      localeUpdatedAt: 0
    });
    if (this.data.compatVersion !== actualCompatVersion) {
      this.data = await updateConfig(this.data);
      await votStorage.set("compatVersion", actualCompatVersion);
    }
    try {
      if (calculatedResLang === "en" && this.data?.enabledDontTranslateLanguages && Array.isArray(this.data?.dontTranslateLanguages) && this.data.dontTranslateLanguages.length === 1 && this.data.dontTranslateLanguages[0] === "en" && typeof this.data.responseLanguage === "string" && this.data.responseLanguage !== "en") {
        const responseLang2 = this.data.responseLanguage;
        this.data.dontTranslateLanguages = [responseLang2];
        await votStorage.set(
          "dontTranslateLanguages",
          this.data.dontTranslateLanguages
        );
      }
    } catch {
    }
    this.uiManager.data = this.data;
    console.log("[VOT] data from db:", this.data);
    if (!this.data.translateProxyEnabled && isProxyOnlyExtension) {
      this.data.translateProxyEnabled = 1;
    }
    await ensureCountryCode();
    if (countryCode && proxyOnlyCountries.includes(countryCode) && this.data.translateProxyEnabledDefault) {
      this.data.translateProxyEnabled = 2;
    }
    debug.log(
      "translateProxyEnabled",
      this.data.translateProxyEnabled,
      this.data.translateProxyEnabledDefault
    );
    debug.log("Extension compatibility passed...");
    await this.initVOTClient();
    this.uiManager.initUI();
    this.uiManager.initUIEvents();
    if (isMobileYouTubeLikeSite(this.site)) {
      this.uiManager.votOverlayView?.updateButtonOpacity(1);
      if (this.uiManager.votOverlayView?.votButton?.container) {
        this.uiManager.votOverlayView.votButton.container.hidden = false;
      }
      this.overlayVisibility?.queueAutoHide();
    }
    this.translateToLang = this.data.responseLanguage ?? "ru";
    this.initExtraEvents();
    if (this.site.host === "custom") {
      this.overlayVisibility?.queueAutoHide();
    }
    this.initialized = true;
  }
  function timeout(ms, message = "Operation timed out") {
    return new Promise(
      (_2, reject) => setTimeout(() => reject(new Error(message)), ms)
    );
  }
  const DEFAULT_CACHE_LOCALE = "und";
  const wordSegmenterCache = new Map();
  const sentenceSegmenterCache = new Map();
  const canonicalizeLocale = (locale) => {
    if (!locale) return void 0;
    try {
      return Intl.getCanonicalLocales(locale)[0];
    } catch {
      return void 0;
    }
  };
  const resolveSegmenterLocale = (locale) => {
    const canonicalLocale = canonicalizeLocale(locale);
    if (!canonicalLocale) return void 0;
    return Intl.Segmenter.supportedLocalesOf([canonicalLocale])[0];
  };
  const getSegmenter = (locale, granularity) => {
    const resolvedLocale = resolveSegmenterLocale(locale);
    const cacheKey2 = `${granularity}:${resolvedLocale ?? DEFAULT_CACHE_LOCALE}`;
    const segmenterCache = granularity === "sentence" ? sentenceSegmenterCache : wordSegmenterCache;
    const cached = segmenterCache.get(cacheKey2);
    if (cached) return cached;
    const segmenter = new Intl.Segmenter(resolvedLocale, { granularity });
    segmenterCache.set(cacheKey2, segmenter);
    return segmenter;
  };
  const segmentText = (text, locale) => {
    if (!text) return [];
    return Array.from(getSegmenter(locale, "word").segment(text), (part) => ({
      text: part.segment,
      index: part.index,
      isWordLike: Boolean(part.isWordLike)
    }));
  };
  const segmentSentences = (text, locale) => {
    if (!text) return [];
    return Array.from(getSegmenter(locale, "sentence").segment(text), (part) => ({
      text: part.segment,
      index: part.index
    }));
  };
  const WHITESPACE_RE = /\s/u;
  const NO_SPACE_BEFORE_RE = /^[,.:;!?%)\]}>В»]/u;
  const NO_SPACE_AFTER_RE = /[([{'"В«вЂћ-]$/u;
  const CJK_CHAR_RE = /[\p{Script=Han}\p{Script=Hiragana}\p{Script=Katakana}\p{Script=Hangul}]/u;
  const hasVisibleText = (value) => Boolean(value.trim());
  const shouldInsertSpaceBetween = (leftText, rightText) => {
    const leftLastChar = leftText.at(-1) ?? "";
    const rightFirstChar = rightText[0] ?? "";
    if (!leftLastChar || !rightFirstChar) return false;
    if (WHITESPACE_RE.test(leftLastChar) || WHITESPACE_RE.test(rightFirstChar)) {
      return false;
    }
    if (NO_SPACE_AFTER_RE.test(leftText) || NO_SPACE_BEFORE_RE.test(rightText)) {
      return false;
    }
    if (CJK_CHAR_RE.test(leftLastChar) && CJK_CHAR_RE.test(rightFirstChar)) {
      return false;
    }
    return true;
  };
  const buildNormalizedLineSpans = (lines) => {
    let streamText = "";
    let previousText = "";
    const spans = [];
    for (const line of lines) {
      const normalizedText = line.text.trim();
      if (!normalizedText) continue;
      if (streamText && shouldInsertSpaceBetween(previousText, normalizedText)) {
        streamText += " ";
      }
      const start = streamText.length;
      streamText += normalizedText;
      const end = streamText.length;
      spans.push({
        line: {
          ...line,
          text: normalizedText
        },
        text: normalizedText,
        start,
        end
      });
      previousText = normalizedText;
    }
    return { streamText, spans };
  };
  const trimSentenceRange = (streamText, start, end) => {
    let trimmedStart = start;
    let trimmedEnd = end;
    while (trimmedStart < trimmedEnd && WHITESPACE_RE.test(streamText[trimmedStart] ?? "")) {
      trimmedStart += 1;
    }
    while (trimmedEnd > trimmedStart && WHITESPACE_RE.test(streamText[trimmedEnd - 1] ?? "")) {
      trimmedEnd -= 1;
    }
    if (trimmedStart >= trimmedEnd) {
      return null;
    }
    return {
      start: trimmedStart,
      end: trimmedEnd
    };
  };
  const sliceLineToken = (span, rangeStart, rangeEnd) => {
    const overlapStart = Math.max(rangeStart, span.start);
    const overlapEnd = Math.min(rangeEnd, span.end);
    if (overlapStart >= overlapEnd) {
      return null;
    }
    const localStart = overlapStart - span.start;
    const localEnd = overlapEnd - span.start;
    const text = span.text.slice(localStart, localEnd);
    if (!text) {
      return null;
    }
    const textLength = Math.max(span.text.length, 1);
    const startMs = span.line.startMs + Math.round(span.line.durationMs * localStart / textLength);
    const endMs = span.line.startMs + Math.round(span.line.durationMs * localEnd / textLength);
    return {
      text,
      startMs,
      durationMs: Math.max(0, endMs - startMs),
      isWordLike: hasVisibleText(text)
    };
  };
  const buildSentenceLine = (streamText, spans, segment) => {
    const rawStart = segment.index;
    const rawEnd = rawStart + segment.text.length;
    const trimmedRange = trimSentenceRange(streamText, rawStart, rawEnd);
    if (!trimmedRange) {
      return null;
    }
    const text = streamText.slice(trimmedRange.start, trimmedRange.end);
    if (!hasVisibleText(text)) {
      return null;
    }
    const tokens = [];
    let speakerId = "0";
    for (const span of spans) {
      const token = sliceLineToken(span, trimmedRange.start, trimmedRange.end);
      if (!token) continue;
      if (tokens.length === 0) {
        speakerId = span.line.speakerId;
      }
      tokens.push(token);
    }
    if (!tokens.length) {
      return null;
    }
    const startMs = Math.min(...tokens.map((token) => token.startMs));
    const endMs = Math.max(
      ...tokens.map((token) => token.startMs + Math.max(0, token.durationMs))
    );
    return {
      text,
      startMs,
      durationMs: Math.max(0, endMs - startMs),
      speakerId,
      tokens
    };
  };
  const mergeAutoGeneratedSubtitleLines = (lines, locale) => {
    const { streamText, spans } = buildNormalizedLineSpans(lines);
    if (!streamText || !spans.length) {
      return [];
    }
    const segments = segmentSentences(streamText, locale);
    const sentenceLines = segments.map((segment) => buildSentenceLine(streamText, spans, segment)).filter((line) => line !== null);
    return sentenceLines.length ? sentenceLines : spans.map(({ line }) => line);
  };
  const isSubtitleFormat = (value) => value === "json" || value === "srt" || value === "vtt" || value === "ass";
  const isRecord$1 = (value) => Boolean(value && typeof value === "object");
  const toFiniteNumber = (value) => typeof value === "number" && Number.isFinite(value) ? value : 0;
  const toNonNegativeNumber = (value) => Math.max(0, toFiniteNumber(value));
  const isSubtitleDescriptor = (arg) => {
    if (!isRecord$1(arg)) return false;
    return typeof arg.source === "string" && isSubtitleFormat(arg.format) && typeof arg.language === "string" && typeof arg.url === "string" && (arg.translatedFromLanguage === void 0 || typeof arg.translatedFromLanguage === "string") && (arg.isAutoGenerated === void 0 || typeof arg.isAutoGenerated === "boolean");
  };
  const pickDescriptorFromVideoData = (videoData2, requestLang, spokenLang) => {
    const list = videoData2.subtitles;
    if (!Array.isArray(list) || list.length === 0) return null;
    const desiredLang = requestLang ?? spokenLang;
    if (desiredLang) {
      const translated = list.find(
        (subtitle) => subtitle.language === desiredLang && typeof subtitle.translatedFromLanguage === "string"
      );
      if (translated) return translated;
      const original = list.find((subtitle) => subtitle.language === desiredLang);
      if (original) return original;
    }
    return list[0] ?? null;
  };
  const normalizeSubtitlesVideoUrl = (rawUrl) => {
    try {
      return new URL(rawUrl, globalThis.location.href).toString();
    } catch {
      return String(rawUrl || "");
    }
  };
  const isCrossOriginDirectMediaUrl = (client, rawUrl) => {
    const normalizedUrl = normalizeSubtitlesVideoUrl(rawUrl);
    if (!normalizedUrl) {
      return false;
    }
    try {
      if (typeof client.isDirectMediaUrl === "function") {
        if (!client.isDirectMediaUrl(normalizedUrl)) {
          return false;
        }
      } else {
        return false;
      }
      return new URL(normalizedUrl).hostname !== globalThis.location.hostname;
    } catch {
      return false;
    }
  };
  const shouldPreferYandexSubtitlesFlow = (client, videoData2) => {
    try {
      const host = String(videoData2?.host || globalThis.location?.hostname || "");
      if (host === "custom") {
        return true;
      }
      return isCrossOriginDirectMediaUrl(client, videoData2?.url);
    } catch {
      return true;
    }
  };
  const appendYoutubePoTokenParams = (inputUrl) => {
    const poToken = YoutubeHelper$1.getPoToken();
    if (!poToken) return inputUrl;
    let normalizedPoToken = poToken;
    for (let i2 = 0; i2 < 10; i2 += 1) {
      let nextValue;
      try {
        nextValue = decodeURIComponent(normalizedPoToken);
      } catch {
        break;
      }
      if (nextValue === normalizedPoToken) {
        break;
      }
      normalizedPoToken = nextValue;
    }
    const deviceParamsRaw = YoutubeHelper$1.getDeviceParams();
    const normalizedDeviceParams = typeof deviceParamsRaw === "string" ? deviceParamsRaw.replace(/^[?&]+/u, "") : "";
    try {
      const parsed = new URL(inputUrl);
      parsed.searchParams.set("potc", "1");
      parsed.searchParams.set("pot", normalizedPoToken);
      if (normalizedDeviceParams) {
        const deviceParams = new URLSearchParams(normalizedDeviceParams);
        for (const [key, value] of deviceParams.entries()) {
          parsed.searchParams.set(key, value);
        }
      }
      return parsed.toString();
    } catch {
      const separator = inputUrl.includes("?") ? "&" : "?";
      const serializedDeviceParams = normalizedDeviceParams ? `&${normalizedDeviceParams}` : "";
      return `${inputUrl}${separator}potc=1&pot=${encodeURIComponent(normalizedPoToken)}${serializedDeviceParams}`;
    }
  };
  const compareNumbers = (left, right) => left - right;
  const compareStrings = (left, right) => {
    if (left < right) return -1;
    if (left > right) return 1;
    return 0;
  };
  const compareRankArrays = (left, right) => {
    const length = Math.min(left.length, right.length);
    for (let i2 = 0; i2 < length; i2 += 1) {
      const diff = left[i2] - right[i2];
      if (diff !== 0) return diff;
    }
    if (left.length !== right.length) {
      return left.length - right.length;
    }
    return 0;
  };
  const getYandexTranslationKindRank = (isYandex, requestLanguageRank, isTranslated) => {
    if (!isYandex) return 0;
    if (requestLanguageRank === 0) {
      return isTranslated ? 1 : 0;
    }
    return isTranslated ? 0 : 1;
  };
  const getTranslatedFromRequestRank = (isYandex, isTranslated, translatedFromLanguage, requestLang) => {
    if (!isYandex || !isTranslated) return 0;
    return translatedFromLanguage === requestLang ? 0 : 1;
  };
  const buildSubtitleRank = (descriptor, requestLang) => {
    const isYandex = descriptor.source === "yandex";
    const sourceRank = isYandex ? 0 : 1;
    const uiLanguageRank = descriptor.language === lang ? 0 : 1;
    const isTranslated = Boolean(descriptor.translatedFromLanguage);
    const requestLanguageRank = requestLang && descriptor.language === requestLang ? 0 : 1;
    const translationKindRank = getYandexTranslationKindRank(
      isYandex,
      requestLanguageRank,
      isTranslated
    );
    const translatedFromRequestRank = getTranslatedFromRequestRank(
      isYandex,
      isTranslated,
      descriptor.translatedFromLanguage,
      requestLang
    );
    const originalRequestLanguageRank = isYandex && !isTranslated ? requestLanguageRank : 0;
    const nonYandexAutogeneratedRank = isYandex ? 0 : Number(Boolean(descriptor.isAutoGenerated));
    return [
      sourceRank,
      uiLanguageRank,
      translationKindRank,
      translatedFromRequestRank,
      originalRequestLanguageRank,
      nonYandexAutogeneratedRank
    ];
  };
  const rankSubtitle = (descriptor, index, requestLang) => ({
    descriptor,
    index,
    rank: buildSubtitleRank(descriptor, requestLang),
    language: descriptor.language,
    translatedFromLanguage: descriptor.translatedFromLanguage ?? "",
    source: descriptor.source,
    url: descriptor.url,
    isAutoGenerated: Number(Boolean(descriptor.isAutoGenerated))
  });
  const sortSubtitles = (subtitles, requestLang) => {
    const ranked = subtitles.map(
      (descriptor, index) => rankSubtitle(descriptor, index, requestLang)
    );
    ranked.sort((left, right) => {
      const rankDiff = compareRankArrays(left.rank, right.rank);
      if (rankDiff !== 0) return rankDiff;
      const descriptorOrder = compareStrings(left.language, right.language) || compareStrings(
        left.translatedFromLanguage,
        right.translatedFromLanguage
      ) || compareStrings(left.source, right.source) || compareStrings(left.url, right.url) || compareNumbers(left.isAutoGenerated, right.isAutoGenerated);
      if (descriptorOrder !== 0) return descriptorOrder;
      return compareNumbers(left.index, right.index);
    });
    return ranked.map((entry) => entry.descriptor);
  };
  const resolveTokenWordLike = (value, text) => {
    if (typeof value === "boolean") return value;
    return Boolean(text.trim());
  };
  const sanitizeToken = (token) => {
    if (!token || typeof token !== "object") {
      return {
        text: "",
        startMs: 0,
        durationMs: 0,
        isWordLike: false
      };
    }
    const raw = token;
    const text = typeof raw.text === "string" ? raw.text : "";
    return {
      text,
      startMs: toNonNegativeNumber(raw.startMs),
      durationMs: toNonNegativeNumber(raw.durationMs),
      isWordLike: resolveTokenWordLike(raw.isWordLike, text)
    };
  };
  const sanitizeLine = (line) => {
    if (!line || typeof line !== "object") {
      return {
        text: "",
        startMs: 0,
        durationMs: 0,
        speakerId: "0",
        tokens: []
      };
    }
    const raw = line;
    const tokens = Array.isArray(raw.tokens) ? raw.tokens.map(sanitizeToken) : [];
    return {
      text: typeof raw.text === "string" ? raw.text : "",
      startMs: toNonNegativeNumber(raw.startMs),
      durationMs: toNonNegativeNumber(raw.durationMs),
      speakerId: typeof raw.speakerId === "string" ? raw.speakerId : "0",
      tokens
    };
  };
  const ensureProcessedSubtitles = (input) => {
    if (!input || typeof input !== "object") {
      return { format: "json", subtitles: [] };
    }
    const payload = input;
    const subtitles = Array.isArray(payload.subtitles) ? payload.subtitles.map(sanitizeLine) : [];
    return { format: payload.format ?? "json", subtitles };
  };
  const VK_INLINE_TIMESTAMP_RE = /<(?:\d{1,2}:)?\d{2}:\d{2}(?:[.,]\d{1,3})?>/gu;
  const VK_DUPLICATE_MAX_GAP_MS = 120;
  const VK_PUNCTUATION_SPACING_RE = /\s+([,.:;!?])/gu;
  const VK_AROUND_NEWLINE_SPACING_RE = /[ \t]*\n[ \t]*/gu;
  const VK_MULTIPLE_SPACES_RE = /[ \t]{2,}/gu;
  const VK_EXCESS_NEWLINES_RE = /\n{3,}/gu;
  const VK_COMPARABLE_SPACING_RE = /\s+/gu;
  const stripHtmlToText = (value) => {
    if (!value.includes("<")) return value;
    if (typeof DOMParser !== "undefined") {
      const doc = new DOMParser().parseFromString(value, "text/html");
      return doc.body.textContent ?? "";
    }
    return value.replaceAll(/<\/?[^>]+>/gu, "");
  };
  const normalizeSubtitleText = (value) => {
    let normalized = value;
    if (normalized.includes("<")) {
      normalized = stripHtmlToText(
        normalized.replaceAll(VK_INLINE_TIMESTAMP_RE, "")
      );
    }
    return normalized.replaceAll(VK_PUNCTUATION_SPACING_RE, "$1").replaceAll(VK_AROUND_NEWLINE_SPACING_RE, "\n").replaceAll(VK_MULTIPLE_SPACES_RE, " ").replaceAll(VK_EXCESS_NEWLINES_RE, "\n\n").trim();
  };
  const normalizeComparableText = (value) => value.toLowerCase().replaceAll(VK_COMPARABLE_SPACING_RE, " ").trim();
  const getYoutubeEventDurationMs = (event, nextEvent) => {
    if (!nextEvent) return Math.max(0, event.dDurationMs);
    if (event.tStartMs + event.dDurationMs <= nextEvent.tStartMs) {
      return Math.max(0, event.dDurationMs);
    }
    return Math.max(0, nextEvent.tStartMs - event.tStartMs);
  };
  const buildYoutubeSourceTokens = (event, segs, durationMs) => {
    const sourceTokens = [];
    let text = "";
    let remainingDuration = durationMs;
    for (let j = 0; j < segs.length; j += 1) {
      const segment = segs[j];
      const rawText = typeof segment.utf8 === "string" ? segment.utf8 : "";
      if (!rawText) continue;
      const offset = Math.max(0, segment.tOffsetMs ?? 0);
      let segmentDuration = durationMs;
      const nextSegment = segs[j + 1];
      if (nextSegment?.tOffsetMs !== void 0) {
        const nextOffset = Math.max(offset, nextSegment.tOffsetMs);
        segmentDuration = Math.max(0, nextOffset - offset);
        remainingDuration = Math.max(remainingDuration - segmentDuration, 0);
      }
      let tokenDuration = Math.max(0, remainingDuration);
      if (nextSegment) {
        tokenDuration = Math.max(0, segmentDuration);
      }
      sourceTokens.push({
        text: rawText,
        startMs: event.tStartMs + offset,
        durationMs: tokenDuration,
        isWordLike: Boolean(rawText.trim())
      });
      text += rawText;
    }
    return { text, sourceTokens };
  };
  const hasPositiveDuration = (token) => token.durationMs > 0;
  const normalizeLineText = (line) => {
    if (line.text) return normalizeSubtitleTextForDisplay(line.text);
    if (!line.tokens.length) return "";
    return normalizeSubtitleTextForDisplay(
      line.tokens.map((token) => token.text).join("")
    );
  };
  const allocateTimingsByLength = (texts, startMs, durationMs) => {
    if (!texts.length) return [];
    const safeDuration = Math.max(0, durationMs);
    const weights = texts.map((text) => Math.max(text.length, 1));
    const totalWeight = weights.reduce((sum, weight) => sum + weight, 0);
    const prefixWeights = new Array(weights.length + 1).fill(0);
    for (let i2 = 0; i2 < weights.length; i2 += 1) {
      prefixWeights[i2 + 1] = prefixWeights[i2] + weights[i2];
    }
    const timings = [];
    for (let i2 = 0; i2 < texts.length; i2 += 1) {
      const from = Math.round(safeDuration * prefixWeights[i2] / totalWeight);
      const to = i2 === texts.length - 1 ? safeDuration : Math.round(safeDuration * prefixWeights[i2 + 1] / totalWeight);
      timings.push({
        startMs: startMs + from,
        durationMs: Math.max(0, to - from)
      });
    }
    return timings;
  };
  const splitSegmentText = (text) => {
    const slices = [];
    let sliceStart = 0;
    for (let index = 0; index < text.length; index += 1) {
      if (text[index] !== "\n") continue;
      if (sliceStart < index) {
        slices.push({
          text: text.slice(sliceStart, index),
          startOffset: sliceStart,
          endOffset: index
        });
      }
      slices.push({
        text: "\n",
        startOffset: index,
        endOffset: index + 1
      });
      sliceStart = index + 1;
    }
    if (sliceStart < text.length) {
      slices.push({
        text: text.slice(sliceStart),
        startOffset: sliceStart,
        endOffset: text.length
      });
    }
    return slices.length ? slices : [
      {
        text,
        startOffset: 0,
        endOffset: text.length
      }
    ];
  };
  const collectSourceTimedWords = (sourceTokens, locale) => {
    const timedWords = [];
    for (const token of sourceTokens) {
      const normalizedTokenText = normalizeSubtitleTextForDisplay(token.text);
      if (!normalizedTokenText || !hasPositiveDuration(token)) continue;
      const segmentedWords = segmentText(normalizedTokenText, locale).filter(
        (segment) => segment.isWordLike && segment.text.trim()
      );
      if (!segmentedWords.length) continue;
      const segmentTimings = allocateTimingsByLength(
        segmentedWords.map((segment) => segment.text),
        token.startMs,
        token.durationMs
      );
      timedWords.push(...segmentTimings);
    }
    return timedWords;
  };
  const buildLineTokens = (line, descriptor, lineText) => {
    if (!lineText) return [];
    const locale = descriptor.language;
    const styledSpans = line.metadata?.styledSpans ?? buildStyledDisplayModel(line.metadata?.rawText ?? line.text ?? lineText).styledSpans;
    const segments = segmentText(lineText, locale);
    if (!segments.length) return [];
    const baseTimings = allocateTimingsByLength(
      segments.map((segment) => segment.text),
      line.startMs,
      line.durationMs
    );
    const nextTokens = [];
    for (let index = 0; index < segments.length; index += 1) {
      const segment = segments[index];
      const segmentStartMs = baseTimings[index]?.startMs ?? line.startMs;
      const segmentDurationMs = baseTimings[index]?.durationMs ?? 0;
      const slices = splitSegmentText(segment.text);
      if (slices.length === 1 && slices[0].text === segment.text) {
        nextTokens.push({
          text: segment.text,
          startMs: segmentStartMs,
          durationMs: segmentDurationMs,
          isWordLike: segment.isWordLike,
          style: getStyleForRange(
            styledSpans,
            segment.index,
            segment.index + segment.text.length
          )
        });
        continue;
      }
      const sliceTimings = allocateTimingsByLength(
        slices.map((slice) => slice.text === "\n" ? "" : slice.text),
        segmentStartMs,
        segmentDurationMs
      );
      for (let sliceIndex = 0; sliceIndex < slices.length; sliceIndex += 1) {
        const slice = slices[sliceIndex];
        const isLineBreak = slice.text === "\n";
        nextTokens.push({
          text: slice.text,
          startMs: sliceTimings[sliceIndex]?.startMs ?? segmentStartMs,
          durationMs: isLineBreak ? 0 : sliceTimings[sliceIndex]?.durationMs ?? 0,
          isWordLike: !isLineBreak && segment.isWordLike,
          style: isLineBreak ? void 0 : getStyleForRange(
            styledSpans,
            segment.index + slice.startOffset,
            segment.index + slice.endOffset
          )
        });
      }
    }
    const sourceTimedWords = collectSourceTimedWords(line.tokens, locale);
    if (!sourceTimedWords.length) return nextTokens;
    const wordIndices = nextTokens.reduce((indices, token, index) => {
      if (token.isWordLike && token.text.trim()) indices.push(index);
      return indices;
    }, []);
    if (!wordIndices.length) return nextTokens;
    const totalTargetWords = wordIndices.length;
    for (let i2 = 0; i2 < totalTargetWords; i2 += 1) {
      const targetIndex = wordIndices[i2];
      const sourceIndex = Math.floor(
        i2 * sourceTimedWords.length / totalTargetWords
      );
      const sourceTiming = sourceTimedWords[Math.min(sourceIndex, sourceTimedWords.length - 1)];
      nextTokens[targetIndex] = {
        ...nextTokens[targetIndex],
        startMs: sourceTiming.startMs,
        durationMs: sourceTiming.durationMs
      };
    }
    return nextTokens;
  };
  const fetchRawSubtitles = async (url, format) => {
    const response = await GM_fetch(url, { timeout: 7e3 });
    if (format === "vtt" || format === "srt" || format === "ass") {
      const text = await response.text();
      return parseSubtitleText(text, format);
    }
    return response.json();
  };
  const normalizeFetchedSubtitles = (rawSubtitles, descriptor) => {
    if (descriptor.source === "youtube") {
      return SubtitlesProcessor.formatYoutubeSubtitles(
        rawSubtitles,
        Boolean(descriptor.isAutoGenerated)
      );
    }
    if (descriptor.format === "srt" || descriptor.format === "vtt" || descriptor.format === "ass") {
      return sortProcessedSubtitles(ensureProcessedSubtitles(rawSubtitles));
    }
    const normalized = ensureProcessedSubtitles(rawSubtitles);
    if (descriptor.source === "vk" && descriptor.format === "json") {
      return SubtitlesProcessor.cleanJsonSubtitles(normalized);
    }
    return sortProcessedSubtitles(normalized);
  };
  const processFetchedSubtitles = (subtitles, descriptor) => {
    const maybeMerged = SubtitlesProcessor.autoMerge(subtitles, descriptor);
    return {
      ...maybeMerged,
      subtitles: SubtitlesProcessor.processTokens(maybeMerged, descriptor)
    };
  };
  const buildYandexSubtitles = (response) => {
    const subtitles = [];
    const seenOriginal = new Set();
    for (const subtitle of response.subtitles ?? []) {
      if (subtitle.language && !seenOriginal.has(subtitle.language)) {
        seenOriginal.add(subtitle.language);
        subtitles.push({
          source: "yandex",
          format: "json",
          language: subtitle.language,
          url: subtitle.url
        });
      }
      if (!subtitle.translatedLanguage) continue;
      subtitles.push({
        source: "yandex",
        format: "json",
        language: subtitle.translatedLanguage,
        translatedFromLanguage: subtitle.language,
        url: subtitle.translatedUrl ?? subtitle.url
      });
    }
    return subtitles;
  };
  const SubtitlesProcessor = {
    processTokens(subtitles, descriptor) {
      const lines = [];
      for (const line of subtitles.subtitles) {
        const text = normalizeLineText(line);
        const tokens = buildLineTokens(line, descriptor, text);
        lines.push({
          ...line,
          text,
          tokens
        });
      }
      return lines;
    },
    formatYoutubeSubtitles(subtitles, isAsr = false) {
      const events = subtitles.events ?? [];
      if (!events.length) {
        console.error("[VOT] Invalid YouTube subtitles format:", subtitles);
        return { format: "json", subtitles: [] };
      }
      const processed = [];
      for (let i2 = 0; i2 < events.length; i2 += 1) {
        const event = events[i2];
        const segs = event.segs;
        if (!segs?.length) continue;
        const nextEvent = events[i2 + 1];
        const durationMs = getYoutubeEventDurationMs(event, nextEvent);
        const { text, sourceTokens } = buildYoutubeSourceTokens(
          event,
          segs,
          durationMs
        );
        const normalizedText = text.trim();
        if (!normalizedText) continue;
        processed.push({
          text: normalizedText,
          startMs: event.tStartMs,
          durationMs,
          speakerId: "0",
          tokens: isAsr ? sourceTokens : []
        });
      }
      return {
        format: "json",
        subtitles: processed
      };
    },
    autoMerge(subtitles, descriptor) {
      if (!descriptor.isAutoGenerated) {
        return subtitles;
      }
      if (subtitles.subtitles.length < 2 || !subtitles.subtitles.some((line) => line.tokens.length > 0)) {
        return subtitles;
      }
      return {
        ...subtitles,
        subtitles: mergeAutoGeneratedSubtitleLines(
          subtitles.subtitles,
          descriptor.language
        )
      };
    },
    cleanJsonSubtitles(subtitles) {
      const lineEndMs = (line) => line.startMs + Math.max(0, line.durationMs);
      const tokensTextLength = (tokens) => {
        let totalLength = 0;
        for (const token of tokens) {
          totalLength += token.text.length;
        }
        return totalLength;
      };
      const cleanedEntries = [];
      for (const line of subtitles.subtitles) {
        const normalizedLineText = normalizeSubtitleText(line.text);
        if (!normalizedLineText) continue;
        const normalizedTokens = [];
        for (const token of line.tokens) {
          const normalizedTokenText = normalizeSubtitleText(token.text);
          if (!normalizedTokenText) continue;
          normalizedTokens.push({
            ...token,
            text: normalizedTokenText
          });
        }
        cleanedEntries.push({
          line: {
            ...line,
            text: normalizedLineText,
            tokens: normalizedTokens
          },
          comparableText: normalizeComparableText(normalizedLineText),
          tokensTextLength: tokensTextLength(normalizedTokens)
        });
      }
      const mergedEntries = [];
      for (const entry of cleanedEntries) {
        const {
          line,
          comparableText: currentComparable,
          tokensTextLength: currentTokensLength
        } = entry;
        if (!currentComparable) continue;
        const previous = mergedEntries.at(-1);
        if (!previous) {
          mergedEntries.push(entry);
          continue;
        }
        const previousEnd = lineEndMs(previous.line);
        const currentEnd = lineEndMs(line);
        const isDuplicateText = previous.comparableText === currentComparable;
        const isNearPrevious = line.startMs <= previousEnd + VK_DUPLICATE_MAX_GAP_MS;
        if (!isDuplicateText || !isNearPrevious) {
          mergedEntries.push(entry);
          continue;
        }
        const mergedStart = Math.min(previous.line.startMs, line.startMs);
        const mergedEnd = Math.max(previousEnd, currentEnd);
        const mergedTokens = currentTokensLength >= previous.tokensTextLength ? line.tokens : previous.line.tokens;
        previous.line = {
          ...previous.line,
          startMs: mergedStart,
          durationMs: Math.max(0, mergedEnd - mergedStart),
          tokens: mergedTokens
        };
        previous.tokensTextLength = Math.max(
          previous.tokensTextLength,
          currentTokensLength
        );
      }
      return {
        ...subtitles,
        subtitles: mergedEntries.map(({ line }) => line)
      };
    },
    async fetchSubtitles(descriptorOrVideoData, requestLang, spokenLang) {
      const descriptor = isSubtitleDescriptor(descriptorOrVideoData) ? descriptorOrVideoData : pickDescriptorFromVideoData(
        descriptorOrVideoData,
        requestLang,
        spokenLang
      );
      if (!descriptor) {
        return { format: "json", subtitles: [] };
      }
      const { source, format } = descriptor;
      let { url } = descriptor;
      if (source === "youtube") {
        url = appendYoutubePoTokenParams(url);
      }
      try {
        const rawSubtitles = await fetchRawSubtitles(url, format);
        const normalized = normalizeFetchedSubtitles(rawSubtitles, descriptor);
        const subtitlesWithTokens = processFetchedSubtitles(
          normalized,
          descriptor
        );
        debug.log("[VOT] Processed subtitles:", subtitlesWithTokens);
        return subtitlesWithTokens;
      } catch (error2) {
        console.error("[VOT] Failed to process subtitles:", error2);
        return { format: "json", subtitles: [] };
      }
    },
    async getSubtitles(client, videoData2) {
      const {
        host,
        url,
        detectedLanguage: requestLang,
        videoId,
        duration,
        subtitles: extraSubtitles = []
      } = videoData2;
      try {
        const requestPayload = {
          videoData: {
            host,
            url,
            videoId,
            duration
          },
          requestLang
        };
        const extendedClient = client;
        const preferYandexSubtitlesFlow = shouldPreferYandexSubtitlesFlow(
          extendedClient,
          videoData2
        );
        const subtitlesRequest = preferYandexSubtitlesFlow && typeof extendedClient.getSubtitlesYAImpl === "function" ? extendedClient.getSubtitlesYAImpl({
          videoData: {
            ...videoData2,
            url: normalizeSubtitlesVideoUrl(videoData2.url)
          },
          requestLang,
          headers: {}
        }) : client.getSubtitles(requestPayload);
        const response = await Promise.race([
          subtitlesRequest,
          timeout(5e3, "Timeout")
        ]);
        const res = response;
        debug.log("[VOT] Subtitles response:", res);
        if (res.waiting) {
          console.error("[VOT] Failed to get Yandex subtitles");
        }
        const yandexSubs = buildYandexSubtitles(res);
        const all = [...yandexSubs, ...extraSubtitles];
        return sortSubtitles(all, requestLang);
      } catch (error2) {
        let message = "Error in getSubtitles function";
        if (error2 instanceof Error && error2.message === "Timeout") {
          message = "Failed to get Yandex subtitles: timeout";
        }
        console.error(`[VOT] ${message}`, error2);
        throw error2;
      }
    }
  };
  const AUDIO_SOURCE_PREFIX = "https://vtrans.s3-private.mds.yandex.net/tts/prod/";
  const AUDIO_PROXY_PATH_PREFIX = "/video-translation/audio-proxy/";
  const SUBTITLE_SOURCE_PREFIX = "https://brosubs.s3-private.mds.yandex.net/vtrans/";
  const SUBTITLE_PROXY_PATH_PREFIX = "/video-subtitles/subtitles-proxy/";
  function getProxyHost(host) {
    return host ?? proxyWorkerHost;
  }
  function isProxyRoutingEnabled(config2) {
    return config2.translateProxyEnabled === 2;
  }
  function proxifyYandexAudioUrl(audioUrl, config2) {
    if (!isProxyRoutingEnabled(config2) || !audioUrl.startsWith(AUDIO_SOURCE_PREFIX)) {
      return audioUrl;
    }
    return audioUrl.replace(
      AUDIO_SOURCE_PREFIX,
      `https://${getProxyHost(config2.proxyWorkerHost)}${AUDIO_PROXY_PATH_PREFIX}`
    );
  }
  function unproxifyYandexAudioUrl(audioUrl) {
    const str = String(audioUrl || "");
    if (!str) return str;
    try {
      const url = new URL(str);
      if (!url.pathname.startsWith(AUDIO_PROXY_PATH_PREFIX)) {
        return str;
      }
      url.host = "vtrans.s3-private.mds.yandex.net";
      url.pathname = `/tts/prod/${url.pathname.slice(AUDIO_PROXY_PATH_PREFIX.length).replace(/^\/+/, "")}`;
      url.protocol = "https:";
      return url.toString();
    } catch {
      return str;
    }
  }
  function isYandexAudioUrlOrProxy(url, config2) {
    return url.startsWith(AUDIO_SOURCE_PREFIX) || url.startsWith(
      `https://${getProxyHost(config2.proxyWorkerHost)}${AUDIO_PROXY_PATH_PREFIX}`
    );
  }
  function proxifyYandexSubtitlesUrl(subtitlesUrl, config2) {
    if (!isProxyRoutingEnabled(config2) || !subtitlesUrl.startsWith(SUBTITLE_SOURCE_PREFIX)) {
      return subtitlesUrl;
    }
    const subtitlesPath = subtitlesUrl.slice(SUBTITLE_SOURCE_PREFIX.length);
    return `https://${getProxyHost(config2.proxyWorkerHost)}${SUBTITLE_PROXY_PATH_PREFIX}${subtitlesPath}`;
  }
  const DISABLED_SUBTITLES_VALUE = "disabled";
  const VALID_SUBTITLE_FORMATS = new Set([
    "srt",
    "vtt",
    "ass",
    "json"
  ]);
  const SUBTITLES_INDEX_OPTION_PATTERN = /^\d+$/u;
  const subtitlesSelectionRequestVersion = new WeakMap();
  const subtitlesListLogKey = new WeakMap();
  function isVkProbeHost() {
    return /(?:^|\.)vkvideo\.ru$|(?:^|\.)vk\.(?:com|ru)$/i.test(
      String(globalThis.location.hostname || "")
    );
  }
  function getCurrentSubtitlesCacheKey(handler) {
    const videoData2 = handler.videoData;
    if (!videoData2?.videoId) {
      return null;
    }
    const detectedLanguage = getCacheDetectedLanguage(handler);
    if (!detectedLanguage) {
      return null;
    }
    const subtitleLanguage = getPreferredSubtitlesLanguage(handler);
    if (!subtitleLanguage) {
      return null;
    }
    return handler.getSubtitlesCacheKey(
      videoData2.videoId,
      detectedLanguage,
      subtitleLanguage
    );
  }
  function getPreferredSubtitlesLanguage(handler) {
    const videoData2 = handler.videoData;
    return handler.getPreferredSubtitlesLanguage(
      videoData2?.detectedLanguage,
      videoData2?.responseLanguage
    ) ?? videoData2?.responseLanguage ?? handler.translateToLang;
  }
  function getCacheDetectedLanguage(handler) {
    const detectedLanguage = String(handler.videoData?.detectedLanguage || "").trim().toLowerCase();
    if (detectedLanguage && detectedLanguage !== "auto") {
      return detectedLanguage;
    }
    const translateFromLang = String(handler.translateFromLang || "").trim().toLowerCase();
    if (translateFromLang && translateFromLang !== "auto") {
      return translateFromLang;
    }
    return "auto";
  }
  function isRecord(value) {
    return value !== null && typeof value === "object";
  }
  function asSubtitleDescriptor(value) {
    if (!isRecord(value)) {
      return null;
    }
    const descriptor = value;
    const format = descriptor.format;
    if (typeof descriptor.source !== "string" || typeof descriptor.language !== "string" || typeof descriptor.url !== "string" || typeof format !== "string" || !VALID_SUBTITLE_FORMATS.has(format)) {
      return null;
    }
    return {
      source: descriptor.source,
      format,
      language: descriptor.language,
      url: descriptor.url,
      translatedFromLanguage: typeof descriptor.translatedFromLanguage === "string" ? descriptor.translatedFromLanguage : void 0,
      isAutoGenerated: typeof descriptor.isAutoGenerated === "boolean" ? descriptor.isAutoGenerated : void 0
    };
  }
  function getIndexedSubtitleDescriptors(subtitles) {
    const descriptors = [];
    for (let index = 0; index < subtitles.length; index += 1) {
      const descriptor = asSubtitleDescriptor(subtitles[index]);
      if (!descriptor) {
        continue;
      }
      descriptors.push({
        descriptor,
        index
      });
    }
    return descriptors;
  }
  function toSubtitleConsoleEntry(item) {
    return {
      index: item.index,
      language: item.descriptor.language,
      translatedFromLanguage: item.descriptor.translatedFromLanguage ?? "",
      source: item.descriptor.source,
      isAutoGenerated: Boolean(item.descriptor.isAutoGenerated),
      url: item.descriptor.url
    };
  }
  function setSubtitleConsoleGlobals(payload) {
    const scope = globalThis;
    if (payload.tracks) {
      scope.__VOT_SUBTITLE_TRACKS__ = payload.tracks;
    }
    if ("selected" in payload) {
      scope.__VOT_LAST_SUBTITLE_TRACK__ = payload.selected;
      const pvlSelectedUrl = typeof payload.selected?.effectiveUrl === "string" ? payload.selected.effectiveUrl : null;
      scope.__PVL_LAST_TRANSLATED_SUBTITLES_URL = pvlSelectedUrl;
      scope.__VOT_LAST_EXTERNAL_SUBTITLE_URL__ = pvlSelectedUrl;
    }
  }
  function logAvailableSubtitleDescriptors(handler, subtitleDescriptors) {
    const entries = subtitleDescriptors.map(toSubtitleConsoleEntry);
    setSubtitleConsoleGlobals({ tracks: entries });
    const logKey = entries.map(
      (entry) => [
        entry.index,
        entry.language,
        entry.translatedFromLanguage,
        entry.source,
        entry.isAutoGenerated ? "1" : "0",
        entry.url
      ].join("|")
    ).join("||");
    if (subtitlesListLogKey.get(handler) === logKey) {
      return;
    }
    subtitlesListLogKey.set(handler, logKey);
  }
  function mergeUniqueSubtitleDescriptors(...lists) {
    const merged = [];
    const seen = new Set();
    for (const list of lists) {
      for (const item of list) {
        const descriptor = asSubtitleDescriptor(item);
        if (!descriptor) {
          continue;
        }
        const key = [
          descriptor.source,
          descriptor.language,
          descriptor.translatedFromLanguage ?? "",
          descriptor.url
        ].join("|");
        if (seen.has(key)) {
          continue;
        }
        seen.add(key);
        merged.push(descriptor);
      }
    }
    return merged;
  }
  function parseSubtitlesOptionIndex(value) {
    if (!SUBTITLES_INDEX_OPTION_PATTERN.test(value)) {
      return null;
    }
    const parsed = Number(value);
    if (!Number.isSafeInteger(parsed)) {
      return null;
    }
    return parsed;
  }
  function getSubtitleDescriptorAtIndex(subtitles, index) {
    if (!Number.isInteger(index) || index < 0 || index >= subtitles.length) {
      return null;
    }
    return asSubtitleDescriptor(subtitles[index]);
  }
  function nextSubtitlesSelectionRequestVersion(handler) {
    const nextVersion = (subtitlesSelectionRequestVersion.get(handler) ?? 0) + 1;
    subtitlesSelectionRequestVersion.set(handler, nextVersion);
    return nextVersion;
  }
  function isCurrentSubtitlesSelectionRequest(handler, requestVersion) {
    return subtitlesSelectionRequestVersion.get(handler) === requestVersion;
  }
  function createDisabledSubtitlesOption() {
    return {
      label: localizationProvider.get("VOTSubtitlesDisabled"),
      value: DISABLED_SUBTITLES_VALUE,
      selected: true,
      disabled: false
    };
  }
  function buildSubtitlesSelectOptions(subtitleDescriptors, selectedValue = DISABLED_SUBTITLES_VALUE) {
    const options = [
      {
        ...createDisabledSubtitlesOption(),
        selected: selectedValue === DISABLED_SUBTITLES_VALUE
      }
    ];
    for (const { descriptor, index } of subtitleDescriptors) {
      options.push({
        label: buildSubtitleLabel(descriptor),
        value: String(index),
        selected: String(index) === selectedValue,
        disabled: false
      });
    }
    return options;
  }
  function getSelectedSubtitlesValue(selectedValues) {
    const iterator = selectedValues[Symbol.iterator]();
    const first = iterator.next();
    return first.done ? void 0 : first.value;
  }
  function buildSubtitleLabel(subtitle) {
    const languageLabel = localizationProvider.getLangLabel(subtitle.language);
    const translatedFromLabel = subtitle.translatedFromLanguage ? ` ${localizationProvider.get("VOTTranslatedFrom")} ${localizationProvider.getLangLabel(
    subtitle.translatedFromLanguage
  )}` : "";
    const sourceSuffix = subtitle.source === "yandex" ? "" : `, ${globalThis.location.hostname}`;
    const autogeneratedSuffix = subtitle.isAutoGenerated ? ` (${localizationProvider.get("VOTAutogenerated")})` : "";
    return `${languageLabel}${translatedFromLabel}${sourceSuffix}${autogeneratedSuffix}`;
  }
  function normalizeLang(lang2) {
    return (lang2 ?? "").toLowerCase();
  }
  function baseLang(lang2) {
    const normalized = normalizeLang(lang2);
    return normalized.split(/[-_]/)[0];
  }
  function langMatches(candidate, desired) {
    if (!candidate || !desired) return false;
    const cand = normalizeLang(candidate);
    const want = normalizeLang(desired);
    return cand === want || baseLang(cand) === baseLang(want);
  }
  function pickBestSubtitlesIndex(subtitles, fromLang, toLang) {
    if (!subtitles.length) return null;
    const from = normalizeLang(fromLang);
    const to = normalizeLang(toLang);
    const fromIsAuto = from === "auto" || from === "";
    const fromBase = baseLang(from);
    const toBase = baseLang(to);
    const isYandex = (s2) => s2.source === "yandex";
    const isAutoGenerated = (s2) => Boolean(s2.isAutoGenerated);
    const matchesPair = (s2, wantFrom, wantTo) => {
      if (!langMatches(s2.language, wantTo)) return false;
      if (fromIsAuto) return true;
      return langMatches(s2.translatedFromLanguage, wantFrom);
    };
    const isSameLangOriginal = (s2, lang2) => {
      if (!langMatches(s2.language, lang2)) return false;
      if (!s2.translatedFromLanguage) return true;
      return langMatches(s2.translatedFromLanguage, lang2);
    };
    const find = (predicate) => subtitles.find(({ descriptor }) => predicate(descriptor))?.index ?? null;
    const findOtherTarget = () => {
      const otherTargetManual = find(
        (s2) => !isYandex(s2) && langMatches(s2.language, to) && !isAutoGenerated(s2)
      );
      if (otherTargetManual != null) return otherTargetManual;
      return find(
        (s2) => !isYandex(s2) && langMatches(s2.language, to) && isAutoGenerated(s2)
      );
    };
    const yandexPair = find((s2) => isYandex(s2) && matchesPair(s2, from, to));
    if (yandexPair != null) return yandexPair;
    if (!fromIsAuto && fromBase && toBase && fromBase === toBase) {
      const nativeManual = find(
        (s2) => isSameLangOriginal(s2, to) && !isAutoGenerated(s2)
      );
      if (nativeManual != null) return nativeManual;
      const nativeAuto = find(
        (s2) => isSameLangOriginal(s2, to) && isAutoGenerated(s2)
      );
      if (nativeAuto != null) return nativeAuto;
      const otherTarget2 = findOtherTarget();
      if (otherTarget2 != null) return otherTarget2;
      const yandexTargetSameLang = find(
        (s2) => isYandex(s2) && langMatches(s2.language, to)
      );
      if (yandexTargetSameLang != null) return yandexTargetSameLang;
    }
    const yandexTarget = find((s2) => isYandex(s2) && langMatches(s2.language, to));
    if (yandexTarget != null) return yandexTarget;
    const otherPair = find((s2) => !isYandex(s2) && matchesPair(s2, from, to));
    if (otherPair != null) return otherPair;
    const otherTarget = findOtherTarget();
    if (otherTarget != null) return otherTarget;
    return null;
  }
  async function changeSubtitlesLang(subs) {
    debug.log("[onchange] subtitles", subs);
    const requestVersion = nextSubtitlesSelectionRequestVersion(this);
    const overlayView = this.uiManager.votOverlayView;
    if (!overlayView?.subtitlesSelect || !overlayView.downloadSubtitlesButton) {
      return this;
    }
    overlayView.subtitlesSelect.setSelectedValue(subs);
    if (subs === DISABLED_SUBTITLES_VALUE) {
      if (this.hasSubtitlesWidget()) {
        this.subtitlesWidget?.setContent(null);
      }
      overlayView.downloadSubtitlesButton.hidden = true;
      this.yandexSubtitles = null;
      setSubtitleConsoleGlobals({ selected: null });
      return this;
    }
    const subtitlesIndex = parseSubtitlesOptionIndex(subs);
    if (subtitlesIndex == null) {
      if (this.hasSubtitlesWidget()) {
        this.subtitlesWidget?.setContent(null);
      }
      overlayView.downloadSubtitlesButton.hidden = true;
      this.yandexSubtitles = null;
      return this;
    }
    const descriptor = getSubtitleDescriptorAtIndex(
      this.subtitles,
      subtitlesIndex
    );
    if (!descriptor) {
      if (this.hasSubtitlesWidget()) {
        this.subtitlesWidget?.setContent(null);
      }
      overlayView.downloadSubtitlesButton.hidden = true;
      this.yandexSubtitles = null;
      setSubtitleConsoleGlobals({ selected: null });
      return this;
    }
    let subtitlesObj = { ...descriptor };
    const proxiedSubtitlesUrl = proxifyYandexSubtitlesUrl(subtitlesObj.url, {
      translateProxyEnabled: this.data?.translateProxyEnabled,
      proxyWorkerHost: this.data?.proxyWorkerHost
    });
    if (proxiedSubtitlesUrl !== subtitlesObj.url) {
      subtitlesObj = {
        ...subtitlesObj,
        url: proxiedSubtitlesUrl
      };
      console.log(`[VOT] Subs proxied via ${subtitlesObj.url}`);
    }
    const selectedTrack = {
      index: subtitlesIndex,
      language: descriptor.language,
      translatedFromLanguage: descriptor.translatedFromLanguage ?? "",
      source: descriptor.source,
      isAutoGenerated: Boolean(descriptor.isAutoGenerated),
      originalUrl: descriptor.url,
      effectiveUrl: subtitlesObj.url
    };
    setSubtitleConsoleGlobals({ selected: selectedTrack });
    let fetchedSubtitles;
    try {
      fetchedSubtitles = await SubtitlesProcessor.fetchSubtitles(subtitlesObj);
    } catch (error2) {
      console.error("[VOT][subtitles][fetch failed]", subtitlesObj, error2);
      return this;
    }
    if (!isCurrentSubtitlesSelectionRequest(this, requestVersion)) {
      return this;
    }
    const hasVisibleSubtitles = Array.isArray(fetchedSubtitles.subtitles) && fetchedSubtitles.subtitles.length > 0;
    if (!hasVisibleSubtitles) {
      debug.log("[subtitles] fetched subtitles are empty", subtitlesObj);
      if (this.hasSubtitlesWidget()) {
        this.subtitlesWidget?.setContent(null);
      }
      overlayView.subtitlesSelect.setSelectedValue(DISABLED_SUBTITLES_VALUE);
      overlayView.downloadSubtitlesButton.hidden = true;
      this.yandexSubtitles = null;
      return this;
    }
    this.yandexSubtitles = fetchedSubtitles;
    this.getSubtitlesWidget().setContent(
      this.yandexSubtitles,
      subtitlesObj.language
    );
    overlayView.downloadSubtitlesButton.hidden = false;
    return this;
  }
  async function updateSubtitlesLangSelect() {
    const overlayView = this.uiManager.votOverlayView;
    if (!overlayView?.subtitlesSelect) {
      return;
    }
    const subtitleDescriptors = getIndexedSubtitleDescriptors(this.subtitles);
    logAvailableSubtitleDescriptors(this, subtitleDescriptors);
    const currentValue = getSelectedSubtitlesValue(overlayView.subtitlesSelect.selectedValues) ?? DISABLED_SUBTITLES_VALUE;
    const canPreserveCurrentSelection = currentValue !== DISABLED_SUBTITLES_VALUE && this.hasSubtitlesWidget();
    const hasCurrentOption = currentValue === DISABLED_SUBTITLES_VALUE || subtitleDescriptors.some(({ index }) => String(index) === currentValue);
    const nextValue = canPreserveCurrentSelection && hasCurrentOption ? currentValue : DISABLED_SUBTITLES_VALUE;
    const updatedOptions = buildSubtitlesSelectOptions(
      subtitleDescriptors,
      nextValue
    );
    overlayView.subtitlesSelect.updateItems(updatedOptions);
    overlayView.subtitlesSelect.setSelectedValue(nextValue);
    overlayView.syncSubtitlesButtonUi?.();
    if (nextValue !== DISABLED_SUBTITLES_VALUE) {
      return;
    }
    if (currentValue !== DISABLED_SUBTITLES_VALUE) {
      if (this.hasSubtitlesWidget()) {
        this.subtitlesWidget?.setContent(null);
      }
      overlayView.downloadSubtitlesButton.hidden = true;
      this.yandexSubtitles = null;
      setSubtitleConsoleGlobals({ selected: null });
    }
  }
  async function ensureSubtitlesForCurrentLangPair() {
    const cacheKey2 = getCurrentSubtitlesCacheKey(this);
    if (!cacheKey2) {
      if (this.subtitlesCacheKey !== null || this.subtitles.length > 0) {
        this.subtitles = [];
        this.subtitlesCacheKey = null;
        await this.updateSubtitlesLangSelect();
      }
      return this;
    }
    if (this.subtitlesCacheKey === cacheKey2 && this.subtitles.length > 0) {
      return this;
    }
    const siteSubtitles = Array.isArray(this.videoData?.subtitles) ? this.videoData.subtitles : [];
    const cachedSubs = this.cacheManager.getSubtitles(cacheKey2);
    if (cachedSubs !== void 0 && cachedSubs.length > 0) {
      this.subtitles = mergeUniqueSubtitleDescriptors(
        siteSubtitles,
        Array.isArray(cachedSubs) ? cachedSubs : []
      );
      this.subtitlesCacheKey = cacheKey2;
      await this.updateSubtitlesLangSelect();
      return this;
    }
    await this.loadSubtitles();
    return this;
  }
  async function enableSubtitlesForCurrentLangPair() {
    const overlayView = this.uiManager.votOverlayView;
    if (!overlayView?.subtitlesSelect) return this;
    try {
      await ensureSubtitlesForCurrentLangPair.call(this);
    } catch (err) {
      if (isVkProbeHost()) {
        console.warn("[VOT][VK probe] auto subtitles failed", err);
      }
      return this;
    }
    const fromLang = getCacheDetectedLanguage(this) ?? this.translateFromLang;
    const toLang = getPreferredSubtitlesLanguage(this);
    if (!toLang) {
      return this;
    }
    const bestIdx = pickBestSubtitlesIndex(
      getIndexedSubtitleDescriptors(this.subtitles),
      fromLang,
      toLang
    );
    if (bestIdx == null) {
      if (isVkProbeHost()) {
        console.warn("[VOT][VK probe] no suitable subtitles after load", {
          count: this.subtitles?.length || 0,
          fromLang,
          toLang,
          subtitles: this.subtitles
        });
      }
      return this;
    }
    await this.changeSubtitlesLang(String(bestIdx));
    return this;
  }
  async function toggleSubtitlesForCurrentLangPair() {
    const overlayView = this.uiManager.votOverlayView;
    if (!overlayView?.subtitlesSelect) return this;
    const currentValue = getSelectedSubtitlesValue(
      overlayView.subtitlesSelect.selectedValues
    );
    const hasRenderedSubtitles = Array.isArray(this.yandexSubtitles?.subtitles) && this.yandexSubtitles.subtitles.length > 0;
    const hasActiveSelection = Boolean(currentValue) && currentValue !== DISABLED_SUBTITLES_VALUE;
    if (hasActiveSelection && hasRenderedSubtitles) {
      await this.changeSubtitlesLang(DISABLED_SUBTITLES_VALUE);
      return this;
    }
    if (hasActiveSelection && currentValue) {
      await this.changeSubtitlesLang(currentValue);
      return this;
    }
    await this.enableSubtitlesForCurrentLangPair();
    return this;
  }
  async function loadSubtitles() {
    if (!this.videoData?.videoId) {
      console.error(
        `[VOT] ${localizationProvider.getDefault("VOTNoVideoIDFound")}`
      );
      this.subtitles = [];
      this.subtitlesCacheKey = null;
      return;
    }
    const subtitleLanguage = getPreferredSubtitlesLanguage(this);
    if (!subtitleLanguage) {
      this.subtitles = [];
      this.subtitlesCacheKey = null;
      await this.updateSubtitlesLangSelect();
      return;
    }
    const detectedLanguage = getCacheDetectedLanguage(this);
    if (!detectedLanguage) {
      this.subtitles = [];
      this.subtitlesCacheKey = null;
      await this.updateSubtitlesLangSelect();
      return;
    }
    const cacheKey2 = this.getSubtitlesCacheKey(
      this.videoData.videoId,
      detectedLanguage,
      subtitleLanguage
    );
    const siteSubtitles = Array.isArray(this.videoData.subtitles) ? this.videoData.subtitles : [];
    try {
      let cachedSubs = this.cacheManager.getSubtitles(cacheKey2);
      if (cachedSubs === void 0 || cachedSubs.length === 0) {
        let inflight = this.subtitlesLoadPromises.get(cacheKey2);
        if (inflight === void 0) {
          inflight = SubtitlesProcessor.getSubtitles(this.votClient, {
            ...this.videoData,
            detectedLanguage,
            responseLanguage: subtitleLanguage
          });
          this.subtitlesLoadPromises.set(cacheKey2, inflight);
        }
        try {
          cachedSubs = await inflight;
          cachedSubs = Array.isArray(cachedSubs) ? cachedSubs : [];
          if (cachedSubs.length > 0) {
            this.cacheManager.setSubtitles(cacheKey2, cachedSubs);
          } else {
            this.cacheManager.deleteSubtitles?.(cacheKey2);
          }
        } finally {
          if (this.subtitlesLoadPromises.get(cacheKey2) === inflight) {
            this.subtitlesLoadPromises.delete(cacheKey2);
          }
        }
      }
      const apiSubtitles = Array.isArray(cachedSubs) ? cachedSubs : [];
      this.subtitles = mergeUniqueSubtitleDescriptors(
        siteSubtitles,
        apiSubtitles
      );
      this.subtitlesCacheKey = cacheKey2;
    } catch (error2) {
      console.error("[VOT] Failed to load subtitles:", error2);
      this.subtitles = mergeUniqueSubtitleDescriptors(siteSubtitles);
      this.subtitlesCacheKey = cacheKey2;
    }
    await this.updateSubtitlesLangSelect();
  }
  const VOLUME_MIN_01 = 0;
  const VOLUME_MAX_01 = 1;
  const SMART_DUCKING_DEFAULT_CONFIG = Object.freeze({
    tickMs: 50,
    thresholdOnRms: 0.012,
    thresholdOffRms: 9e-3,
    rmsAttackTauMs: 60,
    rmsReleaseTauMs: 240,
    holdMs: 520,
    attackTauMs: 110,
    releaseTauMs: 600,
    maxDownPerSec: 3.5,
    maxUpPerSec: 0.9,
    rmsMissingGraceMs: 200,
    maxDtMs: 250,
    externalBaselineDelta01: 0.02,
    unduckTolerance01: 0.01,
    volumeStep01: VIDEO_VOLUME_STEP_01,
    applyDeltaThreshold01: VIDEO_VOLUME_STEP_01 / 2
  });
  function initSmartDuckingRuntime(baseline) {
    return {
      isDucked: false,
      speechGateOpen: false,
      rmsEnvelope: 0,
      baseline: normalizeVolume01(baseline),
      lastApplied: void 0,
      lastTickAt: 0,
      lastSoundAt: 0,
      rmsMissingSinceAt: null
    };
  }
  function resetSmartDuckingRuntime() {
    return initSmartDuckingRuntime();
  }
  function computeSmartDuckingStep(input, runtime, config2 = SMART_DUCKING_DEFAULT_CONFIG) {
    const nextRuntime = normalizeRuntime(runtime);
    const volumeOnStart = normalizeVolume01(input.volumeOnStart);
    if (!input.translationActive || false) {
      return {
        kind: "stop",
        runtime: nextRuntime,
        restoreVolume: nextRuntime.baseline ?? volumeOnStart
      };
    }
    const now2 = Number.isFinite(input.nowMs) ? input.nowMs : Date.now();
    const prevTickAt = nextRuntime.lastTickAt || now2;
    const dtMs = clamp$2(now2 - prevTickAt, 0, config2.maxDtMs);
    const dtSec = dtMs / 1e3;
    nextRuntime.lastTickAt = now2;
    const hasRms = isFiniteNumber(input.rms);
    const rmsValue = hasRms ? clamp$2(input.rms, VOLUME_MIN_01, VOLUME_MAX_01) : 0;
    const prevEnv = nextRuntime.rmsEnvelope;
    const envTauMs = rmsValue > prevEnv ? config2.rmsAttackTauMs : config2.rmsReleaseTauMs;
    const envAlpha = envTauMs > 0 ? -Math.expm1(-dtMs / envTauMs) : 1;
    nextRuntime.rmsEnvelope = clamp$2(
      prevEnv + (rmsValue - prevEnv) * envAlpha,
      VOLUME_MIN_01,
      VOLUME_MAX_01
    );
    let gateOpen = nextRuntime.speechGateOpen;
    if (input.audioIsPlaying && !hasRms) {
      nextRuntime.rmsMissingSinceAt ??= now2;
      if (gateOpen) {
        nextRuntime.lastSoundAt = now2;
      }
      if (nextRuntime.rmsMissingSinceAt !== null && now2 - nextRuntime.rmsMissingSinceAt >= config2.rmsMissingGraceMs) {
        gateOpen = true;
        nextRuntime.lastSoundAt = now2;
      }
    } else {
      nextRuntime.rmsMissingSinceAt = null;
      if (!gateOpen) {
        if (input.audioIsPlaying && nextRuntime.rmsEnvelope >= config2.thresholdOnRms) {
          gateOpen = true;
          nextRuntime.lastSoundAt = now2;
        }
      } else if (input.audioIsPlaying && nextRuntime.rmsEnvelope >= config2.thresholdOffRms) {
        nextRuntime.lastSoundAt = now2;
      } else if (now2 - nextRuntime.lastSoundAt > config2.holdMs) {
        gateOpen = false;
      }
    }
    nextRuntime.speechGateOpen = gateOpen;
    const currentVideoVolume = normalizeVolume01(input.currentVideoVolume);
    if (!isFiniteNumber(currentVideoVolume)) {
      return { kind: "noop", runtime: nextRuntime };
    }
    if (nextRuntime.isDucked && isFiniteNumber(nextRuntime.lastApplied) && Math.abs(currentVideoVolume - nextRuntime.lastApplied) > config2.externalBaselineDelta01) {
      nextRuntime.baseline = currentVideoVolume;
    }
    if (!nextRuntime.isDucked) {
      nextRuntime.baseline = currentVideoVolume;
    }
    const baseline = nextRuntime.baseline ?? volumeOnStart ?? currentVideoVolume;
    nextRuntime.baseline = baseline;
    if (!input.hostVideoActive) {
      nextRuntime.lastApplied = currentVideoVolume;
      return { kind: "noop", runtime: nextRuntime };
    }
    const duckingTarget01 = normalizeVolume01(input.duckingTarget01) ?? baseline;
    const duckedTarget = Math.min(baseline, duckingTarget01);
    let desired = baseline;
    if (gateOpen) {
      if (!nextRuntime.isDucked) {
        nextRuntime.isDucked = true;
      }
      desired = duckedTarget;
    } else if (nextRuntime.isDucked && Math.abs(baseline - currentVideoVolume) < config2.unduckTolerance01) {
      nextRuntime.isDucked = false;
    }
    const smoothingTauMs = desired < currentVideoVolume ? config2.attackTauMs : config2.releaseTauMs;
    const smoothingAlpha = smoothingTauMs > 0 ? -Math.expm1(-dtMs / smoothingTauMs) : 1;
    let nextVolume = currentVideoVolume + (desired - currentVideoVolume) * smoothingAlpha;
    const maxDelta = (desired < currentVideoVolume ? config2.maxDownPerSec : config2.maxUpPerSec) * dtSec;
    if (maxDelta > 0) {
      nextVolume = clamp$2(
        nextVolume,
        currentVideoVolume - maxDelta,
        currentVideoVolume + maxDelta
      );
    }
    nextVolume = clamp$2(nextVolume, VOLUME_MIN_01, VOLUME_MAX_01);
    const quantized = snapVolume01Towards(
      nextVolume,
      currentVideoVolume,
      desired,
      config2.volumeStep01
    );
    const applyDeltaThreshold01 = config2.applyDeltaThreshold01;
    if (Math.abs(quantized - currentVideoVolume) < applyDeltaThreshold01) {
      nextRuntime.lastApplied = quantized;
      return { kind: "noop", runtime: nextRuntime };
    }
    if (!isFiniteNumber(nextRuntime.lastApplied) || Math.abs(quantized - nextRuntime.lastApplied) >= applyDeltaThreshold01) {
      nextRuntime.lastApplied = quantized;
      return {
        kind: "apply",
        runtime: nextRuntime,
        volume01: quantized
      };
    }
    return { kind: "noop", runtime: nextRuntime };
  }
  function normalizeRuntime(runtime) {
    return {
      isDucked: Boolean(runtime.isDucked),
      speechGateOpen: Boolean(runtime.speechGateOpen),
      rmsEnvelope: normalizeVolume01(runtime.rmsEnvelope) ?? 0,
      baseline: normalizeVolume01(runtime.baseline),
      lastApplied: normalizeVolume01(runtime.lastApplied),
      lastTickAt: isFiniteNumber(runtime.lastTickAt) ? runtime.lastTickAt : 0,
      lastSoundAt: isFiniteNumber(runtime.lastSoundAt) ? runtime.lastSoundAt : 0,
      rmsMissingSinceAt: isFiniteNumber(runtime.rmsMissingSinceAt) ? runtime.rmsMissingSinceAt : null
    };
  }
  function normalizeVolume01(value) {
    if (!isFiniteNumber(value)) return void 0;
    return clamp$2(value, VOLUME_MIN_01, VOLUME_MAX_01);
  }
  function isFiniteNumber(value) {
    return typeof value === "number" && Number.isFinite(value);
  }
  const DOUYIN_TRANSLATION_CACHE_KEY = "VOTDouyinTranslationCache";
  async function getStoredDouyinTranslation(cacheKey2) {
    const cache = await votStorage.getRaw(
      DOUYIN_TRANSLATION_CACHE_KEY,
      {}
    );
    const item = cache[cacheKey2];
    if (!item) {
      return void 0;
    }
    if (item.expiresAt <= Date.now()) {
      delete cache[cacheKey2];
      await votStorage.setRaw(DOUYIN_TRANSLATION_CACHE_KEY, cache);
      return void 0;
    }
    return item.value;
  }
  async function setStoredDouyinTranslation(cacheKey2, value) {
    const cache = await votStorage.getRaw(
      DOUYIN_TRANSLATION_CACHE_KEY,
      {}
    );
    cache[cacheKey2] = {
      value,
      expiresAt: Date.now() + YANDEX_TTL_MS
    };
    await votStorage.setRaw(DOUYIN_TRANSLATION_CACHE_KEY, cache);
  }
  const SMART_DUCKING_TICK_MS = SMART_DUCKING_DEFAULT_CONFIG.tickMs;
  const AUDIO_PROBE_TIMEOUT_MS = 1200;
  const AUDIO_PROBE_RETRY_DELAY_MS = 100;
  const AUDIO_PROBE_MAX_ATTEMPTS = 1;
  const TRANSLATED_AUDIO_START_TIMEOUT_MS = 4e3;
  const smartDuckingAnalyserState = new WeakMap();
  function isMediaAbortError(error2) {
    const name = String(error2?.name ?? "");
    const message = String(
      error2?.message ?? error2 ?? ""
    );
    return name === "AbortError" || message.includes(
      "The fetching process for the media resource was aborted"
    ) || message.includes("media resource was aborted by the user agent");
  }
  function isAudioNode(node) {
    if (!node || typeof node !== "object") return false;
    const candidate = node;
    return typeof candidate.connect === "function" && typeof candidate.disconnect === "function";
  }
  function normalizeMediaElementVolume(volume) {
    if (!Number.isFinite(volume)) return 0;
    return Math.max(0, Math.min(1, volume));
  }
  function normalizeGainVolume(volume) {
    if (!Number.isFinite(volume)) return 0;
    return Math.max(0, volume);
  }
  function setAudioParamInstant(param, value, context) {
    const now2 = context?.currentTime;
    if (typeof now2 === "number" && Number.isFinite(now2)) {
      try {
        if (typeof param.cancelAndHoldAtTime === "function") {
          param.cancelAndHoldAtTime(now2);
        } else if (typeof param.cancelScheduledValues === "function") {
          param.cancelScheduledValues(now2);
        }
      } catch {
      }
      if (typeof param.setValueAtTime === "function") {
        param.setValueAtTime(value, now2);
        return;
      }
    }
    param.value = value;
  }
  function safeSetPlayerVolume(player2, volume) {
    if (!player2) return;
    const gainNode = player2.gainNode;
    if (gainNode?.gain) {
      setAudioParamInstant(
        gainNode.gain,
        normalizeGainVolume(volume),
        gainNode.context
      );
    }
    if (typeof player2.volume === "number") {
      player2.volume = normalizeMediaElementVolume(volume);
    }
    const media = getPlayerMediaElement(player2);
    if (media) {
      media.volume = normalizeMediaElementVolume(volume);
    }
  }
  function applyTranslationPlaybackVolume(player2, volumePercent, fallbackVolumePercent) {
    const nextVolume = typeof volumePercent === "number" && Number.isFinite(volumePercent) ? volumePercent : fallbackVolumePercent;
    if (typeof nextVolume !== "number" || !Number.isFinite(nextVolume)) {
      return;
    }
    safeSetPlayerVolume(player2, nextVolume / 100);
  }
  function getPlayerMediaElement(player2) {
    return player2?.audio ?? player2?.audioElement;
  }
  function getAutoplayRecoveryButtonText() {
    return localizationProvider.lang === "ru" ? "Запустить звук перевода" : "Start translated audio";
  }
  function setPendingAutoplayDebugValue(handler, pending) {
    const globalRecord = globalThis;
    if (!pending) {
      try {
        globalRecord.__VOT_PENDING_AUTOPLAY_RECOVERY__ = null;
      } catch (error2) {
        debug.log(
          "[VOT][audio] failed to reset pending autoplay debug value",
          error2
        );
      }
      return;
    }
    try {
      globalRecord.__VOT_PENDING_AUTOPLAY_RECOVERY__ = {
        ...pending,
        siteHost: handler.site.host,
        pageUrl: globalThis.location.href
      };
    } catch (error2) {
      debug.log(
        "[VOT][audio] failed to store pending autoplay debug value",
        error2
      );
    }
  }
  function clearPendingAutoplayRecoveryState(handler) {
    try {
      handler.pendingAutoplayRecoveryAbortController?.abort();
    } catch {
    }
    handler.pendingAutoplayRecoveryAbortController = void 0;
    handler.pendingAutoplayRecovery = null;
    setPendingAutoplayDebugValue(handler, null);
  }
  function hasPlayableMediaState(media) {
    return Boolean(
      media.readyState >= HTMLMediaElement.HAVE_METADATA || media.currentTime > 0 || Number.isFinite(media.duration) && media.duration > 0
    );
  }
  function hasManagedAudioGraph(player2) {
    return Boolean(
      player2?.gainNode || player2?.audioSource || player2?.mediaElementSource
    );
  }
  function hasStartedAudiblePlayback(handler, media, player2) {
    if (media.paused || media.readyState < 2 || media.error) {
      return false;
    }
    const audioContextState = handler.audioPlayer?.audioContext?.state ?? handler.audioContext?.state;
    if (audioContextState === "suspended" && hasManagedAudioGraph(player2)) {
      return false;
    }
    return true;
  }
  function isLikelyAutoplayBlocked(handler) {
    const player2 = handler.audioPlayer?.player;
    const media = getPlayerMediaElement(player2);
    const sourceUrl = String(player2?.currentSrc || player2?.src || "");
    const audioContextState = handler.audioPlayer?.audioContext?.state ?? handler.audioContext?.state;
    if (!player2 || !media || !sourceUrl || !handler.video || handler.video.paused) {
      return false;
    }
    if (media.error) {
      return false;
    }
    if (audioContextState === "suspended" && hasManagedAudioGraph(player2)) {
      return true;
    }
    return media.paused && hasPlayableMediaState(media);
  }
  async function resumePendingAutoplayRecoveryInternal(handler, trigger) {
    const pending = handler.pendingAutoplayRecovery;
    if (!pending) {
      return false;
    }
    const actionContext = {
      gen: pending.gen,
      videoId: pending.videoId
    };
    if (handler.isActionStale(actionContext)) {
      clearPendingAutoplayRecoveryState(handler);
      return false;
    }
    debug.log("[VOT][audio] retrying translated audio after user gesture", {
      trigger,
      sourceUrl: pending.sourceUrl,
      videoId: pending.videoId
    });
    const player2 = handler.audioPlayer?.player;
    const currentSource = String(player2?.currentSrc || player2?.src || "");
    const normalizedPendingSource = normalizeManagedAudioUrl(
      handler,
      pending.sourceUrl
    );
    const normalizedCurrentSource = normalizeManagedAudioUrl(
      handler,
      currentSource
    );
    if (normalizedPendingSource && normalizedCurrentSource !== normalizedPendingSource) {
      const applyResult = await applyTranslationSource(
        handler,
        pending.sourceUrl,
        actionContext
      );
      if (applyResult.status !== "success") {
        debug.log("[VOT][audio] failed to restore pending translated source", {
          trigger,
          applyResult
        });
        return false;
      }
    }
    const resumeResult = await resumePlayerAudioContextIfNeeded(handler);
    if (resumeResult === "failed") {
      debug.log(
        "[VOT][audio] AudioContext resume failed during autoplay recovery"
      );
    }
    await attemptTranslatedPlaybackStart(handler, actionContext);
    const started = await ensureTranslatedAudioStarted(
      handler,
      actionContext,
      1800
    );
    if (!started) {
      debug.log(
        "[VOT][audio] translated audio still did not start after gesture",
        {
          trigger,
          sourceUrl: pending.sourceUrl
        }
      );
      return false;
    }
    clearPendingAutoplayRecoveryState(handler);
    handler.transformBtn("success", localizationProvider.get("disableTranslate"));
    return true;
  }
  function markAutoplayRecoveryPending(handler, sourceUrl, actionContext) {
    clearPendingAutoplayRecoveryState(handler);
    const pending = {
      gen: actionContext?.gen ?? handler.actionsGeneration,
      videoId: actionContext?.videoId ?? handler.videoData?.videoId ?? "",
      sourceUrl,
      createdAt: Date.now()
    };
    const abortController = new AbortController();
    const gestureEvents = [
      "pointerdown",
      "touchstart",
      "click",
      "keydown"
    ];
    const onUserGesture = (event) => {
      debug.log(
        "[VOT][audio] user gesture detected while autoplay recovery is pending",
        {
          type: event.type,
          sourceUrl,
          videoId: pending.videoId
        }
      );
      void resumePendingAutoplayRecoveryInternal(
        handler,
        `gesture:${event.type}`
      );
    };
    handler.pendingAutoplayRecovery = pending;
    handler.pendingAutoplayRecoveryAbortController = abortController;
    setPendingAutoplayDebugValue(handler, pending);
    for (const eventName of gestureEvents) {
      document.addEventListener(eventName, onUserGesture, {
        capture: true,
        passive: eventName !== "keydown",
        signal: abortController.signal
      });
    }
  }
  async function attemptTranslatedPlaybackStart(handler, actionContext) {
    if (handler.isActionStale(actionContext)) {
      return;
    }
    const player2 = handler.audioPlayer?.player;
    const media = getPlayerMediaElement(player2);
    const hostVideo = handler.video;
    const hasSource = Boolean(player2?.currentSrc || player2?.src);
    if (!player2 || !hostVideo || hostVideo.paused || !hasSource) {
      return;
    }
    try {
      player2.lipSync?.("play");
    } catch (error2) {
      debug.log("[updateTranslation] lipSync(play) failed", error2);
    }
    try {
      await player2.play?.();
    } catch (error2) {
      debug.log("[updateTranslation] player.play() failed", error2);
    }
    if (!media) {
      return;
    }
    try {
      media.currentTime = hostVideo.currentTime;
    } catch {
    }
    try {
      media.playbackRate = hostVideo.playbackRate;
    } catch {
    }
    try {
      await media.play();
    } catch (error2) {
      debug.log("[updateTranslation] media.play() failed", error2);
    }
  }
  function clearPendingAutoplayRecovery(resetUi = false) {
    const hadPending = Boolean(this.pendingAutoplayRecovery);
    clearPendingAutoplayRecoveryState(this);
    if (resetUi && hadPending && this.hasActiveSource()) {
      this.transformBtn("success", localizationProvider.get("disableTranslate"));
    }
  }
  function isAwaitingAutoplayRecovery() {
    return Boolean(this.pendingAutoplayRecovery);
  }
  async function resumePendingAutoplayRecovery(trigger = "manual") {
    if (!this.pendingAutoplayRecovery) {
      return false;
    }
    if (this.pendingAutoplayRecoveryPromise) {
      return await this.pendingAutoplayRecoveryPromise;
    }
    const inFlight = resumePendingAutoplayRecoveryInternal(this, trigger).finally(
      () => {
        if (this.pendingAutoplayRecoveryPromise === inFlight) {
          this.pendingAutoplayRecoveryPromise = null;
        }
      }
    );
    this.pendingAutoplayRecoveryPromise = inFlight;
    return await inFlight;
  }
  function syncTranslationPlaybackVolume() {
    const player2 = this.audioPlayer?.player;
    const nextVolume = this.uiManager.votOverlayView?.translationVolumeSlider?.value;
    applyTranslationPlaybackVolume(player2, nextVolume, this.data?.defaultVolume);
  }
  async function primePlaybackByGesture(trigger = "manual") {
    try {
      if (!this.audioPlayer) {
        this.createPlayer();
      }
      const result = await resumePlayerAudioContextIfNeeded(this);
      debug.log("[VOT][audio] primed playback context from user gesture", {
        trigger,
        result,
        player: this.audioPlayer?.player?.constructor?.name ?? "unknown"
      });
    } catch (error2) {
      debug.log("[VOT][audio] failed to prime playback context", {
        trigger,
        error: error2
      });
    }
  }
  function getNowMs() {
    return typeof performance !== "undefined" && typeof performance.now === "function" ? performance.now() : Date.now();
  }
  function getAutoVolumeMode(handler) {
    if (handler.data?.syncVolume || !handler.data?.enabledAutoVolume) {
      return "off";
    }
    return handler.data?.enabledSmartDucking ?? true ? "smart" : "classic";
  }
  async function resumePlayerAudioContextIfNeeded(handler) {
    const ctx = handler.audioPlayer?.audioContext;
    if (!ctx || ctx.state !== "suspended") return "not-needed";
    const RESUME_TIMEOUT_MS = 1500;
    const resumePromise = (async () => {
      try {
        await ctx.resume();
        return "resumed";
      } catch (err) {
        debug.log("[updateTranslation] Failed to resume AudioContext", err);
        return "failed";
      }
    })();
    let timeoutId;
    const timeoutPromise = new Promise((resolve) => {
      timeoutId = setTimeout(() => resolve("timeout"), RESUME_TIMEOUT_MS);
    });
    const result = await Promise.race([resumePromise, timeoutPromise]);
    if (timeoutId !== void 0) {
      clearTimeout(timeoutId);
    }
    if (result === "resumed") {
      debug.log("[updateTranslation] AudioContext resumed");
    } else if (result === "timeout") {
      debug.log("[updateTranslation] AudioContext resume timeout");
    }
    return result;
  }
  async function rollbackStaleAppliedSourceIfStillCurrent(handler, appliedSourceUrl) {
    if (!appliedSourceUrl || !handler.audioPlayer) return;
    const player2 = handler.audioPlayer.player;
    const currentSource = String(player2.currentSrc || player2.src || "");
    const normalizedCurrentUrl = handler.proxifyAudio(
      handler.unproxifyAudio(currentSource)
    );
    const normalizedAppliedUrl = handler.proxifyAudio(
      handler.unproxifyAudio(appliedSourceUrl)
    );
    if (normalizedCurrentUrl !== normalizedAppliedUrl) return;
    try {
      await player2.clear();
      player2.src = "";
      debug.log("[updateTranslation] cleared stale partially-applied source");
    } catch (err) {
      debug.log("[updateTranslation] failed to clear stale source", err);
    }
  }
  function getSmartDuckingAudioContext(handler) {
    return handler.audioPlayer?.audioContext ?? handler.audioContext;
  }
  function disconnectSmartDuckingAnalyser(state) {
    if (state.connectedInputNode && state.analyser) {
      try {
        state.connectedInputNode.disconnect(state.analyser);
      } catch {
      }
    }
    state.connectedInputNode = void 0;
    if (state.createdMediaSource) {
      try {
        state.createdMediaSource.disconnect();
      } catch {
      }
    }
    state.createdMediaSource = void 0;
    if (state.analyser) {
      try {
        state.analyser.disconnect();
      } catch {
      }
    }
    state.analyser = void 0;
    state.analyserFloatData = void 0;
    state.analyserData = void 0;
    state.mediaElement = void 0;
    state.audioContext = void 0;
    state.mediaSourceCreationFailed = false;
  }
  function releaseSmartDuckingAnalyser(handler) {
    const state = smartDuckingAnalyserState.get(handler);
    if (!state) return;
    disconnectSmartDuckingAnalyser(state);
    smartDuckingAnalyserState.delete(handler);
  }
  function resolveSmartDuckingInputNode(player2, media, audioContext, state) {
    if (isAudioNode(player2?.gainNode)) return player2.gainNode;
    if (isAudioNode(player2?.audioSource)) return player2.audioSource;
    if (isAudioNode(player2?.mediaElementSource)) return player2.mediaElementSource;
    if (state.mediaSourceCreationFailed && state.mediaElement === media && state.audioContext === audioContext) {
      return void 0;
    }
    if (state.createdMediaSource && state.mediaElement === media && state.audioContext === audioContext) {
      return state.createdMediaSource;
    }
    try {
      const source = audioContext.createMediaElementSource(media);
      state.createdMediaSource = source;
      state.mediaSourceCreationFailed = false;
      return source;
    } catch (err) {
      state.mediaSourceCreationFailed = true;
      debug.log("[SmartDucking] failed to create media source", err);
      return void 0;
    }
  }
  function ensureSmartDuckingAnalyser(handler, player2, media) {
    const audioContext = getSmartDuckingAudioContext(handler);
    if (!audioContext) return void 0;
    let state = smartDuckingAnalyserState.get(handler);
    if (!state) {
      state = {};
      smartDuckingAnalyserState.set(handler, state);
    }
    if (state.mediaElement && state.mediaElement !== media || state.audioContext && state.audioContext !== audioContext) {
      disconnectSmartDuckingAnalyser(state);
    }
    state.mediaElement = media;
    state.audioContext = audioContext;
    if (!state.analyser) {
      const analyser2 = audioContext.createAnalyser();
      analyser2.fftSize = 512;
      state.analyser = analyser2;
    }
    const inputNode = resolveSmartDuckingInputNode(
      player2,
      media,
      audioContext,
      state
    );
    const analyser = state.analyser;
    if (!inputNode || !analyser) return void 0;
    if (state.connectedInputNode !== inputNode) {
      if (state.connectedInputNode) {
        try {
          state.connectedInputNode.disconnect(analyser);
        } catch {
        }
      }
      try {
        inputNode.connect(analyser);
        state.connectedInputNode = inputNode;
      } catch (err) {
        debug.log("[SmartDucking] failed to connect analyser", err);
        return void 0;
      }
    }
    return { analyser, state };
  }
  function readSmartDuckingRuntime(handler) {
    return {
      isDucked: handler.smartVolumeIsDucked,
      speechGateOpen: handler.smartVolumeSpeechGateOpen,
      rmsEnvelope: handler.smartVolumeRmsEnvelope,
      baseline: handler.smartVolumeDuckingBaseline,
      lastApplied: handler.smartVolumeLastApplied,
      lastTickAt: handler.smartVolumeLastTickAt,
      lastSoundAt: handler.smartVolumeLastSoundAt,
      rmsMissingSinceAt: handler.smartVolumeRmsMissingSinceAt
    };
  }
  function writeSmartDuckingRuntime(handler, runtime) {
    handler.smartVolumeIsDucked = runtime.isDucked;
    handler.smartVolumeSpeechGateOpen = runtime.speechGateOpen;
    handler.smartVolumeRmsEnvelope = runtime.rmsEnvelope;
    handler.smartVolumeDuckingBaseline = runtime.baseline;
    handler.smartVolumeLastApplied = runtime.lastApplied;
    handler.smartVolumeLastTickAt = runtime.lastTickAt;
    handler.smartVolumeLastSoundAt = runtime.lastSoundAt;
    handler.smartVolumeRmsMissingSinceAt = runtime.rmsMissingSinceAt;
  }
  function stopSmartVolumeDucking(handler, options = {}) {
    const { restoreVolume } = options;
    if (handler.smartVolumeDuckingInterval !== void 0) {
      clearTimeout(handler.smartVolumeDuckingInterval);
      handler.smartVolumeDuckingInterval = void 0;
    }
    const baseline = typeof restoreVolume === "number" ? restoreVolume : handler.smartVolumeDuckingBaseline ?? handler.volumeOnStart;
    if (typeof baseline === "number" && (typeof restoreVolume === "number" || handler.smartVolumeIsDucked)) {
      try {
        handler.setVideoVolume(baseline);
      } catch {
      }
    }
    releaseSmartDuckingAnalyser(handler);
    writeSmartDuckingRuntime(handler, resetSmartDuckingRuntime());
  }
  function scheduleNextSmartDuckingTick(handler) {
    if (typeof globalThis === "undefined") return;
    if (handler.smartVolumeDuckingInterval === void 0) return;
    handler.smartVolumeDuckingInterval = globalThis.setTimeout(() => {
      if (handler.smartVolumeDuckingInterval === void 0) return;
      try {
        smartDuckingTick(handler);
      } catch (err) {
        debug.log("[SmartDucking] tick failed, stopping smart ducking", err);
        stopSmartVolumeDucking(handler);
        return;
      }
      if (handler.smartVolumeDuckingInterval === void 0) return;
      scheduleNextSmartDuckingTick(handler);
    }, SMART_DUCKING_TICK_MS);
  }
  function startSmartVolumeDucking(handler) {
    if (typeof globalThis === "undefined") return;
    if (handler.smartVolumeDuckingInterval !== void 0) return;
    if (getAutoVolumeMode(handler) !== "smart") return;
    const currentVideoVolume = handler.getVideoVolume();
    const baseline = typeof handler.smartVolumeDuckingBaseline === "number" ? handler.smartVolumeDuckingBaseline : currentVideoVolume;
    const runtime = initSmartDuckingRuntime(baseline);
    if (Number.isFinite(currentVideoVolume) && Number.isFinite(baseline) && currentVideoVolume < baseline - SMART_DUCKING_DEFAULT_CONFIG.externalBaselineDelta01) {
      const now2 = getNowMs();
      runtime.isDucked = true;
      runtime.speechGateOpen = true;
      runtime.lastApplied = currentVideoVolume;
      runtime.lastSoundAt = now2;
    }
    writeSmartDuckingRuntime(handler, runtime);
    handler.smartVolumeDuckingInterval = globalThis.setTimeout(() => {
    }, 0);
    clearTimeout(handler.smartVolumeDuckingInterval);
    scheduleNextSmartDuckingTick(handler);
  }
  function getTranslatedAudioRms(handler, media) {
    const player2 = handler.audioPlayer?.player;
    const analyserBundle = ensureSmartDuckingAnalyser(handler, player2, media);
    if (!analyserBundle) return void 0;
    const { analyser, state } = analyserBundle;
    try {
      if (typeof analyser.getFloatTimeDomainData === "function") {
        let floatData = state.analyserFloatData;
        if (floatData?.length !== analyser.fftSize) {
          floatData = new Float32Array(analyser.fftSize);
          state.analyserFloatData = floatData;
        }
        analyser.getFloatTimeDomainData(floatData);
        let sum2 = 0;
        for (const value of floatData) {
          sum2 += value * value;
        }
        return clamp$2(Math.sqrt(sum2 / floatData.length), 0, 1);
      }
      let data = state.analyserData;
      if (data?.length !== analyser.fftSize) {
        data = new Uint8Array(analyser.fftSize);
        state.analyserData = data;
      }
      analyser.getByteTimeDomainData(data);
      let sum = 0;
      for (const rawValue of data) {
        const normalizedValue = (rawValue - 128) / 128;
        sum += normalizedValue * normalizedValue;
      }
      return clamp$2(Math.sqrt(sum / data.length), 0, 1);
    } catch {
      return void 0;
    }
  }
  function smartDuckingTick(handler) {
    if (getAutoVolumeMode(handler) !== "smart") {
      setupAudioSettings.call(handler);
      return;
    }
    const player2 = handler.audioPlayer?.player;
    const media = getPlayerMediaElement(player2);
    const audioIsPlaying = !!media && !media.paused && !media.muted &&
(media.volume ?? 1) > 1e-3;
    const now2 = getNowMs();
    const currentVideoVolume = handler.getVideoVolume();
    const hostVideo = handler.video;
    const hostVideoActive = !(hostVideo && (hostVideo.paused || hostVideo.ended));
    const dynamicDuckingTarget = clamp$2(handler.data?.autoVolume ?? defaultAutoVolume, 0, 100) / 100;
    handler.smartVolumeDuckingTarget = dynamicDuckingTarget;
    const rms = audioIsPlaying && media ? getTranslatedAudioRms(handler, media) : 0;
    const decision = computeSmartDuckingStep(
      {
        nowMs: now2,
        translationActive: handler.hasActiveSource(),
        audioIsPlaying,
        rms,
        currentVideoVolume,
        hostVideoActive,
        duckingTarget01: dynamicDuckingTarget,
        volumeOnStart: handler.volumeOnStart
      },
      readSmartDuckingRuntime(handler),
      SMART_DUCKING_DEFAULT_CONFIG
    );
    switch (decision.kind) {
      case "stop":
        stopSmartVolumeDucking(handler, {
          restoreVolume: decision.restoreVolume
        });
        return;
      case "apply":
        handler.setVideoVolume(decision.volume01);
        writeSmartDuckingRuntime(handler, decision.runtime);
        return;
      case "noop":
        writeSmartDuckingRuntime(handler, decision.runtime);
        return;
      default:
        throw new TypeError("Unhandled smart ducking decision");
    }
  }
  function waitForProbeRetry(delayMs, signal) {
    if (signal.aborted) {
      return Promise.resolve();
    }
    return new Promise((resolve) => {
      const timeoutId = setTimeout(() => {
        signal.removeEventListener("abort", onAbort);
        resolve();
      }, delayMs);
      const onAbort = () => {
        clearTimeout(timeoutId);
        signal.removeEventListener("abort", onAbort);
        resolve();
      };
      signal.addEventListener("abort", onAbort, { once: true });
    });
  }
  async function probeAudioUrl(handler, audioUrl, actionContext) {
    const signal = handler.actionsAbortController.signal;
    const fetchOpts = handler.isMultiMethodS3(audioUrl) ? {
      method: "HEAD",
      signal,
      timeout: AUDIO_PROBE_TIMEOUT_MS
    } : {
headers: {
        range: "bytes=0-0"
      },
      signal,
      timeout: AUDIO_PROBE_TIMEOUT_MS
    };
    for (let attempt = 1; attempt <= AUDIO_PROBE_MAX_ATTEMPTS; attempt++) {
      if (handler.isActionStale(actionContext)) return false;
      try {
        const response = await GM_fetch(audioUrl, fetchOpts);
        if (handler.isActionStale(actionContext)) return false;
        debug.log("[validateAudioUrl] probe response", {
          audioUrl,
          attempt,
          ok: response.ok,
          status: response.status
        });
        if (response.ok) return true;
      } catch (err) {
        if (handler.isActionStale(actionContext) || signal.aborted) {
          return false;
        }
        debug.log("[validateAudioUrl] probe error", { audioUrl, attempt, err });
      }
      if (attempt < AUDIO_PROBE_MAX_ATTEMPTS) {
        if (handler.isActionStale(actionContext) || signal.aborted) {
          return false;
        }
        await waitForProbeRetry(AUDIO_PROBE_RETRY_DELAY_MS, signal);
        if (handler.isActionStale(actionContext) || signal.aborted) {
          return false;
        }
      }
    }
    return false;
  }
  async function validateAudioUrl(audioUrl, actionContext) {
    if (this.isActionStale(actionContext)) {
      return audioUrl;
    }
    const rawUrl = String(audioUrl || "");
    const directUrl = this.unproxifyAudio(rawUrl);
    const normalizedInput = this.proxifyAudio(directUrl);
    const currentSource = this.audioPlayer?.player?.currentSrc || this.audioPlayer?.player?.src || "";
    const normalizedCurrent = this.proxifyAudio(
      this.unproxifyAudio(currentSource)
    );
    if (normalizedInput === normalizedCurrent) {
      return audioUrl;
    }
    if (this.site.host === "yandexdisk" || this.site.host === "googledrive" || this.isMultiMethodS3(rawUrl) || this.isMultiMethodS3(directUrl) || directUrl.includes("vtrans.s3-private.mds.yandex.net") || directUrl.includes("/tts/prod/")) {
      return audioUrl;
    }
    const isPrimaryUrlValid = await probeAudioUrl(this, audioUrl, actionContext);
    if (isPrimaryUrlValid) {
      return audioUrl;
    }
    if (directUrl !== audioUrl) {
      const isDirectUrlValid = await probeAudioUrl(
        this,
        directUrl,
        actionContext
      );
      if (isDirectUrlValid) {
        debug.log("[validateAudioUrl] switching to direct audio URL after probe");
        return directUrl;
      }
    }
    return audioUrl;
  }
  function scheduleTranslationRefresh() {
    if (!this.videoData || this.videoData.isStream) {
      return;
    }
    if (!this.hasActiveSource()) return;
    clearTimeout(this.translationRefreshTimeout);
    const refreshDelayMs = Math.max(3e4, YANDEX_TTL_MS - 5 * 60 * 1e3);
    this.translationRefreshTimeout = setTimeout(() => {
      this.refreshTranslationAudio().catch((error2) => {
        debug.log("[scheduleTranslationRefresh] refresh failed", error2);
      });
    }, refreshDelayMs);
  }
  async function requestApplyAndCacheTranslation(self, options) {
    const translateRes = await requestAndApplyTranslation({
      requester: self.translationHandler,
      request: {
        videoData: options.videoData,
        requestLang: options.requestLang,
        responseLang: options.responseLang,
        translationHelp: options.translationHelp,
        useAudioDownload: Boolean(self.data?.useAudioDownload),
        signal: self.actionsAbortController.signal
      },
      actionContext: options.actionContext,
      isActionStale: (ctx) => self.isActionStale(ctx),
      updateTranslation: (url, ctx) => self.updateTranslation(url, ctx),
      scheduleTranslationRefresh: () => self.scheduleTranslationRefresh()
    });
    if (!translateRes) return null;
    if (options.onBeforeCache) {
      await options.onBeforeCache(translateRes);
    }
    setTranslationCacheValue({
      cacheKey: options.cacheKey,
      setTranslation: (key, value) => self.cacheManager.setTranslation(key, value),
      videoId: options.cacheVideoId,
      requestLang: options.cacheRequestLang,
      responseLang: options.cacheResponseLang,
      fallbackUrl: translateRes.url,
      downloadTranslationUrl: self.downloadTranslationUrl,
      usedLivelyVoice: translateRes.usedLivelyVoice
    });
    if (options.videoData.host === "douyin") {
      const cachedValue = self.cacheManager.getTranslation(options.cacheKey);
      if (cachedValue) {
        await setStoredDouyinTranslation(options.cacheKey, cachedValue);
        debug.log("[VOT][douyin] persistent translation cache saved", {
          cacheKey: options.cacheKey,
          videoId: options.cacheVideoId
        });
      }
    }
    return translateRes;
  }
  async function refreshTranslationAudio() {
    if (!this.videoData || this.videoData.isStream) {
      return;
    }
    if (!this.hasActiveSource()) return;
    if (this.isRefreshingTranslation) return;
    const videoId = this.videoData.videoId;
    if (!videoId) return;
    if (this.actionsAbortController?.signal?.aborted) {
      this.resetActionsAbortController("refreshTranslationAudio");
    }
    this.isRefreshingTranslation = true;
    const actionContext = { gen: this.actionsGeneration, videoId };
    const normalizedTranslationHelp = normalizeTranslationHelp(
      this.videoData.translationHelp
    );
    try {
      const translateRes = await requestApplyAndCacheTranslation(this, {
        videoData,
        requestLang: resolvedReqLang,
        responseLang: resLang,
        translationHelp: normalizedTranslationHelp,
        actionContext,
        cacheKey,
        cacheVideoId: VIDEO_ID,
        cacheRequestLang: resolvedReqLang,
        cacheResponseLang: responseLang,
        onBeforeCache: async () => {
          const subsCacheKey = this.videoData ? this.getSubtitlesCacheKey(
            VIDEO_ID,
            this.videoData.detectedLanguage,
            this.videoData.responseLanguage
          ) : null;
          const cachedSubs = subsCacheKey ? this.cacheManager.getSubtitles(subsCacheKey) : null;
          if (!cachedSubs?.some(
            (item) => item.source === "yandex" && item.translatedFromLanguage === videoData.detectedLanguage && item.language === videoData.responseLanguage
          )) {
            if (subsCacheKey) this.cacheManager.deleteSubtitles(subsCacheKey);
            this.subtitles = [];
            this.subtitlesCacheKey = null;
          }
        }
      });
      if (!translateRes) return;
    } finally {
      this.isRefreshingTranslation = false;
    }
  }
  function proxifyAudio(audioUrl) {
    const proxiedAudioUrl = proxifyYandexAudioUrl(audioUrl, {
      translateProxyEnabled: this.data?.translateProxyEnabled,
      proxyWorkerHost: this.data?.proxyWorkerHost
    });
    if (proxiedAudioUrl !== audioUrl) {
      debug.log(`[VOT] Audio proxied via ${proxiedAudioUrl}`);
    }
    return proxiedAudioUrl;
  }
  function unproxifyAudio(audioUrl) {
    return unproxifyYandexAudioUrl(audioUrl);
  }
  async function handleProxySettingsChanged(reason = "proxySettingsChanged") {
    debug.log(`[VOT] ${reason}: clearing translation/subtitles cache`);
    try {
      this.cacheManager.clear();
      this.activeTranslation = null;
    } catch {
    }
    try {
      await this.stopTranslation();
    } catch {
    }
    await this.initVOTClient();
  }
  function isMultiMethodS3(url) {
    return isYandexAudioUrlOrProxy(url, {
      proxyWorkerHost: this.data?.proxyWorkerHost
    });
  }
  function normalizeManagedAudioUrl(handler, url) {
    return handler.proxifyAudio(handler.unproxifyAudio(url));
  }
  async function applyTranslationSource(handler, sourceUrl, actionContext) {
    const didSetSource = handler.audioPlayer.player.src !== sourceUrl;
    let appliedSourceUrl = null;
    if (didSetSource) {
      handler.audioPlayer.player.src = sourceUrl;
      appliedSourceUrl = sourceUrl;
    }
    try {
      if (didSetSource) {
        try {
          await handler.audioPlayer.init();
        } catch (error2) {
          if (!isMediaAbortError(error2) || handler.isActionStale(actionContext)) {
            throw error2;
          }
          debug.log(
            "[updateTranslation] transient media abort, retrying init once",
            {
              sourceUrl,
              error: error2
            }
          );
          await new Promise((resolve) => setTimeout(resolve, 200));
          if (handler.isActionStale(actionContext)) {
            await rollbackStaleAppliedSourceIfStillCurrent(
              handler,
              appliedSourceUrl
            );
            return {
              status: "stale",
              didSetSource,
              appliedSourceUrl
            };
          }
          const currentSrc = String(
            handler.audioPlayer.player.currentSrc || handler.audioPlayer.player.src || ""
          );
          if (!currentSrc) {
            handler.audioPlayer.player.src = sourceUrl;
          }
          await handler.audioPlayer.init();
        }
      }
      if (handler.isActionStale(actionContext)) {
        await rollbackStaleAppliedSourceIfStillCurrent(handler, appliedSourceUrl);
        return {
          status: "stale",
          didSetSource,
          appliedSourceUrl
        };
      }
      const resumeResult = await resumePlayerAudioContextIfNeeded(handler);
      if (resumeResult === "timeout") {
        debug.log(
          "[updateTranslation] continuing after AudioContext resume timeout"
        );
      } else if (resumeResult === "failed") {
        debug.log(
          "[updateTranslation] AudioContext resume failed, continue without deferred resume"
        );
      }
      if (handler.isActionStale(actionContext)) {
        await rollbackStaleAppliedSourceIfStillCurrent(handler, appliedSourceUrl);
        return {
          status: "stale",
          didSetSource,
          appliedSourceUrl
        };
      }
      if (!handler.video.paused && handler.audioPlayer.player.src) {
        await attemptTranslatedPlaybackStart(handler, actionContext);
      }
      return {
        status: "success",
        didSetSource,
        appliedSourceUrl
      };
    } catch (error2) {
      return {
        status: "error",
        didSetSource,
        appliedSourceUrl,
        error: error2
      };
    }
  }
  async function ensureTranslatedAudioStarted(handler, actionContext, timeoutMs = TRANSLATED_AUDIO_START_TIMEOUT_MS) {
    const player2 = handler.audioPlayer?.player;
    const media = getPlayerMediaElement(player2);
    if (!player2) return false;
    const currentSrc = String(player2.currentSrc || player2.src || "");
    if (!currentSrc) return false;
    if (!media) {
      return true;
    }
    if (hasStartedAudiblePlayback(handler, media, player2)) {
      return true;
    }
    await attemptTranslatedPlaybackStart(handler, actionContext);
    if (hasStartedAudiblePlayback(handler, media, player2)) {
      return true;
    }
    return await new Promise((resolve) => {
      let done = false;
      const finish = (value) => {
        if (done) return;
        done = true;
        cleanup();
        resolve(value);
      };
      const onGood = () => {
        void attemptTranslatedPlaybackStart(handler, actionContext);
        finish(true);
      };
      const onBad = () => finish(false);
      const onReady = () => {
        void attemptTranslatedPlaybackStart(handler, actionContext);
        if (hasStartedAudiblePlayback(handler, media, player2)) {
          finish(true);
        }
      };
      const timer = setTimeout(() => {
        void attemptTranslatedPlaybackStart(handler, actionContext);
        finish(hasStartedAudiblePlayback(handler, media, player2));
      }, timeoutMs);
      const cleanup = () => {
        clearTimeout(timer);
        media.removeEventListener("playing", onGood);
        media.removeEventListener("canplay", onReady);
        media.removeEventListener("loadeddata", onReady);
        media.removeEventListener("loadedmetadata", onReady);
        media.removeEventListener("error", onBad);
      };
      media.addEventListener("playing", onGood, { once: true });
      media.addEventListener("canplay", onReady, { once: true });
      media.addEventListener("loadeddata", onReady, { once: true });
      media.addEventListener("loadedmetadata", onReady, { once: true });
      media.addEventListener("error", onBad, { once: true });
      if (handler.isActionStale(actionContext)) {
        finish(false);
      }
    });
  }
  function shouldRequireImmediateTranslatedStart(handler) {
    const hostVideo = handler.video;
    return Boolean(hostVideo && !hostVideo.paused && !hostVideo.ended);
  }
  async function recoverAfterMediaAbort(handler, sourceUrl, actionContext) {
    try {
      await handler.audioPlayer?.player?.clear();
    } catch (err) {
      debug.log("[updateTranslation] player.clear failed during recovery", err);
    }
    try {
      if (handler.audioPlayer?.player) {
        handler.audioPlayer.player.src = "";
      }
    } catch {
    }
    try {
      handler.createPlayer();
    } catch (err) {
      debug.log("[updateTranslation] createPlayer failed during recovery", err);
      return false;
    }
    await new Promise((resolve) => setTimeout(resolve, 150));
    if (handler.isActionStale(actionContext)) {
      return false;
    }
    const retryResult = await applyTranslationSource(
      handler,
      sourceUrl,
      actionContext
    );
    if (retryResult.status !== "success") {
      debug.log("[updateTranslation] recovery retry failed", retryResult);
      return false;
    }
    if (shouldRequireImmediateTranslatedStart(handler)) {
      const started = await ensureTranslatedAudioStarted(
        handler,
        actionContext,
        TRANSLATED_AUDIO_START_TIMEOUT_MS
      );
      if (!started) {
        debug.log(
          "[updateTranslation] recovery retry attached src but playback did not start"
        );
        return false;
      }
    } else {
      debug.log(
        "[updateTranslation] recovery succeeded while host video is paused; skip immediate start check"
      );
    }
    handler.setupAudioSettings();
    handler.transformBtn("success", localizationProvider.get("disableTranslate"));
    handler.afterUpdateTranslation(sourceUrl);
    if (this.data?.autoSubtitles) {
      setTimeout(() => {
        void this.enableSubtitlesForCurrentLangPair();
      }, 1500);
      setTimeout(() => {
        void this.enableSubtitlesForCurrentLangPair();
      }, 5e3);
    }
    return true;
  }
  async function updateTranslation(audioUrl, actionContext) {
    await this.waitForPendingStopTranslate();
    if (this.isActionStale(actionContext)) return;
    clearPendingAutoplayRecoveryState(this);
    if (!this.audioPlayer) {
      this.createPlayer();
    }
    if (this.audioPlayer.audioContext?.state === "closed") {
      debug.log("[updateTranslation] AudioContext is closed, recreating player");
      this.createPlayer();
    }
    const normalizedTargetUrl = normalizeManagedAudioUrl(this, audioUrl);
    const currentSource = this.audioPlayer.player.currentSrc || this.audioPlayer.player.src || "";
    const normalizedCurrentUrl = normalizeManagedAudioUrl(this, currentSource);
    let nextAudioUrl = normalizedTargetUrl;
    if (normalizedTargetUrl !== normalizedCurrentUrl) {
      nextAudioUrl = await this.validateAudioUrl(
        normalizedTargetUrl,
        actionContext
      );
    }
    if (this.isActionStale(actionContext)) return;
    this.externalTranslationSourceUrl = null;
    let applyResult = await applyTranslationSource(
      this,
      nextAudioUrl,
      actionContext
    );
    let appliedSourceUrl = applyResult.appliedSourceUrl;
    if (applyResult.status === "error" && applyResult.didSetSource && !this.isActionStale(actionContext)) {
      const directUrl = this.unproxifyAudio(nextAudioUrl);
      if (directUrl !== nextAudioUrl) {
        try {
          debug.log(
            "[updateTranslation] proxied audio init failed, retrying direct URL"
          );
          const validatedDirectUrl = await this.validateAudioUrl(
            directUrl,
            actionContext
          );
          if (this.isActionStale(actionContext)) {
            await rollbackStaleAppliedSourceIfStillCurrent(
              this,
              appliedSourceUrl
            );
            return;
          }
          nextAudioUrl = validatedDirectUrl;
          applyResult = await applyTranslationSource(
            this,
            validatedDirectUrl,
            actionContext
          );
          appliedSourceUrl = applyResult.appliedSourceUrl;
        } catch (fallbackErr) {
          applyResult = {
            status: "error",
            didSetSource: true,
            appliedSourceUrl,
            error: fallbackErr
          };
        }
      }
    }
    if (applyResult.status === "stale") return;
    if (applyResult.status === "error") {
      debug.log("this.audioPlayer.init() error", applyResult.error);
      if (isMediaAbortError(applyResult.error)) {
        debug.log(
          "[updateTranslation] media abort detected, recreating player and retrying once",
          applyResult.error
        );
        const recovered = await recoverAfterMediaAbort(
          this,
          nextAudioUrl,
          actionContext
        );
        if (recovered) {
          return;
        }
      }
      await rollbackStaleAppliedSourceIfStillCurrent(this, appliedSourceUrl);
      try {
        await this.audioPlayer?.player?.clear();
      } catch (err) {
        debug.log("[updateTranslation] player.clear failed", err);
      }
      try {
        if (this.audioPlayer?.player) {
          this.audioPlayer.player.src = "";
        }
      } catch {
      }
      this.downloadTranslationUrl = null;
      this.activeVoiceMode = null;
      const msg = toErrorMessage(applyResult.error);
      this.transformBtn("error", msg);
      throw applyResult.error instanceof Error ? applyResult.error : new Error(msg);
    }
    if (shouldRequireImmediateTranslatedStart(this)) {
      const started = await ensureTranslatedAudioStarted(
        this,
        actionContext,
        TRANSLATED_AUDIO_START_TIMEOUT_MS
      );
      if (!started) {
        debug.log(
          "[updateTranslation] audio source attached but playback did not start"
        );
        if (isCustomPlaybackTarget(this.site.host, this.videoData?.host)) {
          debug.log(
            "[updateTranslation] custom source: translated audio did not auto-start, keeping translation active"
          );
          this.setupAudioSettings();
          this.afterUpdateTranslation(nextAudioUrl);
          markAutoplayRecoveryPending(this, nextAudioUrl, actionContext);
          this.transformBtn("success", getAutoplayRecoveryButtonText());
          if (this.data?.autoSubtitles) {
            setTimeout(() => {
              void this.enableSubtitlesForCurrentLangPair();
            }, 1500);
            setTimeout(() => {
              void this.enableSubtitlesForCurrentLangPair();
            }, 5e3);
          }
          return;
        }
        if (isLikelyAutoplayBlocked(this)) {
          debug.log(
            "[VOT][audio] translated audio is waiting for a user gesture",
            {
              sourceUrl: nextAudioUrl,
              videoId: actionContext?.videoId ?? this.videoData?.videoId
            }
          );
          markAutoplayRecoveryPending(this, nextAudioUrl, actionContext);
          this.setupAudioSettings();
          this.transformBtn("success", getAutoplayRecoveryButtonText());
          this.afterUpdateTranslation(nextAudioUrl);
          if (this.data?.autoSubtitles) {
            setTimeout(() => {
              void this.enableSubtitlesForCurrentLangPair();
            }, 1500);
            setTimeout(() => {
              void this.enableSubtitlesForCurrentLangPair();
            }, 5e3);
          }
          return;
        }
        const recovered = await recoverAfterMediaAbort(
          this,
          nextAudioUrl,
          actionContext
        );
        if (recovered) {
          return;
        }
        try {
          await this.audioPlayer?.player?.clear();
        } catch (err) {
          debug.log(
            "[updateTranslation] player.clear after no-start failed",
            err
          );
        }
        try {
          if (this.audioPlayer?.player) {
            this.audioPlayer.player.src = "";
          }
        } catch {
        }
        this.downloadTranslationUrl = null;
        this.activeVoiceMode = null;
        this.transformBtn("error", "Translated audio did not start");
        throw new Error("Translated audio did not start");
      }
    } else {
      debug.log(
        "[updateTranslation] translated source attached while host video is paused; skip immediate start check"
      );
    }
    this.setupAudioSettings();
    this.transformBtn("success", localizationProvider.get("disableTranslate"));
    this.afterUpdateTranslation(nextAudioUrl);
  }
  async function translateFunc(VIDEO_ID2, _isStream, requestLang, responseLang2, translationHelp) {
    await this.waitForPendingStopTranslate();
    debug.log("Run videoValidator");
    await this.videoValidator();
    if (this.actionsAbortController?.signal?.aborted) {
      this.resetActionsAbortController("translateFunc");
    }
    const overlayView = this.uiManager.votOverlayView;
    if (!overlayView?.votButton) {
      debug.log("[translateFunc] Overlay view missing, skipping translation");
      return;
    }
    overlayView.votButton.loading = true;
    this.hadAsyncWait = false;
    this.volumeOnStart = this.getVideoVolume();
    if (!VIDEO_ID2) {
      debug.log("Skip translation - no VIDEO_ID resolved yet");
      await this.updateTranslationErrorMsg(
        new VOTLocalizedError("VOTNoVideoIDFound"),
        this.actionsAbortController.signal
      );
      return;
    }
    const videoData2 = this.videoData;
    if (!videoData2) {
      await this.updateTranslationErrorMsg(
        new VOTLocalizedError("VOTNoVideoIDFound"),
        this.actionsAbortController.signal
      );
      return;
    }
    const currentVideoId = VIDEO_ID2;
    if (this.lastTranslationVideoId !== currentVideoId) {
      debug.log("[translateFunc] video changed, recreating player", {
        prev: this.lastTranslationVideoId,
        next: currentVideoId
      });
      this.resetActionsAbortController("translateFunc video changed");
      try {
        await this.audioPlayer?.player?.clear();
      } catch (err) {
        debug.log("[translateFunc] player.clear failed during video switch", err);
      }
      try {
        if (this.audioPlayer?.player) {
          this.audioPlayer.player.src = "";
        }
      } catch {
      }
      try {
        if (this.translationRefreshTimeout !== void 0) {
          clearTimeout(this.translationRefreshTimeout);
          this.translationRefreshTimeout = void 0;
        }
      } catch {
      }
      this.downloadTranslationUrl = null;
      this.activeTranslation = null;
      this.activeVoiceMode = null;
      this.hadAsyncWait = false;
      stopSmartVolumeDucking(this, {
        restoreVolume: this.smartVolumeDuckingBaseline ?? this.volumeOnStart
      });
      this.smartVolumeDuckingBaseline = void 0;
      try {
        this.createPlayer();
      } catch (err) {
        debug.log("[translateFunc] createPlayer failed during video switch", err);
      }
      this.lastTranslationVideoId = currentVideoId;
    }
    const normalizedTranslationHelp = normalizeTranslationHelp(translationHelp);
    await this.videoManager.ensureDetectedLanguageForTranslation(videoData2);
    const resolvedRequestLang = requestLang === "auto" && videoData2.detectedLanguage !== "auto" ? videoData2.detectedLanguage : requestLang;
    const cacheKey2 = this.getTranslationCacheKey(
      VIDEO_ID2,
      resolvedRequestLang,
      responseLang2,
      normalizedTranslationHelp
    );
    const activeKey = `video_${cacheKey2}`;
    if (this.activeTranslation?.key === activeKey) {
      debug.log("[translateFunc] Reusing in-flight translation");
      await this.activeTranslation.promise;
      return;
    }
    const actionContext = {
      gen: this.actionsGeneration,
      videoId: VIDEO_ID2
    };
    const translationPromise = (async () => {
      if (this.isActionStale(actionContext)) {
        debug.log("[translateFunc] Stale translation task - skipping");
        return;
      }
      const reqLang = resolvedRequestLang;
      const resLang2 = responseLang2;
      const applyTranslationUrl = async (url) => await updateTranslationAndSchedule({
        url,
        actionContext,
        isActionStale: (ctx) => this.isActionStale(ctx),
        updateTranslation: (nextUrl, ctx) => this.updateTranslation(nextUrl, ctx),
        scheduleTranslationRefresh: () => this.scheduleTranslationRefresh()
      });
      let cachedEntry = this.cacheManager.getTranslation(cacheKey2);
      if (!cachedEntry && videoData2.host === "douyin") {
        cachedEntry = await getStoredDouyinTranslation(cacheKey2);
        if (cachedEntry) {
          this.cacheManager.setTranslation(cacheKey2, cachedEntry);
          debug.log("[VOT][douyin] persistent cached translation was found", {
            cacheKey: cacheKey2,
            videoId: VIDEO_ID2
          });
        }
      }
      if (cachedEntry?.url) {
        try {
          const updated = await applyTranslationUrl(cachedEntry.url);
          if (updated && this.hasActiveSource()) {
            this.activeVoiceMode = cachedEntry.useLivelyVoice ? "lively" : "standard";
            this.uiManager.votOverlayView?.syncVoiceModeUi();
            debug.log("[translateFunc] Cached translation was received");
            return;
          }
          debug.log(
            "[translateFunc] Cached translation did not activate source, dropping cache and requesting fresh URL"
          );
        } catch (err) {
          debug.log(
            "[translateFunc] Cached translation failed, dropping cache and requesting fresh URL",
            err
          );
        }
        if (typeof this.cacheManager.deleteTranslation === "function") {
          this.cacheManager.deleteTranslation(cacheKey2);
        } else {
          this.cacheManager.clear();
        }
      }
      const translateRes = await requestApplyAndCacheTranslation(this, {
        videoData: videoData2,
        requestLang: reqLang,
        responseLang: resLang2,
        translationHelp: normalizedTranslationHelp,
        actionContext,
        cacheKey: cacheKey2,
        cacheVideoId: VIDEO_ID2,
        cacheRequestLang: resolvedRequestLang,
        cacheResponseLang: responseLang2,
        onBeforeCache: async () => {
          const subsCacheKey = this.videoData ? this.getSubtitlesCacheKey(
            VIDEO_ID2,
            this.videoData.detectedLanguage,
            this.videoData.responseLanguage
          ) : null;
          const cachedSubs = subsCacheKey ? this.cacheManager.getSubtitles(subsCacheKey) : null;
          if (!cachedSubs?.some(
            (item) => item.source === "yandex" && item.translatedFromLanguage === videoData2.detectedLanguage && item.language === videoData2.responseLanguage
          )) {
            if (subsCacheKey) this.cacheManager.deleteSubtitles(subsCacheKey);
            this.subtitles = [];
            this.subtitlesCacheKey = null;
          }
        }
      });
      debug.log("[translateRes]", translateRes);
      if (!translateRes) {
        debug.log("Skip translation");
        return;
      }
      if (this.hasActiveSource()) {
        this.activeVoiceMode = translateRes.usedLivelyVoice ? "lively" : "standard";
        this.uiManager.votOverlayView?.syncVoiceModeUi();
      }
    })();
    this.activeTranslation = {
      key: activeKey,
      promise: translationPromise
    };
    try {
      return await translationPromise;
    } catch (err) {
      debug.log("[translateFunc] transient media abort", err);
      this.hadAsyncWait = notifyTranslationFailureIfNeeded({
        aborted: this.actionsAbortController.signal.aborted,
        translateApiErrorsEnabled: Boolean(this.data?.translateAPIErrors),
        hadAsyncWait: this.hadAsyncWait,
        videoId: VIDEO_ID2,
        error: err,
        notify: (params) => this.notifier.translationFailed(params)
      });
      throw err;
    } finally {
      if (this.activeTranslation?.promise === translationPromise) {
        this.activeTranslation = null;
      }
      const overlayBtn = this.uiManager.votOverlayView?.votButton;
      if (!this.activeTranslation && overlayBtn?.loading && !this.hasActiveSource()) {
        debug.log("[translateFunc] clearing stale loading state");
        this.transformBtn("none", localizationProvider.get("translateVideo"));
      }
    }
  }
  function isYouTubeHosts() {
    return isTranslationDownloadHost(this.site.host);
  }
  function setupAudioSettings() {
    applyTranslationPlaybackVolume(
      this.audioPlayer?.player,
      this.uiManager.votOverlayView?.translationVolumeSlider?.value,
      this.data?.defaultVolume
    );
    const autoVolumeMode = getAutoVolumeMode(this);
    if (autoVolumeMode === "off") {
      stopSmartVolumeDucking(this, {
        restoreVolume: this.smartVolumeDuckingBaseline ?? this.volumeOnStart
      });
      return;
    }
    const targetVolume = clamp$2(this.data.autoVolume ?? defaultAutoVolume, 0, 100) / 100;
    this.smartVolumeDuckingTarget = targetVolume;
    if (!this.hasActiveSource()) {
      return;
    }
    if (autoVolumeMode === "smart") {
      startSmartVolumeDucking(this);
      return;
    }
    if (this.smartVolumeDuckingInterval !== void 0) {
      clearTimeout(this.smartVolumeDuckingInterval);
      this.smartVolumeDuckingInterval = void 0;
    }
    if (typeof this.smartVolumeDuckingBaseline !== "number") {
      this.smartVolumeDuckingBaseline = this.getVideoVolume();
    }
    const baseline = this.smartVolumeDuckingBaseline ?? this.getVideoVolume();
    this.setVideoVolume(Math.min(baseline, targetVolume));
    writeSmartDuckingRuntime(
      this,
      initSmartDuckingRuntime(this.smartVolumeDuckingBaseline)
    );
    this.smartVolumeIsDucked = true;
  }
  function applyManualVideoVolumeOverride(volume01) {
    if (!this.data?.enabledAutoVolume || !this.hasActiveSource()) return;
    const nextVolume = Math.max(0, Math.min(1, volume01));
    this.smartVolumeDuckingBaseline = nextVolume;
    this.smartVolumeLastApplied = nextVolume;
  }
  const RESPONSE_LANG_SET = new Set(availableTTS);
  const isResponseLang = (value) => RESPONSE_LANG_SET.has(value);
  const RESOLVED_VOID_PROMISE = Promise.resolve();
  const GOOGLE_DRIVE_ACTIVE_HANDLER_KEY = "__VOT_GOOGLE_DRIVE_ACTIVE_HANDLER__";
  if (/(?:^|\.)vkvideo\.ru$|(?:^|\.)vk\.(?:com|ru)$/i.test(location.hostname)) {
    installSiteSubtitlesSniffer();
    installManifestSniffer();
  }
  class VideoHandler {
    video;
    container;
    site;
translateFromLang = "auto";
    translateToLang = calculatedResLang;
    data;
    videoData;
    firstPlay = true;
    audioContext;
    votClient;
    audioPlayer;
    abortController;
    actionsAbortController;
actionsGeneration = 0;
    notifier = new Notifier();
    cacheManager;
    votSessionStorage = new VOTSessionStorageCache();
subtitlesLoadPromises = new Map();
    downloadTranslationUrl = null;
    translationRefreshTimeout;
    isRefreshingTranslation = false;
    autoRetry;
votOpts;
    volumeOnStart;
volumeLinkState = {
      initialized: false,
      lastVideoPercent: 0,
      lastTranslationPercent: 0
    };
internalVideoVolumeSetAt = 0;
    internalVideoVolumeSetPercent = null;
    internalVideoVolumeSuppressionMs = 250;
    internalVideoVolumeSetHistory = [];
    internalVideoVolumeSetHistoryLimit = 48;

smartVolumeDuckingInterval;
    smartVolumeDuckingTarget = 0.2;
    smartVolumeDuckingBaseline;
    smartVolumeLastApplied;
smartVolumeLastTickAt = 0;
    smartVolumeLastSoundAt = 0;
    smartVolumeRmsMissingSinceAt = null;
smartVolumeRmsEnvelope = 0;
smartVolumeSpeechGateOpen = false;
    smartVolumeIsDucked = false;
    longWaitingResCount = 0;
    hadAsyncWait = false;

subtitles = [];
    subtitlesCacheKey = null;
    subtitlesWidget;
    activeTranslation = null;
    activeVoiceMode = null;
    lastTranslationVideoId = null;
    externalTranslationSourceUrl = null;
    pendingAutoplayRecovery = null;
    pendingAutoplayRecoveryAbortController;
    pendingAutoplayRecoveryPromise = null;
stopTranslatePromise = null;
interactionChecker;
    uiManager;
    overlayVisibility;
    overlayVisibilityTargetsAbortController;
    translationOrchestrator;
    lifecycleController;
    translationHandler;
    videoManager;
yandexSubtitles = null;
resizeObserver;
    syncVolumeObserver;
    overlayMountObserver;
    mobileYouTubeLifecycleDebounceTimer;
initialized = false;
    onPrimaryAttachReady;
    overlayVerificationGeneration = 0;
mountCache;
errorTranslationCache = new Map();
getFullscreenOverlayRoot() {
      const doc = document;
      const fullscreenEl = doc.fullscreenElement ?? doc.webkitFullscreenElement;
      return resolveScopedFullscreenElement(fullscreenEl, [this.container]);
    }
    findPageScopedContainer() {
      const selector = String(this.site.selector || "").trim();
      if (!selector) {
        return null;
      }
      const selectors = selector.split(",").map((item) => item.trim()).filter(Boolean);
      let fallback = null;
      for (const currentSelector of selectors) {
        let matches;
        try {
          matches = document.querySelectorAll(currentSelector);
        } catch {
          continue;
        }
        for (const match of matches) {
          if (!(match instanceof HTMLElement) || !match.isConnected || match === document.body || match === document.documentElement) {
            continue;
          }
          const rect = match.getBoundingClientRect();
          if (rect.width > 0 && rect.height > 0) {
            return match;
          }
          fallback ??= match;
        }
      }
      return fallback;
    }
    resolveCurrentContainer() {
      if (!this.site.selector) {
        return this.video.parentElement ?? this.container;
      }
      const matched = findConnectedContainerBySelector(
        this.video,
        this.site.selector
      );
      if (matched) {
        return matched;
      }
      if (this.container.isConnected && containsCrossShadow(this.container, this.video)) {
        return this.container;
      }
      if (isMobileYouTubeLikeSite(this.site)) {
        const pageScopedContainer = this.findPageScopedContainer();
        if (pageScopedContainer) {
          return pageScopedContainer;
        }
      }
      return this.video.parentElement ?? this.container;
    }
    getOverlayMountPoints(container = this.container) {
      const fullscreenRoot = this.getFullscreenOverlayRoot();
      const { base, root, portalContainer, subtitlesMountContainer } = resolveOverlayMountTargets({
        container,
        site: this.site,
        fullscreenRoot
      });
      const cache = this.mountCache;
      if (cache?.container === container && cache.base === base && cache.subtitlesMountContainer === subtitlesMountContainer && cache.fullscreenRoot === fullscreenRoot && (cache.root.isConnected ?? document.documentElement.contains(cache.root))) {
        return {
          root: cache.root,
          portalContainer: cache.portalContainer,
          subtitlesMountContainer: cache.subtitlesMountContainer,
          fullscreenRoot: cache.fullscreenRoot
        };
      }
      this.mountCache = {
        container,
        base,
        root,
        portalContainer,
        subtitlesMountContainer,
        fullscreenRoot
      };
      return { root, portalContainer, subtitlesMountContainer, fullscreenRoot };
    }
    getOverlayMount(container = this.container) {
      const { root, portalContainer, subtitlesMountContainer, fullscreenRoot } = this.getOverlayMountPoints(container);
      return {
        root,
        portalContainer,
        subtitlesMountContainer,
        tooltipLayoutRoot: fullscreenRoot ?? this.tooltipLayoutRoot
      };
    }
getTranslationCacheKey(videoId, from, to, translationHelp) {
      const requestLangForApi = this.getRequestLangForTranslation(
        from,
        to
      );
      const useLivelyVoice = this.isLivelyVoiceAllowed(requestLangForApi, to) && this.data?.useLivelyVoice;
      const helpStr = translationHelp === void 0 || translationHelp === null ? "" : stableStringify(translationHelp);
      const helpHash = helpStr ? fnv1a32ToKeyPart(helpStr) : "0";
      return `${videoId}_${requestLangForApi}_${to}_${useLivelyVoice}_${helpHash}`;
    }
getSubtitlesCacheKey(videoId, detectedLanguage, responseLanguage) {
      return `${videoId}_${detectedLanguage}_${responseLanguage}_${Boolean(this.data?.useLivelyVoice)}`;
    }
    getPreferredSubtitlesLanguage(detectedLanguage = this.videoData?.detectedLanguage ?? "auto", responseLanguage = this.videoData?.responseLanguage ?? this.translateToLang) {
      const normalize = (value) => {
        const nextValue = String(value || "").trim().toLowerCase();
        if (!nextValue || nextValue === "auto") {
          return void 0;
        }
        return nextValue;
      };
      return normalize(responseLanguage) ?? normalize(detectedLanguage);
    }
    isActionStale(actionContext) {
      if (!actionContext) return false;
      return this.actionsGeneration !== actionContext.gen || this.videoData?.videoId !== actionContext.videoId;
    }
    updateVOTClientRequestSignal() {
      if (!this.votClient) return;
      this.votClient.fetchOpts = {
        ...this.votClient.fetchOpts ?? {},
        signal: this.actionsAbortController.signal
      };
    }
    resetActionsAbortController(reason) {
      try {
        this.actionsAbortController?.abort(reason);
      } catch {
      }
      this.actionsAbortController = new AbortController();
      this.actionsGeneration++;
      this.updateVOTClientRequestSignal();
    }
constructor(video, container, site) {
      debug.log(
        "[VideoHandler] add video:",
        video,
        "container:",
        container,
        this
      );
      this.video = video;
      this.container = container;
      this.site = site;
      if (String(globalThis.location.hostname || "").toLowerCase() === "youtube.googleapis.com" && globalThis.location.pathname.startsWith("/embed")) {
        globalThis[GOOGLE_DRIVE_ACTIVE_HANDLER_KEY] = this;
      }
      this.abortController = new AbortController();
      this.actionsAbortController = new AbortController();
      this.cacheManager = new InMemoryCacheManager();
      this.interactionChecker = createIntervalIdleChecker();
      this.interactionChecker.start();
      const self = () => this;
      const mount = this.getOverlayMount(container);
      this.uiManager = new UIManager({
        mount,
        data: this.data,
        videoHandler: this,
        intervalIdleChecker: this.interactionChecker
      });
      this.overlayVisibility = new OverlayVisibilityController({
        checker: this.interactionChecker,
        getOverlayView: () => this.uiManager.votOverlayView ?? null,
        getAutoHideDelay: () => this.getAutoHideDelay(),
        isInteractiveNode: (node) => this.isOverlayInteractiveNode(node),
        shouldAutoHide: () => this.uiManager.votOverlayView?.votButton?.status !== "disabled"
      });
      this.translationOrchestrator = new TranslationOrchestrator({
        isFirstPlay: () => this.firstPlay,
        setFirstPlay: (next) => {
          this.firstPlay = next;
        },
        isAutoTranslateEnabled: () => Boolean(this.data?.autoTranslate),
        getVideoId: () => this.videoData?.videoId,
        scheduleAutoTranslate: () => this.runAutoTranslate(),
        isMobileYouTubeMuted: () => this.site.host === "youtube" && this.site.additionalData === "mobile" && this.video.muted,
        setMuteWatcher: (callback) => {
          let done = false;
          const fireOnce = () => {
            if (done) return;
            done = true;
            this.video.removeEventListener("volumechange", onVolumeChange);
            callback();
          };
          const onVolumeChange = () => {
            if (!this.video.muted) {
              fireOnce();
            }
          };
          this.video.addEventListener("volumechange", onVolumeChange, {
            signal: this.abortController.signal
          });
          queueMicrotask(() => {
            if (!this.video.muted) {
              fireOnce();
            }
          });
        }
      });
      const lifecycleHost = {
        get video() {
          return self().video;
        },
        get site() {
          return self().site;
        },
        get container() {
          return self().container;
        },
        set container(value) {
          if (self().container === value) {
            return;
          }
          self().container = value;
          self().uiManager.updateMount(self().getOverlayMount(value));
        },
        get firstPlay() {
          return self().firstPlay;
        },
        set firstPlay(value) {
          self().firstPlay = value;
        },
        stopTranslation: () => this.stopTranslation(),
        get uiManager() {
          return self().uiManager;
        },
        getVideoData: () => this.getVideoData(),
        cacheManager: {
          getSubtitles: (key) => self().cacheManager.getSubtitles(key)
        },
        getSubtitlesCacheKey: (videoId, detectedLanguage, responseLanguage) => this.getSubtitlesCacheKey(videoId, detectedLanguage, responseLanguage),
        ensureSubtitlesForCurrentLangPair: () => this.ensureSubtitlesForCurrentLangPair(),
        updateSubtitlesLangSelect: () => this.updateSubtitlesLangSelect(),
        enableSubtitlesForCurrentLangPair: () => this.enableSubtitlesForCurrentLangPair(),
        setSelectMenuValues: (from, to) => this.setSelectMenuValues(from, to),
        get translateToLang() {
          return self().translateToLang;
        },
        set translateToLang(value) {
          if (isResponseLang(value)) self().translateToLang = value;
        },
        get data() {
          return self().data ?? {};
        },
        get subtitles() {
          return self().subtitles;
        },
        set subtitles(value) {
          self().subtitles = value;
        },
        get subtitlesCacheKey() {
          return self().subtitlesCacheKey;
        },
        set subtitlesCacheKey(value) {
          self().subtitlesCacheKey = value;
        },
        get videoData() {
          return self().videoData;
        },
        set videoData(value) {
          self().videoData = value;
        },
        get actionsAbortController() {
          return self().actionsAbortController;
        },
        set actionsAbortController(value) {
          self().actionsAbortController = value;
        },
        resetActionsAbortController: (reason) => this.resetActionsAbortController(reason),
        initVOTClient: () => this.initVOTClient(),
        translationOrchestrator: this.translationOrchestrator,
        resetSubtitlesWidget: () => this.resetSubtitlesWidget(),
        queueOverlayAutoHide: () => this.overlayVisibility?.queueAutoHide(),
        onPrimaryAttachReady: () => this.onPrimaryAttachReady?.(),
        syncSourceAudioAvailabilityUi: (options) => this.syncSourceAudioAvailabilityUi(options)
      };
      this.lifecycleController = new VideoLifecycleController(lifecycleHost);
      this.translationHandler = new VOTTranslationHandler(this);
      this.videoManager = new VOTVideoManager(this);
    }
getSubtitlesWidget() {
      if (!this.subtitlesWidget) {
        const { subtitlesMountContainer } = this.getOverlayMountPoints();
        this.subtitlesWidget = new SubtitlesWidget(
          this.video,
          subtitlesMountContainer,
          this.interactionChecker,
          this.tooltipLayoutRoot
        );
        if (this.data) {
          this.subtitlesWidget.setSmartLayout(
            typeof this.data.subtitlesSmartLayout === "boolean" ? this.data.subtitlesSmartLayout : true
          );
          if (typeof this.data.subtitlesMaxLength === "number") {
            this.subtitlesWidget.setMaxLength(this.data.subtitlesMaxLength);
          }
          if (typeof this.data.highlightWords === "boolean") {
            this.subtitlesWidget.setHighlightWords(this.data.highlightWords);
          }
          if (typeof this.data.subtitlesFontSize === "number") {
            this.subtitlesWidget.setFontSize(this.data.subtitlesFontSize);
          }
          if (typeof this.data.subtitlesFontFamily === "string") {
            this.subtitlesWidget.setFontFamily(
              this.data.subtitlesFontFamily
            );
          }
          if (typeof this.data.subtitlesOpacity === "number") {
            this.subtitlesWidget.setOpacity(this.data.subtitlesOpacity);
          }
        }
      }
      return this.subtitlesWidget;
    }
hasSubtitlesWidget() {
      return Boolean(this.subtitlesWidget);
    }
    resetSubtitlesWidget() {
      if (this.hasSubtitlesWidget()) {
        this.subtitlesWidget?.release();
        this.subtitlesWidget = void 0;
      }
    }
get uiRoot() {
      return this.getOverlayMountPoints().root;
    }
get portalContainer() {
      return this.getOverlayMountPoints().portalContainer;
    }
get tooltipLayoutRoot() {
      switch (this.site.host) {
        case "kickstarter": {
          return document.getElementById("react-project-header") ?? void 0;
        }
        case "custom": {
          return void 0;
        }
        default: {
          return this.container;
        }
      }
    }
getEventContainer() {
      if (!this.site.eventSelector) return this.container;
      const matched = findConnectedContainerBySelector(
        this.video,
        this.site.eventSelector
      );
      return matched ?? this.container;
    }
async runAutoTranslate() {
      let sourceAudioState = this.syncSourceAudioAvailabilityUi({
        forceVisible: true
      });
      if (!sourceAudioState.ready) {
        console.log("[VOT][source-audio] auto-translate blocked before start", {
          kind: sourceAudioState.kind,
          ready: sourceAudioState.ready,
          audioDetected: sourceAudioState.audioDetected,
          detectionSource: sourceAudioState.detectionSource
        });
        return false;
      }
      await this.videoManager.videoValidator();
      sourceAudioState = this.syncSourceAudioAvailabilityUi({
        forceVisible: true
      });
      if (!sourceAudioState.ready) {
        console.log(
          "[VOT][source-audio] auto-translate blocked after validation",
          {
            kind: sourceAudioState.kind,
            ready: sourceAudioState.ready,
            audioDetected: sourceAudioState.audioDetected,
            detectionSource: sourceAudioState.detectionSource
          }
        );
        return false;
      }
      await this.uiManager.handleTranslationBtnClick();
      return true;
    }
getAudioContext() {
      if (this.audioContext) return this.audioContext;
      if (!this.isAudioContextSupported) return void 0;
      try {
        this.audioContext = initAudioContext();
        return this.audioContext;
      } catch (err) {
        console.warn("[VOT] Failed to init AudioContext, falling back:", err);
        return void 0;
      }
    }
    get isAudioContextSupported() {
      return globalThis.AudioContext !== void 0 || globalThis.webkitAudioContext !== void 0;
    }
getPreferAudio() {
      const siteHost = this.site.host;
      const videoHost = this.videoData?.host;
      const isStream = this.videoData?.isStream ?? false;
      if (isCustomPlaybackTarget(siteHost, videoHost) || isStream) {
        return true;
      }
      const hasAudioContext = Boolean(this.getAudioContext());
      const data = this.data;
      return shouldUsePlainAudioPlayback({
        siteHost,
        videoHost,
        isStream,
        hasAudioContext,
        newAudioPlayer: data?.newAudioPlayer,
        onlyBypassMediaCSP: data?.onlyBypassMediaCSP,
        needBypassCSP: this.site.needBypassCSP
      });
    }
createPlayer() {
      const preferAudio = this.getPreferAudio();
      debug.log("preferAudio:", preferAudio);
      this.audioPlayer = new Chaimu({
        video: this.video,
debug: Boolean(true),
        fetchFn: GM_fetch,
        fetchOpts: {
          timeout: 0
        },
        preferAudio
      });
      if (preferAudio) {
        const playerAudioContext = this.audioPlayer.audioContext;
        this.audioPlayer.audioContext = void 0;
        if (playerAudioContext && playerAudioContext.state !== "closed") {
          void playerAudioContext.close().catch(() => {
          });
        }
        if (this.audioContext && this.audioContext.state !== "closed") {
          const handlerAudioContext = this.audioContext;
          this.audioContext = void 0;
          void handlerAudioContext.close().catch(() => {
          });
        }
      }
      return this;
    }
isLikelyInternalVideoVolumeChange(observedPercent) {
      const now2 = Date.now();
      const history = this.internalVideoVolumeSetHistory;
      if (history.length > 0) {
        let writeIndex = 0;
        let matchFound = false;
        for (const entry of history) {
          if (now2 - entry.at > entry.suppressMs) {
            continue;
          }
          history[writeIndex++] = entry;
          if (!matchFound && Math.abs(observedPercent - entry.percent) <= 1) {
            matchFound = true;
          }
        }
        history.length = writeIndex;
        return matchFound;
      }
      if (this.internalVideoVolumeSetPercent === null) return false;
      const ageMs = now2 - this.internalVideoVolumeSetAt;
      if (ageMs > this.internalVideoVolumeSuppressionMs) return false;
      return Math.abs(observedPercent - this.internalVideoVolumeSetPercent) <= 1;
    }
    callModule(impl, ...args) {
      return impl.call(this, ...args);
    }
    callModuleAsync(impl, ...args) {
      return impl.call(this, ...args);
    }
init() {
      return init.call(this);
    }
async initVOTClient() {
      const transportHost = this.data?.translateProxyEnabled ? this.data?.proxyWorkerHost ?? proxyWorkerHost : workerHost;
      this.votOpts = {
        fetchFn: GM_fetch,
        fetchOpts: {
          signal: this.actionsAbortController.signal,
          forceGmXhr: true
        },
        apiToken: this.data?.account?.token,
        hostVOT: votBackendUrl,
        host: transportHost
      };
      this.votClient = new (this.data?.translateProxyEnabled ? VOTWorkerClient2 : VOTClient2)(this.votOpts);
      this.votClient.sessions = await this.votSessionStorage.restore(
        transportHost,
        this.votClient.sessions
      );
      const originalGetSession = this.votClient.getSession.bind(this.votClient);
      this.votClient.getSession = async (module) => {
        const session = await originalGetSession(module);
        await this.votSessionStorage.persist(
          transportHost,
          this.votClient.sessions
        );
        return session;
      };
      return this;
    }
transformBtn(status, text) {
      this.uiManager.transformBtn(status, text);
      return this;
    }
hasActiveSource() {
      return Boolean(
        this.audioPlayer?.player?.src || this.externalTranslationSourceUrl
      );
    }
    getSourceAudioAvailabilityState() {
      return getSourceAudioAvailability(this.video);
    }
    syncSourceAudioAvailabilityUi(options = {}) {
      const state = this.getSourceAudioAvailabilityState();
      const overlayView = this.uiManager.votOverlayView;
      if (!overlayView?.votButton) {
        return state;
      }
      const previousStatus = overlayView.votButton.status;
      if (options.forceVisible) {
        overlayView.votButton.container.hidden = false;
        overlayView.updateButtonOpacity(1);
      }
      if (this.hasActiveSource() || overlayView.votButton.loading) {
        return state;
      }
      if (!state.ready) {
        this.overlayVisibility?.cancel();
        const label = localizationProvider.get(state.localizationKey);
        this.transformBtn("disabled", label);
        return state;
      }
      if (previousStatus === "disabled") {
        this.transformBtn("none", localizationProvider.get("translateVideo"));
        this.overlayVisibility?.queueAutoHide();
      }
      return state;
    }
    isAwaitingAutoplayRecovery() {
      return this.callModule(isAwaitingAutoplayRecovery);
    }
    clearPendingAutoplayRecovery(resetUi = false) {
      this.callModule(clearPendingAutoplayRecovery, resetUi);
    }
    resumePendingAutoplayRecovery(trigger = "manual") {
      return this.callModuleAsync(resumePendingAutoplayRecovery, trigger);
    }
    primePlaybackByGesture(trigger = "manual") {
      return this.callModuleAsync(primePlaybackByGesture, trigger);
    }
initExtraEvents() {
      return this.callModule(initExtraEvents);
    }
refreshOverlayMount() {
      if (isMobileYouTubeLikeSite(this.site)) {
        const nextContainer = this.resolveCurrentContainer();
        if (nextContainer !== this.container) {
          this.container = nextContainer;
        }
      }
      this.mountCache = void 0;
      const nextMount = this.getOverlayMount(this.container);
      const mountChanged = !isSameOverlayMount(this.uiManager.mount, nextMount);
      this.uiManager.updateMount(nextMount);
      if (!mountChanged) {
        return;
      }
      this.rebindOverlayVisibilityTargets();
    }
    async ensureOverlayVerified(reason = "attach") {
      const verificationGeneration = ++this.overlayVerificationGeneration;
      const retryDelaysMs = [0, 500, 1e3, 1500];
      debug.log("[VOT][observer] overlay mount requested", {
        reason,
        siteHost: this.site.host,
        videoId: this.videoData?.videoId,
        src: this.video.currentSrc || this.video.src || ""
      });
      for (let attempt = 0; attempt < retryDelaysMs.length; attempt++) {
        if (this.abortController.signal.aborted || verificationGeneration !== this.overlayVerificationGeneration) {
          return false;
        }
        const delayMs = retryDelaysMs[attempt];
        if (delayMs > 0) {
          await new Promise((resolve) => {
            globalThis.setTimeout(resolve, delayMs);
          });
        }
        if (this.abortController.signal.aborted || verificationGeneration !== this.overlayVerificationGeneration) {
          return false;
        }
        const before = this.uiManager.getOverlayVerificationState({
          requireVisible: true
        });
        if (before.verified) {
          debug.log("[VOT][observer] overlay verified", {
            reason,
            attempt,
            ...before
          });
          return true;
        }
        this.refreshOverlayMount();
        const after = this.uiManager.ensureOverlayMounted({ forceVisible: true });
        try {
          this.rebindOverlayVisibilityTargets();
        } catch (error2) {
          debug.warn(
            "[VOT][observer] failed to rebind overlay visibility",
            error2
          );
        }
        this.syncSourceAudioAvailabilityUi({ forceVisible: true });
        if (after.verified) {
          debug.log("[VOT][observer] overlay verified", {
            reason,
            attempt,
            ...after
          });
          return true;
        }
        if (attempt < retryDelaysMs.length - 1) {
          debug.log("[VOT][observer] overlay missing after attach, retrying", {
            reason,
            attempt,
            nextDelayMs: retryDelaysMs[attempt + 1],
            ...after
          });
        }
      }
      return false;
    }
rebindOverlayVisibilityTargets = rebindOverlayVisibilityTargets;
setCanPlay() {
      return this.lifecycleController.setCanPlay();
    }
    isOverlayInteractiveNode(node) {
      return this.callModule(isOverlayInteractiveNode, node);
    }
getAutoHideDelay() {
      return this.callModule(getAutoHideDelay);
    }
changeSubtitlesLang = changeSubtitlesLang;
updateSubtitlesLangSelect = updateSubtitlesLangSelect;
ensureSubtitlesForCurrentLangPair = ensureSubtitlesForCurrentLangPair;
loadSubtitles = loadSubtitles;
enableSubtitlesForCurrentLangPair() {
      return this.callModuleAsync(enableSubtitlesForCurrentLangPair);
    }
toggleSubtitlesForCurrentLangPair() {
      return this.callModuleAsync(toggleSubtitlesForCurrentLangPair);
    }
    getRequestLangForTranslation(requestLang, responseLang2) {
      if (this.data?.useLivelyVoice && this.data?.account?.token && responseLang2 === "ru") {
        return "en";
      }
      return requestLang;
    }
    isLivelyVoiceAllowed(requestLang = this.videoData?.detectedLanguage ?? "auto", responseLang2 = this.videoData?.responseLanguage ?? this.translateToLang) {
      const requestLangForApi = this.getRequestLangForTranslation(
        requestLang,
        responseLang2
      );
      if (requestLangForApi !== "en" || responseLang2 !== "ru") {
        return false;
      }
      if (!this.data?.account?.token) {
        return false;
      }
      return true;
    }
getVideoVolume() {
      return this.videoManager.getVideoVolume();
    }
setVideoVolume(volume, options = {}) {
      const snapped = snapVolume01(volume);
      const suppressSyncMs = typeof options.suppressSyncMs === "number" && Number.isFinite(options.suppressSyncMs) ? Math.max(0, options.suppressSyncMs) : this.internalVideoVolumeSuppressionMs;
      const now2 = Date.now();
      const percent = volume01ToPercent(snapped);
      this.internalVideoVolumeSetAt = now2;
      this.internalVideoVolumeSetPercent = percent;
      this.internalVideoVolumeSetHistory.push({
        at: now2,
        percent,
        suppressMs: suppressSyncMs
      });
      if (this.internalVideoVolumeSetHistory.length > this.internalVideoVolumeSetHistoryLimit) {
        this.internalVideoVolumeSetHistory.splice(
          0,
          this.internalVideoVolumeSetHistory.length - this.internalVideoVolumeSetHistoryLimit
        );
      }
      this.videoManager.setVideoVolume(snapped);
      return this;
    }
onVideoVolumeSliderSynced(volumePercent) {
      const normalized = clampPercentInt(volumePercent);
      if (!this.volumeLinkState.initialized) {
        syncVideoLinkSnapshot(this.volumeLinkState, normalized);
        return;
      }
      if (this.data?.syncVolume && this.hasActiveSource() && !this.isLikelyInternalVideoVolumeChange(normalized)) {
        return;
      }
      syncVideoLinkSnapshot(this.volumeLinkState, normalized);
    }
onTranslationVolumeSliderSynced(volumePercent) {
      if (!this.volumeLinkState.initialized) {
        syncTranslationLinkSnapshot(this.volumeLinkState, volumePercent);
        return;
      }
      syncTranslationLinkSnapshot(this.volumeLinkState, volumePercent);
    }
resetVolumeLinkState(videoPercent, translationPercent) {
      syncVideoLinkSnapshot(this.volumeLinkState, videoPercent);
      syncTranslationLinkSnapshot(this.volumeLinkState, translationPercent);
      this.volumeLinkState.initialized = true;
    }
isMuted() {
      return this.videoManager.isMuted();
    }
syncVideoVolumeSlider() {
      this.videoManager.syncVideoVolumeSlider();
    }
setSelectMenuValues(from, to) {
      this.videoManager.setSelectMenuValues(
        from,
        to
      );
    }
syncVolumeWrapper(fromType, newVolume) {
      const overlayView = this.uiManager.votOverlayView;
      if (!overlayView?.isInitialized()) {
        return;
      }
      const videoSlider = overlayView.videoVolumeSlider;
      const translationSlider = overlayView.translationVolumeSlider;
      if (!videoSlider || !translationSlider) {
        return;
      }
      const { nextVideo, nextTranslation } = applyVolumeLinkDelta({
        state: this.volumeLinkState,
        fromType,
        newVolume,
        currentVideo: Number(videoSlider.value),
        currentTranslation: Number(translationSlider.value),
        translationMin: translationSlider.min,
        translationMax: translationSlider.max
      });
      if (typeof nextTranslation === "number") {
        translationSlider.value = nextTranslation;
        if (this.audioPlayer?.player) {
          this.audioPlayer.player.volume = nextTranslation / 100;
        }
        return;
      }
      if (typeof nextVideo === "number") {
        videoSlider.value = nextVideo;
        this.setVideoVolume(nextVideo / 100);
      }
    }
getVideoData() {
      return this.videoManager.getVideoData();
    }
    sanitizeDownloadName(value) {
      return value.replace(/\.(mp4|mkv|mov|webm|avi|m4v|mp3|srt|vtt|json)$/iu, "").replace(/[<>:"/\\|?*]/g, "_").replace(/\s+/g, " ").trim();
    }
    getDownloadBaseName() {
      const fromDownloadTitle = typeof this.videoData?.downloadTitle === "string" && this.videoData.downloadTitle.trim() ? this.videoData.downloadTitle : void 0;
      const fromVideoData = typeof this.videoData?.title === "string" && this.videoData.title.trim() ? this.videoData.title : void 0;
      const fromCurrentVideoTitle = typeof this.videoData?.videoTitle === "string" && this.videoData.videoTitle.trim() ? this.videoData.videoTitle : void 0;
      const raw = fromDownloadTitle || fromVideoData || fromCurrentVideoTitle || `translation-${this.videoData?.videoId ?? "audio"}`;
      return this.sanitizeDownloadName(raw);
    }
videoValidator() {
      return this.videoManager.videoValidator();
    }
stopTranslate() {
      if (this.stopTranslatePromise !== null) {
        return this.stopTranslatePromise;
      }
      const cleanup = async () => {
        this.clearPendingAutoplayRecovery();
        this.externalTranslationSourceUrl = null;
        if (this.audioPlayer?.player) {
          try {
            this.audioPlayer.player.removeVideoEvents();
            this.audioPlayer.player.src = "";
            await this.audioPlayer.player.clear();
          } catch (err) {
            debug.log("[stopTranslate] audioPlayer cleanup error", err);
          }
          debug.log("audioPlayer after stopTranslate", this.audioPlayer);
        }
        this.activeTranslation = null;
        this.activeVoiceMode = null;
        const overlayView = this.uiManager.votOverlayView;
        if (overlayView) {
          if (overlayView.videoVolumeSlider) {
            overlayView.videoVolumeSlider.hidden = true;
          }
          if (overlayView.translationVolumeSlider) {
            overlayView.translationVolumeSlider.hidden = true;
          }
          if (overlayView.downloadTranslationButton) {
            overlayView.downloadTranslationButton.hidden = true;
          }
        }
        this.downloadTranslationUrl = null;
        this.longWaitingResCount = 0;
        this.hadAsyncWait = false;
        this.transformBtn("none", localizationProvider.get("translateVideo"));
        this.syncSourceAudioAvailabilityUi();
        debug.log(`Volume on start: ${this.volumeOnStart}`);
        const restoreVolume = typeof this.smartVolumeDuckingBaseline === "number" ? this.smartVolumeDuckingBaseline : this.volumeOnStart;
        stopSmartVolumeDucking(this, { restoreVolume });
        this.volumeOnStart = void 0;
        if (this.autoRetry !== void 0) {
          clearTimeout(this.autoRetry);
          this.autoRetry = void 0;
        }
        if (this.translationRefreshTimeout !== void 0) {
          clearTimeout(this.translationRefreshTimeout);
          this.translationRefreshTimeout = void 0;
        }
        this.resetActionsAbortController("stopTranslate");
      };
      const inFlight = cleanup().finally(() => {
        if (this.stopTranslatePromise === inFlight) {
          this.stopTranslatePromise = null;
        }
      });
      this.stopTranslatePromise = inFlight;
      return inFlight;
    }
    waitForPendingStopTranslate() {
      return this.stopTranslatePromise ?? RESOLVED_VOID_PROMISE;
    }
async updateTranslationErrorMsg(errorMessage, signal) {
      if (signal?.aborted) {
        return;
      }
      const translationTake2 = localizationProvider.get("translationTake");
      const lang2 = localizationProvider.lang;
      this.longWaitingResCount = errorMessage === localizationProvider.get("translationTakeAboutMinute") ? this.longWaitingResCount + 1 : 0;
      debug.log("longWaitingResCount", this.longWaitingResCount);
      if (this.longWaitingResCount > minLongWaitingCount) {
        errorMessage = new VOTLocalizedError("TranslationDelayed");
      }
      debug.log("updateTranslationErrorMsg message", errorMessage);
      if (errorMessage?.name === "VOTLocalizedError") {
        this.transformBtn("error", errorMessage.localizedMessage);
      } else if (errorMessage instanceof Error) {
        this.transformBtn("error", errorMessage?.message);
      } else if (this.data?.translateAPIErrors && lang2 !== "ru" && !errorMessage?.includes(translationTake2)) {
        const overlayView = this.uiManager.votOverlayView;
        if (!overlayView?.votButton) {
          return;
        }
        const messageStr = Array.isArray(errorMessage) ? errorMessage.join(" ") : String(errorMessage);
        const cacheKey2 = `${lang2}:${messageStr}`;
        const cached = this.errorTranslationCache.get(cacheKey2);
        if (cached) {
          this.transformBtn("error", cached);
        } else {
          overlayView.votButton.loading = true;
          const translatedMessage = await translate(messageStr, "ru", lang2);
          const translatedText = Array.isArray(translatedMessage) ? translatedMessage.join("\n") : String(translatedMessage);
          if (signal?.aborted) {
            return;
          }
          this.errorTranslationCache.set(cacheKey2, translatedText);
          if (this.errorTranslationCache.size > 50) {
            const oldestKey = this.errorTranslationCache.keys().next().value;
            if (oldestKey) this.errorTranslationCache.delete(oldestKey);
          }
          this.transformBtn("error", translatedText);
        }
        if (signal?.aborted) {
          return;
        }
      } else {
        const msg = Array.isArray(errorMessage) ? errorMessage.join("\n") : String(errorMessage ?? "");
        this.transformBtn("error", msg);
      }
      if (signal?.aborted) {
        return;
      }
      if ([
        "Подготавливаем перевод",
        "Видео передано в обработку",
        "Ожидаем перевод видео",
        "Загружаем переведенное аудио"
      ].includes(errorMessage)) {
        if (this.uiManager.votOverlayView?.votButton) {
          this.uiManager.votOverlayView.votButton.loading = true;
        }
      }
    }
afterUpdateTranslation(audioUrl) {
      const overlayView = this.uiManager.votOverlayView;
      if (!overlayView?.votButton) {
        return;
      }
      const isSuccess = overlayView.votButton.container.dataset.status === "success";
      if (overlayView.videoVolumeSlider) {
        overlayView.videoVolumeSlider.hidden = !this.data?.showVideoSlider || !isSuccess;
      }
      if (overlayView.translationVolumeSlider) {
        overlayView.translationVolumeSlider.hidden = !isSuccess;
      }
      if (overlayView.videoVolumeSlider && overlayView.translationVolumeSlider) {
        this.volumeLinkState.lastVideoPercent = Number(
          overlayView.videoVolumeSlider.value
        );
        this.volumeLinkState.lastTranslationPercent = Number(
          overlayView.translationVolumeSlider.value
        );
        this.volumeLinkState.initialized = true;
      } else {
        this.volumeLinkState.initialized = false;
      }
      if (this.videoData && !this.videoData.isStream) {
        if (overlayView.downloadTranslationButton) {
          overlayView.downloadTranslationButton.hidden = false;
        }
        this.downloadTranslationUrl = audioUrl;
      }
      debug.log(
        "afterUpdateTranslation downloadTranslationUrl",
        this.downloadTranslationUrl
      );
      this.syncTranslationPlaybackVolume();
      if (isSuccess && this.site.host === "vk" && this.videoData?.videoId) {
        const vkSubtitlesCacheKey = this.getSubtitlesCacheKey(
          this.videoData.videoId,
          this.videoData.detectedLanguage,
          this.videoData.responseLanguage
        );
        this.cacheManager.deleteSubtitles(vkSubtitlesCacheKey);
        this.subtitles = [];
        this.subtitlesCacheKey = null;
        void (async () => {
          try {
            await this.loadSubtitles();
            if (this.data?.autoSubtitles) {
              await this.enableSubtitlesForCurrentLangPair();
            }
          } catch (error2) {
            debug.log("[VOT][VK subtitles] failed to refresh after translation", {
              error: error2,
              videoId: this.videoData?.videoId
            });
          }
        })();
      }
      if (this.data?.sendNotifyOnComplete && this.hadAsyncWait && isSuccess) {
        this.notifier.translationCompleted(globalThis.location.hostname);
        this.hadAsyncWait = false;
      }
    }
validateAudioUrl(audioUrl, actionContext) {
      return this.callModuleAsync(validateAudioUrl, audioUrl, actionContext);
    }
    scheduleTranslationRefresh() {
      this.callModule(scheduleTranslationRefresh);
    }
    refreshTranslationAudio = refreshTranslationAudio;
proxifyAudio(audioUrl) {
      return this.callModule(proxifyAudio, audioUrl);
    }
unproxifyAudio(audioUrl) {
      return this.callModule(unproxifyAudio, audioUrl);
    }
handleProxySettingsChanged = handleProxySettingsChanged;
    isMultiMethodS3(url) {
      return this.callModule(isMultiMethodS3, url);
    }
updateTranslation = updateTranslation;
translateFunc(VIDEO_ID2, isStream, requestLang, responseLang2, translationHelp) {
      return translateFunc.call(
        this,
        VIDEO_ID2,
        isStream,
        requestLang,
        responseLang2,
        translationHelp
      );
    }
isYouTubeHosts() {
      return this.callModule(isYouTubeHosts);
    }
    canUploadAudioForCurrentSite() {
      const host = this.site.host;
      const rawUrl = String(
        this.videoData?.url || this.video?.currentSrc || this.video?.src || ""
      );
      debug.log("[VOT] canUploadAudioForCurrentSite host:", host);
      const canForceLocalFileUpload = (() => {
        if (!rawUrl) {
          return isCustomPlaybackTarget(host, this.videoData?.host);
        }
        try {
          const normalizedUrl = new URL(
            rawUrl,
            globalThis.location.href
          ).toString();
          if (!this.votClient.isDirectMediaUrl(normalizedUrl)) {
            return isCustomPlaybackTarget(host, this.videoData?.host);
          }
          return isCustomPlaybackTarget(host, this.videoData?.host) || new URL(normalizedUrl).hostname !== globalThis.location.hostname;
        } catch {
          return isCustomPlaybackTarget(host, this.videoData?.host);
        }
      })();
      if (canForceLocalFileUpload) {
        return true;
      }
      if (!this.data?.useAudioDownload) {
        return false;
      }
      return host === "youtube" || host === "invidious" || host === "piped" || host === "yandexdisk" || host === "custom" || host === "vk";
    }
setupAudioSettings() {
      return this.callModule(setupAudioSettings);
    }
    syncTranslationPlaybackVolume() {
      this.callModule(syncTranslationPlaybackVolume);
    }
    applyManualVideoVolumeOverride(volume) {
      this.callModule(applyManualVideoVolumeOverride, volume);
    }
stopTranslation = async () => {
      this.translationOrchestrator?.reset();
      this.overlayVisibility?.cancel();
      await this.stopTranslate();
      this.syncVideoVolumeSlider();
    };
handleSrcChanged() {
      return this.lifecycleController.handleSrcChanged();
    }
async release() {
      debug.log("[VideoHandler] release");
      if (/^(m|music)\.youtube\.com$/i.test(
        String(globalThis.location.hostname || "")
      )) {
        console.log("[VOT][mobile-overlay][handler] VideoHandler.release()", {
          videoId: this.videoData?.videoId,
          page: `${globalThis.location.origin}${globalThis.location.pathname}${globalThis.location.search}`
        });
      }
      this.initialized = false;
      this.overlayVerificationGeneration += 1;
      try {
        await this.stopTranslation();
      } catch (err) {
        debug.log("[VideoHandler] stopTranslation failed during release", err);
      }
      this.lifecycleController?.teardown();
      this.abortController?.abort();
      this.abortController = new AbortController();
      this.overlayVisibility?.release();
      this.releaseExtraEvents();
      if (this.hasSubtitlesWidget()) {
        this.subtitlesWidget?.release();
        this.subtitlesWidget = void 0;
      }
      this.interactionChecker?.destroy();
      this.uiManager.release();
      if (globalThis[GOOGLE_DRIVE_ACTIVE_HANDLER_KEY] === this) {
        Reflect.deleteProperty(
          globalThis,
          GOOGLE_DRIVE_ACTIVE_HANDLER_KEY
        );
      }
    }
collectReportInfo() {
      const info = getEnvironmentInfo();
      const detectedLanguage = this.videoData?.detectedLanguage ?? "unknown";
      const responseLanguage = this.videoData?.responseLanguage ?? "unknown";
      const additionalInfo = `<details>
<summary>Autogenerated by VOT:</summary>
<ul>
  <li>OS: ${info.os}</li>
  <li>Browser: ${info.browser}</li>
  <li>Loader: ${info.loader}</li>
  <li>Script version: ${info.scriptVersion}</li>
  <li>URL: <code>${info.url}</code></li>
  <li>Lang: <code>${detectedLanguage}</code> -> <code>${responseLanguage}</code> (Lively voice: ${this.data?.useLivelyVoice ?? false} | Audio download: ${this.data?.useAudioDownload ?? false})</li>
  <li>Player: ${this.data?.newAudioPlayer ? "New" : "Old"} (CSP only: ${this.data?.onlyBypassMediaCSP ?? false})</li>
  <li>Proxying mode: ${this.data?.translateProxyEnabled ?? 0}</li>
</ul>
</details>`;
      const template = `1-bug-report-${localizationProvider.lang === "ru" ? "ru" : "en"}.yml`;
      return {
        assignees: "Acobat12",
        template,
        os: info.os,
        "script-version": info.scriptVersion,
        "additional-info": additionalInfo
      };
    }
releaseExtraEvents = releaseExtraEvents;
  }
  const videoObserverChecker = createIntervalIdleChecker();
  const videoObserver = new VideoObserver(videoObserverChecker);
  const videosWrappers = new WeakMap();
  let servicesCache = null;
  const bootState = getOrCreateBootState();
  function extractGoogleDriveFileIdFromUrl(url) {
    try {
      const parsed = new URL(url, globalThis.location.origin);
      const pathname = parsed.pathname;
      return /\/file\/d\/([^/]+)/.exec(pathname)?.[1] || /\/videos\/d\/([^/]+)/.exec(pathname)?.[1] || /\/d\/([^/]+)/.exec(pathname)?.[1];
    } catch {
      return void 0;
    }
  }
  function normalizeGoogleDriveStoredTitle(value) {
    if (typeof value !== "string") return void 0;
    const normalized = value.replace(/\s*-\s*Google Drive\s*$/i, "").replace(/\s*-\s*Google Диск\s*$/i, "").trim();
    return normalized || void 0;
  }
  function getGoogleDriveTopFrameTitle() {
    const exactDriveTitle = Array.from(
      document.querySelectorAll('[data-is-tooltip-wrapper="true"] > span')
    ).map((el) => el.textContent?.trim() ?? "").find((text) => /\.(mp4|mkv|mov|webm|avi|m4v)$/i.test(text)) || "";
    const headingTitle = document.querySelector('h1, [role="heading"], div[role="heading"]')?.textContent?.trim() || "";
    const metaTitle = document.querySelector('meta[property="og:title"], meta[name="title"]')?.getAttribute("content")?.trim() || "";
    const docTitle = String(document.title || "").trim();
    return normalizeGoogleDriveStoredTitle(
      exactDriveTitle || headingTitle || metaTitle || docTitle
    );
  }
  async function persistGoogleDriveTopFrameTitleIfNeeded() {
    const host = globalThis.location.hostname;
    if (host !== "drive.google.com" && host !== "docs.google.com") {
      return;
    }
    const fileId = extractGoogleDriveFileIdFromUrl(globalThis.location.href);
    if (!fileId) {
      return;
    }
    const title = getGoogleDriveTopFrameTitle();
    if (!title) {
      return;
    }
    try {
      await votStorage.set(`googledrive:title:${fileId}`, title);
    } catch (error2) {
      console.warn("[VOT] failed to persist Google Drive title", error2);
    }
  }
  function getFrameContext() {
    return {
      frame: isIframe() ? "iframe" : "top",
      host: globalThis.location.hostname || "unknown",
      path: globalThis.location.pathname || "/"
    };
  }
  function logBootstrap(message, details) {
    const ctx = getFrameContext();
    const payload = {
      host: ctx.host,
      path: ctx.path
    };
    if (details) {
      Object.assign(payload, details);
    }
    console.log(`[VOT][bootstrap][${ctx.frame}] ${message}`, payload);
  }
  function matchSite(entry, url) {
    const host = url.hostname;
    const isMatches = (match) => {
      if (match instanceof RegExp) return match.test(host);
      if (typeof match === "string") return host.includes(match);
      if (typeof match === "function") {
        try {
          return Boolean(match(url));
        } catch {
          return false;
        }
      }
      return false;
    };
    return Array.isArray(entry.match) ? entry.match.some(isMatches) : isMatches(entry.match);
  }
  function getServicesCached() {
    if (!servicesCache) {
      const base = getService();
      const url = new URL(globalThis.location.href);
      const matchedBase = base.filter((site) => matchSite(site, url));
      const matchedExtra = extraSites.filter((site) => matchSite(site, url));
      servicesCache = [...matchedExtra, ...matchedBase];
      const genericFallbackService = {
        host: "custom",
        url: "stub",
        selector: GENERIC_PLAYER_SELECTOR,
        eventSelector: GENERIC_PLAYER_SELECTOR,
        rawResult: true,
        priority: -1e3
      };
      const hasGenericFallback = servicesCache.some(
        (site) => String(site.host) === "custom" && site.selector === GENERIC_PLAYER_SELECTOR
      );
      if (!hasGenericFallback) {
        servicesCache.push(genericFallbackService);
      }
    }
    return servicesCache;
  }
  function findContainer(site, video) {
    debug.log("findContainer", site, video);
    if (site.selector) {
      const matched = findConnectedContainerBySelector(video, site.selector);
      if (matched) {
        debug.log("findContainer matched by site.selector", matched);
        return matched;
      }
    }
    const genericMatched = findConnectedContainerBySelector(
      video,
      GENERIC_PLAYER_SELECTOR
    );
    if (genericMatched) {
      debug.log("findContainer matched by generic selector", genericMatched);
      return genericMatched;
    }
    debug.log("findContainer fallback to parentElement");
    return video.parentElement;
  }
  function isYouTubePage() {
    const hostname = String(globalThis.location.hostname || "").toLowerCase();
    return hostname === "youtube.com" || hostname.endsWith(".youtube.com") || hostname === "youtu.be" || hostname.endsWith(".youtube-nocookie.com") || hostname.endsWith(".youtubekids.com") || hostname === "youtube.googleapis.com";
  }
  function isYouTubeMusicPage() {
    return String(globalThis.location.hostname || "").toLowerCase() === "music.youtube.com";
  }
  function shouldInstallManifestSniffer() {
    return isYouTubeMusicPage() || !isYouTubePage();
  }
  async function main() {
    await persistGoogleDriveTopFrameTitleIfNeeded();
    if (shouldInstallManifestSniffer()) {
      installManifestSniffer();
    }
    const iframeMode = isIframe();
    const currentHost = String(globalThis.location.hostname || "").toLowerCase();
    const shouldSkipGoogleDriveTopFrameBootstrap = !iframeMode && (currentHost === "drive.google.com" || currentHost === "docs.google.com");
    if (shouldSkipGoogleDriveTopFrameBootstrap) {
      logBootstrap(
        "Google Drive top frame detected; skipping top-frame VideoHandler bootstrap",
        { host: currentHost }
      );
      await ensureRuntimeActivated("google-drive-top-frame", logBootstrap);
      return;
    }
    const bootstrapMode = resolveBootstrapMode({
      isIframe: isIframe(),
      origin: globalThis.location.origin,
      authOrigin: authCallbackOrigin
    });
    if (bootstrapMode === "skip") {
      logBootstrap("Skipping bootstrap for non-runnable iframe");
      return;
    }
    if (iframeMode && document.visibilityState === "hidden") {
      logBootstrap("Hidden iframe detected; delaying bootstrap until visible");
      await new Promise((resolve) => {
        const onVisibilityChange = () => {
          if (document.visibilityState !== "hidden") {
            document.removeEventListener("visibilitychange", onVisibilityChange);
            resolve();
          }
        };
        document.addEventListener("visibilitychange", onVisibilityChange);
        setTimeout(() => {
          document.removeEventListener("visibilitychange", onVisibilityChange);
          resolve();
        }, 1500);
      });
    }
    logBootstrap("Loading extension");
    if (bootstrapMode === "auth-eager") {
      void ensureRuntimeActivated("auth-page", logBootstrap).catch((err) => {
        console.error("[VOT] Failed to activate runtime", err);
      });
    } else {
      logBootstrap("Lazy bootstrap enabled; waiting for video detection");
    }
    bindObserverListeners({
      videoObserver,
      videosWrappers,
      ensureRuntimeActivated: (reason) => ensureRuntimeActivated(reason, logBootstrap),
      getServicesCached,
      findContainer,
      createVideoHandler: (video, container, site) => new VideoHandler(video, container, site)
    });
    const services2 = getServicesCached();
    const videoObserverPolicy = resolveVideoObserverPolicy({
      hostname: globalThis.location.hostname,
      services: services2
    });
    videoObserver.setPolicy(videoObserverPolicy);
    if (videoObserverPolicy.preferProbeBootstrap) {
      logBootstrap("Lightweight video probe enabled", {
        strategy: videoObserverPolicy.probeStrategy,
        selectors: videoObserverPolicy.probeSelectors.join(", ")
      });
      startLightweightVideoProbe({
        strategy: videoObserverPolicy.probeStrategy,
        selectors: videoObserverPolicy.probeSelectors,
        pollIntervalMs: videoObserverPolicy.probePollIntervalMs,
        pollTimeoutMs: videoObserverPolicy.probePollTimeoutMs,
        onVideoDetected: (reason, video) => {
          logBootstrap("Probe detected candidate video", { reason });
          videoObserver.enable(video);
        },
        onProbeExhausted: (reason) => {
          logBootstrap(
            "Lightweight video probe exhausted; enabling DOM observer fallback",
            {
              reason,
              strategy: videoObserverPolicy.probeStrategy,
              timeoutMs: videoObserverPolicy.probePollTimeoutMs
            }
          );
          videoObserver.setPolicy({
            ...videoObserverPolicy,
            startDomObservationOnEnable: true
          });
          videoObserver.enable();
        }
      });
      return;
    }
    videoObserver.enable();
  }
  const DOM_BOOTSTRAP_ATTR = "data-vot-bootstrap-active";
  if (document.documentElement.hasAttribute(DOM_BOOTSTRAP_ATTR)) {
    logBootstrap("dom bootstrap already initialized, skipping duplicate run");
  } else {
    document.documentElement.setAttribute(DOM_BOOTSTRAP_ATTR, "1");
    if (bootState.status === "booting" || bootState.status === "booted") {
      logBootstrap("bootstrap already initialized, skipping duplicate run", {
        status: bootState.status
      });
    } else {
      const runBootstrap = async () => {
        try {
          await main();
          bootState.status = "booted";
        } catch (e2) {
          bootState.status = "failed";
          bootState.error = e2;
          console.error("[VOT]", e2);
        }
      };
      bootState.status = "booting";
      bootState.promise = runBootstrap();
    }
  }

})();